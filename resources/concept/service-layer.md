---
type: concept
id: service-layer
title: 서비스 계층 (Service Layer)
aliases:
  - Service
  - 서비스 계층
  - 서비스 레이어
  - Service Layer
up:
  - 2024-09-12-Day75
  - 2024-09-29-Day84
  - 2025-07-21-Sync_Async
tags:
  - 설계
  - 계층
  - web
---

# 서비스 계층 (Service Layer)

**업무 로직과 트랜잭션 제어를 담는 층.** 컨트롤러(서블릿)에서 「무엇을 할 것인가」를 떼어 내, 컨트롤러에는 「화면을 어떻게 보일 것인가」만 남긴다.

## 정의

이 회차의 출발점은 서블릿 하나가 두 가지를 겸하고 있다는 관찰이다.

- **DAO 제어** — 어떤 데이터를 어떤 순서로 읽고 쓸 것인가 → [[dao-pattern]]
- **JSP 제어** — 어떤 화면을 어떻게 보일 것인가 → [[jsp]]

「서블릿의 주요 기능은 JSP 제어」라고 보면, 나머지 하나가 **떼어 낼 것**이다.

```
[전]  Servlet ──┬─→ DAO
                └─→ JSP

[후]  Servlet ──┬─→ Service ──→ DAO
                └─→ JSP
```

서비스가 갖는 것이 둘이다.

1. **로직** — 여러 DAO 호출을 순서대로 엮어 하나의 업무를 이룬다
2. **트랜잭션 제어** — 그 여러 번의 쓰기를 **전부 성공하거나 전부 되돌리는** 단위로 묶는다 → [[transaction]]

### 층이 갈리면 층마다 할 일도 갈린다

이름이 Servlet·DAO 에서 Controller·Repository 로 바뀌어도 배치는 같다. 열 달 뒤 파이썬으로 쓴 필기가 같은 세 층에 **비동기를 어디에 두는가**를 얹었다.

| 층 | 역할 | 이 층에서 겹치는 것 |
|---|---|---|
| **Controller** | HTTP 요청·응답 | 프레임워크가 알아서 한다 |
| **Service** | 업무 로직 + **조합** | 여러 Repository 를 **동시에** 부른다 |
| **Repository** | 데이터 접근 | 외부 API·DB 호출 자체 |

**병렬 호출이 서비스에 놓이는 이유가 이 층의 정의와 같다** — 「여러 아래층 호출을 엮어 하나의 업무를 이룬다」는 일이 곧 **무엇과 무엇을 함께 보낼 수 있는지 아는 자리**다. Controller 는 무엇을 부르는지 모르고, Repository 는 자기 것밖에 모른다 → [[async-io]]

같은 서비스 메서드 안에서도 **기다리는 일은 비동기로, 계산하는 일은 그냥 동기로** 둔다 → [[io-bound-vs-cpu-bound]]

## 왜 중요한가

**트랜잭션의 경계가 있어야 할 자리이기 때문이다.** 「게시글을 지우면 첨부파일도 지운다」는 두 번의 DELETE 인데, 하나만 성공하면 데이터가 깨진다. 이 경계를 DAO 에 두면 DAO 하나가 한 트랜잭션이라 여러 DAO 를 묶을 수 없고, 서블릿에 두면 **화면마다 커밋·롤백 코드가 복사된다.** 둘 다 아닌 자리가 필요하고 그것이 서비스다.

**그리고 화면이 없어도 되는 코드가 생긴다.** 서블릿에 로직이 있으면 그 로직을 부르려면 HTTP 요청이 있어야 한다 — 테스트도, 배치 작업도, 다른 화면도 그 코드를 재사용할 수 없다. 서비스로 내려온 로직은 **그냥 자바 메서드**다 → [[coupling]]

**「Servlet 이 바뀌면 전체를 바꿔야 한다」**는 필기의 문제 진술이 이 지점이다. 화면 기술이 바뀌는 것(JSP → 다른 것)과 업무 규칙이 바뀌는 것은 **서로 다른 이유로 일어나는 변경**인데, 한 클래스에 있으면 항상 같이 열어야 한다 → [[cohesion]] · [[solid-principles]]

## 경계와 오해

- **서비스가 「DAO 를 부르는 껍데기」면 층이 하나 늘었을 뿐이다** — `service.list()` 가 `dao.list()` 를 그대로 부르기만 하면 얻은 것이 없다. 이 층이 값을 갖는 것은 **여러 호출을 엮을 때**와 **트랜잭션을 그을 때**다. 단순 조회에서는 얇은 것이 자연스럽고, 그것이 잘못은 아니다
- **서비스는 HTTP 를 몰라야 한다** — `HttpServletRequest` 를 서비스에 넘기면 화면 기술이 다시 딸려 들어와 분리한 이유가 사라진다. 넘길 것은 값이나 도메인 객체다 → [[servlet]]
- **서비스 ≠ 모델 전부** — [[mvc-pattern]] 의 모델은 데이터와 업무 로직을 아우르는 말이고, 서비스는 그 안에서 **로직 쪽**을 맡는 층이다. 데이터 접근은 여전히 DAO 다
- **트랜잭션 제어를 서비스로 옮겨도 자동으로 되지는 않는다** — 이 시점의 코드는 커밋·롤백을 손으로 쓴다. 그 반복을 걷어내는 것이 나중의 일이고, [[dynamic-proxy]] 로 감싸는 방식을 앞 회차에서 이미 봤다
- **층의 이름은 기술마다 다르다** — Servlet·DAO 와 Controller·Repository 는 같은 자리의 다른 이름이다. **책임의 배치가 같은지**로 보는 것이 이름을 외우는 것보다 낫다
- **얇은 서비스는 비동기에서 특히 손해다** — 컨트롤러가 레포지토리를 직접 여럿 부르기 시작하면, **무엇과 무엇을 함께 보낼 수 있는지 아는 자리**가 사라진다. 조합할 것이 있는데 층을 건너뛰면 병렬화할 지점도 함께 없어진다 → [[async-io]]
- **필기에 실습코드가 없다** — 「4. 실습코드」 절이 제목만 있고 비어 있어, **분리의 이유는 남았지만 분리한 모양은 남지 않았다.** 실제 코드는 뒤 회차에서 봐야 한다

## 함께 보는 개념

- [[dao-pattern]] — 서비스가 부르는 아래층
- [[transaction]] — 서비스가 경계를 긋는 대상
- [[mvc-pattern]] — 이 층이 들어가는 배치
- [[front-controller]] — 같은 회차에서 떼어 내는 다른 역할
- [[cohesion]] · [[coupling]] — 떼어 내는 이유를 재는 축
- [[dynamic-proxy]] — 트랜잭션 반복을 걷어내는 장치
- [[object-storage]] — 이 층으로 감싸는 외부 저장소의 예
- [[async-io]] — 이 층이 아래층을 겹쳐 부를 때

## 출처

- [[2024-09-29-Day84]] — 열이레 뒤. Day75 가 비워 둔 「서비스 인터페이스의 모양」이 처음 코드로 나온다 — `StorageService` 가 `upload`·`delete`·`download` 세 메서드만 선언하고, `NcpObjectStorageService` 가 그것을 S3 API 로 구현한다. **업무 로직이 아니라 외부 자원을 감싸는 서비스**라 Day75 의 정의(로직 + 트랜잭션)와는 성격이 다른데, 값은 같은 자리에서 나온다 — 컨트롤러가 클라우드 SDK 를 모르게 된다. 스토리지를 다른 것으로 바꿔도 인터페이스가 그대로인 배치다 → [[object-storage]]
- [[2024-09-12-Day75]] — 「Service」 절이 세 걸음으로 짧게 적혀 있다. 「현황분석」이 **「기존 Servlet의 역할은 DAO제어와 JSP제어를 담당한다 / 만약 Servlet이 바뀌면 전체를 바꿔야한다」**로 문제를 세우고, 「개선방안」이 「Servlet의 주요 기능은 JSP제어 이므로, **로직과 트랜젝션 제어를 따로 분리한다**」로 답한다 — **떼어 낼 것이 둘(로직·트랜잭션)이라고 명시한 것**이 이 노트가 남긴 핵심이다. 나머지는 분리 전후 비교 이미지이고 「실습코드」 절은 비어 있어, 서비스 인터페이스의 모양이나 트랜잭션을 어떻게 긋는지는 이 회차에 없다
- [[2025-07-21-Sync_Async]] — 열 달 뒤 파이썬·FastAPI 로 같은 배치를 다시 적은 필기다. **「레이어별 책임」 표**가 Controller(HTTP 요청/응답) · Service(비즈니스 로직 + 조합) · Repository(데이터 접근)로 Day75 의 셋과 그대로 겹치고, 여기에 **「비동기 사용 이유」 열이 하나 더 붙는다** — Service 는 「여러 Repository 병렬 호출」, Repository 는 「외부 API/DB 호출 최적화」. 서비스 코드 주석의 **「역할: 비즈니스 로직 처리 + 여러 레포지토리 조합 / 핵심: `asyncio.gather` 로 병렬 처리」**가 이 층의 정의와 병렬화가 왜 같은 자리에 있는지를 보인다. **「CPU 집약적 작업은 동기로 처리」**를 같은 클래스 안에 나란히 둔 것도 이 층이 판단하는 자리임을 말한다 → [[io-bound-vs-cpu-bound]]
