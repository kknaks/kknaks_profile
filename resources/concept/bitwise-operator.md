---
type: concept
id: bitwise-operator
title: 비트 연산자 (Bitwise Operator)
aliases:
  - 비트 연산자
  - bitwise operator
  - 비트마스킹
  - bit mask
up:
  - 2024-06-07-Day10
  - 2024-07-18-Day38
tags:
  - java
  - 연산자
  - 비트연산
---

# 비트 연산자 (Bitwise Operator)

값을 비트 단위로 맞대어 계산하는 연산자. `&`(AND) `|`(OR) `^`(XOR) `~`(NOT) 이 있고, 각 자리를 독립적으로 처리한다.

## 정의

같은 자리의 비트끼리 논리 연산을 한다.

| 연산자 | 결과가 1 인 경우 |
|---|---|
| `&` | 양쪽 비트가 모두 1 |
| `\|` | 한쪽이라도 1 |
| `^` | 두 비트가 다를 때 |
| `~` | 모든 비트를 뒤집는다 |

`&`·`|`·`^` 는 논리값에도 쓰이므로 → [[short-circuit-evaluation]] 과 겹친다. 정수에 쓰면 비트 연산, `boolean` 에 쓰면 논리 연산이다.

## 사용 예시

**비트마스킹** — 원하는 자리만 남기거나 켜는 데 쓴다. 픽셀 값에서 색 하나를 제거하는 예다.

```java
int pixel = 0x003f4478;                  // 각 바이트가 '00RRGGBB' 라 가정
System.out.println(pixel & 0x0000ffff);
// pixel = 00000000_00111111_01000100_01111000
//       & 00000000_00000000_11111111_11111111
//         00000000_00000000_01000100_01111000   ← R 자리가 0 이 되었다
```

`&` 는 마스크가 0 인 자리를 **끄고**, 1 인 자리는 원래 값을 **남긴다.** 빨강 자리에 0 을 맞대어 지운 것이다.

반대로 `|` 는 자리를 **켠다.**

```java
int pixel2 = 0x003f4478;
System.out.println(pixel2 | 0x00000057);
// pixel2 = 00000000_00111111_01000100_01111000
//        | 00000000_00000000_00000000_01010111
//          00000000_00111111_01000100_01111111   ← B 자리에 비트가 추가되었다
```

### 자리를 켜는 것이 아니라 조각을 이어 붙이는 데 쓸 때

여섯 주 뒤 회차가 `|` 를 다른 뜻으로 쓴다 — **겹치지 않는 자리들을 합쳐 하나의 값으로 만드는** 것이다.

```java
      user.setNo(in.read() << 24 | in.read() << 16 | in.read() << 8 | in.read());
```

```java
      int len = in.read() << 8 | in.read();
```

`in.read()` 는 0~255 이므로 각 조각이 **하위 8비트에만** 값을 갖고, `<<` 로 서로 다른 자리에 밀어 놓았으므로 **겹치는 비트가 없다.** 겹치지 않으면 `|` 는 「합치기」와 같아진다 — 파일에서 읽은 네 바이트가 `int` 하나로 복원된다 → [[bit-shift]] · [[length-prefix-framing]]

**Day10 의 `|` 는 「이미 있는 값에 자리를 더 켠다」였고 여기서는 「빈 자리에 조각을 놓는다」다.** 연산은 같고 의도가 다르며, 그 차이는 **양쪽이 겹치는 비트를 갖는지**에서 온다.

## 왜 중요한가

**여러 개의 켜짐/꺼짐을 하나의 정수에 담을 수 있다.** 색의 R·G·B 처럼 한 값 안에 여러 정보가 나뉘어 들어 있을 때, 비트 연산이 그 조각을 꺼내고 바꾸는 수단이 된다. 권한 플래그나 옵션 집합도 같은 방식으로 다룬다.

`&` 로 읽고 `|` 로 쓰는 짝이 기본 패턴이다 — **마스크가 1 인 자리에만 관심을 두고 나머지는 건드리지 않는다.**

## 경계와 오해

- **`&` 와 `&&` 는 다른 연산이다** — 정수에 쓰면 비트 연산이고 `boolean` 에 쓰면 논리 연산인데, 후자일 때도 `&&` 와 달리 뒤쪽을 항상 평가한다 → [[short-circuit-evaluation]]
- **`^` 는 비트 연산에서도 "다르면 1"이다** — 논리 연산에서와 같은 규칙이다. 같은 값을 두 번 XOR 하면 원래로 돌아온다.
- **`~` 는 부호까지 뒤집는다** — 모든 비트를 반전하므로 양수가 음수가 된다. [[twos-complement]] 의 1의 보수를 구하는 것과 같다.
- **비트 연산에도 [[type-promotion]] 이 적용된다** — `byte` 끼리 연산해도 결과는 `int` 다.
- **조각을 `|` 로 합치는 것은 겹치지 않을 때만 「합치기」다** — 두 조각이 같은 자리에 비트를 갖고 있으면 `|` 는 그것을 **한 개로 만들어 버린다.** 즉 되돌릴 수 없다. Day38 의 코드가 맞는 것은 `in.read()` 가 0~255 라 각 조각이 하위 8비트에만 있고 `<<` 가 서로 다른 자리로 밀어 두기 때문이다. **읽는 값의 범위가 보장되지 않으면 `(x & 0xFF) << 8` 처럼 마스크로 먼저 자리를 좁혀야 한다** — `byte` 배열에서 직접 조립하면 부호 확장 때문에 정확히 그것이 필요해진다 → [[bit-shift]] · [[type-promotion]]
- **`|` 는 `+` 보다 우선순위가 낮고 `<<` 보다도 낮다** — `in.read() << 8 | in.read()` 가 괄호 없이 맞는 이유이고, `a | b + c` 는 `a | (b + c)` 가 된다. Day38 은 같은 계산을 한 줄은 괄호 없이(`in.read() << 8 | in.read()`) 다른 줄은 괄호로(`(in.read() << 8) | in.read()`) 적어 **한 파일 안에서 표기가 갈렸다** — 둘 다 같은 뜻이므로 버그는 아니지만, 괄호 없는 쪽을 보고 우선순위를 확신할 수 없으면 매번 다시 확인해야 한다 → [[operator]]

## 함께 보는 개념

- [[bit-shift]] — 비트를 옮기는 쪽
- [[short-circuit-evaluation]] — 같은 기호를 논리값에 쓸 때
- [[twos-complement]] — `~` 가 하는 일과 같은 계산
- [[operator]] — 비트 연산자가 속한 갈래
- [[length-prefix-framing]] — 조각을 합쳐 길이를 복원하는 자리
- [[binary-io]] — 바이트 조각과 값 사이를 왕복하는 작업

## 출처

- [[2024-06-07-Day10]] — `&` 로 픽셀에서 빨강 자리를 지우고 `|` 로 자리를 켜는 비트마스킹을 배웠다
- [[2024-07-18-Day38]] — 파일에서 읽은 바이트 조각을 `in.read() << 24 | in.read() << 16 | in.read() << 8 | in.read()` 로 합쳐 `int` 를 복원한다. **`|` 를 「자리를 켠다」가 아니라 「겹치지 않는 조각을 이어 붙인다」로 쓴 첫 코드**이고, 그것이 성립하는 조건(`read()` 가 0~255 를 준다)은 필기에 적혀 있지 않다. 같은 계산을 괄호 있는 표기와 없는 표기로 각각 한 번씩 적었다
