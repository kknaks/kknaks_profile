# CLAUDE.md

이 레포는 **리뉴얼 중**이다. 리뉴얼 이전의 모든 문서·코드·에이전트 설정은 `_archive/` 에
그대로 있고 **읽기 전용**이다.

## 시작

```text
CLAUDE.md      ← 여기. 왜 다시 세우는지
→ _RESUME.md   지금 어디까지 왔고 다음에 뭘 하나. 작업하면서 갱신한다
→ 필요한 문서  아래 지도에서 고른다
```

**작업을 시작하기 전에 `_RESUME.md` 를 읽는다.** 무엇이 이미 정해졌고 무엇이 열려 있는지가
거기 있다. 정해진 것을 다시 논의하지 않고, 열린 것을 추측으로 메우지 않는다.

## 문서 지도

| 문서 | 무엇 | 언제 읽나 |
|---|---|---|
| `_RESUME.md` | 진행 상태 · 열린 것 · 하지 않기로 한 것 | **항상** |
| `architecture.md` | 루트 계약 — `para/` · `app/` · `orchestration/` | 구조를 건드릴 때 |
| `para/para.md` | 네 버킷의 경계 | 문서를 놓을 자리를 정할 때 |
| `para/areas/area.md` | `areas/` 하위와 `concept/` 아홉 영역 | 개념을 쓸 때 |
| `erd.md` | **스키마 정본** — mermaid + DDL | DB 를 건드릴 때 |
| `database.md` | 표면별 컬럼 추출과 그 근거 | 왜 그 컬럼인지 물을 때 |
| `case_flow.md` | **동작 정본** — 케이스 7 | 파이프라인을 건드릴 때 |
| `orchestration/runbook.md` | 코디네이터 런북 | 워커를 발주할 때 |

## 왜 다시 세우나

다섯 문제로 시작했고, 넷은 답이 나왔다.

### 1. 사람 작업과 자동화 작업이 계속 충돌한다 → `case_flow.md`

같은 문서를 양쪽이 건드리면서 서로의 결과를 덮었다. **어떤 문서를 누가 쓰는지가 문서
단위로 정의되어 있지 않은 것**이 원인이었다.

> **자동화가 만든 것은 사람이 승인하고, 사람이 만든 것은 기계가 검증한다.**

### 2. 문서와 DB 사이의 SSOT 가 정의되지 않았다 → `erd.md` · `database.md`

같은 사실이 문서에도 DB 에도 있어 한쪽만 갱신되고 다른 쪽이 조용히 낡았다.

> **표면에 보이는 것은 DB, 문서는 md.** 그리고 **정보는 DB, 상세는 md** —
> DB 는 `detail_path` 로 가리키기만 하고 본문을 복사하지 않는다.

무너진 지점이 어디였는지도 드러났다 — `persona/daily/`(잔디)가 **파생물인데 md 파일로
앉아 있었다.** 그래서 사람도 자동화도 고칠 수 있었고 원천이 둘이 됐다.

### 3. 자동화가 승인 없이 어디까지 해도 되는지 기준이 없다 → `case_flow.md`

케이스마다 갈렸다. 게이트가 필요한 자리(자료 캡처·`problem`)와 필요 없는 자리
(잔디·알고리즘)가 나뉘었다.

### 4. 코드가 중구난방이다 → `app/back/` 계층 규약

router → service → repository 로 내려가고, 계층 사이는 dto(내부)·schemas(front 계약)가
나른다. ORM 은 repository 를 넘지 않고, 아래층은 HTTP 를 모르고 도메인 예외만 던지며,
트랜잭션은 요청 하나가 경계다. **규약 문서는 아직 없다** — 코드가 모범이고, 문서화는
`.agent/` 세울 때 한다.

### 5. 회사 작업이 기록에 안 남고, 잔디는 일반적인 말만 쌓인다 → `case_flow.md` 케이스 3·6

잔디가 커밋 메시지에서 자동 생성되어 「무엇을 완성했다」 수준에 머물렀다.
**원천을 커밋에서 사람 회고로 바꿨다.**

```text
SUMMARY.md §2  적용한 기술·개념  → areas/concept/
SUMMARY.md §3  막혔던 것        → 게이트 → problem (이력서의 알맹이)
커밋 메시지                      → commit.summary → 잔디
```

## 정할 것 — 상태

| # | 항목 | 상태 |
|---|---|---|
| 1 | 작업 주체 경계 | 정해짐 |
| 2 | 문서 ↔ DB SSOT | 정해짐 |
| 3 | 자동화 승인 게이트 | 정해짐 |
| 4 | 코드 규약 | 정해짐(구현) — 문서화만 남음 |
| 5 | 기록의 층 | 정해짐 |
| 6 | 디렉토리 · 에이전트 규칙 | 디렉토리 ○ / **에이전트 규칙 ✗** |

`.agent/` 가 아직 없다. 케이스 2 의 pre-commit, 케이스 4 의 스킬, 문서 템플릿이 갈 자리다.
`agents.md` 의 첫 갈래(「어느 레포를 건드리나」)도 기록만 해 두고 라우터를 안 세웠다.

## 작업 방식

**새 구조에 필요한 내용은 `_archive/` 에서 끌어올린다.** 처음부터 다시 쓰지 않는다.

- 끌어올릴 때 그대로 복사하지 않는다. 위 기준에 맞는지 먼저 확인하고, 맞지 않으면 고쳐서 올린다
- `_archive/` 는 읽기 전용으로 취급한다. 거기서 고치지 않는다
- 아직 안 올린 것은 없는 것으로 둔다. 「나중에 정리」로 남기지 않는다
- **열린 것을 추측으로 메우지 않는다.** `_RESUME.md` §3 과 각 문서의 「아직 안 정한 것」이
  그 목록이다
