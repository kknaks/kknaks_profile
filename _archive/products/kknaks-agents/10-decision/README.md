# Decision Index

규칙: `rules/product-doc-pipeline.md`

> baseline을 제품에 어떻게 적용할지 판단한 결정 목록과 아직 풀어야 할 질문을 관리한다.

첫 decision인 KAG-DEC-001이 2026-08-08 사용자 확정으로 `accepted`가 됐다. 확정 범위는 디렉터리 구조와 의존 방향까지다.

두 번째 decision인 KAG-DEC-002(최소 headless turn runtime 동작 구조)도 2026-08-08 사용자 확정으로 `accepted`가 됐다. 확정 범위는 한 turn의 진행 phase 9 + 종료 state 4 전이, side effect 순서와 불변식, 반복 진입·종료 조건까지이며, 각 문서의 Open Questions는 여전히 미결이다.

세 번째 decision인 KAG-DEC-003(core package 계약 경계)은 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. 공개 계약을 package 하나씩 내려가기로 해서 이번 대상은 `core/` 하나이며, `tools`·`providers`·`sessions`·`context`·`skills`·`process`·`runtime`의 내부 구조는 다루지 않는다. 확정 전에는 spec·work·코드로 내려가지 않는다.

네 번째 decision인 KAG-DEC-004(process package 실행 격리 경계)도 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. 의존 그래프의 아래에서 위로 올라가는 순서라 `core`(L0) 다음이 `process`(L1)이며, 이번 대상은 `process/` 하나다. KAG-DEC-004는 KAG-DEC-003을 확정 사실이 아니라 `proposed` 입력으로만 참조하고, `core` 파일 배치와의 연결은 범주 수준으로만 둔다(KAG-DEC-004 OQ-5).

다섯 번째 decision인 KAG-DEC-005(providers package 변환 경계)도 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. `providers`는 `core`와 `process` 둘 다 참조하는 유일한 package라 아래 둘이 정리된 직후가 순서상 가장 싸며, 이번 대상은 `providers/` 하나다. KAG-DEC-005도 KAG-DEC-003·KAG-DEC-004를 `proposed` 입력으로만 참조하고 두 문서와의 연결을 파일명이 아니라 범주 수준으로만 둔다.

여섯 번째 decision인 KAG-DEC-006(tools package 등록·허용·검증·실행 경계)도 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. KAG-DEC-005 PI7이 “복원한 tool call은 실행 요청이지 실행이 아니다”라며 그 요청의 수신자를 넘겼기 때문에 `providers` 다음이 같은 L2의 `tools`이며, 이번 대상은 `tools/` 하나다. KAG-DEC-006도 KAG-DEC-003·KAG-DEC-005를 `proposed` 입력으로만 참조하고 연결을 범주 수준으로만 둔다.

일곱 번째 decision인 KAG-DEC-007(sessions package event 저장·조회 경계)도 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. KAG-DEC-006 TI6·TR7이 “`tools`는 아무것도 기록하지 않고 거부도 값으로 반환한다”며 기록의 수신자를 넘겼기 때문에 `tools` 다음이 같은 L2의 `sessions`이며, 이번 대상은 `sessions/` 하나다. 이 문서는 **KAG-DEC-003 OQ-2(“session event 저장 경계를 `sessions` 안에서 어떤 형태로 둘지”)에 대한 답을 함께 제안**한다 — store 경계는 `sessions` 안 `store.py`에 계약 하나다. KAG-DEC-007도 KAG-DEC-003·KAG-DEC-006을 `proposed` 입력으로만 참조하고 연결을 범주 수준으로만 둔다.

여덟 번째 decision인 KAG-DEC-008(skills package 등록·선택·prompt 투영 경계)도 2026-08-09 `proposed`로 올라가 **사용자 리뷰 대기** 중이다. KAG-DEC-002 §6이 skill 투영을 “`context`의 입력”으로 규정했으므로 입력을 만드는 `skills`가 `context`보다 먼저이며, 이번 대상은 `skills/` 하나다. 이 문서는 **skills를 최소 tool loop의 필수 요소로 바꾸지 않는다** — REF-0007과 KAG-DEC-002 §6의 “추후에 넣으면 좋을 것 = 조립 가능한 독립 확장 모듈” 분류를 유지하고, “`skills/`를 지워도 첫 vertical slice가 그대로 돈다”를 검증 가능한 성공 조건으로 적었다. KAG-DEC-008도 KAG-DEC-003·KAG-DEC-006을 `proposed` 입력으로만 참조하고 연결을 범주 수준으로만 둔다. 남은 raw 작성 순서는 `context` → `runtime`이다.

## 결정 로그

decision 문서를 만들거나 상태가 바뀌면 이 표를 갱신한다.

| ID | Title | Status | Baseline | Result | Spec |
|---|---|---|---|---|---|
| KAG-DEC-001 | [Runtime 디렉터리 구조와 의존 경계](decision-001-runtime-directory-boundaries.md) | accepted | KAG-BL-001 | 책임별 package 채택 (flat / ports-adapters 기각). package 이름 `kknaks_agents`, 4단 의존 계층 확정 | - |
| KAG-DEC-002 | [최소 headless turn runtime 동작 구조](decision-002-turn-runtime-flow.md) | accepted | KAG-BL-001 | 명시적 phase/state transition을 가진 deterministic turn loop 채택 (불투명 `run()` / middleware-hook pipeline 기각, hook은 후속 확장 후보로 보존). 진행 phase 9 + 종료 state 4, side effect 순서 불변식 3, tool call 직렬 실행, 종료 원인 10종의 terminal/recoverable 판정. `query/` 디렉터리 없이 `runtime`이 흡수. 미결 9건 | - |
| KAG-DEC-003 | [core package 계약 경계 — 파일·타입 범주와 공개 표면](decision-003-core-contract-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | 단일 `contracts.py` / 관심사별 평면 module / 도메인별 중첩 package+`ports` 3안 비교 후 **평면 module 분리 권고**(나머지 둘 비권고, 중첩은 승격 경로로 보존). core 소유 계약 범주 10종, 파일 후보 10개 + `__init__.py`, 파일별 producer/consumer, core 내부 6단 총순서(`events → responses` 단방향), protocol은 “소비자가 구현 package를 import할 수 없을 때만” 판정(`protocols.py` 후보 2 = 필수 1 model 호출 경계 + 권고 1 tool handler. session store는 `runtime → sessions` 허용이라 core 제외), `core/__init__.py` 선별 재수출 권고, 호환성 원칙 7개(선택적 값 추가와 새 판별값 추가를 구분), DEC-002 phase 매핑. 미결 8건 | - |
| KAG-DEC-004 | [process package 실행 격리 경계 — 파일·보안 불변식·실행 계약](decision-004-process-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | 단일 runner module / 관심사별 평면 module / backend·플랫폼별 중첩 / `providers`로 접기 4안 비교 후 **평면 module 분리 권고**(단일 runner와 접기는 비권고, 중첩은 기각). process 소유 책임 8종(P1~P8)과 소유하지 않는 것 9종, **최소 보안 경계 13항목 판정**(argv 목록 실행 · shell 미개방 · 필수 cwd · stdin 비상속 · 환경 allowlist · 호출 단위 시간 상한 · 외부 취소 관찰 · stdout/stderr 분리 고정 · 종료 escalation · exit/신호 원시 보고 · 자식 그룹 회수 · 스트림별 byte 상한 · decode 경계)과 미지정 시 fail-closed 해석, 파일 후보 7개 + `__init__.py`(각 파일이 보안 항목을 명시적으로 소유. 개수 1:1은 아니다), process 내부 5단 총순서로 OS 의존을 한 파일에 모아 대체 지점을 안정된 import 경계로 둠, 실행 lifecycle 5국면 = 순차 4 + **동시 감독 구간 1**(stdin 전달 · 두 스트림 동시 drain · 시간 상한 · 취소 관찰 · 자식 종료 관찰이 spawn 직후부터 동시에 진행되고 먼저 발생한 종료 사건 하나가 구간을 끝낸다 — 순차로 쪼개면 멈춘 자식을 끊지 못하고 파이프 교착이 생긴다. 정리 없이 반환 없음 · 부분 출력 보존 + 불완전 표시), turn / provider 호출 / subprocess 실행 3층 책임 분리(process는 turn 종료 state도 provider 오류도 만들지 않는다)와 fail-closed 원칙 6개, KAG-DEC-002와 겹치지 않음의 항목별 확인, 최소 재수출 권고와 테스트 seam 4종, 관측·민감정보 원칙 7개. KAG-DEC-001 OQ-4(process 유지/접기)는 닫지 않음. 미결 10건 | - |
| KAG-DEC-005 | [providers package 변환 경계 — adapter 파일 배치·호출 lifecycle·capability 계약](decision-005-provider-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | provider별 단일 module / 공용 module + provider별 하위 package / transport별 중첩 / 공용 base class 상속 4안 비교 후 **공용 module + provider별 하위 package 권고**(단일 module은 비권고, transport 중첩과 상속 계층은 기각 — 상속은 한 adapter의 사정이 다른 adapter 코드에 반영되어 KAG-DEC-002가 middleware를 기각한 것과 같은 문제가 된다). providers 소유 책임 9종(A1~A9)과 소유하지 않는 것 12종. **공용/전용 판정 16항목**과 그 기준(“두 번째 adapter가 생겨도 같은 규칙이어야 하는 것만 공용”) — 공용은 실패 분류 매핑·복원 실패 표현·공통 protocol 투영·진단 축약 넷뿐이고 실행 파일명·flag·모델명·wire 형식·환경 allowlist 값·한도 값·capability 선언 값은 전부 adapter 폴더 안. 파일 후보 트리 = 공용 2(`failures`·`text_protocol`) + adapter 5(`capability`·`invocation`·`encode`·`decode`·`adapter`) + `__init__` 2. 공용 2단·adapter 5단 총순서와 **방향 규칙 3개**(공용 → adapter 금지 · adapter 상호 참조 금지 · `process`는 adapter 안에서만 — 앞의 둘은 package 밖 grep으로 잡히지 않아 방향 규칙이 유일한 방어). 호출 lifecycle **6국면**(요청 수용·capability 대조 → 입력 조립 → transport 1회 → **결과의 완전성 먼저 확인** → wire 복원 → 응답 1개 또는 실패 1개)과 불변식 8개(PI1~PI8: 결과는 정확히 하나 · 불완전 출력은 복원하지 않음 · 부분 복원 금지 · 호출당 transport 1회 · capability를 낮춰 실행하지 않음 · 원문은 불투명 · **복원한 tool call은 실행 요청이지 실행이 아님** · adapter는 기록하지 않음). 실패 4계열(호출 이전·transport·복원·계약)과 상황별 소유권 17건 — **adapter는 “이 호출이 실패했다”까지만 말하고 turn 종료 state를 만들지 않으며**, 중복 tool call 식별자·tool call과 텍스트 혼재는 감추지 않고 그대로 복원해 `runtime` 판정에 넘긴다. fail-closed 원칙 6개(PF1~PF6). capability는 값이고 provider를 식별하지 않으며 정책을 담지 않고 turn에 고정된다(CP1~CP3), native tool call 미지원 시 fail-closed가 기본이고 공통 protocol 투영은 세 조건(선언에 드러남·투영 실패는 판정 불가·동등성 주장 금지) 아래에서만. `providers/__init__.py` 무-재수출 권고, 새 provider 구현자의 확장 표면은 **model 호출 Protocol 1개 + capability 선언 1개뿐**(상속·registry·plugin 진입점 없음), contract test seam 5종(그중 provider-agnostic 계약 suite가 상속 없이 “모든 adapter가 같은 순서”를 얻는 자리). 관측·민감정보 원칙 8개. KAG-BL-001 OQ-4(실행 옵션·출력 protocol)·OQ-5(capability 표준)는 닫지 않음. 미결 11건 | - |
| KAG-DEC-006 | [tools package 경계 — 등록·허용 분리, 검증·정책 순서, handler 실행과 결과 정규화](decision-006-tools-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | 단일 `registry.py` / 관심사별 평면 module / tool 분류별 중첩 / 기반 클래스 상속 + 자기 등록 4안을 초기 복잡도·판정 축의 명시성·우회 경로 부재·등록 출처 통제·확장 비용으로 비교해 **관심사별 평면 module 권고**(단일 module은 비권고, 분류별 중첩과 상속·자기 등록은 기각 — 분류는 정책의 입력 값이지 배치 축이 아니고 이 package에는 tool 구현이 살지 않으며, import 부작용·entry-point 자동 등록은 관찰 가능성 면에서 dynamic discovery와 같아진다). tools 소유 책임 9종(T1~T9: 등록 수용 · 등록 시점 가드 · registry revision · 허용 subset 계산 · 공개 표면 투영 · schema 검증 · 정책 판정 · handler 실행과 실행 context 주입 · 결과 정규화)과 소유하지 않는 것 13종(model 호출 · loop와 순서 · 기록과 되먹임 · 요청 조립 · dynamic discovery와 MCP·plugin · 역할/권한 모델 · 승인 lifecycle · 취소 판정 · transport · 로깅 destination · 조립). **등록 registry / turn 허용 subset / model 공개 표면 세 값의 분리**와 규칙 8개(RG1~RG8: 출처는 언제나 호스트 명시 등록 · 등록과 허용은 다른 값 다른 시점 · snapshot은 이름·버전·revision·공개 정의만 담고 handler와 secret은 담지 않음 · **노출과 실행은 같은 계산** · 접근 선언 무기본값이며 미선언은 등록에서 거부 · 조회 실패를 빈 목록으로 접지 않음 · handler는 값이 아니라 프로세스 지역 자원이고 프로세스 경계는 이름·버전·revision만 넘음 · 허용 subset은 turn 내내 불변이라 되먹임이 표면을 넓히지 않음). 허용 subset을 좁히는 세 입력(등록 · 호스트 turn 지정 · 접근 축 판정)과 **승인 축은 노출을 좁히지 않는다**는 판단. 파일 후보 트리 = module 7개(`declaration`·`results`·`registry`·`validation`·`policy`·`selection`·`executor`) + `__init__.py`, 파일별 단일 역할·대표 타입/행동 범주·producer/consumer. 내부 4단 총순서와 **방향 규칙 4개** — `executor`를 아무도 참조하지 않음(handler 호출 경로가 하나로 유지되고 우회 경로 부재가 파일 단위 질문이 된다) · `policy` ↛ `selection` · `registry`는 실행을 모름 · 형제 package 무참조. tool call 하나의 **국면 8개**(존재 확인 → 동일성 확인 → schema 검증 → 접근 축 → 승인 축 → 실행 context 주입과 handler 호출 → 결과 정규화 → 결과 1개 확정)로 **부작용은 6국면에서만** 발생, 불변식 8개(TI1~TI8: 결과 정확히 하나이고 거부도 결과 · **1~5국면 없이 handler에 닿는 경로 없음** · 판정 기준은 현재 registry가 아니라 snapshot · model이 주장한 사용자·권한을 신뢰하지 않음 · 여섯 경우 fail-closed · tools는 기록하지도 되먹이지도 않음 · 스스로 model이나 다음 tool을 부르지 않음 · 이 call 하나만 끝내고 turn 종료를 판정하지 않음). KAG-DEC-002 side effect 12단계 중 소유는 6·7단계뿐임을 항목별로 확인. 거부 5계열(존재·동일성·입력·**접근**·**승인** — 접근과 승인을 나누지 않으면 감사 기록이 “거부됨”만 남는다) + handler 실패, 상황별 소유권 16건, fail-closed 원칙 6개(TF1~TF6). 호스트가 등록할 때 채우는 다섯 가지(공개 정의·handler·접근 선언·승인 정책·부작용 분류)뿐이고 **상속할 기반 클래스도 plugin 진입점도 없음**, handler에 registry·다른 tool·session을 넘기지 않음(넘기면 TI2·TI7이 동시에 뚫린다), 접근 축에서 라이브러리는 판정 지점과 기본 거부만 갖고 역할·권한 모델은 갖지 않음(내장 최소 규칙 권고 + 주입 대안은 OQ-5), 승인 축은 “이 실행에 대한 승인이 실렸는가”만 봄, transport와 MCP는 handler 안쪽이며 등록은 여전히 명시적. `tools/__init__.py` **선별 재수출** 권고(등록 경로만 노출하고 판정·계산·실행 진입은 module 경로 — 판정을 재수출하면 호스트가 자기 화면용으로 따로 불러 RG4가 깨진다), contract test seam 5종(등록 가드 · 허용 계산 · 순수 판정 · handler 대체 · **우회 경로 부재는 동작이 아니라 구조를 보는 검사**). 관측·민감정보 원칙 8개(TR1~TR8). KAG-BL-001 OQ-2·OQ-3과 KAG-DEC-002 OQ-3·OQ-4·OQ-6은 닫지 않음. 미결 13건 | - |
| KAG-DEC-007 | [sessions package 경계 — event 저장·조회 계약, 순서·원자성 불변식, memory/durable 확장 seam](decision-007-sessions-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | 단일 `store.py` / 관심사별 평면 module / store 백엔드별 하위 package / event 종류별 분리 4안을 초기 복잡도·계약과 구현의 분리·책임 누수 저항·durable 확장 비용·event 종류 무지의 유지로 비교해 **관심사별 평면 module + 계약/구현 파일 분리 권고**(단일 module과 백엔드별 중첩은 비권고, 종류별 분리는 기각 — `providers`와 달리 store 백엔드가 서로 다른 것은 저장 매체 접근 하나뿐이라 봉인 폴더의 이득이 판정 비용보다 작고, event 종류별 분기는 새 종류마다 이 package를 열어 KAG-DEC-001이 제외한 판단을 들여온다). **KAG-DEC-003 OQ-2에 대한 답을 함께 제안** — store 경계는 `sessions` 안 `store.py`에 계약 하나이고 append와 조회를 쪼개지 않는다(read-your-write가 두 계약에 걸쳐 있어 어느 쪽도 단독 보장이 안 된다). sessions 소유 책임 9종(S1~S9: append 수용 · 순서 부여와 보존 · 원자성 · 중복 판정 · 충돌 감지 · 순서 있는 조회 · store 계약 소유 · memory 구현 · 저장 표현 경계)과 소유하지 않는 것 13종(무엇을 언제 남길지의 결정 · 투영과 compaction · folding된 상태 · 검색과 analytics · 짝/종료 판정 · event 종류 목록 · 알림과 구독 · 접근 제어 · 민감 판정과 마스킹 · queue/lease/worker · DB와 migration · 암호화와 retention · 조립). 식별 3층과 **“식별자는 호출자가 주고 위치는 sessions가 부여한다”**는 판정(발급을 store가 하면 store마다 발급 방식이 달라져 append 이전에 식별자를 참조할 수 없다). 순서 규칙 5개(O1~O5: 전순서는 session 안에서만 · **기준은 도착 순서이지 벽시계 시각이 아니다**(시각 기준이면 같은 session을 두 번 읽을 때 순서가 달라진다) · 위치는 단조 증가하되 조밀할 필요 없음(대가: 빠진 것을 번호만으로 판정 못 함) · 부여된 위치 불변 · 현재 끝을 가리키는 값 하나). append 계약 규칙 8개(SG1~SG8: append-only · **compaction과 요약은 원본을 대체하지 않고 요약도 새 event다** · 식별자는 호출자 · 순서는 도착 순서 · 원자성 · **종류에 따른 의미·정책 분기 금지**(표현 복원을 위한 구조적 판별은 예외이고 `serialization` 안에서만) · **알리지 않는다**(알림이 append에 얽히면 “기록 먼저 알림 나중”이 깨진다) · 실패를 성공으로 접지 않음). 동시 writer는 **감지 값 하나까지만** — 지금 writer는 turn을 소유한 runtime 하나뿐이라 조정(lock·lease·큐)을 만들지 않고, “당신이 본 끝이 더 이상 끝이 아니다” 한마디만 하며 그 다음은 호출자가 정한다(값을 지금 만드는 이유는 나중에 만들면 store 계약이 열리고 그때는 구현이 둘이기 때문). 파일 후보 트리 = module 7개(`records`·`outcomes`·`ordering`·`serialization`·`store`·`memory`·`reading`) + `__init__.py`, **`replay`가 아니라 `reading`인 이유**(재생은 접어서 상태를 만든다로 읽히는데 folding은 이 package 것이 아니다)와 `records`가 `events`가 아닌 이유(payload는 core 것이고 여기 사는 것은 배치 좌표가 붙은 저장 단위). 내부 4단 총순서(v0 값 → v1 순수 규칙 → v2 계약 → v3 구현과 되읽기)와 **방향 규칙 4개** — **`store`는 어떤 구현도 참조하지 않음**(계약이 구현을 알면 durable 붙일 때 계약 파일이 열린다. import 목록 비교만으로 검사 가능) · `reading`은 특정 store를 모름(모르면 memory와 durable이 같은 순서 보장을 얻는다) · **`memory`는 `serialization`을 참조하지 않음**(의도이자 대가 — 직렬화 규칙이 첫 slice 실행 경로에서 한 번도 안 돈다) · 형제 package 무참조(특히 “읽으면서 축약해 주면 편하니까” `context`를 부르는 것이 가장 흔한 누수 경로). append 국면 6개(대상 확인 → **중복 판정** → 위치 기대 대조 → 표현 확정 → 원자적 반영 → 결과 1개)로 **중복 판정이 위치 대조보다 먼저**인 이유(재시도는 위치 기대가 이미 낡아 있어, 순서를 뒤집으면 성공한 append의 재시도가 경합으로 오해된다), 조회 국면 5개(**범위는 위치와 개수만 받고 종류·내용 조건은 받지 않는다**), 불변식 10개(SI1~SI10: 결과 정확히 하나 · **read-your-write** — KAG-DEC-002 I1의 “기록 먼저 사용 나중”은 기록이 다음 읽기에 보일 때만 의미가 있다 · 부분 반영 없음 · 중복은 두 번째 event를 만들지도 조용히 성공으로 접지도 않음 · 조회는 순서 그대로이고 필터·병합·요약·중복 제거 없음 · 이미 읽은 구간 불변 · **모르는 판별값·version에서 멈춤**(건너뛰면 “읽어서 없는 것”과 “원래 없는 것”이 구분되지 않아 lossless가 이름만 남는다) · **의미·정책 분기 없음**(표현 복원을 위한 구조적 판별은 예외)·짝 판정·알림 없음 · 원본 삭제 경로 없음 · **SI10 전달받은 canonical event를 바꾸지 않음** — 마스킹·축약·정규화 금지이며, 저장된 것과 runtime이 판정에 쓰는 것이 갈라지면 KAG-DEC-002 I1이 성립하지 않는다). KAG-DEC-002 12단계와의 대조로 append 다섯 지점과 읽기 한 지점만 관여하고 **무엇을 남길지는 전부 runtime**임을 항목별 확인, “부분 진행은 사라지지 않는다”가 SI2·SI3 위에 서 있음을 명시. 거부·실패 6계열(대상·위치·**중복**·표현·저장·조회) — **중복을 거부로도 성공으로도 두지 않고 “이미 반영됐고 위치는 이것”이라는 구분된 결과**로 두며, **멱등 키 없는 append는 멱등이 아니다**(두 번 부르면 두 번 붙는다)를 계약에 명시. 상황별 소유권 15건(없는 session을 만들어 주지 않음 · 내용 비교로 중복을 추측하지 않음 · 옮길 수 있는 부분만 저장하지 않음 · 실패를 버퍼링하지 않음 · **큰 tool 결과를 자르지 않음**(자를 것이면 append 이전에 잘려 있어야 한다) · turn 실패·취소가 이미 붙은 것을 되돌리지 않고 종료 사실은 새 event) 와 fail-closed 원칙 6개(SF1~SF6). memory store가 만족해야 하는 것 6항목과 **“계약이 아닌 것”**(프로세스와 함께 사라지는 것은 구현의 성질이지 계약의 성질이 아니다), memory에서 저절로 되는 것(전순서·원자성·read-your-write)을 계약에 적어야 하는 이유와 저절로 되지 않는 것(중복 판정) 구분, durable extension seam에서 **열리는 것 4·열리지 않는 것 4**(계약 파일은 안 열린다)와 상속할 기반 클래스 없음, 직렬화·버전 원칙 6개(Z1~Z6: **저장 표현은 계약이다** — 저장된 것은 코드보다 오래 산다 · version 값 부착 · 모르는 version에서 멈춤 · 모르는 판별값에서 멈춤 · **왕복 무손실** · provider 원문은 미결이고 담긴다면 불투명 값)와 **표현 복원(허용)/의미·정책 분기(금지) 경계 표** — serializer는 판별값을 읽어 어떤 typed core event를 만들지 정하고 만들 수 없는 것에서 멈출 수 있으나 종류별 저장 경로·조회 결과·필터·상태 판정·보존 정책은 금지이고, 아는 판별값의 원천은 `core` event 계약이며 그것을 얻는 방법은 `core` codec이 확정되지 않아 미결(OQ-15). **되읽기와 재생의 구분** — 제공하는 것은 같은 순서로 다시 내주기뿐이고 folding·context 재구성·turn 재실행은 아니며, 되읽기는 재실행이 아니다(tool 결과 event를 되읽는다고 tool이 다시 돌지 않는다). 소비자 folding이 멱등하려면 **안정된 순서와 중복 없는 원본** 둘이 필요하고 sessions가 주는 것은 그 둘까지다(화면 view·model context·감사 리포트가 같은 접기를 쓸 이유가 없어 folding을 대신하지 않는다). `sessions/__init__.py` **선별 재수출** 권고(store를 만드는 경로는 노출하고 순서 규칙·직렬화는 module 경로 — `providers` 무-재수출과 다르게 가는 이유 명시), 새 store 구현자의 요구 7개(받은 canonical event를 바꾸지 않고 그대로 돌려주기 포함), contract test seam 6종(**store-agnostic 계약 suite**가 상속 없이 “모든 store가 같은 계약”을 얻는 자리 · 순수 규칙 직접 호출 · 실패 주입 store · 되읽기 안정성 · 직렬화 왕복 강제 store · **삭제·수정·분기 경로 부재는 동작이 아니라 구조를 보는 검사이고, 그 기준은 “종류 이름이 어디에도 없는지”가 아니라 “종류를 보는 코드가 `serialization` 밖에 없는지”다**(존재 금지로 두면 Z4의 unknown fail-closed와 Z5의 왕복 무손실을 구현할 방법이 없다)). 보안·관측 원칙 10개(SR1~SR8, SR3는 SR3′·SR3″로 갈라진다) — 특히 **마스킹은 event를 구성하기 전에 끝난다**(여기서 만들면 “손실 없는 원본”과 “마스킹된 것”이 같은 자리에서 싸운다)와 **SR3′ — runtime은 기록한 canonical 값과 이후 판정·되먹임에 쓰는 값을 다르게 둘 수 없다**(이미 구성된 model 응답·tool 결과를 저장용으로 축약한 뒤 더 풍부한 원본으로 판정하는 경로 금지)와 SR3″(event에 담지 않기로 한 것과 담겼다가 저장 시 잘린 것은 다르다 — 전자는 정책, 후자는 I1 위반)와 **SF6과 법적 삭제 요구의 충돌을 지금 적어 둠**. KAG-BL-001 OQ-2·OQ-8·OQ-11과 KAG-DEC-002 OQ-7은 닫지 않음. 미결 15건 | - |
| KAG-DEC-008 | [skills package 경계 — 명시적 등록·선택 입력, prompt projection, turn 고정과 신뢰 경계](decision-008-skills-boundaries.md) | **proposed** (리뷰 대기) | KAG-BL-001 | 단일 `registry.py` / 관심사별 평면 module / 자산 형식·도메인별 중첩 / loader 기반 자동 탐색 4안을 초기 복잡도·등록 출처 통제·선택 주체의 명시성·투영 경계 유지·확장 비용으로 비교해 **관심사별 평면 module 권고**(단일 module은 비권고, 형식·도메인별 중첩과 loader 자동 탐색은 기각 — 이 package에는 skill 내용이 살지 않아 분류할 대상이 없고, 자동 탐색은 KAG-DEC-002 §6의 완전 제외이며 skill에서는 특히 나쁘다: tool 자동 탐색이 “실행 가능한 것이 늘어난다”라면 skill 자동 탐색은 “**model에게 말할 수 있는 사람이 늘어난다**”이다). **이 문서는 skills를 최소 runtime의 필수 요소로 바꾸지 않는다** — 확장 모듈 규칙 5개(SKB1~SKB5: skill 없이 turn이 완결되고 빈 선택은 실패가 아님 · 새 phase를 만들지 않음 · `core` 밖 형제 무참조 · **빼도 다른 package의 파일이 열리지 않음** · 넣어도 turn 불변식과 판정 축이 늘지 않음)와 성공 조건 “`skills/`를 지워도 첫 vertical slice가 그대로 돈다”. skills 소유 책임 7종(SK1~SK7: 등록 수용 · 등록 시점 가드 · registry revision · turn 선택 set 확정 · 순서·충돌·예산 판정 · prompt projection 값 생성 · provenance와 신뢰 등급 표시의 보존)과 소유하지 않는 것 13종(최종 prompt·request 조립과 compaction → `context` · tool 등록/허용/실행 · provider 변환 · turn loop · session 기록 · model 출력 판정 · **filesystem scan·plugin·marketplace·원격 registry·dynamic discovery** · **model 주도 설치·자기선택·권한 상승** · 선택의 도메인 규칙 · skill 본문 저작 · 신뢰 등급의 정의와 서명 인프라 · 로깅 destination · 조립). **등록 registry / turn 선택 set / prompt projection 세 값의 분리**와 타입에 맞는 관계 3종(**상한** 선택 set의 등록 식별자 집합 ⊆ registry의 것 — 여기서만 ⊆ · **변환** 선택 set → projection의 사상(담는 것이 다른 두 타입) · **출처 추적** 투영 조각 → 선택 set 원소 하나). **선택과 투영은 하나의 계산이 아니라 고정 순서 2단계**이며 단일 원자 facade를 두지 않고, 대가는 규칙 2개로 갚는다(SKC1 투영의 입력은 선택 set 하나뿐 · SKC2 순서 고정, 되돌아가지 않음). 규칙 8개(SKG1~SKG8: 출처는 언제나 호스트 명시 등록 · 등록과 선택은 다른 값 다른 시점 · **선택 입력은 호스트가 주고 model이 선택하지 않음** · 경로·secret·호스트 자원은 투영 값에 없음 · **선택 지정이 없으면 빈 집합**(KAG-DEC-006 OQ-11과 달리 여기서는 정할 수 있다 — skill 없는 turn이 정상이라 fail-closed가 기능 정지가 아니다) · 계산 실패를 빈 집합으로 접지 않음 · **본문은 등록 시점에 확정되고 turn 중 다시 읽지 않음** · 선택 set은 turn 내내 불변). 선택 set을 좁히는 입력은 registry와 호스트 선택 지정 둘뿐이고 **model 출력은 입력이 아니다**(tool의 접근 축 자리가 비어 있고, 그 자리를 “model이 고른다”로 채우려는 것이 가장 자연스러운 유혹이다). 파일 후보 트리 = module 6개(`definition`·`outcomes`·`registry`·`ordering`·`selection`·`projection`) + `__init__.py`, **REF-0007 `loader.py` 가안을 두지 않는 판단**(“loader”는 “어딘가에서 읽어온다”를 이름으로 인정하는 것이고, 이름을 지우는 것이 규칙보다 강한 방어다 — 이 문서가 설계 노트를 뒤집는 유일한 지점)과 `projection`이지 `prompt`·`renderer`가 아닌 이유. **이 package에는 부작용이 없다**(skill은 실행되지 않으므로 위험은 “잘못 실행됨”이 아니라 “잘못 노출됨”). 내부 4단 총순서(w0 값·표현 → w1 보관과 순수 판정 → w2 선택 확정 → w3 투영)와 **방향 규칙 5개** — **`projection`을 아무도 참조하지 않음**(model에 나갈 값이 만들어지는 경로가 하나) · `ordering` ↛ `registry` · **`projection` ↛ `registry`**(SKC1의 구조적 형태) · `registry`는 선택·투영을 모름 · 형제 package 무참조(**`context`가 가장 흔한 누수 경로** — “투영한 김에 문면까지”). **`core`가 skill 계약을 소유하지 않는다는 판단**(K1~K10에 skill 항목이 없는 것은 지금은 누락이 아니다)과 두 선택지(core 새 계약 vs 기존 message·content block 표현) 비교, 권고는 후자 우선 검토이되 provenance 자리 때문에 미확정. **고정 순서 2단계 · 합쳐서 국면 6개**(1단계 선택 = 수용 → 존재·동일성 → 순서·충돌 / 단계 경계 / 2단계 투영 = **예산 판정** → 투영 값 생성)이고 **실행 조건이 비대칭**이다 — 선택은 turn당 항상 한 번, 투영은 **선택 set을 받았을 때만** 한 번이며 **선택 거부는 투영을 부르지 않고 그 자리가 terminal**이다(거부는 선택 set이 아니라 SKC1상 넘길 값이 없다). lifecycle은 **terminal result 셋**(투영(빈 투영 포함) · 선택 거부 · 투영 거부) 중 정확히 하나로 끝나고, **빈 선택 set은 거부가 아니라 성공값이라 경계를 넘어 빈 투영이 된다**(SKG6은 유지 — “고른 것이 없다”와 “고르지 못했다”는 다른 결과). 예산 판정이 투영 생성보다 먼저인 이유(만든 뒤 재면 자르고 싶어지고, 잘린 instruction은 조용히 다른 지시가 된다), 불변식 9개(SKI1~SKI9: **실행된 각 단계가 결과 하나를 내고 lifecycle 전체는 terminal result 하나로 끝남**(“두 단계가 항상 실행된다”는 뜻이 아니다) · 빈 집합도 빈 투영도 거부도 결과 · **선택은 호스트 입력에서만** · 판정 기준은 turn 고정 값이지 현재 registry가 아님 · **본문은 등록 시점 확정값이고 turn 중 외부를 읽지 않음** · **skill은 tool을 만들지도 넓히지도 않음** · 투영 값은 지시가 아니라 데이터 · 기록하지도 model을 부르지도 않음 · 빈 선택은 정상 · **같은 입력이면 같은 순서·같은 투영**). KAG-DEC-002 12단계와의 대조로 **어떤 단계도 소유하지 않고** 1단계의 재료와 3단계의 입력만 제공함을 확인하고, **§3 snapshot 5항목에 skill 항목을 더하지 않는다**(accepted 문서의 개정이므로 권한 밖 → OQ-13). 투영 경계 원칙 7개(PJ1~PJ7: **투영은 권한을 만들지 않는다** — 본문이 tool X를 쓰라고 적어도 허용 subset에 없으면 실행되지 않는다(KAG-DEC-006 TI2) · 정책·한도·종료 조건을 담지 않음 · 라이브러리가 본문을 해석하지 않음 · **provenance가 값에 따라붙음** · 신뢰 등급은 등록 시점에 붙고 turn 중 올라가지 않으며 값의 의미는 호스트 것 · **model 출력이 skill이 되는 경로 없음**(자기설치 방어 — 이 경로가 열리면 한 번의 injection이 영구적이 된다) · **예산 초과는 자르지 않고 거부**). prompt injection 경로 10건의 판정에서 **일곱은 구조로 막히고 셋은 막지 못한다**(간접 injection — tool 결과 텍스트를 model이 지시로 읽는 것 자체는 막을 수 없고 보장은 “권한·선택·정책을 바꾸지 못한다”까지 · 신뢰할 수 없는 등록 — 등록은 호스트의 판단이고 라이브러리가 대신하지 않으며 주는 것은 provenance와 신뢰 등급의 자리까지 · model이 지시를 따르는지 — 보장 대상 아님), 실제 안전 보장은 “**skill 텍스트는 model이 무엇을 시도할지만 바꾸고 무엇이 가능한지는 바꾸지 못한다**” 한 문장이며 그 이상을 주장하지 않음. 관측·민감정보 원칙 8개(SKR1~SKR8, 특히 **skill 본문 축약은 어느 쪽에서도 하지 않는 편이 권고** — tool 결과는 데이터라 줄여도 의미가 남지만 지시문은 줄이면 다른 지시가 된다). `skills/__init__.py` **선별 재수출** 권고와 `selection`·`projection`을 둘 다 재수출하는 이유(`tools`가 판정 재수출을 막은 것은 RG4의 “같은 계산” 때문인데 skill에는 실행 축이 없어 그 요구가 없고, 지켜야 할 SKC1·SKC2는 호스트가 두 진입을 순서대로 부르는 것과 충돌하지 않는다. 대신 **순서를 라이브러리가 강제하지 않는다는 것이 facade 기각의 대가**), **skill 자산 거처는 (가)(호스트가 본문 문자열을 넘김)를 권고 구조의 전제로 명시**하고 (나)(경로를 받아 등록 시점 1회 읽기)/(다)(package 동봉)를 고를 때 **함께 supersede해야 할 항목을 표로 못박음**(나: 읽기 소유 파일 추가(**`registry`에 숨기지 않는다**)·검사 기준을 “존재 금지”에서 “한 파일로 국소화”로 완화·§1 서술·`loader` 부재 판단 / 다: 그 넷 + “skill 내용이 살지 않는다”와 SKB4의 의미). **어느 안에서도 dynamic discovery·plugin·marketplace 완전 제외는 유지**되며, OQ-2는 열어 두되 세 안이 대등하지 않다는 사실은 닫음(KAG-DEC-001 OQ-6 유지), contract test seam 7종(등록 가드 · 순수 판정 · 선택 단독 · 투영 결정성 · **단계 실행 조건**(선택 거부 turn에 투영이 호출되지 않는지) · **외부 읽기·노출 경로 부재는 구조 검사** · **확장 모듈성은 조립 검사**). KAG-BL-001 OQ-2·OQ-6·OQ-7·OQ-9와 KAG-DEC-001 OQ-1·OQ-6, KAG-DEC-003 OQ-6은 닫지 않음. 미결 16건 | - |

## 미결 사항

KAG-DEC-001이 다루기로 한 질문은 그 문서의 Open Questions 표가 owning view다. 아래에는 이 index가 추적할 항목만 요약하고 본문을 복사하지 않는다. 아직 어떤 decision에도 들어가지 않은 질문은 [KAG-BL-001의 Open Questions](../00-baseline/baseline-001-provider-neutral-llm-runtime.md#open-questions)에 남아 있다.

| ID | Question | Owner | Next |
|---|---|---|---|
| KAG-DEC-001 OQ-1 | PyPI 배포명을 import 이름과 같게 갈지 | 사용자 | 첫 배포 검토 시점 |
| KAG-DEC-001 OQ-3 | CLI·queue worker·web server를 언제·어디에 둘지 | 사용자 | 첫 vertical slice 이후 |
| KAG-DEC-001 OQ-8 | `products/open-kknaks/`와의 관계 | 사용자 | 별도 decision |
| (그 외) | KAG-DEC-001 OQ-2·4·5·6·7 | planner | [decision-001](decision-001-runtime-directory-boundaries.md#open-questions) 참조 |
| KAG-DEC-002 OQ-8 | 한 응답의 tool call 일부가 거부됐을 때 남은 호출을 계속 실행할지 | 사용자 | 첫 vertical slice 실행 결과 후 |
| (그 외) | KAG-DEC-002 OQ-1~7·9 (malformed repair, provider 재시도, 취소 전파, tool timeout, final 검증 기준, loop 방어, snapshot 기록, 공개 진입점) | planner | [decision-002](decision-002-turn-runtime-flow.md#open-questions) 참조 |
| KAG-DEC-003 OQ-1 | 재수출 이름의 안정 API 약속 수준과 버전 표기·deprecation | 사용자 | 첫 배포 검토 시점. KAG-DEC-001 OQ-1과 함께 |
| KAG-DEC-003 OQ-5 | `core/tooling.py` 이름이 형제 package `tools/`와 충분히 구분되는지 | 사용자 | 첫 파일 생성 직전 |
| (그 외) | KAG-DEC-003 OQ-2·3·4·6·7·8 (session event 저장 경계 형태 → `sessions` 상세 decision, turn 결과 타입 소유, package-root 재수출, evidence 계약, 신뢰 등급 부착 시점, tier 정적 검사) | planner | [decision-003](decision-003-core-contract-boundaries.md#open-questions) 참조 |
| KAG-DEC-004 OQ-4 | shell 실행 경로를 훗날 열 것인지와 그 조건 | 사용자 | 파이프라인이 실제로 필요한 사용처가 생긴 뒤. 그전까지는 열지 않는다 |
| KAG-DEC-004 OQ-10 | 종료 유예·출력 상한·시간 상한의 기본 수치를 라이브러리가 제안할지 | 사용자 | 첫 vertical slice 실행 결과 후 |
| (그 외) | KAG-DEC-004 OQ-1·2·3·5·6·7·8·9 (process 유지/접기 = KAG-DEC-001 OQ-4 유지, HTTP provider 공용 격리, 취소 전달 경로, `core` 참조 여부, 실패의 값/예외 표현, 호출 상한과 turn 상한의 관계, tier 정적 검사, 보안 항목 추가) | planner | [decision-004](decision-004-process-boundaries.md#open-questions) 참조 |
| KAG-DEC-005 OQ-5 | `providers/__init__.py`에 재수출을 두지 않는 것이 맞는지 | 사용자 | 첫 호스트 예제를 써 본 뒤 |
| KAG-DEC-005 OQ-10 | adapter 하위 package 이름 규칙(provider 제품명 표기) | 사용자 | 첫 파일 생성 직전 |
| KAG-DEC-005 OQ-11 | 첫 backend의 정확한 실행 옵션과 출력 protocol | 사용자 | KAG-BL-001 OQ-4 유지. 실제 실행 실험 후 |
| (그 외) | KAG-DEC-005 OQ-1·2·3·4·6·7·8·9 (재시도 = KAG-DEC-002 OQ-2 유지, 취소 전달 형태, `process` 호출을 직접 import할지 주입할지, 공통 protocol 투영의 거처, fake provider 거처, capability 축 = KAG-BL-001 OQ-5 유지, 원문 보관 범위 = KAG-BL-001 OQ-8 유지, 방향 규칙 정적 검사) | planner | [decision-005](decision-005-provider-boundaries.md#open-questions) 참조 |
| KAG-DEC-006 OQ-5 | 접근 판정 규칙을 라이브러리 내장(집합 포함)으로 둘지 호스트 주입으로 둘지 | 사용자 | 첫 호스트 예제에서 실제 권한 모델을 붙여 본 뒤. 어느 쪽이든 판정 지점은 하나 |
| KAG-DEC-006 OQ-6 | 승인이 필요한 tool을 노출에서 감출지 보이되 실행 시 거부할지, 거부에 요구치를 어디까지 실을지 | 사용자 | 첫 write tool이 생기는 시점 |
| KAG-DEC-006 OQ-11 | turn 허용 지정이 없을 때를 “등록 전부”로 읽을지 “허용 없음”으로 읽을지 | 사용자 | 첫 호스트 예제 작성 시 |
| (그 외) | KAG-DEC-006 OQ-1·2·3·4·7·8·9·10·12·13 (schema validator·dialect = KAG-BL-001 OQ-3 유지, sync/async = KAG-BL-001 OQ-2 유지, tool별 timeout = KAG-DEC-002 OQ-4 유지, 취소 전파 = KAG-DEC-002 OQ-3 유지, `declaration` 분리/이름, revision 발급 주체, 실패의 값/예외 표현, 방향 규칙 정적 검사, fake handler 거처, 결과 크기 상한) | planner | [decision-006](decision-006-tools-boundaries.md#open-questions) 참조 |
| KAG-DEC-007 OQ-4 | 위치 기대(expected revision)를 선택으로 둘지 필수로 둘지, session revision이 마지막 위치와 같은 값인지 | 사용자 | 첫 호스트 예제 작성 시. 선택은 편하고 필수는 fail-closed와 일관된다 |
| KAG-DEC-007 OQ-9 | provider 원문을 event에 보관할지, 보관한다면 어디까지 | 사용자 | KAG-BL-001 OQ-8 유지. KAG-DEC-005 OQ-8과 같은 질문 |
| KAG-DEC-007 OQ-10 | retention·법적 삭제 요구를 어디에 둘지, SF6(원본 삭제 경로 없음)과의 충돌 처리 | 사용자 | 실제 운영을 고려하는 시점. 라이브러리 append·조회 표면에는 뚫지 않는다까지가 권고 |
| (그 외) | KAG-DEC-007 OQ-1·2·3·5·6·7·8·11·12·13·14·15 (store 계약 형태 = KAG-DEC-003 OQ-2 잔여, sync/async = KAG-BL-001 OQ-2 유지, append 단위 크기, 멱등 키 발급 주체, memory 직렬화 왕복 강제, `reading` 분리/합치기, 저장 format과 version, 실패의 값/예외 표현, 재수출 범위와 fake store 거처, 방향 규칙 정적 검사, snapshot 기록 = KAG-DEC-002 OQ-7 유지, `serialization`이 아는 판별값을 얻는 방법 = `core` codec 미확정) | planner | [decision-007](decision-007-sessions-boundaries.md#open-questions) 참조 |
| KAG-DEC-008 OQ-1 | skill 선택을 호스트 명시 지정만으로 둘지, 호스트가 주입하는 비-model selector의 자리를 만들지 | 사용자 | KAG-BL-001 OQ-7 유지. 어느 쪽이든 model이 선택하지 않는다(SKG3)와 “확정은 turn 시작 한 번”은 유지 |
| KAG-DEC-008 OQ-2 | skill 자산이 package 안에 사는지 호스트가 소유하는지 (문자열 등록 / 등록 시점 1회 읽기 / package 동봉) | 사용자 | KAG-DEC-001 OQ-6 유지. 첫 호스트 예제 이후 |
| KAG-DEC-008 OQ-8 | 복수 skill의 순서 결정 기준 — 호스트 지정 순서인가 등록 순서인가 우선순위 값인가 | 사용자 | 첫 skill 두 개가 생기는 시점 |
| KAG-DEC-008 OQ-13 | KAG-DEC-002 §3 snapshot에 “선택된 skill set”을 여섯 번째 항목으로 더할지(그 문서의 supersede 필요), 호스트 turn 입력으로 둘지 | 사용자 | skills를 실제로 조립에 넣는 시점. **KAG-DEC-008은 KAG-DEC-002를 수정하지 않았다** |
| KAG-DEC-008 OQ-14 | 조립을 L4가 할지 `runtime`이 `skills`를 직접 부를지 | 사용자 | 첫 호스트 예제 작성 시. L4 조립이 확장 모듈성을 코드로 성립시킨다는 것이 권고 |
| KAG-DEC-008 OQ-15 | skill 자산이 배포물에 포함되는지와 packaging 형태 | 사용자 | KAG-DEC-001 OQ-1과 함께 |
| (그 외) | KAG-DEC-008 OQ-3·4·5·6·7·9·10·11·12·16 (투영 값의 형태와 prompt syntax → `context` 상세, 저장 포맷, version 동시 선택, 충돌 판정 범위, 예산 단위 = KAG-BL-001 OQ-6 유지, sync/async = KAG-BL-001 OQ-2 유지, 신뢰 등급 형태 = KAG-DEC-003 OQ-6과 동일, `ordering` 합치기, 방향 규칙·구조 검사 정적화, fake registry 거처) | planner | [decision-008](decision-008-skills-boundaries.md#open-questions) 참조 |
| (미착수) | Codex CLI 격리 옵션, 첫 vertical slice 범위 등 | - | KAG-BL-001 Open Questions 참조 |

> KAG-DEC-001 OQ-2(공개 import 표면)는 KAG-DEC-003 §5.2에서 세 조각으로 쪼개졌다. (a) `core/__init__.py` 재수출은 KAG-DEC-003이 권고안으로 다루고, (b) package-root 재수출은 KAG-DEC-003 OQ-4로, (c) 안정 API 약속은 KAG-DEC-003 OQ-1로 이관 제안됐다. KAG-DEC-003이 확정되기 전까지 owning view는 여전히 [decision-001](decision-001-runtime-directory-boundaries.md#open-questions)이다.

## Next

KAG-DEC-001·KAG-DEC-002는 `accepted` 완료. 현재 게이트는 **KAG-DEC-003(core 계약 경계)·KAG-DEC-004(process 실행 격리 경계)·KAG-DEC-005(providers 변환 경계)·KAG-DEC-006(tools 등록·허용·검증·실행 경계)·KAG-DEC-007(sessions event 저장·조회 경계)·KAG-DEC-008(skills 등록·선택·prompt 투영 경계)의 사용자 리뷰** 여섯 건이다. 공개 계약은 디렉터리 하나씩 의존 그래프 아래에서 위로 내려가고, 지금까지 raw 제안이 올라온 것은 `core`(L0)·`process`(L1)·`providers`(L2)·`tools`(L2)·`sessions`(L2)·`skills`(L2)다. 여섯 문서는 서로를 `proposed` 입력으로만 참조하므로 함께 읽는 편이 낫다.

남은 raw 작성 순서는 **`context` → `runtime`**이다. `runtime`이 마지막인 이유는 나머지를 조립하는 자리이기 때문이다. `context`는 KAG-DEC-007이 원본 쪽만 다루고 남긴 절반(model context 투영·compaction·요약 취급)과 KAG-DEC-008이 값까지만 만들고 넘긴 skill 투영의 문면 배치를 함께 받는다. spec은 그 뒤에 연다. 확정 전에는 spec·work·코드로 내려가지 않으며, KAG-DEC-009 이후의 ID를 미리 선점하지 않는다.
