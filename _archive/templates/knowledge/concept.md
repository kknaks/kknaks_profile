---
type: concept
id: <파일명 stem 과 동일. **날짜를 붙이지 않는다** — 개념은 특정 시점에 묶이지 않는다>
title: <개념 이름. 한국어 우선, 필요하면 괄호로 원어 병기>
aliases:
  - <**링크는 영문 stem 으로 건다** — 파일명이 `inheritance.md` 면 `[[inheritance]]` 가
     별칭 없이 그대로 풀린다. 여기는 그 외의 표기를 받는 자리다>
  - <약어(USL) · 흔한 오기(캐시/캐쉬) · 한국어 표기 · 띄어쓰기 변형(커넥션 풀/커넥션풀)>
  - <같은 개념을 다른 이름으로 찾게 되는 것만 넣는다. 안 쓸 이름을 미리 깔지 않는다>
up:
  - <이 개념이 나온 reference stem. **본문 「출처」에도 `[[]]` 로 있어야 한다**>
tags:
  - <검색용 키워드 2~5개>
---

# <개념 이름>

<한두 문장으로 "이 개념이 무엇인가".
**여기가 이 개념의 SoT다** — reference 와 permanent 는 이 내용을 재서술하지 않고
`[[이 stem]]` 으로 위임한다.>

## 정의

<개념의 구성 요소나 단계. 필요하면 번호 목록으로.
**사실만 적는다** — "우리 제품에 어떻게 쓸까" 같은 판단은 permanent 종합 노트가 소유한다.
출처가 여럿이면 각 출처가 더한 내용이 여기서 하나로 합쳐진다.>

## 사용 예시

<**선택.** 코드로 보여줄 수 있는 개념만 둔다 — 원론적·추상적 개념(법칙·성질·전략)에
억지로 코드를 붙이면 개념이 오히려 좁아진다.

「정의」와 역할이 다르다. **정의는 문법 골격**(최소 형태), **여기는 도메인이 붙은 실제
코드**다. `Parent`/`Child` 가 정의라면 `SmartPhone extends Phone` 이 여기다.

**출처에 예시가 있으면 반드시 살린다.** 그 예시가 그 자료를 읽은 값의 절반이다 —
버리면 나중에 원본을 다시 열게 된다.>

## 왜 중요한가

<이 개념을 모르면 무엇이 잘못되는가. 어떤 문제를 설명하거나 예방하는가.
"중요하다"가 아니라 **무엇이 달라지는지**를 적는다.>

## 경계와 오해

<이 개념이 **아닌** 것, 흔히 혼동되는 인접 개념, 적용 한계.
개념이 잘못 쓰이는 지점을 여기 남긴다 — 나중에 이 노트를 다시 읽을 때
가장 값이 큰 섹션이다.>

- **X ≠ Y** — <혼동되는 지점과 구분 기준>

## 함께 보는 개념

<관련 concept 를 `[[]]` 로 잇는다. 상하 관계가 아니라 연상 관계면 여기 둔다
(`up:` 에 넣지 않는다 — `up:` 은 계보 전용이다).>

- [[<concept stem>]] — <어떤 관계인지 한 줄>

## 출처

- [[<reference stem>]] — <그 자료가 이 개념에 대해 말한 것 한 줄>

<!--
경로: resources/concept/{slug}.md — **flat**. 하위 디렉토리를 두지 않는다
      (개념은 분류 트리가 아니라 링크 그래프로 조직된다).

입도 판단: 다른 자료·다른 맥락에서 이 개념만 따로 등장할 수 있으면 별개 개념이다.
  아니면 상위 개념의 구성 요소로 본문에 둔다. 상세는 rules/knowledge-note-pipeline.md.

개념 성장 — 같은 개념에 두 번째 출처가 오면 **새 파일을 만들지 않는다**:
  1. frontmatter `up:` 에 새 reference stem 추가
  2. 본문 「출처」에 `[[stem]]` 한 줄 추가 (L3 — up 은 본문 링크의 부분집합이어야 한다)
  3. 새 출처가 더한 내용을 해당 섹션에 **녹인다** — 덧붙이는 게 아니라 다시 쓴다
  4. 새 이름으로 불렸다면 `aliases` 에도 추가

검증: `aliases`·`up` 누락은 L2 ERROR 로 부팅이 막힌다.

묶음 단위 — **개념 하나씩이 아니라 출처 하나씩** 만든다. 개념은 서로를 `[[]]` 로
  가리키므로 하나만 만들면 나머지가 전부 L1 dead link 가 된다. 한 출처에서 나오는
  개념을 한 묶음으로 만들어야 그 안에서 링크가 닫힌다. 작업 중 깨져 있는 것은
  괜찮다 — 묶음이 끝날 때 0이면 된다.
-->
