---
type: concept
id: twos-complement
title: 2의 보수 (Two's Complement)
aliases:
  - 2의 보수
  - 이의 보수
  - twos complement
  - 1의 보수
  - sign-magnitude
up:
  - 2024-06-03-Day07
  - 2024-07-18-Day38
tags:
  - 자료형
  - 비트연산
  - 메모리
---

# 2의 보수 (Two's Complement)

음수를 비트로 저장하는 방법. **1의 보수(비트 반전)에 1을 더한 값**이며, Java 를 비롯한 대부분의 언어가 이 방식을 쓴다.

## 정의

음수를 담는 방법은 세 번에 걸쳐 정해졌다. 앞의 둘이 왜 버려졌는지가 이 개념의 내용이다.

### 1. Sign-Magnitude — 부호 비트 + 절대값

최상위 비트를 부호로 쓰고(양수 `0`, 음수 `1`) 나머지에 절대값을 담는다. 간단하지만 두 가지가 깨진다.

```plaintext
0001 (1) + 1001 (-1) = 1010 (-2) ?!
```

- **덧셈이 성립하지 않는다** — 부호를 따로 보고 분기해야 한다
- **0 이 둘이다** — `+0` 과 `-0` 이 다른 비트열이 된다

그래서 일반 정수에는 쓰지 않고, [[floating-point]] 의 **가수부**에 쓰인다.

### 2. 1의 보수 — 비트 반전

주어진 비트를 그대로 뒤집는다. 여전히 양수 + 음수 계산이 어긋나고 0 이 둘이다.

### 3. 2의 보수 — 1의 보수 + 1

1의 보수를 구한 뒤 1 을 더한다. 이렇게 하면 **부호가 다른 같은 수를 더했을 때 0 이 나온다** — 자리 올림이 최상위 밖으로 넘어가(overflow) 버려지기 때문이다.

## 왜 중요한가

**메모리 안의 표기가 곧 파일 안의 표기가 된다.** 6주 뒤 회차가 정수를 파일에 적을 때 하는 일은 변환이 아니라 **그 4바이트를 그대로 내보내는 것**이다 — 「타입별로 인코딩하는 방식이 다르다」에서 정수 칸에 적히는 이름이 바로 2의 보수다. 이 표기가 표준이 아니었다면 파일마다 「이 정수는 어느 방식으로 적혔나」가 따라붙어야 했다 → [[binary-io]]

**부호를 신경 쓰지 않고 더하기만 하면 된다.** CPU 가 음수를 위한 별도 회로를 갖지 않아도 되고, 뺄셈도 "2의 보수를 더하는 것"으로 처리된다. 하드웨어가 단순해지는 것이 이 방식이 이긴 이유다.

0 이 하나라는 점도 크다. `+0`·`-0` 이 갈리면 "이 값이 0인가"를 비교할 때마다 두 경우를 봐야 하는데, 그 문제가 사라진다.

## 경계와 오해

- **음수 범위가 양수보다 하나 넓다** — 0 이 하나로 합쳐진 자리만큼 음수가 하나 더 들어간다. `byte` 가 `-128 ~ 127` 인 이유이고, 그래서 `-128` 의 절댓값은 같은 타입에 담기지 않는다.
- **최상위 비트는 부호 비트가 아니다** — 2의 보수에서 최상위 비트가 1이면 음수인 것은 맞지만, 나머지 비트가 절대값인 것은 아니다. sign-magnitude 와 혼동되는 지점이다.
- **overflow 가 버그가 아니라 원리다** — 부호 다른 수를 더해 0 이 나오는 것은 자리 올림이 버려지기 때문이고, 이것이 정상 동작이다.
- **`boolean` 은 2의 보수로 저장되지 않는다** — Day38 의 「타입별 인코딩」 표가 `true --> 2의 보수` 로 적었는데 **틀렸다.** 2의 보수는 **음수를 담는 방법**이고 `boolean` 에는 음수가 없다 — 담을 값이 둘뿐이라 보수를 취할 대상 자체가 없다. `DataOutputStream.writeBoolean` 은 `1` 또는 `0` 을 1바이트로 쓴다. 같은 표의 `"abc"`→UTF-8, `3.14`→IEEE-754 는 각각 맞고, **이 한 줄만 바로 위의 `20` 에서 따라온 것**이다 → [[binary-io]] · [[data-type]]
- **「정수는 2의 보수로 저장된다」와 「20을 2의 보수로 바꾼다」는 다른 말이다** — `int` 가 2의 보수 **체계의 타입**인 것은 맞지만, 양수 20 의 비트는 그냥 이진수 `10100` 이고 보수를 구하는 계산이 일어나지 않는다. 이 방식이 실제로 하는 일은 `-20` 을 적을 때 나타난다. 「타입별 인코딩」 표에서 정수 칸에 적을 이름은 **「2의 보수 표기의 고정 길이 정수」**이지 「2의 보수를 구한다」가 아니고, Day38 의 `20 --> 2의 보수` 를 뒤쪽으로 읽으면 어긋난다.

## 함께 보는 개념

- [[floating-point]] — 가수부에 sign-magnitude, 지수부에 bias 를 쓰는 다른 조합
- [[class-file-format]] — 값이 실제로 바이트로 어떻게 놓이는지 확인해 본 자리
- [[binary-io]] — 타입마다 다른 표기를 파일에 그대로 적는 층
- [[bit-shift]] — 그 표기에서 바이트를 꺼내는 도구

## 출처

- [[2024-06-03-Day07]] — sign-magnitude 의 덧셈 오류와 `±0` 문제, 1의 보수도 같은 문제를 남긴다는 것, 2의 보수에서 부호 다른 같은 수를 더하면 overflow 로 0 이 된다는 것을 배웠다
- [[2024-07-18-Day38]] — 「타입별로 인코딩하는 방식이 다르다」로 파일에 적히는 표기를 타입별로 나열하면서 정수 칸에 2의 보수를 적었다 — **메모리 안의 표기가 그대로 파일로 나가는 것**을 확인하는 자리다. 다만 표의 `true --> 2의 보수` 한 줄은 앞 줄에서 따라온 오류이고, `20 --> 2의 보수` 도 「체계의 이름」과 「하는 계산」을 겹쳐 놓았다
