# ax-knowledge-graph Log

- 2026-07-24 AXKG-SPEC-015 구현(2단계 자동화) + 전략 B→A 정정. ax-graph `50d87b7` 배포 완료. **git_sync.py**(GitClient=mediness publish 미러: subprocess·safe.directory·PAT 커맨드마다 URL주입). **전략 A(commit-then-rebase-push)** 구현 — B(reset-first) 기각: AXKG 승인=DB+파일 결합이라 push 실패 시 reset --hard가 미커밋 문서를 되돌려 불일치, A는 push 실패해도 커밋 누적·파일 유실 없음. 승인 훅(`approval_gates` 문서게이트&sync on → background `sync_after_approval`), pull 루프(`main` lifespan `pull_reindex_loop`: 주기 fetch_ff→`run_startup_scan` 재빌드, §6), api Dockerfile git 설치, compose api를 **repo 루트 마운트**(`/mnt/data/axkg/repo:/workspace`, `AXKG_MARKDOWN_ROOT=/workspace/documents`)해 `.git` 노출. **sync 기본 off**(이 배포 동작보존) — 켜려면 ENV_PROD에 `AXKG_DOCS_GIT_SYNC_ENABLED=true`·`_TOKEN`·`_AUTHOR_EMAIL`. 서버 클론 full 전환(blobless→full, promisor 회피). 전체 477 테스트 통과. SPEC-015 §3·4·6·7·8·9 A 기준 정정.
- 2026-07-24 AXKG-DEC-010 + AXKG-SPEC-015 작성. 문서 SoT(`/mnt/data/axkg/documents`, 현행 git 아님·백업 0·앱 root 자동 write)를 **ax-graph 코드 레포 하위 `documents/`** 로 git 버전관리 결정. **승인 시점 commit+push**(문서화 승인 `ApplyExecutor.apply()` 파일 확정 뒤 background `add documents/ → commit → pull --rebase → push`) + **사람 교정 루프**(로컬 clone에서 `documents/` pull→수정→push). 서버 SoT는 코드 레포 **cone sparse-checkout(`documents/`만)** 워킹트리로 전환, 바인드 마운트 재지정. 정합 불변식: 서버는 승인마다 즉시 commit(uncommitted 방치 금지), 충돌(같은 파일 AI+사람)은 자동병합 금지·Slack 알림 후 정지, push 실패 비치명. **CI(`deploy-prod.yml`) 트리거에 `documents/**` 없음 확인** → 문서 push 재빌드 미발동. push 크리덴셜(GitHub email·PAT)은 `.env` 시크릿(`AXKG_DOCS_GIT_TOKEN`). 재인덱싱: qmd 검색인덱스는 사이드카 주기적 증분이 자동 흡수(staleness 수용, 2026-07-14 정정), **api 그래프 엣지 cache는 pull 후 변경 md 재빌드 필요**(계약 필수). 대안 전용 repo(ax-graph-documents)는 보류(운영 부담 시 재검토). DEC-002 OQ-001(배포 host path) 해소. 미구현 — 구현계획 `tasks/active/kknaks-profile/axkg-docs-git-sync-plan.md`. 10-decision/README·20-spec/README 행 추가.
- 2026-07-14 retriever-empirical-correction. WORK-008(PLAN-013-T-006) 구현 실측(CPU-only)으로 사용자 수용 확정한 **2건 스펙 정정**(소규모 amendment·이 2건만·발명 금지·work-008 무접촉[워커 갱신]·코드 무접촉). **① 리랭크 기본값 on → off**: CPU-only에서 LLM 리랭크 1회 60초+ 실측(qmd 공식도 CPU는 off 권장), 기본 off·설정(SPEC-007 표면) on 가능·GPU 배포 시 on 권장 — OQ였던 리랭크 기본값이 결정으로 승격. **② 인덱싱: api 쓰기/확정 시점 이벤트 구동 → 사이드카 소유 주기적 증분 재인덱싱**: qmd MCP가 index/update 툴 미노출이라 api 이벤트 구동 불가, 사이드카가 주기적 증분 재인덱싱 소유·확정 직후 반영까지 수분 staleness 허용(수용). **"채팅 요청 경로 인덱싱 비용 0" 계약 불변**. **SPEC-006**: §1 Meta(리랭크 기본 off·인덱싱 주기적 증분·staleness)·§5 Implementation Rules 2건(리랭크 기본 off 근거·인덱싱 사이드카 소유)·§7 OQ(리랭크 기본값 OQ에서 빠지고 off 확정). **SPEC-011**: retriever 공유 컴포넌트 규칙(리랭크 기본 off·인덱싱 사이드카 소유·staleness)·§7 OQ(리랭크 기본값·인덱싱 방식 확정으로 OQ에서 제거). **DEC-003** 개정 섹션: 1단/인덱싱 bullet 2건 정정 마킹 + 신규 §실측 수용 정정(dated·계약 골격 불변·기본값/방식만) + OQ·"결정하지 않는 것"에서 리랭크 기본값 제거. **튜닝 확정값(top-K·edge 가중치·hop 감쇠) 스펙에 안 박음** — "구현 기본값" OQ 패턴 유지(코드 소관). qmd 통합 형태(HTTP MCP)는 이 2건 밖이라 스펙 OQ 유지(무접촉). "리랭크 기본 on"·"쓰기/확정 시점"(retriever 인덱싱 문맥) 잔재 grep 0. **status stable 유지**. validator errors 0/warnings 0(--strict). PLAN-013-T-007.
- 2026-07-14 plan013-wp-decompose. 채팅 고도화 라운드(PLAN-013) 스펙 라운드(T-001~T-004 확정분)를 **코드 발주용 WP 3건으로 분해**(사용자 승인 분해대로·추가 발명 금지·코드/라이브 무접촉·status 전부 초기 todo). **신규 WORK 3건**(30-work/): **AXKG-WORK-008(WP7 Graph RAG 2단 retriever, qmd 사이드카·BE 단독)** — Covers SPEC-006(retriever §1·§5)·SPEC-011(공유 컴포넌트·`RETRIEVER_FALLBACK_USED`)·DEC-003 개정, C-item 6(qmd 사이드카 docker 배선·증분 인덱싱·1단 qmd 하이브리드[리랭크 토글 기본 on·설정 표면=SPEC-007]·2단 그래프 확장[edge 가중치·hop 감쇠·다중 시드 합산·선택 노드 우선·근거 경로 강화]·keyword+edge graceful fallback[관찰 기록]·chat④·문서화③ 공유 적용). **AXKG-WORK-009(WP8 채팅→인박스 push, BE+FE)** — Covers SPEC-006(push S-4·API)·SPEC-003(chat 채널 데이터 계약)·SPEC-008(접근 경계 매트릭스 push 행)·DEC-006 개정, C-item 5(push endpoint[staff·admin 비대칭 쓰기]·대화 전체 직렬화[형식·조립 위치는 SPEC-006 §7 OQ대로 구현 확정]·chat source 생성[source_channel=chat·raw_text 필수·provenance]·요약→분류 합류[User Note Fallback 재사용·분류 승인 admin]·FE push CTA·상태 표면[인박스 표면 미노출·page-graph.html 시안]). **AXKG-WORK-010(WP9 인박스 md 업로드 intake, BE+FE)** — Covers SPEC-003(upload 채널·S-5·`UNSUPPORTED_UPLOAD_TYPE`·`original_filename`)·SPEC-012(경계 — adapter 대상 아님), C-item 4(업로드 endpoint[.md만·admin 전용]·upload source 생성·요약 직행[md 본문=원문·fallback 아님]·FE Direct Inbox Modal 업로드 UI). **역링크 갱신**: SPEC-006 works+008·009, SPEC-003 works+009·010, SPEC-011 works+008, SPEC-008 works+009, SPEC-012 works+010, DEC-003 works+008, DEC-006 works+009. **30-work/README** WP Map(WP7·8·9 행·병렬 note)·Status Board(3행)·Spec Coverage(SPEC-006/003/008/011/012 → in-progress, 신규 WP 잔여 반영) 갱신. **구현 세부(클래스/파일 경로) 발명 금지** — 계약·스펙 참조 수준. 스펙 본문 재개정 없음(§Covers 참조만). status todo(착수 표기는 코드 라운드). validator errors 0/warnings 0(--strict). PLAN-013-T-005.
- 2026-07-14 inbox-md-upload-intake. 채팅 고도화 라운드(PLAN-013 ③) **Source Inbox md 파일 업로드 intake 스펙 확정**(사용자 2026-07-14 추가 확정·확정 내용만·발명 금지·코드/라이브 무접촉). 인박스 intake에 **md 파일 업로드 추가, v1은 `.md`만**(pdf/docx 등 후속 확장·파싱 계층 필요·parking). 의미: URL 수집(SPEC-012) **스킵**하고 **업로드 md 본문 자체가 원문** — 그대로 요약 입력이 되어 기존 요약→분류→문서화 파이프라인 합류(chat의 User Note Fallback과 다름: fallback 아니라 원문 그 자체). **DEC 판단: 신규/개정 불요** — 권한 경계 무변경(admin 전용 유지·SPEC-008 무변경)이라 intake 채널 추가는 SPEC-003 계약 확장일 뿐 ADR급 결정 아님(T-002 chat push는 비대칭 권한이라 DEC-006 개정 필요했던 것과 대조). `source_channel` 신값=**`upload`**(컨벤션 정합). **SPEC-003 개정**: intro·Scope in(md 업로드)·out(md 외 포맷 후속)·**`source_channel` enum 3곳 전부 `…chat`→`…chat, upload`**(Validation·Data Contract·Source Inbox Document Form)·`source_url`/`slack_message_ts`(upload=null)·`raw_text`(upload=md 본문·필수·원문 그 자체)·신규 필드 `original_filename`(업로드 원본 파일명 보존)·U-3 Direct Inbox Modal에 md 업로드 방식 추가(같은 표면·admin 전용)·신규 S-5(admin md 업로드 시나리오)·Case Matrix `UNSUPPORTED_UPLOAD_TYPE`(.md 아니면 거부·source 미생성·수집 실패 아님)·Impl Rule·AC 4건·§7 OQ(파일 크기 상한·저장 위치 DB vs FS·md frontmatter 처리·md 외 포맷 parking). **SPEC-012 정합**(수집 어댑터 경계): intro 경계 blockquote(upload=adapter 대상 아님·원문 그 자체)·Scope out(업로드 md intake 제외)·§3 Failure Contract 경계(URL 수집 실패 코드·재시도 계약 upload 미적용·`.md` 아님은 intake validation 거부이지 수집 실패 아님·요약 실행 실패는 SPEC-011 계약)·§5 Integration(upload은 adapter collect 스킵, 요약 입력 직접 — SourceMaterial 합성·content_format 값은 구현 소관). **SPEC-008 무접촉**(경계 변경 없음·기존 "소스 Inbox 표면" 행에 포섭 확인만 — 태스크 요구). 파일 크기 상한·저장 위치·md frontmatter·엔드포인트 최종 shape 발명 금지(OQ 박제). 권한 경계 무변경(staff 업로드 개방 X). `source_channel` enum 잔여 stale(upload 누락) 0(grep 확인). **status stable 유지**. validator errors 0/warnings 0(--strict). PLAN-013-T-004.
- 2026-07-14 chat-push-payload-full-conversation. chat→inbox push **payload 정의 1건 정정**(사용자 2026-07-14 추가 확정·T-002 산출물 위 소규모 amendment·코드/라이브 무접촉). push되는 `raw_text` = 방안 본문만이 아니라 **push 시점까지의 해당 채팅 대화 내용 전부**(user·assistant 메시지 이력, 제시된 방안은 assistant 메시지로 자연 포함). 근거: 방안만 떼면 방안이 나온 맥락·근거가 유실되므로 대화 전체를 넣고 요약①이 정제(User Note Fallback 경로 그대로). **SPEC-006**: push API `raw_text` 정의·S-4 시나리오(step3~6)·U-2 기대결과·suggested_actions 계약·Impl Rule·Case Matrix(`EMPTY_PUSH_TEXT`)·AC에서 "방안 본문"→"push 시점까지의 대화 내용 전부(방안 포함)" 정정, request `run_id`를 push 컷오프+provenance로 명확화, **§7 신규 OQ**(대화 직렬화 형식·조립 위치[클라 직렬화 vs 서버 chat_id 조립]·길이 상한/truncation은 구현 소관/미결). **SPEC-003**: chat source 데이터 계약의 `raw_text` 서술 정정(Scope·S-4·Validation `source_url`/`raw_text` 행·Data Contract·Impl Rule·AC), 직렬화 형식은 SPEC-006 §7 참조. **DEC-006** 개정 섹션에 payload=대화 전부 한 절 보강(경계 결정 자체 무변경·payload SSOT=SPEC-006/003). **T-002 확정사항 전부 불변**: 권한 비대칭(staff push만·인박스 표면 admin)·CTA 기반 push만·`source_channel=chat`·분류 승인 admin·provenance(chat/run). endpoint·권한·enum·게이트 무접촉. "방안 본문"이 raw_text 정의로 남은 곳 0(grep 확인). **status stable 유지**. validator errors 0/warnings 0(--strict). PLAN-013-T-003.
- 2026-07-14 chat-to-inbox-push. 채팅 고도화 라운드(PLAN-013 ② 채팅 활용방안) **생각→방안 제시→chat push flow 스펙 확정**(사용자 확정 2026-07-14·확정 내용만·발명 금지·코드/라이브 무접촉). flow: 사용자가 생각/아이디어를 채팅④에 물음 → 시스템이 관련 노드 근거로 **방안 제시** → 사용자가 `Source Inbox에 추가`로 방안을 push(SPEC-006 기존 CTA `Source Inbox에 추가`의 실체화). **DEC-006 in-place dated amendment**(신규 DEC 아님 — 기존 접근 경계의 refinement, DEC-005 선례): **채팅→Inbox push = 전 유저(staff 포함) 허용 단일 쓰기 액션**(사용자 명시 CTA만·자동 push 없음), **인박스 표면[목록·조회·관리·삭제/무시]은 admin 전용 유지**(비대칭: staff 쓰기 O·읽기/관리 X, 본인 push분도 열람 불가), 분류 승인 admin 유지(SPEC-001/002 무변경), 채팅 접근 자체 무변경. links.specs에 SPEC-003·006 추가. **SPEC-008 정합**(경계 매트릭스 SSOT): §4 매트릭스에 `채팅→Inbox push(단일 쓰기 액션)|SPEC-006/003|✅|✅` 행 추가 + 소스 Inbox 행을 "표면(목록·조회·관리·수집·삭제/무시)"으로 명확화, 비대칭 권한 설명 bullet, Protected Routes에 `Chat→Inbox Push|staff|POST /graph/chats/{chat_id}/push-to-inbox` 행. **SPEC-006 개정**: Scope in(방안 제시+push)·out(인박스 표면·자동 push 제외), U-2 기대결과(방안→push·staff 허용·인박스 표면 미노출), 신규 S-4(생각→방안→push 시나리오), API `POST /graph/chats/{chat_id}/push-to-inbox`(authenticated staff·admin)+push 권한 경계 blockquote+Request/Response(raw_text·run_id / source_id·status), suggested_actions에 방안 본문 동봉 계약, Impl Rules 5건(방안 제시·CTA만 push·source_channel=chat 생성·User Note Fallback 재사용·비대칭 권한), Case Matrix `EMPTY_PUSH_TEXT`, AC 4건, links.decisions에 DEC-006. **SPEC-003 개정**(chat source 데이터 계약 소유): intro·Scope in(chat intake)·권한 각주(chat push 예외=staff 단일 쓰기·인박스 표면 admin 유지)·Validation(`source_channel` slack/manual/**chat**·chat이면 `source_url=null`·`raw_text` 방안 본문 필수)·Data Contract(동일 3필드 정합·chat=URL 없음·submitted_by=push 유저)·Source Inbox Document Form frontmatter enum·신규 S-4(chat push intake 시나리오)·Impl Rule(chat source=`source_channel=chat`/`source_url=null`/`slack_message_ts=null`/`raw_text` 필수·URL 없어 원문 수집 없이 raw_text가 요약 입력·SPEC-012 User Note Fallback 재사용)·AC 2건, links.decisions에 DEC-006. **push endpoint 설계 판단**: 기존 admin 전용 `/sources/manual` 확장이 아니라 채팅 표면의 **신규 dedicated endpoint**(staff 접근 가능한 `/graph/chats/*`에 위치, 단일 쓰기만 열어 인박스 표면 admin 경계 보존). 20-spec README SPEC-003/006 Decision 컬럼 갱신. 하지 말 것 준수: staff 인박스 열람(본인 제출분 포함) 안 엶·자동 push 발명 안 함·별도 인박스 신설 안 함·컴포넌트/파일경로/최종 endpoint shape 발명 금지(계약 수준까지·BE 정합). SPEC-012 무접촉(User Note Fallback 경로 참조만). **status stable 유지**(계약 표면 개정·스코프 불변). validator errors 0/warnings 0(--strict). PLAN-013-T-002.
- 2026-07-14 graph-rag-2stage-retriever. 채팅 고도화 라운드(PLAN-013 ① Graph RAG 고도화) **retriever 2단 재설계 스펙 확정**(사용자 확정 2026-07-14·확정 내용만·발명 금지·코드/라이브 무접촉). **DEC-003 in-place dated amendment**(supersede/신규 DEC 아님 — 핵심 계약 Graph RAG·기본 claude·`INSUFFICIENT_GRAPH_CONTEXT` 불변, 검색 메커니즘만 개정, DEC-005 amendment 선례 준수): ① **embedding 검색 = pgvector 대신 qmd 사이드카 채택**(github.com/tobi/qmd — 로컬 md 검색엔진: BM25+로컬 embedding 벡터 RRF+LLM 리랭크, SQLite 자체 인덱스, CLI/MCP), **pgvector는 계속 파킹**(도입 안 함, qmd가 그 자리 대체). ② **retriever 2단 구조**: 1단 후보 발굴=qmd 하이브리드(BM25+벡터 RRF)·**리랭크 설정 토글 기본 on**(설정 표면 owning spec=SPEC-007 경계 참조만), 2단 그래프 확장=자체 구현(qmd top-K 시드 wikilink 탐색·edge 타입 가중치·hop 감쇠·다중 시드 합산·선택 노드 우선 유지·근거 경로 산출 강화). ③ **인덱싱=문서 쓰기/확정 시점 증분**(채팅 요청 경로 인덱싱 비용 0). ④ **graceful fallback**: qmd 장애 시 기존 `keyword+edge distance`로 자동 폴백(현행 MVP 로직 삭제 아님·폴백으로 강등 잔존). 위 Decision "keyword+edge ranking"·"embedding·pgvector MVP 제외" 두 채택 bullet + Implementation Notes 2건 개정 마킹, Resulting Spec에 SPEC-011 행 추가, links.specs에 SPEC-011 추가, OQ(튜닝 숫자·qmd 통합 형태). **SPEC-006 정합**: §1 Meta `Retriever` 항 2단 재서술(종전 `Ranking: keyword+edge`)·Scope bullet·§5 Implementation Rules retriever 규칙 계약 재설계(2단·리랭크 토글·인덱싱 시점·폴백·pgvector 파킹·SSOT=SPEC-011)·§7 OQ. **SPEC-011 정합**(retriever 공유 컴포넌트 SSOT): Meta DEC-003 개정 표기·Out of scope·Connection Candidate Context 표 1단 후보 서술(`keyword+edge`→qmd 2단+폴백)·공유 컴포넌트 규칙에 2단 전환이 문서화③에도 동일 적용 + qmd 폴백 관찰 기록 + **용어 주의**(연결 후보 컨텍스트 "2단"[retriever+index] ≠ retriever 내부 "2단"[qmd+그래프] 축 구분)·**Case Matrix 신규 `RETRIEVER_FALLBACK_USED`**(qmd 장애→keyword+edge 폴백, 실패 아님·품질 강등·③④ 공통, 기존 `*_FALLBACK_USED` 패턴)·§7 OQ 확장(retriever 튜닝·qmd 통합 OQ SSOT). 알고리즘 튜닝 숫자·qmd 통합 형태(subprocess vs MCP)·클래스/파일 경로 발명 금지(OQ 박제). 파킹 항목(프롬프트 재분할·pgvector 해제) 무접촉. **status stable 유지**(계약 표면 개정·스코프 불변). validator errors 0/warnings 0(--strict). PLAN-013-T-001.
- 2026-07-11 doc-library-summaries-db-source. 요약 열람 **서빙 소스를 DB로 정정**(사용자 2026-07-11·T-004 파일시스템 방식 폐기·코드 무접촉). T-004는 요약을 `summaries/` 서브트리 파일시스템 스캔으로 계약했으나, 서빙 소스를 **DB 요약 원본**(`sources.summary_payload` active 버전, SoT=SPEC-003 §7)으로 정정 — 백업 md 파일(`data/documents/summaries/{stem}.md`)은 이 표면이 읽지 않음(아무도 안 읽는 side-output 그대로). **효과: "확정 문서=DB, 요약=DB"로 파일시스템 예외 없이 단일화**. **SPEC-013 정정**: ① API 2계약 구조(목록+본문·`markdown_full` FE 재사용) 유지하되 식별자·응답을 DB 기반으로 정합 — `GET /summaries`→`{items:[{source_id,name,path}]}`(active 요약 가진 source만), `GET /summaries/{source_id}`→`{source_id,name,path,markdown_full}`(source의 active 요약 md 렌더). path=표시용 파생(실파일 아님). ② **파일시스템 예외 서술 전면 제거**(intro/Scope/API 성격/Validation/Impl), 트리 표기만 `summaries/` 브랜치 유지. ③ **Case Matrix 정리**: `PATH_NOT_ALLOWED`(경로 탈출) **삭제**(파일 경로 입력 없음→케이스 소멸), `SUMMARY_NOT_FOUND`(404)는 "대상 source/active 요약 없음"으로 유지. ④ S-4/Flow(요약 소스=DB active)/Data Contract(얇은 읽기 전용 view, active만 읽어 `source_summary_revisions` 히스토리 무영향)/AC 정합. 요약 저장 모델·side-output md SoT는 SPEC-003 §7 참조만(재서술 금지). **DEC amendment 불요**(DB 뷰 조회는 구현 경계 선택, ADR 변경 아님)·**status stable 유지**(정정은 계약 표면 명확화, 스코프 불변). validator errors 0/warnings 0(--strict). PLAN-012-T-005.
- 2026-07-11 doc-library-summaries-join. 문서 라이브러리에 **요약(summaries/) 트리 합류** 확정 반영(사용자 2026-07-11·확정 내용만·코드 무접촉·스펙 선행). **SPEC-013 개정**: ① Scope in에 요약 합류 추가(열람 경계=라이브러리 경계 staff·admin 그대로)·검색/역링크/삭제 등 out 유지. ② **신규 읽기 전용 BE API 2종 계약 확정 정의**(documents API 스타일) — `GET /summaries`(요약 목록, 응답 `{items:[{name,path}]}`, `path`=`summaries/<file>`)·`GET /summaries/{name}`(본문 read-through, 응답 `{name,path,markdown_full}`, 필드명 확정 문서와 일치=FE 렌더 자산 재사용). ③ **파일시스템 예외 박제**: 요약 md는 `documents` row 없는 side-output(SoT=SPEC-003 §7/SPEC-005)이라 DB 열거 불가 → 이 2종에 한해 `summaries/` 서브트리 파일시스템 기반(확정 문서 트리의 "파일시스템 스캔 아님" 원칙의 명시적 예외), 조회 범위 summaries/ 하위 한정(탈출 불가), 그래프/인덱스/documents lifecycle 무영향. ④ 종전 "summaries는 documents row 없어 자연 제외"→"documents 트리에선 제외, 별도 summaries 계약으로 합류"로 Validation·S-1·Scope 정합, S-4(요약 열람 시나리오)·UX(트리 summaries/ 브랜치·ASCII·U-1)·Flow(alt 분기)·Data Contract(얇은 읽기 전용 resource) 추가. ⑤ **Case Matrix**: 신규 `SUMMARY_NOT_FOUND`(404)·서브트리 탈출은 기존 `PATH_NOT_ALLOWED`(400, SPEC-005 §4) 재사용·요약 목록 조회 실패. ⑥ AC에 요약 합류·경로 거부 케이스 추가. 요약 SoT/보관 성격은 SPEC-003/005 참조만(재서술 금지), links.specs에 SPEC-003 추가. **DEC amendment 불요**(기존 문서 모델 위 읽기 뷰 + summaries 서브트리 FS 예외는 SPEC-013 구현 경계 선택, ADR 승격 근거 없음). **status stable 유지**(합류는 확정 스코프, 잔여 OQ=쓰기 조작·superseded는 비차단). validator errors 0/warnings 0(--strict). PLAN-012-T-004.
- 2026-07-11 doc-library-access-boundary. 문서 라이브러리 접근 경계 **확정 정합**(사용자 2026-07-11·확정 내용만·코드 무접촉). 확정: **문서 라이브러리(`/documents`) = staff·admin 모두 열람 허용**(읽기 전용, staff의 `/graph` 문서 열람과 동급). ① **SPEC-008 §4 접근 경계 매트릭스(SSOT)**에 `문서 라이브러리(/documents, 읽기 전용 열람) | AXKG-SPEC-013 | ✅ | ✅` 행 추가 + dated bullet(2026-07-11, 쓰기 조작 없음·페이지 계약 SPEC-013 소유), links.specs에 SPEC-013 추가. ② **DEC-006 §2** 접근 경계 staff 열거에 `/documents` 읽기 전용 추가(2026-07-11 dated 확장, 매트릭스 세부는 SPEC-008 §4·페이지 계약은 SPEC-013 참조 — 매트릭스 중복 금지), links.specs에 SPEC-013 추가. ③ **SPEC-013 §7 OQ 1(staff 접근) 해소**(취소선+확정 요지), §1 Meta·§3 intro·§4 API Contract(권한 컬럼 `staff·admin`, as-is bullet 정합)·§5 Impl의 "확정 전/행 없음" 서술을 확정 상태로 정합(경계 서술은 SPEC-008 참조 유지). ④ **SPEC-013 status draft→stable 승격**(핵심 계약 확정·FE 발주 ready, 잔여 OQ=쓰기 조작 후속·superseded 열람은 out-of-scope 위임이라 비차단), tags status/draft→stable, 20-spec README status 갱신. 잔여 OQ(쓰기 조작 후속 라운드·superseded 열람) open 유지. 타 페이지 경계 무변경. validator errors 0/warnings 0(--strict). PLAN-012-T-003.
- 2026-07-11 spec-add-document-library. 문서 라이브러리 라운드(PLAN-012) **스펙 확정**(사용자 승인 2026-07-11·확정 내용만·발명 금지·코드 무접촉). **신규 AXKG-SPEC-013(문서 라이브러리: 저장 문서 트리 열람, status draft)** 생성 — 저장 문서 열람 페이지의 owning spec 부재를 채운다. 계약: ① **읽기 전용** `/documents` 페이지(데스크탑 좌 트리+우 본문, 모바일 트리↔본문 전환형), ② 트리 = DB `documents`의 `path` split(파일시스템 스캔 아님)·`current`만(목록 API 기본, `superseded` 제외·`summaries/`는 row 없어 자연 제외), ③ 본문 = 기존 `GET /documents/{id}` `markdown_full` read-through 재사용, **BE 신규 API 없음**(FE 표면만). 경로 컨벤션은 AXKG-SPEC-005 §4, lifecycle current/superseded는 AXKG-SPEC-004를 **참조만**(재서술 금지). §7 OQ **박제**: (1) staff의 라이브러리 접근 허용 여부 미논의(경계 SSOT=SPEC-008 §4에 이 페이지 행 없음, as-is BE authz는 로그인만이나 내비/매트릭스 노출은 제품 결정 — FE 발주 전 확정 필요), (2) 새 폴더 생성(원 요구 ②)·이동/이름변경/삭제 등 쓰기 조작 일체 이번 라운드 제외(경로 컨벤션·apply_executor allowlist 공존 설계 라운드 별도), (3) superseded 열람 후속. 링크: decisions=DEC-005·006, specs=SPEC-004·005·008. SPEC-004:435의 4탭 문서함(승인 워크스페이스) owning-spec 갭은 **별 표면**이라 미변경(SPEC-013은 읽기 전용 라이브러리 소유). 20-spec README 행/읽는 순서 추가. 컴포넌트명·파일경로·렌더 라이브러리 등 구현 발명 금지(FE work 소관). validator errors 0/warnings 0(--strict). PLAN-012-T-001.
- 2026-07-10 wp6-done. WORK-007(WP6 로그인·유저·역할 권한) **코드 완료 기준 close**(status todo→done, progress 0→100). 근거 = BE PLAN-010-T-003(BE-1~4)·FE PLAN-010-T-004(FE-5~7)·admin 라이브 검증. **BE**: `users.role`(admin/staff)·`is_active` 마이그레이션 **alembic 0019**(down 0018, CHECK `ck_users_role`, 활성 22명 로스터 email 멱등 upsert 전담, 기존 seed=admin 흡수·비번 보존, 실 PG 라운드트립 검증·재upgrade 멱등·0015 role-free 분리로 회귀 방지) → `require_admin` authz(admin 전용 `/sources`·`/gates`·`/documentation-gates`·`/settings`·`/prompts`·`/templates`·`/users`, 로그인만 `/graph/*`·`/documents`, Slack signing secret 유지) + `FORBIDDEN`(403)·`INACTIVE_ACCOUNT`(401)·응답 `user.role` → 유저 관리 API `/users`(목록·생성[기본비번 1234·409 EMAIL_EXISTS·422 role]·`PATCH /role`·`PATCH /active`, admin 전용) → 본인 비번 변경 `POST /auth/password`(강제 아님). pytest **356 passed**(신규 25 `test_authz_roles.py`, 회귀 0). **FE**: `lib/access.ts`(SPEC-008 §4 매트릭스 FE 단일 원천·staff 허용 `/graph`·`/account`)·app-shell role 내비 필터·auth-guard 리다이렉트·`defaultLanding`, admin 유저 관리 **`/users`**(설정 톤 차용)·본인 계정 **`/account`**(비번 변경), Case Matrix(`FORBIDDEN`·`INACTIVE_ACCOUNT`) 표면. tsc/build 통과·BE 실물 계약 대조(비번 경로 `/auth/change-password`→`/auth/password` 교정). **admin 라이브 검증(2026-07-10)**: dev api 재빌드+0019 적용, 22명(admin 3: kknaks·dante·sykim) 시드, kknaks role=admin/staff 계정 role=staff 실측. FE 실물 표면(`/users`·`/account`)은 SPEC-008 §4 매트릭스와 정합(staff=그래프+본인 계정, /users admin 전용) — **스펙 무변경**. **21-html 시안 부재 blocker 해소**(설정 톤 차용·사용자 결정). 30-work README WP Map/Status Board/Spec Coverage(SPEC-008 → done) 정합. **OQ 유지**: admin의 타 유저 비번 리셋(SPEC-008 §7). 커밋/push 미수행(사용자 일괄). validator warnings/errors 0(--strict). PLAN-010-T-005.
- 2026-07-10 wp6-work-add. 로그인/유저/권한 구현 **WORK 신설**(PLAN-010-T-002, SPEC-008 확장·DEC-006 기준·코드 무접촉·구현 발명 없음). **AXKG-WORK-007 (WP6: 로그인·유저·역할 권한)** 생성 — 다음 WORK 시퀀스 번호, status todo, 선행 WP0. 범위는 SPEC-008+DEC-006에서만 도출: `users.role`(admin/staff)·`is_active` 마이그레이션 + 활성 22명 시드(email 멱등), 라우트 authz(게이트·소스·설정·유저 관리=admin / 그래프·채팅=로그인만 / 본인 계정=본인), 유저 관리 API·화면(목록·생성·역할 변경·활성 토글, admin 전용, 기본비번 `1234`), 본인 비밀번호 변경(강제 아님), role 분기 내비/가드, `FORBIDDEN`·`INACTIVE_ACCOUNT` 처리. **태스크=커밋 단위 7 Phase**(BE-1~4 / FE-5~7, 트랙 병렬 발주 가능·계약=SPEC-008 `role`/Case Matrix). 수용 기준은 SPEC-008 §6 AC·§4 Case Matrix 참조(본문 재서술 금지). 구현 세부(클래스·시그니처·파일경로)는 발명 안 함 — 산출물 경계·태스크 이름 수준까지만. **scope out**: admin 타 유저 비번 리셋(SPEC-008 §7 OQ)·강제 변경(DEC-006 미채택). **Progress Checklist + Change Log 섹션 포함**(task 명시 요구 — 워커 태스크별 SoT). 30-work README WP Map/Status Board/Spec Coverage 등재(SPEC-008 커버리지 = WP0 골격 done + WP6 role/authz todo → in-progress). **주의**: 유저 관리·비밀번호 변경 화면 `21-html/` 시안 부재 = Open Issue(FE 착수 전 design 결정). validator warnings/errors 0(--strict). PLAN-010-T-002.
- 2026-07-10 role-authz-access-boundary. 로그인/유저/권한 라운드(PLAN-010) **스펙 확정**(사용자 승인 2026-07-10·확정 내용만·발명 금지·코드 무접촉). **SPEC-008 확장**(신규 SPEC 분리 안 함·같은 auth/user 도메인): 제목 "간단한 토큰 로그인"→"토큰 로그인·유저·역할 권한 경계", **제품 전체 접근 경계 매트릭스 SSOT를 SPEC-008이 소유**. ① **role 2값 `admin`/`staff`**, `users`에 `role`+`is_active`(비활성=로그인 차단·admin 토글). ② **접근 경계 매트릭스**: staff=`/graph`(그래프+채팅④)+본인 계정(me/비밀번호 변경)만, admin=전부(소스 inbox/수집·분류②·문서화③ 게이트·설정·유저 관리), 게이트 승인 admin만(staff는 표면 자체 접근 불가), **FE 가드+BE 라우트 authz 이중 강제**. ③ 유저 생성=admin 전용(공개 가입 없음)·기본 비밀번호 `1234`. ④ 비밀번호 변경=본인 자율, **최초 로그인 강제 변경 미채택**(사용자 명시 선택 dated 박제). ⑤ 역할 변경=admin 전용. ⑥ **시드=활성 22명 로스터**(admin 3: 이건학·전창원·김수연 + staff 19) email 멱등·비활성 4명 제외·department/position 미저장·기존 seed 흡수, 로스터 전문 SPEC-008 수록. Scope in/out 갱신(role/permission·비밀번호 변경·유저 관리 in / 공개 가입·강제 변경·refresh·MFA out), Case Matrix `INACTIVE_ACCOUNT`·`FORBIDDEN` 추가, `/auth/me`·login response에 `role`. §7 OQ **박제**: admin의 타 유저 비밀번호 리셋(`1234` 재초기화) 제공 여부 미논의. 유저 관리 엔드포인트 경로/shape·마이그레이션은 후속 BE work 소관(발명 안 함). **신규 DEC-006**(역할 권한 모델·접근 경계, accepted) 생성 — 강제 변경 미채택 근거 dated 박제, DEC index 등재. **타 스펙 참조 정합(매트릭스 중복 금지)**: SPEC-002·004(게이트 admin 전용)·007·009·010(설정 admin 전용)·003(소스 inbox admin 전용)에 "admin 전용·경계 SSOT=SPEC-008" blockquote 1줄씩만 추가(권한 컬럼 스윕 없음). 20-spec README SPEC-008 제목/DEC 갱신. validator warnings/errors 0(--strict). PLAN-010-T-001.
- 2026-07-10 e7-triage-trigger. E-7(stale 후보 AI 사전 심사) 재논의 확정 반영(사용자 승인·확정 내용만·발명 금지·E-7 구현 서술 추가 안 함). **구현 착수 안 함·조건부 OQ 유지**하되 종전 흐릿한 조건("stale 대량 누적이 실증되면")을 **구체 트리거로 교체**. 재논의 실측(2026-07-10): stale 누적 이력 총 1건(dismissed)·미처리 0·permanent 1개 = 과잉 배지 구조적 부재 규모, 심사 1건 입력(permanent 전문+변경 요지)이 재생성과 유사 연산이라 소량 국면 심사비용>효용(역마진). **도입 재검토 트리거(둘 중 하나)**: ① 정량=미처리 stale 상시 10건 이상, ② 정성=재검토 탭 "열어보니 무영향 dismiss"가 처리량 다수. **도입 시 순서**: AI triage 직행 대신 lexical 사전 필터(변경 요지 키워드↔permanent 본문 인용 기계 대조)를 1차, AI 심사 후순위. **SPEC-004 §7** E-7 OQ 개정(취소선 아님·open 유지+조건 구체화, 기준 상세 SSOT). **DEC-005** E-7 앵커는 참조로 정합(기준 상세는 SPEC-004 §7 한 곳, 중복 방지). 경로 컨벤션·Layer Taxonomy 불변. validator warnings/errors 0. PLAN-009-T-043.
- 2026-07-10 improve-oq3-resolved. 2026-07-10 개선 라운드에서 **코드로 해소된 개선 OQ 3건**(T-038/039/040, 전부 admin 게이트+라이브 검증)을 스펙에 환류(스펙 문서만·목록 밖 첨가 금지·커밋 admin/사용자·work-004는 done 유지로 미변경). **① 경로 시스템 강제=경로 결정 주체 이관**(T-040, SPEC-004 §7 OQ 해소): 문서화③ 경로 **디렉토리 결정을 AI→시스템 이관**. AI `output_schema`(`documentation_gate` v3)는 파일명/stem만 — `document_draft`=`filename_candidate`, `derived_suggestions[]`=create류 `filename_candidate`/supplement `target_stem`(if/then 강제, `target_path` 제거). 디렉토리 조립·최종 `target_path`는 Phase 2 `wrap_documentation_output` 빌더(main=prior current main 재사용, 파생 create=suggestion_type 디렉토리, 파생 modify=resolver로 target_stem→기존 경로), 매핑 SSOT=공용 `services/document_paths.py`. executor `PATH_NOT_ALLOWED`=안전망 유지, envelope `target_path`는 시스템이 채워 **FE 계약 무변경**·구형 payload 하위호환. 라이브 실측(Knowledge graph 소스→main `resources/…`+파생 concept 2건 `permanent/concepts/` 조립, 승인 1트라이·교정 0). **SPEC-004** §4 경로 결정 주체 서브섹션 신설·Data Contract(`filename_candidate`/`target_stem`)·Apply Matrix intro 주·§5 규칙·§7 OQ 해소. **② 파서 wikilink 코드 영역 제외**(T-038, SPEC-005 §7 OQ 해소): `extract_wikilinks`가 코드펜스(3개↑, 미닫힘=문서끝)·인라인 코드스팬(같은 길이 backtick run 쌍, CommonMark 최소) 내부 `[[ ]]` 파싱 제외 → `BROKEN_WIKILINK` 오탐 해소, 판정 3곳(엣지 빌드·preview·executor) 자동 반영·링크 정규화 불변. **SPEC-005** Link Syntax Contract 코드 영역 규칙+Path Convention 시스템 조립 각주+§5 규칙+§7 OQ 해소. **③ 형제 reviewable supersede sweep**(T-039, SPEC-002 §7 OQ 해소): "게이트당 최신 하나만 reviewable" 강제 — 새 revision reviewable 전이 직전(분류·문서화 공통) 같은 게이트 다른 모든 reviewable→superseded sweep(종전=직전 parent 단건), 승인 확정 시점(분류 approve·문서화 apply)에도 동일 sweep=안전망, drafting 미터치. **SPEC-002** §5 sweep 규칙+§7 OQ 해소+documentation.v1 output_schema 경로 필드(T-040: `filename_candidate`/`target_stem`·`target_path`=system) 정합(동반: apply_plan `file_action` T-018 이전 잔재 `patch_markdown`→`overwrite_markdown` 정합, SSOT SPEC-004 §5). **DEC-005** dated amendment(3건 해소+경로 결정 주체 이관 in-place 앵커=output_schema 경계 연장). 경로 컨벤션 표(디렉토리 매핑)·Layer Taxonomy 불변, 코드 세부 발명 금지. validator warnings/errors 0. PLAN-009-T-042.
- 2026-07-10 doc3-round-close. 문서화③ 라운드 마감 정합 — **2026-07-10 풀 E2E 완주**(4소스, 요약→분류→문서화→supplement→stale→재생성→해제 전 구간 라이브)로 DEC-005 **D·E 구현 완료**를 스펙 실물 정합(스펙 문서만·목록 밖 첨가 금지·커밋 admin/사용자). **① D/E done 전이**(SPEC-004): **D(파생 concept 버전 lifecycle)** 서브섹션·§7 OQ "구현 후속 WP"→**구현 완료**(PLAN-009-T-027 + 라이브 concept v1→2): create=v1/supplement=v++/`producing_revision_id`·`source_id` 스탬프, main 계보 supersede 판단에서 concept 제외. **E(stale 연쇄)** 서브섹션·§7 OQ→**구현 완료**(BE T-030/FE T-031·032·033 + 라이브): E-1~6 계약 그대로, 저장=`document_stale_marks`(alembic 0018), API 3종(`GET /documents/stale`·dismiss·regenerate=producing source 문서화 게이트 재문서화 재사용), FE stale=문서함 `재검토` 탭(좌 목록+우 상세·`GET /documents/{id}` markdown_full) 현황 서술. E-7 triage OQ 잔존. **② Case Matrix 3종 등재**(SPEC-004): `SUPPLEMENT_TARGET_NOT_CONCEPT`(422, supplement 대상 concept만 — reference v2 오염 실측·T-036)·`STALE_REGENERATION_NOT_ALLOWED`(409)·`DOCUMENT_NOT_FOUND`(404, T-030). **③ 실행 옵션 계약**(SPEC-007): 문서화(`documentation_gate`) 태스크 seed 기본값 `max_turns=12`(`error_max_turns` 실측)·`timeout_sec=600`(stale 3입력+연결 후보 전문 300s 초과 실측), T-035 시드. **④ 개선 OQ 신규 등재(해소 아님·관찰 실측)**: 경로 시스템 강제(target_path 디렉토리 오생성→빌더/executor 결정·AI 파일명만, SPEC-004 §7)·파서 wikilink 오탐(코드스팬/펜스 내 `[[ ]]` 파싱 제외, SPEC-005 §7)·형제 reviewable revision dangling(승인 시 형제 supersede, SPEC-002 §7). **DEC-005** 앵커 dated 보강(2026-07-10 amendment)+Rationale/OQ D/E done 전이. open_kknaks result subtype 유실은 **제품 밖 별건**(제품 스펙 미등재). Layer Taxonomy·경로 컨벤션 표 불변. validator warnings/errors 0. PLAN-009-T-037.
- 2026-07-09 concept-template-and-supplement. 라운드 A 라이브 실측 확정 2건 스펙 정합(스펙 문서만·E 서술 불변·D는 done 선언 안 함). **① concept 템플릿 신설(T-023 C항 개정)**: 템플릿 4종째 `concept` 추가 — concept는 고정 골격(정의/맥락/근거 출처)을 가진 문서 타입이라 Layer Taxonomy상 뼈대는 템플릿 소유가 맞음. 파생이라 destination 매핑 없어 main 3종(destination 선택)과 달리 **문서화③ 조립 시 고정 동봉** 주입. 종전 "concept는 템플릿 대상 아님·형식은 프롬프트 소관"(T-023 C) 개정(배선 부재 타협 해소), 채우는 작성 방법(how)은 여전히 프롬프트 소관. **② supplement 자발 제안**: 전문 주입된 기존 concept에 출처가 새 정보를 담으면 AI가 `supplement_existing_concept` **자발 제안**(링크만 걸고 넘어가지 않음) — 라운드 A 실측: 유도 피드백 없이는 supplement 미산출. **SPEC-010** 경계 note(문서화③ 전용에 concept 추가·주입 방식 구분 blockquote)·MVP scope 3종→4종·U-1 Template List 키·Validation·Data Contract 키·Impl Rules(키 파생·T-023 concept 배제 bullet 개정)·AC 개정. **SPEC-004** Impl Rules에 supplement 자발 제안 규칙(계약 한 줄) 추가. **DEC-005** T-023 amendment C항에 개정 취소선 + 라운드 B dated 보강(concept 템플릿·supplement 자발 제안·D as-is/to-be 유지·E 불변). 경로 컨벤션·Layer Taxonomy·E(stale) 서술 불변, 클래스·컬럼 발명 금지. validator warnings/errors 0. PLAN-009-T-028.
- 2026-07-09 stale-cascade-detail. T-023 지식 아키텍처(DEC-005 A~E) 중 **E(stale 연쇄) 동작 계약 상세 + B 범위 정정**을 후속 논의 확정분으로 반영(스펙 문서만·구현 여전히 후속 WP·경로/Layer Taxonomy/타 스펙 불변). **B 정정**: "개념 상세를 재서술하지 않는다"=**상세 설명 섹션 복사 금지**의 뜻, permanent 판단 문장에 개념 요지가 인용되는 것은 **허용·필연**(예: "성숙도 4단계 중 2단계이므로 확산 집중"). concept 개정 시 permanent 판단이 낡을 수 있음 = E의 실질 근거. "SoT 위임이 오염을 구조적으로 0으로 만든다"는 서술 **금지**(표면적을 줄일 뿐 0 아님) — 금지 규율로 명문화. **E 동작 계약(E-1~6)**: ① stale 배지=**"영향 가능성 있음"**(참조 기반 과잉 포함), "수정 필요" 판단 아님(시스템은 수정 필요 판단 안 함). ② 감지=concept 새 버전 승인(문서화 게이트 supplement 승인) 시 `document_edges` backlink 쿼리(**AI 없음**), 배지만 부착+concept **변경 요지 동봉**(게이트 payload 재사용). ③ 재생성=**문서당 독립 태스크**(1 프롬프트=1 문서, 입력=대상 permanent 전문+바뀐 concept 전문+변경 요지), 일괄 판단 없음. ④ **1 승인=1 문서**(version++), 일괄 승인/실행 없음. ⑤ **부분 처리 허용**(트랜잭션 아님) — 미처리 stale은 배지 유지, concept v2는 이미 확정 적용됨. ⑥ 출력 규율=옛 전제 의존 판단만 수정·미지적 판단 보존(피드백 v2 규율 계열)·암묵 전제도 탐지·최종 방어는 사용자 게이트. **E-7 triage OQ**: AI 사전 심사(영향 유/무 판정)는 **v1 제외**, 대량 누적 실증 시 도입 검토. **SPEC-004** §4 SoT 위임에 B 범위 명확화 문단, Document Lifecycle E 서브섹션 intro "갱신 필요"→"영향 가능성"+E-1~6 계약, §7 OQ에 E-7 triage+E OQ 문구 갱신. **DEC-005** T-023 amendment에 후속 논의 dated 보강(B 정정+E 동작 계약 요지+E-7 OQ). 확정 목록 밖·UI 문안·컬럼/클래스명 발명 금지. validator warnings/errors 0. PLAN-009-T-024.
- 2026-07-09 knowledge-architecture. **지식 아키텍처(4층·SoT 위임·concept 버전/연쇄) 스펙 반영**(사용자 확정 2026-07-09, 스펙 문서만·경로 컨벤션 변경 없음·D/E 코드는 후속 WP). 지식이 `출처 → 원자 개념 → 종합/전략 → 실행` 방향으로 자라는 모델을 확정. **A 4층 문서 정체성**: 출처 기록(reference←resource, `resources/`) / 원자 개념(concept, `permanent/concepts/`, **사실의 SoT**) / 종합 노트(permanent←area, `permanent/`) / 실행 문서(baseline←project, `projects/`) — permanent 안 두 층(concepts/=원자·루트=종합)은 위계 유지·합치지 않음, 요약 md는 그래프 무관 side-output(재확인). **B SoT 위임(중복 서술 금지)**: 개념 상세 SoT=concept 한 곳, reference는 요지+`[[concept]]` 위임("출처가 개념 인용"), permanent 종합 노트도 개념 재서술 없이 `[[concept]]` 참조·내 판단만 소유, 두 번째 출처는 새 concept 아니라 기존 concept **보충**(`supplement_existing_concept`)으로 합류=성장 메커니즘. **C 템플릿 재배치**: 현행 permanent 템플릿의 "한 줄 주장/맥락/내 결론" 골격=concept 모양 → concept 작성 규칙(가이드+프롬프트)으로 이사(concept는 파생이라 템플릿 주입 대상 아님), permanent 템플릿=종합 노트 골격(영역 주제/종합·판단/구성 개념 `[[concept]]`/열린 질문)으로 재정의(세부 문안은 BE 소관). **D concept 버전 확장**(방향 확정·구현 후속 WP): 기존 버전 모델(md=현재본 하나/DB immutable revision/`documents.version`++)을 파생 concept에도 확장 — 현행 파생은 overwrite만 되는 갭(as-is), 파일명 버전(`concept_v1.md`) 불채택(stem 불변+DB 버전, `[[ ]]` 링크 안정), 그래프는 current만. **E concept→permanent stale 연쇄**(신규·구현 후속 WP): concept 새 버전 승인 시 backlink(`document_edges`)로 참조 permanent를 "구성 개념 갱신됨" stale 표시, 자동 재작성 없이 **재생성 게이트**(사용자 경유)로만 갱신. **SPEC-004** §4에 "지식 아키텍처와 SoT 위임(4층)" 서브섹션 신설(A·B)+Document Lifecycle에 파생 concept 버전 확장(D)·stale 연쇄(E) 서브섹션+OQ 2건. **SPEC-010** Impl에 permanent=종합 노트 골격·concept는 템플릿 대상 아님(C)+AC 2건. **DEC-005** dated amendment(A~E in-place 앵커)+Rationale+OQ. 경로 컨벤션 표(SPEC-005) 변경 없음. 확정 목록 밖·코드 디테일(컬럼·apply action·stale 플래그) 발명 금지, D/E 구현은 후속 WP. validator warnings/errors 0. PLAN-009-T-023.
- 2026-07-09 spec011-guide-status-fix. SPEC-011 §4 "스테이지별 가이드 자산 현황(as-is)"·§7 OQ의 stale 서술 1건 정정(소폭). "**문서화③은 전용 가이드 파일이 없고** 정의/규칙이 SPEC-004/005·`document-link-rules.md`에 흩어져 있다"는 서술이 실물과 어긋남 — **2026-07-09 PLAN-009-T-010에서 `context/documentation-guide.md` 이미 신설**(ax-graph 코드레포 grounding 확인: 정의/규칙 소유, 링크 문법은 `document-link-rules.md` SSOT 참조, 이후 T-016/T-020 갱신). §4 현황을 "4스테이지 모두 전용 가이드 파일 존재"(③=`documentation-guide.md`)로 갱신, §7 OQ를 신설 완료로 해소(취소선+T-010 참조). 40-arch README:53의 `document-link-rules.md` 참조는 stale 서술 아님(파일 목록)·자율 첨가 금지 원칙에 따라 미변경. Layer Taxonomy 본문·타 OQ 불변. validator warnings/errors 0. PLAN-009-T-022.
- 2026-07-09 doc-gate3-confirmed-model. 문서화 게이트③ **확정 모델 6개를 스펙 정합**(사용자 확정 2026-07-09, 스펙 문서만·코드는 BE T-016/FE T-017 병렬). ① **파생 본문 계약**: `derived_suggestions[].draft_markdown` **전 suggestion_type 공통 필수**(종전 전량 `no_draft_markdown` 스킵→파생 문서 미생성 갭 해소). ② **A1 modify 모델**: diff/patch 엔진 없음 — 빌더가 retriever 상위 후보 **전문**(per-doc cap) 주입, AI가 **수정 전문**을 draft_markdown으로 출력, executor **overwrite**. `diff_preview`는 리뷰 표시용 요지(적용 입력 아님). ③ **경로 컨벤션**(executor `PATH_NOT_ALLOWED` 강제): reference→`resources/`·permanent(area)→`permanent/`·baseline(project)→`projects/`·create_new_concept→`permanent/concepts/`·create_project_baseline→`projects/`·supplement(modify)→기존 경로. 종전 `products/ax-knowledge-graph/00-baseline`(product 메타문서 트리 혼동)을 `projects/`(지식 볼트)로 정정. ④ **frontmatter `id` 강등** 필수→선택(resolve `stem→alias→id` 유지). ⑤ **템플릿 frontmatter 정합**: `source_url`→`source`·aliases 추가·`up` list·`created_at` 제거(SPEC-005 Required Frontmatter 준수). ⑥ **executor 검증 확장**: 파생 draft_markdown wikilink도 `BROKEN_WIKILINK`/`UP_WITHOUT_BODY_LINK` 검증, `ApplyResult.skipped` apply_plans 박제(관측성). **SPEC-004** Data Contract(draft_markdown 필수·diff_preview 표시용·ApplyPlan.skipped)·Apply Matrix(modify=overwrite_markdown·경로)·예시·Case Matrix(PATH_NOT_ALLOWED·BROKEN_WIKILINK)·Validation·Impl Rules(file_actions create/overwrite·overwrite 모델·executor 거부·파생 필수)·AC. **SPEC-005** Required Frontmatter(id 선택)·**Path Convention 표 신설(SSOT)**·Case Matrix(PATH_NOT_ALLOWED)·Impl(resolve 우선순위·파생 링크 검증). **SPEC-010** 템플릿 frontmatter를 SPEC-005 계약에 정합(id 없음·source·aliases·up list·created_at 없음)·AC. **SPEC-011** Connection Candidate Context/S-2/Impl에 modify용 후보 전문 주입 추가. **DEC-005** 6개 항 dated amendment 앵커(in-place). validator warnings/errors 0. 코드 미착수·발명 금지(클래스·시그니처 X). PLAN-009-T-018.
- 2026-07-09 summary-doc-oq-resolved. T-013이 등재한 **요약 문서 OQ 3건 + T-012 코드 확정 OQ를 결정으로 승격**(사용자 확정, 스펙 문서만). **요약 문서(md) 확정 사항**: (1) 저장 위치 = `data/documents/summaries/{stem}.md`, (2) 성격 = **보관용(archival) side-output** — 그래프 노드 아님·인덱스/retriever/`/graph/documents` 미편입·요약→PARA lineage 없음·downstream은 DB `summary_payload`/active revision을 읽음, (3) 재확정 = 현재 active 버전으로 **overwrite**(파일 supersede 안 함), 히스토리는 DB 박제. **T-012 코드 확정 반영**: 요약 버전 저장 = 별도 테이블 **`source_summary_revisions`**(summary_payload=active 포인터), 문서 lifecycle supersede action = **`supersede_document`**·컬럼 `documents.status`(current/superseded)/`version`/`producing_revision_id`/`source_id`, **frontmatter lifecycle 스탬프 미적용**(`.md`는 순수 본문, lifecycle은 DB에만). **SPEC-003** §7 4개 OQ→결정(테이블·md 위치·노드 아님·overwrite)+본문 3곳 미결참조→결정 사실. **SPEC-011** §4 B·Impl·§7 OQ 결정 승격. **SPEC-004** Document Lifecycle "요약 문서 동형" 과잉주장 교정(공통은 draft→확정md 저장 패턴뿐, documents lifecycle은 PARA 전용·요약은 side-output overwrite)+§7 OQ 2건 결정(supersede_document·컬럼·frontmatter 미스탬프·요약 문서 비적용). **SPEC-005** Impl(요약 문서=노드 아님·frontmatter 스탬프 미적용)+§7 OQ 결정. 문서≠게이트≠문서화 용어 구분 유지(T-013). 코드 미착수·발명 금지. validator warnings/errors 0, 요약문서 미결 잔재 0. PLAN-009-T-015.
- 2026-07-09 two-md-creation-points. **md 문서 생성 지점 2곳 확정**(사용자 최종, 스펙 문서만). 종전 "요약=DB only, md는 문서화③ apply에서만"을 **폐기·정정** → md는 (1) **요약 확정**([분류] 누름 = 요약 draft의 active 버전이 요약 문서 md로 확정)과 (2) **문서화 게이트 승인**(PARA 지식 문서 md) 두 곳에서 생성. **저장 모델 각 단계 공통 = `draft(DB 박제 v1/v2, 피드백 히스토리) → 확정(md, 현재 최종본 하나)`**. **★용어 엄격 구분**: 문서(md 산출물) ≠ 게이트(승인 단계) ≠ 문서화(PARA 지식 문서 단계) — "문서화 게이트만 md 생성"은 틀림(요약 확정도 md), md↔문서화 게이트 등치 금지. 분류 게이트②는 destination metadata만·md 안 만듦. **SPEC-003** U-2/S-1(step8·9)/State note/Impl/AC에 [분류] 확정→요약 md 반영, "확정 md는 문서화 후"(요약=DB only) 폐기, DEC-005 링크 추가, OQ 3건(요약 md 저장 위치·그래프 편입/lineage·재확정 버전 갱신). **SPEC-011** §4 B(draft→확정md 공통·md 2곳·요약 템플릿 없어도 md 산출) 정정·Stage① 소비처·S-1 step6·Resume Wiring·Impl·OQ. **SPEC-004** Document Lifecycle에 **요약 문서 동형 적용** 문단+용어주의 blockquote+OQ. **SPEC-001** U-2/U-3/U-3.1/Impl/AC에 분류 게이트=metadata·md 안 만듦·요약 md는 [분류]서 이미 생성. **SPEC-002** 요약 초안 [분류]=확정 지점(md 산출)·md는 확정 행위서 산출·분류 승인은 md 지점 아님·"DB 임시"→"DB 박제". **SPEC-005** PARA 문서=그래프 노드 확정·요약 문서 노드 여부 OQ. **B(저장 직교)·C(draft 박제) 원칙 유지·요약으로 확장**. 코드 미착수, 발명 금지·미결 OQ. grep 잔재("md는 문서화 apply에서만"·"요약=DB only") 0. PLAN-009-T-013.
- 2026-07-09 summary-template-reverted. T-009가 반영한 **"요약①도 body_markdown 템플릿 보유"(결정 A)를 폐기·반전**(사용자 최종 확정, 스펙 문서만). 근거 = 실제 라이브 요약 출력이 **원문(출처)의 자체 구조를 따라가는 적응형**(강연=강연자소개→목차→주제, 기사=기사 구조)이라 고정 뼈대(템플릿)가 안 맞고 씌우면 현재 품질을 해침. **최종 SSOT: template 층 = 문서화③ 전용**(reference/permanent/project_baseline), 요약①·분류②·채팅④는 템플릿 없음. 요약 md 형식 규약(원문 구조 `##` 추종·인용 `>`·엔티티 볼드·불확실 "출처 미상" 정직 표기)은 **프롬프트(작성 방법) 소관**. **★유지(안 건드림)**: `body_markdown`은 요약 `output_schema` 필드로 유지(요약이 md 본문 출력함은 맞음, 템플릿만 없음) / 템플릿≠output_schema 구분 유지 / **B(저장 직교)·C(버전 박제)·D(문서 lifecycle)는 T-009 결과 그대로 유지**. **SPEC-011** §4 taxonomy 표(⑵ 템플릿=문서화③ 전용·요약=적응형), A1 폐기노트·출력양식 정정노트·경계테스트 행(요약 형식 규약→프롬프트 예시로)·Assembly(③ 템플릿)·Stage(① 입력서 템플릿 제거·출력 body_markdown 유지)·Validation·Flow mermaid(template load=documentation_gate)·Impl·AC(①②④는 prompt_version_id만) 반전. **SPEC-010** 템플릿 문서화③ 전용으로 되돌림(요약 body_markdown 템플릿 in-scope·요약 draft 스탬프 항목 제거, 요약 템플릿 key OQ 철회). **SPEC-009** Data Contract 예시 요약 output_schema에 **`body_markdown` 추가**(누락 정합). **DEC-005** A 결정·후속 spec 표·Rationale에 T-011 재정정 명시(B/C/D 유지). grep 잔재 0(요약=템플릿 서술 없음, body_markdown 필드 존재). 코드 미착수. PLAN-009-T-011.
- 2026-07-09 output-form-storage-version-lifecycle. T-007/008이 세운 3층 taxonomy의 **출력양식 층·저장·버전·문서 lifecycle 4개를 정합**(사용자 확정, 스펙 문서만). **A 출력양식 층 보편화(템플릿≠output_schema)**: `output_schema`=출력 JSON 필드 shape만(title/summary/keywords/body_markdown 등), **템플릿=md 뼈대 별도 아티팩트**(조립 시 프롬프트 주입, output_schema에 안 넣음), 템플릿 층을 **md 산출 스테이지(요약①·문서화③)에 보편화** — 종전 "요약도 output_schema뿐(A1)" 폐기·정정, **요약①도 `body_markdown` 템플릿 보유**. 분류②·채팅④는 md 없어 output_schema만. **B 저장 직교축**: draft=DB(JSONB)/확정=.md(markdown_root), 저장은 템플릿 유무·lifecycle과 무관(잘못된 결합 차단). **C 버전 박제**: 요약 draft `summary_payload` 재요약을 v2 덮어쓰기→**immutable 버전 체인**(v1 read-only 보존·v2 parent 참조, 게이트 revision과 동일). **D 문서 lifecycle**: 확정 `documents`에 `status`(current/superseded)+`version`+producing revision/source 링크, 재문서화 시 옛 문서 superseded 박제·새 문서 current, apply executor `write_new`→버전/supersede 확장(commit 경계 너머). **SPEC-011** §4 taxonomy 표를 output_schema(shape)/템플릿(md 뼈대) 2아티팩트로 분리·요약 템플릿 보편화·저장 직교축 명시, Assembly/Stage/Validation/Flow/Impl/AC에 ①③ 템플릿 조립·`template_version_id` 스냅샷 반영. **SPEC-010** 템플릿 층을 ①③ 공통으로 확장(요약 body_markdown 템플릿 in-scope·요약 draft는 frontmatter 대신 `ai_tasks.template_version_id` 스냅샷), key 편입 방식 OQ. **SPEC-003/002** 요약 draft 박제(비덮어쓰기·v1 보존) 명시, 저장 위치·구조 OQ. **SPEC-004** Document Lifecycle 서브섹션(status/version/producing 링크·supersede 확장)+Impl+OQ. **SPEC-005** superseded 문서 `/graph/documents` 기본 노출 제외·producing 링크. **DEC-005** in-place dated amendment(A~D Decision 항목+정합 후속 표+Rationale, 신규 DEC 안 만듦 — taxonomy는 DEC-005 소유·T-007 개정 연장). **코드/guide 미착수**(요약 템플릿 key·요약 버전 저장·문서 lifecycle 컬럼·supersede action 명칭은 OQ로 코드 소관). PLAN-009-T-009.
- 2026-07-09 taxonomy-label-fix. T-007이 프롬프트 층을 `프롬프트(스킬)` 병기 라벨 + "프롬프트=스킬(how to write)" 등식으로 서술한 **표현 과잉을 교정**(사용자 교정). **층 이름 = `프롬프트` 단독**, "스킬"은 성격 비유로만(라벨/등식 아님). SPEC-011 §4 Layer Taxonomy 표 헤더 `프롬프트(스킬)`→`프롬프트`, 경계 테스트 "규칙 vs 스킬 vs 양식"→"규칙 vs 작성 방법 vs 양식", 이전 모델 "프롬프트=스킬(how to write)"→"프롬프트=작성 방법(how to write)", Assembly Contract·S-1 step3·Stage 표·Impl Rules·§7 OQ의 단독 명사 "스킬"→"작성 방법(how)"으로 스윕. SPEC-009 "프롬프트의 정체=스킬" bullet→"프롬프트 층의 정의"(등식 제거), SPEC-010 "채우는 스킬(작성 방법)"→"채우는 작성 방법", DEC-005 amendment·Rationale "작성 스킬(how)"→"작성 방법(how)". 역할 비유 "스킬처럼"은 층 정의(:160)·배포 근거(:177) 2곳만 유지(규칙 4). **개념·경계·배포 근거 로직 불변** — 라벨/등식 표현만 교정. 금지 패턴(`프롬프트(스킬)`·`=스킬`) grep 0 확인. 코드/guide 무관. PLAN-009-T-008.
- 2026-07-09 layer-taxonomy. AI 스테이지 실행 자산의 **3층 taxonomy(가이드/프롬프트/템플릿·스키마)를 제품 전체 cross-cutting SSOT로 정립**(사용자 확정). 종전 SPEC-011의 "프롬프트=무엇을 할지(what)만, 어떻게 하는지(how)는 프로젝트 컨텍스트(가이드)가 담당" 경계를 **뒤집음** → **가이드=자료·대상의 정의/정책/판단 규칙(what the material IS), 프롬프트=스킬(이 실행을 어떻게 작성할지·출력 규율), 템플릿/스키마=출력 양식**. 근거=배포 특성 정렬(자주 튜닝하는 스킬은 DB 프롬프트[즉시], 안정적 정의/규칙은 worker 가이드[재빌드]). **SPEC-011** §4에 `Layer Taxonomy` 서브섹션(3층 표+경계 테스트+가이드 자산 현황) 신설 + Assembly Contract를 taxonomy의 전달 축으로 재위치(3원천=원천1 가이드/원천2 프롬프트+템플릿·스키마/원천3 런타임 데이터[층 아님]) + S-1 step3·:165·:167·:248·Flow note 경계 문구 정합 + §7 OQ 2건(가이드/프롬프트 코드 재분할·③ 전용 가이드 신설 여부) 추가. **SPEC-009** Meta에 "프롬프트=스킬" 경계 명시(what-only 뉘앙스 교정). **SPEC-010** 경계에 출력 양식 층 위치 + JSON stage(①②④) `output_schema` 대칭 명시(템플릿=문서화③ 전용). **DEC-005** 정합=curator 판단으로 **DEC-005 in-place dated amendment**(자체 `2026-07-08 개정` 선례 관례 준수) — 가이드/프롬프트 경계 재정렬 Decision 항목 + Rationale 정합, 별도 DEC 신설 안 함. **코드/guide 파일 미변경**(worker `context/*.md`·프롬프트 재분할은 후속 profile-be 단계, 이 태스크는 스펙 문서만). PLAN-009-T-007.
- 2026-07-09 live-e2e-done. WP4·WP5 **라이브 e2e 완료 반영**(admin 라이브 확인, 코드 아님·products/ 정합만). docker api 재빌드 + dev DB 재시드(max_turns 전역3/chat6) 후 브라우저 실연동 확인. **WP4 Graph Chat**(work-005): 채팅 전송 → **프론트 → 백엔드 → redis → AI worker(claude 실행) → 백엔드 → 폴링** 전 구간 라이브 완주·실제 답변 생성·evidence 렌더 확인(open-kknaks는 `AXKG_REDIS_URL` broker, worker는 task_type 무관 범용 실행기; `base_url`은 미사용 죽은 값). Phase 2 검증·"코드 done ≠ 배포 완료" 블록·Open Issues 라이브 잔여 → **라이브 완료(2026-07-09)**로 정정, Open Issues "없음". **WP5 설정**(work-006): `/settings` 3탭 라이브(GET `/graph/chats`·`/settings/ai-provider` 등 200, provider/prompts/templates 저장·조회 왕복 정상), Phase 4 "admin docker 재빌드로 확인" → **라이브 완료(2026-07-09)**로 정정. 이전 로그(wp4-done·wp5-done)에 남았던 라이브 e2e 잔여가 해소됨. PLAN-007-T-015.
- 2026-07-09 wp4-done. WORK-005(WP4 Graph RAG Chat) **코드 완료 기준 close**(status in-progress→done, progress 33→100). 문서엔 Phase 1만 DONE이고 Phase 2/3가 TODO로 남아 SSOT 부패했던 것을 admin 확정으로 정합. **Phase 2(chat 스테이지 배선)** — ④ `GraphRagChatContextBuilder`(신규 `services/ai/graph_rag_chat.py`) retriever evidence + 세션 이력 조립·응답 파싱·저장, 근거 부족 시 `INSUFFICIENT_GRAPH_CONTEXT`(단정 answer 없이 missing_context 표면화), 세션 resume 배선. 신규 6 passed·전체 회귀 pytest 239 passed 무회귀(PLAN-007-T-002). **Phase 3(FE chat 패널)** — `/graph` 우측 `[graph] | [채팅]` split view, 세션 목록/메시지/run polling·Evidence Block·실패 표면(page-graph.html 시안 기준, PLAN-007-T-003), T-005에서 evidence 키 정합(`snippet`→`excerpt`·`link_reason`→`reason`)·SPEC-006 U-3 렌더 보강, tsc/build 통과. 후속 정합 T-006(seed 프롬프트 rules)·T-007(rules 위상 문서)도 완료. 30-work README WP Map/Status Board/Spec Coverage(SPEC-006 done, SPEC-011 ④ planned→done — 전체 SPEC-011은 004③ 잔여로 in-progress 유지) 정합. **라이브 e2e 잔여 정직 유지**: 코드·게이트 done이나 브라우저 실연동(그래프↔채팅 실제 답변·evidence 렌더)은 docker 재빌드·재시드 후 admin 확인 예정 = 코드 done ≠ 배포 완료. PLAN-007-T-014.
- 2026-07-09 wp5-done. WORK-006(WP5 설정) **코드 완료 기준 close**(status todo→done, progress 100). BE API 3종 + FE 설정 탭 3개, 회귀 275 passed·FE tsc/build 통과, BE↔FE 계약 정합(Templates `name`/`is_active` 포함·FE 무수정 연동). **Phase 1** AI Provider 설정 API(조회/갱신 + task-overrides CRUD + health) — 기존 `resolve_execution_config` 병합 로직 재사용, 전역 저장이 override 보존(U-3), override는 등록+`enabled=true` definition만 허용, pytest 254(PLAN-007-T-008). **Phase 2** Prompts API(목록/버전/저장=새 버전+활성/롤백=포인터 이동, JSON Schema 검증) — 활성은 `active_version_id` 포인터로 판별, pytest 264(PLAN-007-T-009). **Phase 3** Templates API(Prompts 대칭·`body`만, key seed 3종만 유효), pytest 275(264+11 무회귀, PLAN-007-T-010). **Phase 4** FE `/settings` 탭 3개 — 공통 `versioned-editor`·`confirm-dialog`로 Prompts·Templates 흡수, page-settings.html 시안 기준(PLAN-007-T-011). **spec-007 Case Matrix 정합**: T-008이 신설한 `UNKNOWN_TASK_DEFINITION`(404, task override 대상 `task_key` 미등록/`enabled=false`) 행 추가 — spec §4 Validation의 `task_overrides` 규칙을 API 레벨에서 강제(BE `settings.py`·FE `settings.ts` 문구 grounding·계약 정합). 30-work README WP Map/Status Board/Spec Coverage(SPEC-007/009/010 planned→done) 정합. **max_turns 기본값 정합은 미처리**(spec 전역 3 vs 코드 seed/resolution 6 불일치는 코드 변경 걸린 결정 — admin/사용자 별도 방향 후 처리, 이번엔 spec max_turns=3 그대로 유지). PLAN-007-T-012.
- 2026-07-08 wp4-phase1-done. WORK-005(WP4 Graph RAG Chat) **Phase 1(세션/run API)** 코드 DONE 반영(PLAN-007-T-001, profile-be T-011). chat 세션/메시지/run lifecycle·폴링 API 5종(`GET/POST /graph/chats`, `GET /graph/chats/{id}`, `POST /graph/chats/{id}/messages`, `GET /graph/chats/{id}/runs/{run_id}`), 검증(EMPTY_QUESTION/NODE_NOT_FOUND), user 스코프 격리(타 유저 404), run은 `queued`로만 생성(AI 실행 호출 없음·`# TODO(T-012)` seam), **pytest 10 passed·전체 회귀 233 passed**. AI 실행 배선(④ context builder·resume·응답 파싱)은 Phase 2(진행 중), FE chat 패널은 Phase 3(TODO). work-005 status todo→in-progress(33%), 30-work README WP Map/Status Board/Spec Coverage(SPEC-006 in-progress) 정합. 커밋은 admin scope 검수 대기(공유 작업트리 미커밋 누적·`graph.py`에 WP2/WP4 혼재). PLAN-007-T-004.
- 2026-07-08 wp2-wp3-progress-sync. WP2(work-003)·WP3(work-004) work 문서를 실제 완료 상태로 동기화(admin/curator 갱신 — 코드 워커는 `products/` 읽기 전용). **WP2**: Phase 1(parser+index)·2(edges rebuild+트리거)·3(retriever+조회 API) **BE DONE**(PLAN-006-T-002, admin 게이트 194 passed·신규 39 테스트), Phase 4(그래프 뷰 FE)는 크리티컬 패스 밖 미착수 → status todo→in-progress(75%). Code Surface를 실제 파일과 정합(신규 `repositories/documents.py`·`dto/document.py`·`schemas/documents.py`·`schemas/graph.py`, 채운 스텁 parser/root/services/workers/routes, `main.py` startup scan). retriever 기본값 `top_n=8`/`snippet_len=240`/neighborhood boost는 fixture 검증 상태 — 라이브 데이터 후 SPEC-011 OQ 튜닝. **WP3**: Phase 1(게이트 공통+분류 게이트②) **DONE + 라이브 e2e 전구간 통과**(PLAN-006-T-001, 분류 트리거→AI 실행→review_pending→승인→destination 확정, archive/문서화 컨테이너 분기, open-kknaks Session Rule 저장, admin 게이트 155). Phase 2(문서화 게이트) **IN-PROGRESS**(PLAN-006-T-004 profile-be 발주). Phase 3(Apply Executor)·재분류 재오픈 TODO. **FE 분류 게이트 부분은 선착수 DONE**(PLAN-006-T-003 — 별도 `/approval` 은퇴, Source Inbox 원페이지 2컬럼 통합, 파생 라벨 진행 표시, tsc+build 통과; 사용자 수동 수정으로 "문서함" 네이밍+inbox/승인 2탭) → Phase 4를 분류 게이트 FE(done)/문서화 게이트 FE(Phase 2 후)로 쪼갬. status todo→in-progress(25%). 30-work README WP Map/Status Board/Spec Coverage(SPEC-001/002/004/005 in-progress, SPEC-011 ②done·③in-progress) 정합. **미완은 미완으로 유지** — 임의 done 처리 없음. PLAN-006-T-005.
- 2026-07-08 wp1-phase56-code-done. work-002 Phase 5(메모 fallback)·Phase 6(요약 초안 게이트) **문서 계약 DONE → 코드 DONE 최종 반영**. Phase 5: BE(collect_source user_note fallback·queue-collection note 재요약·Slack `<< >>`, pytest 124, T-013) + FE(collection_failed 메모 추가 UI, T-014), 메모 fallback 수집 구제 라이브 A/B 확인(메모 있으면 수집 단계 통과). Phase 6: BE(`POST /sources/{id}/summary-feedback` + `options.resume={mode:session,session_id}` 배선·v1 read-only `metadata.summary_versions[]` 아카이브·`/classify` 501 스텁, pytest 133, T-016) + FE(문서보기 모달·피드백·분류, T-017), **세션 resume 재요약 라이브 실증**(피드백 payload 2,318자 vs v1 21,052자 ≈ 9배 축소, 반환 session id v1과 동일=재개, 원문 재전송 없이 v2). **max_turns 픽스**(요약 실행 크래시 `error_max_turns` exit 1 근본원인=`max_turns=3`이 guide Read agentic turn 못 버팀 → 3→6, resolution.py DEFAULT+seeds.py+DB) Phase 3 노트 추가. WP status는 done 유지(원 스코프 Phase 1~4 + 확장 5·6 코드 완결). **라이브 잔여는 정직 유지**: Phase 3 공개 배포·Phase 4 Slack 실연동(앱 등록+env+배포)·삭제/무시 엔드포인트 실배선·경로명(`/classify` vs SPEC-001 `classification-gates`)·버전 저장 위치 최종 정합(WP3) — 코드 done ≠ 배포 완료. PLAN-005-T-018.
- 2026-07-08 summary-draft-gate. 요약이 자동으로 분류까지 넘어가던 흐름을 **요약 초안 검토 게이트**로 전환(사용자 확정). 요약 → DB 초안(`summary_payload`, 임시) → 렌더(수정 가능 형태) → `[피드백]`(세션 resume 재요약, v2) / `[분류]`(분류 게이트 트리거)로 분기, **요약이 자동으로 분류로 넘어가지 않음**. 핵심 = 세션 resume: 요약 결과와 함께 받은 `open_kknaks_session_id`로 claude 세션을 이어 피드백만 던져 원문·지침 재전송 없이 재생성(인프라: session_id 저장 O·`resolve_resume_session` 계산 O·`options.resume` 전달 배선만 미완). SPEC-011(Feedback Regeneration Resume Wiring ①②③ 공통 배선 계약·① row 재생성 입력·UX 표면·AC), SPEC-002(피드백→v2 + v1 read-only + resume 규칙을 요약 초안①에도 적용 명시 — 단 저장은 `summary_payload`, advance 액션은 승인 아닌 `[분류]`, gate 상태기계 미적용), SPEC-003(U-2 요약 초안 수정 가능 렌더·`[피드백]`/`[분류]`·자동 분류 제거·Impl Rules·AC·spec-002 역링크), SPEC-001(요약·분류 병합 카드 **분리** — 분류기 AI는 `[분류]` 트리거로만 실행, 자동 실행 서술 제거·Placement/U-2/S-1/Flow/AC/Impl Rule) 개정, `21-html/page-approval.html` 요약 초안 카드 분리(병합 아님·`[피드백]|[분류]` CTA·데모 라벨), work-002 **Phase 6(요약 초안 게이트)** 신설(문서 계약 done·코드 BE T-016/FE T-017 병렬 대기). Phase 6은 WP1 close 이후 추가 스코프 — WP 보드 done은 원 스코프 기준 유지. 분류 AI 실행·md 변환은 WP3(work-004) 범위 밖. PLAN-005-T-015.
- 2026-07-08 memo-fallback. 수집 실패 시 **메모 fallback** MVP 결정 문서 정합(사용자 확정). Cloudflare/봇 방어 사이트(medium 등)는 static·dynamic 모두 원문 수집 불가 — 수집 못해도 사용자가 넣은 메모/복붙 텍스트로 요약하도록 수집 체인 확장. 확정 4개: ① 수집 체인 `url 성공→원문` / `url 실패+메모 있음→메모로 요약` / `url 실패+메모 없음→collection_failed`(url 성공 시 원문 우선), ② 메모/원문 구분 표기 안 함(source_basis 배지 없음), ③ Slack 메모 식별자 `/inbox <url> << 메모 >>`, ④ `collection_failed`에 메모 추가→재요약. SPEC-012(수집 체인 4단계 최종 fallback·`user_note` SourceMaterial[adapter/content_format/fetch_method=user_note, canonical_url=원 URL]·Failure Contract "원문 실패 AND 메모 없음"일 때만 collection_failed·AC), SPEC-011(요약 스테이지① `user_note` 입력 유형·메모에 없는 사실 추측 금지·구분 플래그 없음), SPEC-003(U-2 메모 추가/재요약·U-3 fallback 명시·S-1 `<< >>` 규약·`queue-collection` optional `note`·Validation·AC) 개정, work-002 **Phase 5(메모 fallback)** 신설(문서 계약 done·코드 BE T-013/FE T-014 병렬 대기). Phase 5는 WP1 close 이후 추가 스코프 — WP 보드 done은 원 스코프 기준 유지. 메모 "있음/없음"은 trim 후 non-empty 기준, 최소길이 컷 등 미확정은 미반영. PLAN-005-T-012.
- 2026-07-08 wp1-close. WORK-002(WP1 Source Intake) **코드 완료 기준으로 close**(status in_progress→done, progress 100). Phase 3(요약 스테이지) 라이브 e2e 성공 반영 — manual URL → `summarized` + summary_payload 고품질 실값, claude가 worker workspace 프로젝트 컨텍스트(`context/source-summary-guide.md`)를 실제 Read 확인(재설계 후 pytest 89). Phase 4(Slack 슬래시 커맨드 intake) 코드 DONE — `POST /api/v1/slack/commands` 서명검증·URL추출·멱등·저장·ack·봇 앵커·스레드 회신 구현+pytest 107, 라이브(Slack 앱 등록+env 2키+공개 배포)는 admin 후속. 원문 전달 방식 OQ(SPEC-011 §7) = **submit 프롬프트 인라인으로 확정**(builder가 `SourceMaterial` 원문을 데이터 블록 인라인, workspace 파일은 방법 지침만) → SPEC-011 §4/§7 해소. **라이브 잔여는 숨기지 않고 Open Issues로 명시**: Phase 4 Slack 실연동·공개 배포, 삭제/무시 엔드포인트(`DELETE /sources/{id}`·`POST /sources/{id}/ignore`) 실배선(계약 정의까지가 WP 범위). 30-work README WP Map/Status Board/Coverage(SPEC-003/012 done, SPEC-011 002① done) 정합. 근거 PLAN-005-T-008/009/010, 클로즈 PLAN-005-T-011.
- 2026-07-08 summary-execution-model. 요약 실행 입력 모델 개정(WP1 Phase 3 라이브 e2e 재정의) — 방법·배경 지침을 **api 프롬프트 인라인 3자 조립 → worker 이미지 내장 프로젝트 컨텍스트**로 이동. worker가 claude를 프로젝트 안에서 실행(진입 `CLAUDE.md → agent.md → context/`, claude가 실행 디렉토리에서 지침 읽음), 프로젝트 컨텍스트는 빌드 시점 이미지 내장(마운트/git pull 런타임 의존 회피). api submit 표면 = **DB 프롬프트(작업 지시)+output_schema (+③ 활성 템플릿)+런타임 원문(`SourceMaterial`)**만 — `context/source-summary-guide.md` 미로드·파일 fallback 제거(프롬프트/템플릿 DB 로드 fallback은 유지). SPEC-011 Assembly Contract를 '실행 입력 3원천'(프로젝트 컨텍스트/DB 아티팩트/런타임 데이터)으로 재서술 + S-1·Impl Rules·Flow·OQ 정합, DEC-005 3자 조립 결정 개정(빌드 시점 이미지 내장·`apps/worker/workspace/` 명시), 40-arch system(open-kknaks 경계·AI Task Resolution·Monorepo layout에 `apps/worker/workspace/`)·deploy(Worker Project Context 절 신설·`WORK_DIR`·마운트 아님) 환류, work-002 Phase 3 서술·Open Issues 갱신. 원문 전달 방식(인라인 vs `WORK_DIR` 파일 드롭)은 OQ. 코드측(`apps/worker/workspace/` 신설·builder 재배선)은 profile-be T-008 병렬. PLAN-005-T-009.
- 2026-07-08 wp1-phase3-and-arch. WORK-002(WP1) Phase 3(요약 스테이지 배선, SPEC-011①) 코드 DONE 반영 + Phase 2(Collection Adapter, SPEC-012) 완료 소급 반영(T-004, 73 passed). open-kknaks 실 바인딩 = **Redis 직결 `AgentClient`로 확정**(HTTP 아님, work-002 Open Issue 해소): api=producer `RedisOpenKknaksClient`가 `AgentClient(RedisBroker(url=AXKG_REDIS_URL, namespace="axkg"))`를 감싸 `submit(..., queue="default", max_retries=0)`, worker(`ClaudeWorker`)가 같은 namespace/queue consumer, credential(`CLAUDE_CODE_OAUTH_TOKEN`)은 worker 측에만(SPEC-007). collection adapter 실행 위치 = **api 프로세스 내 BackgroundTask로 확정**(SPEC-011/012 OQ 해소, 별도 browser worker 분리 안 함) — 요약 AI만 open-kknaks worker로. Phase 3 pytest 88 passed·producer 체인 라이브 검증 완료, 최종 `summarized` 라이브 e2e만 worker claude 환경(토큰+바이너리) 대기(admin 후속). 40-architecture system/deploy README 환류(open-kknaks 바인딩·`redis_open_kknaks.py`·Source Intake flow·AXKG_REDIS_URL/CLAUDE_CODE_OAUTH_TOKEN env·Deployment Rule). progress 70%. PLAN-005-T-007.
- 2026-07-07 slack-slash-command. Slack intake 트리거를 Events API(채널 구독) → **슬래시 커맨드**로 확정 반영. 등록 Request URL `POST /api/v1/slack/commands`(백엔드 공개 주소 `ax-api.kknaks.cloud`, 등록 문자열과 rewrite 없이 문자 일치 — 무prefix 관례 예외). 수신 계약: 서명 검증(`AXKG_SLACK_SIGNING_SECRET`)·`text`서 URL 추출·URL 없으면 사용법 ephemeral·`trigger_id` 멱등·3초 ephemeral ack, source `received`(source_channel=slack). 아웃바운드: 봇 앵커 메시지 post 후 `ts` 저장, summarized/collection_failed 시 앵커 스레드 회신(`AXKG_SLACK_BOT_TOKEN` required 승격). SPEC-003(S-1·§4 API/Validation/Case Matrix/Flow/Impl Rules, `/integrations/slack/sources`→대체)·40-arch deploy/system(Slack Intake 절·env·라우터 표)·work-002 Phase 4·Scope·External dep 정합. 삭제/무시 gap도 SPEC-003 §4에 `DELETE /sources/{id}`·`POST /sources/{id}/ignore` 추가로 닫음(실배선은 후속 admin 발주). 커맨드 이름은 하드코딩 안 함(서버 command-무관). PLAN-005-T-005.
- 2026-07-07 wp1-phase1-done. WORK-002(WP1 Source Intake) Phase 1 완료 — sources API(수신·목록·상세·중복·재시도 큐잉, SPEC-003 계약)와 Source Inbox 화면(큐/상세/직접 입력 모달, 21-html 시안 기준). BE `uv run pytest` 41 passed(신규 20), FE `tsc` exit 0·`npm run build` 통과. SPEC-003 U-1~U-3·S-2·S-3 AC 충족(근거: PLAN-005-T-001/002 리포트). FE는 auth 가드 재사용 위해 `(inbox)` 그룹 대신 기존 `(app)/page.tsx`(nav "/")에 구현 — Code Surface 경로 정정. work status in_progress(25%, Phase 1/4). 삭제/무시 엔드포인트·Slack intake 트리거는 admin 별도 논의(범위 제외). 다음: Phase 2 Collection Adapter(SPEC-012).
- 2026-07-07 wp0-done. WP0 완료(100%). FE 로그인+앱 셸+기본 페이지 4개 골격 완성 — Tailwind v4로 21-html 시안 톤 재현, localStorage 토큰+클라이언트 가드, headless Chromium 17개 체크 전부 PASS. UI 카피 한국어 정리(네비 승인/그래프/설정, 로그인/로그아웃, 그래프 페이지 제목 시안 동기 — FE 공통 기준 규칙 반영). CORS 미들웨어 추가(AXKG_CORS_ORIGINS env). compose 스택(postgres 45432/redis 46380/api 48100) 기동·seed 검증 — 사용자 로그인 e2e 확인 완료. 다음: WP1(work-002)·WP2(work-003) 병렬 착수.
- 2026-07-07 wp-breakdown. 도메인 WP 문서 5건 생성: AXKG-WORK-002(WP1 Source Intake — SPEC-003/012/011①, open-kknaks 실 바인딩 확정 포함), WORK-003(WP2 문서·그래프 코어 — SPEC-005, parser/index/rebuild 3트리거/retriever/그래프 뷰), WORK-004(WP3 승인 게이트 — SPEC-001/002/004/011②③, 연결 후보 2단 컨텍스트·Apply Executor·재분류 재오픈), WORK-005(WP4 Graph Chat — SPEC-006/011④), WORK-006(WP5 설정 — SPEC-007/009/010). 각 WP에 Phase 분해·Code Surface(계층 경로)·의존 관계 명시. 30-work README WP Map/Status Board/Coverage 갱신, 전체 spec 12건의 works 역링크를 담당 WP로 재배정(SPEC-011은 001+002+004+005). FE 로그인+기본 페이지(WP0 Phase 4 잔여)는 백그라운드 에이전트 진행 중.
- 2026-07-07 wp0-phase5-and-docker. WP0 Phase 5(AI 실행 골격, SPEC-011) 구현 완료: ai_task_definitions 해석→SPEC-007 병합(global→defaults→overrides)→ai_tasks 생성·스냅샷→블록 조립(변수 치환 없음, 코드 프레임)→open-kknaks client ABC→JSON 파싱/스키마 검증(OUTPUT_PARSE_FAILED/OUTPUT_SCHEMA_MISMATCH, 실패 시 미소비)→코드 fallback(version id null+payload 기록)→retry 체인·resume 헬퍼. pytest 21 passed. open-kknaks HTTP 표면 미확정이라 client는 ABC+fake, 실 바인딩은 Redis broker(AgentClient) 유력 — 도메인 WP에서 확정. 코드레포에 docker 셋업 추가: apps/api Dockerfile(uv frozen sync), apps/worker(open-kknaks ClaudeWorker, 호스트 claude mount 패턴), docker-compose.yml(postgres 45432·redis 46380·api 48100·worker, web profile, 기존 스택과 포트 무충돌), .env.example. compose config·api 이미지 빌드 검증. deploy README Open Items 갱신.
- 2026-07-07 wp0-phase34. WP0 Phase 3(async DB: 20테이블 ORM + Alembic 15단계 + idempotent seed, PG 16 라운드트립 검증)·Phase 4 BE(SPEC-008 auth 전체: login/logout/me, token hash+TTL, Bearer dependency 기본 적용, pytest 10 passed) 완료. 계층 구조(routes→services→repositories, dto/schemas, pydantic v2) 적용. documentation gate의 ai_task_definitions.template_key는 null 확정(destination→템플릿 매핑은 context builder 소관) — DB README 주석 갱신. Phase 4 잔여는 FE 로그인 페이지. progress 60%.
- 2026-07-07 backend-layering. 백엔드 계층 확정: 라우터→서비스→레포지토리 3계층(의존 단방향, DB session은 레포지토리만 접근), DB 접속은 SQLAlchemy 2.0 async + `postgresql+psycopg`(psycopg3 async, Alembic도 async 설정), 객체 분리 dto(서비스 계층 입출력)/schemas(API 요청·응답, 라우터 전용) — 둘 다 pydantic v2, schemas↔dto 변환은 라우터·dto↔ORM 변환은 레포지토리. ax-graph 확정 구조(context/ 배경지식, apps/web·apps/api/axkg 계층 모듈, packages/contracts, data/documents)를 40-architecture system README에 역반영(Monorepo Layout 교체 + API Layering 절 신설 + FastAPI Modules 계층 재편) + database README 접속 계층 명시. 템플릿/프롬프트 seed 전략 확정: 별도 파일 디렉토리 없음 — DB가 런타임 SSOT(SPEC-009/010), 초기 seed·코드 fallback은 코드(마이그레이션/상수) 소유 → SPEC-009 OQ(DB 우선 vs 파일 seed) resolved. work-001 Phase 2(디렉토리 구조 scaffold) DONE, progress 25%.
- 2026-07-07 spec-012-unified-web-adapter. static/dynamic web adapter를 단일 계약으로 통합 — 텍스트 획득 방식(HTTP HTML 파싱 vs Playwright 렌더링)만 다르고 DOM 제거·metadata 추출·후처리·수집 기준·AI 입력 지시는 공통. 정교한 본문 추출(article container·텍스트 밀도·readability) 제거: UI 제거 후 페이지 전체 visible text(`content_format=page_text` 통일)를 넘기고 본문/노이즈 구분은 요약 AI 책임(동적과 동일 모델). 실패 표면 축소(추출 휴리스틱 미달로 죽던 케이스가 dynamic fallback→AI 구분으로 살아남음), title/author/published_at은 meta 기반 추출 유지(없으면 null, 실패 아님). page_kind 판정 static/dynamic 공통 명시. 본문 추출 고도화는 품질 관찰 후 OQ. ax-graph context/source-summary-guide.md 동기.
- 2026-07-07 spec-012-youtube-fallback. YouTube adapter를 기존 profile 구현(content_enrich.py) 동작에 정합: 수집은 metadata(yt-dlp, description 포함)+transcript 두 갈래임을 명시, `metadata.description` 필드 추가, 자막 없는 영상은 실패 대신 **description fallback**(`content_format=video_description`, `fetch_method=youtube_metadata`)으로 진행하고 둘 다 부실할 때만 `TRANSCRIPT_UNAVAILABLE`. AC 갱신. ax-graph context/source-summary-guide.md에 video_description 케이스 반영.
- 2026-07-07 wp-plan. MVP 구현을 WP 6개로 분해 확정. work-001을 WP0(모노레포 scaffold + Alembic migration/seed + auth[SPEC-008] + AI 실행 골격[SPEC-011 해석→조립→fallback 프레임])로 재정의 — Phase 2~5 재구성, Covers를 SPEC-008/011로 축소. WP1 intake(003/012/011①)·WP2 그래프 코어(005)·WP3 게이트(001/002/004/011②③)·WP4 chat(006/011④)·WP5 설정(007/009/010)은 WP0 Phase 2(디렉토리 구조 확정) 후 work-002~006으로 생성. 의존: WP2가 WP3 선행(연결 후보 컨텍스트·엣지 rebuild가 그래프 코어 전제), WP4/WP5 병렬. 30-work README에 WP Map 신설.
- 2026-07-07 spec-stable. gap 리뷰(blocker/major/minor) 전량 해소 + SPEC-012 통합 완료로 AXKG-SPEC-001~012 전체 status를 draft → stable로 전환. 잔존 OQ는 구현 중 결정 가능한 비차단 항목만(SPEC-012 snapshot 저장·dynamic 실행 분리·list 후속 UX, SPEC-010 마이그레이션 정책, SPEC-009 seed 전략, work-001 Redis 시점). 다음 단계: WP 분해(WP0 scaffold → 도메인별 WP).
- 2026-07-07 spec-012-integration. AXKG-SPEC-012(Source Collection Adapter: YouTube·정적 웹·동적 웹 수집, 사용자 작성) 교차 정합 반영. SourceMaterial의 `source_type` 필드를 `adapter`로 개명(콘텐츠 유형 source_type[article/video/document/unknown] 어휘와 충돌 제거, adapter→유형 힌트 매핑 명시). canonical_url↔sources.normalized_url 갱신·중복 재검사 규칙 추가(youtu.be 합류 케이스). UNSUPPORTED_SOURCE_TYPE(PDF/RSS)은 재시도 무의미 — "지원 예정 형식" 안내 병기. INVALID_URL 경계 명확화(YouTube 판정 URL 한정). published_at ISO-8601 통일. page_kind=list 후속 UX를 OQ로 등재. SPEC-011에 chunk 요약 병합 규칙 추가. SPEC-003에 수집 범위/normalized_url 갱신 규칙 + 011/012 역링크. work-001 Covers·30-work README·system README(Source Intake flow adapter 단계, integrations.source_collection 모듈)·제품 README 커버리지 반영. DEC-005 수집 위치 OQ를 SPEC-012로 resolved 마킹.
- 2026-07-07 minor-resolution. gap 리뷰 minor 마감. SPEC-008 Protected Routes를 현행 API로 갱신(`/graph/chats` run polling, 공통 게이트 API, Slack 서명 검증 예외 명시). SPEC-002에서 `held` 상태 제거(개별 보류 폐지 잔재)·`cancelled` 용도 명시 + DB enum 동기. SPEC-004 U-2 `reference→`→`resource→` 오기 수정. SPEC-003에 collection 용어 정의(수집=요약 task 전체). SPEC-005에 깨진 링크 정책 통합(생성 경로 거부 / `is_broken`은 외부 편집 사후 표시), concept 경로 관례(`permanent/concepts/`), `type=source` 그래프 기본 제외, BL-001 노드타입 제안(case/tool/person/organization) 불채택 명문화. DEC-003 Implementation Notes에 superseded 주석(`/graph/chat`→chats+runs). system API Surface 누락 3건 추가(link-preview, sources ai-tasks, task-overrides PUT/DELETE) + apply_plan pre-validation 시점 명시. DB sources.metadata slack_events 규약 추가. 20-spec README 제목·Decision 열 정합. 역링크 위생: 전체 spec 11건에 WORK-001 works 링크, BL-001에 SPEC-009/010/011·DEC-002/004에 SPEC-009/010 링크 보충, Meta Decision reference 줄 4건 정합. BL-001 source.ref의 삭제된 inbox 경로는 이력 참조로 유지.
- 2026-07-07 major-resolution. gap 리뷰 major 정리. ① 게이트 단일화: 문서화 게이트를 ApprovalGate(gate_kind=documentation) 공통 모델로 확정, SPEC-004 독자 status는 파생 표시 라벨(매핑표 신설), 액션 API를 `/gates/{gate_id}/*` 공통으로 단일화(`/documentation-gates`는 조회 전용, SPEC-001 `permanent-note` 폐기). ② 파이프라인 상태 6종(classify_*/doc_*)을 sources.status+게이트 상태의 파생 라벨로 확정 — 매핑표를 SPEC-001에 SSOT로 신설. ③ 재분류("이 destination이 아님") = 분류 게이트 재오픈(approved→regenerating, approved revision superseded 마킹, destination 리셋, 문서화 게이트 cancelled) — SPEC-002/004/DB 반영. ④ document_type 어휘를 DB enum 8종으로 통일, payload `product` 제거(project 초안=baseline, MVP는 baseline 후보만). ⑤ archived 단일 상태+진입 경로 2개 명문화, SPEC-003 SSOT에 분류-archive 전이 추가 + T-002 잔재 3곳 정리. ⑥ task_type enum SSOT를 SPEC-011로 이전. ⑦ SPEC-005 개별 연결 승인 UX 잔재 제거(게이트 단일 승인). ⑧ SPEC-009 키 파생 원천을 SPEC-011 4스테이지로 수정, VoltAgent OQ 해소(레거시 참고용, 바인딩 안 함·자체 구현). ⑨ Slack intake를 API Surface(`/integrations/slack/sources`)+deploy(signing secret 검증, Events 직수신)에 반영. ⑩ Graph Rebuild 트리거 확정(startup scan+apply 증분+`POST /graph/rebuild` 수동, watcher는 post-MVP). ⑪ tool/workflow=코드레포 경계 문장을 system 아키텍처에 명시. ⑫ work-001 Covers를 SPEC-001~011로 확장.
- 2026-07-07 blocker-resolution. 구현 착수 전 gap 리뷰(3축: 도메인/스펙/아키텍처)에서 확인된 blocker 5건을 결정·반영. AXKG-DEC-005 신설(① 문서초안 AI 연결 후보 컨텍스트 = 2단 하이브리드: Graph RAG retriever top-N + documents index 스냅샷, Graphify류 외부 도구는 서빙 경로 제외 ② 3자 조립 = BE context builder가 template→prompt→output_schema 순 조립 ③ output_schema = JSON Schema 확정 ④ AI 실행 파이프라인 spec 신설 ⑤ project 문서화 MVP 포함 + SPEC-009/010 채택 근거 소급, DEC-004 template 항목 supersede). AXKG-SPEC-011 신설(4스테이지 공통 실행 계약: 컨텍스트 조립·구조화 출력 파싱·실패 매핑·콘텐츠 수집·fallback — 요약 AI 실행 소관 공백 해소). SPEC-004(초안 입력 컨텍스트·flow·OQ 문구), SPEC-005(S-1 연결 후보·index 스냅샷 규칙), SPEC-009(OQ2 해소·조립 주체·envelope 경계), SPEC-010(OQ1·OQ3 해소, MVP scope에 project_baseline) 개정. 40-architecture system(AI Task Resolution 3자 조립+후보 컨텍스트+fallback, documentation gate flow, domain.graph 공유 retriever)·database(ai_task_definitions.template_key, ai_tasks.template_version_id, seed 키 명시) 반영. 잔여 gap(major/minor)은 별도 처리 예정.
- 2026-07-07 settings-html-tabs. page-settings.html 설정 레이아웃을 좌측 nav 세로 스택 → **탭(shadcn Tabs)** 으로 전환. AI Provider / Prompts / Templates / Auth·Login 각 탭이 전체 폭 사용, 긴 프롬프트(본문 h-40 + 출력 스키마 h-56)·템플릿(md 뼈대 h-80) 편집기를 크게 표시. 데모 플레이어가 currentStep 기준으로 탭 자동 전환(provider→prompts→templates). 사용자 피드백 반영(T-009 후속). PLAN-004-T-009.
- 2026-07-07 settings-html-regen. page-settings.html을 4개 스펙 기준으로 재생성(덮어쓰기): AI Provider(007) + Prompts(009, 프롬프트 본문+출력 JSON schema 편집·버전·롤백) + Templates(010, md 뼈대 편집·버전·롤백 + frontmatter template 스탬프 안내) + Auth/Login(008, 상태·로그아웃). 설정 좌측 nav 5행, Prompts·Templates 좌목록/우편집기+버전히스토리 대칭 패턴. Interactive Demo Player 12 steps(로그인→provider→프롬프트 저장→템플릿 저장). shadcn 매핑 주석·CSS 변수·lucide SVG·앱셸(active=설정)·폭1440·이모지 0 규약 유지. page-graph/page-approval.html·승인 스펙 미변경. PLAN-004-T-009.
- 2026-07-07 AXKG-SPEC-010 작성. ax-graph가 AI로 생성하는 지식 문서 템플릿(뼈대 = frontmatter 필드 + 섹션 구조)을 md 형태로 PostgreSQL(DEC-002)에 저장하고 버전 저장·활성·롤백, 생성 문서 frontmatter에 적용 템플릿 버전 스탬프(예: `reference@v3`) 계약 정의. SPEC-009(프롬프트) 대칭 구조 — 템플릿=뼈대, 프롬프트=채우기, 문서화 게이트(③) 초안 AI가 템플릿+프롬프트+output_schema 3자 조립. 설정 = AI Provider(007)+Prompts(009)+Templates(010). MVP=reference·permanent만, project/archive·마이그레이션·조립 형식은 OQ. 레포 `templates/product/**`(curator 메타문서 템플릿)와 별개 경계 명시. 20-spec/README 행 추가, SPEC-009에 경계 한 줄+링크. 승인 스펙(001~005)·page-approval.html 미변경(사용자 편집 중). PLAN-004-T-008.
- 2026-07-07 feedback-modal. 두 게이트(분류 ②·문서화 ③) 피드백 입력을 인라인 텍스트에어리어 → **모달**로 이동. 게이트 카드 CTA는 `피드백`·`승인` 두 버튼만, `피드백`=모달 트리거. 모달: 대상 게이트/현재 버전 라벨 + 텍스트 + `재생성`(v2)·`취소`. 재생성 계약(feedback→v2, v1 read-only) 불변, 입력 경로만 변경. SPEC-002(Placement/U-1/U-2/S-1/S-2/Case Matrix/Impl Rules) 모달 기반 재서술 + SPEC-001·004 피드백 언급 모달 정합. page-approval.html: 게이트 인라인 피드백 details 2개 제거 → 모달 트리거 버튼, 피드백 모달(shadcn Dialog) 추가 + data-* 라벨 주입 JS. section-spec-002 스트립은 버전 규칙 설명 전용. T-006 반영본 위 작업. PLAN-004-T-007.
- 2026-07-07 drop-derived-approval. 문서화 승인 게이트(SPEC-004, ③)에서 파생지식 개별 승인 제거. 파생지식을 초안과 한 덩어리(단일 단위)로 재정의 — 게이트 레벨 `피드백`/`승인` 단일 액션에만 딸림(부분 승인·부분 재생성 없음). SPEC-004 개정: 개별 승인 API(`derived-suggestions/.../approve`)·Validation 필드(suggestion_type/target_note_id)·Case Matrix(MISSING_TARGET_NOTE)·State machine(derived_pending/approved/held) 제거, DerivedSuggestion을 DocumentDraft 동반 산출물로 재서술. page-approval.html: 파생지식 항목별 승인/보류 버튼 삭제 → 읽기용 목록. 다른 게이트(분류)·다른 스펙 미변경. PLAN-004-T-006.
- 2026-07-07 AXKG-SPEC-009 보강. 프롬프트 정의 = 프롬프트 텍스트 + 출력 JSON schema 한 쌍으로 확장, 함께 버전 관리(저장·롤백 한 쌍). AI 실행 시 활성 버전 텍스트+스키마 로드해 구조화 출력(JSON) 강제. output_schema ↔ 승인 게이트 UI 필드 계약 연결(분류기 destination_type 등 / 요약 title 등). 편집기 출력 형식 영역, INVALID_OUTPUT_SCHEMA, 스키마 호환 OQ 추가. PLAN-004-T-005.
- 2026-07-07 approval-redesign. 승인 워크플로우를 2-게이트로 재구성(T-002 단일 병합 취소·재분할): ① 요약·분류 병합 카드 → ② 분류 게이트(PARA only, 연결 후보 제거) → ③ 문서화 승인 게이트(SPEC-004 재정의, destination별 AI 초안 + up:/[[ ]] + 파생지식, 인라인). 연결은 별도 게이트 아님(초안에 흡수). resource≡reference. 우측 사이드바 제거. 두 게이트 공통 버전(v2|v1 read-only). SPEC-001·002·004 개정 + SPEC-005 참조 정합 + DEC-001 직접 갱신. page-approval.html 재생성(중앙 세로 스택 3블록). PLAN-004-T-004.
- 2026-07-07 AXKG-SPEC-009 작성. AI 프롬프트를 PostgreSQL(DEC-002)에 저장하고 버전 저장·활성 지정·롤백, AI 실행 시 활성 버전 로드 + 코드 fallback 계약 정의. 설정 페이지 = AI Provider(SPEC-007) + Prompts(SPEC-009). MVP는 프롬프트만(tool/workflow는 OQ). SPEC-007에 경계 한 줄 표기. 20-spec/README 행 추가. PLAN-004-T-003.
- 2026-07-07 workflow-fix. 확정 흐름 반영: ① 요약 AI 자동 트리거(`received → summarizing → summarized`, 수동 `수집 시작` 제거), ② 분류기 AI가 PARA 분류+연결 추천을 한 번에 생성하는 단일 승인 게이트로 분류/연결 게이트 병합, `summarized`만 게이트 대상. SPEC-001·002·003·004 개정 + DEC-001 직접 갱신. source lifecycle SSOT는 SPEC-003. PLAN-004-T-002.
- 2026-07-07 spec-html-add. 8개 SPEC을 3개 baseline HTML 시안으로 변환(React+shadcn 계약). IDs: AXKG-SPEC-001~008. Links: `21-html/page-graph.html`(005·006), `21-html/page-approval.html`(001·002·003·004), `21-html/page-settings.html`(007·008). PLAN-004-T-001.
- 2026-07-07 AXKG-DEC-002 작성. 최종 문서는 Markdown file SoT, 운영 상태/index/cache는 PostgreSQL로 결정. Database architecture README 갱신.
- 2026-07-07 AXKG-SPEC-008 작성. seed user(`kknaks@medisolveai.com`/`1234`)와 브라우저 저장 token 기반 MVP 로그인 계약 정의.
- 2026-07-07 코드 레포 local clone 경로를 `/Users/kknaks/git/toy_pr2/ax-graph`로 변경.
- 2026-07-07 AXKG-SPEC-007 작성. open-kknaks 기반 내부 AI 실행과 Claude/Codex provider 설정 페이지 계약 정의.
- 2026-07-07 AXKG-SPEC-006 작성. 그래프 탐색과 그래프 기반 채팅 응답 계약 정의.
- 2026-07-07 AXKG-SPEC-005 작성. Obsidian과 제품 페이지가 공유하는 wikilink/frontmatter `up` 기반 문서 연결 계약 정의.
- 2026-07-07 AXKG-SPEC-004에 reference 최종승인 후 기존 개념 보충, 신규 개념 생성, project baseline 후보 생성 흐름 추가.
- 2026-07-07 AXKG-SPEC-004 작성. resource source를 reference note로 전환하는 승인/피드백/보류/최종승인 사이드바 흐름 정의.
- 2026-07-07 Source Inbox raw record 양식을 AXKG-SPEC-003 안의 문서 계약으로 반영.
- 2026-07-07 AXKG-SPEC-003 갱신. Slack URL과 페이지 직접 입력 URL을 Source Inbox로 받는 흐름 정의.
- 2026-07-07 AXKG-DEC-001, AXKG-SPEC-001, AXKG-SPEC-002 작성. 정보 수집, 분류 승인, 연결 승인, 영구 문서화 흐름을 제품 계약으로 정의.
- 2026-07-07 제품 SSOT 디렉터리 생성. 원본: `inbox/2026-07-07-ax-knowledge-graph-from-curated-sources.md`.
