# secret
.env
.env.local
.env.*.local
.env.production
.env.development

# Claude Code 로컬 설정 (절대 경로 + 머신별)
.claude/settings.local.json
# .claude 는 .agent 심볼릭 링크라 git 은 실제 경로로 봄 → 동일 파일 실경로도 제외
.agent/settings.local.json

# open-kknaks worker 바인드 마운트 자산 (호스트 setup.sh 로 박음, ADR-04 §2.2)
.claude-tools/

# worker 컨테이너 산출물
app/worker/__pycache__/
app/slack_bridge/__pycache__/

# 옵시디언 사용자 설정 (사용자 PC vs 홈서버 분리)
.obsidian/

# Python 빌드 산출물
**/__pycache__/
*.py[cod]
app/back/__pycache__/
app/back/**/__pycache__/
app/back/.pytest_cache/
app/back/.venv/
app/back/dist/
app/back/*.egg-info/

# Next.js 빌드 산출물
app/front/node_modules/
app/front/.next/
app/front/out/
app/front/dist/
app/front/tsconfig.tsbuildinfo

# 시스템
.DS_Store
*.swp
*.swo

# log
*.log
app/back/logs/

# 지식그래프 빌더 산출물 (KDEV-WORK-001, 부팅 시 derive — git 트래킹 X)
_graph.json


# import-project skill의 외부 레포 둘 자리 (gitignore — 본 repo 일부 X)
workspace/

# 디자인 시안 작업 폴더 (정본은 별도 보관, repo 트래킹 X)
claude_design/
