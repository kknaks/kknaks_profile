---
type: concept
id: web-application
title: 웹 애플리케이션 (Web Application)
aliases:
  - 웹 애플리케이션
  - 웹앱
  - web application
up:
  - 2024-05-24-Day01
  - 2024-05-27-Day02
  - 2024-08-26-Day63
tags:
  - web
  - 소프트웨어분류
  - 기초
---

# 웹 애플리케이션 (Web Application)

내려받아 설치하지 않고, 웹 브라우저 위에서 통신을 통해 이용하는 소프트웨어.

## 정의

컴퓨터에서 출발해 네 번 갈라 내려오면 웹 애플리케이션에 도달한다.

| 갈림 | 두 갈래 | 웹 애플리케이션이 속한 쪽 |
|---|---|---|
| 컴퓨터 | 하드웨어 / 소프트웨어 | 소프트웨어 |
| 소프트웨어 | System S/W / Application S/W | Application S/W |
| 애플리케이션 | Standalone / Client·Server | Client/Server |
| 클라이언트/서버 | 설치형 / 비설치형 | **비설치형** |

각 갈래의 기준은 아래와 같다.

- **하드웨어** — CPU·HDD·RAM 처럼 컴퓨터의 물리적 부분
- **소프트웨어** — 컴퓨터 프로그램과 그에 수반되는 문서
- **System S/W** — 하드웨어를 제어하는 소프트웨어 (OS, 드라이버, 임베디드, IoT)
- **Application S/W** — System S/W 를 제외한 응용프로그램 (MS-Office, LoL)
- **Standalone** — 외부 소프트웨어의 도움 없이 설치 후 독자적으로 실행 (MS-Office)
- **Client/Server** — 외부 소프트웨어와 통신해야 실행 (LoL) → [[client-server-model]]
- **설치형** — 프로그램을 내려받아 실행하며 그것이 외부와 통신
- **비설치형** — 내려받지 않고 웹 브라우저를 통해 실행 → **웹 애플리케이션**

### 통신 상대편에 있는 것

브라우저 반대편은 하나가 아니다. 웹 애플리케이션에 필요한 기술은 **웹 서버와 DBMS 사이의 통신**을 위한 것들이다 — 요청을 받아 처리하는 쪽(웹 서버)과 데이터를 보관하는 쪽(DBMS)이 따로 있고, 그 둘을 잇는 기술이 필요하다.

### 석 달 뒤 Day63 — 「웹 서버」 한 칸이 셋으로 갈린다

Day02 가 「웹 서버와 DBMS」로 두 칸을 세운 자리에, Day63(2024-08-26)이 **세 칸을 넣는다.**

| Day02 의 칸 | Day63 의 칸 | 하는 일 |
|---|---|---|
| (브라우저) | Web Browser | 요청을 보내고 받은 것을 그린다 |
| **웹 서버** | [[web-server]] | 파일로 이미 있는 것만 답하고 나머지는 넘긴다 |
| | [[web-application-server]] | 넘겨받아 **코드를 실행한다** |
| | Java Application (내 [[servlet]] 들) | 업무 로직 — DBMS 와 이야기하는 것도 여기다 |
| DBMS | DBMS | 데이터를 보관한다 → [[jdbc]] |

**Day02 의 문장이 틀린 것이 아니라 눈금이 굵었다.** 다만 굵은 채로 두면 한 가지가 어긋난다 — **DBMS 와 이야기하는 것은 웹 서버가 아니라 내 코드**다. Day63 기준으로 「웹 서버와 DBMS 사이의 통신」은 **「내 애플리케이션과 DBMS 사이의 통신」**으로 읽어야 하고, 웹 서버는 그 앞에 서서 정적 요청을 끊어 주는 자리다 → [[static-and-dynamic-content]]

그리고 「비설치형」이라는 성질에 값이 붙는다 — 내 코드가 **남이 만든 서버 안에서 돈다**는 것이다. 실행의 시작점이 내 `main` 이 아니게 되고, 배포가 「파일을 서버가 볼 자리에 놓는 일」이 된다 → [[servlet-container]] · [[web-application-deployment]] · [[tomcat]]

## 왜 중요한가

"웹 개발"이 무엇을 만드는 일인지를 이 분류가 정한다. 웹 애플리케이션은 클라이언트/서버의 한 종류이므로 **서버가 없으면 성립하지 않는다** — 브라우저에 보이는 화면은 절반이고, 나머지 절반은 웹 서버와 DBMS 로 통신 상대편에 있다. 백엔드가 왜 따로 있어야 하는지가 여기서 나온다.

**Day63 기준으로 그 「나머지 절반」이 다시 절반으로 갈린다** — 요청을 받아 주는 쪽(서버 제품)과 그 안에서 도는 내 코드다. 내가 만드는 것은 뒤쪽뿐이고, 그래서 「웹 개발을 배운다」의 큰 부분이 **남이 만든 서버가 내 코드를 언제 어떻게 부르는지 배우는 일**이 된다 → [[servlet]] · [[java-ee]]

비설치형이라는 성질에서 배포의 성격도 갈린다. 설치형은 사용자가 새 버전을 내려받아야 바뀌지만, 웹 애플리케이션은 서버만 갱신하면 다음 접속부터 모두가 새 버전을 본다.

## 경계와 오해

- **웹 애플리케이션 ≠ 웹사이트** — 브라우저로 열린다는 것만으로는 가르지 못한다. 읽기만 하는 정적 페이지도 브라우저로 열리지만, 그 안에서 **기능이 동작하는가**가 애플리케이션이라 부르는 기준이다.
- **비설치형 ≠ 설치가 아예 없다** — 브라우저는 이미 깔려 있어야 한다. 내려받지 않는 것은 **그 애플리케이션**이고, 실행 환경은 있다는 전제다.
- **Standalone ≠ 오프라인 전용** — 기준은 네트워크를 쓰는지가 아니라 외부 소프트웨어에 **의존해야 실행되는가**다.

## 함께 보는 개념

- [[client-server-model]] — 웹 애플리케이션이 속한 상위 구조
- [[web-server]] · [[web-application-server]] — Day02 의 「웹 서버」 한 칸이 갈린 결과
- [[static-and-dynamic-content]] — 그 둘을 가르는 축
- [[servlet]] · [[servlet-container]] — 이 구조에서 내가 쓰는 것과 나를 부르는 것
- [[tomcat]] · [[web-application-deployment]] — 내 코드가 서버로 들어가는 자리
- [[java-ee]] — 이 구조가 기대는 표준

## 출처

- [[2024-05-24-Day01]] — 하드웨어에서 비설치형까지 네 번 갈라 내려오는 분류로 웹 애플리케이션을 정의했다
- [[2024-05-27-Day02]] — 필요한 기술이 웹 서버와 DBMS 간 통신을 위한 것이라는 구성을 배웠다
- [[2024-08-26-Day63]] — 석 달 뒤. **Day02 의 「웹 서버」 한 칸이 웹 서버 · WAS · Java Application 셋으로 갈리는 회차**다. 「웹 애플리케이션 아키텍처 및 구동 원리」 절이 요청의 흐름을 브라우저 → 웹 서버 → WAS → Java Application 으로 적고 응답이 그 역순으로 돌아온다고 했으며, 「WAS와 상호 통신하는 JavaApp을 개발하는 개발자를 웹프로그래밍 개발자라고 한다」·「WAS와 JavaApp이 상호 통신을 하기위해서는 JavaEE 기술명세에 따라 작성을 해야한다」로 **내가 만드는 것이 그 흐름의 어느 칸인지**를 못 박았다. 이 회차 기준으로 Day02 의 「웹 서버와 DBMS 사이의 통신」은 눈금이 굵은 표현으로 읽어야 한다 — DBMS 와 이야기하는 것은 웹 서버가 아니라 내 애플리케이션이다. 다만 웹 애플리케이션을 무엇이라 부르는지(Day01 의 정의)는 이 회차에서 다시 다루지 않고, 「비설치형」이 브라우저 쪽 이야기였던 것에 비해 이 절은 전부 서버 쪽이다
