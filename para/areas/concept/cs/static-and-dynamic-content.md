---
type: concept
id: static-and-dynamic-content
title: 정적 콘텐츠와 동적 콘텐츠 (Static / Dynamic Content)
aliases:
  - 정적 콘텐츠
  - 동적 콘텐츠
  - 정적 자원
  - 동적 자원
  - static content
  - dynamic content
up:
  - 2024-08-26-Day63
  - 2024-08-29-Day66
tags:
  - web
  - 아키텍처
  - 기초
---

# 정적 콘텐츠와 동적 콘텐츠 (Static / Dynamic Content)

**응답이 이미 파일로 있는가, 요청을 받고 나서 만들어지는가.** Day63 은 이 낱말을 정의하지 않고 쓰지만, **웹 서버와 WAS 를 가르는 기준이 이 한 축**이고 그 노트의 표 여섯 줄이 전부 이 축을 다시 말한 것이다 → [[web-server]] · [[web-application-server]]

## 정의

Day63 이 두 쪽에 든 예를 나란히 세우면 기준이 하나로 좁아진다.

| | 정적 콘텐츠 | 동적 콘텐츠 |
|---|---|---|
| Day63 의 예 | HTML · CSS · 이미지 · JavaScript 파일 | 서블릿 · JSP · PHP · ASP.NET |
| 응답이 만들어지는 시점 | **요청 전** — 파일로 이미 있다 | **요청 후** — 그때 만든다 |
| 누가 요청을 처리하나 | [[web-server]] | [[web-application-server]] |
| 같은 URL 에 두 사람이 오면 | **같은 것**을 받는다 | **다른 것**을 받을 수 있다 |
| 서버가 하는 일 | 파일을 찾아 그대로 내보낸다 | 코드를 실행해 결과를 만든다 |

**「같은 URL 에 두 사람이 오면 어떻게 되나」가 실무에서 쓰는 판정법이다** — 답이 같다면 그 응답은 미리 만들어 둘 수도, 남이 대신 답할 수도 있다. 그 성질이 곧 아래 「왜 중요한가」의 전부다 → [[caching]]

## 왜 중요한가

**이 축 하나가 서버를 두 개로 나눈 이유다.** 파일을 찾아 내보내는 일과 코드를 실행하는 일은 필요한 것이 다르다 — 앞쪽은 디스크와 네트워크만 쓰고 뒤쪽은 [[thread]] 를 하나 잡고 DB 연결을 빌린다. 그래서 이미지 100개를 받는 페이지 하나가 **정적 요청 100번 + 동적 요청 1번**이고, 그 100번이 WAS 까지 들어오면 **화면 하나를 그리는 데 쓰레드 100개를 쓴다.** 앞단에서 갈라 놓으면 그 100번이 WAS 에 도달하지 않는다 → [[little-law]] · [[universal-scalability-law]]

**남이 대신 답할 수 있는지가 여기서 갈린다.** 정적 응답은 브라우저·앞단 서버가 사본을 들고 있어도 맞지만, 동적 응답은 **그 사람의 그 시점에만 맞다** — 남의 화면을 캐시해 두고 돌려주면 그것이 곧 사고다. 「캐시를 켠다」가 어떤 자리에서는 설정이고 어떤 자리에서는 정보 유출인 이유가 이 구분에 있다 → [[caching]]

**그리고 「어디를 고쳐야 하나」가 갈린다.** 정적 콘텐츠가 틀렸으면 파일을 바꾸면 되고, 동적 콘텐츠가 틀렸으면 **코드를 고치고 다시 올려야** 한다. 배포의 단위가 갈리는 자리이기도 하다 → [[web-application-deployment]]

### 사흘 뒤 — 정적이던 것이 동적으로 옮겨간다

Day65 의 등록 폼은 `form.html` 파일이었다. Day66 이 그것을 서블릿으로 바꾼다.

```java
@WebServlet("/user/form")
public class UserFormServlet extends GenericServlet {
  @Override
  public void service(ServletRequest req, ServletResponse res) throws ServletException, IOException {
    res.setContentType("text/html;charset=UTF-8");
    PrintWriter out = res.getWriter();

    req.getRequestDispatcher("/header").include(req, res);   // ← 이것 때문에 옮겼다

    out.println("<h1>회원 등록</h1>");
    out.println("<form action='/user/add'>");
    ...
```

`index.html` 도 `HomeServlet` 이 된다. **옮긴 이유가 「내용이 변해서」가 아니다** — 머리말을 끼워 넣으려면 코드가 필요하고, 정적 파일은 그것을 할 수 없다.

**즉 「정적/동적」의 경계가 내용이 아니라 「누가 조립하나」로 옮겨간다.** 화면에 보이는 것은 Day65 와 한 글자도 다르지 않은데 만드는 방법이 바뀌었다 → [[request-dispatcher]]

## 경계와 오해

- **정적 ≠ 변하지 않는 것** — 낱말이 「고정」처럼 읽히지만 파일을 바꿔 올리면 정적 콘텐츠도 바뀐다. 기준은 **변하는가**가 아니라 **요청을 받고 나서 만들어지는가**다. 「자주 바뀌는 페이지라서 동적」으로 읽으면, 하루에 한 번 만들어 파일로 놓아 두는 방식(정적 생성)이 후보에서 아예 사라진다.
- **동적 ≠ 화면이 움직이는 것, 동적 ≠ JavaScript** — 가장 자주 겹쳐 읽는 자리다. Day63 의 표는 **JavaScript 파일을 정적 콘텐츠 쪽에** 두었고 그것이 맞다 — 그 `.js` 는 서버가 파일로 갖고 있어 누구에게나 같은 것이 나가고, 브라우저에서 화면을 바꾸는 것은 **받은 다음의 일**이다. 즉 축이 둘이다: **누가 만드나**(서버 / 브라우저)와 **언제 만드나**(요청 전 / 요청 후). 두 축을 겹치면 SPA 를 「전부 동적」으로 세게 되는데, SPA 의 배포물은 **정적 파일 한 벌**이고 동적인 것은 그것이 부르는 API 응답뿐이다 — 그래서 SPA 는 웹 서버만으로 서비스되고 WAS 는 API 자리에만 필요하다.
- **동적 ≠ DB 를 쓴다** — 시각을 찍어 주는 응답도, 로그인한 사람의 이름을 넣어 주는 응답도 동적이다. 반대로 DB 에서 읽어 온 값을 파일로 내려 놓고 그것을 내보내면 그 응답은 정적이다. **재료가 어디서 왔는지가 아니라 응답이 언제 만들어졌는지**가 기준이다 → [[jdbc]]
- **정적/동적은 콘텐츠의 성질이고 서버의 종류가 아니다** — Day63 의 표는 「주로 사용하는 콘텐츠」 칸으로 두 서버를 갈랐지만, 같은 표의 다른 칸이 「WAS 는 자체적으로 웹 서버 기능도 포함」이라 적는다. 즉 **WAS 도 정적 파일을 준다.** 「정적이면 웹 서버, 동적이면 WAS」는 배치의 관행이고 능력의 경계가 아니다 → [[tomcat]]
- **필기의 「Web Brower : 정적자원을 실행하는 주체이다」는 낱말이 어긋난다** — 브라우저는 HTML·CSS·이미지를 **실행하는 것이 아니라 받아서 그린다.** 실행되는 것은 JavaScript 뿐이다. 그리고 이 문장처럼 정적/동적을 「누가 실행하나」로 읽는 순간 위의 두 축이 곧바로 섞인다 — 브라우저가 JS 를 실행하니 브라우저가 동적 콘텐츠를 만드는 쪽처럼 보이고, 같은 노트의 표가 JS 를 정적으로 분류한 것과 어긋나 보인다. **「누가 실행하나」가 아니라 「응답이 언제 만들어지나」**로 잡아야 두 문장이 함께 맞는다.
- **정적 콘텐츠 ≠ HTML** — 서블릿이 만들어 내보내는 것도 결국 HTML 문자열이다. 확장자나 형식으로는 가를 수 없고, 그래서 브라우저는 자기가 받은 것이 파일에서 온 것인지 코드가 만든 것인지 **모른다.** 이것이 [[client-server-model]] 노트가 Day61 의 콘솔 서버를 「웹의 축소판」이라 적은 자리와 같다 — 그 서버도 화면 문자열을 만들어 보냈다.

## 함께 보는 개념

- [[web-server]] · [[web-application-server]] — 이 축으로 갈린 두 서버
- [[servlet]] — 동적 콘텐츠를 만드는 쪽의 자바 규격
- [[tomcat]] — 한 프로세스 안에 두 쪽을 다 가진 구현
- [[caching]] — 「남이 대신 답할 수 있는가」가 정하는 것
- [[little-law]] · [[universal-scalability-law]] — 정적 요청이 WAS 로 들어올 때 세는 축
- [[web-application]] — 이 콘텐츠들을 합쳐 부르는 이름
- [[web-application-deployment]] — 정적 자원과 코드가 서로 다른 폴더로 갈리는 자리

## 출처

- [[2024-08-29-Day66]] — 사흘 뒤. 「동적으로 HTML관리하기」 절이 `form.html`·`index.html` 을 `UserFormServlet`·`HomeServlet` 으로 옮긴다. **내용이 변해서가 아니라 머리말을 `include` 로 끼우려고 옮긴 것**이라, 이 축의 실질이 「변하는가」에서 「조립이 필요한가」로 이동하는 자리다
- [[2024-08-26-Day63]] — 낱말을 정의하지 않고 **분업의 기준으로만** 쓴다. 웹 서버는 「클라이언트(브라우저)에서 요청한 정적 콘텐츠(HTML, CSS, 이미지, JavaScript 파일 등)를 제공하는 서버」이고 WAS 는 「동적인 콘텐츠를 생성하는 서버」로, 비교 표의 「주로 사용하는 콘텐츠」 칸이 「정적 콘텐츠 (HTML, CSS, JavaScript, 이미지)」 대 「동적 콘텐츠 (서블릿, JSP, PHP, ASP.NET)」다. **JavaScript 파일을 정적 쪽에 둔 것이 이 회차에서 가장 정확한 한 칸**인데, 같은 노트의 Tomcat 구조 절은 「Web Brower : 정적자원을 실행하는 주체이다」·「Java App : Servlet을 수행하며 동적자원을 관리한다」로 축을 「누가 실행하나」로 바꿔 적어 두 설명이 갈린다. 왜 이 구분이 성능·캐시·배포를 정하는지는 다루지 않았고, 「자체적으로 웹 서버 기능도 포함」이라는 칸이 이 구분을 능력의 경계로 읽으면 안 된다는 단서로만 남아 있다
