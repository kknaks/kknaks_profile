---
type: concept
id: server-sent-events
title: SSE (Server-Sent Events)
aliases:
  - SSE
  - Server-Sent Events
  - 서버 전송 이벤트
up:
  - 2024-12-30-Day01
  - 2025-01-02-Day03
  - 2026-09-04-kakao-task
tags:
  - web
  - 통신
  - 실시간
---

# SSE (Server-Sent Events)

**연결을 한 번 열어 두고 서버가 클라이언트에게 계속 밀어 보내는 단방향 통신.** 클라이언트가 물어보는 것이 아니라 **서버가 생길 때마다 보낸다.**

## 정의

```
클라이언트 ──요청 한 번──▶ 서버
          ◀──── 이벤트 ────      (새 것이 생길 때)
          ◀──── 이벤트 ────
          ◀──── 이벤트 ────      연결은 계속 열려 있다
```

**방향이 하나뿐**이다 — 서버 → 클라이언트. 클라이언트가 무언가를 보내려면 **평범한 HTTP 요청을 따로** 해야 한다.

[[polling]] 과 갈리는 축이 「누가 시작하는가」다.

| | 누가 말을 거나 | 연결 | 방향 |
|---|---|---|---|
| 폴링 | 클라이언트가 주기적으로 | 요청마다 새로 | 요청↔응답 |
| SSE | **서버가 생길 때** | **하나를 계속** | 서버 → 클라이언트 |

## 왜 중요한가

**헛걸음이 사라진다.** 폴링은 새 것이 없어도 요청이 오가지만, SSE 는 **보낼 것이 있을 때만** 데이터가 흐른다. 지연도 「다음 요청까지」가 아니라 **생기는 즉시**다 → [[polling]]

**그리고 여전히 HTTP 다.** 별도의 프로토콜이 아니라 응답을 끊지 않고 이어 보내는 방식이라, 기존 서버·인증·방화벽을 그대로 쓴다 — 이것이 양방향 연결과 갈리는 실질적 이점이다 → [[http-message]] · [[socket]]

## 경계와 오해

- **단방향이라는 것이 제약이자 단순함이다** — 채팅처럼 **양쪽이 보내야 하는** 것에는 반쪽만 맞는다. 필기가 「클라이언트가 서버에게 요청을 보내는 방식이 아니」라고 적은 그 성질 때문에, 보내기는 따로 만들어야 한다
- **필기의 「요청 간격을 조절할 수 없다」는 단점이 아니다** — 조절할 간격이 **애초에 없는 것**이다. 폴링의 어휘로 SSE 를 설명하려다 생긴 문장이고, 실제 제약은 「클라이언트가 보낼 수 없다」쪽이다
- **연결이 사용자 수만큼 열린다** — 각 클라이언트가 연결 하나를 계속 붙들므로, 동시 접속자가 곧 열린 연결 수다. 쓰레드당 요청 모델에서는 이것이 한계가 된다 → [[thread]] · [[connection-pool-sizing-formula]]
- **끊기면 다시 붙어야 한다** — 브라우저가 자동 재연결을 하지만, **끊긴 동안 놓친 것**은 따로 챙겨야 한다. 마지막으로 받은 이벤트 id 를 보내 이어받는 장치가 그래서 있다 → [[polling]] 의 커서와 같은 문제다
- **프록시가 응답을 모아 버리면 안 나간다** — 중간의 서버가 응답을 버퍼링하면 이벤트가 뭉쳐서 도착한다. 「코드는 맞는데 실시간이 아닌」 흔한 원인이다

## 함께 보는 개념

- [[polling]] — 반대편(클라이언트가 묻는 쪽)
- [[client-server-model]] — 누가 말을 거는가라는 축
- [[http-message]] — 이 방식이 얹혀 있는 프로토콜
- [[socket]] — 양방향이 필요할 때 가는 곳
- [[thread]] — 연결이 오래 열려 있을 때의 비용
- [[websocket]] — 양방향이 필요할 때의 다음 단계
- [[osiv]] — 장시간 연결과 최악으로 만나는 설정
- [[filesystem-change-notification]] — 밀어 보낼 「변경」을 잡아내는 앞단

## 출처

- [[2026-09-04-kakao-task]] — Rust axum 에서 파일 감시가 잡은 대화 델타를 `/api/stream` SSE 로 밀어 보냄. 애플리케이션이 연결 목록을 직접 드는 스프링 방식과 달리, **경계 있는 broadcast 채널(bounded 256 + Lagged drop)**로 느린 구독자가 밀릴 때 메모리 폭주 대신 이벤트를 버리게 했다. SSE push 를 사진 다운로드보다 먼저 내보내 순서를 잡은 것도 이 회차의 판단
- [[2025-01-02-Day03]] — 사흘 뒤. **구현이 나오고, 그 대가도 함께 나온다.** 스프링에서는 `SseEmitter` 한 객체가 연결 하나이고, `produces = MediaType.TEXT_EVENT_STREAM_VALUE` 로 응답 타입을 선언한다. 열린 연결들을 `CopyOnWriteArrayList<SseEmitter>` 에 모아 두고 `emitter.onCompletion` 으로 끊긴 것을 지우는데 — **연결 목록을 애플리케이션이 직접 관리한다**는 것이 이 방식의 성격이다. 「HTTP/1.1 스펙에 포함되어 있어 별도의 라이브러리 없이 사용 가능하다」는 설명도 여기서 나온다. 그리고 같은 노트가 **이 방식의 진짜 비용**을 짚는다 — 장시간 연결이 OSIV 와 겹쳐 DB 커넥션을 계속 점유한다 → [[osiv]] · [[websocket]]
- [[2024-12-30-Day01]] — 「SSE(Server-Sent Events) 통신 방식」 절이 세 줄로 정의한다 — **「서버에서 클라이언트로 데이터를 보내는 단방향 통신 방식」**이 핵심이고, 「클라이언트가 서버에게 요청을 보내는 방식이 아닌」이 폴링과의 대비다. 다만 세 번째 줄(「요청을 보내는 시간 간격을 조절할 수 없음」)은 폴링의 어휘로 SSE 를 설명한 것이라 단점처럼 읽히는데, 조절할 간격이 없는 것이 이 방식의 성질이다. 이 회차의 실제 구현은 SSE 가 아니라 폴링으로 갔고, SSE 는 선택지로만 소개된다 → [[polling]]
