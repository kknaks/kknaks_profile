---
type: concept
id: do-while-loop
title: do~while 문
aliases:
  - do while
  - do~while
  - 두와일
up:
  - 2024-06-05-Day09
  - 2024-06-11-Day11
tags:
  - java
  - 제어문
  - 문법
---

# do~while 문

반복문의 하나. `do` 블록을 **먼저 실행하고** `while` 의 조건을 판단해 다시 실행할지 끝낼지 정한다.

## 정의

```java
do {
    // 루프에서 실행할 코드
    // 첫 번째는 무조건 실행
    // 이후 while 문의 참/거짓에 따라 실행
} while (조건식);
```

조건 판단이 **블록 뒤에** 있으므로 **최소 한 번은 실행된다.** [[while-loop]] 은 조건이 처음부터 거짓이면 한 번도 실행되지 않는데, 그 점이 다르다. 그래서 고르는 기준이 반복 횟수로 정리된다.

| 최소 반복 횟수 | 쓸 것 |
|---|---|
| 0 번 이상 | `while` |
| 1 번 이상 | `do ~ while` |

끝에 세미콜론이 붙는다 — `while (조건식);` 이다.

## 사용 예시

메뉴를 보여주고 입력을 받는 흐름에 맞는다. **입력을 받아 봐야 끝낼지 알 수 있기** 때문이다.

```java
int menuNo;
do {
    System.out.print("> ");
    menuNo = scanner.nextInt();
    // ... 처리
} while (menuNo != 6);
```

`menuNo` 를 루프 **밖에서** 선언한 것에 주의한다. 조건식이 그 값을 봐야 하므로, 블록 안에서 선언하면 조건식에서 보이지 않는다.

## 왜 중요한가

**"일단 한 번 해 보고 판단하는" 흐름이 실제로 자주 나온다.** 사용자 입력, 메뉴, 재시도 같은 것은 값을 얻기 전에는 조건을 평가할 수 없다. `while` 로 쓰려면 루프 앞에 같은 코드를 한 번 더 두거나 조건을 억지로 참으로 시작해야 하는데, `do~while` 은 그 중복이 없다.

## 경계와 오해

- **`while` 과 실행 횟수가 다르다** — 조건이 처음부터 거짓일 때 `while` 은 0번, `do~while` 은 1번 실행한다. 이 차이가 곧 선택 기준이다.
- **최소 한 번 실행이 항상 장점은 아니다** — 종료 판단이 조건식 한 줄에 담기지 않으면 `do~while` 의 이점이 사라진다. 이 필기의 메뉴 루프가 그랬다 — 종료 조건이 "6번을 입력했나"(`menuNo != 6`)에서 **"고른 메뉴가 종료인가"**로 바뀌자, 값을 배열과 견주기 전에는 판단할 수 없어서 `while (true)` + `break` 로 갈아탔다 → [[while-loop]] · [[break-continue]]
- **세미콜론을 빠뜨리기 쉽다** — `} while (조건)` 뒤에 `;` 가 필요하다. 다른 제어문 블록에는 없는 것이라 빠뜨리면 문법 오류가 난다.
- **조건에 쓸 변수는 밖에서 선언한다** — 블록 안에서 선언한 변수는 조건식에서 보이지 않는다.
- **`continue` 는 아래쪽 조건식으로 간다** — `while` 처럼 위로 올라가는 것이 아니라 블록 끝의 조건 검사로 내려간다. 결과는 같지만 흐름을 읽을 때 방향이 반대다 → [[break-continue]]

## 함께 보는 개념

- [[while-loop]] — 조건을 앞에서 보는 형태. 실행 횟수가 갈린다
- [[for-loop]] — 횟수가 정해진 반복
- [[break-continue]] — 조건식 대신 본문에서 반복을 끊는 방법
- [[switch-statement]] — 루프 안에서 값을 분기하는 짝
- [[standard-input]] — `do~while` 이 잘 맞는 입력 처리
- [[variable]] — 조건에 쓸 변수의 선언 위치가 문제가 되는 자리

## 출처

- [[2024-06-05-Day09]] — `do` 를 먼저 실행하고 `while` 조건으로 반복을 정한다는 것, 메뉴 입력 루프에 적용하는 것을 배웠다
- [[2024-06-11-Day11]] — "0번 이상 반복이면 `while`, 한 번 이상 반복이면 `do~while`" 이라는 선택 기준, 그리고 종료 조건이 복잡해지자 같은 메뉴 루프를 `while (true)` + `break` 로 바꾸게 된 것을 배웠다
