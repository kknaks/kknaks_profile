---
type: reference
id: 2024-08-19-Day58
title: Day58 실습프로젝트(JDBC 캡슐화)
summary: MyBatis 의 SqlSession 을 본떠 Connection 을 품고 SQL 문자열과 값을 받아 실행하는 클래스를 만들었다. insert(String sql, Object... values) 하나가 PreparedStatement 준비·in-parameter 바인딩·executeUpdate 를 감싸고 update·delete 는 그 메서드에 그대로 위임한다. mybatis 구조와 selectList 는 소제목만 남고 채워지지 않았다
author: 이건학
date: 2024.08.19
tags:
- bitcamp
- SQL
stack: [Java, JDBC, MyBatis, MySQL]
links: [2024-08-16-Day57, 2024-08-20-Day59]
---

# Sql 세션 만들기
## mybatis 구조 살펴보기

## insert문 만들기

```java
  public int insert(String sql, Object... values) throws Exception{
    try(PreparedStatement stmt = con.prepareStatement(sql)){
      
      int inparameterIndex = 1;
      for(Object value : values) {
        stmt.setString(inparameterIndex++,value.toString());
      }
    return stmt.executeUpdate();
    }
  }
  
  public int update(String sql, Object... values) throws Exception{
    return insert(sql,values);
  }
  
  public int delete(String sql, Object... values) throws Exception{
    return insert(sql,values);
  }
```

## update문 만들기

```java
  public int update(String sql, Object... values) throws Exception{
    return insert(sql,values);
  }
```

## delete문 만들기

```java
  public int delete(String sql, Object... values) throws Exception{
    return insert(sql,values);
  }
```

## selectList 만들기

```java

```
