# Product Log

> 제품 단위 통합 변경 로그. baseline, decision, spec, work 변경 이력을 한 곳에 모은다.

| Date | Type | IDs | Summary | Links |
|---|---|---|---|---|
| 2026-08-07 | scaffold | - | 제품 문서 스캐폴딩 생성 (README, log, 00-baseline / 10-decision / 20-spec / 30-work / 40-architecture index). `products/README.md` 제품 표에 `kknaks-agents` 행 추가 | [README](README.md) |
| 2026-08-07 | baseline-add | KAG-BL-001 | provider-neutral LLM runtime 라이브러리 baseline 등록. 특정 agent 제품 종속 문제, provider adapter 경계, 외부 tool 주입, server-owned loop/session/context/compaction/skill, Codex CLI subprocess 첫 실험, 운영 사례와 clean-room 관찰 범위, 미결 질문 11건 | [KAG-BL-001](00-baseline/baseline-001-provider-neutral-llm-runtime.md) |
| 2026-08-08 | decision-add | KAG-DEC-001 | Runtime 디렉터리 구조와 의존 경계 제안(작성 시점 `proposed`, 당시 내용은 전부 권고안). flat / 책임별 package / ports-adapters 3안 비교 후 **책임별 package 권고**(나머지 둘 비권고), 확정 시 채택. repo 상위 구조(`src`·`tests`·`examples`·`docs`), package 이름 `kknaks_agents` 제안, package 내부 8개 책임(core·process·providers·tools·sessions·context·skills·runtime), 4단 의존 계층과 금지 의존 표, provider 종속 코드의 `providers/` 격리 규칙, CLI/queue/web을 라이브러리 바깥 계층에 두자는 권고. 미결 8건 | [KAG-DEC-001](10-decision/decision-001-runtime-directory-boundaries.md) |
| 2026-08-08 | status-change | KAG-BL-001 | decision 검토 시작에 따라 baseline `raw` → `reviewing`. `links.decisions`에 KAG-DEC-001 추가. decision이 proposed인 동안 accepted로 올리지 않음 | [KAG-BL-001](00-baseline/baseline-001-provider-neutral-llm-runtime.md) |
| 2026-08-08 | mapping-change | KAG-BL-001, KAG-DEC-001 | baseline index(Status·Decision·Next), decision index(결정 로그·미결 사항·Next), 제품 README(현재 상태 표·package 이름 주석·최근 로그) 갱신 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-08 | status-change | KAG-DEC-001 | 사용자 리뷰 확정 — decision `proposed` → `accepted`. Option B(책임별 package) 채택, A(flat)·C(ports/adapters) 기각. 본문의 권고/비권고 어투를 확정 어투로 정리하고 확정 문구(확정일·supersede 조건·여전히 미결인 Open Questions와 Out 범위)를 명시. import package 이름 `kknaks_agents` 확정, PyPI 배포명은 OQ-1로 계속 미결 | [KAG-DEC-001](10-decision/decision-001-runtime-directory-boundaries.md) |
| 2026-08-08 | status-change | KAG-BL-001 | decision 반영에 따라 baseline `reviewing` → `accepted`. 반영 범위는 디렉터리 구조와 의존 방향까지이고, 나머지 Possible Direction과 Open Questions는 이후 decision 대상으로 남는다 | [KAG-BL-001](00-baseline/baseline-001-provider-neutral-llm-runtime.md) |
| 2026-08-08 | mapping-change | KAG-BL-001, KAG-DEC-001 | accepted 상태 정합 — baseline index(Status accepted·Next), decision index(서문·Result 열·Next), 제품 README(현재 상태 표·package 이름 주석·최근 로그) 갱신. 다음 단계는 동작 구조 decision이며 문서는 아직 만들지 않았다 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-08 | decision-add | KAG-DEC-002 | 최소 headless turn runtime 동작 구조 제안(`proposed`, 본문 전체가 권고안). 불투명 단일 `run()` / 명시적 state transition / middleware-hook pipeline 3안을 재현성·보안·테스트 가능성·초기 복잡도로 비교해 **명시적 state transition을 가진 deterministic turn loop 권고**(나머지 둘 비권고, hook은 후속 확장 후보로 보존). turn phase 전이(진행 phase 9 + 종료 state 4)와 정상 tool loop sequence, turn 시작 snapshot 5항목(책임 수준), side effect 순서 12단계와 불변식 3개(기록 먼저·검증 먼저·step당 provider 1회), final/tool call 혼재 판정과 다중 tool call 직렬 실행 규칙, 반복 진입 조건 5개와 step 회계 단위, 종료 원인 10종의 terminal/recoverable 판정(되먹임은 tool call 단위 오류만), 추후 확장 11항목과 완전 제외 7항목 분리. `query/` 디렉터리를 만들지 않고 `runtime` 책임으로 흡수, KAG-DEC-001의 `runtime`↛`providers` 경계 유지. 미결 9건 | [KAG-DEC-002](10-decision/decision-002-turn-runtime-flow.md) |
| 2026-08-08 | mapping-change | KAG-BL-001, KAG-DEC-002 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-002 추가 및 Next 갱신(상태는 `accepted` 유지), baseline index Decision 열·Next, decision index(서문·결정 로그 행·미결 사항·Next), 제품 README(현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-BL-001의 accepted는 그대로 두고 되돌리지 않음. spec/work/architecture 문서와 DEC-003은 생성하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-08 | decision-change | KAG-DEC-002 | 검수 보정 (상태 `proposed` 유지, OQ-8 규칙 변경 없음). ① 개수 정합 — 다이어그램의 비종료 노드는 9개인데 본문·index·log가 “상태 6개”로 적던 불일치를 수정. §0에 **진행 phase(9) / 종료 state(4)** 용어 구분 표를 신설하고 §1 제목·읽는 법·Options trade-off·Rationale·Scope와 decision index Result 열, log 본문의 개수·용어를 전부 다이어그램에 맞춤. Rationale 리스크에 이 불일치가 실제로 났다는 사실을 명시. ② §5 공통 규칙에 “되먹임은 실행 표면을 넓히지 않는다” 추가 — §3 snapshot이 turn 내내 불변이므로 policy 거부를 model에 되먹여도 허용 tool subset과 권한은 그대로이고, model이 거부를 근거로 더 넓은 권한·미등록 tool을 요구해도 §4 I2 검증을 다시 통과하지 못한다 | [KAG-DEC-002](10-decision/decision-002-turn-runtime-flow.md) |
| 2026-08-08 | status-change | KAG-DEC-002 | 사용자 리뷰 확정 — decision `proposed` → `accepted`. Option B(명시적 phase/state transition을 가진 deterministic turn loop) 채택, A(불투명 단일 `run()`)·C(middleware/hook pipeline) 기각(C는 §6 추후 확장 후보로 보존). 본문의 권고/비권고 어투를 채택/기각 어투로 정리하고 확정 문구(확정일 2026-08-08·supersede 조건·여전히 미결인 Open Questions 9건과 Out 범위)를 명시. 진행 phase 9 + 종료 state 4, side effect 순서 12단계와 불변식 3개, tool call 직렬 실행, 종료 원인 10종 판정, snapshot 5항목은 내용 변경 없이 그대로 확정. OQ-1~OQ-9는 해결 처리하지 않았고 OQ-8의 규칙·owner(사용자)도 그대로 유지 | [KAG-DEC-002](10-decision/decision-002-turn-runtime-flow.md) |
| 2026-08-08 | mapping-change | KAG-BL-001, KAG-DEC-002 | accepted 상태 정합 — baseline 본문 Next(반영 범위를 동작 흐름까지 확장, 상태는 `accepted` 유지), baseline index(Decision 열·Next), decision index(서문·결정 로그 Status/Result·Next), 제품 README(현재 상태 표·서문·최근 로그) 갱신. KAG-DEC-001·KAG-BL-001의 기존 accepted 내용은 되돌리지 않음. 다음 게이트는 공개 계약 decision이며 KAG-DEC-003·spec·work·architecture 문서는 생성하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-add | KAG-DEC-003 | core package 계약 경계 제안(`proposed`, 본문 전체가 권고안). 공개 계약을 디렉터리 하나씩 내려가기로 해 이번 대상은 `core/` 하나. 단일 `contracts.py` / 계약 관심사별 평면 module / 도메인별 중첩 package+`ports` 3안을 초기 학습 비용·순환 의존 방지·검색성·공개 표면 통제·확장 비용으로 비교해 **평면 module 분리 권고**(나머지 둘 비권고, 중첩 package는 module 승격 경로로 보존). core 소유 계약 범주 10종(K1~K10)과 소유하지 않는 것 10종, 파일 후보 트리(`ids`·`errors`·`content`·`capabilities`·`execution`·`tooling`·`requests`·`responses`·`events`·`protocols` + `__init__`), 파일별 단일 역할·대표 타입 범주·producer/consumer 표, core 내부 module 간 6단 총순서(같은 tier 상호 참조 금지)로 순환을 tier 비교로 환원하고 model 응답 event가 공통 응답을 단방향 참조하도록 `events`를 `responses`보다 위 tier에 배치(저장용 응답 형태 중복 정의 금지 — KAG-DEC-002 I1의 손실 없는 기록), 값 계약과 행동 protocol의 구분 규칙("소비자가 구현 package를 import할 수 없을 때만 protocol" — `protocols.py` 후보 2건 = 필수 1건(model 호출 경계) + 권고 1건(tool handler). session event 저장 경계는 KAG-DEC-001이 `runtime → sessions`를 허용하므로 기준 미통과로 core에서 제외하고 `sessions` 상세 decision으로 이관. 나머지 후보 7건은 core protocol 아님)과 core에 두지 않는 구현 종류, `core/__init__.py` 재수출 3안 중 선별 재수출 권고, KAG-DEC-001 OQ-2를 (a) core 표면 / (b) package-root 표면 / (c) 안정 API 약속 셋으로 분할해 (a)만 이번에 다룸, 호환성·versioning 원칙 7개 — 선택적 값 추가(V2)만 비파괴적이고 새 event/오류/content 판별값 추가(V3′)는 V3의 fail-closed 때문에 기존 소비자를 멈추므로 version/capability 협의 없이는 breaking으로 취급, KAG-DEC-002 진행 phase 9 ↔ core 계약 매핑과 중복 4건·누락 5건 검증. 진행 phase 이름을 코드 식별자로 승격하지 않음. 미결 8건 | [KAG-DEC-003](10-decision/decision-003-core-contract-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-003 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-003 추가·`updated_at` 갱신·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음), baseline index Decision 열·Next, decision index(서문·결정 로그 행·미결 사항 3행과 OQ-2 분할 주석·Next), 제품 README(서문·현재 상태 표·package 이름 주석·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted는 그대로 두고 되돌리지 않음. spec/work/architecture 문서와 KAG-DEC-004는 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-change | KAG-DEC-003 | 코디네이터 검수 보정 3건 (상태 `proposed` 유지, OQ-1·OQ-5는 사용자 결정 사항이라 손대지 않음). ① §4.1 기준 적용 오류 — session event 저장 경계를 core 권고에 넣었으나 KAG-DEC-001 §4가 `runtime → sessions`를 허용하므로 “소비자가 구현 package를 import할 수 없을 때만” 기준을 통과하지 못한다. core 후보에서 제외하고 `sessions` 상세 decision으로 이관, `protocols.py` 후보를 3→2건(필수 1 + 권고 1)으로 정정하고 §3.1 protocols 행·OQ-2·index·log 수치를 맞춤. “교체 지점인 것”과 “core protocol이 필요한 것”이 다르다는 설명 추가. ② tier 배치 오류 — `events`를 `responses`와 같은 tier에 두면 상호 참조 금지 때문에 저장용 응답 형태를 중복 정의하게 되고, 이는 KAG-DEC-002 I1의 손실 없는 기록을 위협한다. model 응답 event가 공통 응답을 참조함을 명시하고 `events`를 T4로 올려 총순서를 5단→6단으로 재정합(mermaid·tier 표·효용 항목·Scope·중복 점검 3→4건). ③ V2/V3 모순 — “추가는 열려 있다”와 unknown fail-closed가 충돌했다. V2를 선택적 값 추가로 좁히고 V3′를 신설해 새 event/오류/content 판별값 추가는 version·capability 협의 없이는 breaking으로 취급한다고 명시(원칙 6→7개) | [KAG-DEC-003](10-decision/decision-003-core-contract-boundaries.md) |
| 2026-08-09 | decision-add | KAG-DEC-004 | process package 실행 격리 경계 제안(`proposed`, 본문 전체가 권고안). 공개 계약을 의존 그래프 아래에서 위로 내려가기로 해 `core`(L0) 다음 대상은 `process`(L1) 하나. 단일 runner module / 관심사별 평면 module / backend·플랫폼별 중첩 / `providers`로 접기 4안을 초기 복잡도·보안 규칙의 단독 검증 가능성·외부 프로세스 없는 테스트 가능성·경계 유지력·확장 비용으로 비교해 **평면 module 분리 권고**(단일 runner와 접기는 비권고, 대상/플랫폼별 중첩은 기각 — 대상별 중첩은 KAG-DEC-001 §5 위반이고 플랫폼 분기는 OS 호출 파일 안의 분기로 충분). process 소유 책임 8종(P1~P8: 실행 요청 값·환경 격리 규칙·호출 단위 한도 집행·취소 관찰·스트림 분리 수집·종료와 정리·구조화 결과·진단 축약)과 소유하지 않는 것 9종(실행 파일명·flag·wire 형식·exit code 의미 판정·provider 오류 변환·turn 한도 판정·재시도·HTTP 격리·로깅 destination). **최소 보안 경계 13항목 판정** — argv 목록 실행(문자열 재조립 금지, 탐색 경로 의존을 전제로 삼지 않음), shell command string 경로를 열지 않음(환경 allowlist·신호 전달·escalation 세 항목이 동시에 무력화될 수 있어 별도 decision 없이는 열지 않고 여는 조건 3개를 명시), cwd 필수 명시(부모 cwd 암묵 상속 금지), stdin 비상속(요청 payload만 쓰고 즉시 EOF), 환경 이름 allowlist(빈 목록 = 전면 차단이지 전면 허용이 아님), 호출 단위 시간 상한 필수, 외부 취소 관찰, stdout/stderr 분리 고정(합치는 옵션을 두지 않음), 종료 escalation(협조적 종료 → 유한 유예 → 강제 → 회수 확인), exit code/신호의 원시 보고(성패 해석 금지), 자식 process 그룹 격리와 회수, 스트림별 byte 상한(stdout 초과는 실패 사유 · stderr 초과는 잘라내고 표시), decode 경계(stdout은 byte 그대로 · stderr만 안전 decode). 각 항목에 “미지정 시 fail-closed 해석”을 붙여 빠뜨린 항목이 열린 기본값으로 굴러가지 않게 함. 파일 후보 트리 7개 + `__init__.py`(`environment`·`redaction`·`request`·`outcome`·`spawn`·`streams`·`runner`)와 각 파일↔보안 항목 1:1 대응, 파일별 단일 역할·대표 타입/행동 범주·producer/consumer, process 내부 5단 총순서(T0 순수 규칙 → T1 값 → T2 OS 경계 → T3 수집 → T4 집행)로 **OS 의존을 한 파일에 격리해 실제 프로세스 없이 lifecycle을 검증 가능하게** 함. 실행 lifecycle 6단계(입력 준비 → spawn → I/O 수집 → 한도·취소 감시 → 종료·정리 → bounded result)와 불변식(격리 조건 미비 시 spawn하지 않음 · 어떤 경로로도 정리를 건너뛰지 않음 · 부분 출력은 보존하되 불완전 표시). turn / provider 호출 / subprocess 실행 3층 책임 분리 — **process가 돌려준 시간 초과는 turn 한도종료가 아니며 process는 turn 종료 state도 provider 오류도 만들지 않는다**(KAG-DEC-002 §5의 변환은 `providers` → `runtime`), 상황별 소유권 10건과 fail-closed 원칙 6개(F1 사유 구분 · F2 불완전 출력 위장 금지 · F3 미지정 한도를 무제한으로 읽지 않음 · F4 미지정 allowlist를 전면 허용으로 읽지 않음 · F5 정리 실패 은폐 금지 · F6 해석 금지). KAG-DEC-002의 phase 9·종료 state 4·snapshot·side effect 순서·step 회계와 겹치지 않음을 항목별로 확인. `process/__init__.py` 최소 재수출 권고와 package-root 미노출, 테스트 seam 4종(OS 경계 대체·순수 규칙 직접 호출·상한 단독 검증·최소한의 실제 프로세스 검증), 관측·민감정보 원칙 7개(인자·환경 값 원문 금지, secret은 인자 대신 환경/입력, 축약은 결과 직전 한 번, stdout은 축약 대상 아님, 민감 목록은 주입). KAG-DEC-003은 `proposed` 입력으로만 참조하고 `core` 파일 배치와의 연결은 범주 수준으로만 둠. KAG-DEC-001 OQ-4(process를 유지할지 `providers`로 접을지)는 닫지 않고 그대로 유지. 미결 10건 | [KAG-DEC-004](10-decision/decision-004-process-boundaries.md) |
| 2026-08-09 | decision-change | KAG-DEC-004 | 코디네이터 검수 보정 3건 (상태 `proposed` 유지, 권고 Option B·Open Questions 10건·OQ owner는 변경 없음). ① **lifecycle 모델 오류** — I/O 수집과 한도·취소 감시를 `S3 → S4` 순차 단계로 그려 “EOF를 기다린 뒤 timeout을 본다”는 오해를 만들었고, 그렇게 구현하면 출력을 멈추고 매달린 자식을 끊지 못한다. §4를 **순차 4국면 + 동시 감독 구간 1**(총 5국면)로 다시 그려 stdin 전달·stdout drain·stderr drain·시간 상한·취소 관찰·자식 종료 관찰이 spawn 직후부터 동시에 살아 있고 먼저 발생한 종료 사건 하나가 구간을 끝내도록(first-wins) 정정. 순차 구현이 깨뜨리는 것 3가지를 명시(멈춘 자식에 상한이 평가되지 않음 · 한쪽만 읽으면 파이프 버퍼가 차서 자식이 멈추는 교착 · stdin 쓰기를 상한 밖에 두면 우회 경로가 생김), stderr 상한 초과와 stdin 완료는 종료 사건이 아니고 자식 종료도 잔여 drain 전에는 구간을 끝내지 않음을 추가. 동시성의 실행 모델(sync/async·라이브러리)은 계속 미확정. mermaid·읽는 법(5→6줄)·§3.1 트리 주석·§3.2 `streams`/`runner` 행·§6 대조 표·Scope·index 수치를 6단계 → 5국면으로 정합. ② **Option A 평가 과장** — “단독 테스트할 자리가 없다 / 실제 프로세스 없이 lifecycle을 돌릴 수 없다 / 모든 검증이 integration이 된다”는 사실이 아니다. 단일 module에서도 순수 함수 직접 호출과 OS 호출 주입·치환으로 같은 검증이 가능하다. 비교 축을 “단독 검증 가능성”에서 **“보안 규칙 경계의 명시성과 OS 대체 지점의 안정성”**으로 바꾸고, B의 이점을 “불가능을 가능하게 한다”가 아니라 **검증·대체 지점이 규약이 아니라 import 경로로 유지된다**로 재서술. Options 서두·A Cons·B Pros·trade-off 문단·§3.3 효용 1·Rationale 판단 기준 2와 대안 비교를 함께 수정. ③ **`process`의 위상 과장과 1:1 표현** — “외부 세계와 실제로 접촉하는 유일한 자리”를 **“subprocess로 OS와 접촉하는 지점을 격리하는 자리”**로 좁히고 provider transport·host tool handler도 외부 접점임을 명시. 13개 보안 항목과 7개 module이 개수로 1:1이 아니므로(한 파일이 여러 항목을 소유하고 §2.12 상한은 `request` 값과 `streams` 집행으로 나뉜다) “1:1 대응”을 **“모든 항목에 소유 파일이 하나 이상 있고 소유할 항목이 없는 파일은 만들지 않는다”**로 정정하고 §3.1·§Options·Rationale·Scope·index를 같은 표현으로 통일 | [KAG-DEC-004](10-decision/decision-004-process-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-004 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-004 추가·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음. Codex CLI 격리 옵션을 묻는 Open Question 4는 여전히 미결이라고 명시), baseline index Decision 열·Next, decision index(서문·결정 로그 행·미결 사항 3행·Next에 남은 raw 작성 순서 `providers` → `tools` → `sessions` → `skills` → `context` → `runtime` 명시), 제품 README(서문·현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted와 KAG-DEC-003의 proposed 본문은 그대로 두고 되돌리거나 승격하지 않음. spec/work/architecture 문서와 KAG-DEC-005는 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-add | KAG-DEC-005 | providers package 변환 경계 제안(`proposed`, 본문 전체가 권고안). 의존 그래프 아래에서 위로 내려가는 순서에서 `core`(L0)·`process`(L1) 다음 대상은 `providers`(L2) 하나 — `core`와 `process` 둘 다 참조하는 유일한 package라 아래 둘이 정리된 직후가 가장 싸다. provider별 단일 module 평면 배치 / 공용 module + provider별 하위 package / transport별 중첩 / 공용 base class 상속 4안을 초기 복잡도·provider 종속 정보의 국소성·transport 이질성 수용·공용화의 안전성·두 번째 adapter 추가 비용으로 비교해 **공용 module + provider별 하위 package 권고**(단일 module 평면 배치는 비권고, transport별 중첩과 상속 계층은 기각 — 중첩은 한 provider가 두 transport를 가질 때 봉인 단위가 쪼개지고, 상속은 한 adapter의 사정이 다른 adapter 코드에 반영되어 KAG-DEC-002가 middleware/hook을 기각한 것과 같은 구조의 문제가 된다). providers 소유 책임 9종(A1~A9: 요청 변환·호출 조건 결정·transport 1회 수행·출력 복원과 검증·공통 응답 변환·capability 선언·실패의 provider 의미 변환·종속 식별자 격리 보관·원문과 계측의 경계 있는 반출)과 소유하지 않는 것 12종(loop·tool 실행·기록·context·skill·응답 형식 판정과 turn 종료·취소 판정·격리 집행·재시도·provider 내장 harness·로깅 destination·조립). **공용/전용 판정 16항목**과 판정 기준 “두 번째 adapter가 생겨도 같은 규칙이어야 하는 것만 공용이고 provider마다 달라도 되는 것은 전부 adapter 안” — 공용은 실패 분류 매핑·복원 실패와 unknown 판별값의 표현·공통 protocol 투영·진단 축약 **넷뿐**이고, 실행 파일명·flag·제품명·모델명·wire 형식·환경 allowlist 값·한도 값·usage 정규화·capability 선언 값은 전부 adapter 폴더 안에 봉인. 파일 후보 트리 = 공용 2(`failures`·`text_protocol`) + adapter 5(`capability`·`invocation`·`encode`·`decode`·`adapter`) + `__init__.py` 2, 파일별 단일 역할·대표 타입/행동 범주·producer/consumer. 공용 2단·adapter 5단 총순서에 더해 **방향 규칙 3개** — 공용 → adapter 금지(위반이 package 밖 grep으로 잡히지 않아 방향 규칙이 유일한 방어), adapter 상호 참조 금지, `process`는 adapter 안에서만 참조(`providers → process`는 예외 방향이지 기본 경로가 아니다). 한 번의 provider 호출 lifecycle **6국면**(요청 수용과 capability 대조 → provider 입력 조립 → transport 호출 정확히 1회 → **결과의 완전성을 복원보다 먼저 확인** → wire 복원과 검증 → 공통 응답 1개 또는 provider 실패 1개)과 불변식 8개(PI1~PI8: 결과는 정확히 하나 · 불완전 출력은 복원하지 않음 · 부분 복원 금지 · 호출당 transport 1회 · 선언을 넘는 요청을 낮춰 실행하지 않음 · 원문은 불투명 값으로만 · **복원한 tool call은 실행 요청이지 실행이 아님** · adapter는 아무것도 기록하지 않음). 실패 4계열(호출 이전·transport·복원·계약)과 상황별 소유권 17건 — **adapter는 “이 호출이 실패했다”까지만 말하고 turn 종료 state를 만들지 않으며**(KAG-DEC-004 §5.1이 넘긴 변환의 수신자가 여기다), 시간 초과는 provider 오류이지 turn 한도종료가 아니고, 중복 tool call 식별자와 tool call·텍스트 혼재는 감추지 않고 그대로 복원해 `runtime` 판정에 넘긴다. fail-closed 원칙 6개(PF1~PF6). capability는 값이며 provider를 식별하지 않고 정책을 담지 않고 turn에 고정된다(CP1~CP3), 축의 후보만 적고 확정 목록은 만들지 않음, 이름 기반 분기 방지는 contract test로 관찰, native tool call 미지원 시 **fail-closed가 기본값**이고 공통 protocol 투영은 세 조건(capability에 드러남 · 투영 실패는 판정 불가이지 final 후보가 아님 · 동등성 주장 금지) 아래에서만 허용. subprocess adapter와 향후 HTTP/raw API adapter의 관심사 대조로 “두 번째 adapter가 붙을 때 열리는 것은 새 폴더 하나”임을 확인. `providers/__init__.py` 무-재수출 권고(adapter별 선택적 의존·import 줄에 provider 이름이 남음·새 adapter가 공용 파일을 열지 않음), 새 provider 구현자의 확장 표면은 **model 호출 Protocol 1개 + capability 선언 1개뿐**(상속할 기반 클래스·registry·plugin 진입점 없음), contract test seam 5종(transport 대체 · 변환 고정 입력 · 공용 규칙 직접 호출 · **provider-agnostic 계약 suite** · 최소한의 실제 provider 검증)에서 계약 suite가 상속 없이 “모든 adapter가 같은 순서를 따른다”를 얻는 자리임을 명시. 관측·민감정보 원칙 8개(PR1~PR8). KAG-DEC-003·KAG-DEC-004는 `proposed` 입력으로만 참조하고 연결을 파일명이 아니라 범주 수준으로만 둠. KAG-BL-001 OQ-4(실행 옵션·출력 protocol)·OQ-5(capability 표준)·OQ-8(원문 보관)과 KAG-DEC-002 OQ-2(재시도)는 닫지 않음. 미결 11건 | [KAG-DEC-005](10-decision/decision-005-provider-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-005 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-005 추가·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음. Codex CLI 실행 옵션을 묻는 Open Question 4와 capability 표준을 묻는 Open Question 5가 여전히 미결이라고 명시), baseline index Decision 열·Next, decision index(서문 문단·결정 로그 행·미결 사항 4행·Next에 남은 raw 작성 순서 `tools` → `sessions` → `skills` → `context` → `runtime` 명시), 제품 README(서문·현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted와 KAG-DEC-003·KAG-DEC-004의 proposed 본문은 그대로 두고 되돌리거나 승격하지 않음. spec/work/architecture 문서와 KAG-DEC-006은 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-add | KAG-DEC-006 | tools package 등록·허용·검증·실행 경계 제안(`proposed`, 본문 전체가 권고안). 의존 그래프 아래에서 위로 내려가는 순서에서 `providers`(L2) 다음 대상은 같은 L2의 `tools` — KAG-DEC-005 PI7이 “복원한 tool call은 실행 요청이지 실행이 아니다”라며 그 요청의 수신자를 이 문서로 넘겼다. 단일 `registry.py` / 관심사별 평면 module / tool 분류별 중첩 / 기반 클래스 상속 + 자기 등록 4안을 초기 복잡도·판정 축의 명시성·우회 경로 부재·등록 출처 통제·확장 비용으로 비교해 **관심사별 평면 module 권고**(단일 module은 비권고, 분류별 중첩과 상속·자기 등록은 기각 — read/write와 namespace는 정책 판정의 입력 값이자 배치 축이지 권한 축이 아니고 이 package에는 tool 구현 자체가 살지 않으며, import 부작용이나 entry-point로 registry가 채워지면 “무엇이 등록됐는가”가 import 그래프·설치 상태의 함수가 되어 KAG-DEC-001·KAG-DEC-002가 제외한 dynamic discovery·plugin 자동 설치와 관찰 가능성 면에서 같아진다). tools 소유 책임 9종(T1~T9: 등록 수용 · 등록 시점 가드 · registry revision · turn 허용 subset 계산 · model 공개 표면 투영 · 입력 schema 검증 · 접근/승인 정책 판정 · handler 실행과 실행 context 주입 · 결과 정규화)과 소유하지 않는 것 13종(model 호출·wire · turn loop와 tool call 순서 · session 기록과 되먹임 · 요청 조립과 결과 축약 · dynamic discovery/MCP/plugin · 역할·권한 모델 · 승인 저장소와 UI · 취소 판정과 한도 · transport · 로깅 destination · 조립). **등록 registry / turn 허용 subset / model 공개 표면 세 값의 분리**(registry ⊇ 허용 subset ⊇ 공개 표면이고 셋을 잇는 계산은 하나)와 분리 규칙 8개(RG1~RG8: tool 출처는 언제나 L4 호스트의 명시적 등록 · 등록과 허용은 다른 값 다른 시점 · snapshot은 이름·버전·revision·공개 정의만 고정하고 handler·secret·권한 내부는 model 노출 값에 넣지 않음 · **노출 목록과 실행 허용 판정은 같은 계산 결과** · 접근 선언 무기본값이고 미선언은 “누구나”가 아니라 “아직 판단하지 않음”이라 등록에서 거부 · 조회·계산 실패를 빈 목록으로 접지 않음 · handler는 값이 아니라 프로세스 지역 자원이라 직렬화하지 않고 프로세스 경계는 이름·버전·revision만 넘김 · 허용 subset은 turn 내내 불변이라 거부 되먹임이 실행 표면을 넓히지 않음). 허용 subset을 좁히는 세 입력(등록 registry · 호스트의 turn 허용 지정 · 접근 축 판정)과 **승인 축은 노출을 좁히지 않는다**는 판단(감추면 model이 그 tool을 존재하지 않는 것으로 보고 승인 필요 사실 자체가 사라진다. 노출은 실행이 아니므로 fail-open이 아니다). 파일 후보 트리 = module 7개 + `__init__.py`(`declaration`·`results`·`registry`·`validation`·`policy`·`selection`·`executor`)와 각 파일이 소유하는 책임의 대응, 파일별 단일 역할·대표 타입/행동 범주·producer/consumer(`policy`는 정책 **판정**이지 정책 **정의**가 아니고, `selection`은 접근 축만 쓰며, `executor`는 registry가 아니라 허용 subset을 본다). 내부 4단 총순서(u0 값·표현 → u1 보관과 순수 판정 → u2 허용 계산 → u3 집행)와 **방향 규칙 4개** — **`executor`를 아무도 참조하지 않음**(handler 호출 경로가 위에서 아래로 한 번뿐이라 우회 경로 부재가 파일 단위 질문으로 줄어든다) · `policy` ↛ `selection`(반대로 서면 RG4의 “같은 계산”이 두 번 도는 다른 계산이 된다) · `registry`는 `selection`·`executor`를 모름 · `core` 밖 형제 package 무참조. tool call 한 건의 **국면 8개**(존재 확인 → 동일성 확인 → 입력 schema 검증 → 접근 축 → 승인 축 → 실행 context 주입과 handler 호출 → 결과 정규화 → 결과 1개 확정)와 **1~5국면은 부작용 없음 · 부작용은 6국면에서만**, 동일성 확인이 schema 검증보다 먼저인 이유(버전이 다르면 schema 자체가 달라 틀린 자로 재게 된다), 불변식 8개(TI1~TI8: 결과 정확히 하나이고 거부도 결과 · **1~5국면을 통과하지 않고 handler에 닿는 경로가 없다** · 판정 기준은 현재 registry가 아니라 turn snapshot이고 버전·revision 불일치는 맞춰주지 않음 · model이 인자로 주장한 사용자·tenant·권한을 신뢰하지 않음 · 미등록/허용 밖/revision 불일치/schema 불일치/권한 누락/승인 누락은 fail-closed · tools는 기록하지도 되먹이지도 않고 값만 반환 · model을 부르거나 스스로 다음 tool을 부르지 않음 · 이 call 하나만 끝내고 남은 호출 중단이나 turn 종료를 판정하지 않음). KAG-DEC-002 side effect 12단계와의 대조로 `tools` 소유가 6·7단계뿐임을 항목별 확인. 거부 5계열(존재·동일성·입력·**접근**·**승인**)과 handler 실패 — 접근과 승인을 한 계열로 합치면 감사 기록에 “거부됨”만 남아 권한을 더 줘야 하는 상황과 승인을 받아야 하는 상황이 구분되지 않는다. 상황별 소유권 16건(모르는 필드를 조용히 버리지 않음 · 인자의 권한 주장 필드를 실행 context로 승격하지 않음 · 접근 선언 없는 등록은 실행 아닌 등록에서 실패 · handler 실패를 성공으로 감싸지 않고 예외로 turn을 죽이지도 않음 · 허용 계산 실패는 빈 목록이 아니라 실패)와 fail-closed 원칙 6개(TF1~TF6). 호스트가 등록할 때 채우는 다섯 가지(공개 정의·handler·접근 선언·승인 정책 선언·부작용 분류)뿐이고 **상속할 기반 클래스·plugin 진입점·자동 등록 데코레이터가 없음**, handler에 registry·다른 tool·session·context·provider를 넘기지 않음(넘기면 handler 안에서 국면을 지나지 않는 호출이 생겨 TI2·TI7이 동시에 뚫린다), 접근 축에서 라이브러리가 갖는 것(무기본값 규칙 · 단 하나의 판정 지점 · 축 구분 반환 · 기본 거부)과 갖지 않는 것(역할 모델 · 도메인 개념 · 권한 저장·조회 · 상속·위임 규칙)의 분리와 최소 규칙 내장 권고(주입 대안은 미결, 어느 쪽이든 판정 지점은 하나), 승인 축은 “이 실행에 대한 승인이 실렸는가”만 보고 발급·만료·철회·화면은 호스트, transport와 MCP는 handler 안쪽이며 MCP도 명시적 등록을 우회하지 않음. `tools/__init__.py` **선별 재수출** 권고(등록 경로만 노출하고 `validation`·`policy`·`selection`·`executor`는 module 경로 — 판정을 재수출하면 호스트가 자기 화면용으로 따로 불러 RG4의 한 계산이 여러 벌이 된다. `providers`의 무-재수출과 다르게 가는 이유도 명시), 확장 표면은 tool 추가가 호스트 코드 한 곳이고 **판정 축 추가는 그보다 비싸다**는 사실을 구조에 남김, contract test seam 5종(등록 가드 단독 · 허용 계산 단독 · 순수 판정 직접 호출 · handler 대체 · **우회 경로 부재는 동작이 아니라 구조를 보는 검사**). 관측·민감정보 원칙 8개(TR1~TR8). KAG-DEC-003·KAG-DEC-005는 `proposed` 입력으로만 참조하고 연결을 파일명이 아니라 범주 수준으로만 둠. KAG-BL-001 OQ-2(sync/async)·OQ-3(schema validator)과 KAG-DEC-002 OQ-3(취소 전파)·OQ-4(tool timeout)·OQ-6(반복 호출 방어)은 닫지 않음. 미결 13건 | [KAG-DEC-006](10-decision/decision-006-tools-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-006 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-006 추가·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음. schema validator를 묻는 Open Question 3과 sync/async를 묻는 Open Question 2는 여전히 미결이고 역할·권한 모델과 승인 lifecycle은 호스트 몫으로 남겼다고 명시), baseline index Decision 열·Next(남은 순서 `sessions` → `skills` → `context` → `runtime`), decision index(서문 문단·결정 로그 행·미결 사항 4행·Next), 제품 README(서문·현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted와 KAG-DEC-003·KAG-DEC-004·KAG-DEC-005의 proposed 본문은 그대로 두고 되돌리거나 승격하지 않음. spec/work/architecture 문서와 KAG-DEC-007은 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-add | KAG-DEC-007 | sessions package event 저장·조회 경계 제안(`proposed`, 본문 전체가 권고안). 의존 그래프 아래에서 위로 내려가는 순서에서 `tools`(L2) 다음 대상은 같은 L2의 `sessions` — KAG-DEC-006 TI6·TR7이 “`tools`는 아무것도 기록하지 않고 거부도 값으로 반환한다”며 기록의 수신자를 이 문서로 넘겼다. 단일 `store.py` / 관심사별 평면 module / store 백엔드별 하위 package / event 종류별 분리 4안을 초기 복잡도·계약과 구현의 분리·책임 누수 저항·durable 확장 비용·event 종류 무지의 유지로 비교해 **관심사별 평면 module + 계약/구현 파일 분리 권고**(단일 module과 백엔드별 중첩은 비권고, 종류별 분리는 기각 — `providers`와 달리 store 백엔드가 서로 다른 것은 저장 매체 접근 하나뿐이라 봉인 폴더의 이득이 공용/전용 판정 비용보다 작고, event 종류별 분기는 새 종류마다 이 package를 열어 KAG-DEC-001이 제외한 판단을 들여오며 KAG-DEC-003 V3′의 파괴적 변경이 store 구현까지 번진다). **KAG-DEC-003 OQ-2에 대한 답을 함께 제안** — store 경계는 `sessions` 안 `store.py`에 계약 하나이고 append와 조회를 두 계약으로 쪼개지 않는다(read-your-write가 두 계약에 걸쳐 있어 어느 쪽도 단독으로 보장하지 못하고 결국 “같은 구현이면 그렇다”는 규약으로 내려앉는다). sessions 소유 책임 9종(S1~S9: append 수용 · session 내 순서 부여와 보존 · append 원자성 · 중복 판정 · 동시 writer 충돌 감지 · 순서 있는 조회 · store 계약 소유 · memory store 구현 · 저장 표현 경계)과 소유하지 않는 것 13종(무엇을 언제 append할지의 결정 · model context 투영과 compaction · folding된 상태와 화면 view · 검색·색인·analytics · 짝/종료/불변식 판정 · event 종류 목록과 payload 형태 · 알림·구독·push · 접근 제어 · 민감 판정과 마스킹 · queue/lease/claim/worker · DB 제품과 migration · 암호화와 retention · 조립). 식별 3층(session·turn·event)과 **“식별자는 호출자가 주고 위치는 sessions가 부여한다”**는 판정 — 발급을 store가 하면 store마다 발급 방식이 달라져 계약이 약해지고 `runtime`이 append 이전에 식별자를 참조할 수 없어 KAG-DEC-002 §4.2의 짝 판정이 밀린다. 순서 규칙 5개(O1~O5: 전순서는 session 안에서만이고 전역 순서는 보장하지 않음 · **기준은 append 도착 순서이지 벽시계 시각이 아님**(시계는 뒤로 갈 수 있고 시각 기준이면 같은 session을 두 번 읽을 때 순서가 달라져 재현성이 사라진다) · 위치는 단조 증가하되 조밀할 필요 없음(대가: 빠진 것을 번호만으로 판정 못 함) · 부여된 위치 불변 · session의 현재 끝을 가리키는 값 하나). append 계약 규칙 8개(SG1~SG8: append-only이고 수정·삭제·재배치 경로를 계약에 두지 않음 · **compaction과 요약은 원본을 대체하지 않고 요약도 새 event다** · 식별자는 호출자 · 순서는 도착 순서 · 한 단위는 원자적 · **event 종류에 따른 의미·정책 분기 금지**(표현 복원을 위한 구조적 판별은 예외이고 `serialization` 안에서만) · **알리지 않는다**(알림이 append 안에 있으면 알림 실패가 기록 실패로 번지거나 기록 실패를 알림 성공이 가린다) · append 실패를 성공으로 접지 않음). 동시 writer는 **감지 값 하나까지만** — 지금 writer는 turn을 소유한 `runtime` 하나뿐이라(KAG-DEC-002 §3) lock·lease·큐·사용자별 직렬화를 만들지 않고 “당신이 본 끝이 더 이상 끝이 아니다” 한마디만 하며 그 다음은 호출자가 정한다. 값을 지금 만드는 이유는 나중에 만들면 store 계약이 열리고 그 시점엔 memory와 durable이 이미 다른 계약을 갖기 때문이고, 지금 강제하지 않는 이유는 관리할 이유가 없는 값이 형식적으로 채워지기 때문이다(선택적 입력 권고, 필수화는 OQ-4). 파일 후보 트리 = module 7개(`records`·`outcomes`·`ordering`·`serialization`·`store`·`memory`·`reading`) + `__init__.py`와 각 파일이 소유하는 책임의 대응, **`replay`가 아니라 `reading`인 이유**(재생은 “접어서 상태를 만든다”로 읽히는데 folding은 이 package 것이 아니고, 이름이 책임을 넘겨받는 일이 실제로 생긴다)와 `records`가 `events`가 아닌 이유(payload는 core 것이고 여기 사는 것은 배치 좌표가 붙은 저장 단위 — `events`로 부르면 payload가 두 곳에 정의된 것처럼 읽히고 그것이 실제로 두 벌이 되는 첫걸음이다). 파일별 단일 역할·대표 타입/Protocol 범주·producer/consumer(`store`는 계약만 갖고 기본 구현·공통 로직·mixin을 두지 않으며, `reading`은 위치와 개수는 받고 종류·내용 조건은 받지 않는다). 내부 4단 총순서(v0 값·표현 → v1 순수 규칙 → v2 계약 → v3 구현과 되읽기)와 **방향 규칙 4개** — **`store`는 어떤 구현도 참조하지 않음**(계약이 구현을 알면 durable을 붙일 때 계약 파일이 열린다. import 목록 비교만으로 검사되는 규칙) · `reading`은 특정 store를 모름(모르면 memory와 durable이 같은 순서 보장을 얻고, 알면 되읽기가 두 벌이 되어 반드시 갈라진다) · **`memory`는 `serialization`을 참조하지 않음**(의도이자 대가 — 직렬화 규칙이 첫 slice 실행 경로에서 한 번도 돌지 않는다) · 형제 package 무참조(특히 “읽으면서 축약해 주면 편하니까” `context`를 부르는 것이 이 package가 넘어가는 가장 흔한 경로). append 국면 6개(대상 확인 → **중복 판정** → 위치 기대 대조 → 표현 확정 → 원자적 반영 → 결과 1개 확정)에서 **중복 판정이 위치 대조보다 먼저**인 이유(재시도로 다시 온 append는 앞선 시도가 성공했다면 위치 기대가 이미 낡아 있어, 순서를 뒤집으면 성공한 append의 재시도가 경합으로 보고된다)와 **저장이 바뀌는 것은 5국면에서만**, 조회 국면 5개(**범위는 위치와 개수만 받고 종류·내용 조건은 받지 않으며**, 모르는 판별값에서 멈추고, 중복 제거도 하지 않으며, 조회 실패는 빈 목록이 아니다). 불변식 10개(SI1~SI10: 결과 정확히 하나이고 거부도 결과 · **read-your-write** — KAG-DEC-002 I1의 “기록 먼저 사용 나중”은 기록이 다음 읽기에 보일 때만 의미가 있고 보이지 않으면 기록되지 않은 재료로 요청이 조립된다 · 부분 반영 없음 · 중복은 두 번째 event를 만들지도 조용히 성공으로 접지도 않음 · 조회는 append 순서 그대로이고 필터·병합·요약·중복 제거·정렬 변경 없음 · 이미 읽은 구간은 나중 append로 바뀌지 않음 · **모르는 판별값·모르는 format version에서 멈춤**(건너뛰면 “읽어서 없는 것”과 “원래 없는 것”이 구분되지 않아 lossless가 이름만 남는다) · **의미·정책 분기 없음**(표현 복원을 위한 구조적 판별은 예외)·짝 판정·상태 판정·알림 없음 · 원본을 지우거나 덮는 경로가 계약에 없음 · **SI10 전달받은 canonical event를 바꾸지 않음** — 마스킹·축약·잘라내기·정규화를 하지 않고 받은 값 그대로 저장하고 그대로 돌려주며, 저장된 것과 runtime이 판정에 쓰는 것이 갈라지면 KAG-DEC-002 I1이 성립하지 않는다). KAG-DEC-002 side effect 12단계와의 대조로 append 다섯 지점(2·5·6·8·11)과 읽기 한 지점(3)만 관여하고 **무엇을 남길지의 결정은 전부 `runtime`**임을 항목별 확인, KAG-DEC-002 §5의 “부분 진행은 사라지지 않는다”가 SI2·SI3 위에 서 있음을 명시. 거부·실패 6계열(대상·위치·**중복**·표현·저장·조회) — **중복을 거부로도 성공으로도 두지 않고 “이미 반영됐고 그때 부여된 위치는 이것”이라는 구분된 결과**로 둔다(성공으로 접으면 호출자가 기대하는 위치와 실제가 갈라지고, 거부로만 두면 재개 경로가 정상 흐름에서 실패를 받는다). **멱등 키 없는 append는 멱등이 아니다**(두 번 부르면 두 번 붙는다)를 계약에 적어 호출자가 멱등을 공짜로 기대하지 않게 함. 상황별 소유권 15건(없는 session을 그 자리에서 만들어 주지 않음 · 같은 식별자로 두 번 만들려는 시도를 거부 · 최신 끝으로 맞춰 붙이지 않음 · **내용 비교로 중복을 추측하지 않음**(추측하면 정말 두 번 일어난 일을 지운다) · 옮길 수 있는 부분만 저장하지 않음 · 모르는 판별값을 건너뛰지 않음 · 실패를 재시도하거나 버퍼링하지 않음 · 부분 조회 결과를 완결된 것처럼 주지 않음 · 읽기 잠금을 걸지 않음 · 동시 append를 조정하지 않음 · 긴 session을 자동 축약하거나 지우지 않음 · **큰 tool 결과를 자르지 않음**(자를 것이면 append 이전에 잘려 있어야 하고, 자르는 쪽이 sessions가 되면 lossless가 깨진다 — KAG-DEC-006 TR8과 이어짐) · turn 실패·취소가 이미 붙은 것을 되돌리지 않고 종료 사실은 앞 event의 수정이 아니라 새 event)와 fail-closed 원칙 6개(SF1~SF6). memory store가 만족해야 하는 것 6항목과 **“계약이 아닌 것”** — 프로세스와 함께 사라진다는 것은 구현의 성질이지 계약의 성질이 아니며, memory에서 저절로 되는 것(전순서·원자성·read-your-write)일수록 계약에 적어야 durable에서 요구사항인지 알 수 있고 저절로 되지 않는 것(중복 판정)은 memory에서도 명시 구현해야 계약이 처음부터 준수된다. durable extension seam에서 **열리는 것 4·열리지 않는 것 4**(계약 파일·순서 규칙·되읽기·형제 package는 안 열린다)와 상속할 기반 클래스 없음, 확정하지 않는 것(DB 제품·스키마·연결 관리·migration·sync/async·트랜잭션 경계 소유). 직렬화·버전 원칙 6개(Z1~Z6: **저장 표현은 계약이다** — 저장된 것은 코드보다 오래 살고 이것이 wire 형식과 다른 점이다 · format version 값 부착 · 모르는 version에서 멈춤 · 모르는 판별값에서 멈춤 · **왕복 무손실** · provider 원문은 미결이고 담긴다면 구조를 읽지 않는 불투명 값)와 **표현 복원(허용)/의미·정책 분기(금지) 경계 표** — serializer는 판별값을 읽어 어떤 typed core event를 만들지 정하고 만들 수 없는 것에서 멈출 수 있으나 종류별 저장 경로·조회 결과·필터·상태 판정·보존 정책은 금지이며, 판별이 일어나는 자리는 `serialization` 하나이고, 아는 판별값의 원천은 `core` event 계약이되 그것을 얻는 방법은 `core` codec 미확정으로 미결(OQ-15). **되읽기와 재생의 구분** — 제공하는 것은 같은 순서로 다시 내주기·이어읽기·범위 읽기·앞 구간 불변 보장이고, folding·model context 재구성·turn/tool 재실행·화면 view는 제공하지 않으며, **되읽기는 재실행이 아니다**(tool 결과 event를 되읽는다고 tool이 다시 돌지 않는다). 소비자 folding이 멱등하려면 **안정된 순서와 중복 없는 원본** 둘이 필요하고 sessions가 주는 것은 정확히 그 둘까지다 — folding을 대신하면 화면 view·model context·감사 리포트에 같은 접기를 강요하게 된다. `sessions/__init__.py` **선별 재수출** 권고(memory store 생성 경로·store 계약·되읽기 진입은 노출하고 순서 규칙·직렬화·저장 단위 내부는 module 경로 — `providers` 무-재수출과 다르게 가는 이유는 store 고르기가 L4의 조립이고 import 줄에 이름이 남는 이점이 없기 때문). 새 store 구현자의 요구 7개(받은 canonical event를 바꾸지 않고 그대로 돌려주기 포함), contract test seam 6종(**store-agnostic 계약 suite**가 상속 없이 “모든 store가 같은 계약”을 얻는 자리 · 순수 규칙 직접 호출 · 실패 주입 store · 되읽기 안정성 · 직렬화 왕복 강제 store · **삭제·수정·분기 경로 부재는 동작이 아니라 구조를 보는 검사이고, 그 기준은 “종류 이름이 어디에도 없는지”가 아니라 “종류를 보는 코드가 `serialization` 밖에 없는지”다**(존재 금지로 두면 Z4의 unknown fail-closed와 Z5의 왕복 무손실을 구현할 방법이 없다)). 보안·관측 원칙 10개(SR1~SR8, SR3는 SR3′·SR3″로 갈라진다) — 특히 **마스킹은 event를 구성하기 전에 끝난다**(여기서 만들면 “손실 없는 원본”과 “마스킹된 것”이 같은 자리에서 싸우고 무엇이 원본인지 정해지지 않는다)와 **SR3′ — runtime은 기록한 canonical 값과 이후 판정·되먹임·요청 조립에 쓰는 값을 다르게 둘 수 없다**(이미 구성된 model 응답·tool 결과를 저장용으로 축약한 뒤 더 풍부한 원본으로 판정하는 경로 금지)와 SR3″(event에 담지 않기로 한 것과 담겼다가 저장 시 잘린 것은 다르다 — 전자는 정책, 후자는 I1 위반), 접근 제어를 하지 않으므로 **식별자를 추측 가능한 값으로 두지 않는 것이 호스트 몫**, **SF6(원본 삭제 경로 없음)과 법적 삭제 요구의 충돌을 지금 적어 둠**. KAG-DEC-003·KAG-DEC-006은 `proposed` 입력으로만 참조하고 연결을 파일명이 아니라 범주 수준으로만 둠. KAG-BL-001 OQ-2(sync/async)·OQ-8(원문 보관)·OQ-11(queue·multi-worker)과 KAG-DEC-002 OQ-7(snapshot 기록)은 닫지 않음. 미결 15건 | [KAG-DEC-007](10-decision/decision-007-sessions-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-007 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-007 추가·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음. “session 원본과 model context의 분리” 중 **원본 쪽 절반만** 제안됐고 context 쪽은 남아 있으며, provider 원문 보관을 묻는 Open Question 8과 queue·multi-worker를 범위 밖에 둔 Open Question 11은 여전히 미결이라고 명시. 마지막 문단의 “아직 decision으로 내려가지 않은 것”에서 session 원본 항목을 context 쪽으로 좁힘), baseline index Decision 열·Next(남은 순서 `skills` → `context` → `runtime`), decision index(서문 문단·결정 로그 행·미결 사항 4행·Next), 제품 README(서문·현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted와 KAG-DEC-003~006의 proposed 본문은 그대로 두고 되돌리거나 승격하지 않음. **KAG-DEC-003 OQ-2는 KAG-DEC-007이 답을 제안했을 뿐 KAG-DEC-003 본문은 수정하지 않았다** — 확정은 사용자 리뷰 몫이다. spec/work/architecture 문서와 KAG-DEC-008은 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-change | KAG-DEC-007 | 코디네이터 검수 보정 3건 (상태 `proposed` 유지, 권고 Option B·Open Questions owner·KAG-DEC-001~006 상태는 변경 없음). ① **중복 SI6 행 제거와 SI10 신설** — 보정 전 §4.3에는 **동일한 SI6 행이 연속 2회** 존재했다. 중복 행을 제거하고, ③이 요구한 “`sessions`는 전달받은 canonical event를 무손실 저장한다”가 규칙으로 존재하지 않아 별도 불변식으로 신설했다 — **SI10: 마스킹·축약·잘라내기·필드 제거·정규화를 하지 않고 받은 값 그대로 저장하고 그대로 돌려준다.** 그 결과 현재 §4.3은 **SI1~SI10이 각각 한 번씩만 존재하는 10행**으로 고유하게 정합됐다. 그에 따라 §4.3 제목(10개), §7.2 store 구현자 요구(6→7, “받은 canonical event를 바꾸지 않고 그대로 돌려준다” 추가), §7.3 store-agnostic suite 커버리지(SI1~SI7 → SI1~SI7·SI10), Scope In, decision index, 제품 README를 실물 행 수에 맞춤. ② **표현 복원과 의미 분기의 충돌 해소** — SG6/SI8(“종류를 해석하지 않는다”)과 Z4/SI7(“모르는 판별값에서 멈춘다”)과 §7.3의 “event 종류 이름이 package 어디에도 없어야 한다”가 그대로는 동시에 성립하지 않았다. 저장 표현을 typed core event로 되돌리려면 어딘가는 판별해야 하므로, 존재 금지 기준이면 Z4의 unknown fail-closed와 Z5의 왕복 무손실을 구현할 방법이 없다. §6.3에 **허용(표현 복원 codec) / 금지(의미·정책 분기) 경계 표**를 신설해 — 허용: 판별값을 읽어 어떤 typed event를 만들지 정하기·만들 수 없는 것에서 멈추기·왕복 무손실 검증·복원 실패를 표현 계열로 분류하기, 금지: 종류별 저장 경로·종류별 조회 결과와 필터·종류로 짝/종료/상태 판정·종류별 실패 정책·종류별 보존 기간과 마스킹과 상한 — 을 갈랐고, 경계 3줄(판별은 “무엇을 만들어야 하는가”까지이고 해석은 “무슨 뜻인가”부터 · 판별이 일어나는 자리는 `serialization` 하나 · 아는 판별값의 원천은 `core` event 계약이지 `sessions`의 정책 표가 아님)을 못박았다. SG6·SI7·SI8·Z4 문면과 §1 마지막 줄, §4.2 읽는 법 1을 같은 구분으로 정정하고, **§7.3 구조 검사 기준을 “종류 이름의 부재”에서 “종류를 보는 코드가 `serialization` 밖에 없는지”(국소화)로 교체**했다 — 금지 대상은 존재가 아니라 위치다. KAG-DEC-003이 codec을 두지 않았으므로 **새 `core` API를 발명하지 않고**, 그 원천을 얻는 방법(`core`가 codec을 제공 vs `sessions` 매핑 + 동기화 강제)은 OQ-15로 신설해 열어 두었다(미결 14→15건). Scope Out에도 같은 항목을 추가했다. ③ **SR3의 append 전 마스킹 범위를 KAG-DEC-002 I1과 정합** — 기존 SR3는 “projection이 필요하면 append 이전에 끝나 있어야 한다”여서, 구성된 event를 저장용으로 줄인 뒤 더 풍부한 원본으로 판정하는 경로를 배제하지 못했다. SR3를 **“`sessions`는 전달받은 canonical event를 무손실 저장하고, projection이 필요하면 event를 구성하기 전에 끝나 있어야 한다 — 구성된 event를 줄이는 것이 아니라 애초에 그 값으로 event를 만든다”**로 고치고 두 항을 신설했다. **SR3′ — `runtime`은 기록한 canonical 값과 이후 판정·되먹임·요청 조립에 쓰는 값을 다르게 둘 수 없다**(I1은 기록된 것이 곧 쓰이는 것일 때만 성립하고, 갈라지면 재현성과 “부분 진행은 사라지지 않는다”가 동시에 이름만 남는다. 주체는 runtime이지만 결과를 떠안는 것은 sessions라 여기 적는다). **SR3″ — 민감 진단·provider 원문을 event에 넣을지는 runtime·host 정책이고 넣지 않기로 한 것은 sessions가 잃은 것이 아니다**(“event에 담기지 않은 것”과 “담겼다가 저장 시 잘린 것”은 다르며 후자가 I1 위반). §0.1에 “canonical event” 표기 규칙을 추가해 이 문서의 lossless가 **canonical event에 대한 것**임을 명시하고, §1의 마스킹 행과 §5.2의 “큰 tool 결과” 행(자를 것이면 event 구성 전에 잘려 있어야 하고 잘린 그 값이 곧 canonical)을 같은 기준으로 정정했다. 보안·관측 원칙은 8→10항(SR1~SR8, SR3는 SR3′·SR3″로 갈라짐). Rationale 리스크 2건 추가(표현/의미 경계는 `serialization` 안에서의 위반까지는 구조 검사로 잡히지 않는다 · 아는 판별값의 원천이 확정되기 전 임시 매핑이 오래 살면 그것이 곧 종류별 판단이 된다) | [KAG-DEC-007](10-decision/decision-007-sessions-boundaries.md) |
| 2026-08-09 | decision-add | KAG-DEC-008 | skills package 등록·선택·prompt 투영 경계 제안(`proposed`, 본문 전체가 권고안). KAG-DEC-002 §6이 skill 투영을 “`context`의 입력”으로 규정했으므로 입력을 만드는 `skills`(L2)가 `context`보다 먼저이며, 이번 대상은 `skills/` 하나다. 단일 `registry.py` / 관심사별 평면 module / 자산 형식·도메인별 중첩 / loader 기반 자동 탐색 4안을 초기 복잡도·등록 출처 통제·선택 주체의 명시성·투영 경계 유지·확장 비용으로 비교해 **관심사별 평면 module 권고**(단일 module은 비권고, 형식·도메인별 중첩과 loader 자동 탐색은 기각 — 이 package에는 skill 내용이 살지 않아 분류할 대상 자체가 없고, 자동 탐색은 KAG-DEC-002 §6의 완전 제외이며 skill에서 특히 나쁘다: tool 자동 탐색이 “실행 가능한 것이 늘어난다”라면 skill 자동 탐색은 “**model에게 말할 수 있는 사람이 늘어난다**”이다). **이 문서는 skills를 최소 tool loop의 필수 요소로 바꾸지 않는다** — REF-0007과 KAG-DEC-002 §6의 “추후에 넣으면 좋을 것 = 조립 가능한 독립 확장 모듈” 분류를 유지하고, 그것이 문서에만 남지 않도록 확장 모듈 규칙 5개(SKB1~SKB5: skill 없이 turn이 완결되고 빈 선택 set은 정상이며 실패가 아님 · 새 phase를 만들지 않고 요청 조립의 입력으로만 들어감 · `core` 밖 형제 package 무참조 · **조립에서 빼도 다른 package의 파일이 열리지 않음** · 넣어도 turn 불변식·종료 조건·판정 축이 늘지 않음)와 검증 가능한 성공 조건(“`skills/`를 통째로 지워도 첫 vertical slice가 그대로 돈다”)을 못박음. skills 소유 책임 7종(SK1~SK7: 등록 수용 · 등록 시점 가드 · registry revision · turn 선택 set 확정 · 순서·충돌·예산 판정 · prompt projection 값 생성 · provenance와 신뢰 등급 표시의 보존·전달)과 소유하지 않는 것 13종(**최종 prompt·request 조립과 compaction은 `context`** · tool 등록/허용/검증/실행 · provider 변환 · turn loop와 종료 · session 기록 · model 출력 판정과 되먹임 · **filesystem scan·plugin loader·marketplace·원격 registry·dynamic discovery** · **model 주도 설치·자기선택·권한 상승** · 선택의 도메인 규칙 · skill 본문 저작과 승인 절차 · 신뢰 등급의 정의와 서명·검증 인프라 · 로깅 destination · 조립). **등록 registry / turn 선택 set / prompt projection 세 값의 분리**와 그 관계 3종 — 셋은 타입이 달라 집합 포함으로 쓰지 않고 **상한**(선택 set의 등록 식별자 집합 ⊆ registry의 것 — 여기서만 ⊆를 쓴다) · **변환**(선택 set → projection의 사상. 등록 단위와 투영 조각은 담는 것이 다르다) · **출처 추적**(투영의 각 조각 → 선택 set의 원소 하나. 이것이 성립해야 PJ4가 값으로 실현된다)으로 나눠 적고, 한 줄로는 “선택은 registry를 넘지 못하고, 투영은 선택 set만을 입력으로 받으며, 나간 조각은 전부 출처가 하나로 정해진다”. **선택과 투영은 하나의 계산이 아니라 고정 순서 2단계**이고 단일 원자 facade를 두지 않으며(facade는 방향 규칙 1을 약화시키고, 두 값의 소비자가 `runtime`과 `context`로 다르며, 단독 seam이 무너진다), 그 대가를 규칙 2개로 갚는다(SKC1 **투영의 입력은 선택 set 하나뿐**이고 registry도 선택 입력 원본도 다시 보지 않음 · SKC2 **순서는 고정이고 되돌아가지 않음** — 예산 초과 시 선택을 줄여 재계산하는 경로를 만들지 않고 줄일지는 호스트가 정한다). 규칙 8개(SKG1~SKG8: skill의 출처는 언제나 L4 호스트의 명시적 등록 · 등록과 선택은 다른 값 다른 시점 · **선택 입력은 호스트가 주고 model이 선택하지 않으며 model 응답이 선택을 켜거나 끄거나 순서를 바꿀 수 없음** · 고정되는 것은 identity·version·revision·순서이고 호스트 자원·경로·secret은 투영 값에 없음 · **선택 지정이 없으면 빈 집합이고 “등록된 전부”로 읽지 않음** — KAG-DEC-006 OQ-11과 달리 여기서는 정할 수 있다: skill 없는 turn이 정상이므로 fail-closed를 골라도 잃는 것이 없다 · 계산 실패를 빈 집합으로 접지 않음(지정 없음과 실패는 다른 결과) · **instruction 본문은 등록 시점에 확정되고 선택·투영 중 파일·네트워크를 다시 읽지 않음** · 선택 set은 turn 시작에 확정되고 turn 내내 불변). 선택 set을 좁히는 입력은 등록 registry와 호스트의 turn 선택 지정 둘뿐이고 **model 출력은 입력이 아니다** — tool에서 접근 축이 있던 세 번째 자리가 skill에는 비어 있고, 그 자리를 “model이 고른다”로 채우려는 것이 가장 자연스러운 유혹이라 SKG3이 그것을 막는다. 파일 후보 트리 = module 6개(`definition`·`outcomes`·`registry`·`ordering`·`selection`·`projection`) + `__init__.py`와 각 파일이 소유하는 책임의 대응, **REF-0007 `skills/loader.py` 가안을 두지 않는 판단**(“loader”는 “어딘가에서 읽어온다”를 이름으로 인정하는 것이고 그 자리가 있으면 SKG1이 금지한 경로가 “이미 있는 파일에 함수를 하나 더하는 일”이 된다. 이름을 지우는 것이 규칙보다 강한 방어이며, **이 문서가 설계 노트를 뒤집는 유일한 지점**)과 `definition`이 `skill`이 아닌 이유(tool의 `declaration`과 같은 판단이되 본문 자체가 등록 단위에 실릴 수 있어 “정의”가 덜 왜곡한다)와 `projection`이 `prompt`·`renderer`가 아닌 이유(문면은 `context`의 것). **이 package에는 부작용이 없다** — 여섯 module 전부가 순수 판정과 값 생성이고, skill은 실행되지 않으므로 위험은 “잘못 실행됨”이 아니라 “잘못 노출됨”이다. 내부 4단 총순서(w0 값·표현 → w1 보관과 순수 판정 → w2 선택 확정 → w3 투영)와 **방향 규칙 5개** — **`projection`을 아무도 참조하지 않음**(model에 나갈 값이 만들어지는 경로가 하나로 유지되고, KAG-DEC-006이 실행 경로에 적용한 규칙을 노출 경로에 적용한 형태) · **`projection`은 `registry`를 참조하지 않음**(SKC1의 구조적 형태 — 참조가 생기면 “선택되지 않았지만 등록된 것”이 실릴 경로가 만들어진다) · `ordering` ↛ `registry`(“등록 순서”가 규칙에 스며들면 고정 입력 검증이 불가능해진다) · `registry`는 선택·투영을 모름 · 형제 package 무참조(**`context`가 가장 흔한 누수 경로** — “투영한 김에 요청 문면까지 만들어 주면 편하다”가 이 package가 경계를 넘는 방식이다). 배치 효용 셋 중 둘이 검사 가능성이다 — 노출 경로 부재가 파일 단위 질문이 되고, 읽어올 자리를 없앴으므로 “`skills/` 어느 파일에도 파일·네트워크·import 부작용 접근이 없는가” 하나로 SKG1·SKG7·SKI4가 동시에 방어된다. **`core`가 skill 계약을 소유하지 않는다는 판단**(KAG-DEC-003 K1~K10에 skill 항목이 없는 것은 지금은 누락이 아니다 — §4.1 기준상 소비자 `context`도 `skills`도 서로를 import할 수 없는 L2 형제라 core protocol 기준을 통과하지 못한다)과 두 선택지(core 새 값 계약으로 올리기 vs 기존 message·content block 계약으로 표현하기) 비교, 권고는 후자 우선 검토이되 provenance를 실을 자리 때문에 확정하지 않고 **KAG-DEC-003 본문은 수정하지 않음**. **고정 순서 2단계로 나뉘고 합쳐서 국면 6개**(1단계 선택 = 선택 입력 수용 → 존재·동일성 확인 → 순서·충돌 판정 / **단계 경계** / 2단계 투영 = **예산 판정** → 투영 값 생성)이며 **실행 조건이 비대칭**이다 — 선택은 turn당 항상 한 번 실행되고 투영은 **선택 set을 받았을 때만** 한 번 실행되며, **선택 거부는 투영을 부르지 않고 그 자리가 terminal**이다(SKC1상 투영의 입력은 선택 set 하나뿐이라 거부에는 넘길 값이 없다). lifecycle은 **terminal result 셋** 중 정확히 하나로 끝난다(투영(빈 투영 포함) · 선택 거부 · 투영 거부). KAG-DEC-006의 tool call 건당 lifecycle과 단위가 다름을 명시하고, 예산 판정이 투영 생성보다 먼저인 이유(다 만든 뒤에 재면 이미 만든 값을 자르고 싶어지는데 잘린 instruction은 조용히 다른 지시가 된다 — 앞부분만 남은 “다음 조건에서만 X하라”는 “X하라”가 된다)와 3국면이 중복을 제거하지 않고 **거부**하는 이유. 불변식 9개(SKI1~SKI9: **실행된 각 단계가 결과 하나를 내고 lifecycle 전체는 terminal result 하나로 끝나며** 이것은 “두 단계가 항상 실행된다”는 뜻이 아니다 · 빈 집합도 빈 투영도 거부도 결과이고 **빈 선택 set은 거부가 아니라 성공값이라 경계를 넘어 빈 투영이 된다**(SKG6 유지 — “고른 것이 없다”와 “고르지 못했다”는 다른 결과) · **선택은 호스트 입력에서만 오고 model 출력을 읽지 않음** · 판정 기준은 turn 고정 identity·version·revision이지 현재 registry가 아니며 불일치는 맞춰주지 않음 · **본문은 등록 시점 확정값이고 turn 중 외부를 읽지 않음** · **skill은 tool을 만들지도 넓히지도 않으며 허용 subset·실행 context·권한·승인·한도에 영향이 없음** · 투영 값은 지시가 아니라 데이터이고 runtime·tools의 판정 분기를 바꾸지 않음 · 기록하지도 model을 부르지도 다른 package를 호출하지도 않음 · 빈 선택은 정상이고 경고도 아님 · **같은 입력·같은 revision이면 같은 순서와 같은 투영**). KAG-DEC-002 side effect 12단계와의 대조로 **어떤 단계도 소유하지 않고** 1단계(turn 고정)의 재료와 3단계(요청 조립)의 입력만 제공함을 확인하고, **§3 snapshot 5항목에 skill 항목을 더하지 않음**(accepted 문서의 열거를 고치는 것은 supersede이므로 권한 밖이고, 그때까지는 호스트가 turn 입력으로 함께 고정하는 방식으로 같은 성질을 얻는다 → OQ-13). sequence는 **L4 조립**을 권고 — KAG-DEC-001 §4가 `runtime → skills`를 허용하지만 `runtime`이 직접 부르면 skill 없는 조립에서도 `runtime`이 skills를 알게 되어 SKB4가 규약으로 내려앉는다(→ OQ-14). 투영 경계 원칙 7개(PJ1~PJ7: **투영은 권한을 만들지 않는다** — 본문이 “tool X를 써라”라고 적어도 X가 허용 subset에 없으면 실행되지 않고(KAG-DEC-006 TI2·RG8) skill 본문은 model이 무엇을 *시도할지*만 바꾸며 무엇이 *가능한지*는 turn 시작 고정값이 다시 판정한다 · 정책·승인·한도·종료 조건을 담지 않음 · 라이브러리가 본문 구조를 해석하지 않음(해석하면 본문을 쓴 사람이 라이브러리를 프로그래밍하는 것이 된다. 대가는 skill로 할 수 있는 일이 “글로 부탁하기”로 제한되는 것이고 의도한 제한이다) · **provenance가 값에 따라붙음**(없으면 skill 둘 이상일 때 어느 것 때문인지 복원 불가) · 신뢰 등급은 등록 시점에 붙고 turn 중 올라가지 않으며 값의 의미는 호스트 것 · **model 출력이 skill이 되는 경로 없음** — 자기설치 방어이며 이 경로가 열리면 한 번의 injection이 영구적이 된다 · **예산 초과는 자르지 않고 거부**). prompt injection 경로 10건의 판정에서 **일곱은 구조로 막히고 셋은 막지 못한다** — 막히는 것: 본문의 권한 요구 · 다른 skill 켜기 · 한도 재정의 · model 출력의 skill화 · 파일시스템·plugin·marketplace 자동 등록 · 등록 후 원본 변경(TOCTOU) · 축약에 의한 의미 변조. 막지 못하는 것: **간접 injection**(tool 결과 텍스트는 skill이 되지 않지만 model이 그것을 지시로 읽는 것 자체는 막을 수 없고, 보장은 “권한·선택·정책을 바꾸지 못한다”까지이며 잘못된 시도는 KAG-DEC-006의 국면 8개에서 다시 판정된다) · **신뢰할 수 없는 등록**(등록은 호스트의 판단이고 라이브러리가 대신하지 않는다. 주는 것은 provenance 보존과 신뢰 등급의 자리까지이며, 이것을 “막는다”고 적지 않는 것이 표의 정직함이다) · **model이 지시를 따르는지**(보장 대상 아님). 실제 안전 보장은 “**skill 텍스트는 model이 무엇을 시도할지만 바꾸고 무엇이 가능한지는 바꾸지 못한다**” 한 문장이며 그 이상을 주장하지 않음. 관측·민감정보 원칙 8개(SKR1~SKR8: 로깅 destination 미선택 · 본문에 secret 금지(라이브러리가 검사하지 못하므로 등록하는 호스트의 책임임을 명시) · 경로·내부 식별자를 투영 값에 담지 않음(실리면 model이 tool 인자로 되쓴다) · 민감 목록은 주입 · 거부 결과에 본문 원문 금지 · 무엇을 event에 남길지는 runtime이되 재현하려면 identity·version·revision이 남아야 함 · **skill 본문 축약은 어느 쪽에서도 하지 않는 편이 권고** — tool 결과는 데이터라 줄여도 의미가 남지만 지시문은 줄이면 다른 지시가 되므로 KAG-DEC-006 TR8과 성격은 같되 결론이 다르다 · 본문도 외부 provider로 나가는 값이라 데이터 등급 정책 대상). `skills/__init__.py` **선별 재수출** 권고(재수출 후보는 등록 경로 둘 + **2단계 진입 둘**)와 `selection`·`projection`을 둘 다 재수출하는 것이 `tools`와 다른 이유(`tools`가 판정 재수출을 막은 것은 RG4의 “같은 계산”이 여러 벌이 되기 때문인데 skill에는 실행 축이 없어 그 요구 자체가 없고, 대신 지켜야 할 SKC1·SKC2는 호스트가 두 진입을 순서대로 부르는 것과 충돌하지 않는다. 다만 **순서를 라이브러리가 강제하지 않고 문서와 값의 타입으로만 유지한다는 것이 facade를 기각한 대가**임을 함께 적음), **skill 자산 거처는 (가)(호스트가 본문 문자열을 넘김)를 이 문서 권고 구조의 전제로 명시**하고 — “이 package에는 skill 내용이 살지 않는다”·“읽어올 자리를 없앴다”·“파일·네트워크 접근이 어디에도 없다”가 전부 (가) 위에서만 성립하므로 — (나)(경로를 받아 등록 시점 1회 읽기)/(다)(package 동봉)를 고를 경우 **함께 supersede해야 할 항목을 표로 못박았다**(나: §4.1 파일 트리에 읽기 소유 파일 추가(**`registry`에 숨기지 않는다**) · §4.3 효용 2와 §8.4 seam 5의 검사 기준을 “존재 금지”에서 “한 파일로 국소화”로 완화 · §1의 “읽어오는 주체가 없다” 서술 · `loader` 부재 판단 / 다: 그 넷에 더해 §1의 “skill 내용이 살지 않는다”와 SKB4의 의미). **어느 안에서도 dynamic discovery·plugin·marketplace·원격 registry의 완전 제외는 유지된다**((나)는 호스트가 경로를 하나씩 명시하는 것이고 (다)는 동봉된 것을 명시적으로 등록하는 것이라, 그 구분이 무너지면 그것은 기각된 Option D다). OQ-2는 계속 열어 두되 **세 안이 대등하지 않다는 사실과 답이 (나)/(다)일 때 무엇이 열리는지는 닫아 두었다**(KAG-DEC-001 OQ-6 유지). 확장 표면은 skill 추가가 호스트 코드 한 곳이고 상속·plugin 진입점·데코레이터·디렉터리 규약이 없으며 판정 축 추가가 더 비싸다는 사실이 구조에 남음, contract test seam 7종(등록 가드 단독 · 순수 판정 직접 호출 · 선택 단독(지정 없음과 계산 실패가 다른 결과인지) · 투영 결정성과 내용 경계 · **단계 실행 조건**(선택 거부 turn에 투영이 호출되지 않고 그 turn의 terminal이 “빈 투영”이 아니라 “선택 거부”로 남는지) · **외부 읽기·노출 경로 부재는 동작이 아니라 구조를 보는 검사** · **확장 모듈성은 조립을 보는 검사** — skills 없이 첫 slice가 그대로 도는지). KAG-DEC-003·KAG-DEC-006을 `proposed` 입력으로만 참조하고 연결을 파일명이 아니라 범주 수준으로만 둠. KAG-BL-001 OQ-2(sync/async)·OQ-6(token 계산)·OQ-7(skill 선택 주체)·OQ-9(데이터 등급)와 KAG-DEC-001 OQ-1(배포명)·OQ-6(skill 자산 거처), KAG-DEC-003 OQ-6(신뢰 등급 부착 시점)은 닫지 않음. 미결 16건 | [KAG-DEC-008](10-decision/decision-008-skills-boundaries.md) |
| 2026-08-09 | mapping-change | KAG-BL-001, KAG-DEC-008 | proposed 상태 정합 — baseline `links.decisions`에 KAG-DEC-008 추가·본문 Next에 리뷰 대기 문단 추가(상태는 `accepted` 유지, 반영 범위는 넓히지 않음. skills가 **최소 runtime의 필수 요소가 아니라 조립 가능한 독립 확장 모듈**이라는 분류를 유지했고, skill 선택 주체를 묻는 Open Question 7과 skill 자산의 거처는 선택지만 비교하고 풀지 않았다고 명시), baseline index Decision 열·Next(남은 순서 `context` → `runtime`), decision index(서문 문단·결정 로그 행·미결 사항 7행·Next), 제품 README(서문·현재 상태 표·최근 로그) 갱신. KAG-DEC-001·KAG-DEC-002·KAG-BL-001의 accepted와 KAG-DEC-003~007의 proposed 본문은 그대로 두고 되돌리거나 승격하지 않음. **KAG-DEC-002 §3의 snapshot 5항목은 수정하지 않았다** — skill set을 여섯 번째 항목으로 더할지는 KAG-DEC-008 OQ-13으로 사용자에게 남겼다. spec/work/architecture 문서와 KAG-DEC-009는 생성하지 않음. `products/README.md`는 수정하지 않음 | [00-baseline](00-baseline/README.md) · [10-decision](10-decision/README.md) · [README](README.md) |
| 2026-08-09 | decision-change | KAG-DEC-008 | 코디네이터 검수 보정 3건 (상태 `proposed` 유지, 권고 Option B·독립 확장 모듈 분류·Open Questions owner·KAG-DEC-001~007 상태는 변경 없음). ① **선택과 투영의 호출 소유권이 모호했다** — §3.1은 “셋을 잇는 계산은 하나”이고 투영이 “선택 set과 같은 계산”이라고 썼는데, §8.1은 `selection`·`projection`을 둘 다 재수출하고 호스트가 따로 부른다고 썼으며 §5.1 sequence도 L4가 두 번 부르는 그림이었다. §3.2를 신설해 **고정 순서 2단계 + 단일 원자 facade 미채택**으로 하나로 정하고 근거 넷을 적었다(facade는 §4.3 규칙 1을 “projection을 참조하는 것은 facade뿐”으로 약화시킨다 · 두 값의 소비자가 `runtime`과 `context`로 달라 수명이 다르다 · seam 3·4의 단독 검증이 무너진다 · `tools`와 다른 이유는 RG4의 부재로 §3.1에 이미 있다). 나눈 대가는 규칙 2개로 갚았다 — **SKC1 투영의 입력은 선택 set 하나뿐**(registry도 선택 입력 원본도 다시 보지 않음)과 **SKC2 순서는 고정이고 되돌아가지 않음**(예산 초과 시 선택을 줄여 재계산하는 경로 금지, 줄일지는 호스트가 정한다). 그에 맞춰 §3.1 표의 투영 시점, tools RG4 대비 문단, §4.2의 `selection`·`projection` 행, §4.3에 **방향 규칙 3(`projection` ↛ `registry`) 신설(4→5개)**, §5를 단계 표 + 단계 경계가 보이는 다이어그램 + 읽는 법 7줄로 재작성, **SKI1을 “두 단계가 각각 하나의 결과”로 정정**, §5.1 sequence에 단계 경계 note와 예산 판정의 소유(1단계 SEL→ORD에서 2단계 PRJ→ORD로 이동), §8.1 재수출 근거와 §8.4 seam 4·5, Scope In, OQ-12 문면을 전부 같은 말로 맞췄다. 구체 signature는 발명하지 않았다. ② **`registry ⊇ 선택 set ⊇(투영) projection`이 타입 오류였다** — 세 값은 담는 것이 서로 달라(등록 단위에는 선택 메타·출처·신뢰 표시가, 투영 조각에는 본문과 provenance가 있다) 집합 포함으로 이어 쓸 수 없다. §3.1의 한 줄을 관계 3종 표로 대체했다 — **상한**(선택 set의 등록 식별자 집합 ⊆ registry의 것. **여기서만 ⊆를 쓴다**) · **변환**(선택 set → projection의 사상, 다른 타입 사이) · **출처 추적**(투영 조각 → 선택 set 원소 하나, PJ4가 값으로 실현되는 조건). 한 줄 요약도 “선택은 registry를 넘지 못하고, 투영은 선택 set만을 입력으로 받으며, 나간 조각은 전부 출처가 하나로 정해진다”로 바꾸고 decision index Result와 log의 같은 기호를 함께 고쳤다(**KAG-DEC-006 행의 `registry ⊇ 허용 subset ⊇ 공개 표면`은 그 문서 본문의 표기이므로 손대지 않았다** — 같은 성격의 문제가 있다면 KAG-DEC-006 검수에서 다룬다). ③ **열린 OQ-2가 이미 확정한 구조를 몰래 깨고 있었다** — §8.2는 (나) 경로 읽기와 (다) package 동봉을 선택 가능한 안으로 두고 “세 안 모두 SKG1·SKG7·SKI4를 만족할 수 있다”고 썼는데, 본문의 무-I/O·무-loader·package 비소유는 (가) 위에서만 성립한다. §8.2를 **“이 문서의 권고 구조는 (가)를 전제한다”**로 다시 쓰고, 표의 마지막 열을 “대가”에서 **“이 문서에서 함께 supersede해야 하는 것”**으로 바꿔 (나)는 넷(§4.1 파일 트리에 읽기 소유 파일 추가 — **`registry`에 숨기지 않는다** · §4.3 효용 2와 §8.4 seam 5의 기준을 “존재 금지”에서 “한 파일로 국소화”로 완화 · §1의 “읽어오는 주체가 없다” 서술 · `loader` 부재 판단), (다)는 그 넷 + 둘(§1의 “skill 내용이 살지 않는다” = Option C 기각 근거이자 `definition` 이름 판단의 근거 · SKB4의 의미)을 항목으로 못박았다. **어느 안에서도 유지되는 것**(SKG1·SKG7·SKI4와 dynamic discovery·plugin·marketplace·원격 registry 완전 제외 — (나)는 경로를 하나씩 명시하는 것이고 (다)는 동봉된 것을 명시적으로 등록하는 것이라, 그 구분이 무너지면 기각된 Option D다)과 **(가)에서만 구조로 보장되는 것**(“위반할 코드 자체가 없다”)을 갈랐다. §0.1·§1 마지막 줄·§4.3 효용 2·§8.4 seam 5·OQ-2·Scope In을 같은 전제로 정정하고, Rationale 리스크 2건을 추가했다(2단계 순서를 라이브러리가 강제하지 않음 · (가) 전제가 OQ-2의 답에 걸려 있어 (나)/(다)면 재작성 범위가 작지 않음). §4.4의 (가)/(나)와 §8.2의 (가)/(나)/(다)가 혼동되지 않도록 Rationale의 참조도 “§4.4의 (가)안”으로 명시했다. 미결은 16건 그대로이고 owner도 그대로다 | [KAG-DEC-008](10-decision/decision-008-skills-boundaries.md) |
| 2026-08-09 | decision-change | KAG-DEC-008 | 코디네이터 검수 보정 2 — lifecycle 실행 조건 모순 1건 (상태 `proposed` 유지, 권고 Option B·확장 모듈 분류·고정 순서 2단계·facade 미채택·SKC1·SKC2·Open Questions 16건과 owner는 변경 없음). **보정 1이 만든 모순**: §5가 선택 단계는 “선택 set 또는 거부”를 낸다고 쓰면서도 “두 단계는 turn당 각각 정확히 한 번 돈다”고 적었고, 다이어그램의 R1(선택 set 또는 거부)에서 무조건 4국면 projection으로 이어졌다. 그러나 SKC1상 **투영의 입력은 선택 set 하나뿐이므로 선택 거부에는 투영을 부를 값 자체가 없고**, SKI1을 “두 단계 각각 결과”로 읽으면 선택 거부 turn에서 실행되지도 않은 투영 결과를 요구하게 된다. **고친 것**: ① §5 서두에서 “각각 한 번”을 걷어내고 **실행 조건의 비대칭**을 명시 — 선택 단계는 조건 없이 turn당 정확히 한 번, 투영 단계는 **선택이 선택 set을 냈을 때만** 정확히 한 번, 그리고 “각 단계가 결과 하나를 낸다”는 **실행된** 단계에 대한 말이지 “두 단계가 항상 실행된다”가 아님을 못박음. ② **§5.1 terminal result 셋 신설** — 투영(빈 투영 포함) · 선택 거부 · 투영 거부 중 정확히 하나로 lifecycle이 끝나며, 각각의 투영 단계 실행 여부(실행됨 / **실행되지 않음** / 실행됨)를 표로 고정. **선택 거부 turn에는 투영 결과가 존재하지 않으며 없는 것을 “빈 투영”으로 채우지 않는다**(빈 투영 = 성공했고 고른 것이 없음, 선택 거부 = 고르지 못함). ③ 국면 표(§5.2)에 **실행 조건** 열을 추가하고 선택 행의 결과를 “선택 set → 경계를 넘는다 / 거부 → 여기서 terminal, 투영을 부르지 않는다”로 갈랐다. ④ **다이어그램 재작성** — 단일 R1을 `OK1`(선택 set, 빈 집합 포함 = 성공값)과 `NG1`(선택 거부 = terminal)로 쪼개 `NG1`이 4국면으로 가지 않고 곧장 lifecycle terminal 노드로 가게 하고, 투영 쪽도 `OK2`/`NG2` 둘 다 terminal로 모았다. 읽는 법을 7→8줄로 늘려 “선택 거부는 단계 경계를 넘지 않는다”(4번)를 신설하고 마지막 줄을 “실행된 단계는 각각 하나만 내고 lifecycle 전체는 terminal 하나로 끝난다”로 교체. ⑤ **SKI1 재작성** — “실행된 각 단계는 정확히 하나의 결과를 내고, 한 turn의 skill lifecycle은 정확히 하나의 terminal result로 끝난다”로 바꾸고 “두 단계가 항상 실행된다는 뜻이 아니다”를 규칙 안에 명시, 어기면 생기는 일에 **거부를 투영에 넘겨야 해서 SKC1이 깨진다**를 적었다. ⑥ **SKI8 재작성** — “빈 선택 set은 거부가 아니라 성공값이고 그대로 경계를 넘어 빈 투영으로 정상 종료한다”로 강화하되 **SKG6(계산 실패를 빈 집합으로 접지 않음)은 그대로 유지**하고 둘의 구분을 규칙 안에 적었다. ⑦ SKC1 행에 “선택 거부는 선택 set이 아니므로 투영에 넘기지 않는다”를 추가하고, §5.3 sequence에 **`alt` 갈래**(선택 거부 → `PRJ` 미등장 / 선택 set → 투영 호출)를 넣어 terminal 셋과 1:1이 되게 했다. ⑧ **seam 신설(6→7종)** — “단계 실행 조건”(선택 거부 turn에 투영이 호출되지 않는지, 그 turn의 terminal이 “빈 투영”이 아니라 “선택 거부”로 남는지)을 seam 5로 넣고 구조 검사 둘을 seam 6·7로 밀어 §4.3 효용·§8.2·OQ-12의 seam 번호를 전부 재정합. §5.1 신설로 기존 §5.1(KAG-DEC-002 대조)은 **§5.3**이 되어 §3.2·§8.1·Scope·OQ-14의 참조도 함께 옮겼다. Scope In의 lifecycle 항목과 decision index Result·log 요약도 같은 말로 맞췄다. signature는 발명하지 않았고 DEC-009·spec·WP·code는 만들지 않았다 | [KAG-DEC-008](10-decision/decision-008-skills-boundaries.md) |
