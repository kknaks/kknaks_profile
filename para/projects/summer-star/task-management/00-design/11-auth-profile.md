# 로그인 · 계정 · 프로필

원본: `로그인 · 계정 · 프로필.dc.html` (1920×1080 보드, `data-screen-label`로 식별)

## 화면 목록

| # | data-screen-label | 설명 |
|---|---|---|
| 1 | 로그인 | 브랜드 패널 + 폼 |
| 2 | 설정 · 개인 설정 | 프로필 / 경력 / 목소리 3패널 |
| 3 | 설정 · 개인 설정 · 시간 선택 | 업무 시간 셀렉터 열림 |
| 4 | 설정 · 개인 설정 · 경력 추가 인라인 | 경력 한 줄 추가 상태 |
| 5 | 설정 · 개인 설정 · 목소리 녹음 드로어 | 드로어 840, 녹음 중 |
| 6 | 설정 · 비밀번호 변경 드로어 | 드로어 840 |
| 7 | 설정 · 연동 관리 | 메일 · 카카오톡 · 슬랙 · 외부 캘린더 |

## 로그인

- 좌측 브랜드 패널 760 고정, `linear-gradient(160deg,#DCE6FB,#E7EDFC 34%,#F5F8FE 68%,#FFFFFF)` + radial 2겹. 로고 left 72 / top 64, 헤드라인 44/700, 하단 특징 3줄.
- 우측 폼 400 고정, left 1140 / top 264. 이메일 · 비밀번호(눈 아이콘) · 로그인 상태 유지 체크.
- 버튼 50px: 로그인(#7181F8) → 구분선 '또는' → 회사 계정으로 계속하기(1px #D9D9D9).
- 하단 약관 · 개인정보 · 고객지원 12/#9EA2AE, 폼 컬럼 기준 중앙.
- '비밀번호 찾기'는 비밀번호 라벨 우측, '회원가입'은 폼 맨 아래.

## 설정 공통 골격

- 사이드바 200 / 여백 240 / 본문 1640.
- 좌측 메뉴 카드: left 240, top 140, width 260. 항목 38px, 현재 항목 배경 #F1F2F5 + 700.
- 메뉴 하단(구분선 위): **로그아웃**, **계정 삭제**(hover #E2685B) → 둘 다 모달 확인 후 실행.
- 메뉴 최하단: 버전 · 마지막 저장 캡션.
- 우측 패널 스택: left 524, width 1356, 패널 간 gap 20. 카드 = 1px #D9D9D9 / radius 16 / #fff.
- **두 컬럼의 아래 끝을 같은 y에 맞춘다.** 개인 설정 780, 경력 인라인 상태 837 (메뉴 카드 높이를 같은 값으로 둔다).
- 패널에는 저장 버튼이 없다. 프로필 패널 헤더 우측에 "입력을 마치면 자동으로 저장됩니다" 캡션만 둔다.

## 프로필 패널

- 헤더 60: `프로필` 16/700.
- 아바타 행(padding 24/28): 아바타 88 원 + 이름 22/700 + 이메일 13/#757575 + `이미지 변경` / `삭제`, 행 우측 끝에 **비밀번호 변경** 버튼(36px, 1px #D9D9D9) → 드로어 열기.
- 필드 그리드 2열 gap 24/32, 입력 42px: 회사 · 소속 · 직무 · 업무 시간(시작 – 종료 셀렉터).
- 저장 시점: 포커스 해제. 이메일 변경 기능은 없다(표시 전용).
- 아바타는 사이드바 프로필과 이 패널에서만 쓴다. 목록·카드·로그에는 사람 표기 없음(원칙 유지).

## 경력 패널

- 헤더 60: `경력` + 캡션 + 우측 `경력 추가`(32px).
- 행 64px: 로고 34(이니셜) · 회사/소속·직무 300 · 기간 · 현재 칩(#F1F2FE/#4B52A8) · 편집.
- 현재 재직 행은 배경 #FBFCFF + `현재` 칩. 정렬은 시작일 내림차순.
- 추가·편집은 **인라인 한 줄**(56px, 컨트롤 36px, 배경 #FBFCFF): 로고 · 회사명 · 소속 · 직무 · 시작 – 종료 · 재직 중 체크 · 취소/추가. 드로어를 쓰지 않는다.
- `재직 중` 체크 시 종료 필드는 비활성(#F5F6F8, "현재").
- 프로필의 회사·소속·직무는 '현재' 경력과 같은 값을 본다.

## 목소리 패널

- 한 줄 76px: 마이크 아이콘 38 · `목소리` 16/700 · "샘플 3개 · 마지막 녹음 08.24" · 상태(등록됨 #3BA776 / 미등록 #9EA2AE) · `다시 녹음`.
- 정확도·학습 횟수 같은 수치는 노출하지 않는다(사용자가 행동할 수 없는 값).
- 드로어(840): 새 샘플 녹음 카드(읽을 문장 · 파형 68px · 00:12 / 00:30 · 녹음 중지 #E2685B / 다시 녹음 · 마이크 표기), 등록된 샘플 목록(재생 · 상태 칩 · 삭제), 회의 녹음본 자동 삭제 토글. 하단 취소 / 저장 + "저장하면 학습이 자동으로 시작됩니다 · 약 1분".

## 비밀번호 변경 드로어

- 헤더: `비밀번호 변경` + 계정 이메일.
- 입력 44px 3개: 현재 · 새 · 새 확인. 모두 눈 아이콘으로 표시 전환.
- 강도: 4칸 4px 바 + 조건 3개(8자 이상 / 숫자 / 특수문자). 충족 #3BA776 체크, 미충족 빈 원. 점수 문구는 쓰지 않는다.
- `다른 기기 모두 로그아웃` 토글(기본 켜짐).
- 하단 취소 / 변경. 성공 시 드로어 닫고 토스트.

## 셀렉터(업무 시간 등)

- 트리거 42px, 열림 상태 border #7181F8 + `0 0 0 3px rgba(113,129,248,0.16)`, 화살표 반전.
- 목록은 트리거 폭과 동일(최소 200), 트리거에 붙여 1px 겹치게, 좌측 정렬. 항목 36px.
- 현재 값 배경 #F4F5FF + #4B52A8 + 체크. 구분선 아래 `직접 입력`.
- 아래 공간이 모자라면 위로 연다. 선택 즉시 반영, 확인 버튼 없음.

## v1에서 뺀 것

- 2단계 인증, 기록 자동 백업, 로그인 중인 기기 목록 — 단일 사용자 도구 범위 밖.
- 데이터 내보내기 — v2. 마크업은 계정 영역에 주석으로 남겨 둠.
- 목소리 인식 정확도 · 샘플 수 지표.

## 아직 그리지 않은 화면 (구현 전 디자인 필요)

- 로그인 실패(비밀번호 오류) · 잠김
- 회원가입
- 비밀번호 찾기 — 메일 발송 / 재설정
- 로그아웃 확인 모달, 계정 삭제 확인 모달(600)
- 이 영역의 1280 반응형 1종
