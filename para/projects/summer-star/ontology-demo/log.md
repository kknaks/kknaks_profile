# Product Log

> 제품 단위 통합 변경 로그. baseline, decision, spec, work 변경 이력을 한 곳에 모은다.

| Date | Entry | Links |
|---|---|---|
| 2026-09-02 | spec-change · SPEC-001 (→ v0.0.4) | **backend 실측 2건 반영.** ① 노드 표 `visits` 라벨을 정본 `nodes.csv` 원형인 **「총 내원 수」**로 정정(이전 「총 내원」 — 정정 이력 표도 동기). ② **OQ-3 닫음** — WORK-001 P4 빌드 실측으로 `gold_kpi_monthly` **8행** · `gold_retention_monthly` **8행** 확정. §4 골드 표의 `—` 를 실측값으로 채우고 §6 AC-2 재현 대조값과 AC-6b 에 등재. `20-spec/README.md` 미결 표에서 SPEC-001 OQ-3 제거 | `20-spec/spec-001-data-layer-contract.md`, `20-spec/README.md` |
| 2026-09-02 | spec-change · SPEC-001·002·003·004·005 (patch → v0.0.3 / SPEC-004 v0.0.2) | **정본 대조 정정 — WORK-001 backend 실측 반영(SPEC-001 OQ-6 닫힘).** ① `node_id` 25종 표 8건 정정: 표기 5(`dow`→`weekday` · `sales`→`sales_total` · `retention_rate`→`retention_rate_60d` · `foreign_channel`→`foreign_inflow_channel` · `promo_avg_discount`→`discount_rate`) + 교체 3(정본에 없는 `review_campaign`·`organic_new`·`review_positive_rate` 삭제 → `visits`·`new_patients_domestic`·`sales_foreign_est` 등재). ※ 표시 전건 해소 — 유형 합계가 성립하도록 `naver_reviews`=`intervention`, `gu_reviews`=`organic` 으로 확정(기록 07 의 「개입·조작 가능」 판정과 일치). 신규 3노드는 **좌표 미배정** — SPEC-004 좌표 자산 재키잉 시 배정. ② `node_type` 은 **정본 영문 enum**(`kpi`/`intervention`/`organic`/`exogenous`/`unobserved`/`attribute`)이 계약 값이고, 한글 표기는 화면 카피 매핑으로 SPEC-004 가 갖는다. ③ **엣지 판정 라벨을 정본 한글값으로 치환** — spec 이 만든 `산출0`·`외생` 을 `자동 확정`·`선언` 으로 전수 교체(SPEC-001·002·003·004·005 + 20-spec/README Terms). 인과 서술 허용 = 채택·자동 확정·선언 3종, 건수 4·14·3·3·3. SPEC-004 U-5 매핑표는 판정이 번역 없는 항등이 되고 노드 타입만 번역 축으로 남게 재작성. ④ **lag 규칙 교체** — 적재는 정본 문자열 원형 보존(`2w`·빈 값 포함, 형식 강제 없음), API·도구는 `lag`(원형) + **`lag_days`**(정수, `2w`=14, 빈 값=null) 병기. SPEC-002·003 의 「전건 `<정수>d`」 AC 를 이 규칙으로 교체하고 JSON 예시에 `lag_days` 반영 | `20-spec/spec-001-data-layer-contract.md` 외 4건, `20-spec/README.md` |
| 2026-09-02 | work-add · WORK-001~005 (전건 `todo`) | **spec 5건을 빌드 계획으로 내림 — WP 5건 · 총 22 phase.** 코디 확정 분할(초안 10건 → 5건, **작업 단위는 WP 안 phase**): WORK-001 `data-foundation`(be, 5 phase) 브론즈~온톨로지 한 DB — DB 부트스트랩·`load_bronze`+**게이트 1**·마스킹 뷰·실버/골드 이식+**월 View 신설**·온톨로지 적재+**게이트 2** / WORK-002 `tools-and-api`(be, 5 phase) FastMCP 골격·도구 4종·API 앱+접속 게이트·계층 조회·KPI/그래프/예보 / WORK-003 `agent-loop-and-chat`(be, 4 phase) open-kknaks 제출부+codex 워커(queue=ontology)·채팅 API+폴딩 소비자·응답 스키마 강제(정량 추정 금지)·**게이트 4·5** 회귀 3본 / WORK-004 `frontend-three-screens`(fe, 4 phase) P1 셸·토큰 레이어 `--ont-*`·Pretendard·접속 게이트 → P2 모니터링 → P3 데이터 → P4 채팅 / WORK-005 `integration-and-deploy`(be+fe, **`work_type: release`**, 4 phase) 게이트 전건 재실행+**게이트 3 PII 0건 스캔**·회귀 자동화·홈서버 compose/NPM·Vercel env·비번 주입 절차. 게이트 배치는 1·2→001, 4·5→003, 전건+3→005. 완료 증거에 게이트 수치를 박아 뒀다(브론즈 16테이블 행수 · 재현 대조값 5종 · 회귀 3본 기록 08 실측값). rollback 은 전건 비파괴 — DB 는 재빌드, 프론트는 라우트 그룹 삭제. spec frontmatter 는 손대지 않았다(SPEC→WORK 단방향, work 의 `links.specs` 만). `30-work/README.md` 를 Status Board 5행 + Work List + **Spec Coverage(공백 0)** + 게이트 배치표 + Release Gate 로 재작성 | `30-work/work-001-data-foundation.md` 외 4건, `30-work/README.md` |
| 2026-09-02 | spec-add · SPEC-004 (v0.0.1 `draft`) · spec-change · SPEC-001·002·003·005 (→ v0.0.2 **`ready`**) · decision-update ×5 | **디자인 실현성 검토 반영.** 리뷰 판정은 전 페이지 FEASIBLE-WITH-CHANGES · **BLOCKED 0건**. SPEC-004 신규 — 라우트 `app/(ontology)` + `/ontology/{monitoring,chat,data}`(기본 진입 monitoring, 기존 `/chat` 불변) · 셸 계약 · **토큰 레이어**(`globals.css` 불변 + `--ont-*` 스코프 + `color-scheme: light` 재선언, hex 는 디자인 `01-tokens.md` 가 SoT — 전사 금지) · **Pretendard 를 next/font 로**(CDN `@import` 금지, 데모 그룹에만) · 최소 폭 스코프 · 접속 게이트 화면 · **파생 카운트 12항목 하드코딩 금지 표** · `node_id`/`edge_id`/URL 직렬화 · 화면 U-1~U-15 · AC 23건. **사용자 확정 4건**: ① 채팅 우측 520px 근거 그래프 패널 **제거** — `used_edges` 는 답변 안 칩 + 칩 클릭 시 `?edge=` 점프로 일원화, 게이트 5-③ 검증 대상 = **모니터링 그래프 단일**(SPEC-005 OQ-4 닫힘, 리포트 C-4 A/B 안 둘 다 폐기) ② Pretendard 도입 ③ 답변 패턴 C(계수 기반 정량 추정) **문구 제거** — 관계 방향·근거(r·n)까지만, SPEC-005 §5 에 「도구가 주지 않는 수치 생성 금지」 명문화, 계수 도구는 범위 밖 ④ 월 집계 = **골드 `gold_kpi_monthly` View**(빌드 생성, 도구는 조회만 — S-002 유지). 개정: SPEC-001 월 View·`v_bronze_reviews` 에 `authorName` 추가·**`node_id` 25종 표**(snake_case + 좌표 + ※ 정본 대조 6행)·lag `<정수>d` 통일 / SPEC-002 `grain=monthly` 출처 확정·`edge_id` / SPEC-003 `cards`(dod·dod_pct·unit·format·grain·spark·node_id·기간)·`graph`(kind·note·노드 source·edge_id)·`forecast`(title·message·edge.evidence, 수치를 기록 07 정본으로 정정 — 취소율→예약 `0d`·중간·r=−0.583, 강남언니→신환 `14d`·낮음·r=0.691 n=30)·`lineage`(rule_id·gate·downstream·is_provisional)·`tables.flows_to` / SPEC-005 `drilldown`·`followups`·`citations[].row_count`·`used_edges[].edge_id`·정량 추정 금지·G5-③ 화면 확정. 「제안」 21건 전건 승인으로 딱지 제거(SPEC-001 OQ-3 행수만 빌드 실측 유지). **디자인 조정 대기 20건**은 SPEC-004 §7.2 표로만 남겼다 — spec 이 대신 그리지 않는다 | `20-spec/spec-004-three-screens.md` 외 4건, `20-spec/README.md`, `10-decision/README.md` |
| 2026-09-02 | spec-add · SPEC-001 · 002 · 003 · 005 (전건 v0.0.1 `draft`) · decision-update ×5 | **decision 5건을 발주 가능한 계약으로 내림.** SPEC-001 데이터 계층 — 계층 경계(상위는 바로 아래만 읽음·브론즈 불변)·브론즈 16테이블/실버 6/골드 4/온톨로지 2 목록과 행수 대조값(vegas **78,216** · 리뷰 **1,962** csv 파서 기준[`wc -l` 2,118] · nexus 14종 기록 02 실측)·마스킹 뷰 `v_*` 와 **표기 형식 확정**(`김○○` · `010-****-1234` · `1990-**-**`)·게이트 실패 코드 7종·AC 9건(재현 대조값 매출 2,615,555,218 · 결제내원 5,428 · 신환 3,447 · 내원 47,537 · 일별 235). SPEC-002 도구 4종 MCP — 파라미터/응답/에러 10종·allowlist 판정·상한 초과는 **절단 아니라 거부**·`trace_ontology` 는 판정 5종과 기각 사유를 응답에 싣고 보류·기각은 `usable_for_causal_claim:false`·AC 11건. SPEC-005 에이전트 — 루프 3단계(전제 검증→엣지 역추적→근거 수치)·**답변 스키마 SoT**(`used_edges`·`citations`·`premise_correction`·`unknowns`·`excluded_edges`)·게이트 5종 수치 기준·회귀 3본(R-1 노쇼율 4주 · R-2 8월 매출 전제교정 · R-3 브론즈 드릴다운 20건)을 기록 08 실측값으로 단언. SPEC-003 API — 엔드포인트 15개·화면 7종↔엔드포인트 매핑·접속 게이트(env `ONTOLOGY_DEMO_PASSWORD` 1개, 값 미기재·rate limit 없음)·채팅 폴딩+2초 폴링·에러 14종·AC 12건. **SPEC-004(화면)는 결번** — 디자인 세션 귀환 대기. 기록에 근거 없는 설계 선택은 전부 「제안 — 리뷰 필요」로 OQ 표에 표시(20-spec/README 에 집계). DEC 5건의 Resulting Spec 표를 실 ID 로 갱신하고 미결 표 이관 처리 | `20-spec/spec-001-data-layer-contract.md` 외 3건, `20-spec/README.md`, `10-decision/README.md` |
| 2026-09-02 | decision-update · DEC-002 | **근거 개념 연결.** 신설 개념 `pii-masking`(출처: 기록 03 글로서리의 PII 반입 규칙)을 `up:` 에 추가하고 「근거 개념」 절에 역할 한 줄 — 원값 보존과 표면 마스킹의 분리, 가리는 지점의 마스킹 뷰 단일화가 채택안의 근거다. 기존 `medallion-architecture` 는 유지(브론즈도 마스킹·접근통제가 별개로 필요하다는 함정 항목). DEC-004 는 프론트 개념 출처 노트 부재로 `up: []` + 사유 유지 | `10-decision/decision-002-pii-masking-boundary.md`, `[[pii-masking]]` |
| 2026-09-02 | decision-add · DEC-001~005 전건 `accepted` | **2026-09-02 사용자 문답으로 확정된 결정 5건을 내림.** DEC-001 DB = 메달리온 전 계층(브론즈 원형까지 한 SQLite) + 빌드는 입출력만 DB 로 이식(변환 규칙·게이트는 기록 04·05 그대로). DEC-002 PII 는 DB 원값 보존 + 화면·API·에이전트 응답 마스킹, 에이전트도 마스킹 뷰 경유만 — 내부 공유 데모라 소거 사본은 만들지 않는다(전제가 바뀌면 재검토). DEC-003 LLM SDK 직접 import 금지(ADR-04 = KDEV-DEC-027 현행 유지), open-kknaks(AgentClient + RedisBroker) 경유 · codex `gpt-5.6-terra` · 조회 도구 4종은 `app/ontology-agent/tools/` FastMCP(Streamable HTTP) — **자유 SQL 도구 없음**(S-002). DEC-004 기록 09 의 「FastAPI 단일 페이지 또는 Streamlit」을 뒤집어 `app/front/` 통합 3페이지(채팅·모니터링·데이터), 화면 7종 매핑 확정, 백은 API 서버로 축소. DEC-005 내부 공유 배포 — 프론트 Vercel · 백/redis/워커 홈서버(docker + NPM), 가드는 공유 비밀번호 하나(rate limit·토큰 상한 없음), 배포 DB 는 홈서버 볼륨. `up:` 은 DEC-001·002 `medallion-architecture`, DEC-003 `ai-agent`·`prompt-injection`, DEC-005 `container`·`reverse-proxy`, DEC-004 는 대응 개념 부재로 `[]` | `10-decision/decision-001-db-medallion-all-layers.md` 외 4건, `10-decision/README.md` |
| 2026-09-02 | baseline-accept · BASE-001 `raw` → `accepted` | 사용자 리뷰 통과. `links.decisions` 에 DEC-001~005 연결, 00-baseline index 의 Status·Decision 열 동기 | `00-baseline/baseline-001-demo-agent-app.md`, `00-baseline/README.md` |
| 2026-09-02 | baseline-add · BASE-001 `raw` | **데모 에이전트 앱 baseline 등재.** 기록 09(데모 에이전트 앱 구축 계획)를 날것 입력 1건으로 내림 — 원칙(앱은 기록 01~08 구축 순서 재현, 어느 계층이든 원본까지 드릴다운) · 아키텍처 결정 7건 · 화면 7종 · 구현 순서 8단계 · 게이트 5종 · 범위 밖 요약. 웹 형태는 기록 09 의 「FastAPI 단일 페이지(또는 Streamlit)」 미결이 정식 프론트(`app/front/` 통합 3페이지)로 전환된 경과를 「가능한 방향」에 부기 — 확정은 10-decision 몫 | `00-baseline/baseline-001-demo-agent-app.md`, `[[2026-09-01-ontology-09-agent-app-plan]]` |
