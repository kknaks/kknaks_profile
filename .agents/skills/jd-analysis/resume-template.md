<!--
  resume 템플릿 — 분석 문서 §4 어필 전략의 산출물.
  자리: resume/YYYY-MM-DD-<회사slug>/resume.md
  §4(항목×소스 매핑 · 약점 경합 · 배치)가 확정된 뒤에만 쓴다 — 전략 없이 쓰면
  나열식 이력서가 된다.

  작성 규칙
  - 이름·기본 직함·연차·지역·email·github·linkedin·기본 stack 은
    `para/areas/personal/summer-star/profile.md` 만 본다. 이 파일은 운영 DB(profile)의
    매일 생성 파생물이므로 seed_profile.py 나 옛 이력서보다 우선한다. 현재 회사 역할을
    직함에 덧붙일 때만 `para/areas/personal/company/`의 career DB 파생 문서를 확인한다.
  - frontmatter 에 전략 요약을 박는다 — 문서를 다시 열었을 때 「왜 이렇게 썼나」가
    보이게. PDF 에는 frontmatter 가 안 들어간다.
  - 기술 표는 한 번만. 경력 절마다 스택을 반복하지 않는다.
  - 경력은 최신순. 각 bullet 은 「문제 → 판단 → 결과」로, 결과는 수치·출시·운영으로
    끝낸다. 기능 서술의 주어는 사용자다 ("의사가 ~하는 동안", "상담사가 ~할 수 있게").
  - 사이드는 부록이 아니다 — 회사 경력으로 못 덮는 축(§4.1)의 본 증거다.
    각 제품: ① 무엇을 하는 제품인지(쓰는 사람 관점 1~2문장) ② 내가 만든 범위와 결과
    ③ 링크(DB project.links / product.links 에서 — 스토어·PyPI·repo·라이브).
  - 침묵 항목(§4.2)은 어디에도 쓰지 않는다. 없는 실물을 나열하는 것이 최악수다.
  - 톤은 SKILL.md 「문장 톤」 그대로. 작성 후 `$korean-humanizer`를 적용한다
    (SKILL.md 절차 9 — 산문 줄표 제거·제목 구분자 보존).
  - 줄표(—)는 제목(h3 · bullet 머리)의 구분자로만 쓴다. 산문 문장 안에는 쓰지
    않는다 — 문장을 나누거나 어미로 잇는다.
  - 링크 행(→ …)은 반드시 앞 문단과 빈 줄로 띄운다 — 붙이면 PDF 에서 스타일이
    안 먹는다.

  톤 기준 — `resume/2026-08-26-allosta/resume.md`
  - 이 파일은 문장 밀도와 카드 구성만 참고한다. 사실·수치·링크의 근거로 쓰지 않는다.
  - 이력서는 서술형 경력기술서가 아니라 증거 카드 모음이다. 「무엇을 했다」보다
    「어떤 문제를 어떤 판단으로 바꿔 결과가 얼마였나」가 먼저 보여야 한다.
  - 소개는 2문단·문단당 2문장 이내. 첫 문장에 역할과 가장 강한 수치, 다음 문장에
    문제 해결 방식이나 오너십을 둔다. 포부·성장 의지·지원 동기 문장은 넣지 않는다.
  - 회사 설명은 한 줄로 끝낸다. `~를 맡았습니다` 같은 도입을 여러 번 반복하지 않는다.
  - 경력 bullet 은 **결과가 보이는 굵은 제목** + 본문 1~2문장. 한 카드 안에서
    배경 설명·기능 목록·아키텍처 해설을 모두 하지 않는다.
  - 제목에는 제품/문제와 결과를 같이 쓴다:
    `**뉴스·공시 조회 성능 — 처리량 3.2배**`.
  - 본문은 `문제 → 핵심 판단/변경 → 결과`만 남긴다. 결과가 제목에 있으면 본문에서는
    측정 조건이나 세부 수치만 보강한다.
  - 결과 없는 업무는 기능을 길게 설명하지 않는다. 범위와 핵심 판단만 한 문장으로 쓰고,
    분석 문서에서 `없음 — 발굴 필요`로 둔 결과를 만들어내지 않는다.
  - 사이드 프로젝트는 사용자 관점 1문장 + 내가 만든 범위·출시 결과 1문장으로 끝낸다.
-->

---
target: <회사 (법인명)> · <포지션>
jd: <URL>
based_on: <분석 문서 파일명> §4 어필 전략
심사축: <공고의 역량 축 요약>
정면: <수치로 정면 부각할 카드들> / 경합: <약점 방어 항목> / 침묵: <안 쓰는 것>
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# <이름>

<직함 한 줄>\
<지역> · \
email : <이메일>
github : <URL>
blog : <URL>

<!-- 헤더 규약 — build_pdf.py 가 이 형식을 파싱한다:
     h1 직후 문단에서 라벨 없는 줄(직함·지역)은 이름 아래 한 줄로,
     email/github/blog 라벨 줄은 우측 연락처 블록(이메일/깃허브/블로그)으로 간다.
     URL 의 https:// 는 표시에서 자동 제거된다. -->

## 소개

<2문단·문단당 2문장 이내.
첫 문단 — 역할·약점 경합의 선제 재프레임 + 가장 강한 결과 수치.
둘째 문단 — 오너십·AI 축의 실물. 포부·지원 동기·일반론은 쓰지 않는다.>

## 기술

| 구분 | 사용 기술 |
|---|---|
| 서버 / 프론트·모바일 / AI / 인프라 | <실제 쓴 것만. 침묵 항목 미기재> |

## 경력

### <회사> — <역할> · YYYY.MM – <YYYY.MM|현재>

<회사·담당 한 줄.>

- **<제품/문제 — 결과 수치>.** <문제 → 핵심 판단/변경 → 측정 조건·세부 결과. 1~2문장.>
- ...

## 사이드 프로젝트 — <사업체명>

<한 줄 — 완주·라이브 선언.>

### <제품명> — <출시/운영 상태>

<사용자 관점의 제품 설명 1문장. 내가 만든 범위와 출시·운영 결과 1문장.>

→ [App Store](<>) · [Google Play](<>) · [PyPI](<>) · [github](<>)

## 교육

- <과정> · YYYY.MM – YYYY.MM
