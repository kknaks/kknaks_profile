# [architect] SPEC-007 재작성 — 회의 중 · STT · 2트랙 · AI 배치

너는 **task-management `architect` 워커**다.
역할 문서: `/Users/kknaks/orca/workspaces/kknaks_profile/task-management-app/orchestration/roles/task-management/architect/role.md`
작업 워크트리: `/Users/kknaks/orca/workspaces/kknaks_profile/task-management-app` (문서 레포 · 코디 워크트리)

**산출물: `para/projects/summer-star/task-management/20-spec/spec-007-meeting-live.md` 1개.** 지금 없다 — 새로 만든다.

## ⛔ 0. 지난 SPEC 이 왜 폐기됐나 — 이걸 먼저 읽어라

2026-09-06, 회의록 SPEC 3개(2,552줄)와 WP 3개(1,048줄)를 **전부 지웠다.** 코디가 브리프에
**「이번엔 시안이 정본이다. 전 화면을 열고 쓴다」**를 맨 위에 박고, 표에서 기획서를 **「왜 만드나」**로
내렸기 때문이다. 그래서 워커는 **시안 2,882줄에 보이는 것을 전부 계약으로 옮겼다.**

**기획에 없는데 계약이 된 것**
- 시작 전 **AI 안건 생성** — `POST /agendas/draft` · `agenda_draft_failed` 를 팠다.
  기획 BASE-003 L37 은 **「AI 제안은 필요 없다 — 회의 중 AI는 AI 탭만 채우면 된다」**로 배제한다
- **「되돌리기」**(시안 L1883) — 편집 이력 표가 ERD 에 없고 정책은 「포커스 해제 자동 저장」이다
- **상태 바 파형**(시안 L813~829) — 오디오 분석이 캡처 경로에 없다
- **취소된 회의 행**(시안 L120~127) — status 4종(`scheduled|recording|generating|ended`)에 없다
- **「내 목소리 등록됨」**(시안 L644) — DEC-003 §2 「화자 표기: 참석자·발언자 이름 없음」 위반

**기획에 있는데 OQ 로 올라간 것**
- **회의 상세 드로어**(`S008-OQ-18`) — DEC-005 §2 L41 에 답이 있었다. **안 찾은 것이다**

**그리고 OQ 가 66건 나왔다.** 대부분은 **SPEC 이 정하면 될 것**(API 경로·에러코드·검증 규칙·수치)을
사용자에게 물은 것이다. DEC-003 L141 이 **「수치는 spec 에서 정한다」**고 이미 위임해 놨다.

---

## ⛔ 1. 정본 — 축을 나눈다

| 무엇 | 정본 |
|---|---|
| **기능** — 어떤 화면·필드·상태·흐름·문구·API 가 있나 | **기획 BASE-003 + 정책 DEC-003** |
| **시각** — 색·크기·간격·배치 | **디자인 시스템 + 시안 `.dc.html`** |

**판정은 기획·정책 한 축뿐이다. 시안은 판정에 들어오지 않는다.**

| 기획·정책 | 판정 |
|---|---|
| **있다** | **만든다.** 시안에 화면이 있으면 그 배치를 따르고, **없으면 디자인 시스템 컴포넌트로 조립한다** |
| **없다** | **안 만든다.** 시안에 그려져 있어도 |

- **시안에 있고 기획·정책에 없다** → 만들지 않는다. **조용히 빼지도 마라** —
  §7 에 **「시안 L### 에 있으나 기획·정책에 없어 제외」**로 줄 번호와 함께 적는다
- **기획·정책에 있고 시안에 없다** → **만든다.** 「시안 없음」은 막힐 이유가 아니다.
  정책이 「무엇과 같은 구조」라고 지정했으면 그 화면을 쓰고, 아니면 디자인 시스템으로 조립한다.
  **이걸로 OQ 를 만들지 마라**

## ⛔ 2. OQ 규칙 — 막지 않는다. 근거를 요구한다

**OQ 를 0 으로 맞추려고 구멍을 덮지 마라.** 진짜 구멍은 반드시 올려라. 대신 **가짜 OQ 를 쓰지 마라.**

| 종류 | 무엇 | 처리 |
|---|---|---|
| **가짜** | ① 기획·정책·다른 DEC·기존 SPEC 에 답이 있는데 못 찾음 ② **SPEC 이 정하면 되는 것** — API 경로·요청/응답 필드·에러코드·검증 규칙·수치·화면 조립 | **쓰지 마라. SPEC 이 채운다** |
| **진짜** | 기획·정책에 정말 없다 / 기획과 정책이 **서로 충돌**한다 | **§7 에 올린다** |

**OQ 를 하나라도 쓸 거면 그 항목에 반드시 함께 적어라.**
```
- 어디를 찾았나 — 파일·섹션·grep 한 문자열과 결과 건수
- 왜 SPEC 이 못 정하나
```
**이게 없는 OQ 는 반려한다.** 안 찾고 올린 것으로 본다.

**찾아야 할 곳(최소)**
```
00-baseline/baseline-003-meeting-notes.md
10-decision/decision-003-meeting-notes.md
10-decision/decision-001-auth-settings.md · decision-002-my-tasks.md · decision-004-library.md · decision-005-calendar.md
20-spec/spec-003-tasks-crud.md · spec-004-tasks-status-views.md · spec-005-library.md · spec-009-calendar.md
40-architecture/  (전부)
orchestration/work/docs-v1/design-requests.md  §A 승인된 정정
```

## 3. 읽을 것

| 순위 | 파일 | 역할 |
|---|---|---|
| 1 | `para/projects/summer-star/task-management/00-baseline/baseline-003-meeting-notes.md` | **§Raw(L33~43) + 기능명세 15행(L61~77) + 인바운드/아웃바운드.** 여기 없으면 안 만든다 |
| 2 | `para/.../10-decision/decision-003-meeting-notes.md` | **정책 §1~§8 + STT·배치 파이프라인(L154~164).** 뒤집지 마라 |
| 3 | `orchestration/work/docs-v1/design-requests.md` §A | 승인된 정정 — **시안보다 우선** |
| 4 | `para/.../00-design/회의록.dc.html` | **시각만.** 담당 줄 범위는 아래 §4 |
| 5 | `para/.../00-design/디자인 시스템.dc.html` **[09] MEETING NOTE(L649~755)** + 05·06·07·10 | 공통 규격 · 조립 부품 |
| 6 | `para/.../40-architecture/` (`database/domains/meeting.md` 포함) | 기존 계약·ERD |

**`00-design/*.md` 요약본을 열지 마라.** 그걸 읽고 시안을 안 본 것이 첫 번째 사고였다.

**형식·깊이 기준** — `20-spec/spec-003-tasks-crud.md` · `spec-004-tasks-status-views.md`

## 4. 네 담당 — 기능명세 #3 · #4 · #5 · #6 · #7 + 일시정지

**BASE-003 기능명세(L61~77)에서 네 것**

| # | 기능 | 근거 |
|---|---|---|
| 3 | **실시간 받아쓰기** — 마이크 캡처 → 백엔드 중계 → Soniox → 트랜스크립트·화자 분리 → 프론트 | BASE-003 L65 · DEC-003 STT 표 |
| 4 | **녹음 원본 저장** — 백엔드가 중계하며 파일 적재 | BASE-003 L66 · DEC-003 §6 |
| 5 | **사람 트랙** — 회의록 탭, 프롬프트(`/`)로 안건·줄 직접 입력 | BASE-003 L67 |
| 6 | **AI 트랙(배치 요약)** — AI 요약 탭. 같은 구조를 배치가 독립적으로 채움 + 근거 타임스탬프. **회의 중 증분만** | BASE-003 L68 · DEC-003 §4 |
| 7 | **근거 타임칩** — 줄 펼침 → 상세 + 구간 칩 → 클릭 시 스크립트 해당 발화로 스크롤·하이라이트 | BASE-003 L69 |
| — | **일시정지** | DEC-003 §1 표 · L61~67 |
| — | **회의 중 첨부 열기** | BASE-003 L73 |

**담당 시안 줄 범위**

| 화면 | 줄 |
|---|---|
| 회의 중 | **761~1047** |
| 회의 중 · 요약 | **1048~1197** |
| 회의 중 · 첨부 파일 열기 | **1198~1402** |

**이 범위에서 이미 확인된 「기획에 없는 것」 — 빼고 §7 에 적어라**
- **상태 바 파형**(L813~829) → 오디오 분석이 캡처 경로(DEC-003 STT 표)에 없다. **상태 바는 문구만**(「기록 중 · 경과 시간」)
- **「회의실 A」 장소**(L795) → DEC-003 §1 표

**정책이 이미 닫아 놓은 것 — OQ 로 올리지 마라**
- **줄 종류는 4종**(논의·결정·**업무**·액션). 시안 회의 중 팝오버(**L925~936**)가 3종만 그린 것은 **누락**이다(DEC-003 §1 표 L39, 근거 L49~54). **「업무」 줄을 넣어라**
- **AI 는 AI 요약 탭만 채운다.** 사람 트랙에 개입하지 않고 **줄 제안도 하지 않는다**(§4 L95)
- **AI 안건**은 `track='ai'` 에만 존재하고 **회의 중 사람 탭에 보이지 않는다**(§4 L96)
- **AI 증분은 즉시 반영 + 배치 회차 표시**(「배치 2회 → 3회 → 종결」). 버퍼링 금지(§4 L97)
- **AI 요약 탭 렌더 = 안건 > 줄 트리 + 근거 칩 + 배치 회차 표시.** 카드 아님(DEC-003 OQ-1 L174)
- **일시정지**: 시안 헤더 L798·L1085·L1235 그대로. **마이크·스트림 실패도 일시정지 상태 + 사유 배너**, 재시작은 사용자가 누른다. **「다시 연결」 버튼을 만들지 마라. 자동 재연결 금지**(§1 표 L41 · §7)
- **첨부**: 자료함 md · URL 두 갈래만 실동작. **로컬 업로드는 UI 만 + 「v2에서 제공됩니다」 토스트, 네트워크 요청 0**(§1 표 L42). **「최대 50MB」는 문구만 그리고 계약에 담지 마라**
- **화자는 익명 라벨(화자 1/2)까지만**(§2 L74)
- **트랜스크립트는 확정 토큰만 적재**, 잠정 토큰은 화면 표시용(§3 L87)
- **300분 초과·세션 경계 복원은 다루지 않는다**(§4 L105)
- **캡처는 내 마이크만**(§STT L161)

**수치는 DEC-003 이 이미 확정했다(§STT L164) — 그대로 써라. 묻지 마라**
```
확정 발화 600자 · 안건 전환 즉시(미처리 80자 미만이면 생략) · 상한 180초
배치 타임아웃 120초 · 회의당 AI 세션 하나(동시 실행 금지)
모델 stt-rt-v5 · language_hints ["ko"] · enable_speaker_diarization true · endpoint detection 미사용
open-kknaks 워커 한 세션 유지 · 회의 시작 시 웜스타트(결과 무시)
```

**DEC-003 §7 이 열거한 실패만 처리한다 — 그 밖의 예외는 fallback 하지 말고 전파**
```
회의 중 배치 실패    → 조용히 넘기고 그 구간을 다음 배치에 합쳐 재시도. 사용자에게 표시 안 함
JSON 스키마 위반     → 그 배치 결과 전체 폐기. 부분 파싱 금지. 폐기 구간은 다음 배치로
없는 업무 참조       → 웜스타트 화이트리스트 검증. 밖의 ID 는 거부.
                       참조 틀린 줄은 taskId 만 떼고 action 줄로 강등 — 내용은 살린다
```
**웜스타트 컨텍스트** = 선택 프로젝트 + 그 프로젝트의 업무 + 미리 작성된 안건.
**무소속 회의는 무소속 업무를 준다**(§4 L98 · §8 L149).

**SPEC-006 이 정한 계약을 물려받는다** — 상세 응답·상태값·첨부. **SPEC-006 을 고치지 마라.**
어긋나면 §7 에 적어라(코디가 맞춘다).

## 6. 산출물 구조

SPEC-003·004 와 같게 —
```
1. Context (Meta · Business Requirement · Scope)
2. UX Contract (Placement + U-1..U-n)
3. Scenarios
4. Data Contract (API 표 · Request/Response · Validation · Case Matrix)
5. Flow (mermaid)
6. Acceptance
7. Open Questions   ← §2 규칙. 그리고 「시안에 있으나 기획에 없어 제외」 목록을 여기에
8. 근거 표
```

**§2 규칙**
- 화면 하나 = U-n 하나. **값마다 시안 줄 번호**를 단다
- **상태 · 문구 · CTA · 기대 결과** 넷을 반드시 쓴다
- 시안에 있어도 기획·정책에 없으면 U-n 에 담지 않는다

**§4 규칙**
- **화면에 나오는 모든 값의 출처를 정의한다.** 컬럼이 없으면 「ERD 변경 필요」로 명시한다(OQ 아님)
- **에러코드는 SPEC 이 정한다.** 다만 Case Matrix 의 행마다 **기획·정책의 어느 실패인지** 근거를 단다.
  DEC-003 §7 이 열거한 실패 밖의 것을 만들지 마라 — **「설계한 것 이외의 에러는 그대로 터뜨린다」(BASE-003 L43)**
- **fallback 을 만들지 마라**

## 7. 지킬 것

1. **기획·정책에 없으면 만들지 마라.** 시안에 있어도
2. **기획·정책에 있으면 만들어라.** 시안에 없어도 — 디자인 시스템으로 조립한다
3. **정책을 뒤집지 마라.** 충돌은 §7 OQ 로
4. **네 SPEC 파일 1개만 만든다.** 다른 SPEC·기획·정책·인덱스·시안을 건드리지 마라
   — 지금 워커 3명이 같은 워크트리에서 **동시에** 돈다. 남의 파일을 만지면 덮어쓴다
5. **커밋·push 하지 마라**
6. 막히면 물어라 — `orca terminal send --terminal term_6a4ac855-2a13-4484-b808-4c25182cbb2b --text "..." --enter`

## 8. Done Criteria — 코디가 이걸로 검수한다

- [ ] 담당 시안 화면을 전부 열었고 **모든 U-n 에 시안 줄 번호**가 있다
- [ ] **기획·정책에 없는 기능이 계약에 0건**이다
- [ ] §7 에 **「시안 L### 에 있으나 기획·정책에 없어 제외」 목록**이 있다
- [ ] **기획·정책에 있는데 「시안 없음」을 이유로 빠진 것이 0건**이다
- [ ] **OQ 마다 「어디를 찾았나」(파일·섹션·grep 문자열·결과 건수)가 있다.** 없으면 반려
- [ ] **가짜 OQ 0건** — API 경로·에러코드·검증·수치·화면 조립을 묻지 않았다
- [ ] **화면에 나오는 모든 값의 출처가 §4 에 있다**
- [ ] Case Matrix 의 행마다 DEC-003 §7 의 어느 실패인지 근거가 있다
- [ ] 정책을 뒤집은 곳이 없다
- [ ] **네 파일 1개 외에 아무것도 고치지 않았다**

## 9. 완료 보고 — 문구 변경 금지

```bash
orca orchestration send \
  --to term_6a4ac855-2a13-4484-b808-4c25182cbb2b --from term_95f47648-153b-4ec8-a4e4-97e9d0bebae3 \
  --type worker_done \
  --task-id <taskId> --dispatch-id <dispatchId> \
  --subject "<네 SPEC> 재작성 완료" \
  --body "U-n 개수와 시안 줄 / **기획·정책에 없어 제외한 것 목록(시안 줄 번호 포함)** / 시안에 없어 디자인 시스템으로 조립한 화면 / §4 계약 요약 / ERD 변경 필요 / **OQ 건수와 각각의 「어디를 찾았나」** / 정책 충돌"

orca terminal send --terminal term_6a4ac855-2a13-4484-b808-4c25182cbb2b \
  --text "[worker_done] <네 SPEC> 재작성 완료. 상세는 인박스." --enter
```
