# 리뷰 리포트 (재검수 2차) — strong-hajin-projects / WORK-006 수정1 (planner 모드, 2026-09-22)

## 판정: WARN — FAIL 0 · WARN 3 · 경미 4. **승인저지 0건 — 진행 가능**

1차 검수의 **F-1·F-2·F-3 · W-1~W-8 · R-6 열두 건이 전부 닫혔다.** 근거는 아래 처분표에 위치와 함께 있다.
수정으로 생긴 **직접 회귀는 없다** — 계약 불변식 I-1~I-8 원문 불변, AC 43 · 실측 15 · DEC 9 전수 유지,
미수행 상태 유지. 새로 적는 **WARN 3 · 경미 4 는 전부 한 줄~두 줄 수정**이고, 어느 것도 발주를 막지 않는다.

코디가 이미 정한 구현 선택(OQ-W03 arm64 이미지 · OQ-W04 ingress 경로 분기 · F-3 의 번호 방식)은
**재개방하지 않는다.** 아래 §4 에 「1차 검수와 다르게 처리했고 그것이 맞는 것」을 따로 적었다.

---

## 검수 범위

- 대상: `para/projects/summer-star/strong-hajin/30-work/work-006-tauri-wrapper.md` — **999 → 1282줄**
- 입력: `review-tauri-wp-report.md`(1차) · `tauri-wp-fix1-brief.md`(코디 판단 · 우선) · `tauri-wp-fix1-report.md` ·
  SPEC-006 v0.2.3 · DEC-005 D-01~09 · `orchestration/runbook.md`
- 실행한 검사 (전부 읽기 성격 — **빌드·테스트·실측·서버 접속·배포 0회**)
  - AC/실측/DEC/I 전수 카운트 · frontmatter `yaml.safe_load` · 템플릿 14절 대조 · Phase Status·`[x]` 스캔
  - 1차 지적 문구의 잔존 스캔: `strong-hajin-tauri` **0** · `실행은 아니다` **0** · `여덟 Phase`/`8 Phase` **0** ·
    `열 개` **0** · `1~5·7` **0** · 「마이크가 열리는 **유일한 자리**」 **1건(한정된 문장)**
  - 코드 기준 재확인(read-only): `…/Strong_hajin/strong-hajin-projects` HEAD **`a1f6791`** · `status --porcelain` **0건**
  - 수정으로 **새로 들어온 사실 주장** 검증: `settings.py` `developer_auth_enabled = {DEVELOPMENT, TEST}` ✓ ·
    `delivery/README.md:38` 인용문(「빌드는 회사가 통제하는 builder에서 수행하고 고객에게는 검증한 최종
    image archive와 digest·운영 문서·필수 OSS 고지만 전달한다」) **원문 일치** ✓ ·
    `liveTranscription.ts:13-15` 후보 셋 ✓ · `microphone.ts:17` 단일 MIME ✓ · `vite.config.ts` 평문 5173 ✓
- allowed_paths: 이번 판에서 `work-006-tauri-wrapper.md` **한 파일**과 `tauri-wp-fix1-report.md` 만 늘었다 —
  SPEC·DEC·index·log·제품 코드·인프라 **diff 0줄**. **이탈 0건.**

---

## 1. 1차 지적 전건 처분 — **12/12 닫힘**

| # | 1차 지적 | 판정 | 근거 (위치) |
|---|---|---|---|
| **F-1** | 「코드가 겹치지 않는다 / 워크트리를 따로 잡는다」 | **닫힘** | Meta `:56-77` — `Depends on work` 가 「**코드 접점이 있다**」로 뒤집히고 `App.tsx`·`labels.ts` 가 이름으로 박혔다. **「작업 단위와 워크트리」 절 신설**: 런북 §1 인용 · 「WORK 번호로 새 워크트리를 만들지 않는다」 · 새 워크트리가 생기는 유일한 경우와 base 조건 · 2026-09-05 사고 형태 명시. `Branch/PR`(`:115`)에서 `strong-hajin-tauri` **삭제**되고 기존 워크트리·브랜치로 교체. `Parallel work`(`:78-82`)는 「같은 워크트리 → 브랜치 간 충돌 없음, 대신 같은 파일에 워커 둘을 동시에 태우지 않는다」로 재작성. **관측값 단서**(「이 머지 상태는 관측값이지 고정된 사실이 아니다 — 발주 시점에 다시 센다」)도 붙었다 |
| **F-2** | 운영 주소를 실재하게 만드는 실행 Phase 부재 | **닫힘** | Phase **6a**(`:672`)/**6b**(`:740`) 분리. 6b 에 **산출물별 소유표**(차트·Application·values=인프라 워커 / arm64 이미지·프로파일 정리=`backend` / 정적 서빙=`frontend` / 배포 실행=코디 발주·검증)와 파일 범위·작업 7개·검증 6개·완료 기준이 있고, 완료 기준이 **「운영 https 주소가 실재하고 로그인이 선다 — Phase 8 의 입력이 충족됐다」**(`:799`). Phase 8 의존이 **`Phase 5 · Phase 6b · Phase 7`**(`:905`). Scope 「포함」도 **설계(6a)와 실행(6b) 둘 다**로 갱신(`:149-151`). 「문서 워커의 실행 금지는 Phase 1~9 에 적용되지 않는다 / 사용자에게 명령을 떠넘기지 않는다」가 Execution 머리말·Role Assignment·6b 「하지 말 것」 **세 곳**(`:339` `:121-122` `:775`) |
| **F-3** | Phase 7 발행이 운영 origin 없이 가능하다고 표기 | **닫힘** | **7(fixture 판·M-10) → 8(운영 origin 설정 → 최종 빌드 → 그 아티팩트 검증) → 9(그 아티팩트를 그대로 발행)**. Phase 7 「하지 말 것」에 「**여기서 Release 를 발행하지 마라**」(`:828`), Phase 8 「하지 말 것」에 「**검증한 뒤에 설정을 고치지 마라. 고쳤으면 다시 빌드하고 다시 검증한다**」(`:886`), Phase 9 「하지 말 것」에 「**여기서 빌드하지 마라**」(`:944`). **해시 대조**가 8 검증(`:896`)과 9 검증(`:948`) 양쪽에. 미결 게이트도 재배치 — OQ-T02 「**6b 의 ingress·DNS · 8 · 9**」, OQ-T03 「**9(발행) · 8 의 서명본 검증**」(`:1106-1108`) |
| **W-1** | 「Phase 1~5 는 막히지 않는다」 모순 | **닫힘** | `:276-277` — 「**Phase 1~5 의 macOS 축은** … **Windows 축은 OQ-W01 에 매인다**」 |
| **W-2** | M-1 fixture 의 https 누락 | **닫힘** | Phase 1 출력 ④ **fixture 둘**(`:356`) · 작업 3·7(`:365` `:381-`) · 실측표 M-1 방법(`:963`) · Phase 2 측정 규칙·검증(`:443` `:452`) 전부 「**평문 `localhost` 로 대신하지 않는다**」 |
| **W-3** | Phase 5 의 여덟 AC 가 인용인지 재측정인지 불명 | **닫힘 (1차 권고보다 엄격)** | Phase 5 에 **항목별 판단표**(`:617-628`) 신설 — M-9(AC-T01~04)·M-2b(AC-T28)·M-6(AC-T32·T38) **제품판 필수**, M-5(AC-T26) **제품 셸판 필요**, **인용은 AC-T27 하나**. 측정 규칙·검증에 「**인용한 증거는 인용임을 표시한다(출처·회차)**」(`:633` `:646`). §3 에서 따로 평가 |
| **W-4** | Application 「열 개」 | **닫힘** | Code Surface(`:238`)·6a 작업 2(`:700`) 모두 「**레포 9개** / 2026-09-22 클러스터 조회 10개」 |
| **W-5** | 「macOS 만 먼저 낸다」에 승인 주체 없음 | **닫힘 (1차 권고보다 강함)** | Phase 7 실패 조치(`:851-853`)가 「**Windows 축을 보류한다 · macOS 단독 출시를 기본 행동으로 쓰지 않는다 · 범위 축소는 OQ-W06 처분으로 사용자 승인 뒤**」. D-07 상태 칸(`:1090`)·OQ-W06 행(`:1117`)·Pre-deploy(`:1162`)·Done Criteria(`:1222`) 네 곳에 같은 규칙 |
| **W-6 ①** | OQ-W02 권장안 없음 | **닫힘** | `:1114` 「**권장안: 그 레포를 그대로 쓴다**」 + 차단을 **6b 작업 5(푸시·PR) 한 지점**으로 축소 |
| **W-6 ②** | OQ-W04 가 불필요한 사용자 gate | **닫힘** | 「구현이 고르는 것」 절로 이동(`:1136`), 6-3 이 **코디 기본값 A 로 확정**(`:688`) |
| **W-7** | 「마이크가 열리는 유일한 자리」 | **닫힘** | `:207` 「**회의 라이브에서** … (제품 전체로는 둘 — `microphone.ts:31` · `liveTranscription.ts:11`)」 |
| **W-8** | M-2 가 두 녹음 경로를 뭉갬 | **닫힘** | 실측표 M-2 가 **ⓐ 회의 라이브 / ⓑ 브라우저 인터랙션 × 셋 = 여섯 칸**(`:964`), Phase 1 계측 페이지도 두 경로를 따로 찍고(`:370-373`), Phase 2 측정 규칙·검증·**판정표 세 칸 전부**가 경로를 가른다(`:437-441` `:451` `:466-470`) |
| **R-6** | rollback 의 데이터 항목 보강 | **닫힘 (구조 교체)** | Rollback 이 **A 되돌리는 것 / B 지키는 것(보존·백업·복구 증거) / C 되돌릴 수 없는 것** 셋으로 갈리고(`:1165-1204`), **규칙 넷**(배포 실패를 데이터 삭제로 풀지 않는다 / 볼륨 삭제는 rollback 이 아니라 파기 / 백업 없으면 배포하지 않는다 / 복구 안 해 본 백업은 백업이 아니다). Pre-deploy 에 「**백업이 한 번 이상 확보돼 있다**」(`:1157`) |

**회귀 검증 (수치)**

| 항목 | 결과 |
|---|---|
| AC 전수 | SPEC 유니크 **43** / 추적표 행 **43** · 유니크 43 · **중복 0 · 누락 0 · 초과 0** (P3=15·P4=5·P5=21·P7=1·P8=1·6a/6b/9=0 명시) |
| 실측 전수 | 실측 계획표 **15행 · ID 증가 0**. 증거 단계 칸(① fixture ② 제품판 ③ 운영판) 신설, Phase 5 「기존 넷」은 **같은 ID 의 둘째 단계** |
| DEC | **9행 전수** — D-02·D-06·D-07·D-08·D-09 갱신, 상태 표기(받음/부분/경계만) 사실과 일치 |
| 계약 불변식 | **I-1~I-8 여덟 줄 원문 불변**(1차 검수본과 문자 단위 동일) |
| 선행 순환 | `1←∅ 2←1 3←2 4←3 5←4 · 6a←∅ 6b←6a · 7←5 · 8←5,6b,7 · 9←8` — **역참조 0 · 주인 없는 선행 0** |
| 미수행 상태 | `- [x]` **0건** · **Phase Status 10개 전부 `TODO`** · `progress: 0` · `status: todo` |
| 양식·YAML | 템플릿 14절 **전부·순서 일치** · frontmatter 필수 키 전부 · wikilink 2건 실재 |

---

## 2. 새로 적는 지적 — **WARN 3 · 경미 4 (전부 비저지)**

### WARN-1 — Done Criteria 가 **서버 배포(6b)와 데스크톱 설치파일(8·9)을 「같은 아티팩트」로 뭉갠다**

- 위치: `:1212-1213`
  > 「**운영 주소가 실재하고**(6b) **그 주소를 박은 판이 검증되고**(8) **그 판이 발행됐다**(9) — 셋이 **같은 아티팩트**를 가리킨다」
- 근거: 6b 의 산출은 **서버 쪽**이다 — 출력 ⑤ 「배포 기록(**이미지 태그**·sync 결과·경로별 응답)」(`:753`).
  8·9 의 산출은 **데스크톱 설치파일**이고, 그 둘 사이에서만 **해시 일치**가 요구된다(`:896` `:948`).
  6b 와 8·9 를 잇는 것은 같은 아티팩트가 아니라 **같은 운영 origin**이고, 서버·웹 판과 셸 판의 관계는
  **호환 조합 기록**(Phase 8 작업 6 `shell_api`·여는 주소 / Phase 9 작업 4 기록 셋)이다.
- 영향: 문장 그대로 읽으면 「서버 이미지와 설치파일이 같은 것」이 되어, 완료 판정 때
  **해시를 맞출 대상을 잘못 고를 수 있다.** 또 **서버·웹 배포 판 ↔ 셸 판의 호환 조합**이 한 자리에
  기록되지 않는다(6b 의 이미지 태그와 9 의 기록 셋이 서로를 가리키지 않는다).
- 최소 수정 (한 줄을 두 줄로)
  - 「**8 과 9 가 같은 설치파일 아티팩트를 가리킨다** — 해시(또는 빌드 식별자)가 같다」
  - 「**6b 의 배포 판(이미지 태그·웹 빌드)과 그 셸 판의 호환 조합**이 Phase 9 의 기록 셋에 함께 적혔다 —
    셸이 여는 운영 주소 · `shell_api` · **그때 서버에 떠 있던 이미지 태그**」

### WARN-2 — **AC-T27 인용의 전제(「서버·웹 변경 없음」)가 Phase 6b 뒤에는 보장되지 않는다.** 「최종판에서 달라진 것을 재검증한다」는 규칙이 없다

- 위치: Phase 5 판단표 `:624` · 측정 규칙 `:633` · AC 추적표 `:1046`
  > AC-T27 은 「**서버·웹 동작이고 배선과 무관**하다 → Phase 2 기록을 인용하고 인용임을 표시한다」
- 근거: 인용의 근거인 「배선과 무관」은 **Phase 4 웹 배선**에 대해서는 타당하다. 그러나 **Phase 6b 작업 3**
  이 고치는 파일이 바로 인증·세션 경로다 — `http.py` · `http_auth.py` · `settings.py`(`:750` 소유표).
  인용 증거는 **DEVELOPMENT 프로파일의 fixture**(`developer_auth_enabled = {DEVELOPMENT, TEST}` — 확인함)
  에서 나오고, 운영판은 **PRODUCTION + 6b 내부 정리**다. 6b 검증(`:790-795`)은 로그인·`Secure`·권한 불변까지
  보지만 **만료 동작은 보지 않고**, Phase 8 의 최종 확인은 `AC-T24`·M-1·M-5 뿐이다(`:868`).
- 영향: 43건 중 **유일하게 인용으로 닫히는 항목**이 하필 6b 가 만지는 표면 위에 있다. 문서 어디에도
  「최종판에서 달라진 부분의 인용 증거를 갱신한다」는 규칙이 없어, 조용히 낡은 증거로 닫힐 수 있다.
- 최소 수정 (둘 중 하나, 한 줄)
  - Phase 6b **검증**에 추가: 「**세션 만료 동작이 그대로다** — 만료된 세션으로 재접속하면 로그인 화면이고,
    시각 경과만으로 열린 것이 닫히지 않는다(AC-T27 의 인용 증거를 운영 프로파일에서 한 번 갱신)」
  - 또는 Phase 5 판단표 AC-T27 칸에 단서: 「**단 6b 가 인증·세션 경로를 고치므로, 그 변경 뒤 이 인용은
    6b 검증에서 한 번 갱신한다**」

### WARN-3 — Phase 2 판정표에 **「브라우저 인터랙션 경로가 아예 안 열리는 경우」**의 칸이 없다

- 위치: 판정표 `:466-470`
  - **진행** = 「M-1 왕복 성립 **그리고 회의 라이브 경로**의 M-2 세 갈래 모두 통과」
  - **보완** = 「… 또는 **브라우저 인터랙션 경로만 후보가 내려앉았다**(예: mp4 로 떨어졌고 **서버가 받는다**)」
  - **중단** = 「M-1 불성립 **또는 회의 라이브 경로**가 안 열린다」
- 근거: 세 칸 어디에도 **「후보 셋이 전부 실패한다」 또는 「서버가 그 업로드를 받지 않는다」**가 없다.
  그 경우 **진행 조건이 그대로 성립**하므로 처분 없이 Phase 3 으로 넘어가고, 문제는
  **Phase 5 의 AC-T07**(두 녹음 화면 각각)·**AC-T11**(두 화면 어디서든 U-3)에서야 드러난다.
  W-8 수정이 「브라우저 경로만 **내려앉은**」 경우는 정확히 다뤘으나 **완전 실패**는 칸이 비었다.
- 영향: 「회의 MIME 만 성공하면 무조건 진행」하는 틈이 한 칸 남는다. 중단 사유로 올릴 필요는 없다
  (래퍼의 목적은 회의 녹음이고 브라우저 경로는 이미 후보를 갖는다) — **처분이 비어 있는 것**이 문제다.
- 최소 수정 (보완 행 한 구절): 「… 또는 **브라우저 경로가 아예 열리지 않는다**(후보 셋 전부 실패 또는
  서버가 그 업로드를 거부). 이때는 **그 사실을 기록하고 `AC-T07`·`AC-T11` 의 브라우저 축을 미통과로 연 채**
  Phase 3 을 착수하며, 후보 보강을 별도 work 로 올릴지는 코디가 판단한다」

### 경미 4건 (문구·한 줄)

| # | 위치 | 내용 | 최소 수정 |
|---|---|---|---|
| **m-1** | `:1256` log 갱신안 | 「**9 Phase**(6a·6b 포함)」 — 실제 단계는 **열**(1~5 · 6a · 6b · 7 · 8 · 9)이고 최대 번호가 9다. Status Board 의 `1–9`(`:1251`)와 Done Criteria 의 「Phase 1 ~ 9 … (6a·6b 포함)」(`:1208`)는 정확하다 | 「**10 Phase**(1~5·6a·6b·7·8·9)」 |
| **m-2** | Phase 1 의존 `:406` | 「없음. **미결 어느 것에도 막히지 않는다**」 — 선행 의존표 끝(`:276-277`)은 이미 macOS/Windows 축으로 한정했는데 이 줄만 무한정이다. Windows 쪽 탐침 실행·마이크 프롬프트(M-4)는 OQ-W01 에 매인다 | 「… 막히지 않는다(**macOS 축 기준** — Windows 탐침 실행은 OQ-W01) 」 |
| **m-3** | Phase 6b 의존 `:802` | 「Phase 6a · OQ-T02. 작업 5 만 OQ-W02」 — **P-5(인프라 워커 정의)** 가 빠졌다. P-5 자체는 계획 안에 있다(Role Assignment `:133` · 6b 소유표 `:747` · Open Issues `:1244` 「**Phase 6b 의 선행이다**」 · 6a 작업 7 이 발주 단위를 가른다) → **§2-7 요구는 충족**이고, 의존 줄 한 줄이면 완전히 닫힌다 | 의존에 「**P-5 인프라 워커 정의(코디)**」 추가 |
| **m-4** | 6a 작업 4 `:702` · 6-4 처분 `:690` | 운영용 **비보호 arm64 이미지**를 따로 만드는 기본안에서, 그 이미지가 **납품 경로로 새지 않도록 하는 경계**(레지스트리·태그 네임스페이스 분리)가 태그 규칙에 명시되지 않았다. `delivery/README.md:38` 의 보호 요구는 **고객에게 전달되는 아티팩트**에 걸린다 | 작업 4 에 한 줄: 「**운영 이미지는 납품 이미지와 레지스트리·태그 네임스페이스를 분리한다** — 운영용 비보호 이미지가 납품 경로로 나가지 않는다」 |

---

## 3. 코디가 지정한 확인 항목별 답

| # | 확인 요구 | 답 |
|---|---|---|
| **1** | F1~3·W1~8·R6 전건 처분 / 워크트리 유지 / 운영 구현 주체·파일·완료 기준 / origin→최종빌드→검증→해시 동일판 발행의 도달 가능성 · 의존 공백·순환 0 | **전건 닫힘(12/12).** 워크트리는 기존 것을 잇는다고 Meta·Work Summary 두 곳에 박혔다. 6b 가 산출물별 소유·파일 범위·완료 기준(「운영 https 주소가 실재하고 로그인이 선다」)을 갖는다. 발행 사슬은 **8 이 설정→빌드→검증하고 9 가 해시 대조 후 그대로 발행**하며 8·9 양쪽에 해시 항목이 있다 — **도달 가능**하고 **의존 공백 0 · 순환 0** |
| **2** | 6a·6b ↔ 8·9 책임 일치, **서버 이미지와 설치파일을 같은 아티팩트로 뭉개지 않았는가** | Phase 본문은 잘 갈라져 있다(6b=이미지 태그·sync 기록 / 8·9=설치파일 해시). **다만 Done Criteria 한 문장이 셋을 「같은 아티팩트」로 묶는다 → WARN-1.** 해시 일치가 필요한 곳은 **8↔9**, 호환 조합을 기록해야 하는 곳은 **6b 의 배포 판 ↔ 9 의 기록 셋**이다 |
| **3** | W-3 의 강화가 타당한가 / AC-T27 인용 가정이 6b 뒤에도 유효한가 / 최종판 변경분 재검증 규칙인가 | **강화는 타당하다** — 「AC-T01 이 〈회의 업스트림으로 마이크가 열린 뒤〉를 조건으로 건다」는 근거는 SPEC 문장 그대로이고, M-6·M-2b 를 제품판으로 올린 것도 맞다(「하나만 뜨는가」·「점유가 유지되는가」는 구현 뒤에만 난다). **AC-T27 인용 가정만 6b 뒤 유효성이 비었고, 변경분 재검증 규칙이 없다 → WARN-2** |
| **4** | W-8 — 두 경로 각각 포맷·서버 수용, 브라우저 경로만 실패한 경우의 gate, 회의 MIME 만 성공하면 무조건 진행하는 틈 | 경로 분리와 **서버 수용을 WS 스트림 / 멀티파트 업로드로 각각** 보는 것까지 반영됐고, 중단 조건을 회의 경로로 한정한 것도 맞다. **「후보가 내려앉은」 경우는 보완으로 잡히지만 「아예 안 열리는」 경우의 칸이 비어 틈이 한 칸 남는다 → WARN-3** |
| **5** | 사용자 gate 축소가 지시대로인가 / FE 서빙·ARM 이미지를 다시 사용자 결정으로 올리지 않았는가 / 보호·권한 의미 보존 | **지시대로다.** OQ-W03·OQ-W04 는 「구현이 고르는 것」으로 내려갔고 코디 기본값이 박혔다 — **이 검수는 그 선택을 재개방하지 않는다.** 보호 보존: 기본안이 **납품 경로 파일을 한 줄도 바꾸지 않고**, 낮추는 선택만 사용자 결정으로 남았다(6a 작업 1 이 적용 범위를 먼저 확인). 권한 보존: 조건이 「**사용자에게 보이는 권한이 한 칸도 넓어지지 않는다**」 하나로 통일됐고, **development/test 우회 금지**가 Scope·6-1·6b·Pre-deploy 네 곳에 있다. 남은 한 틈은 이미지 유출 경계(**경미 m-4**) |
| **6** | Windows 축 보류 / 요약이 Windows 실측 필요성과 충돌하면 한정 / Phase 수 혼동 | 보류가 **기본 행동**이 됐고 macOS 단독 출시는 승인 뒤에만이다(네 곳). 요약 한정도 `:276-277` 에서 됐다 — **남은 무한정 문구는 Phase 1 의존 한 줄(경미 m-2)**. Phase 수 표기는 **경미 m-1** 로만 취급한다(지시대로) |
| **7** | P-5 인프라 워커를 구현 전에 준비하는 담당·위치가 계획에 있는가 | **있다** — Role Assignment(Ops = 코디 + 인프라 워커, 「아직 정의되지 않았다」) · 6b 소유표 · Open Issues P-5(「**Phase 6b 의 선행**」 + repo·allowed_paths 제안 + 「**코디 몫 — 사용자 gate 아님**」) · 6a 작업 7(발주 단위 분할). 실제 config 변경이 없는 것은 **FAIL 아님**. 의존 줄 보강만 **경미 m-3** |
| **8** | AC43·실측15·DEC9·I1~8 보존, 회귀만, 미수행 유지 | **전부 보존**(§1 수치표). **직접 회귀 0건.** `[x]` 0 · Status 10개 TODO · `progress: 0` — 미수행 유지 |

---

## 4. 1차 검수와 다르게 처리됐고 **그것이 맞는 것** — 재개방하지 않는다

| 자리 | 1차 검수 권고 | 이번 판 | 이 검수의 판단 |
|---|---|---|---|
| **F-3 번호 방식** | `7a / 7b` 분할 | **번호 +1**(7 → 8 → 9) | **이쪽이 낫다.** 7a/7b 로 가르면 실행 순서가 `7a → 8 → 7b` 가 되어 번호와 순서가 어긋나고, 그것이 F-3 이 막으려던 종류의 착오다. 지금은 `1→2→3→4→5→6a→6b→7→8→9` 가 단조다 |
| **W-3 인용 범위** | 「M-5·M-2b·M-6 은 인용, M-9 만 제품판」 | **셋 중 둘은 제품판 필수, M-5 는 제품 셸판, 인용은 AC-T27 하나** | **이쪽이 맞다.** 근거가 AC 문장 자체에 있다(「점유도 유지된다」·「하나만 뜨는가」·셸 설정 차이). 1차 권고가 느슨했다 |
| **OQ-W03 arm64 이미지** | 「이미지 하나가 느는 비용 결정이라 사용자 몫」 | **구현 선택 — 보호 수준을 낮출 때만 사용자 결정** | **코디 판단이 맞다.** arm64 이미지는 운영 목표에 필요한 구현이고, 기본안이 납품 경로를 건드리지 않으므로 보호 요구가 훼손되지 않는다. **재개방하지 않는다** |
| **OQ-W04 FE 서빙** | 「구현 선택으로 내려라」 | 코디 기본값 A 로 확정 | 권고대로. **재개방하지 않는다** |
| **OQ-W05 운영 프로파일** | 「별도 decision 으로 미는 것이 타당」 | **내부 정리는 구현 / 권한 완화·새 인증만 gate** | **이쪽이 정확하다.** 권한이 넓어지지 않는 변경은 새 계약이 아니다. 「권한 한 칸도 안 넓어진다」는 조건과 development 우회 금지가 그 경계를 지킨다 |

---

## 5. 남은 사용자 결정 — 이번 판 기준 **다섯 + 조건부 둘** (검수도 같은 판단)

`OQ-T01`(최소 OS·아키텍처·형식) · `OQ-T02`(운영 도메인) · `OQ-T03`(배포 대상·서명·공증·업데이트) ·
`OQ-W01`(Windows 실기) · `OQ-W06`(Windows 빌드 환경 + 범위 축소 승인)
— **조건부**: `OQ-W02`(6b 작업 5 의 org 푸시 범위) · `OQ-W05`(권한 완화·새 인증이 **필요해졌을 때만**).

**코디 몫(사용자 gate 아님)**: `P-4` frontend 워커 `verify` 에 Rust 검증 · **`P-5` 인프라 워커 정의(6b 선행)** ·
index·log 갱신 적용.

---

## 기존 부채 (이번 판정 제외)

- `work-005-projects.md:101` 「커밋은 아직 0건이다」가 여전히 낡았다(지금은 `a1f6791` 로 들어가 워크트리가 깨끗하다).
  WORK-006 밖의 문서라 이번에도 고쳐지지 않았다 — 코디 몫.
- `k8s_infra_mac/README.md` 의 `k8s-test` 설명이 실물(`main` 추적)과 다르다. 이 계획은 인용하지 않고 사실을 다시 센다.

## 검수하지 않은 것 (숨기지 않는다)

- **실측·빌드·테스트·설치·배포·서버 접속 0회.** 계획의 실행 가능성은 문서·코드 읽기로만 판단했다.
- `scripts/lint-pipeline.py` **미실행** — 레포에 없다(디렉토리 자체 부재). `rules/check-concept-map.py` 는
  1차 검수에서 exit 0 이었고 이번 수정이 개념 문서를 건드리지 않아 다시 돌리지 않았다.
- 인프라 레포는 **차트·Application 구조만** 읽었고 **비밀값 파일을 열지 않았다.**
- Tauri v2 의 `remote.urls`·네비게이션 훅·https fixture 의 실제 동작은 **검증하지 않았다** —
  그것이 M-1·M-3·M-7 의 목적이고, 이 계획이 미수행으로 둔 것이 맞다.
