# tauri-wp-fix2 결과 보고 — WORK-006 마지막 문구 정리

## 상태: done

R2 재검수의 **WARN 3 · 경미 4, 일곱 항목만** 고쳤다. **구조를 다시 설계하지 않았다** —
Phase 구성·번호·소유·의존 방향·AC 배정·실측 ID·계약 불변식 어느 것도 건드리지 않았다.

`work-006-tauri-wrapper.md` **1282 → 1303줄**(+21). 그 밖은 읽기 전용.
커밋·push·PR·실측·빌드·테스트·서버 변경·추가 워커 **0회**.

---

## 1. 일곱 항목 — 위치와 전/후

### WARN-1 — 서버 배포와 설치파일을 「같은 아티팩트」로 뭉갠 문장

**두 자리를 고쳤다.**

| 자리 | 전 | 후 |
|---|---|---|
| **Done Criteria** (`:1213` 근처) | 「운영 주소가 실재하고(6b) 그 주소를 박은 판이 검증되고(8) 그 판이 발행됐다(9) — **셋이 같은 아티팩트를 가리킨다**」 **한 줄** | **두 줄로 갈랐다.** ① 「**Phase 8 이 검증한 설치파일과 Phase 9 가 발행한 설치파일이 같다** — 해시(또는 빌드 식별자)가 일치한다. **해시를 맞출 대상은 이 둘뿐이다**」 ② 「**6b 의 배포 판과 그 셸 판의 호환 조합**이 Phase 9 의 기록에 함께 적혔다 — **운영 origin · `shell_api` · 그때 서버에 떠 있던 이미지 태그·웹 빌드 · 셸 판 번호**. **서버 아티팩트와 설치파일은 같은 것이 아니다** — 둘을 잇는 것은 **같은 운영 origin 과 이 조합 기록**이다」 |
| **Phase 9 작업 4** (`:941-943`) | 「기록으로 남길 것 셋 — 판 번호 · 그 판이 여는 운영 주소 · `shell_api` 값」 | 같은 줄 뒤에 **호환 조합**을 붙였다 — 「여기에 **그때 서버에 떠 있던 이미지 태그와 웹 빌드**(Phase 6b 의 배포 기록)를 함께 적는다. **서버 판과 셸 판은 같은 아티팩트가 아니다**」 |

**「서버와 설치파일이 같은 아티팩트」라는 표현은 문서에서 사라졌다** — 남은 `같은 아티팩트` 문자열은
Phase 9 작업 4 의 **부정문**(「같은 아티팩트가 **아니다**」) 하나뿐이다.
해시 일치 요구는 **8↔9 설치파일**에만 남았고(8 검증·9 검증·Done Criteria 세 곳 일관),
6b ↔ 9 는 **호환 조합 기록**으로 이어진다.

---

### WARN-2 — AC-T27 인용의 전제가 6b 뒤에는 보장되지 않는다

**세 자리를 고쳤다.** 인용은 **없애지 않고 「Phase 5 당시 임시 유효」로 한정**한 뒤, **갱신 자리를 6b 에 두었다.**

| 자리 | 무엇을 더했나 |
|---|---|
| **Phase 5 판단표 M-2b 칸** (`:624`) | 「AC-T27 은 서버·웹 동작이고 **«배선»과 무관**하다 → 인용」 뒤에 단서: **「⚠ 단 이 인용은 Phase 5 시점의 임시 유효다 — Phase 6b 작업 3 이 인증·세션 경로(`http.py`·`http_auth.py`·`settings.py`)를 고치므로, 그 변경 뒤 6b 검증에서 운영 프로파일로 한 번 갱신한다」** |
| **Phase 5 측정 규칙** (`:636` 근처) | **일반 규칙 한 줄 신설** — 「**증거 갱신 규칙 — 이 work 전체에 건다.** 뒤 Phase 가 그 증거의 전제였던 **코드·설정을 고치면**, 앞에서 딴 증거(인용이든 관측이든)를 **그 변경 뒤에 한 번 갱신한다.** 갱신하는 자리는 **그 변경을 낸 Phase 의 검증**이다. 지금 이 규칙이 실제로 걸리는 자리는 **AC-T27 ← Phase 6b** 하나다」 |
| **Phase 6b 검증** (`:795-801`) | 항목 하나 추가 — 「**세션 만료 동작이 그대로다 — AC-T27 의 인용 증거를 여기서 갱신한다.** **운영 프로파일의 «격리 검증 환경»** 에서 **AC-T27·SPEC §3 S-8 원문 그대로** 본다: ① 만료된 세션으로 **앱을 다시 열거나 스트림에 새로 붙으면** 로그인 화면으로 돌아간다 ② **시각이 지났다는 것만으로** 화면이 로그인으로 바뀌거나 **열린 스트림이 닫히지 않는다**」 |

**브리프의 금지 셋을 문장으로 박았다.**

- 「⚠ **운영 DB 의 세션 행을 조작하지 않는다** — 격리 환경에서 잰다」
- 「⚠ **SPEC 에 없는 동작을 만들어 확인하지 않는다** — **자동 401 로그인 전환**도, **열린 WS 의 자동 종료**도
  이 제품에 없다(`E-10`·S-8). **없는 것을 없는 대로 확인하는 것**이 이 항목이다」

**AC 주 배정과 실측 ID 는 늘리지 않았다** — AC-T27 의 주 배정은 **Phase 5 그대로**이고,
6b 는 **그 증거를 갱신하는 검증 항목**일 뿐이다. 실측 ID 는 **M-2b 하나 그대로**(15건 불변).

---

### WARN-3 — 브라우저 경로가 아예 안 열리는 경우의 칸이 없다

**Phase 2 판정표 두 행**(`:468-469`).

| 판정 | 전 | 후 |
|---|---|---|
| **진행** | 「M-1 왕복 성립 **그리고** 회의 라이브 경로의 M-2 세 갈래 모두 통과」 | 같은 조건 + **「그리고 아래 「보완」·「중단」의 어느 조건도 해당하지 않는다」** — 세 칸이 겹치지 않게 한 구절만 더했다 |
| **보완** | 「① M-3~M-9 중 일부가 어긋난다, 또는 ② 브라우저 경로만 후보가 **내려앉았다**」 | **③ 을 신설** — 「또는 **브라우저 경로가 아예 열리지 않는다**(**후보 셋 전부 실패** 또는 **서버가 그 업로드를 거부**)」. 「다음」 칸도 ①②③ 으로 갈라 적었다 |

**③ 의 처분**(「다음」 칸): 「**그 사실을 기록하고 `AC-T07`·`AC-T11` 의 브라우저 축을 미통과로 «연 채»**
Phase 3 을 착수한다 — **보완 담당과 결과 기록을 따로 세우고**(후보 보강을 별도 work 로 올릴지는 코디가
판단한다), **그 축이 닫히기 전에 「최종 지원판 검증」·「Release 완료」로 넘기지 않는다**.
**셸 구현 자체는 브라우저 축과 독립이라 계속 간다**」

브리프의 넷을 그대로 담았다 — ① 미통과 유지 ② 별도 보완 담당·결과 기록 ③ 독립 셸 구현은 가능
④ 최종 검증·Release 완료로는 넘기지 않음. **중단 조건은 회의 라이브 경로 그대로**(건드리지 않았다).

---

### 경미 4건

| # | 자리 | 전 | 후 |
|---|---|---|---|
| **m-1** | Open Issues 의 `log.md` 갱신안 (`:1275` 근처) | 「**9 Phase**(6a·6b 포함)에 배정」 | 「**10 Phase(1~5·6a·6b·7·8·9)** 에 배정」 — **실제 단계 열 / 최대 번호 9** 를 구분해 적었다 |
| **m-2** | Phase 1 의존 (`:406`) | 「없음. **미결 어느 것에도 막히지 않는다.**」 | 「… 막히지 않는다 — 단 **macOS 축 기준**이다. **Windows 쪽 탐침 실행과 마이크 프롬프트(M-4)는 장비 조건에 매인다**(OQ-W01)」 |
| **m-3** | Phase 6b 의존 (`:805`) | 「Phase 6a · OQ-T02(도메인). 작업 5 만 OQ-W02 에 매인다」 | 「Phase 6a · OQ-T02(도메인) · **P-5 인프라 워커 정의(코디 몫 — §Open Issues)**. 작업 5 만 OQ-W02 에 매인다」 |
| **m-4** | Phase 6a 작업 4 (`:705-708`) | 「운영 이미지 안을 쓴다 — 기본안 · 빌드 명령 · **태그 규칙** · 누가 굽는지」 | 뒤에 세 줄 추가 — 「**운영 이미지와 납품 이미지의 저장 자리를 갈라 적는다** — **기존 레지스트리 안에서 image repository 와 tag namespace 를 나누는 것으로 충분하다**(**새 레지스트리 서비스를 세우지 않는다**). ⚠ **운영용 비보호 이미지가 납품 경로로 나가지 않는 것**이 조건이다 — `delivery/README.md` 의 보호 요구는 **고객에게 전달되는 아티팩트**에 걸리므로, 두 계열의 이름이 섞이면 그 경계가 흐려진다」 |

---

## 2. 검증 — 변경 구간과 유지 불변식

### 유지돼야 하는 것 (전수 재확인)

| 항목 | 기준 | 결과 |
|---|---|---|
| AC 전수 | SPEC `AC-T01`~`AC-T43` | **43 / 43** — 누락 0 · 초과 0 |
| AC Phase 배정 | 중복 0 | 추적표 행 **43 · 유니크 43 · 중복 0** — P3=**15** · P4=**5** · P5=**21** · P7=**1** · P8=**1** (**배정 이동 0건**) |
| 실측 ID | 15 | 실측 계획표 **15행** — **증가 0**. WARN-2 는 M-2b 의 **증거 갱신 자리**만 더했다 |
| DEC | D-01~D-09 | **9행** — 본문 변경 0 |
| 계약 불변식 | I-1~I-8 | **8행 · 원문 불변** |
| 미수행 상태 | 선체크 금지 | `- [x]` **0건** · Phase Status **10개 전부 `TODO`** · `progress: 0` · `status: todo` |
| YAML | `yaml.safe_load` | **OK** — `WORK-006` / `todo` / `0` |
| 분량 | 불필요 증가 금지 | **1282 → 1303줄 (+21)** — 일곱 항목에만 들어갔다 |

### 변경 구간 자체 검증

- **「서버와 설치파일이 같은 아티팩트」 표현 잔존 0** — `grep '셋이 \*\*같은 아티팩트\*\*'` **0건**.
  남은 `같은 아티팩트` 는 Phase 9 의 **부정문 한 줄**뿐
- **해시 일치 대상이 한 쌍으로 통일** — Phase 8 검증 · Phase 9 검증 · Done Criteria **세 곳 모두
  「8 이 검증한 설치파일 ↔ 9 가 발행한 설치파일」**. 6b 가 해시 대상으로 등장하는 자리 **0건**
- **판정표 세 칸이 겹치지 않는다** — 진행에 「보완·중단 어느 조건도 해당하지 않는다」가 붙어,
  「회의 MIME 만 성공하면 무조건 진행」 경로가 닫혔다
- **AC-T27 의 주 배정은 Phase 5 그대로** — AC 추적표 `:1046` 행 **무수정**. 6b 는 검증 항목만 늘었다
- **SPEC 원문 대조** — 6b 에 새로 쓴 두 문장이 SPEC `:838-839`(AC-T27)와 글자 뜻이 같다:
  「앱을 다시 열거나 스트림에 새로 붙을 때」 · 「시각이 지났다는 것만으로 … 열린 스트림이 닫히지 않는다」.
  **없는 동작(자동 401 전환·WS 자동 종료)을 발명하지 않았고, 그것이 없다는 사실을 명시**했다
- **`log.md` 갱신안 외 Phase 수 표기 재확인** — Status Board `1–9`(최대 번호)와
  Done Criteria 「Phase 1 ~ 9 … (6a·6b 포함)」은 R2 가 **정확하다고 판정**해 **건드리지 않았다**

---

## 3. 계약 준수

- **범위**: `work-006-tauri-wrapper.md` **한 파일 수정** + 이 보고서 신규. 기존 리포트·SPEC·DEC·index·log·
  제품 코드·인프라 **diff 0줄**
- **금지 준수**: 커밋·push·PR·실측·빌드·테스트·서버 변경·추가 워커 **0회**. 비밀값 미열람
- **구조 보존**: Phase 수·번호·소유·의존 방향·AC 배정·실측 ID·I-1~I-8 **변경 0**.
  R2 가 「재개방하지 않는다」고 못박은 넷(F-3 번호 방식 · OQ-W03 · OQ-W04 · OQ-W05 분리)도 **그대로 두었다**
- **분량**: +21줄. 일곱 항목 밖의 문단을 늘리지 않았다

---

## 4. 미결·주의점

- **새로 여는 미결 없음.** 사용자 결정은 R2 판정 그대로 **다섯 + 조건부 둘**
  (`OQ-T01` · `OQ-T02` · `OQ-T03` · `OQ-W01` · `OQ-W06` / 조건부 `OQ-W02` · `OQ-W05`)
- **코디 몫도 그대로** — `P-4`(frontend 워커 `verify` 에 Rust 검증) · **`P-5`(인프라 워커 정의 — 6b 선행,
  이번에 6b 의존 줄에도 박았다)** · index·log 갱신 적용(갱신안은 「10 Phase」로 고쳐 두었다)
- **WARN-2 의 갱신 자리를 6b 「검증」에 둔 이유** — 6b 가 그 코드를 고치는 Phase 이기 때문이다.
  Phase 8 로 미루면 「변경한 Phase 가 자기 변경의 영향을 확인하지 않는」 구조가 된다.
  R2 가 제시한 두 선택지 중 **첫째(6b 검증에 추가)를 골랐고**, 둘째(Phase 5 단서)도 **함께** 넣었다 —
  인용하는 자리와 갱신하는 자리 양쪽에서 보이게 하려는 것이다
- **WARN-3 의 ③ 을 「중단」이 아니라 「보완」에 둔 것**은 R2 권고 그대로다 — 래퍼의 목적은 회의 녹음이고,
  브라우저 경로는 이미 후보 셋을 갖는다. 다만 **미통과를 연 채 끌고 가는 것**과
  **Release 완료로 넘기지 않는 것**을 같은 칸에 박아, 조용히 닫히지 않게 했다
- **기존 부채는 이번에도 그대로** — `work-005-projects.md:101` 의 낡은 「커밋은 아직 0건이다」,
  `k8s_infra_mac/README.md` 의 `k8s-test` 설명. **둘 다 WORK-006 밖이라 건드리지 않았다**(코디 몫)
