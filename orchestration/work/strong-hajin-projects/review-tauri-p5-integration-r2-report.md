# WORK-006 Phase 5 통합 검증 — 독립 검수

- **검수 대상**: `tauri-measurement-r2.md`
- **대조 기준**: `tauri-p5-integration-brief.md` · `work-006-tauri-wrapper.md` Phase 5 · `tauri-measurement-r1.md`
- **코드**: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects` · HEAD `a1f6791`(변동 없음)
- **제품 코드·SPEC·WORK 수정 없음. 이 보고서 한 파일만 작성.**

---

## 0. 종합 판정 — **PASS · FAIL 0 · WARN 2 · 이월 8**

**발주가 검수하라고 한 네 항목이 모두 통과다.**

1. **자동검증 명령·수치** — R-1 ~ R-9 를 **전부 재실행**했고 수치가 일치한다(R-3 은 §2-1).
2. **AC 21건** — WORK 의 목록과 **한 건도 빠지거나 겹치지 않고** 일치하며, 각 칸이 증거 상태를
   갖는다. 집계(✅2 · 🟡11 · ⬜8)도 **산술이 맞다.**
3. **M-1 · Windows · 운영 origin · 설치/절전/마이크 실기** — **어느 것도 통과로 쓰지 않았다.**
4. **M-2b/AC-T27 의 Phase 2 인용** — **인용할 기록이 실제로 없다**는 것을 보고서가 스스로
   찾아내 **미측정으로 두었다.** 발주가 지시한 수정 방향을 **이미 선취했다.**

FAIL 은 없다. WARN 2 건은 **이 기록의 갱신 사항**이고, 8 건은 보고서가 이미 **Phase 6a 이월로
분류해 둔 것**이다. 둘을 §4·§5 에서 갈라 적는다.

---

## 1. 판정표

| # | 발주 검수 항목 | 판정 |
|---|---|---|
| 1 | 자동검증 명령·수치가 실제 실행과 일치 | **PASS** (R-3 통계는 WARN N-1) |
| 2 | AC 21건 누락·중복 없이 각각 증거 상태 보유 | **PASS** |
| 3 | M-1 을 통과로 쓰지 않음 | **PASS** |
| 4 | Windows 를 통과로 쓰지 않음 | **PASS** |
| 5 | 운영 origin 을 통과로 쓰지 않음 | **PASS** |
| 6 | 설치·절전·마이크 실기를 통과로 쓰지 않음 | **PASS** |
| 7 | M-2b/AC-T27 인용이 실제 기록과 맞음 | **PASS** (기록 부재를 스스로 찾아 미측정 처리) |
| 8 | 제품 코드·SPEC·WORK 무수정 | **PASS** (이번 회차 코드 0줄) |

---

## 2. 항목 1 — 명령·수치 재현

**R-1 ~ R-9 를 직접 다시 돌렸다.**

| # | 명령 | 보고서 | **재현 결과** | 일치 |
|---|---|---|---|---|
| R-1 | `npx tsc --noEmit` | exit 0 · 0줄 | **exit 0 · 0줄** | ✅ |
| R-2 | `vitest run shell.test.ts browser/ meetings/` | 215 passed | **10 files · 215 passed** | ✅ |
| R-3 | `make frontend-test` ×N | 4회 중 3회 1053 · 1회 `1 failed/1052` | **3회 중 1회 1053 · 2회 `1 failed/1052`** | 성격 일치 · **빈도 다름**(N-1) |
| R-4 | `make frontend-assets` | exit 0 · assets 9 | **exit 0 · `"assets":9 … verified`** | ✅ |
| R-5 | `make frontend-build` | exit 0 · built | **exit 0 · `✓ built in 1.44s`** | ✅ |
| R-6 | `cargo check --all-targets` | exit 0 · warning 0 | **exit 0** | ✅ |
| R-7 | `cargo test` | 45 passed · 0 failed | **45 passed · 0 failed** | ✅ |
| R-8 | `cargo clippy --all-targets -- -D warnings` | 통과 | **exit 0** | ✅ |
| R-9 | `cargo check --locked --target x86_64-pc-windows-msvc` | `NotAttempted("llvm-rc")` | **exit 101 · `NotAttempted("llvm-rc")`** | ✅ |

### 2-1. R-3 — 「기준선 불안정」이라는 §1-1 의 **결론은 옳다**

- 내 3회의 실패 파일은 `ActionCenter.test.tsx` · `MeetingMaterials.test.tsx` — **매번 다르다.**
- 특히 `MeetingMaterials.test.tsx` 는 **R-2 의 격리 실행(215건)에서는 통과**했다.
  같은 시험이 전체 스위트에서만 깨지는 것은 **교차 간섭·시간 의존**의 모양이고,
  **Phase 4 배선의 회귀가 아니다.** §1-1 의 논거를 검수 쪽에서도 확인한다.
- 다만 **빈도는 보고서 스냅샷보다 나쁘다** → N-1.

---

## 3. 항목 2~7 — 확인 내역

### AC 21건 (PASS)
- WORK 의 목록과 **문자 그대로 일치**: `T01~T07`(7) `T19`(1) `T26~T30`(5) `T32`(1) `T36~T41`(6) `T43`(1) = **21**.
  **누락 0 · 중복 0**, 보고서 표의 행 수도 21.
- **각 행이 증거 상태를 갖는다**: ✅(자동으로 닫힘) / 🟡(코드 증거만) / ⬜(미측정) / 🚫(환경 차단)
  범례가 앞에 서 있고, 미측정 칸에는 **왜 못 쟀는지**가 M 번호와 함께 붙어 있다.
- **집계 검산**: ✅ = `T30`·`T40` **2** · ⬜(순수 미측정) = `T01 T05 T06 T07 T26 T27 T28 T32` **8** ·
  🟡 = 나머지 **11**. **보고서 수치와 정확히 같다.**

### M-1 · Windows · 운영 origin · 실기 (PASS — 전부 통과로 쓰지 않음)
| 축 | 보고서 표기 | 판정 |
|---|---|---|
| **M-1** | §6-3 「**미해소.** 19:54 재측정도 동일 · 이번 회차에서 재시도하지 않았다」 | ✅ 통과 아님 |
| **Windows** | §5 「**실행 미검증이자 컴파일 미검증** · macOS 결과로 대신 쓰지 않는다(OQ-W01)」 · 표의 Windows 열 **전부 🚫** | ✅ |
| **운영 origin** | §7-6 「미정 · 자리표시」 — AC 표의 어느 행도 이를 근거로 닫지 않았다 | ✅ |
| **설치·절전·마이크 실기** | §3 「**전부 실기 필요 · 오늘 자동으로 딸 수 있는 칸이 없다**」 · §4 말미 「실기 전에는 **어떤 AC 도 실기 기반으로 닫히지 않았다**」 | ✅ |
- §3-1 이 「인접 증거」를 **M 칸을 채우지 않는 것**으로 못 박고 한계까지 적은 것은 특히 정확하다
  (재무장 기구는 검증됐으나 「OS 가 실제로 절전을 막았는지는 안 봤다」).

### M-2b / AC-T27 의 Phase 2 인용 (PASS)
- **WORK 의 계획**: AC-T27 은 「**Phase 2 기록을 인용하고 인용임을 표시한다**」.
- **실제 기록 확인**(검수가 직접 열어 봄) — `tauri-measurement-r1.md`:
  - 요약표 `| **M-2b** | 만료 넘겨 열린 스트림 유지 | **미측정** | 검증 불가(장비 없음) |`
  - 본문 `## M-2b … — **미측정**`, 그 절의 **막는 인수조건은 `AC-T28` 하나**뿐 — **AC-T27 없음**
- → **인용할 관측이 존재하지 않는다.** 보고서 §2-1 의 판단과 처리(**미측정으로 둔다 · 계획의
  전제가 어긋난 자리로 코디에 올린다**)가 **정확하다.** 발주의 「기록이 없으면 미측정으로
  고친다」는 **이미 충족돼 있어 고칠 것이 없다.**

### 무수정 (PASS)
- 보고서 §8 「코드 수정 0줄 · 문서 1개만 썼다」. 검수 시점에도 제품 코드 변경 흔적 없음.

---

## 4. WARN — 이 기록에서 갱신할 것 (2건)

| # | 내용 | 수정 방향 |
|---|---|---|
| **N-1** | **§1-1 의 「3/4 성공」이 안정성 지표로 읽힐 수 있다.** 같은 명령을 검수에서 3회 돌리니 **2회 실패**했다(보고서 1/4 → 검수 2/3). 결론(「기준선 불안정 · 새 실패 0」)은 옳지만, **회차 표본이 작아 비율을 성적처럼 쓰면 안 된다** | §1-1 에 「빈도는 회차마다 크게 흔들린다(검수 재현 2/3 실패)」 한 줄 추가. **판정은 그대로 둔다** |
| **N-2** | **§1-1 의 실패 파일 목록이 낡았다.** 「`CreateWork`·`Checklist`·`App`·`CalendarPage`·`ProjectPage`·`ActionCenter`」에 **`MeetingMaterials.test.tsx`(= `src/features/meetings/` 안)** 가 빠져 있다. 「배선과 접점이 없다」는 논거는 **여전히 유효**하나(SCR-106 자료 공유이고 wake guard 와 무관), **배선 디렉터리 안에서도 불안정이 난다**는 사실은 적어 두는 편이 정직하다. 같은 파일이 **R-2 격리 실행에서는 통과**한 것도 함께 적으면 논거가 더 강해진다 | 목록에 파일 하나 추가 + 「격리 실행에서는 통과 = 교차 간섭」 한 줄 |

> 둘 다 **판정을 바꾸지 않는 기록 정확도 문제**다. FAIL 이 아니다.

---

## 5. 이월 — 다음 Phase 로 넘어가는 것 (8건, 보고서 §7 그대로 유효)

**WARN 과 구분한다**: 아래는 이 기록의 흠이 아니라 **이 Phase 가 닫을 수 없어 넘기는 것**이고,
보고서가 이미 성격(코디 판단 / 사용자 / 범위)과 **막는 대상**까지 붙여 분류해 두었다.

| # | 항목 | 성격 | 검수 의견 |
|---|---|---|---|
| 1 | 화면 배선 회귀 시험 파일 허용 | 코디 | **가장 시급하다.** 🟡 11건의 코드 증거가 시험으로 고정되지 않아, R-1(StrictMode) 같은 회귀가 재유입될 수 있다 |
| 2 | **AC-T27 의 인용 전제 정정** | **계획 정정** | WORK §Phase 5 표의 「Phase 2 기록을 인용」 문장 자체를 고치거나, 실기로 재야 한다. **WORK 수정은 이번 검수 범위 밖이라 손대지 않았다** |
| 3 | 실기 환경 준비(마이크 승인 · 제3자 절전 도구 해제 + `T` 설정 · 앱 재시작 왕복) | 사용자 | **⬜ 8칸 전부**를 막는다 |
| 4 | CA 신뢰 설치(M-1) | 사용자 | M-1 및 그에 매인 AC 최종 통과 |
| 5 | Windows 실기 + `llvm-rc` | 코디·장비 | Windows 축 **전부**(컴파일 포함) |
| 6 | 운영 origin 확정(OQ-T02) | 사용자·코디 | 셸 설정 두 자리 |
| 7 | `open_external` 실패의 사용자 표시 | SPEC 판단 | Phase 4 이월 R-3 |
| 8 | **백엔드 축 미실행**(`make verify` 전체) | 범위 | WORK Phase 5 검증 체크박스 「`make verify` 새 실패 0」이 **미충족으로 남는다** — §1-2 의 범위 해석은 타당하나, **Phase 5 를 닫을 때 이 칸이 비어 있음을 명시해야 한다** |

- **추가 이월 1건**: 보고서 §6-5(AC-T41 의 네이티브 보상 수단 없음, Phase 3 한계)는 §7 표에
  올라 있지 않다. M-14 실측 항목으로만 남아 있으니 **6a 목록에도 한 줄 얹는 편이 안전하다.**

---

## 6. 보고서 자체의 정확성

- **과장 0.** 「실기에 들어갈 수 있는 상태다. **단 실기 전에는 어떤 AC 도 실기 기반으로 닫히지
  않았다**」처럼, 녹색 수치 뒤에 **한계를 같은 문단에서** 붙였다.
- **자기 계획의 오류를 스스로 올렸다**(§2-1, AC-T27). 이번 발주가 검수에 맡긴 항목을
  **구현 쪽이 먼저 찾아낸 것**이라 별도 시정 지시가 필요 없다.
- **1차 기록과 섞지 않았다** — 머리말에서 회차 분리를 명시하고, §3-1 에서 「인접 증거」가
  M 칸을 채우지 않음을 못 박았다.
- **정정할 것**: N-1 · N-2 두 줄뿐이다.

---

## 7. 코디에게 올리는 결정거리

1. **N-1 · N-2 를 `tauri-measurement-r2.md` §1-1 에 반영**(두 줄). 판정은 그대로.
2. **이월 2(AC-T27)** — WORK 의 「Phase 2 인용」 문장을 **누가 언제 고칠지** 정해야 한다.
   그 전까지 AC-T27 은 **미측정**이 맞다.
3. **이월 1(배선 시험 파일)** — Phase 6a 에서 가장 먼저 여는 것을 권고한다.
4. **이월 8(백엔드 `make verify`)** — Phase 5 완료 판정 문서에 「미실행」으로 남길지 확인.
5. **이월 3~6(실기·장비·origin)** — 사용자·장비가 준비돼야 진행된다. **Phase 5 는 이 상태로
   「자동 검증 완료 · 실기 미착수」로 닫는 것이 정확하다.**
