# WORK-006 Phase 8 preflight 최종 — 독립 재검수

- **코드 워크트리**: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects` · HEAD `a1f6791`(변동 없음)
- **검수 대상**: `frontend/scripts/verify-final-build.mjs` ↔ `tauri-p8-preflight-fix-report.md` (**W-1 · W-2**)
- **코드·설정·문서 수정 0줄. 이 검수 보고서 한 파일만 작성.**
- **운영 배포·설치·Release·태그·push 없음.** 돌린 것은 **쓰기 API 가 없는 관문**과 **스크래치패드 사본**뿐이다.

---

## 0. 종합 판정 — **FAIL 0 · WARN 2(둘 다 낮음) · 나머지 PASS**

**W-1 의 핵심이 정확히 닫혔다.** 연습 실행은 이제 **exit 3** 이고, `exit 0` 은 **기본 `src-tauri`
에서만** 나온다. 「경고 문구는 기계가 읽지 않는다」는 진단이 옳았고, 고친 방향(문구가 아니라
**종료 코드**를 가르기)도 옳다. **연습을 못 하게 막은 것이 아니라 «통과로 집계되지 못하게» 막았다** —
연습 경로는 그대로 쓸 수 있다.

**없는 경로도 스택이 아니라 판정으로 답한다**(exit 2), 그리고 **「관문을 돌리지 못했다」(2)를
「관문이 막았다」(1)와 섞지 않았다.** 이 구분은 자동화가 원인을 읽는 데 실제로 쓰인다.

**`exit 0` 분기가 죽지 않았음을 검수가 독립 재현했다** — 스크립트 사본에 자기 `src-tauri` 를
붙여 돌리니 **exit 0 · `rehearsal: false` · `originsAgree: true` · 「관문 통과」**.

WARN 2 건은 **`make` 가 종료 코드를 2 로 뭉갠다는 것**과 **보고서가 스스로 밝힌 잔여 우회**다.
둘 다 **연습이 0 을 낼 수 없다는 안전 성질은 깨지 않는다.**

---

## 1. 판정표

| # | 검수 항목 | 판정 |
|---|---|---|
| 1 | 연습(+evidence)이 0 이 아닌 별도 코드 · manifest/banner 에 rehearsal | **PASS** (exit **3**) |
| 2 | `SHELL_ROOT` 부재 시 친절한 차단 메시지 + nonzero | **PASS** (exit **2**, 스택 없음) |
| 3 | 실제 root 에서 origin/D-4/evidence 없으면 기존처럼 차단 | **PASS** (exit **1** · 7건) |
| 4 | M-5 가 「앱 재시작 후 쿠키/로그인 유지」로 설명 | **PASS** |
| 5 | 설정·Makefile·제품·인프라·Release 무변경 · Phase 7 회귀 · 미측정 표기 유지 | **PASS** |

---

## 2. FAIL

**없다.**

---

## 3. 항목별 확인 — 전부 직접 실행

| # | 실행 | 종료 코드 | 확인한 것 |
|---|---|---|---|
| **V-1** | 진짜 root · 입력 없음 | **1** | `관문이 막혔다 — 7건` — **직전 회차와 동일**하게 막는다 |
| **V-2** | **연습 + 증거 충족**(이전에 **0** 이던 자리) | **3** | `"rehearsal": true` · `연습 통과(관문 통과가 아니다)` · `⚠⚠ **exit 3 (연습)** … **exit 0 은 진짜 설정에서만 나온다** — 이 실행을 관문 통과로 집계하지 말 것` |
| **V-3** | 연습 + 증거 없음 | **1** | `관문이 막혔다 — 4건` — G5 가 여전히 앞선다 |
| **V-4** | `SHELL_ROOT=/nonexistent-root` | **2** | `셸 설정 트리가 없다: …` + `**이 스크립트는 경로를 만들지 않는다.**` — **ENOENT 스택 없음** |
| **V-5** | `SHELL_ROOT=/tmp`(있으나 셸 트리 아님) | **2** | 없는 다섯을 **이름으로 나열**(`shell.config.json`·`tauri.conf.json`·`Cargo.toml`·`src/lib.rs`·`capabilities`) + `**관문을 돌릴 수 없다** — 읽을 것이 없는 것은 «통과»도 «차단»도 아니다` |
| **V-6** | **`exit 0` 분기 생존**(검수 독립 재현) | **0** | 스크립트 사본 + 자기 `src-tauri` → `"rehearsal": false` · `"originsAgree": true` · `관문 통과 — … 미결 4건이 기록되었다` |
| **V-7** | M-5 라벨 | — | `✗ G5 **M-5 — 앱 재시작 후 로그인(쿠키) 유지** — **미결**. … — **유지되지 않으면 사용자가 열 때마다 다시 로그인한다**` |

### 3-1. 종료 코드 네 갈래가 코드에 있다 (PASS)

```js
const EXIT_PASS = 0;        // 진짜 관문 통과 — 오직 기본 src-tauri 에서만
const EXIT_BLOCKED = 1;     // 관문이 막았다
const EXIT_USAGE = 2;       // 돌릴 수 없다(입력·경로 문제)
const EXIT_REHEARSAL = 3;   // 연습 — 모든 관문을 지나도 이 코드다
```

통과 분기 **맨 끝**에서 한 번 더 가른다(`if (rehearsal) … exit(3)` → `exit(0)`).
**0 으로 가는 길에 연습이 섞일 수 없다.**

### 3-2. 읽기 전 사전 검사 (PASS)

`REQUIRED` 다섯을 `existsSync`/`statSync` 로 **읽기 전에** 확인하고 exit 2 로 끊는다.
→ 「도구의 사고」가 「관문의 판정」으로 바뀌었다. **2 를 1 과 섞지 않은 판단이 옳다.**

### 3-3. 연습 문구까지 손봤다 (PASS — 요구 밖의 정직함)

연습일 때 「다음 단계 1)」이 `(이미 일치함을 확인했다)` 라고 말하던 줄을
「**진짜** shell.config.json · capability 를 … 확정 — **이번 실행은 사본만 보았다**」로 고쳤다.
**사본만 본 실행이 진짜 설정을 확인했다고 말하지 않는다.** 지적하지 않은 자리를 스스로 찾았다.

### 3-4. M-5 라벨 · 「미결 넷」 (PASS)

- `what: "M-5 — 앱 재시작 후 로그인(쿠키) 유지"` · `why` 에 **결과**까지 붙었다.
- 머리말 표의 `**미결 셋**` → **`미결 넷`** 으로 바로잡았다(항목은 처음부터 넷이었다).
  검수 확인: `verify-final-build.mjs:23` 이 `**미결 넷**(D-4 · 운영 서버 실재 · M-1 · M-5)`.

### 3-5. 불변·회귀 (PASS)

| 대상 | 확인 |
|---|---|
| **이번 회차 변경** | `find -newermt "2026-09-22 23:59"` → **`verify-final-build.mjs` 하나**. **Makefile 은 이번에 안 건드렸다** — 보이는 48줄은 **직전 dispatch** 의 `shell-final-preflight` 타깃이고, 보고서 §5 의 설명이 **사실과 맞다** |
| `frontend/src-tauri/**` | `git diff --stat` **0줄** · 실험 전후 `shell.config.json`·`product-shell.json` **shasum 동일** · `operationalOrigin` 여전히 `null`, capability 여전히 `.invalid` |
| 제품 프론트 · backend · 인프라 | 무변경 |
| **Phase 7 회귀** | `make shell-verify` **exit 0** · `문제 0건 · 구성 미비 1건 · 호스트 한계 1건` / `shell-verify-strict` **exit 2** · `실패 1건` — **그대로** |
| `make shell-final-preflight` | **여전히 막는다**(7건) — 의도한 동작 |
| 태그 · CI · Release · push · 설치 | `git tag --points-at HEAD` **없음** · `.github/workflows` **없음** · HEAD `a1f6791` |
| 미측정 표기 | §6 이 운영 origin · D-4 · 서버 실재 · M-1 · **M-5(재시작 후 쿠키 유지)** · M-10 · Windows · `.icns` · N-2 를 **그대로** 유지 |

---

## 4. WARN (2건 · 둘 다 낮음)

| # | 내용 | 근거 | 수정 방향 |
|---|---|---|---|
| **W-1** | **`make` 를 거치면 네 갈래가 다시 뭉개진다.** GNU make 는 레시피 실패를 자기 코드 **2** 로 되돌리므로, `make shell-final-preflight` 로는 **차단(1)·사용법(2)·연습(3)이 전부 `2`** 로 보인다. 검수 확인: 같은 연습 실행이 **node 직접 → 3**, **make 경유 → 2**. **안전 성질은 깨지지 않는다**(연습이 0 을 내는 일은 어느 경로에서도 없다) — 잃는 것은 **원인 구분**뿐이다. 다만 문서화된 진입점이 `make` 하나라, 자동화가 그대로 `make` 를 부르면 W-1 이 만든 정보가 보이지 않는다 | `make` 경유 3→2 · 2→2 재현 | 보고서/Makefile 주석에 「**자동화는 `node scripts/verify-final-build.mjs` 를 직접 불러 종료 코드를 읽는다**」 한 줄. 또는 코드를 그대로 넘기는 별도 타깃 |
| **W-2** | **잔여 우회 — 보고서가 스스로 밝힌 것**(§4-1). `rehearsal` 이 「기본이 아닌 root 를 지정했는가」로 판정되므로, **스크립트를 옮기면** 사본 옆의 트리가 «기본»이 되어 **exit 0** 이 나온다. 검수도 V-6 에서 **같은 방법으로 재현**했다 — 즉 통과 분기를 증명한 기법이 곧 우회 기법이다. 보고서가 「관문은 **실수와 자동화의 오집계**를 막고 **의도적 우회는 막지 못한다** — 그것은 리뷰와 서명의 몫」이라고 **한계를 정확히 적었다** | `verify-final-build.mjs:81` `rehearsal = shell !== defaultShell` · V-6 | 이번 범위의 결함이 아니다. **CI 를 걸 때** ① 스크립트 경로를 고정하고 ② 실행 전 `git status --porcelain frontend/src-tauri` 가 비었는지 함께 본다 — 그 두 줄이 이 잔여를 덮는다 |

> 둘 다 **「연습이 통과로 집계되지 않는다」는 W-1 의 목적을 깨지 않는다.** 그래서 FAIL 이 아니다.

---

## 5. 차단·미결 (이번 회차가 만든 것 아님 — 유지)

| # | 항목 | 주인 |
|---|---|---|
| 1 | **운영 origin**(OQ-T02) — 미정. G1·G2·G3 가 여기서 멈춘다 | 사용자·코디 |
| 2 | **D-4** — PRODUCTION 세션 발급 수단 부재 | 코디·사용자(OQ-W05) |
| 3 | **운영 서버 실재** · **M-1** · **M-5**(재시작 후 쿠키 유지) · **M-10** | 사용자(실기) |
| 4 | **Windows 축 전부** — 검증 불가(이 기기) | OQ-W01 |
| 5 | macOS **`.icns`** — `shell-verify --strict` 가 잡는다 | Phase 7 후속/8 |
| 6 | **N-2 프론트 스위트 불안정** — 이번 회차 `frontend/src` 무접촉, 재실행하지 않음 | 코디 |

---

## 6. 한 줄 정리

**「경고 문구는 기계가 읽지 않는다」를 종료 코드로 고친 것이 옳고, 일곱 갈래를 직접 돌려 전부
재현했다** — 연습 **3** · 차단 **1** · 사용법 **2** · 진짜 통과 **0**(검수가 독립 재현). 저장소는
해시까지 동일하고 Phase 7 도 그대로다. 남은 것은 **`make` 가 코드를 2 로 뭉갠다는 것(W-1)** 과
**보고서가 이미 밝힌 잔여 우회(W-2)** 뿐이며, **Phase 8 은 여전히 exit 1 로 «굽지 말라»고 말한다.**
