# 문서 정정 — D-31 뒤집힘 (「프로젝트 추가」는 모든 사람에게 뜬다)

**고칠 파일 둘**: `decision-004-projects.md` · `spec-005-projects.md`. 그 밖은 건드리지 마라.
커밋·push 금지. **코드는 read-only** (FE 가 이미 고쳤다 — 문서만 맞추는 일이다).

## 무엇이 뒤집혔나

**D-31**(생성 버튼의 게이트는 `PROJECT_MANAGE` capability 보유 — **코디 기본값**)을
**사용자가 뒤집었다.**

> **사용자 원문 (2026-09-22)**: 「**추가는 모든 사람이 할 수 있다고**」
> (앞선 맥락: 「**프로젝트 추가는 모든 사람이 다 뜨는건데**」)

**새 결정**: **「프로젝트 추가」 버튼은 게이트 없이 누구에게나 선다.**

## 고칠 것

1. **`D-31` 을 지우지 말고 뒤집힌 기록으로** — 1루프의 ~~D-12~~ 와 **같은 서식**:
   제목에 취소선 · 「**2026-09-22 뒤집음 — 사유**」 블록 · **대체 결정 번호**를 새로 달아라(D-39 등)
   · 본문은 보존
2. **그 번호를 가리키는 자리 전부** 갱신 — 분류표 · 윤곽 · In Scope · 기각 · Resulting Spec ·
   추적표. **grep 으로 세어서 한 곳도 남기지 마라** (1루프 때 일곱 자리가 남아 FAIL 이 났다)
3. **SPEC-005**:
   - `:259`(§2.10 게이트 행) · `:679`(§4) · `:1257`(§5 「이 화면이 세우는 게이트 둘」) ·
     `:1262`(근거 인용) — **생성 버튼 쪽을 「게이트 없음」으로.**
     ⚠ **상태 드롭다운의 `task.self_manage` 게이트는 그대로다** — 둘을 섞지 마라
   - `:1506` **L-17** — 지금 「만들 자격이 없는 세션으로 열면 버튼이 없다」를 잰다.
     **그 인수조건은 이제 거짓이다.** **삭제하지 말고 «뒤집어» 다시 써라** —
     「역량이 없는 세션으로 열어도 **버튼이 선다**」
   - **「빈 상태에서도 선다」는 그대로 참이다** — 이유만 바뀐다(게이트가 없으니 당연히 선다)
4. **서버가 거절하는 경로는 남는다** — 역량 없는 사람이 실제로 만들려 하면 **API 가 거절**한다.
   **「버튼은 뜨고, 거절은 모달이 말한다」**를 계약에 명시하라.
   생성 거절 갈래(자격·이름·기간 역전·`external_key` 중복)에 **그 사실을 잇는다**

## 하지 말 것

- **새 결정을 만들지 마라** — 위 하나뿐이다
- **코드를 고치지 마라** · 색인·`log.md` 금지(코디 몫) · 커밋 금지

## 검증

- `D-31` 잔재를 **grep 으로 세어 보고에 수치로** 적어라 — 옛 뜻으로 남은 곳 **0건**
- **L-17 이 뒤집혀** 있고 「삭제」가 아니다
- 상태 드롭다운 게이트(`task.self_manage`)가 **안 흔들렸다**
- `git status --porcelain` 이 **두 파일만** 바뀐 것을 보인다

## 역할·맥락

너는 **strong-hajin `architect` 워커**다. 먼저 읽어라:
- `.../roles/strong-hajin/planner/role.md` (+ 같은 폴더 `.md`. ⚠ `scripts/lint-pipeline.py` 없음 — 린트 건너뛰어라)
- `decision-004-projects.md` 의 **~~D-12~~ 뒤집힘 기록**(같은 서식을 따른다) ·
  `loop2-final-fix-report.md`(FE 가 코드에서 무엇을 뺐는지)

워크트리 `/Users/kknaks/orca/workspaces/kknaks_profile/strong_hajin` (코디가 함께 있다).

## 완료 보고 — **문구 변경 금지**

```bash
orca orchestration send \
  --to term_bdba055b-758b-4dc9-85c3-7836d56b615a --from term_cf5765cd-30c3-4936-98f5-6fe48ea6fb8d \
  --type worker_done --task-id <taskId> --dispatch-id <dispatchId> \
  --subject "architect 완료: D-31 뒤집힘 반영" \
  --body "새 결정 번호 / D-31 잔재 grep 수치 / L-17 뒤집힌 문장 / 상태 드롭다운 게이트 무변경 확인 / git status"

orca terminal send --terminal term_bdba055b-758b-4dc9-85c3-7836d56b615a \
  --text "[worker_done] architect 완료 — D-31 뒤집힘 반영. 상세는 인박스." --enter
```
