# v2 스펙 최소 정정 3차 (fix3) — RW2-W1~W8 (2026-09-17)

재검수 2차 WARN 8건만 닫았다. **재조사 0건 · 신규 정책 0건 · 새 사용자 질문 0건 · 실행 0건.**
OQ-203 · OQ-206 은 **답 대기 그대로**다. WORK-002 는 쓰지 않았다.

## 변경 파일 (4건)

| 파일 | 닫은 항목 |
|---|---|
| `10-decision/decision-002-task-lifecycle-v2.md` | RW2-W1 · W3 · W6 |
| `20-spec/spec-003-task-lifecycle-v2.md` | RW2-W2 · W4 · W5 · W8(version) |
| `20-spec/spec-001-work-management.md` | RW2-W8(`updated_at`) |
| `20-spec/spec-002-action-item-review.md` | RW2-W7 · W8(`updated_at`) |

---

## 1. 필수 정정 셋

### RW2-W1 — DEC OQ-203 행의 `complete` 문구 (`decision-002` OQ-203 행 마지막 문장)

- **전**: 「**두 안 모두 새 endpoint 를 만들지 않는다** — 같은 `complete` 에 선행 검사를 어디까지 거는가의 차이다.」
- **후**: 「**두 안 모두 새 명령·새 endpoint 를 만들지 않는다** — **제출 명령(`completion-report`)에
  하위 검사를 더할 것인가**의 차이다.」
- **근거**: SPEC-003 §7 OQ-203 이 이미 같은 말로 적혀 있다(「**제출 명령에 하위 검사를 더할 것인가**
  하나다 … **어느 안도 새 명령·새 endpoint 를 만들지 않고**」). 실물 제출 입구는 `completion-report`
  이고 `complete` 가 아니다(BASE-002 O-34 · SPEC-003 §4 API 표). **DEC 만 SPEC 에 맞췄다.**
- **질문의 내용(안 A/안 B·기본값)은 건드리지 않았다.** 뒷문장 「본인·배정 업무는 `complete` 가 곧
  최종 완료라 어느 안이든 막힌다」도 그대로 두었다.

### RW2-W2 — SPEC-003 줄번호 인용 잔존 3건 (`spec-003` §4 완료 승인 1항)

- **전**: 「SPEC-001 의 승인자 0..1 지정\n   (`spec-001:443`·`:460`·`:498`)은 **요청 Task 에서만** 좁혀지고 …」
- **후**: 「SPEC-001 **§4 Validation·Data 의\n   승인자 0..1 지정**은 **요청 Task 에서만** 좁혀지고 …」
- **확인**: SPEC-001 §4 Validation 의 `approver_id` 행(「0 또는 1명. 재직 중. 담당자 본인은 승인자가
  될 수 없다」)과 §4 Data Contract 의 「사람 | 담당자 1 · 요청자 · 승인자 0..1」 두 자리가 실제 근거다.
  §1 이 세운 **「줄 번호로 지목하지 않는다」** 원칙과 같은 처리다.
- **잔존 확인**: `spec-00N:숫자` 형식 인용을 `20-spec/` · `10-decision/` 전체에서 grep — **0건**.

### RW2-W5 — 담당 변경 명령을 부를 자리 (`spec-003` §2.4 · U-9)

- **§2.4 에 bullet 하나 추가**(기존 「담당 변경 대기는 … 함께 보이는 자리다」 바로 뒤):

  > - **담당 변경 제안(`reassign`) 과 그 응답(제안 수락 · 제안 거절)을 부를 자리가 있어야 한다
  >   (정책 V-18).** 제안은 담당자 또는 배정 권한이, 수락·거절은 **그 제안의 수신자만** 부른다 —
  >   **권한은 §4·§5 계약 그대로이고 이 절은 자리만 요구한다.**

  권한 문구는 **§4 API 표(`POST /api/tasks/{id}/reassign` = 담당자 또는 배정 권한 /
  `POST /api/task-assignments/{id}/accept`·`decline` = 그 제안의 수신자만)와 §5 권한 표를 그대로
  옮긴 것**이고 새로 만든 권한이 없다.
- **따라 고친 것**: 그 아래 인용문 「위 **다섯**은 시안에 자리가 없다」 → 「위 **여섯**은」
  (bullet 이 5→6 이 되었다).
- **U-9 (§6 UX 인수조건)**
  - **전**: 「**철회 · 취소 제안 · 동의 · 재개**를 부를 자리가 있다」
  - **후**: 「**철회 · 취소 제안 · 동의 · 재개 · 담당 변경 제안과 그 응답(수락 · 거절)**을 부를 자리가 있다」
  - 판정 주체 칸(`명령 존재: 에이전트 / 자리: 사용자`)은 그대로다. U-8(표시)은 건드리지 않았다.

---

## 2. 동시 문구 정정 다섯

### RW2-W3 — `01` OQ-06 을 갈래 ② 에 올렸다 (`decision-002` § 시안 반영 세 갈래)

갈래 ② 표 마지막 행(`01` OQ-08) 뒤에 한 행 추가:

> | `01` OQ-06 모달에서 시안에 없는 현재 필드(담당자 모드 · 시작일 · 참조자 · 참고 업무)를 어디로 |
> **계약은 그대로 두고 화면 자리만 정한다**(SPEC-003 §2.7 — 「시안에 없다는 이유로 계약에서 지우지
> 않는다」). 다만 **담당자를 지정하는 자리는 계약이 요구한다**(정책 V-2) — 그 자리가 없으면 요청을
> 보낼 길이 화면에서 사라진다 |

SPEC-003 §2.7 의 두 bullet(담당자 지정 자리 V-2 · 「시작일·참조자·참고 업무는 기존 계약에 있다」)을
그대로 원장에 연결한 것이다. **새 미정을 만들지 않았다** — 갈래 ③(M-20~M-24)은 그대로 5건이다.
이로써 `01` 8건 전수가 원장에 선다.

### RW2-W6 — OQ-202 의 「프론트 개편 때」 (`decision-002` 기본값 통보 표)

- **전**: 「… **보이는 동작이므로 프론트 개편 때 묻는다.** 저장 방식은 구현자 몫(M-10)」
- **후**: 「… **보이는 동작이므로 WORK-002 의 화면 범위에서 고른다**(M-20 과 같은 성격 — 화면 결정).
  저장 방식은 구현자 몫(M-10)」
- **정책(백엔드 계약 = 요청자 목록에서 빠지고 로그 보존)은 한 글자도 바꾸지 않았다.**

### RW2-W7 — SPEC-002 상단 안내 중복 한 줄 삭제

- **삭제**: 「> **판단 계약 자체(회차·불변 판정·마지막 판정자)는 그대로다.**」
- **남김**: 「> **판단 계약 자체는 그대로 정본이다** — 항목 하나에 회차가 쌓이는 모양 · 판정의 불변성 ·
  마지막 판정자 · 종류 칩 둘 · 봉투가 명령을 내려 준다는 원칙.」 (뒤 줄이 상위 집합)

### RW2-W4 — 수신함 분류 축 · 카드 단추 두 벌 (`spec-003` §2.8, 두 줄 추가)

**전수 조사를 하지 않았다.** SPEC-002 §2.2·§2.3 종류 칩 실물과만 대조했다.

> - **분류 축은 기존 계약에 없다 — 좁은 미설계다.** 시안의 「전체 / 업무 / 참고」는 **종류 축**이고,
>   계약이 정한 좌 레일 필터 축은 **SPEC-002 §2.3 의 종류 칩 둘**(`문의 요청` · `승인 요청` —
>   봉투 `chip` 은 `inquiry`·`approval`·`none`)이며 현재 코드의 축은 **상태 축**(판단 대기·조정 필요)이다.
>   **대조 결과 「업무/참고」는 그 둘 중 어느 축과도 대응하지 않는다** — 칩 둘로 닫히지 않는다.
>   그 축을 세우는 것은 **새 계약**이므로 이 SPEC 이 만들지 않고 **WORK-002 의 화면 범위에서 먼저
>   확인한다.** 사용자 질문으로 올리지 않는다.
> - **카드 단추 두 벌 중 「내 업무로」만 계약에 닿아 있다 (미정 M-7).** 나머지 둘은 **기존 계약이 없다** —
>   **「답장」**은 SPEC-002 가 사내 문의 응답과 **「밖으로 나간 답장」을 다른 자리로 갈라 두었을 뿐**
>   수신함 카드의 그것이 어느 쪽인지 정한 계약이 없고, **「원문 확인하기」**는 **유입 원문을 여는 계약이
>   없다** — 출처를 봉투에 실어 내는 계약 자체가 없고(M-22) 메일·메신저 통합은 **승인 밖**(§2.10).
>   **이 SPEC 이 없는 명령을 새로 만들지 않는다.** 둘 다 **좁은 미설계**로 두고 **WORK-002 선행 확인**
>   항목이다.

- **대조 근거(읽은 자리만)**: SPEC-002 §2.2 종류 표 6행 · §2.3 종류 칩(「문구: `문의 요청` · `승인 요청`
  **둘**」 / 「좌 레일에서는 같은 두 칩이 필터로 선다」) · §4 봉투의 `chip` = `inquiry`·`approval`·`none` ·
  SPEC-002 §3 「밖으로 나간 답장과는 자리가 다르다」.
- **존재하지 않는 명령을 만들지 않았고, 새 미정 ID 도 만들지 않았다** — 기존 M-7·M-22 에 붙였다.

### RW2-W8 — frontmatter (코디 판단 지시대로)

| 파일 | `version` | `updated_at` |
|---|---|---|
| `spec-001` | `0.2.4` **유지**(W1 계약 본문은 안 바뀌었다 — 안내만 갱신) | `2026-09-15` → **`2026-09-17`** |
| `spec-002` | `0.2.2` **유지**(같은 이유) | `2026-09-15` → **`2026-09-17`** |
| `spec-003` | `0.3.0` → **`0.3.1`**(patch) | `2026-09-17` (이미 맞음) |

`decision-002` 는 이미 `updated_at: 2026-09-17` 이라 손대지 않았다. 세 SPEC 모두 **버전 이력 표
섹션이 없어** 추가한 행이 없다. 파일 안 안내문의 「2026-09-17」과 frontmatter 불일치가 사라졌다.

---

## 3. 범위 준수

- **손댄 파일 4건뿐.** BASE-002 · WORK-002 · 원문(SSOT) · index · log 는 건드리지 않았다.
- **코드·DB·테스트·빌드 0건. 커밋·push·PR·stash·reset·checkout 0건.**
- **SPEC-001 · SPEC-002 는 frontmatter `updated_at` 한 줄(+SPEC-002 상단 안내 중복 한 줄 삭제)만.**
  본문 계약은 한 글자도 안 바꿨다.
- **검증은 문구 정합만.** grep 으로 ① `spec-00N:숫자` 인용 0건 ② `프론트 개편 때` 0건
  ③ 중복 안내문 0건 을 확인했다. **실행 검증 0건**(브리프 §8).

## 4. 미결 · 주의점

- **OQ-203 · OQ-206 은 사용자 답 대기 그대로다.** 이번 정정은 OQ-203 **설명 문구**만 실물에 맞췄고
  선택지·기본값은 그대로다.
- **RW2-W4 가 남긴 것은 WORK-002 가 받는다** — 수신함 분류 축(업무/참고) · 「답장」 · 「원문 확인하기」
  셋 다 **새 계약 여부를 화면 범위에서 먼저 확인**해야 한다. 새 계약이라고 판명되면 그때 사용자 결정이다.
- **§2.11 「신규로 필요한 부품」 행**은 이미 「담당 변경 대기 · 취소 제안/재개를 여는 자리」를 세고 있어
  W5 로 늘어난 자리와 모순이 없다 — **최소 정정 범위라 손대지 않았다.**
