# 리뷰 리포트 — strong-hajin-work / planner (최종 업무·요청 프레임 + 읽음·선행 SPEC/WP, 2026-09-19)

## 판정: FAIL

**계약의 알맹이는 거의 다 왔다.** 브리프 검수 중점 1~8 의 실질 내용 — 최종 모달 프레임 ·
업무/요청 갈래 분기 · CC 읽음 영수증 · 선행업무와 시작 게이트 · payload 분리 · 표면 일치 ·
미결 보존 · 기존 계약 보존 — 은 **전부 계약으로 내려와 있고, 확인한 사실 주장도 코드에서 실물로
확인됐다**(아래 § 확인한 것).

FAIL 사유는 둘이다.

1. **확정 프레임과 정면으로 어긋나는 문장이 같은 SPEC 안에 남아 있다** — §3 시나리오와 §4 Flow 가
   「`[업무 만들기]` 를 열고 **`담당`** 에서 고른다」는 **폐기된 창**을 그대로 말한다. FE 워커가
   §2 U-6 을 읽으면 담당 칸을 없애고, §3·§4 를 읽으면 담당 칸을 만든다. 발주 브리프가
   「이번 확정 프레임과 충돌하는 문구는 정정한다」로 명시한 바로 그 항목이다.
2. **파이프라인 단방향·링크 규칙 위반 7건** — SPEC 이 WORK 를 역참조한다(`project.md:144·155`).
   이 프로젝트에서 **이전 검수가 이미 강제해 PASS 근거로 적은 항목**이다
   (`review-spec-report.md:300`·`:500`).

둘 다 **범위가 좁고 세 파일 안에서 닫힌다.** 계약을 다시 설계할 일은 없다 —
writer 수정 재발주 1회로 충분하다. **그 전까지 BE·FE 코드 발주를 막는다.**

## 검수 범위

- **git diff 로 범위를 낼 수 없다.** `para/projects/summer-star/strong-hajin/` 전체가 아직
  untracked 다(`git status --porcelain` → `?? para/projects/summer-star/strong-hajin/`).
  커밋된 이전 판이 없어 `git diff <base>...HEAD` 가 빈다.
- 대신 **mtime 으로 이번 dispatch 의 변경 파일을 특정**했다. `2026-09-19` 는 셋뿐이다 —
  `30-work/work-003-…md`(21:06) · `10-decision/decision-001-work-page.md`(21:07) ·
  `20-spec/spec-001-work-management.md`(21:07). 나머지 13개 문서는 09-15~09-17 그대로다.
  → **allowed paths(writer brief § 산출물/허용 파일) 이탈 0건.**
- 검수한 것: 위 세 파일 **전문**(696 + 1,349 + 466줄), 기준으로 `para/projects/project.md`
  §3.2~3.5, 기존 BASE-001·DEC-002·SPEC-002·SPEC-003·WORK-001·WORK-002 및 단계 index 셋,
  writer brief, 이전 검수 리포트(`review-spec-report.md`·`review-wp-report.md`).
- 실행한 검사
  - `git status --porcelain --untracked-files=all` · `find -newer`/`stat` mtime 대조
  - 본문 wikilink 전수(`awk` 로 frontmatter 밖 `[[ ]]` 추출)
  - 코드 레포 **read-only 대조**(`/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-work`):
    allowed paths 27개 존재 확인 · `Makefile` 타깃 · `task_creation.py` · `request_commands.py` ·
    `requests.py` · `application.py` · `persistence.py` · `tool_catalog.py` · `WorkModals.tsx` grep
  - **테스트·빌드·DB·브라우저 실행 0건**(브리프 금지 준수)
- **`python3 scripts/lint-pipeline.py --strict` 는 실행하지 못했다 — 파일이 없다**
  (`scripts/` 에 `check-concept-map.py`·`hooks`·`rules.md` 뿐, 레포 전체에 `lint*pipeline*` 0건).
  브리프 지시대로 WARN 으로 기록한다(아래 W-6). 그래서 frontmatter·자리·링크는 **수동으로**
  기존 문서 넷과 대조했다.

## 위반 (FAIL 사유)

### F-1. 확정 프레임이 지운 「업무 만들기 창의 담당 칸」이 §3·§4 에 그대로 살아 있다

- `20-spec/spec-001-work-management.md:470` — S-1 ①
  「`[업무 만들기]` 를 연다. **`담당` 은 나로 채워져 있고** `출처` 줄은 없다.」
- `20-spec/spec-001-work-management.md:481-482` — S-2 ①
  「구성원이 `[업무 만들기]` 를 열고 **`담당` 에서 동료를 고른다.** 후보는 내가 보낼 수 있는 사람만」
- `20-spec/spec-001-work-management.md:863` — §4 Flow 시퀀스
  「요청자->>FE: **업무 만들기 + 담당 = 동료**」

근거: 같은 문서 `:267` U-6-a 표 「담당자 | **줄이 없다** | `담당자` — 한 명」, `:271-272`
「**`업무` 갈래는 담당자를 묻지 않는다.** 화면에 칸이 없고 서버가 현재 사용자를 담당자로 기록한다.
남에게 보내는 길은 `요청` 갈래 하나다」 · `decision-001:435`·`:439-440` D-21 ·
`decision-001:503` 기각 항목 「**업무 갈래에 담당자 칸 두기**」 ·
writer brief 「기존 문서의 … 이번 확정 프레임과 충돌하는 문구는 정정한다」.

무엇이 깨지나: S-1·S-2 는 **FE 인수 시나리오로 읽히는 자리**다. Phase 4 의 「`업무` 갈래에
담당자 칸을 **세우지 않는다**」(work-003:354)와 S-2 가 정반대를 지시한다. 세 줄 모두 **UI 서술**이라
「API 는 `assignee_id` 를 계속 받는다」(`:666`, 보존 대상)로 변호되지 않는다.

- **권장 수정(최소)**
  - `:470` → 「`[업무 만들기]` 를 연다. 갈래는 `업무` 고 **담당자 칸이 없다**(서버가 나를 담당자로
    기록한다). `출처` 줄은 없다.」
  - `:481-482` → 「구성원이 `[업무 만들기]` 를 열고 **머리의 토글을 `요청` 으로 바꾼 뒤** `담당자`
    에서 동료를 고른다. 후보는 …」
  - `:863` → 「요청자->>FE: **업무 만들기 · 갈래=요청** + 담당자 = 동료」
  - 세 줄을 고치면 §2·§3·§4 가 한 프레임을 말한다. 계약값은 하나도 바뀌지 않는다.

### F-2. SPEC 이 WORK 를 역참조한다 — 파이프라인 단방향 규정 위반

- `20-spec/spec-001-work-management.md:23` — frontmatter
  `works: - "[[work-003-inbox-predecessor-and-create-frame|WORK-003]]"`
- `20-spec/spec-001-work-management.md:76` — 본문
  「실행 WP 는 `[[work-003-…|WORK-003]]` 이 갖는다.」

근거: `para/projects/project.md:144` 「**SPEC → WORK 는 단방향이다.** spec 본문에 work ID 를
박지 않는다. work 의 frontmatter `links.specs` 가 소유하고, spec 중심 목록은
`30-work/README.md` 의 Spec Coverage 가 **derived view** 로 만든다」 · `:155`
`works: []  # spec 은 비워 둔다 — 위 단방향 규정`.
같은 프로젝트의 기존 세 SPEC 은 전부 `works: []` 다(spec-002:21 · spec-003:22).
이전 검수가 이 항목을 **PASS 근거로 명시**했다 — `review-spec-report.md:300`
「spec 의 `links.works` 는 둘 다 `[]` — `project.md:155` 단방향 규정 준수」.

- **권장 수정**: `:23` 줄을 지우고 `works: []` 로 되돌린다. `:76` 은 wikilink 를 빼고
  「실행 WP 는 **WORK-003** 이 갖는다」로 ID 서술만 남긴다(F-3 과 같은 고침).

### F-3. 본문 관계 wikilink — frontmatter 중복 금지 위반 (이번 diff 5건)

| 파일:줄 | 대상 |
|---|---|
| `decision-001-work-page.md:463` | `[[spec-003-task-lifecycle-v2\|SPEC-003]]` |
| `decision-001-work-page.md:620` | `[[spec-003-task-lifecycle-v2\|SPEC-003]]` (OQ-M 행) |
| `decision-001-work-page.md:656` | `[[work-003-…\|WORK-003]]` |
| `spec-001-work-management.md:837` | `[[spec-003-task-lifecycle-v2\|SPEC-003]]` |
| `spec-001-work-management.md:1323` | `[[spec-003-task-lifecycle-v2\|SPEC-003]]` (OQ-M) |

근거: `para/projects/project.md:148` 「관계 링크는 frontmatter `links` 에만 둔다.
**본문에 중복 작성하지 않는다**」. 다섯 대상 전부 이미 frontmatter 에 있다
(dec-001:24 `related` · spec-001:21 `specs` · dec-001:21 `works`).
이전 검수가 같은 규칙으로 **「frontmatter 밖 `[[…]]` 0건(네 문서 전수)」**를 PASS 근거로 적었다
(`review-spec-report.md:500`).

- **권장 수정**: 다섯 곳 모두 `SPEC-003` · `WORK-003` 처럼 **ID 서술로 낮춘다.** 본문의 다른
  참조(`BASE-001`·`SPEC-002`·`WORK-001`)가 이미 그 모양이다.
- `spec-001:33`(2026-09-17 대체 안내 블록의 `[[spec-003…]]`)은 **이번 diff 밖**이라 아래
  「기존 부채」로 뺀다.

## 경미 (WARN)

- **W-1. 단계 index 와 `log.md` 가 갱신되지 않았다.**
  `30-work/README.md` 의 Status Board · Work List · Spec Coverage 에 **WORK-003 행이 없고**
  (마지막 수정 09-17), `log.md` 에 2026-09-19 줄이 없다. 근거: `project.md:3.5`
  「문서를 만들거나 고치면 **해당 단계 index(README) 와 `log.md` 를 같이 갱신한다**」.
  **writer 의 잘못이 아니다** — 이번 브리프의 허용 파일이 세 개뿐이었고, writer 는 이 사실을
  `work-003:449-450`·`:457-458` 에 Open Issue 로 정직하게 남겼다.
  → **코디네이터 조치**: 다음 발주의 허용 파일에 `30-work/README.md` · `log.md` 를 넣는다.
  코드 발주 전에 닫는 것이 맞다(Status Board 가 실행 상태의 owning view 라서).

- **W-2. CTA 문구가 §2 와 §3 에서 다르다.** `spec-001:299` 는 `[업무 추가]` / `[업무 요청 보내기]`,
  `:471`·`:483` 은 `[생성]`. 확정 프레임이 버튼 이름을 갈래로 갈랐으므로 §3 을 맞춘다.

- **W-3. 화면의 「제목 / 내용」 두 칸이 payload 의 무엇으로 가는지 한 줄이 없다.**
  `spec-001:265`·`:268` U-6-a 는 `업무 제목` 과 `업무 내용` 을 **다른 줄**로 세우는데,
  `:665` 생성 payload 는 `content` 한 필드(「첫 줄이 제목이다. 1~200자, **줄바꿈 불가**」)다.
  줄바꿈을 금지한 한 필드에 「제목 + 내용」 두 칸을 실을 수 없다.
  코드 실물은 `title`(≤300) + `description` **두 필드**다
  (`backend/.../modules/work/task_creation.py:14-15`), SPEC-003 `:642`·`:646` 도 같은
  `content` 관행을 쓰고 있어 **이번 dispatch 가 만든 부채는 아니다.**
  다만 Phase 4 가 두 칸을 실제로 보내므로, `§4 Request / Response` 에
  「화면의 제목·내용은 payload 의 무엇으로 간다」 한 줄이 필요하다.

- **W-4. Case Matrix 의 표시 위치가 없어진 칸을 가리킨다.** `spec-001:806`·`:807` 의
  `WORK_RECIPIENT_NOT_ALLOWED` · `WORK_RECIPIENT_INACTIVE` 표시 위치가 **「생성 창 담당 필드」**다.
  확정 프레임에서 그 칸은 `요청` 갈래에만 있다. → 「생성 창 **요청 갈래**의 담당자 필드」로 좁힌다.
  `:621` 「**생성 창의 담당 후보**」도 같은 손질 대상이다.

- **W-5. Phase 3 의 요청 갈래 결재자 처리가 구현자에게 양자택일로 열려 있다.**
  `work-003:321-322` 「받아서 무시하는 API 를 만들지 않는다(**받지 않고 거절하거나, 아예 필드를
  열지 않는다**)」 + `:330` 「거절이든 미개방이든 말이 된다」. OQ-M gate 자체는 옳게 세웠으나,
  둘 중 무엇인지에 따라 **FE 가 보게 되는 것이 422 냐 404 냐**로 갈린다.
  → 「**필드를 열지 않는다**(보내면 `extra='forbid'` 로 422)」처럼 한 쪽으로 고정하기를 권한다.
  참고로 현행 코드의 `WorkRequestCreateInput` 은 `model_config = ConfigDict(extra='forbid')`
  상속이라 **미개방이 이미 기본 동작**이다.

- **W-6. 린트를 실행하지 못했다.** `scripts/lint-pipeline.py` 가 이 레포에 **없다**.
  브리프가 지시한 대로 실행 불가를 기록한다. frontmatter 필수 필드(`type`·`id`·`title`·
  `status`·`created_at`·`updated_at`·`tags`·`links`)·상태값(`decision: proposed` ·
  `spec: draft` · `work: todo`)·태그 패턴(`product/` · `doc/` · `status/`)·파일명 slug 는
  `project.md:3.4` 기준으로 **수동 확인했고 세 파일 모두 충족**한다.

- **W-7. 「같은 값 한 벌」 목록에서 `reference_task_ids` 가 빠졌다.** `spec-001:685-686` 은
  공통 한 벌을 「제목·내용·시작일·기한·프로젝트·상위·참조자·체크리스트·선행」으로 적는데,
  코드의 공통 payload(`WorkPayloadFields`)는 `reference_task_ids` 를 포함하고 같은 문서
  `:676` 도 그 필드를 「계약으로 남지만 만들기 창에서는 받지 않는다」로 유지한다.
  → 목록에 한 항목을 더하고 「창에서는 안 받는다」를 괄호로 붙이면 닫힌다.

## 기존 부채 (이번 판정 제외)

- `spec-001:33` — 2026-09-17 대체 안내 블록의 본문 wikilink `[[spec-003-…|SPEC-003]]`.
  `project.md:148` 기준으로는 같은 위반이지만 **이번 diff 밖**이다. F-3 을 고칠 때 함께
  손대면 깨끗해진다(코디네이터 판단).
- `spec-001:665` / `spec-003:642` 의 `content`(첫 줄이 제목) 관행과 코드의 `title`+`description`
  두 필드 사이의 어긋남 — W-3 이 가리키는 그 부채의 본체. **문서 넷에 걸쳐 있어 이번 수정
  범위로 끌고 들어오지 않기를 권한다.**
- `work-001:118` — 「체크리스트·하위 업무·참고 업무·프로젝트 연결의 1차 UI 강제 | 하지 않는다」.
  D-21 이 그 넷 중 셋을 **선택 탭**으로 올렸으므로 지금 읽으면 어긋나 보인다. 다만 WORK-001 은
  **끝난 작업의 범위 기록**이라 소급해 고칠 자리가 아니다 — 판단은 코디네이터 몫.
- `20-spec/README.md` · `10-decision/README.md` 의 「최종 수정: 2026-09-17」 — W-1 과 같은 뿌리.

## 확인한 것 (PASS 근거)

체크리스트 항목별로, 그리고 브리프 검수 중점 1~9 별로 남긴다. **확인 안 한 것은 그렇게 적는다.**

### 브리프 검수 중점

1. **최종 모달 프레임** — ✔ 계약됨. 제목 갈래 분기(`spec-001:257`) · 골격 넷(`:238-254`) ·
   필수 `기본 정보` 하나 + 선택 탭 셋(`:259`·`:285-289`) · 갈래별 기본 정보 표(`:263-269`) ·
   `업무 연결` 탭의 상위·프로젝트·선행(`:288`·`:423`) · 자료 두 단계(`:289`·`:294`) ·
   체크리스트 선택 탭(`:287`) · **참고 업무 UI 내림 + 계약 보존**(`:291-293`).
   DEC-001 D-21(`:415-491`)이 같은 것을 결정으로 갖고, D-16 정정을 **표로 대체**해
   「지우지 않는다」를 명시(`:473-483`). **단 §3·§4 의 잔재가 F-1.**
2. **담당자·참조자·결재자 정합** — ✔. 업무 갈래 담당자 없음 + 서버 기록(`:267`·`:271-272`),
   요청 갈래 담당자 1명 필수(`:267`·`:299`), 참조자 다수 · 결재자(=`approver_id`) 1명(`:269`·
   `:276-279`), 권한은 D-9 그대로(`:782` Validation · `:1043` 권한표).
   **코드 대조**: `TaskCreateInput`/`WorkRequestCreateInput` 이 `WorkPayloadFields` 를 공유하고
   `cc_member_ids` 가 **업무 갈래에서도 실제 저장**된다(`application.py:201·207·228`) —
   writer 의 「현행 화면 문구가 낡았다」 주장이 **실물로 확인됨**.
3. **CC 읽음 영수증** — ✔. 사용자별(`:401`·`:936`) · 멱등(`:723-724`·`:1024-1026`) ·
   `version` 불변(`:725-726`) · CC 관계 불변(`:727`·`:1080`) ·
   **`reference` 갈래에만 필터**(`:732-740`) · 요청 목록(`참조 업무` 탭)과 업무 조회에는 없음
   (`:738-740`·`:626`) · 업무 요청 수락/거절 흐름 불변(`:728`·`:417-418`) ·
   MCP 조회는 같은 projection, **명령은 미노출**(`:743-745`·`:1095-1096`).
   **코드 대조**: MCP 도구 설명의 「including processed ones」가 실제로 있다
   (`modules/ax_execution/tool_catalog.py:70`) — 고칠 문구가 실재한다.
   영수증 모양의 선례 `work_request_list_entries` 도 실재(`platform/persistence.py:1592`).
4. **선행업무** — ✔ 전 항목. parent/reference 와의 분리표(`:975-984`, DEC `:341-348`) ·
   같은 프로젝트(`:791`) · 복수(`:429`) · 자기/중복/순환(`:791`·`:829-831`) ·
   프로젝트 변경 거부(`:790`·`:832`) · finish-to-start 시작 게이트(`:913-921`) ·
   **`시작 전 → 완료` 우회 차단**(`:918`·`:451-452`) · 취소 선행 예외(`:845`·`:920`) ·
   동시성(`:1027-1030`) · 뗀 행 보존(`:1082-1083`).
5. **payload 분리** — ✔(W-7 의 한 항목 누락 제외). 공통 표 `:663-676` 에
   `approver_id`·`preceding_task_ids`·`project_id`·`parent_task_id`·`cc_member_ids`·
   `checklist`·`start_date`·`due_date` 가 모두 서고, 요청 전용은 **담당자 하나**로 명시(`:691-692`).
   **코드 대조**: `WorkPayloadFields` 가 공통 9값, 요청 전용은 `assignee_id`·
   `supersedes_request_id` 둘 — 문서의 분리와 코드의 분리가 **같은 모양**이다.
   `start_date` 가 요청 생성에도 실재한다(`requests.py:66·264` · `WorkPayloadFields` 상속) —
   writer 의 「화면이 낡았다」가 맞고, 반대로 말하는 FE 주석(`WorkModals.tsx:3492`)이 낡았다.
6. **surface · 권한 · 오류 · 인수조건 · WP 분담** — ✔. REST/MCP/AX/회의 후속이 같은 계약
   (`:1086-1098`), 권한표에 새 줄 셋(`:1052-1054`), 오류 코드 8종이 **기존 409/422 계열에
   맞춰** 신설되고 `WORK_CHILDREN_UNFINISHED` 와 **합치지 않는다**는 이유까지 적혔다
   (`:826-848`). SPEC-003 `:769` 에 그 형제 코드가 실제로 409 로 서 있음을 확인.
   인수조건 세 블록(`:1185-1260`)이 WP Phase 와 1:1 로 닿고, WORK-003 이 BE 3 · FE 3 · 공통 1
   으로 갈라 **allowed paths · 의존성 · 검증 명령 · 완료 증거**를 전부 갖는다.
7. **미결 보존** — ✔. OQ-M(`dec:620`·`spec:1322-1326`)·OQ-N(`dec:621`·`spec:1327`)·
   OQ-L(`dec:630`)·OQ-A·OQ-H~K 를 **임의 확정하지 않았다.** OQ-M 은 `work-003:61-63`·`:334`
   에서 **Phase 3 의 한 조각만 gate** 로 격리되고 나머지 전부가 착수 가능하게 갈렸다.
   SPEC-003 대조: 「요청 Task 의 확인자는 **요청자로 고정**」이 실재하고(`spec-003:76`·`:579`·
   `:1410`) M-22·M-20 도 실재(`:1425`·`:1427`) — **OQ-M 의 전제가 사실**이다.
8. **기존 계약 보존** — ✔ 삭제·약화 0건. D-1~D-18 본문이 그대로 있고, D-16 만 **표로 정정**되며
   「지우지 않는다」가 명시(`dec:483`). 보존 절(`spec:1073-1084`)과 WORK-003 Done Criteria
   (`:448`)·Pre-deploy(`:430`)가 같은 것을 못 박는다. WP 는 **테스트(unit/contract/PostgreSQL/
   FE/e2e)·migration 부재 사실·allowed paths·동시성 검증**을 전부 포함한다
   (`work-003:155-159`·`:187-193`·`:262`·`:305`·`:416-422`).
9. **allowed paths · frontmatter · 자리 · 중복 · 출처** — allowed paths ✔(mtime 대조, 위 §검수 범위),
   frontmatter ✔(수동, W-6), 문서 자리 ✔(`10-`/`20-`/`30-` 정위치, 새 최상위 디렉토리 0),
   중복 ✔(WORK-003 은 WORK-001·002 를 복제하지 않고 `Depends on work` 로 가리킨다),
   출처 ✔(도메인 사실이 BASE-001·회사 원문 줄번호·SPEC-003 절 이름으로 달렸다).
   **링크 규칙만 F-2·F-3 으로 깨졌다.**

### 역할 rules.md 체크리스트

- **린트** — ✗ **확인 안 함**: 스크립트 부재(W-6). 수동 대조로 대체했다.
- **frontmatter** — ✔ 세 파일 필수 필드·상태값·태그 충족. `work-003` 의 phase 상태(전부 `TODO`)와
  `status: todo`·`progress: 0` 이 `project.md:3.4` 동기화 규칙에 맞는다.
- **자리 규칙** — ✔.
- **출처** — ✔. 특히 writer 가 「현행 동작」으로 단정한 것 다섯을 **코드에서 직접 확인**했고
  전부 사실이었다(참조자 저장 · 요청 시작일 수용 · MCP 도구 설명 문구 · 영수증 선례 표 ·
  `preceding_task_ids` 이름의 출처 주석 `task_creation.py:53`). 「담당자 서버 고정」은 writer 가
  **「현행 동작으로 보이나 계약으로 고정된 적이 없다」로 헷지**하고 회귀로 못 박게 했다 —
  옳은 처리다.
- **표·인덱스 정합** — ✗ W-1(허용 파일 밖, writer 가 Open Issue 로 남김).
- **중복** — ✔ 0건.
- **allowed_paths** — ✔ 이탈 0건.

## 코드 발주 전 조치 (FAIL 이므로)

**writer 수정 재발주 1회.** 허용 파일은 이번과 같은 셋 + `30-work/README.md` · `log.md`
(W-1 을 함께 닫으려면). 수정 목록은 여섯 줄이고 전부 국소적이다.

| # | 파일 | 줄 | 무엇 |
|---|---|---|---|
| 1 | spec-001 | 470 · 481-482 · 863 | 폐기된 「생성 창 담당 칸」 서술을 확정 프레임으로 (F-1) |
| 2 | spec-001 | 23 · 76 | `works: []` 로 되돌리고 본문 work 역참조 제거 (F-2) |
| 3 | spec-001 / decision-001 | 837 · 1323 / 463 · 620 · 656 | 본문 wikilink → ID 서술 (F-3) |
| 4 | spec-001 | 299 vs 471 · 483 | CTA 문구 일치 (W-2) |
| 5 | spec-001 | 665 부근 · 806-807 · 621 · 685 | 제목·내용 매핑 한 줄 · 「요청 갈래」 한정 · 공통 한 벌에 참고 업무 (W-3·W-4·W-7) |
| 6 | work-003 | 321-322 · 330 | 요청 갈래 결재자 처리를 한 쪽으로 고정 (W-5) |

**계약 내용은 건드리지 않는다.** 위 여섯은 전부 **서술·링크·문구**이고, 다시 설계할 결정이
하나도 없다. 재검수는 이 리포트에 `## 재검수 (2차)` 절을 더해 해소 여부만 본다.

---

## 재검수 (2차, 2026-09-19)

writer 수정 dispatch(`task_d37125a8d871 / ctx_4e1e0f6ccb01`)의 결과를 1차 지적 항목별로만 확인했다.

### 판정: PASS — 코드 발주를 허용한다

**초기 FAIL 셋(F-1·F-2·F-3)이 전부 해소됐고, WARN 일곱(W-1~W-7)도 전부 닫혔다.**
계약값이 바뀐 자리는 없다 — 고쳐진 것은 서술·링크·문구와 index 뿐이다.
아래 § 잔여는 **계약을 막지 않는 두 줄**이고, 코드 발주 전에 반드시 닫아야 할 것은 없다.

### 재검수 범위

- 변경 파일(mtime `2026-09-19 21:36~21:37`): `decision-001-work-page.md`(696줄, 길이 동일) ·
  `spec-001-work-management.md`(1,349 → **1,356**줄) ·
  `work-003-…-create-frame.md`(466 → **470**줄) · `30-work/README.md` · `log.md`.
  **다섯 파일 전부 이번 수정 브리프의 허용 범위 안이다 — 이탈 0건.**
  나머지 11개 문서는 09-15~09-17 mtime 그대로다.
- 실행한 검사: 지적 자리 직접 조회 · 본문 wikilink 전수(`awk`) · 오류 코드 전수 ·
  D-N / OQ / Phase / 인수조건 블록 구조 대조 · `find` 로 린트 스크립트 재확인.
  **코드·DB·테스트·브라우저 실행 0건.** 문서 파일은 한 글자도 고치지 않았다.

### F 항목 — 전부 해소

| # | 1차 지적 | 현재 | 근거 |
|---|---|---|---|
| **F-1** | S-1·S-2·Flow 가 폐기된 「생성 창 담당 칸」을 서술 | **해소** | `spec-001:468-469` 「갈래는 `업무` 고 **담당자 칸이 없다** — 서버가 나를 담당자로 기록한다(U-6-a)」 · `:480-481` 「**머리의 토글을 `요청` 으로 바꾼 뒤** `담당자` 에서 동료를 고른다(U-6-a)」 · `:870` 「업무 만들기 · **갈래=요청** + 담당자 = 동료」. 폐기 문구 잔재 grep 0건(`담당\` 은` · `업무 만들기 + 담당` · `생성 창 담당` 전부 0) |
| **F-2** | SPEC 이 WORK 를 역참조(frontmatter + 본문) | **해소** | `spec-001:22` `works: []` · 본문 WORK 역참조 wikilink 0건. `links.specs`(`:19-21`)와 나머지 관계 링크는 **보존**됐다 |
| **F-3** | 본문 관계 wikilink 5건 | **해소** | 세 파일 **frontmatter 밖 `[[…]]` 0건**(`awk` 전수). `dec:656` 「실행 문서는 **WORK-003** 이 갖는다」 · `dec:463` · `dec:620` · `spec:837` · `spec:1329` 전부 ID 서술로 강등. **1차에서 「기존 부채」로 뺐던 `spec:33` 까지 함께 정리**됐다(`:32` 「이 SPEC 의 일부 절은 **SPEC-003** 이 대체한다」) — 대체 안내 본문은 그대로다 |

### W 항목 — 전부 닫힘

| # | 현재 | 근거 |
|---|---|---|
| **W-1** | **닫힘** | `30-work/README.md:8` 최종 수정 `2026-09-19` · `:26` Status Board 에 WORK-003 행(Phase 1–7 · todo · Blocker 「없음(OQ-M은 Phase 3의 한 조각만 gate)」) · `:36` Work List(todo · 0%) · `:44` Spec Coverage(SPEC-001 → WORK-001·002·**003**). `log.md` 에 2026-09-19 세 줄(D-19/D-20 · D-21 · 검수 수정). `work-003:452-454`·`:461-462` 가 「문서 쪽은 닫혔고 구현 때 상태·진척만 다시 움직인다」로 정합 |
| **W-2** | **닫힘** | CTA 전수 4곳이 갈래별로 일관 — `spec-001:250`(골격 발) · `:297`(U-6-c) · `:470` `[업무 추가]` · `:482` `[업무 요청 보내기]`. 잔재 `[생성]` **0건**(세 파일) |
| **W-3** | **닫힘** | `spec-001:677-681` 에 매핑 블록 신설 — 「`업무 제목`/`요청할 업무 제목` → 업무의 제목, `업무 내용`/`요청 내용` → 업무의 본문」, 「`content` 는 **기존 계약 표기**이고 **이번에 재설계하지 않는다**」, 「코드 실물은 두 필드」. **기존 `content` 계약은 손대지 않았다** — `:664` 표 행과 `:787` Validation 행이 1차와 **동일**하다 |
| **W-4** | **닫힘** | `spec-001:813`·`:814` 표시 위치가 「생성 창 **`요청` 갈래**의 담당자 필드」로 좁혀졌고, `:620` 후보 조회도 「생성 창 `요청` 갈래의 담당자 후보 — **`업무` 갈래에는 이 칸이 없다(U-6-a)**」로 명시 |
| **W-5** | **닫힘** | 양자택일이 사라지고 **한 쪽으로 고정**됐다 — `work-003:321-324` 「`요청` 갈래는 `approver_id` **필드를 열지 않는다**. 보내면 **`extra='forbid'` 로 422**(요청 생성 입력이 이미 그 설정을 상속한다). 받아서 무시하는 API 를 만들지 않고 **거절 경로를 새로 만들지도 않는다**」 · 검증 `:332` 「실어 보내면 **422**」 · Gate `:336`. SPEC 쪽도 같은 말로 정렬됐다(`spec-001:696-697`). **OQ-M 은 열린 채 그대로다** |
| **W-6** | **정직하게 기록됨 + 1차 서술 정정** | 린트는 **여전히 실행 불가**다. 1차 리포트가 「`scripts/` 에 …뿐」이라고 적은 것은 부정확하다 — **`scripts/` 디렉토리 자체가 없고**(`ls: scripts: No such file or directory`), 그 파일들은 `rules/`(`check-concept-map.py`·`hooks`·`rules.md`)에 있다. 레포 전체(아카이브 제외)에 `lint*` **0건**. 2차도 frontmatter·상태값·태그·자리·링크를 **수동 대조**로 대신했고 세 문서 모두 `project.md:3.4` 충족 |
| **W-7** | **닫힘** | `spec-001:690-691` 공통 한 벌에 「**참고 업무**〔만들기 창에서는 받지 않는다 — U-6-b〕」가 더해졌다. `:675` 표 행과 뜻이 어긋나지 않는다 |

### 삭제·약화 회귀 점검 — 0건

- **DEC-001**: `D-1`~`D-21` 25개 항목(3b·3c·17b 포함) 전부 존재. 기각 11줄 · 보류표 · Open Questions
  **OQ-A~OQ-N 14개 행 전부** 존재. D-21 의 「요청 갈래 결재자는 확정 계약이 아니다」(`:463-466`)와
  D-16 보존 규칙(`:453`·`:505`) 그대로.
- **SPEC-001**: U-1~U-15 · S-1~S-12 27절 유지. 오류 코드 **29종 전수 동일**(선행 6 + 읽음 1 신설분 포함).
  권한표 새 줄 셋(`:1059-1061`) 유지. 인수조건 **일곱 블록**(W1·W2·업무 페이지·읽음·선행·프레임·회귀)
  유지. §7 OQ-A·D2·OQ-H~N 과 추적 절 유지.
- **WORK-003**: Phase 1~7 전부 `TODO` · 완료 증거 전부 「미작성」 · allowed paths 표 22행 ·
  Scope 제외표 · Pre-deploy · Rollback · Done Criteria · Open Issues 유지. 참고 업무 보존 조항
  (`work-003` 제외표 · Done Criteria `:451`) 그대로.

### 잔여 (WARN · 코드 발주를 막지 않는다)

- **R-1. `spec-001:681` 의 포인터가 빈자리를 가리킨다.** 「낱말을 맞추는 일은 SPEC-003 과 함께
  닫는다(**§7 기존 부채**)」라고 적었으나 §7 에 그 이름의 항목이 없다(문서 전체에서 「부채」 1회 = 이 줄뿐).
  §7 의 실제 자리는 **「타 문서로 넘어가는 추적」**(`:1338-1345`)이다.
  → 최소 수정: 참조를 「§7 타 문서로 넘어가는 추적」으로 바꾸거나, 그 절에 한 줄을 더한다.
- **R-2(관찰, 판정 외). `decision-001:20-21` 의 `works: [[work-003-…|WORK-003]]`.**
  같은 프로젝트의 `decision-002:20` 은 `works: []` 다. 다만 `project.md:144·155` 의 단방향 규정은
  **문면상 spec 을 지목**하므로 decision 에 대해서는 근거가 불충분하다 — 1차에서도 위반으로 올리지
  않았고 2차에서도 올리지 않는다. **통일 여부는 코디네이터 판단.**
- **W-6 유지**: 린트 스크립트 부재로 `lint-pipeline.py --strict` 는 이번에도 **실행하지 못했다.**
- 1차 「기존 부채」 중 `20-spec/README.md`·`10-decision/README.md` 의 「최종 수정: 2026-09-17」은
  그대로다. SPEC-001·DEC-001 행 자체는 이미 그 표에 있어 **내용 누락은 없다.**

### 코드 발주 가부

**허용한다.** BE(Phase 1~3)·FE(Phase 4~6) 병렬 발주를 막는 문서 결함이 없다.
착수 시 지켜야 할 것 둘만 그대로 남는다 — **OQ-M gate**(`요청` 갈래 `approver_id` 미개방,
`work-003:336`)와 **Phase 2·3 의 파일 공유 순서**(`work-003:315`).
