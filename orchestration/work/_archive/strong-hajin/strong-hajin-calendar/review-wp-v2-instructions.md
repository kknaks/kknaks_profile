# [reviewer] WORK-004 2루프 갱신 검수

대상: `para/projects/summer-star/strong-hajin/30-work/work-004-calendar-scheduling.md`
**756 → 1022줄** (+266) · `30-work/README.md` 세 행.
리포트: **새 파일** `review-work-004-v2-report.md`. read-only.

기준: **SPEC-004 v0.3.1**(커밋 `bfe299a`, 계약이고 닫혔다) · **DEC-003 증보 8 + K24** ·
`work-002` (형식) · 1루프 WP 검수 리포트(`review-work-004-report.md`).

## 코디가 확인한 것

- SPEC §6 체크박스 **실측 148** = WP 주장 148 (1루프 124 + 2루프 24). **합계는 맞다**
- Phase 8개(1루프 DONE 4 + 2루프 TODO 4) · 완료 판정 8개 · 허용 파일 둘만

## 볼 것 일곱

1. **인수조건 매핑이 참인가.** 합계는 내가 쟀다. 네가 볼 것은 **묶음별 줄 수가 실제 §6 과 맞는지**
   와 **닫는 Phase 배정이 옳은지**다. 특히 새 O 행(겹침 금지 14)의 BE-3 4 / BE-4 6 / 공유 3 /
   문서 1 갈래, 그리고 K 행(화면)이 12 → 22 로 는 것.
   **닫는 Phase 가 없는 줄이 하나라도 있으면 FAIL.**
   단 「한계가 문서에 적혀 있다」류는 **문서가 닫는다** — writer 가 그렇게 표시했다. 맞는지 보라.
2. **Phase 경계가 옳은가.** BE-3(배정 쪽) / BE-4(회의 쪽 + `approval` + 대장) 로 가른 것이
   **실제로 독립적인가** — BE-3 만 서도 쓸 수 있나, BE-4 가 BE-3 의 함수를 재사용하나.
   FE-3(읽기·그리기) / FE-4(쓰기 문구·다른 화면) 도 같은 눈으로.
3. **「스키마를 바꾸지 않는다」가 참인가.** `approval` 은 합본 조회 **응답 필드**지 열이 아니다.
   WP 가 오해 셋을 표로 부정했다는데, **그 표가 실제로 다 부정하나.**
4. **Code Surface 2루프 11행이 빠짐없나.** 특히 **회의 도메인**에서 닿는 자리 —
   회의 생성 경로 · 시각 변경 경로 · `quick-start` 가 일반 예약과 코드를 공유하는가 ·
   `overlapping_blocks` 가 쓸 회의 조회의 **다른 소비처**. 1루프에서 `meetings_visible_to`
   소비처 셋이 걸렸던 것과 같은 자리다. **빠진 자리가 이 WP 의 구멍이다.**
5. **운영 대장 셈** — 새 라우트 없음 · `http_count` 159 **불변** 이 맞나.
   `approval` 이 `http_signature` 에 드는지는 확인 항목으로 남았나.
6. **1루프 Phase 를 DONE 으로 닫은 것이 정확한가** — sha 넷이 맞나
   (`1c15d02` · `2a85176` · `c5b109d` · `aa8576b`).
7. **1루프 WP 가 갖고 있던 것이 회귀하지 않았나** — 인수조건 A~N·DS-gaps 처분·검증 계획.
   특히 인수조건 **ID 를 안 밀었다**는 writer 주장(O 를 J 와 K 사이에 넣었다)이 정당한가.

## 판정

FAIL 은 「구현자가 서로 다르게 읽어 다른 것이 돌아가는 자리」·「닫는 Phase 없는 인수조건」·
「빠진 Code Surface」에만. 문장 다듬기 지적 금지.
WARN 은 **어느 Phase 브리프에 실을지**를 적어라 — 그대로 싣는다.
