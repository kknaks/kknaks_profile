# 리뷰 리포트 — WORK-004 2루프 갱신 검수 (2026-09-21)

## 판정: **PASS**

**일곱 항목이 전부 통과했다.** FAIL 셋(다르게 읽히는 자리 · 닫는 Phase 없는 인수조건 ·
빠진 Code Surface)에 걸리는 것이 없다.

가장 걱정한 자리 — **회의 도메인 Code Surface** — 를 **내가 직접 세어 확인했다.**
회의 생성 **둘** · 시각 변경 **하나** · 저장소 쓰기 문 **하나**가 전부이고,
**넷 다 WP 가 적은 행 안에 있다.** 빠진 파일이 없다.

묶음별 줄 수 **15묶음 전부** SPEC 실측과 일치하고, 코디가 짚은 두 갈래
(**O 의 4/6/3/1** · **K 의 22**)를 **SPEC §6 의 실제 줄에 하나씩 대 봐서** 배정이 옳은 것을 확인했다.

WARN 둘은 **구현 중에 닫아도 되는 것**이고 어느 브리프에 실을지 적었다.

---

## 검수 범위

`work-004-calendar-scheduling.md` **756 → 1022줄**(+266) · `30-work/README.md` **세 행**.
기준: **SPEC-004 v0.3.1** · DEC-003 증보 8 + K24 · `work-002`(형식) · 1루프 WP 검수 리포트.
`git status` 에 **허용된 둘만** 떴다(+ 리포트가 사는 untracked `orchestration/`).
확인이 필요한 자리만 **코드 워크트리를 읽었다** — 고친 것은 없다.

---

## 1. 인수조건 매핑이 참인가 — **묶음별 줄 수 15개 전부 일치. 배정도 옳다**

### 묶음별 실측 대조

SPEC §6 을 직접 세어 WP 표와 맞췄다.

| 묶음 | SPEC 실측 | WP | 묶음 | SPEC 실측 | WP |
|---|---|---|---|---|---|
| A 배정 만들기 | 13 | 13 ✓ | I 회의 | 9 | 9 ✓ |
| B 하루 한 칸 | 10 | 10 ✓ | J 금지는 말로 | 2 | 2 ✓ |
| C 회차 | 10 | 10 ✓ | **O 겹침 금지** | **14** | **14 ✓** |
| D 시각 조정 | 5 | 5 ✓ | **K 화면** | **22** | **22 ✓** |
| E 기간 이동·조정 | 7 | 7 ✓ | L 표면 등록 | 8 | 8 ✓ |
| F (가) 기간 밖 | 20 | 20 ✓ | M 「선다」 | 7 | 7 ✓ |
| G (나) 업무 종료 | 7 | 7 ✓ | N 계층 | 1 | 1 ✓ |
| H 합본 조회 | 13 | 13 ✓ | **합계** | **148** | **148 ✓** |

### O(겹침 금지 14) 갈래 — **14줄을 하나씩 대 봤다. 빠짐도 겹침도 없다**

| SPEC §6 겹침 금지 줄 | WP 배정 | 맞나 |
|---|---|---|
| 1 배정 409 · 2 경계 통과 · 3 **멱등 영수증** · 4 시각 변경 409 | **BE-3 (4)** | ✓ 전부 배정 쪽 |
| 5 회의 409 · 6 `quick-start` · 7 사외 · 8 제목·내용 없음 · 9 자정 분할 · 14 회의 계약 회귀 | **BE-4 (6)** | ✓ 전부 회의 쪽 |
| 10 기존 데이터 · 11 읽는 문 하나 · 12 한 트랜잭션 | **공유 (3)** | ✓ 두 Phase 에 걸린다 |
| 13 한계가 문서에 적혀 있다 | **문서 (1)** | ✓ 아래 참조 |

4+6+3+1 = **14**, **한 줄도 두 번 세지 않았고 빠진 줄도 없다.**
줄 8(「거절 문구가 이름까지만」)을 BE-4 에 둔 것도 맞다 — **배정 쪽은 「나의 시간만」이라
남의 일정이 애초에 안 나온다.**

### K(화면 22) 갈래 — **새로 는 10줄이 정확히 갈렸다**

1루프 12(FE-1 10 · FE-2 2)는 그대로이고, 2루프가 더한 10줄이 이렇게 갈렸다:

| SPEC §6 화면 줄 | WP 배정 |
|---|---|
| 7 K19 배지 둘 · 9 「완료」로만 0개 · 11 K20 월 뷰 · 12 시간순 · 13 주 뷰 회귀 · 14 K21 나란히 | **FE-3 (6)** ✓ |
| 15 K23 회의가 선다 · 16 K24 업무 그대로 · 17 K24 회의 탭 무관 | **FE-4 (3)** ✓ |
| 8 합본 조회가 `approval` 을 낸다 | **BE-4 (1)** ✓ — 서버 필드라 BE 가 맞다 |

10 + 12 = **22** ✓. **`approval`(BE-4)이 그것을 쓰는 FE-3 보다 먼저** 온다(순서 BE-3→BE-4→FE-3→FE-4) —
**의존이 역행하지 않는다.**

### 「문서가 닫는다」 한 줄 — **정당하다**

SPEC §6 그 줄 자체가 「**이 줄은 구현이 아니라 문서가 닫는다**」로 적혀 있고,
WP 가 그것을 **표 아래 인용으로 명시**했다(「Phase 에 매달 자리가 없어 여기 명시한다 —
「닫는 Phase 가 없다」로 읽히지 않게」). 실제로 SPEC §2.9 와 WP §Domain / Schema 두 곳이
같은 말을 갖는다. **닫는 자리가 없는 줄이 아니다.**

**→ 148줄 중 닫는 Phase(또는 문서)가 없는 줄 0개.**

---

## 2. Phase 경계가 옳은가 — **BE-3/BE-4 도 FE-3/FE-4 도 독립적이다**

### BE-3 / BE-4

- **가른 이유가 적혀 있다** — 「**회의 도메인을 건드리는 것(BE-4)이 `§1 Scope Out` 을 뒤집는
  자리**다. **배정 쪽(BE-3)만 먼저 서도 그것만으로 쓸 수 있고**, 회의 쪽에서 막히면
  **배정 쪽이 인질이 되지 않는다.**」
- **BE-3 만 서도 쓸 수 있나 — 쓸 수 있다.** BE-3 이 `overlapping_blocks` 를 만들고
  **배정 생성·시각 변경**에 건다. 시간 블록이 둘(배정·회의)이므로 **BE-3 만으로도
  「내 배정이 내 일정(회의 포함)과 안 겹친다」가 성립한다.** 회의 생성 쪽만 아직 안 막힐 뿐이다.
- **BE-4 가 BE-3 의 함수를 재사용하나 — 한다.** BE-4 작업 1 이 「**BE-3 이 만든 같은 함수를 쓴다**」로
  못 박았고, SPEC §2.9 의 「읽는 문은 하나다」와 같은 말이다. 인수조건도 공유 3줄에 「읽는 문이 하나」가 있다.

### FE-3 / FE-4

- **FE-3** 「보이는 것을 고친다. **쓰기는 안 는다**」 — K20 월 뷰 · K21 나란히 · K19 배지. 전부 렌더링.
- **FE-4** 「**말과 자리**를 붙인다」 — K22 거절 문구(쓰기 경로의 말) · K23·K24(**다른 화면**).
- **겹치지 않는다.** FE-3 은 캘린더 화면의 그리기, FE-4 는 문구와 업무 탭 레일이다.
  FE-3 이 막혀도 FE-4 의 업무 탭 작업이 인질이 되지 않는다.

**직렬 순서와 의존이 맞다** — BE-3 → BE-4 → FE-3(`approval` 필요) → FE-4.
「BE 가 닫히기 전에 FE 를 태우지 않는다」·「BE-2 뒤에 실제 API 계약을 FE 브리프에 박는다」도 유지됐다.

---

## 3. 「스키마를 바꾸지 않는다」가 참인가 — **참이고, 오해 셋을 다 부정한다**

`:329-336` 의 표가 **셋을 각각** 부정한다.

| 오해 | WP 의 답 | 맞나 |
|---|---|---|
| `approval` 을 `task_schedules` 에 넣나 | **아니다 — 합본 조회의 응답 필드**다. 업무 쪽 기존 값을 **투영**할 뿐 저장하지 않는다 | ✓ SPEC §4 가 응답 행 필드로 적었고, 값도 `derived.approval` 계승이라 **저장 원본이 이미 있다** |
| 겹침을 부분 unique 로 막나 | **못 한다 — 두 표에 걸쳐 있다** | ✓ SPEC §2.9 와 같은 말 |
| 회의 쪽에 열을 더하나 | **아니다 — 검증 한 겹뿐이고 행 모양·요청 필드 그대로** | ✓ SPEC §4 회의 표와 같은 말 |

**한계 표도 WP 가 다시 갖는다**(DB vs application · 동시 둘이면 둘 다 설 수 있다) — SPEC §2.9 와 일치.
**2루프 Code Surface 11행에 `persistence.py` 가 없다** — 스키마를 안 바꾼다는 말과 행동이 같다.

---

## 4. Code Surface — **빠진 자리 없다. 회의 도메인을 직접 세어 확인했다**

WP 는 이 다섯을 「**BE-4 착수 전에 `grep` 으로 센다**」로 **질문 형태로** 남겼다.
**질문은 정확하다**(1루프에서 `meetings_visible_to` 소비처 셋이 걸렸던 것과 같은 자리를 짚었다).
그래서 **내가 세어 봤다** — 그 답이 WP 의 행 안에 드는지가 이 항목의 판정이기 때문이다.

| 물음 | 실측 | WP 의 어느 행에 드나 |
|---|---|---|
| **회의 생성 경로가 몇 개인가** | **둘** — `MeetingApplication.create`(`modules/meetings/application.py:290`) · `quick_start`(`:376`) | **#27** ✓ |
| **`quick-start` 가 일반 예약과 코드를 공유하는가** | **공유하지 않는다.** 둘이 `self._repository.create(...)` 를 **각각**(`:319` · `:383`) 부른다 | — **분기가 필요 없다.** `create` 에만 걸면 `quick_start` 는 자연히 안 탄다 |
| **시각 변경 경로가 몇 개인가** | **하나** — `update_info`(`:413`), 쓰기는 `:443-444` | **#27** ✓ |
| **저장소 쓰기 문** | **하나** — `platform/meetings.py:69` 의 `create` | **#25** ✓ |
| **`overlapping_blocks` 가 쓸 회의 조회의 다른 소비처** | WP 가 「**고치지 말고 새로 읽는다**」로 **선제 차단** | **#25** ✓ — 1루프 교훈이 규칙으로 들어갔다 |

**→ 넷 다 WP 가 적은 행(#25 · #27) 안에 있다. 빠진 파일이 없다.**
회귀 가드 표와 「`mcp.py`·`tool_catalog.py` 는 건드리지 않는다」도 1루프 그대로 남았다.

다만 **답을 WP 가 안 들고 있다** — 그것이 아래 **WARN-1** 이다.

---

## 5. 운영 대장 셈 — **맞다**

- 1루프: `http_count` **156 → 159**(라우트 셋) · `tool_count` **129 불변** — 그대로 남았다.
- 2루프: **새 라우트 없음** → `http_count` **159 그대로**(`:704`·`:717`·`:946`).
  **검증도 겹침 검증 한 겹뿐이라 라우트가 늘 이유가 없다** — SPEC §4 회의 표와 일치.
- **`approval` 이 `http_signature` 에 드는지는 확인 항목으로 남았다** ✓ —
  `:704` 「**확인하고**, 들면 **그 행만** 패치한다. **전체 재작성 금지**」.
  BE-4 완료 판정(`:717`)도 「대장 diff 가 **그 항목만**이고 `acceptance_evidence` 가 보존됐다」를 갖는다.
  **모르는 것을 단정하지 않고 확인 항목으로 둔 것이 맞다.**

---

## 6. 1루프 Phase 를 DONE 으로 닫은 것 — **sha 넷이 실재하고 순서도 맞다**

코드 워크트리 `git log` 로 대조했다.

| Phase | WP | 실제 커밋 |
|---|---|---|
| BE-1 | `1c15d02` | `1c15d02 feat(calendar): task_schedules 와 배정 CRUD · 합본 조회 (BE-1)` ✓ |
| BE-2 | `2a85176` | `2a85176 … D1 세 자리 검증과 schedule_release · 표면 등록 (BE-2)` ✓ |
| FE-1 | `c5b109d` | `c5b109d … 3분할 화면 골격과 월·주 뷰 (FE-1)` ✓ |
| FE-2 | `aa8576b` | `aa8576b … 상호작용 넷과 금지 문구 (FE-2)` ✓ |

**넷 다 존재하고 Phase 이름이 커밋 제목과 맞으며 순서도 WP 의 직렬 그림과 같다.**
`README.md` 세 행도 같은 sha 넷을 싣고 `in_progress` · 「1루프 4/4 · 2루프 0/4」로 갱신됐다.

---

## 7. 1루프 WP 가 갖고 있던 것이 회귀하지 않았나 — **0건**

- **인수조건 A~N** — 열넷 전부 남았고 **줄 수도 1루프와 같다**(위 표).
  각 행의 닫는 Phase 와 확인 방법도 그대로다.
- **DS-gaps 8행** — `| G-CAL-0` 행 **8개** 그대로.
- **검증 계획** — `make test-postgres POSTGRES_TEST_URL=…` 전체 명령 유지, 2루프 Phase 가
  「BE-3·BE-4(회귀)」·「FE-4(2루프 최종)」로 **추가**됐다. `make verify` 는 각 루프 마지막에 한 번.
- **회귀 가드 표 · `mcp.py` 미터치 · D1 세 자리 · 상태 아홉 경로** — 전부 남았다.

### ID 를 안 민 것 — **정당하다**

`O` 를 `J` 와 `K` 사이에 넣고 **`A`~`N` 을 그대로 두었다.** 이유도 적혀 있다 —
「알파벳 순서가 아니라 **SPEC §6 의 등장 순서**에 맞췄다 · 1루프 ID 를 **밀지 않는다** —
밀면 아래 「갈라지는 줄」과 **옛 보고가 가리키는 글자가 어긋난다**」.

**맞는 판단이다.** 1루프 검수 리포트가 「F 20줄」·「M 7줄」처럼 **글자로** 가리키고 있어,
밀었으면 그 문서들이 전부 다른 것을 가리키게 된다. **읽는 순서를 지키면서 참조를 안 깨는 유일한 방법**이다.

---

## WARN — 구현 중에 닫아도 되는 것

### WARN-1 → **Phase BE-4 브리프** (그리고 BE-3 착수 전에 한 번 보기)

**WP 가 회의 도메인 census 를 「착수 전에 센다」로 미뤘다.** 질문은 정확한데 **답이 WP 에 없다.**
「목록 밖에서 FAIL 이 난다」는 경계심은 옳지만, **이번 건은 실제로 작다 — 내가 세어 뒀으니
그 숫자로 시작하면 된다.**

**브리프에 실을 것** (내가 실측한 값):

> 회의 도메인에서 닿는 자리는 **넷**이고 전부 Code Surface 안에 있다.
> - **생성 둘** — `MeetingApplication.create`(`modules/meetings/application.py:290`) ·
>   `quick_start`(`:376`). **둘은 코드를 공유하지 않는다** — 각각 `_repository.create` 를
>   부른다(`:319` · `:383`). **그래서 분기가 필요 없다: `create` 에만 걸면 `quick_start` 는
>   자연히 검사를 안 탄다.**
> - **시각 변경 하나** — `update_info`(`:413`), 쓰기는 `:443-444`.
> - **저장소 쓰기 문 하나** — `platform/meetings.py:69`.
>
> **착수 전 `grep` 은 그대로 하되, 위와 다르면 그 사실이 곧 발견이다** — 그때 코디에게 올린다.

### WARN-2 → **Phase BE-3 브리프**

**자정 분할의 구현은 BE-3, 인수조건은 BE-4 다.**
BE-3 작업 3 이 「자정 넘는 회의를 날짜별로 쪼개 비교한다 — **이 함수 안에서 한다**」인데,
O 갈래는 그 인수조건(SPEC §6 겹침 금지 9번 줄)을 **BE-4 (6줄)** 에 넣었다.
**닫는 Phase 가 있으니 구멍은 아니다** — 다만 **BE-3 이 자기가 만든 로직을 증명하지 않고 넘어간다.**

**브리프에 실을 한 줄**: 「BE-3 완료 판정에 한 줄 더한다 —
**「자정을 넘는 회의와 겹치는 배정이 막힌다」**. 배정 쪽도 회의를 블록으로 읽으므로 BE-3 에서
증명할 수 있다. SPEC §6 의 그 줄(회의 쪽)은 BE-4 가 그대로 닫는다.」

---

## 확인한 것 (근거)

- **1** SPEC §6 을 직접 세어(15묶음 148줄) WP 표와 **묶음별로** 맞췄다. O 14줄·K 22줄은
  **SPEC 의 실제 줄을 뽑아 배정에 하나씩 대 봤다** — 중복 0, 누락 0.
  「문서가 닫는다」 한 줄은 SPEC 원문이 그렇게 적은 것을 확인했다.
- **2** 직렬 그림과 BE-3/BE-4·FE-3/FE-4 의 설명·작업·완료 판정을 읽고 **독립성과 재사용**
  (BE-4 작업 1 의 「BE-3 이 만든 같은 함수를 쓴다」)을 확인했다. `approval` 의 의존 순서도 맞췄다.
- **3** 오해 표 세 행을 SPEC §4·§2.9 에 각각 대조했고, Code Surface 에 `persistence.py` 가
  없는 것으로 행동이 말과 같은지 확인했다.
- **4** **코드 워크트리를 읽어** 회의 생성 둘·시각 변경 하나·저장소 문 하나를 **직접 셌고**,
  `quick_start` 가 `create` 와 코드를 공유하지 않는 것까지 확인했다. 전부 #25·#27 안에 든다.
- **5** `http_count` 159 불변과 `approval`→`http_signature` 확인 항목이 남은 것을 확인했다.
- **6** `git log` 로 sha 넷의 실재·제목·순서를 대조했다.
- **7** A~N 열넷의 줄 수, DS-gaps 8행, 검증 계획, 회귀 가드가 남은 것을 확인했고
  **ID 를 안 민 이유**가 옛 보고의 참조를 지키는 것임을 확인했다.
- **read-only 준수** — WP·README·코드 한 줄도 고치지 않았다. 산출물은 이 리포트 하나.

---

## `git -C …/kknaks_profile/strong_hajin status --short`

```text
 M para/projects/summer-star/strong-hajin/30-work/README.md
 M para/projects/summer-star/strong-hajin/30-work/work-004-calendar-scheduling.md
?? orchestration/work/strong-hajin-calendar/
```

**허용된 둘만 떴다.** `orchestration/` 은 리포트가 사는 untracked 디렉토리다.
코드 워크트리(`Strong_hajin/strong-hajin-calendar`)는 **읽기만 했고 `git status` 가 깨끗하다.**
