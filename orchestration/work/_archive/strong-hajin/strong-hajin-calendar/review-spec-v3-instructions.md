# [reviewer] SPEC-004 v0.3.1 재검수

대상: `para/projects/summer-star/strong-hajin/20-spec/spec-004-calendar-scheduling.md`
**v0.3.1 (1836줄, v0.3.0 에서 +39)**

리포트: `review-spec-004-v2-report.md` 에 **「## 2차 검수」 절을 이어 붙인다**(1차를 지우지 마라).
read-only. 코드·문서 한 줄도 고치지 마라.

## 네 FAIL 둘에 대한 수정

**F-2 (K22 가 K12 를 좁힘)** — 셋을 고쳤다고 한다:
1. §5 괄호에 `TASK_SCHEDULE_OVERLAP` 추가 + **「이 목록은 닫힌 열거가 아니다 — 생성 경로에
   409 가 더 생기면 그것도 같은 순서를 탄다」** 를 붙임 (같은 실수 재발 방지)
2. §5 에 전용 bullet — 왜 위험한지(재전송이 자기 배정과 겹친다) + `MEETING_TIME_OVERLAP` 은
   K12 사정권 밖임을 못 박음
3. Case Matrix `OVERLAP` 행에 형제 둘과 같은 단서
4. §6 인수조건 한 줄 — 「같은 멱등 키의 재전송은 자기 배정과 겹쳐도 `200` 영수증」

**F-1 (K24 미반영)** — 다섯을 고쳤다고 한다: §2.10 을 K24 축 표로 교체 · §5 의 합본 조회를
**회의 절반에만** 한정 · OQ-501 을 닫힘으로 · 「미결 하나」 수정 · 추적표에 K24 행.

## 볼 것

1. **F-2 가 실제로 닫혔나.** 드래그 연타를 다시 따라가라 — 같은 키 재전송이 `200` 영수증으로
   끝나는 것이 **한 가지로만** 읽히나. 아직 두 가지로 읽히는 자리가 남았나
2. **F-1 이 실제로 닫혔나.** 「보낸 업무 탭에서 보낸 업무가 사라진다」가 이제 안 일어나나.
   §2.10 · §5 · §6 · 추적표가 **같은 말**을 하나
3. **+39줄이 새 자리를 열었나**
4. **1루프 계약(K1~K18) 회귀 0건인가.** 특히 K12·K10 이 이번 수정으로 흔들리지 않았나
5. 저장소 `.py/.ts/.tsx:줄` 0건 (W-7 회귀)

## 판정

FAIL 은 「구현자가 서로 다르게 읽어 다른 것이 돌아가는 자리」에만. 문장 다듬기 지적 금지.
**PASS 면 WP 갱신으로 넘어간다** — 남은 WARN 은 「WP 로 넘길 것」 목록으로 모아라.
