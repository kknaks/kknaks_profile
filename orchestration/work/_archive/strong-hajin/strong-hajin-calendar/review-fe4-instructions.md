# [reviewer] Phase FE-4 코드 검수 — **이 작업의 마지막 검수다**

너는 **strong-hajin `reviewer` 워커**다. FE-3 을 네가 검수했다면 맥락이 이어진다.
아니면 먼저 읽어라 — `roles/strong-hajin/reviewer/` · `review-fe3-report.md` · `review-be4-report.md`.

대상: 코드 워크트리의 **uncommitted diff** (수정 18, 전부 `frontend/`).
FE-3 은 커밋됐다(`3c3dce2`). 리포트: **새 파일** `review-fe4-report.md`.
read-only. **테스트 돌리지 마라 — 내가 돌렸다.**

기준: SPEC-004 **v0.3.1**(`bfe299a`) · DEC-003 **증보 8~10 (K17~K30)** · WORK `Phase FE-4`.

## 코디가 확인한 것

- `frontend/` 밖 **0건**
- `make frontend-test` **954 passed / 0 failed** (기준선 929 → +25) · `tsc` **exit 0**
- 테스트 파일 diff **+314 / −2** — 거의 순수 추가
- 워커 보고: `make verify` exit 2 는 `material/job-queue` **기존 flaky**
  (`flaky-baseline-evidence.md` 참조) · 나머지 타겟 통과 · `test-postgres` 102p 재통과

## 이 Phase 가 한 셋

| | 무엇 |
|---|---|
| **K22** | 겹침 거절 문구 — **배정은 FE 가드 + 409**, **회의는 서버 문자열 그대로** |
| **K23·K24** | 업무 탭 우측 레일에 회의 — **업무 축은 그대로** |
| **K17** | 「업무 만들기」 단추 |

## 볼 것 아홉

1. **문구의 주인이 옳게 갈렸나** — 회의 겹침은 **서버 문자열을 그대로** 띄우나
   (이름이 들어 있어 FE 가 못 만든다). 배정 겹침은 FE 가 만드나.
   **`detail.code` 를 읽는 자리가 하나라도 있으면 FAIL** — 겹침 `detail` 은 **문자열**이다.
   **마침표를 덧붙이지 않았나.**
2. **경계가 서버와 같은가** — **11:00 에 끝나는 일정 + 11:00 시작은 둘 다 선다**(반열림).
   화면 가드가 그걸 막으면 **「서버가 받는 걸 화면이 막는다」** — K14 에서 겪은 갈림의 재발이다. FAIL.
3. **업무 축이 안 바뀌었나 (K24)** — 「보낸 업무」 탭에서 **보낸 업무가 사라지지 않나.**
   WP 가 이것을 회귀 인수조건으로 올렸다. `MyWorkPage.tsx` 와 `CalendarRail.tsx` 가 바뀌었으니
   **그 회귀를 실제로 확인하라.**
4. **회의가 탭과 무관하게 나오나** — 내 회의. 「보낸 회의」 같은 것을 만들지 않았나.
   **새 표면을 만들었나** — 합본 조회의 회의 절반을 써야 한다.
5. **K17 단추가 기존 모달을 여나** — 시안 `TaskCreateModal` 필드 구성을 따라가지 않았나.
6. **테스트 파일 여덟이 바뀐 이유가 정당한가 — 이게 이번 검수의 핵심이다.**
   `+2` 짜리 여섯은 `CalendarRail` prop 이 늘어 번진 것으로 보이는데 **맞나.**
   **그중 하나라도 단언을 무르게 한 자리가 있으면 FAIL.** `−2` 두 줄이 무엇인지 확인하라.
7. **1루프·FE-3 규율 회귀 0건인가** — `span_from`/`span_to` 로만 그리기(K14) ·
   한 화면 한 요청 · 레일과 격자 같은 범위(K18) · 격자는 상태를 말하지 않음 ·
   `api.ts` 밖 `fetch` 0건 · **조용한 거절 0개**.
8. **+25 테스트에 공회전이 있나** — 픽스처에 겹치는 블록이 없는데 「겹침 문구」를 단언한다든지.
   **BE-4 워커가 변이 검사로 이런 자리를 하나 잡았다** — 같은 눈으로 보라.
9. **K30 을 화면이 약속하지 않나** — 참석자만 추가하는 것은 겹침 검사를 안 지난다.
   사람을 나중에 부르는 화면이 **「충돌 없음」을 약속하면 안 된다.**

## 판정

FAIL 은 계약과 다른 것이 도는 자리·회귀·테스트 약화에만.
**PASS 면 이 작업의 구현이 끝난다** — 남은 것은 **사용자 E2E** 뿐이다.
그러니 마지막으로 **「사용자가 실물에서 만날 자리」**를 따로 모아라 — 2루프가 그렇게 시작됐다.

## 보고

```bash
orca terminal send --terminal term_a9d49810-1ce8-4885-8c5f-b93200c501ac \
  --text "[worker_done] reviewer FE-4 검수 완료 — <판정과 한 줄>" --enter
```
