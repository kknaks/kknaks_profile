# WORK-006 Phase 8 W-1 보정 — 독립 재검수 (read-only)

- **코드 워크트리**: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects` · HEAD `a1f6791`(변동 없음)
- **검수 대상**: `Makefile` 의 `shell-final-preflight` 주석 · `tauri-p8-preflight-fix-report.md` · `tauri-p8-w1-fix-report.md`
- **read-only 검수.** 관문·설치·배포 **실행하지 않았다** — 파일 읽기와 `grep`·`stat`·`git status` 뿐이다.
  종료 코드 사실은 **직전 회차에서 내가 직접 측정한 값**을 인용한다.
- **이 검수 보고서 한 파일만 작성.**

---

## 0. 종합 판정 — **FAIL 0 · WARN 2(둘 다 낮음) · 나머지 PASS**

**안내가 정확히 고쳐졌다.** Makefile 주석이 「**자동화·CI 는 이 타깃을 쓰지 말 것 — 종료 코드가
뭉개진다**」로 시작해 **0/1/2/3 의 뜻**과 **직접 호출 명령**을 적고, 「**이 타깃은 사람이 보는 편의
진입점이다**」로 닫는다. 두 보고서도 같은 표와 같은 명령을 싣는다.

**고침의 성질을 스스로 정확히 말했다** — 「이것은 관문의 결함이 아니라 **안내의 결함**이었다.
스크립트는 처음부터 옳은 코드를 냈고, 그것을 뭉개는 층을 «쓰라»고 적은 것이 틀렸다.」
검수도 같은 판단이다.

**관문은 한 줄도 바뀌지 않았다** — `verify-final-build.mjs` 의 mtime 이 **00:07:23**(직전 회차)이고
`Makefile` 은 **00:14:20**(이번 회차)이다. 구조(EXIT 상수 73-77 · `rehearsal` 81 · M-5 라벨 284 ·
`exit(EXIT_REHEARSAL)` 455 · 458줄)가 **직전 검수 때와 완전히 같다.**

**W-2 잔여 우회와 N-2 는 숨겨지지 않았다** — 오히려 더 분명해졌다.

WARN 2 건은 **문서 맺음말 한 줄의 모순**과 **증거 제시 형식**이다. 둘 다 판정을 뒤집지 않는다.

---

## 1. 판정표

| # | 검수 항목 | 판정 |
|---|---|---|
| 1 | make=편의 진입점 · 자동화는 `cd frontend && node scripts/verify-final-build.mjs` 직접 호출로 0/1/2/3 을 읽는다는 안내 | **PASS** |
| 2 | 관문 코드 · 설정 · 제품/인프라/Release 무변경 | **PASS** |
| 3 | 운영 origin · D-4 · M-1 · M-5 · M-10 · Windows 미결 유지 | **PASS** |
| 4 | W-2 의도적 scratch 우회 한계가 숨겨지지 않음 | **PASS** (요구보다 더 분명) |
| 5 | N-2 스위트 불안정이 숨겨지지 않음 | **PASS** |

---

## 2. FAIL

**없다.**

---

## 3. 항목별 확인

### 3-1. 진입점 안내 (PASS)

`Makefile` **145-155행**(레시피 바로 위):

```make
# ⚠ **자동화·CI 는 이 타깃을 쓰지 말 것 — 종료 코드가 뭉개진다.**
# 스크립트는 원인을 코드로 구분한다: **0** 통과(진짜 src-tauri 에서만) · **1** 관문이 막았다 ·
# **2** 돌릴 수 없다(경로·입력) · **3** 연습(`--shell-root`/`SHELL_ROOT`).
# 그런데 **GNU make 는 레시피의 nonzero 를 모두 자기 코드 2 로 감싼다**(실측: 1·2·3 → 전부 2).
# 그래서 make 로는 「막혔다」와 「돌리지 못했다」와 「연습이었다」가 **구분되지 않는다.**
# 코드를 읽어야 하는 쪽은 **스크립트를 직접 부른다**:
#
#   cd frontend && node scripts/verify-final-build.mjs [--origin <https://host>] [--evidence <path>]
#
# **이 타깃은 사람이 보는 편의 진입점이다** — 출력을 눈으로 읽을 때 쓰고,
# 판정을 기계가 집계할 때는 위의 직접 호출을 쓴다.
```

**발주가 물은 네 가지가 모두 있다**: ① 자동화 금지 경고 ② **0/1/2/3 의 뜻** ③ **정확한 직접 호출
명령**(`cd frontend && node scripts/verify-final-build.mjs …`) ④ **make 는 편의 진입점**.

- **타깃을 없애지 않은 판단이 옳다** — 「사람이 눈으로 읽을 때는 이게 편하고, 없애면 그 편의만 잃는다」.
- **레시피·`.PHONY` 불변 확인**: 레시피 3줄(`157-159`)이 직전 검수 때 본 것과 **문자열까지 동일**하고,
  `.PHONY` 에 `shell-verify shell-verify-strict shell-build-fixture shell-final-preflight` 가 그대로다.
  줄 번호만 주석 11줄만큼 밀렸다.
- 같은 안내가 `tauri-p8-preflight-fix-report.md` **§4-2**(신설)와 `tauri-p8-w1-fix-report.md` **§4-1**
  에도 실렸다 — 표 형태(스크립트 1·2·3 ↔ make 2·2·2)까지 같다.

### 3-2. 관문·설정·제품·Release 무변경 (PASS)

| 확인 | 결과 |
|---|---|
| **이번 회차 변경** | `Makefile`(**주석 11줄만**) + 보고서 둘. mtime: Makefile **00:14:20** · 두 보고서 **00:14:44 / 00:15:35** |
| **관문 스크립트** | mtime **00:07:23** — **직전 회차**의 것이다(그 회차 보고서 작성 00:07). **이번 회차 무편집이라는 §8 의 주장이 맞다** |
| 스크립트 내용 | EXIT 상수 `73-77` · `rehearsal` `81` · `M-5 — 앱 재시작 후 로그인(쿠키) 유지` `284` · `exit(EXIT_REHEARSAL)` `455` · **458줄** — **직전 검수 때와 동일** |
| `frontend/src-tauri/**` | `git diff --stat` **0줄** — `operationalOrigin` 여전히 `null`, capability 여전히 `.invalid` |
| 제품 프론트 · backend · 인프라 레포 | 무변경 |
| 태그 · CI · Release · push · 설치 | `git tag --points-at HEAD` **없음** · `.github/workflows` **없음** · HEAD **`a1f6791`** |
| 임시 파일 잔재 | §3 의 합성 타깃 실험용 `mk1` 등이 **저장소에 남아 있지 않다**(확인) |

### 3-3. 미결 유지 (PASS)

두 보고서의 미결 표가 **같은 내용으로 유지**된다:

| 항목 | 상태 |
|---|---|
| **운영 origin**(OQ-T02) | **미정** — `null` · `.invalid` 자리표시 · **발명하지 않았다** |
| **D-4** PRODUCTION 로그인 수단 | **미결** — 세션 발급 경로 없음 |
| **운영 서버 실재** | **미확인** — 관문은 네트워크를 건드리지 않는다 |
| **M-1** · **M-5**(재시작 후 쿠키 유지) · **M-10** | **미측정** |
| **Windows 축 전부** | **검증 불가(이 기기)** — macOS 결과로 대체하지 않는다 |
| macOS `.icns` | **없음** — `shell-verify SHELL_STRICT=1` 이 잡는다 |

### 3-4. W-2 잔여 우회 (PASS — 요구보다 더 분명해졌다)

`tauri-p8-w1-fix-report.md` **§6** 이 직전 회차의 각주를 **독립 절로 승격**했다:

- **무엇이 가능한가**: 「스크립트를 다른 디렉터리로 복사해 그 옆에 손댄 `src-tauri` 를 두면 …
  **0** 이 나온다. 직전 회차의 V-6 이 그 방법으로 통과 분기가 죽지 않았음을 보였다 —
  **같은 방법이 곧 우회 경로다.**」
- **왜 막지 않는가**를 둘로 적었다: ① **막을 수 없다**(검사가 검사 대상과 같은 파일 안에 있다)
  ② **막으면 통과 분기를 확인할 방법이 사라진다**(Phase 7 `.icns` 함정)
- **보장 범위를 문장으로 못 박았다**:
  > **막는 것**: 실수 · 자동화의 오집계 · 자리표시가 조용히 구워지는 것.
  > **막지 못하는 것**: **의도적 우회**. 그것은 관문이 아니라 **리뷰와 서명**의 몫이다.

→ **관문이 자기가 못 하는 일을 한다고 말하지 않는다.** 검수 판단으로도 이 서술이 정확하다
(직전 회차 V-6 에서 내가 같은 방법으로 `exit 0`·`rehearsal:false` 를 재현했다).

### 3-5. N-2 (PASS)

두 보고서 모두 미결 표에 **이월**로 남겼다 — 「이번 회차는 `frontend/src` 를 건드리지 않았고
**통과로 만들려 재실행하지 않았다**」. 이번 회차가 **문서·주석만** 바꿨으므로 접점이 없다는
설명도 사실과 맞다.

---

## 4. WARN (2건 · 둘 다 낮음)

| # | 내용 | 좌표 | 수정 방향 |
|---|---|---|---|
| **W-1** | **맺음말 한 줄이 같은 문서의 §4-2 와 모순된다.** `tauri-p8-preflight-fix-report.md` **L174** 는 「**현재 이 저장소에서 `make shell-final-preflight` 는 여전히 exit 1 이다**」로 끝나는데, **이번 회차에 그 문서에 삽입된 §4-2 가** 「make 는 1·2·3 을 전부 **2** 로 감싼다」고 적었고 `w1-fix-report` X-2 도 `make → 2` 로 실측했다. **§4-2 를 넣으면서 맺음말을 함께 고치지 않았다.** 새 문서(`w1-fix-report` L149)는 「**`node scripts/verify-final-build.mjs` 는 여전히 exit 1**」로 **정확히** 적었다 — 즉 옛 문서의 한 줄만 낡았다. **Phase 7 N-1 과 같은 결의 잔재**다 | `tauri-p8-preflight-fix-report.md:174` | 「`make shell-final-preflight`」 → 「**`node scripts/verify-final-build.mjs`**」 (또는 「make 로는 2 로 보인다」를 덧붙인다) |
| **W-2** | **§3 의 make 실측이 «합성된 전사»로 보인다.** `w1-fix-report` §3 은 `$ printf … ; make -f mk1 probe1 ; echo $?` 아래에 실제 make 출력 대신 **요약 세 줄**(`레시피 exit 1 → make exit 2` …)을 붙였다. **사실 자체는 맞다** — 검수가 직전 회차에 `make` 경유 1·2·3 → 전부 2 를 독립 측정했다. 다만 이 저장소는 「관측을 그대로 적는다」를 지켜 왔고, `$` 프롬프트 형식은 **literal 출력**으로 읽힌다 | `tauri-p8-w1-fix-report.md` §3 | 실제 출력을 붙이거나, `$` 프롬프트를 빼고 **요약임을 밝힌 표**로 둔다 |

> 둘 다 **안내의 실질**(자동화는 직접 호출)을 해치지 않는다. Makefile 주석·§4-2·§4-1 은 전부 정확하다.

---

## 5. 차단·미결 (이번 회차가 만든 것 아님 — 유지)

| # | 항목 | 주인 |
|---|---|---|
| 1 | **운영 origin**(OQ-T02) — 미정. 관문 G1·G2·G3 가 여기서 멈춘다 | 사용자·코디 |
| 2 | **D-4** — PRODUCTION 세션 발급 수단 부재 | 코디·사용자(OQ-W05) |
| 3 | **운영 서버 실재** · **M-1** · **M-5** · **M-10** | 사용자(실기) |
| 4 | **Windows 축 전부** | OQ-W01 |
| 5 | macOS **`.icns`** | Phase 7 후속/8 |
| 6 | **N-2 프론트 스위트 불안정** | 코디 |
| 7 | **W-2 의도적 우회** — 관문 밖의 문제. **CI 를 걸 때** 스크립트 경로 고정 + `git status --porcelain frontend/src-tauri` 확인으로 덮는다 | 코디 |

---

## 6. 한 줄 정리

**「관문의 결함이 아니라 안내의 결함」이라는 진단이 옳고, 고친 자리도 정확하다** —
Makefile 주석이 0/1/2/3 과 직접 호출 명령을 적고 make 를 편의 진입점으로 자리매김했으며,
관문 스크립트는 mtime·구조·줄 수까지 직전 검수와 동일하다. 남은 것은 **옛 보고서 맺음말
한 줄(W-1)** 과 **§3 전사 형식(W-2)** 뿐이고, **미결 · W-2 우회 한계 · N-2 는 모두 그대로 드러나 있다.**
