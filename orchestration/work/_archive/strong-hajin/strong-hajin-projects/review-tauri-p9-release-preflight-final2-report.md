# WORK-006 Phase 9 최종2 — 독립 검수

- **코드 워크트리**: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects` · HEAD `a1f6791`(변동 없음)
- **검수 대상**: `verify-release-artifact.mjs`(**361 → 367줄**) 헤더·로직 · `Makefile` `shell-release-preflight` ·
  Phase 9 보고서 **넷**(본편 · fix · fix2 · **fix3**)
- **read-only 검수.** 설정 · 제품 · 빌드 · Release · 태그 · push · 서명 **무접촉.**
- **이 검수 보고서 한 파일만 작성.**

---

## 0. 종합 판정 — **FAIL 0 · WARN 1(낮음) · 나머지 PASS**

**직전 회차에 내가 올린 WARN 두 갈래가 모두 닫혔고, 한쪽은 지적보다 더 나아갔다.**

- **코드 5 설명**이 「파일」 시절 문장에서 **실제 세 갈래**(누락 · **파일/디렉터리 추가** ·
  **종류 불일치**)로 넓어졌다.
- **「이 관문은 `1` 을 판정으로 쓰지 않는다」** 가 헤더에 들어갔고, **내가 제안하지 않은 단서**를
  스스로 덧댔다 — 「**예상 못 한 예외로 죽으면 node 자신이 1 로 끝낸다. 그때의 `1` 은 판정이 아니라
  «사고»다.**」 **「1 을 쓰지 않는다」만 적었으면 거짓이 되는 경우**를 스스로 찾아 막았다.

**동작은 한 글자도 바뀌지 않았다** — 종료 코드 상수 여덟 개와 핵심 로직 세 자리가 **정확히 +6줄씩
이동**했을 뿐이고(헤더가 6줄 늘었다), `node --check` 통과, 쓰기·실행·네트워크 API **0건**.

**여덟 코드를 전부 다시 돌려 헤더·Makefile·동작 셋이 일치함을 확인**했고, **현재 저장소는
여전히 2·7 로 막혀** 있다.

WARN 1 건은 **fix3 가 스스로 적은 구조적 위험**을 이월한 것이다.

---

## 1. 판정표

| # | 발주 확인 항목 | 판정 |
|---|---|---|
| 1 | 코드 0/2/3/4/5/6/7/8 정의가 **헤더 · Makefile · 동작**에서 일치 | **PASS** |
| 2 | 정상 fixture만 0 | **PASS** |
| 3 | 누락 / 추가 파일 / 추가 디렉터리 / 종류 불일치 = **5** | **PASS** (네 갈래 모두) |
| 4 | 해시 불일치 = **6** · 조합 = **7** · 해시 없는 기록 자산 = **8** | **PASS** |
| 5 | 현재 저장소 nonzero | **PASS** (2 · 7) |
| 6 | 설정·제품·빌드·Release·태그·push·서명 무변경 | **PASS** |
| 7 | 미결(D-4 · origin · M-1 · M-5 · M-10 · Windows) 유지 | **PASS** |

---

## 2. FAIL

**없다.**

---

## 3. 코드 정의 ↔ 동작 — **11건 전수 재현**

fixture 는 **스크래치패드**에만 만들고 지웠다(조합 대조가 지금 트리를 읽으므로
**스크립트 사본 + 조합이 맞는 `src-tauri` 사본**에서 돌렸다).

| 헤더가 말하는 것 | Makefile | **실측 exit** |
|---|---|---|
| **0** 동일하다 | 0 동일 | **0** (정상) |
| **2** 돌릴 수 없다 | 2 돌릴 수 없음 | **2** (입력 없음) |
| **3** 검증되지 않은 manifest | 3 미검증 manifest | **3** (rehearsal) |
| **4** 아티팩트가 없다 | 4 아티팩트 없음 | **4** (`artifacts: []`) |
| **5** 집합 불일치 — **누락 · 파일/디렉터리 추가 · 종류 다름** | 5 집합 불일치 | **5 · 5 · 5 · 5** — **네 갈래 모두**(누락 / 추가 파일 / **추가 디렉터리** / **종류 불일치**) |
| **6** 해시 불일치 | 6 해시 불일치 | **6** |
| **7** 조합 불일치 | 7 조합 불일치 | **7** |
| **8** 대조 불가 자산 | 8 대조 불가 자산 — …「해시가 없다」를 「같다」로 바꾸지 않는다 | **8** (기록된 `.app` 이 candidate 에 있다) |
| **1 은 판정으로 쓰지 않는다** | — | `process.exit(1)`·`EXIT_*=1` **0건**(grep) |

→ **세 자리가 번호·뜻·실제 동작에서 어긋나지 않는다.** 발주 항목 ①~④ 가 모두 성립한다.

### 3-1. 코드 5 가 넓어진 것이 **표에만 그치지 않았다**

직전 검수에서 「표는 «파일»만 말한다」고 지적한 자리다. 이번에 **표 문구가 실제 범위로 넓어졌고**,
그 세 갈래가 **실측으로 전부 5** 다 — 문구와 동작이 같은 곳을 가리킨다.

### 3-2. 「1」 처리 — **지적보다 나은 답**

```
⚠ **이 관문은 `1` 을 판정으로 쓰지 않는다** — 내는 코드는 `0 · 2 · 3 · 4 · 5 · 6 · 7 · 8` 뿐이다.
Phase 8 관문이 쓰는 `1`(「관문이 막았다」)과 **겹치지 않게** 비워 둔 자리다 …
(다만 **예상 못 한 예외로 죽으면 node 자신이 1 로 끝낸다** — 그때의 `1` 은 판정이 아니라 **사고**다.)
```

- 내가 제안한 것은 **「1 을 쓰지 않는다」 한 줄**이었다. 구현은 **그 문장이 거짓이 되는 경우**
  (던져진 예외 → node 가 1)를 스스로 찾아 **「이 파일의 1 = 사고」**로 못박았다.
- **관문이 자기가 보장하지 못하는 것을 보장한다고 말하지 않는다** — 이 저장소가 여러 Phase에 걸쳐
  지켜 온 원칙과 같다.

---

## 4. 「헤더 주석만」 검증 (PASS)

| 확인 | 결과 |
|---|---|
| 줄 수 | 361 → **367**(+6, 헤더 증가분과 일치) |
| **종료 코드 상수 8개** | `58-66` → **`64-72`** — **값·순서 그대로, 정확히 +6 이동** |
| **핵심 로직 3자리** | `present` 229→**235** · `wrongKind` 238→**244** · `extra` 239→**245** — **전부 +6** |
| 구문 | `node --check` **통과** |
| 쓰기·실행·네트워크 | `writeFile`·`mkdir`·`exec`·`spawn`·`fetch` **0건** |

→ **실행 로직 0줄 변경**이라는 fix3 §3 의 주장이 **줄 이동량으로 재확인**된다.

---

## 5. 현재 저장소 (PASS)

| 실행 | **exit** |
|---|---|
| `node scripts/verify-release-artifact.mjs` | **2** |
| `… --manifest <정상 fixture> --candidate <dir>` | **7** (`origin … 트리=null`) |
| `make shell-release-preflight` | **2** (헤더·주석이 경고한 «뭉개짐») |

**저장소 현재값**: `"operationalOrigin": null` · capability `"https://operational-origin-not-yet-decided.invalid/*"`.
**Phase 8 이 아직 exit 0 이 아니고 아무것도 굽지 않았으므로 nonzero 가 옳다.**

---

## 6. 무변경 · 미결 (PASS)

| 대상 | 확인 |
|---|---|
| **fix3 회차 변경** | `verify-release-artifact.mjs` **하나**(mtime **00:45:18**) + 보고서 |
| **Makefile** | **무편집** — mtime **00:39:37**(fix2 회차) |
| **설정** | `shell.config.json` **20:22:51** · `product-shell.json` **20:26:14** — **Phase 3 이후 그대로** |
| `frontend/src-tauri/**` | `git diff --stat` **0줄** |
| 태그 · CI · Release · push · 서명 · 빌드 | `git tag --points-at HEAD` **없음** · `.github/workflows` **없음** · HEAD **`a1f6791`** |
| 00:46 이후 변경 | **0건** |
| **미결 표기** | **네 보고서 전부**에 D-4 · 운영 origin(OQ-T02) · M-1 · M-5 · M-10 · **Windows 미검증** · N-2 이월 · 잔여 우회가 present(기계로 셈) |

- **M-10** 은 「**이 관문의 통과를 설치 성공으로 쓰지 않는다**」로 못박혀 있다.
- **Windows** 는 통과 화면에서도 「미검증」으로 적힌다.

---

## 7. WARN (1건 · 낮음 — 이월)

### W-1 — **코드 목록이 두 곳에 손으로 복제돼 있다.** 이번 Phase 에서 **두 번 어긋났다**

**fix3 §4-3 이 스스로 적었다**: 「**두 곳을 손으로 맞춰야 하는 구조**라는 사실은 적어 둔다 —
이번엔 어긋나지 않았지만, 코드가 또 넓어지면 **양쪽을 함께 봐야 한다.**」

**검수 의견: 이 위험은 «가설»이 아니라 이미 두 번 실현됐다.**

| 회차 | 어긋난 자리 | 내가 올린 WARN |
|---|---|---|
| fix(코드 8 신설) | **Makefile 주석**이 7 에서 끊김 | Phase 9 fix 검수 W-1 |
| fix2(코드 5 확대) | **스크립트 헤더 표**가 「파일」 시절 문구 | Phase 9 최종 검수 W-1 |

- 두 번 다 **동작은 옳고 설명만 낡았다** — 그래서 매번 WARN 이었고 FAIL 이 아니었다.
- 그러나 **세 번째가 없으리라는 근거는 없다.** 코드가 또 늘면 같은 일이 반복된다.
- 지금은 **Makefile · 헤더 표 · 상수 정의 셋이 일치**한다(§3에서 확인) — **이월 사항이지 현재 결함이 아니다.**

**수정 방향**(택일)
1. **기계로 묶는다** — 상수 정의(`EXIT_* = n`)를 읽어 헤더 표·Makefile 주석에 같은 번호 집합이
   있는지 보는 **작은 self-check**(예: `--explain-codes` 가 상수에서 표를 생성해 찍고, 사람이 대조)
2. **한 곳으로 줄인다** — Makefile 주석은 번호를 나열하지 않고 「코드 정의는 스크립트 헤더를 보라」로만

> **FAIL 이 아닌 이유**: 현재 셋이 일치하고 동작도 정확하다. 이것은 **다음 변경을 위한 대비**다.

---

## 8. 차단·미결 (이번 회차가 만든 것 아님 — 유지)

| # | 항목 | 주인 |
|---|---|---|
| 1 | **운영 origin**(OQ-T02) 미정 → Phase 8 이 exit 0 이 아니고 **쓸 수 있는 manifest 가 없다** | 사용자·코디 |
| 2 | **D-4** — PRODUCTION 세션 발급 수단 부재 | 코디·사용자(OQ-W05) |
| 3 | **M-1 · M-5 · M-10** 미측정 · **Windows 자산 전부 미검증** | 사용자 · OQ-W01 |
| 4 | **서명 · 공증 · 업데이트** — 범위 밖 | Phase 10+ |
| 5 | **N-2 스위트 불안정** · macOS **`.icns`** | 코디 · Phase 7 후속 |
| 6 | **의도적 우회**(스크립트 이동) — Phase 8 §6 · Phase 9 §6-1 과 동일 | 코디(리뷰·서명의 몫) |

---

## 9. 한 줄 정리

**지적 두 갈래가 닫혔고, 「1」 쪽은 내가 제안한 것보다 정확해졌다**(node 자신의 1 = 사고라는 단서).
헤더만 +6줄 늘고 상수·로직은 **정확히 +6 이동**해 동작 불변이 줄 번호로 확인되며,
여덟 코드가 **헤더·Makefile·실측 셋에서 일치**하고 현재 저장소는 **2·7 로 옳게 막혀** 있다.
남은 것은 **코드 목록 이중 관리라는 이월 위험(W-1)** 뿐이고, **Phase 9 에 기능적 지적은 없다.**
