# WORK-006 Phase 5 — 2차 측정 기록 (제품 배선판, R2)

> **이 문서는 「이렇게 관측했다」만 적는다.** 오늘 자동·정적으로 확인한 것과 **내일 실기가
> 필요한 것**을 섞지 않는다. **M-1 · Windows · 운영 origin · 실제 절전/마이크/서버 E2E 를
> 통과로 쓰지 않는다.**
>
> **이 기록은 실기 설치 검증의 완료를 뜻하지 않는다.**

- **측정 시각**: 2026-09-22 21:20 ~ 21:35 KST
- **코드 워크트리**: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects`
  · 브랜치 `kknaksss/strong-hajin-projects` · **HEAD `a1f6791`** · Phase 4 변경이 워킹트리에 얹힌 상태
- **1차 기록(fixture)**: `tauri-measurement-r1.md` — **섞지 않는다.** 인용할 때는 회차를 밝힌다
- **이번 회차 코드 변경**: **0줄** (검증·기록만. 발주 「Phase 5 에서 임의로 고쳐 통과 처리하지 않는다」)

---

## 1. 실행한 명령과 결과

| # | 명령 | 결과 | 판정 |
|---|---|---|---|
| R-1 | `npx tsc --noEmit` | **exit 0 · 출력 0줄** | **성공** |
| R-2 | `npx vitest run src/lib/shell.test.ts src/features/browser/ src/features/meetings/` | **215 passed (215)** | **성공** |
| R-3 | `make frontend-test` × 4 | run1 `1053 passed` · run2 **`1 failed / 1052`** · run3 `1053` · run4 `1053` | **3/4 성공 · 1회 기준선 불안정**(§1-1) |
| R-4 | `make frontend-assets` | exit 0 — `assets: 9 · verified` | **성공** |
| R-5 | `make frontend-build` | exit 0 — `✓ built in 1.45s` | **성공** |
| R-6 | `cargo check --all-targets` (제품 셸) | **exit 0 · warning 0 · error 0** | **성공** |
| R-7 | `cargo test` (제품 셸) | **45 passed · 0 failed** | **성공** |
| R-8 | `cargo clippy --all-targets -- -D warnings` | **통과** | **성공** |
| R-9 | `cargo check --locked --target x86_64-pc-windows-msvc` | ❌ `NotAttempted("llvm-rc")` | **차단(환경)** — §5 |

### 1-1. R-3 의 1회 실패는 «기준선 불안정»이다 — 새 실패가 아니다

- 실패 파일: `src/features/work/Checklist.test.tsx` — **Phase 4 배선과 접점이 없다**
- 배선이 닿는 테스트(R-2, 215건)는 **전건 통과**
- 같은 현상을 앞선 회차에서 **기준선(Phase 4 변경을 되돌린 트리)으로 4회 중 2회 실패**로
  측정해 두었고, 실패 파일이 매번 다르다(`CreateWork` · `Checklist` · `App` · `CalendarPage` ·
  `ProjectPage` · `ActionCenter` · **`MeetingMaterials`**) — 전부 「불러오는 중」에서 요소를
  못 찾는 모양이다
- **`MeetingMaterials.test.tsx` 는 배선이 닿는 파일인데도**, **격리 실행(R-2 의
  `src/features/meetings/` 15건 포함)에서는 통과했다.** 즉 이 파일의 실패도
  **전체 스위트를 함께 돌릴 때만** 나타나는 같은 불안정이고, 배선의 결함이 아니다

> ⚠ **3/4 를 «안정성 성적»으로 읽지 말 것 — 표본이 흔들린다.**
> 이 문서의 4회는 `1 failed` 1회(3/4 통과)였지만, **검수 재현에서는 3회 중 2회가 실패(2/3)**
> 했다. 앞선 회차의 기준선 측정도 4회 중 2회 실패였다. **회차마다 빈도가 크게 달라지므로
> 어느 쪽 숫자도 「이 정도로 안정적이다」의 근거가 되지 않는다** — 표본이 적고 실패가
> 산발적이라는 사실만 말한다.

- → **기준선 대비 새 실패 0**(결론 유지). 다만 **불안정 자체는 여전히 있고, 빈도는 회차마다
  다르다**(§6-4)

### 1-2. `make verify` 는 돌리지 않았다

WORK Phase 5 의 검증 항목에 `make verify` 가 있으나, 그것은 **백엔드 전체 스위트**(`test`
`test-scale` `test-release`)를 포함한다. 이번 발주의 허용 범위는 「tsc · 관련 테스트 · **전체
프론트** 검증」이라 **프론트 쪽 구성요소만**(R-3 · R-4 · R-5) 실행했다.
**백엔드 축은 이번 회차에서 «미실행»이고, 통과로 쓰지 않는다.**

---

## 2. 인수조건 21건 — 증거 상태

**범례**: ✅ 자동/정적 증거로 닫힘 · 🟡 코드 증거만(실기 필요) · ⬜ **미측정 / 내일 실기 필요** ·
🚫 이번 회차에서 닫을 수 없음(환경 차단)

| AC | 무엇 | 증거 위치 | 상태 |
|---|---|---|---|
| **AC-T01** | 마이크 열린 뒤 자동 절전 시간을 넘겨도 안 잠든다 | `power.rs:180` `PreventUserIdleSystemSleep` · `stream.ts` 마이크 성공 직후 acquire | ⬜ **미측정 / 내일 실기** — 제품 녹음 위 관측 필요(M-9·M-11) |
| **AC-T02** | 화면은 평소대로 **꺼진다** | `power.rs:178,313` — `NoDisplaySleep`·`ES_DISPLAY_REQUIRED` **미사용**(정적 확인) | 🟡 코드상 성립 · ⬜ 실기 |
| **AC-T03** | 수동 잠자기·덮개 닫기는 **막지 않는다** | 같은 자리 — `PreventSystemSleep`·`ES_AWAYMODE_REQUIRED` 미사용 | 🟡 코드상 성립 · ⬜ 실기 |
| **AC-T04** | 회의가 끝나면 평소 절전으로 돌아온다 | `stream.ts` L-02/L-05/L-10 해제 · `cargo test` 해제 시험 | 🟡 · ⬜ 실기 |
| **AC-T05** | 창 숨김·최소화로 `max(3T,30분)` 넘겨도 점유 유지 | — | ⬜ **미측정** (M-11) |
| **AC-T06** | 화면 꺼진 채 같은 관찰 시간 | — | ⬜ **미측정** (M-11) |
| **AC-T07** | **두 녹음 화면 각각** T05·T06 성립 | 배선은 두 경로 모두 있음(`stream.ts`·`BrowserRecordingPage.tsx`) | ⬜ **미측정** (M-11) |
| **AC-T19** | 수동 절전 복귀 후 재무장 / 끝났으면 해제 | `stream.ts:289` `visibilitychange` 재확인 · `power.rs` **재무장이 assertion 을 실제로 다시 만든다**(실행 통과) | 🟡 **재무장 기구는 실행 검증됨** · ⬜ 복귀 실기(M-12) |
| **AC-T26** | 앱 재시작 후 로그인 유지 | `lib.rs:379` `incognito(false)` | ⬜ **미측정** (M-5 제품 셸판) |
| **AC-T27** | 만료 뒤 **재접속·재실행**에서만 로그인 화면 | — | ⬜ **미측정** · ⚠ **인용할 Phase 2 기록이 없다**(§2-1) |
| **AC-T28** | 녹음 중 만료 시각이 지나도 녹음·점유 유지 | — | ⬜ **미측정** (M-2b 제품판) |
| **AC-T29** | 앱에서 회의·메모·안건·자료·업무가 브라우저와 같게 | 프론트 전체 1053건 통과(웹 동작 불변) | 🟡 **웹 회귀는 통과** · ⬜ **앱 안에서의 확인은 미측정** |
| **AC-T30** | 브라우저에서 열었을 때 지금과 완전히 같다 | `shell.test.ts` 「커맨드를 한 번도 부르지 않는다」(호출 0건) + 전체 스위트 | ✅ **자동 증거로 닫힘** |
| **AC-T32** | 녹음 중 창 닫기에 되묻기가 뜨고 즉시 안 닫힌다 | `BrowserRecordingPage.tsx:73` `beforeunload` | ⬜ **미측정** (M-6) |
| **AC-T36** | `계속 녹음` 을 고르면 창·녹음·**점유가 그대로** | `lib.rs:434` `CloseRequested` 에서 **아무것도 풀지 않음**(정적) | 🟡 · ⬜ 실기(M-13) |
| **AC-T37** | 막힌 이동에서 점유 유지 | `lib.rs` `on_navigation` 이 `false` 반환만 하고 정리 호출 없음(정적) | 🟡 · ⬜ 실기(M-13) |
| **AC-T38** | 되묻기가 **하나만** 뜬다 | 제품 셸에 `prevent_close` **없음**(정적 확인) → 되묻기는 웹 `beforeunload` 하나뿐 | 🟡 **코드상 하나** · ⬜ 실기(M-6) |
| **AC-T39** | `degraded` 에서 사건 기반 재무장으로 `on` 복귀 | `stream.ts` 재확인 배선 + `power.rs` 재무장 실행 시험 | 🟡 · ⬜ 실기(M-12) |
| **AC-T40** | 늦은 획득/정리 경합 뒤 **최종 잔존 0** | `shell.test.ts` 직렬화 3건 · `lib.rs` 16스레드 동시 획득·해제 시험(잔존 0) | ✅ **자동 증거로 닫힘**(웹·네이티브 양쪽) |
| **AC-T41** | 문서 교체 뒤 이전 문서의 획득이 새 점유를 만들지 않음 | `lib.rs:409` `PageLoadEvent::Started` → `clear_window` | 🟡 **정리는 있음** · ⚠ **네이티브 보상 수단 없음**(Phase 3 가 남긴 한계) · ⬜ 실기(M-14) |
| **AC-T43** | 해제 실패가 드러나고, 창을 닫으면 정리된다 | `shell.ts:55,174` 정리 실패 기록 · `shell.test.ts` E-14b 3건 · `lib.rs` `Destroyed` 정리 | 🟡 **웹 쪽 노출은 자동 검증** · ⬜ 실기(M-14) |

**집계**: ✅ **2** (AC-T30 · AC-T40) · 🟡 코드 증거만 **11** · ⬜ 완전 미측정 **8**
→ **21건 중 «실기 없이 닫힌 것은 2건»이다.** 나머지는 내일 실기가 필요하다.

### 2-1. ⚠ AC-T27 — 인용하라고 한 Phase 2 기록이 **존재하지 않는다**

WORK Phase 5 는 AC-T27 을 「**Phase 2 기록을 인용하고 인용임을 표시한다**」로 계획했다.
그러나 **1차 기록(`tauri-measurement-r1.md`)의 M-2b 는 「미측정」**이고, AC-T27 을 뒷받침하는
관측이 그 문서에 **없다**(해당 절은 막는 인수조건으로 `AC-T28` 만 적는다).

→ **인용할 것이 없으므로 AC-T27 을 「인용으로 닫힘」으로 쓰지 않는다.** 미측정으로 둔다.
**계획의 전제가 어긋난 자리**이므로 코디 판단이 필요하다(§7-2).

---

## 3. 제품판 측정표 — M-9 · M-2b · M-5 · M-6 · M-11 ~ M-14

**전부 실기(장비·서버·설치·사람의 조작)가 필요하다.** 오늘 자동으로 딸 수 있는 칸이 없다.

| M | 무엇 | macOS | Windows | 왜 오늘 못 재나 |
|---|---|---|---|---|
| **M-9**(제품판) | 절전 방지의 세 갈래를 **제품 녹음 위**에서 | ⬜ **미측정 / 내일 실기** | 🚫 **검증 불가(장비 없음)** | ① 마이크 승인이 필요(대행 금지) ② 이 기기는 **제3자가 이미 절전을 붙들고 있어 관측이 무효**(Phase 2 §0-2: Amphetamine·caffeinate·powerd) ③ `T` 를 의미 있는 값으로 두어야 한다 |
| **M-2b**(제품판) | 만료 넘겨 **녹음·점유 유지** | ⬜ **미측정** | 🚫 검증 불가 | 살아 있는 회의 스트림(마이크+로컬 스택)이 선행. 개발 DB 세션 행 조작은 가능하나 **대상이 없으면 무의미** |
| **M-5**(제품 셸판) | 앱 재시작 후 쿠키 유지 | ⬜ **미측정** | 🚫 검증 불가 | 앱을 완전히 끄고 다시 여는 왕복이 필요. **제품 셸은 탐침과 설정이 달라** Phase 1 기록을 쓸 수 없다 |
| **M-6**(제품판) | 되묻기가 **하나만** 뜨는가 | ⬜ **미측정** | 🚫 검증 불가 | 창 닫기 조작이 필요. 코드상 셸은 가로채지 않아 「하나」가 **예상**이지만 관측이 아니다 |
| **M-11** | 숨김·최소화·화면 꺼짐 **장시간**, 두 녹음 화면 각각 | ⬜ **미측정** | 🚫 검증 불가 | 관찰 시간 하한 `max(3T, 30분)`. M-9 와 **같은 회차에 병기**해야 한다 |
| **M-12** | 수동 절전 복귀와 재무장 | ⬜ **미측정** | 🚫 검증 불가 | 기기를 실제로 재우고 깨워야 한다 |
| **M-13** | **취소된 사건**에서 점유 보존(`계속 녹음` 뒤에도 안 잠듦) | ⬜ **미측정** | 🚫 검증 불가 | 되묻기 조작 + 절전 시간 초과 관찰 |
| **M-14** | 늦은 완료와 **정리 실패** — 최종 잔존 0 | ⬜ **미측정** | 🚫 검증 불가 | 실기에서의 잔존은 `pmset -g assertions` 로 본다. **단위 시험의 잔존 0(AC-T40)과 다른 층**이다 |

### 3-1. 오늘 딴 «인접 증거» — M 칸을 채우지는 않는다

아래는 실기 관측이 **아니고**, 위 표의 어떤 칸도 채우지 않는다. 다만 실기 때 무엇이 이미
코드로 서 있는지 가리킨다.

| 항목 | 오늘의 증거 | 한계 |
|---|---|---|
| 재무장 기구(M-12 의 절반) | `power.rs` 시험이 **진짜 IOKit 을 불러** 재무장 시 assertion id 가 바뀜을 확인(통과) | OS 가 실제로 절전을 막았는지는 **안 봤다** |
| 잔존 0(M-14 의 절반) | 웹 직렬화 3건 + 네이티브 16스레드 동시 시험 | **프로세스 안의 장부**를 본 것이지 OS 쪽 잔존이 아니다 |
| 되묻기 층(M-6 의 절반) | 제품 셸에 `prevent_close` 없음 → 웹 `beforeunload` 하나 | 웹 되묻기가 **앱 창 닫기에서 실제로 도는지**는 미관측 |

---

## 4. 제품 배선이 실기 준비 상태인가 — 오늘 확인한 것

| 확인 | 결과 |
|---|---|
| 타입 검사 | **통과**(0줄) |
| 배선이 닿는 테스트 215건 | **전건 통과** |
| 프론트 전체 1053건 | **3/4 통과 · 1회는 기준선 불안정**(§1-1) |
| 프론트 자산·빌드 | **통과** |
| 제품 셸 컴파일·시험·clippy | **통과**(45 tests · warning 0) |
| 웹↔셸 경계 | 셸 부재 시 **커맨드 호출 0건**(자동 증거) |

→ **실기에 들어갈 수 있는 상태다.** 단, 실기가 시작되기 전에는 **어떤 AC 도 실기 기반으로
닫히지 않았다.**

---

## 5. Windows 축 — 전 항목 **검증 불가**

```text
$ cargo check --locked --target x86_64-pc-windows-msvc
thread 'main' panicked at …/tauri-winres-0.3.6/src/lib.rs:543:14:
called `Result::unwrap()` on an `Err` value: NotAttempted("llvm-rc")
```

- **실행 미검증**이자 **컴파일 미검증**이다. 두 칸 모두 비어 있다
- 우리 소스의 오류가 아니라 **호스트에 `llvm-rc` 가 없어서**다. 타깃 자체는 설치돼 있다
- `#[cfg(windows)]` 코드 — `power::windows`(W-5 수정 포함) · `allow_microphone_on_webview2` —
  는 macOS 의 `cargo check` 가 **한 줄도 보지 않는다.** R-6 의 exit 0 은 Windows 증거가 아니다
- **macOS 결과로 Windows 를 대신 쓰지 않는다**(OQ-W01)

---

## 6. Phase 4 이월 WARN 추적

| # | 항목 | 지금 상태 | 이번 회차에서 한 일 |
|---|---|---|---|
| **6-1** | **화면 배선 회귀 시험 부재**(R-5) | **미해소.** `stream.ts`·두 화면의 배선(마이크 열린 뒤에만 acquire · 구독/거부 미획득 · 회차 키 교체 · **StrictMode `mounted` 복원**)은 여전히 **코드 검토 + tsc** 증거뿐 | 파일 추가가 이번 허용 범위 밖이라 **만들지 않았다.** §7-1 로 올린다 |
| **6-2** | **`open_external` 실패가 사용자에게 무반응**(R-3) | **미해소.** 반환값 `"failed"` + `console.warn` 뿐, 화면 표시는 없다 | SPEC 판단 사항이라 **임의로 UI 를 더하지 않았다** |
| **6-3** | **M-1 미측정** | **미해소.** 19:54 재측정도 동일(CA 가 어느 키체인·신뢰 도메인에도 없었다) | 이번 회차에서 **재시도하지 않았다** — 신뢰 설치는 사용자 조작이고 발주 범위 밖이다 |
| **6-4** | 스위트 전반의 시간 의존 불안정 | **여전히 있다.** 이번 4회 중 1회 발생 | 기준선 대비 **새 실패 0** 임을 §1-1 로 확인 |
| **6-5** | AC-T41 의 네이티브 보상 수단 없음(Phase 3) | **미해소.** 문서 교체 시 정리는 있으나, 「교체 직전에 날아간 획득」을 네이티브가 가를 수단이 없다 | M-14 에서 실측할 항목으로 남긴다 |

---

## 7. Phase 6a 로 넘길 차단·미결

| # | 항목 | 성격 | 막는 것 |
|---|---|---|---|
| **1** | **화면 배선 회귀 시험**을 만들 파일 허용 | 코디 판단 | 21건 중 🟡 11건의 **코드 증거**가 시험으로 고정되지 않는다. **R-1(StrictMode) 같은 회귀가 재유입 가능** |
| **2** | **AC-T27 의 인용 전제가 어긋났다**(§2-1) | **계획 정정 필요** | AC-T27 을 어떻게 닫을지 — 실기로 재든지, WORK 의 「Phase 2 인용」 문장을 고치든지 |
| **3** | **실기 환경 준비** — ① 마이크 승인 ② **제3자 절전 도구 해제 + `T` 설정** ③ 앱 재시작 왕복 | 사용자 | M-9 · M-11 ~ M-14 · M-5 · M-6 · M-2b **전부** |
| **4** | **CA 신뢰 설치**(M-1) | 사용자 1회 | M-1 · 그리고 M-1 에 매인 인수조건의 최종 통과 |
| **5** | **Windows 실기 + `llvm-rc`** | 코디·장비 | Windows 축 **전부**(컴파일 포함) |
| **6** | **운영 origin 확정**(OQ-T02) | 사용자·코디 | 제품 셸의 `shell.config.json`·capability 두 자리. 지금은 자리표시 |
| **7** | `open_external` 실패의 사용자 표시 여부 | SPEC 판단 | 6-2 |
| **8** | 백엔드 축(`make verify` 전체) 미실행 | 범위 | WORK Phase 5 의 「`make verify` 새 실패 0」 항목 |

---

## 8. 이번 회차에서 하지 않은 것

- **코드 수정 0줄.** 자동검증으로 재현된 **Phase 4 범위의 새 결함이 없었다** —
  고칠 것이 없어 고치지 않았다(발주: 「임의로 고쳐 통과 처리하지 않는다」)
- M-1 재측정 · CA 신뢰 설치 · 마이크 승인 · 절전 설정 변경 · Windows 실기 조작
- 설치파일 발행 · 운영 서버 배포 · 비밀값 열람
- 커밋 · push · PR · Release
- **문서 1개만 썼다**: `tauri-measurement-r2.md`

---

## 9. 한 줄 정리

**자동·정적으로 딸 수 있는 것은 전부 녹색이다**(tsc · 215 · 1053 · 자산 · 빌드 · 셸 45 · clippy).
그러나 **인수조건 21건 중 실기 없이 닫힌 것은 2건뿐**(AC-T30 · AC-T40)이고,
**제품판 측정 8칸은 전부 「미측정 / 내일 실기 필요」**다. Windows 는 컴파일부터 막혀 있다.
**이 기록은 실기 설치 검증의 완료가 아니다.**
