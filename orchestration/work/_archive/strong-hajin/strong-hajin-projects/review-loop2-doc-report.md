# 리뷰 리포트 — strong-hajin-projects / 루프2 문서 검수 (2026-09-22)

## 판정: **FAIL 6 · WARN 4**

**계약 자체는 백로그 11건과 어긋나지 않는다.** 사용자 지시는 열한 건 전부 결정→계약→Phase 로
내려왔고, 인수조건 48줄 전수 배정도 **내가 다시 세어 맞다.** FAIL 은 다른 데서 난다 —
**코드 사실 하나가 틀렸고**(`external_key`), **뒤집힌 D-12 가 일곱 자리에 옛 뜻으로 남았고**,
**선례에서 물려받을 규칙 하나와 게이트 값 하나가 빠졌으며**, **그 게이트를 읽을 자리가
파일 목록 밖(`App.tsx`)에 있다.**

## 검수 범위

- 대상: `10-decision/decision-004-projects.md`(1215줄) · `20-spec/spec-005-projects.md`(1636줄) ·
  `30-work/work-005-projects.md`(1774줄). **셋 다 untracked 라 diff 가 없다** — 전문을 읽었다.
- 기준: `loop2-backlog.md`(사용자 지시 11건 정본) · `loop2-survey-report.md`(범위·비용) ·
  1루프 산출물.
- 실행한 검사 — **읽기만.** 문서·코드 수정 0건, 테스트·서버 0건.
  - 코드 read-only: `/Users/kknaks/orca/workspaces/Strong_hajin/strong-hajin-projects`
  - `grep`: `access` / `read_only` / `may_assign_in` / `PROJECT_MANAGE` / `CreateProjectRequest` /
    `canManageOwnTasks` / `allowedTaskTransitions` / `external_key`
  - 인수조건 줄 수는 `awk`·`grep -c` 로 직접 셈
- **확인 안 함**: `scripts/lint-pipeline.py` — **이 리뉴얼 레포에 `scripts/` 자체가 없다.**
  린트를 돌릴 수 없어 frontmatter·자리 규칙은 눈으로만 봤다(위반 없음).

---

## 위반 (FAIL)

### [FAIL-1] `POST /api/projects` 는 `external_key` 를 **받는다** — 계약이 반대로 적혔다

- **무엇이**: SPEC 이 「`external_key` 는 API 가 안 받는다」를 **코드 사실로** 네 자리에 적고,
  그 위에 인수조건 L-19 와 WORK 작업 3 을 세웠다.
- **파일:줄**
  - `20-spec/spec-005-projects.md:653-654` 「**API 가 그 필드를 받지 않는다** — 데이터셋
    가져오기 전용이다. **화면에 칸을 만들면 보낼 곳이 없다**」
  - `20-spec/spec-005-projects.md:1034` Validation 「**`external_key` 는 받지 않는다**」
  - `20-spec/spec-005-projects.md:1113` Data Contract 「**`external_key` 는 안 받는다**」
  - `30-work/work-005-projects.md:1261` 「**`external_key` 칸을 만들지 마라** — **API 가 안 받는다**」
- **무엇과 어긋나나 — 코드**
  - `entrypoints/http.py:79` `ProjectCreateInput as CreateProjectRequest`
  - `entrypoints/http.py:1389-1399` `create_project(request: CreateProjectRequest ...)` 가
    **`external_key=request.external_key` 를 그대로 넘긴다**
  - `modules/work/project_commands.py:23` `external_key: str | None = Field(default=None, max_length=200)`
  - 즉 **입력 모델에 있는 필드**이고 「모르는 필드」가 아니다(`extra="forbid"` 와 무관).
  - **조사 리포트는 맞게 적었다** — `loop2-survey-report.md:258` 이 `external_key` 를
    **API 입력 표의 한 행**으로 세고 중복 거절까지 적었다. `:273` 이 말한 「네 칸」은
    **FE 래퍼**(`frontend/src/lib/api.ts:805-812`)의 시그니처다.
    **작성자가 그 둘을 바꿔 읽었다** (백로그 B-1 정정 절도 같은 오독을 갖고 있다).
- **파생 피해 하나 더**: `spec-005:1080` 「프로젝트 만들기의 거절은 **기존 세 갈래**(이름 · 기간 역전 ·
  자격)」 — **넷이다.** `modules/work/projects.py:162-163` 이 `external_key` 중복에
  `ProjectError("이미 있는 프로젝트 key입니다")` 를 낸다(조사 `:258` 도 적었다).
- **한 줄 수정안**: 근거를 「API 가 안 받는다」→「**FE api 래퍼가 네 칸만 보내고,
  `external_key` 는 데이터셋 import 가 쓰는 값이라 화면 칸을 두지 않는다**」로 바꾸고,
  §4 에러의 「세 갈래」를 **네 갈래**로 고친다. **결정(칸을 안 둔다) 자체는 그대로 선다.**

### [FAIL-2] 뒤집힌 ~~D-12~~ 가 **일곱 자리에 옛 뜻 그대로** 남아 있다

지시 §2 는 「한 곳이라도 옛 뜻으로 남아 있으면 FAIL」이다. 본문 취소선·사유·대체(D-28)는
**온전하고 본문도 안 지워졌다** — 그러나 **그 번호를 가리키는 자리**가 다 안 따라왔다.

| # | 파일:줄 | 남아 있는 옛 뜻 |
|---|---|---|
| ① | `20-spec:1505` | 추적표 행 `| **D-12** | 열쇠는 배정 권한이다 | …` — **취소선이 없다.** 같은 절 `:1526` 이 「뒤집힘」이라 적어 **한 섹션 안에서 정면으로 모순** |
| ② | `20-spec:170` | §1 In scope 「자동 초대 … **(확정 — D-11·D-12)**」 — 지시가 콕 집은 「In Scope」다 |
| ③ | `20-spec:775` | §4 API Contract 표 「자동 초대 한 겹이 붙는다 **(확정 — D-11·D-12)**」 |
| ④ | `20-spec:1118` | §4 Data Contract 「새 오류 코드 \| 0건 \| **(도출 — D-12·D-14·D-19)**」. ⚠ **`:1114` 에 같은 이름의 행이 하나 더 있다**(「새 오류 코드 (2루프) \| 0건 \| (도출 — D-28·D-29·D-30)」) — **한 표에 같은 항목 두 행** |
| ⑤ | `20-spec:1257` · `:1349` | §6 인수조건 「(D-11·D-12)」 · 묶음 제목 「**자동 초대 (D-11·D-12)**」 |
| ⑥ | `10-decision:1214` | **Resulting Spec** 「조문으로 내려야 할 자리 … **자동 초대의 권한 경로(D-12)**」 — D-28 은 **권한 경로가 없다**고 정했다. 지시가 콕 집은 「Resulting Spec」이다 |
| ⑦ | `10-decision:955` | § 기각 「자동 초대의 열쇠를 `project.manage` 로 두는 것 — **배정하는 사람은 대개 참여자다**. 그 문만 쓰면 자동 초대가 거의 항상 막힌다 (D-12)」 — 결론은 살아 있으나 **근거 문장이 D-28 이 「덮지 못했다」고 부정한 바로 그 논리**이고 표시가 없다 |

- **한 줄 수정안**: ①은 `~~D-12~~` + 「2026-09-22 뒤집힘」으로, ②③⑤는 `~~D-12~~ → D-28`,
  ④는 두 행을 하나로 합쳐 `(도출 — D-14·D-19 · 2루프는 D-28·D-29·D-30)`,
  ⑥은 「~~자동 초대의 권한 경로(D-12)~~ → **게이트 제거(D-28)**」, ⑦은 「(~~D-12~~ → D-28 이
  더 넓게 기각)」 한 마디를 붙인다.

### [FAIL-3] 「결정 27건」이 세 자리에 남아 **38건과 충돌**한다

- `10-decision:87` 「`_RESUME.md` §2 가 사용자와 닫은 **결정 27건**이다」 — 그 아래 표가 D-38 까지 있다.
- `10-decision:1212` 「**SPEC-005 가 받는 것은 결정 27건**(D-01 ~ D-27)과 …」 —
  **Resulting Spec 절**이고, 바로 위 `:1207-1210` 이 「이번 판이 더한 **D-28 ~ D-38** 로 다시
  개정된다」고 적는다. **같은 절 안에서 모순.**
- `20-spec:1490` 「**27건 전부에 자리가 있다.**」 — **바로 위 제목이 `### DEC-004 결정 추적 —
  D-01 ~ D-38 전수`** 다.
- **한 줄 수정안**: 셋 다 **38건**(또는 「1루프 27건 + 2루프 11건」)으로 고친다.

### [FAIL-4] 「업무 화면 로직 그대로」에서 **막힘(block) 사유 모달**이 빠졌다 — 물려받을 규칙이 셋인데 둘만 적혔다

- **무엇이**: D-32·§2.6·FE-5 가 **물려받을 규칙을 「둘」**(① 요청 업무의 완료 → 완료 보고 모달 ·
  ② 수락 전 배정에는 안 세운다)이라고 못 박았다.
- **파일:줄**: `10-decision:771-774` 「그 자리에 이미 담긴 **규칙 둘**을 같이 물려받는다」 ·
  `20-spec:502-513`(상태 드롭다운 표에 막힘 행이 없다) · `30-work:1270-1271`
  (「허용 전이 계산과 **완료 보고 모달**을 가져다 쓴다」) · 인수조건에도 L-line 이 없다.
- **무엇과 어긋나나 — 코드**
  - `frontend/src/features/work/WorkModals.tsx:108` `in_progress: { blocked: "block", done: "complete" }`
    → **드롭다운이 「막힘」을 실제로 낸다.**
  - `frontend/src/features/work/MyWorkPage.tsx:1391-1394` 「막힘은 사유를 받아야 한다 — 고른 자리에서
    바로 보내지 않는다」 → `setBlocking(true)`
  - `frontend/src/features/work/MyWorkPage.tsx:1431-1440` `<BlockReasonPrompt … onSubmit={(reason) =>
    onTransition(task, "block", reason)} />` — **`BlockReasonPrompt` 는 `WorkModals` 가 export 한다**
    (`WorkViews.tsx:9` 가 이미 import 해 쓴다).
  - 즉 가로채는 규칙은 **셋**이고, 문서가 **완료 보고 모달만** 재사용 부품으로 지명했다.
    그대로 구현하면 **사유 없는 `block` 이 나가 선례와 다르게 동작한다.**
- **한 줄 수정안**: D-32 ⚠ 를 「규칙 **셋**」으로 늘려 「**막힘도 드롭다운에서 바로 보내지 않는다 —
  `BlockReasonPrompt` 를 띄운다**」를 추가하고, §2.6 표에 그 행을, §6 에 L-line 하나를
  (예: 「막힘을 고르면 사유 입력이 뜨고, 사유 없이 전이가 나가지 않는다」) 더한다.

### [FAIL-5] 상태 드롭다운 게이트가 **`access == "owner"` 하나**로 적혔다 — 선례 게이트는 둘이다

- **파일:줄**: `20-spec:506-507` 「언제 드롭다운인가 \| **활성 담당이 나일 때만** … 그 판정은 누가
  하나 \| **서버다.** 업무 상세가 내는 **접근 값(`access == "owner"`)** 을 쓴다」 ·
  `20-spec:1194-1196` · `30-work:1268-1269` 「**판정은 서버가 낸 접근 값**을 쓰고」.
- **무엇과 어긋나나 — 코드**
  - `MyWorkPage.tsx:1113` `canManage={canManageOwnTasks && !row.awaitingAcceptance}`
  - `MyWorkPage.tsx:1379` `if (!canManage || transitions.length === 0) return <StatusText … />`
  - `MyWorkPage.tsx:1216` 다른 소비처도 `canManage={detail.manage}` 인데 그 값의 출처가
    `:578` `manage: canManageOwnTasks` 다.
  - `App.tsx:369` `canManageOwnTasks: has("task.self_manage")` — **세션 봉투의 역량이지
    업무 상세의 값이 아니다.** 서버 응답 어디에도 그 판정이 없다
    (`task_results.py:236` 의 `access` 는 **활성 배정 보유** 하나만 가른다).
  - 결과: `task.self_manage` 가 없는 담당자에게 드롭다운이 서고 **고를 때마다 서버가 거절**한다.
  - (반대로 **수락 전 배정**은 `access` 가 이미 `read_only` 라 자동으로 막힌다 —
    §2.6 의 그 줄은 해롭지 않으나 중복이다.)
- **한 줄 수정안**: 게이트를 「**`access == "owner"` **그리고** 세션의 `task.self_manage`**」
  두 값으로 적고, 「화면이 담당을 다시 추론하지 않는다」는 문장은 **담당 판정에만** 한정한다.

### [FAIL-6] 게이트가 읽을 값이 **화면에 안 온다** — `App.tsx` 가 2루프 파일 목록에 없다

- **무엇이**: 2루프가 **게이트 둘**을 새로 세운다 — 생성 버튼(`PROJECT_MANAGE` 보유, D-31)과
  상태 드롭다운(FAIL-5). **둘 다 세션 역량이 필요**한데 `ProjectPage` 는 그 값을 받지 않고,
  그 배선을 할 파일이 발주 목록에 없다.
- **파일:줄**
  - `30-work:258-269` 「프론트엔드 — Phase FE-4 · FE-5」 표 **F1~F8 에 `App.tsx` 가 없다.**
  - `20-spec:638` 「게이트 \| **프로젝트를 만들 자격(`PROJECT_MANAGE`)**」 — **어디서 읽는지는 없다.**
  - `30-work:1256-1257` 「**게이트는 만들 자격**이고 「관리」의 `may_manage` 와 다른 값이다」 — 같음.
- **무엇과 어긋나나 — 코드**
  - `frontend/src/App.tsx:532-537` `<ProjectPage {...pageProps} onOpenTask … onRegisterHeaderActions …
    onRegisterRails … />` — **`{...sharedWorkProps}` 를 안 넘긴다.**
  - `App.tsx:363` `pageProps = { personaId, onError, onRegisterRefresh }` — **역량 값이 없다.**
  - `App.tsx:364-372` `sharedWorkProps` 에만 `canManageOwnTasks: has("task.self_manage")` 등이 있고
    `TodayPage`(`:474-482`)·`CalendarPage`(`:485-489`)는 그것을 받는다.
  - `ProjectPage.tsx:41-55` props 는 `personaId` · `onError` · 콜백 셋뿐.
  - `modules/organization_access/domain.py:97` `PROJECT_MANAGE = "project.manage"` —
    프론트에 그 값을 내는 유일한 통로는 세션(`/api/auth/me` → `OrganizationProfile.capabilities`)이다.
  - 즉 **FE-5 는 반드시 `App.tsx` 를 고쳐야 하는데**, 그 파일은 5개 화면이 공유하고
    §공유 자산 표에도 없다. 워커가 「발주서 밖 파일」을 만나 멈추거나, 조용히 만진다.
- **한 줄 수정안**: F-표에 `frontend/src/App.tsx` 를 **FE-5** 로 한 행 넣고
  (「`ProjectPage` 에 `canManageProjects: has("project.manage")` · `canManageOwnTasks` 를 넘긴다 —
  다른 화면에 넘기는 값을 바꾸지 않는다」), SPEC §2.10 게이트 행에 **값의 출처(세션 역량)** 를 적는다.

---

## 경미 (WARN)

### [WARN-1] 미결 개수가 **2 와 3 사이에서 흔들린다**

- `20-spec:86` 「**미결은 2건**이다 — DEC-004 § Open Questions 「2루프가 연 것」」
- `20-spec:1555` 「### 2루프가 연 것 — **셋. 열려 있다**」 · `30-work:1646` 「**미결 셋**」
- 실제는 **DEC 둘 + SPEC 이 연 OQ-606 = 셋**이다. `:86` 만 2 로 멈춰 있다.
- 덧붙여 `20-spec:1553` 「이 절은 그 원장을 다시 쓰지 않고, **이 SPEC 이 직접 연 것만** 적는다」인데
  표의 OQ-604·OQ-605 는 본문이 스스로 「DEC-004 미결 10·11 과 **같은 자리**」라 적는다 — **원장을 다시 쓴다.**
- **수정안**: `:86` 을 「**미결 3건** — DEC-004 둘 + 이 SPEC 이 연 OQ-606 하나」로,
  `:1553` 을 「원장 둘을 번호로 잇고, 이 SPEC 이 직접 연 것 하나를 더한다」로.

### [WARN-2] 상위 인덱스·상태표가 **1루프 사실에 멈춰 있다** (검수 대상 셋 밖)

- `10-decision/README.md` DEC-004 행 「**결정 27건 · 미결 0건** — SPEC 발주 가능」 → 38건 · 미결 2건
- `10-decision/README.md` 미결 표 「DEC-004 는 미결 **0건**」 → 2건
- `README.md` 「**DEC-004 결정 27건 · 미결 0건**」 · 「SPEC-005 확정 — 검수 통과·**미결 0**」
- `30-work/README.md` WORK-005 행 「**인수조건 75줄 전수 배정**」 → 76 + 48
- `log.md` 에 **2026-09-22 행이 없다** (마지막이 2026-09-21)
- 근거: reviewer `rules.md` § planner 리뷰 「표·인덱스 정합」.

### [WARN-3] 줄 수·묶음 수의 낡은 수치 셋

- `30-work:59` 「Spec reference: **SPEC-005**(**1121줄**…)」 → 실제 **1636줄**
- `30-work:1542` 「아래 셋은 SPEC-005 §6 의 **75줄**에 없다」 → 같은 문서 `:1452`·`:1469` 는 **76**
- `30-work:1611` Done Criteria 「인수조건 **75줄** 전항」 → 76
- (`:1453` 이 「75 → 76」을 설명해 두었으므로 세 자리만 안 따라온 것이다.)

### [WARN-4] D-31 게이트의 **관측 가능성**이 L-17 에서 증명되기 어렵다

- `20-spec:1437` 「**L-17** 만들 **자격이 없는** 사람에게는 **생성 버튼이 없다**」
- 지금 데모·테스트에서 `project.manage` 없는 페르소나로 프로젝트 화면을 여는 경로가 세어져 있지 않다
  (`ProjectPage.test.tsx` 는 `may_manage` 만 다룬다 — `:142`·`:432`).
- FAIL-6 를 고치면 자연히 풀리므로 **FAIL 로 세지 않는다.** 다만 FE-5 완료 판정에
  「그 역량이 없는 세션으로 렌더한 단언」을 한 줄 명시하는 편이 안전하다.

---

## 「반드시 확인할 것」 10항목 — 각각의 결과

| # | 항목 | 결과 |
|---|---|---|
| 1 | 사용자 지시 11항목 전부 내려왔나 · B-1/B-9 정정 반영 | **PASS.** 11건 전부 결정→계약→Phase 로 내려왔다(아래 대조표). B-1 정정(헤더 등록은 이미 있고 `may_manage` 탓)은 D-30 근거·`work:1689-1691` 에, B-9 정정(버그가 아니었다)은 D-38 `:910-915` · `spec:541-543` · `work:1689-1691` 에 **정정된 사실로** 들어갔다 |
| 2 | D-12 뒤집힘이 온전한가 | **FAIL-2.** 본문 취소선·사유·대체는 온전하고 **본문도 안 지워졌다**(`dec:339-372`). 그러나 **일곱 자리**(spec 170·775·1118·1257·1349·1505 · dec 955·1214)가 옛 뜻이다. 분류표(`dec:102·111`)·윤곽표(`:1032·1041`)·In Scope(`:1119-1121`)·하지 않는 것(`:1066`)은 **제대로 갱신됐다** |
| 3 | 계약 변경이 하나뿐인가 · 업무 올리는 게이트를 안 건드렸나 | **PASS.** 계약 변경은 D-29 하나다. 「업무를 **올리는**」 게이트는 세 문서가 전부 지켰다 — `dec:652-653` · `spec:847-850` · `work:248`(B2) · `work:1150-1151`(BE-3 작업 2) · `work:1174`. 코드로 확인: 게이트를 읽는 자리는 정확히 **넷**이고(`projects.py:230` 자동 초대 · `projects.py:494` 정의 · `assignments.py:59` Protocol · `assignments.py:171` `plan_project_work`), 마지막 것이 「이 프로젝트에 업무를 올릴 수 있는 자격이 없습니다」를 던진다. **FE 히트 0건**도 맞다. 몰래 바뀐 계약은 못 찾았다 — 이 화면에 쓰기 둘이 생긴 것은 `spec:1075-1078` 이 **정정으로 명시**한다 |
| 4 | 가르는 방식 ⓑ 의 근거가 서는가 (`access` 를 grep 으로 세라) | **PASS — 문서보다 강한 근거가 나온다.** `access` 를 읽는 자리: 타입 `task_results.py:236` · 생산 `application.py:942,944` · 소비 `bootstrap/application.py:920`(`!= "owner"`) · `platform/actions.py:2101`(`!= "owner"`) · FE 타입 `viewModels.ts:238` · FE 소비 `WorkModals.tsx:539`(`=== "read_only"`)·`:803`(`!== "read_only"`) · 계약 테스트 **8개 파일 11줄**. **`WorkModals.tsx:803` 이 결정적이다** — `viewerDrives = … && task.access !== "read_only"` 이므로 셋째 값 `project` 를 넣으면 **남의 업무에 조용히 조작 UI 가 선다.** ⓐ 기각은 옳다. ⓑ 도 구현 가능하다 — 프로젝트 판정이 이미 있고(`application.py:1111-1118` `_may_read_beyond_holding` 이 **프로젝트 범위를 먼저 본다**) 프로젝트 구성원은 **이미 `read_only` 로 상세를 읽는다**. ⚠ 다만 `spec:991` 이 「무엇이 함께 움직이는지가 **이 판에서 세어지지 않았다**」라고 적은 것은 사실이다(정직한 서술이라 감점하지 않는다) |
| 5 | SPEC-003 에 안 닿는다는 주장 (직접 grep) | **PASS.** `grep -n "access" 20-spec/spec-00{1,2,3,4}` → **0건.** SPEC-003 §4 Data Contract(`:882-899`)는 수행 상태·담당 관계 상태·요청/제안 상태·관계·파생 표시·시각만 적고 **접근 값도 체크리스트 투영 규칙도 없다.** SPEC-003 의 `checklist` 유일 등장(`:646`)은 **요청 발송 «입력» 필드**이고 「기존 계약 그대로」다. 주장 셋 다 맞다 |
| 6 | 인수조건 48줄 전수 배정 (직접 세라) · 기존 76줄 재배열 여부 | **PASS.** 내가 센 값: `§6` 의 `- [ ]` 중 **1247~1398 이 정확히 76줄**, `L-` 번호가 **L-01~L-48 연속 48개, 결번·중복 0**. 배정표(`work:1505-1517`) 합 6+6+1+9+1+10+2+1+5+7 = **48**, 부담표(`:1523-1526`) **BE-3 12 / FE-4 8 / FE-5 27 / 문서 1 = 48**. 커버리지도 겹침 없이 1회씩: BE-3 = L-01~L-09·L-11~L-13, FE-5 = L-10·L-14~L-22·L-24~L-35·L-37~L-41, FE-4 = L-36·L-42~L-48, 문서 = L-23. **기존 76줄은 번호도 자리도 안 바뀌었고** 새 줄은 맨 뒤 묶음으로 붙었다(`spec:1399-1403`). 뜻이 바뀐 기존 두 줄(`:1332-1334`·`:1356-1359`)은 **그 자리에서 취소선으로** 고쳤고 머리(`:43-44`)가 그 방식을 선언한다 |
| 7 | 직렬 순서 · Dependency 의 「왜 병렬이 아닌가」 · FE 를 둘로 쪼갠 근거 | **PASS.** 순서는 `work:993-1001`(그림) · `:1003-1010` · `:512-521`(Dependency 표)에 있고, 「왜 BE 먼저」가 **D-29 가 FE 응답을 바꾼다**로 적혀 있다(`:1005-1007`·`:517`) — 사용자 원문도 인용됐다. **파일 중복은 사실이다**: `projects.css` 는 한 장이고(FE-4 = 라벨 칸 sticky·완료 바 fill / FE-5 = 제목 색·메타 표·레일 헤더), `ProjectRail.tsx`(139줄)는 **헤더(`:66-87`)와 선택 카드(`:61`·`:101-110`)를 한 파일에 갖는다** — FE-4 의 「선택된 카드로 스크롤」과 FE-5 의 「헤더 한 줄」이 같은 파일이다. 근거가 선다 |
| 8 | 빈 상태에서도 생성 버튼이 선다 — 실현 가능한가 · 반영됐나 | **PASS.** 코드 사실이 맞다 — `ProjectPage.tsx:230-235` 는 `noProjects`면 `onRegisterRails({})` 로 레일을 안 세우고, `:292-298` 이 `Empty` 만 그리는 이른 반환이며, 헤더 등록(`:280-290`)은 `selected?.may_manage` 라 `selected` 가 없으면 `null` 을 넣는다. **실현 가능하다** — 헤더 등록 `useEffect` 는 **이른 반환보다 먼저 돌므로**(훅이다) 그 자리에 역량 기반 버튼을 얹으면 된다. 반영도 됐다: `spec:639-640`(D-22 를 안 깬다는 설명까지) · `L-15` · `work:1258-1259`. ⚠ **값을 어디서 읽는지만 비어 있다 → FAIL-6** |
| 9 | 새 결정을 만들지 않았나 — `(확정)`/`(제안)`/`(도출)` 구분 | **PASS.** 백로그 밖 판단은 `(제안)`·`(도출)`·「코디 기본값」으로 갈려 있다 — 가르는 방식 ⓑ `(제안)`(`spec:987·992`) · 접근 값의 뜻 `(제안)`(`:1033·1111`) · 게이트 D-31 「코디 기본값」(`dec:738`) · D-38 「코디 판단」(`dec:891`) · 새 오류 0건 `(도출)`. 의심스러웠던 「**수락 전 배정에는 드롭다운을 안 세운다**」(`spec:511` 「확정 — 같은 자리」)도 **발명이 아니다** — 조사 `loop2-survey-report.md:120` 이 `!row.awaitingAcceptance` 를 물려받으라고 적었고 `MyWorkPage.tsx:1113` 이 그 코드다. **백로그에 없는 자리는 결정이 아니라 미결(OQ-604~606)로 올라갔다** |
| 10 | 저장 구조 서술이 SPEC 에 없나 · WORK 의 「2루프는 저장 구조를 안 바꾼다」가 맞나 | **PASS.** SPEC 에 column·index·FK·migration 서술이 없다 — `grep 컬럼|migration|인덱스` 결과가 전부 **「여기 없다」는 경계 선언**이거나(`:45·53·197·1087·1182`) **「진행률 저장 컬럼을 안 만든다」는 금지**(`:128·202·1146·1309`)다. WORK 의 주장(`:483-497`)도 맞다: D-29 는 응답 투영(`_related_view` 가 돌려주는 dict 에 필드를 더하는 일) · D-28 은 `projects.py:230` 한 줄 삭제 · D-30/31 은 기존 표면 · D-32~D-38 은 화면. `task_assignments.auto_project_join` 은 **1루프가 세운 칸**이고 붙는 사람이 늘어도 뜻이 안 바뀐다 |

---

## 사용자 지시 11항목 대조표

| 백로그 | 지시 요지 | 결정 | 계약(SPEC) | Phase / 인수조건 | 판정 |
|---|---|---|---|---|---|
| **A-1** | 배정받으면 무조건 참여자로 붙는다 | **D-28**(`dec:636-670`) | §4 자동 초대(`:822-850`) · Validation `:1029` · Data `:1108` | **BE-3** / L-01~L-06 | **PASS** (D-12 흔적은 FAIL-2) |
| **B-1** | 헤더 「프로젝트 추가」 + 생성 모달 · 루프3 과 가른다 | **D-30 · D-31** | **§2.10 신설**(`:630-662`) · §2.7 `:558-568` | **FE-5** / L-14~L-23 | **FAIL-1**(`external_key` 근거) · 나머지 PASS |
| **B-2** | 간트 선택 시 좌 레일 자동 스크롤 | **D-37** | §2.4 | **FE-4** / L-46 · L-47 | **PASS** |
| **B-3** | 첫 진입 오늘 기준 · 라벨 칸 틀고정 | **D-36** | §2.4 · §5 `:1180-1181` | **FE-4** / L-42~L-45 | **PASS** (부수 셋 z-index·배경·축 스페이서까지 `work:1208-1211`) |
| **B-4** | 완료 바 투명 초록 · 토큰 금지 | **D-35** | §2.4 바 색 표 · §5 `:1178` | **FE-4** / L-36 | **PASS** |
| **B-5** | 우 레일 제목·소제목 `ink-strong` | **D-35** | §2.6 `:497` | **FE-5** / L-37 | **PASS** |
| **B-6** | 메타 표 정렬선·행 높이·버튼 시인성 (+관계 블록도) | **D-35**(`:833-836`) | §2.6 `:498-500` | **FE-5** / L-38~L-41 | **PASS** — **「두 자리 모두」와 「누르면 이동 유지」가 L-40·L-41 에 다 들어갔다** |
| **B-7** | 「업무 정보」 블록 신설 · 순서 넷 | **D-33** | §2.6 `:468-483` | **FE-5** / L-24~L-26 | **PASS** |
| **B-8** | 내 업무면 상태 드롭다운 · 로직 그대로 · 액션 버튼은 안 옮김 | **D-32** | §2.6 `:502-517` · §5 `:1194-1200` | **FE-5** / L-27~L-32 | **FAIL-4**(막힘 모달 누락) · **FAIL-5**(게이트 값) |
| **B-10** | 좌 레일 헤더 한 줄 · 배지는 「프로젝트」 옆 | **D-34** | §2.2 `:268` | **FE-5** / L-34 · L-35 | **PASS** — 「그 문장이 안 틀린다」 확인도 L-35 로 잡혔고 읽힘 문제는 OQ-605 |
| **B-11** | 같은 프로젝트면 업무 내용·체크리스트 다 본다 | **D-29** | **§4 읽기 범위 신설**(`:970-1013`) | **BE-3**(+FE-5 L-10) / L-07~L-13 | **PASS** — SPEC-003 미접촉 주장까지 확인됨 |

> **B-9 는 사용자 지시가 아니라 코디 발견**이고, 조사가 「버그가 아니다」로 뒤집은 것이
> D-38 과 OQ-604 로 **정정된 사실**로 남았다. 11항목 **누락 0건.**

---

## 기존 부채 (이번 판정 제외)

- `scripts/lint-pipeline.py` 부재 — 리뉴얼 레포에 `scripts/` 자체가 없다. 파이프라인 린트를
  돌릴 수 없어 이 검수는 **눈과 grep 으로만** 했다.
- `30-work:1611`·`:1542` 의 「75줄」은 1루프 개정(`:1453` 의 75→76)이 안 따라간 자리다 (WARN-3 에 포함).

## 확인한 것 (PASS 근거 요약)

- **read-only 준수**: 세 문서·코드 수정 0건. 산출물은 이 리포트 하나.
- **자리 규칙·frontmatter**: `10-decision/`·`20-spec/`·`30-work/` 제자리. `spec` 의 `version: 0.2.0`,
  `updated_at: 2026-09-22` 셋 다 갱신됨. 새 최상위 디렉토리 없음.
- **중복 문서 없음**: 2루프가 새 문서를 만들지 않고 셋을 개정했다.
- **줄 번호 인용 표본 검증**(전부 실물과 일치): `projects.py:153`(PROJECT_MANAGE) · `:230`(게이트) ·
  `:494`(정의) · `assignments.py:59`·`:171` · `application.py:940-944`·`:1111-1118` ·
  `ProjectPage.tsx:230-235`·`:280-290` · `MyWorkPage.tsx:1383-1416` · `api.ts:805-812`.
