# WORK-006 Phase 5 측정 기록 보정 — 독립 검수

- **검수 대상**: `tauri-measurement-r2.md` (직전 검수 `review-tauri-p5-integration-r2-report.md` 의 **N-1 · N-2** 보정)
- **범위**: 발주대로 **보정 네 항목만** 본다
- **코드·SPEC·WORK 수정 없음. 이 보고서 한 파일만 작성.**

---

## 0. 종합 판정 — **PASS · FAIL 0 · WARN 1(사소)**

**요구한 보정 네 항목이 모두 들어갔다.** 표본 변동 세 숫자(3/4 · 2/3 · 2/4)와 「안정성 성적이
아니다」가 §1-1 에 명시됐고, `MeetingMaterials.test.tsx` 가 실패 파일 목록에 추가되면서
**격리 실행에서는 통과했다**는 설명이 함께 붙었다. AC 상태·실기 미측정·AC-T27 미측정 판단은
**한 글자도 바뀌지 않았다.** 이번 회차 변경은 **측정 기록 한 파일뿐**이다.

---

## 1. 판정표

| # | 발주 확인 항목 | 판정 |
|---|---|---|
| 1 | §1-1 에 **3/4 · 2/3 · 2/4** 표본 변동 | **PASS** |
| 2 | §1-1 에 **「안정성 성적 아님」** 명시 | **PASS** |
| 3 | 실패 파일 목록에 **`MeetingMaterials.test.tsx`** | **PASS** |
| 4 | **격리 실행 통과** 설명 | **PASS** |
| 5 | **AC 상태** 유지 | **PASS** (무변경) |
| 6 | **실기 미측정** 유지 | **PASS** (무변경) |
| 7 | **AC-T27 미측정** 판단 유지 | **PASS** (무변경) |
| 8 | 변경 파일이 **측정 기록 한 개뿐** | **PASS** |
| 9 | 검수 보고서 한 개만 작성 | **PASS** |

---

## 2. 보정 내용 확인 (위치 명시)

### 2-1. 표본 변동 세 숫자 + 「성적 아님」 — `§1-1` L43~47

> ⚠ **3/4 를 «안정성 성적»으로 읽지 말 것 — 표본이 흔들린다.**
> 이 문서의 4회는 `1 failed` 1회(**3/4** 통과)였지만, **검수 재현에서는 3회 중 2회가 실패(2/3)**
> 했다. 앞선 회차의 기준선 측정도 **4회 중 2회 실패**였다. **회차마다 빈도가 크게 달라지므로
> 어느 쪽 숫자도 「이 정도로 안정적이다」의 근거가 되지 않는다**

- **3/4**(이 문서) · **2/3**(검수 재현) · **4회 중 2회 = 2/4**(기준선) — **세 표본이 모두 적혔다.** ✅
- 「**안정성 성적으로 읽지 말 것**」 + 「**어느 쪽 숫자도 근거가 되지 않는다**」 — 요구를 정확히 충족. ✅
- L49~50 이 결론을 **유지**한다: 「기준선 대비 **새 실패 0**(결론 유지). 다만 불안정 자체는
  여전히 있고 **빈도는 회차마다 다르다**」 — **판정을 흔들지 않고 해석만 좁혔다.** 적절하다.

### 2-2. `MeetingMaterials` 추가 + 격리 실행 설명 — `§1-1` L36~41

- 실패 파일 목록 L36~37: `CreateWork · Checklist · App · CalendarPage · ProjectPage ·
  ActionCenter · **MeetingMaterials**` — **추가됨.** ✅
- 격리 실행 설명 L39~41:
  > **`MeetingMaterials.test.tsx` 는 배선이 닿는 파일인데도**, **격리 실행(R-2 의
  > `src/features/meetings/` 15건 포함)에서는 통과했다.** 즉 이 파일의 실패도
  > **전체 스위트를 함께 돌릴 때만** 나타나는 같은 불안정이고, 배선의 결함이 아니다
- **논거가 오히려 강해졌다** — 「목록에 없다」를 덮는 데 그치지 않고, **같은 파일이 격리에서
  통과한다는 사실로 「교차 간섭」을 직접 증명**한다.
- **수치 검산**: `npx vitest run src/features/meetings/MeetingMaterials.test.tsx` →
  **15 passed (15)**. 본문의 「15건」이 **실제와 일치한다.** ✅

### 2-3. AC 상태 · 실기 미측정 · AC-T27 — 전부 유지

| 확인 | 결과 |
|---|---|
| AC 표 21행(`§2` L66~88) | **무변경** — 상태 기호·증거 위치 그대로 |
| 집계 `✅2 · 🟡11 · ⬜8`(L90) | **무변경** |
| `§2-1` AC-T27 「인용할 Phase 2 기록이 없다 → 미측정으로 둔다」 | **무변경** ✅ |
| `§3` 제품판 8칸 전부 ⬜/🚫 | **무변경** — 실기 미측정 유지 |
| `§5` Windows 「실행·컴파일 둘 다 미검증 · macOS 로 대신 쓰지 않는다」 | **무변경** |
| `§6-3` M-1 「미해소」 · `§7-6` 운영 origin 「미정·자리표시」 | **무변경** |
| `§8` 「코드 수정 0줄」 | **무변경** |

→ **보정이 판정에 손대지 않았다.** 요구 그대로 «표현만» 고쳤다.

### 2-4. 변경 파일 — 측정 기록 하나뿐

- 21:32 이후 작업 디렉터리에서 바뀐 파일: `tauri-measurement-r2.md` **1개**
  (그 밖의 `*-brief.md` 둘은 **코디가 쓴 발주서**, `review-tauri-p5-integration-r2-report.md` 는
  **직전 검수 산출물**이라 이번 보정 회차의 변경이 아니다)
- **제품 코드 변경 0건** — `frontend/src`·`frontend/src-tauri` 에 21:32 이후 수정 파일 없음.
  `git status` 의 항목은 전부 **Phase 4 에서 이미 있던 변경** 그대로다.

---

## 3. FAIL

**없다.**

---

## 4. WARN (1건, 사소)

| # | 내용 | 위치 | 수정 방향 |
|---|---|---|---|
| **N-1** | **`3/4` 표기가 §1-1 밖에 두 군데 더 남아 있다.** R-3 표(L23)의 「**3/4 성공** · 1회 기준선 불안정」과 §4 표(L138)의 「**3/4 통과** · 1회는 기준선 불안정」이다. **둘 다 `(§1-1)` 을 달고 있어** 읽는 사람이 주의로 넘어갈 수 있으므로 **오독 위험은 낮다.** 같은 이유로 §6-4 의 「이번 4회 중 1회 발생」도 이 문서 자체 회차를 적은 것이라 틀리지 않다 | `§1` L23 · `§4` L138 · (`§6-4`) | 굳이 고친다면 두 자리에 「(표본 변동 — §1-1)」 정도만 덧붙이면 된다. **고치지 않아도 FAIL 이 아니다** |

---

## 5. 다음 Phase 이월 (이번 보정과 무관 — 직전 검수에서 그대로)

보정 범위 밖이라 **여기서 다시 판정하지 않는다.** 직전 검수 `§5` 의 8건이 그대로 유효하고,
그중 **화면 배선 회귀 시험 파일 허용**과 **AC-T27 의 WORK 계획 문장 정정**이 여전히 가장 시급하다.
`§6-5`(AC-T41 네이티브 보상 수단 없음)를 §7 이월 표에도 얹자는 제안도 **미반영 상태**다 —
이번 발주가 그 자리를 열지 않았으므로 **누락이 아니라 범위 밖**이다.

---

## 6. 한 줄 정리

**요구한 보정은 전부 들어갔고, 판정은 하나도 흔들리지 않았다.** `MeetingMaterials` 건은
단순 추가에 그치지 않고 **격리 실행 통과라는 증거로 논거를 보강**했으며, 그 「15건」 수치까지
재현해 확인했다. 남은 것은 `3/4` 표기 두 자리의 **문체 정리 수준**이다.
