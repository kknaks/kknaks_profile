# 검수 3 — SPEC-004 0.4.1 수정분 리포트

- 작성: 2026-09-10 / `reviewer_spec` · **수정분만**(전체 재검수 아님)
- 기준: `review-02-spec-004-report.md` 의 F-A · F-B · WARN 9 · §7 · 코디 결정 D16 · 시안 최신본
- 검사: 항목별 `grep` 대조(SPEC · WP 6건 · 10-decision · 시안 2본) · `lint --strict` · `git status`

---

## 0. 총평

# PASS (WARN 1)

**FAIL 2건(F-A · F-B)과 WARN 8항목이 전부 해소됐다.** D16 이 정한 기본값이 SPEC 본문·WP·10-decision·시안에 빠짐없이 내려왔고, 고치는 과정에서 계약이 서로 어긋난 곳도 없다. `document_version` 0.4.1 · 판 이력 · lint sc-ax 0/0 · `git status` 전부 정상.

남은 것은 **재발주가 필요 없는 기록 누락 하나**다 — D16 의 「회의실 판정 데모 제외」를 SPEC·WP 는 반영했는데 **시안이 아직 「가능」 배지를 낸다.** F-B 때 R-24 에 충돌을 적고 시안을 함께 고친 그 처리를 이 건에도 한 번 더 하면 닫힌다.

---

## 1. 항목별 판정

| # | 확인할 것 | 판정 | 근거 (파일:줄) |
|---|---|---|---|
| **1** | **F-A 해소** — 회의 정보 편집 = 참석자 전원 | **PASS** | §3.3 `spec-004:144` 참석자 행에 「**회의 정보 편집**」 이동, 만든 사람 행(`:143`)에서 제거 · §3.1-7 `:114-115` 여는 상태 예정·완료 유지 · `work-001:36`·`:98`(「**actor 게이트는 「그 회의의 참석자」다** — 만든 사람으로 좁히지 않는다」 + 회의록 편집과 대비 명시)·`:140`. 시안 `회의실.dc.html:1183` `btnHeadEdit: (planned \|\| settled) && **attendee**`(`:1118` `attendee = 만든 사람 \| 참석자`)와 **일치**. §12 **R-32** 신설로 기획 통보까지 |
| **2** | **F-B 해소** — 시안에서 「기타」 제거 + §12 충돌 기록 | **PASS** | 시안 `회의실.dc.html` 에서 `const ETC`·`noteSource` 소멸 — 남은 2건은 **제거를 설명하는 주석**뿐(`:1129` 「「기타」 블록을 두지 않는다 (D14)」 · `:1265`). **화면 노출 0.** SPEC §4.1-7 `:167` 폐기 유지 · §12 **R-24** `:473` 에 「**확정 시안이 아직 「기타」 블록을 내므로 시안에서도 함께 걷는다**(2026-09-10 디자이너 지시)」 충돌 기록 |
| **3-①** | 삭제 갈래 | **PASS** | §3.1-9 `:123-124` — 「**[회의 취소]** 회의 자체 … the Connect로 취소」 · 「**[회의록만 삭제]** 회의록과 붙은 자료를 지우고 **회의 예약은 남긴다**」. `work-001:37` Scope · `:141` 작업 · `:146` 검증(「예약과 캘린더를 건드리지 않는다」) · §12 **R-31** 기획 통보 |
| **3-②** | 출처 값 · R-19 | **PASS** | SPEC §4.1-2 `:162` 셋 유지 · 시안 픽스처 `회의록.dc.html:907` `src: '세트'` → **`'직접 입력'`** 교체 완료 · R-19 `:468` 이 「확정 시안의 픽스처(`src: '세트'`)도 함께 고친다」로 갱신 |
| **3-③** | 화자 매핑 BE = WP-002 | **PASS** | `work-002` Scope `:42`(다음 회의 이어짐·소급 반영) · 제외 `:50`(「**화면**만 WP-006 — 이 WP는 매핑의 백엔드와 소급 반영을 소유한다」) · entity `:86` `MeetingSpeakerName` · invariant `:93` · 의존 `:105`(WP-006 에 API 제공) · Phase 작업 `:158` · 검증 `:163`·`:165`(떼면 지난 회의 표시 복원, 발화 본문 불변). `work-006` 은 `:45`·`:119` **화면만** — 모순 0 |
| **3-④** | `/end` 소유 표기 양쪽 | **PASS** | `work-001:100` 「**`/end` 표면의 소유는 이 WP**이고 … WP-004가 이 표면을 **소비**해 얹는다」 · `work-004:94` 「**표면의 소유는 SCAX-WP-001** … 이 WP는 그것을 **소비**해」 — 양쪽 문장이 서로를 가리켜 일치 |
| **3-⑤** | 10-decision 제목 | **PASS** | `10-decision.md:61` 「### 2026-09-09 · 09-10 — 회의·미팅노트 구조 결정 (**D1~D14 · F-1**)」 |
| **3-⑥** | §4.2-5 결론 열 | **PASS** | `spec-004:192-200` 표에 **「결론 표시」 4번째 열** 신설 — 완료만 「**선다** — AI가 내고 만든 사람이 고친다」, 실패는 「없다 — 합성이 안 됐다」. 시안 `REPORT-회의실-v2:73`(「완료에만 … 실패에도 안 낸다」)과 일치 |
| **3-⑦** | 회의실 판정 데모 제외 | **PASS** (기록은 ⑨ 참조) | §1 `:49` 「**회의실 고르기**」로 개명 + 「데모에서는 정적 목록」 · §2.2 표 `:100` 행 신설 · §3.1-2 `:107` · §3.1-4 `:109` · `work-001:48` 제외 · `work-006:39`·`:53`·`:99`·`:107`·`:149` |
| **3-⑧** | owner TBD 유지 | **PASS** | WP 6건 `owner: TBD` 각 1건 · `30-work.md` Status Board·WP List Owner 전부 `TBD` — frontmatter 와 표가 일치 |
| **4** | 새 어긋남 없는가 | **WARN 1** | 삭제 갈래 문구는 시안 모달(`회의록.dc.html:465` 「무엇을 삭제할까요?」 · `:470` [회의록만 삭제] · `:471` [회의 취소] · `:928-929` 토스트 둘)과 **버튼 이름·갈래 수가 정확히 같다** ✓ · WP-002↔WP-006 화자 계약 모순 0 ✓ · §5.1 상태표(`:218-223`)가 §3.1-7 여는 상태와 일치 ✓. **다만 회의실 「가능」에서 새 어긋남 하나** → ⑨ |
| **5** | 버전 · lint · status | **PASS** | `version: 0.4.1` · 판 이력 `:35` 0.3.0→0.4.0→**0.4.1**(사유 4개 명시) · `lint --strict` **exit 0 · 0 error 264 warning · 출력에 `sc-ax` 0회**(264건 전부 `products/selly/` 기존 부채) · `git status` = **4 M + 6 ??** 그대로 |

---

## 2. 남은 것 (WARN 1 · 재발주 불필요)

**⑨ 회의실 「가능」 — SPEC·WP 는 뺐는데 시안이 아직 낸다**

- SPEC §2.2 `:100` · `work-006:107` 검증 — 「회의실에 「가능」 판정 배지가 **없고** the Connect 요청이 나가지 않는다」
- 시안 `design/회의록.dc.html:426` — `<sc-if value="{{ r.ok }}">…**가능**</span></sc-if>` 가 **그대로 있고**, `:423` 이 `disabled="{{ r.disabled }}"` 로 못 쓰는 방을 잠근다

D16(「회의실 판정 = 데모 범위 밖, 정적 목록」)을 SPEC·WP 가 충실히 반영한 결과이고 planner 의 오독이 아니다. 문제는 **그 충돌이 어디에도 적히지 않은 것**이다 — §12 32행(R-1~R-32) 어디에도 회의실 관련 행이 없다. 지금 상태로는 `work-006` 안에서 두 문장이 부딪친다: `:23` 「**화면의 정본은 시안이다 … 여기서 화면 결정을 새로 하지 않는다**」 ↔ `:107` 「「가능」 배지가 없다」.

**F-B 때 쓴 처리를 그대로** 하면 닫힌다 — ① 시안에서 `r.ok` 「가능」 배지와 `r.disabled` 잠금을 걷어 정적 목록으로 만들고 ② §12 에 R-33 「`MOD-102-E04`·`T21`·`X-190` — 데모에서는 회의실이 정적 목록이다. 확정 시안이 아직 「가능」을 내므로 시안에서도 함께 걷는다」 한 줄을 더한다.

---

## 3. 사용자 결정 — 없음

검수 2 §7 의 두 건(「기타」 처리 · 삭제 둘째 갈래)은 D16 으로 닫혔고 그대로 반영됐다. ⑨ 는 이미 정해진 D16 의 뒷정리라 새 결정이 아니다.

---

## 4. 확인 안 한 것

이번은 수정분 검수라 검수 2 에서 PASS 로 닫은 축(범위 · 검수 1 해소 · D12~D15 · WP 6건 구조 · frontmatter/템플릿/`doc_no`)은 다시 열지 않았다. lint 와 `git status` 로만 회귀를 확인했다.
