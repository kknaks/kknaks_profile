변경 파일 10개(소스 7 · 테스트 3, 신규 없음) — modules/meetings/rooms.py(RoomBookingRefused 추가 · RoomReservationError 가 available 후보를 이고 감 · RoomReservation 에 replaced·requested_room_name · choose_replacement·headcount·local_slot · gateway 계약에 available) · platform/the_connect.py(available(date,start,end) 추가 — 예약 조회 한 번으로 전부 가른다 · create 가 찼을 때 가능한 방 목록을 실어 거절 · 중복이던 _busy 제거) · bootstrap/application.py(_reserve_room 이 회의보다 먼저 돌고 대체까지 책임 · _available_room_views · _release_orphan_reservation · create_meeting 순서 역전 · meeting_rooms 에 시간대 필터) · entrypoints/http.py(RoomBookingRefused → 409 · GET rooms 에 starts_at/ends_at) · modules/meetings/application.py(reservation_draft — 아직 없는 회의의 예약 입력) · domain.py·materials.py 는 아래 소수정 둘. 계약 — ① 대체: 고른 방이 거절되면 그 시간 가능한 공용 회의실 중 **정원 ≥ 인원(사내+사외, 만든 사람은 사내에 이미 포함)인 가장 작은 방**으로 다시 잡는다. 성공하면 room_reservation={status:'booked', room_name:<대체된 방>, reason:null, replaced:true, requested_room_name:<원래 고른 방>} 이고 location 도 대체된 방 이름이다. 대체가 아니면 replaced:false·requested_room_name:null. ② 거절: 대체할 방도 없거나 인증·연결 실패면 **회의를 만들지 않는다** — POST /api/meetings → 409 {detail:{code, message, available_rooms:[{room_id,name,capacity}]}}, code 는 room_unavailable|reservation_auth_failed|reservation_unavailable|room_reservation_failed. 인증·연결 실패면 available_rooms 는 빈 배열(닿지도 못했으니 지어내지 않는다). 회의 원장에 아무것도 남지 않는다. **순서를 뒤집었다**: 예약 → 성공 시 회의 저장. 회의 저장이 깨지면 잡은 자리를 cancel 로 되돌린다(되돌리기도 실패하면 로그만 남긴다). ③ rooms 필터: GET /api/meetings/rooms?starts_at=<ISO>&ends_at=<ISO> → 그 시간에 빈 방만 [{room_id,name,capacity,available:true}]. 파라미터 없으면 종전대로 전체 목록(available 키 없음), 둘 중 하나만 오면 전체 목록. 시각은 Asia/Seoul 로 환산해 묻는다. ④ 「회의실 선택 안 함」(room_id null)·quick-start 는 호출 0, 일시 변경·취소 동기화는 그대로. 검증 수치: uv run pytest -q tests/contract/test_meeting_rooms.py tests/contract/test_meeting_materials.py tests/contract/test_meeting_core.py tests/contract/test_meeting_finalize.py tests/architecture -m 'not integration' → 130 passed (250.26s), 실패 0, 경고 5(starlette deprecation, 무관). 검증 1회. 지시된 네 파일에 test_meeting_finalize.py 를 더해 돌렸다 — D38 이 안건 생성 자리를 한 군데로 모아 합성 적재(commit_finalized)도 그 자리를 지나기 때문이다. 전부 대역(FakeRoomGateway), 실물 호출 0. 새 테스트 9건 — 대체(정원 맞는 가장 작은 방 · 정원 모자란 방은 후보 아님) · 후보 없음 409 + 회의 미생성 · 거절이 가능한 방 목록을 실음 · 연결 실패 409 빈 후보 · 계정 없음 409 빈 후보 · 예약 성공 뒤 회의 저장 실패 → cancel 로 되돌림 · rooms?starts_at 필터(+ 물어본 시각이 지역 시각인지) · 파라미터 없으면 전체 목록. FE 인계 — ① POST /api/meetings 가 이제 **409 로 실패할 수 있다**: detail.code 로 문구를 고르고 detail.available_rooms 로 회의실 칸을 그 자리에서 다시 그린다(회의는 만들어지지 않았으니 모달을 닫지 말 것). ② 성공 응답의 room_reservation 에 replaced·requested_room_name 두 칸이 늘었다 — replaced 면 「<requested_room_name> 대신 <room_name> 으로 예약됐습니다」 토스트. status 는 이제 booked|cancelled 만 새로 생긴다(failed 는 D36 이전 저장분에만 남는다). ③ GET /api/meetings/rooms?starts_at&ends_at 로 시간대별 가능 목록을 받는다(available:true 가 붙는다). 미결 3건 — ① 대체는 **한 번만** 시도한다: 대체할 방을 고르는 사이 그 방마저 차면(경합) 두 번째 create 가 RoomUnavailable 로 올라가 409 가 된다. 재시도 루프를 두지 않은 것은 [만들기] 대기 시간이 왕복마다 늘기 때문이다 — 필요하면 코디 판단으로 1회 더 열 수 있다. ② 회의실을 나중에 바꾸는 경로는 여전히 없다(PATCH 는 room_id 미수신). ③ 대체 판정은 정원만 본다 — 모니터 같은 장비 조건은 우리 쪽에 입력이 없다(참고 구현에는 있다). 소수정 D37(자료 미리보기 제외 → 새 탭): GET /api/meetings/{id}/materials/{mid}/content 의 Content-Disposition 을 PDF·Markdown 에 한해 inline 으로 바꿨고 filename*=UTF-8'' 은 그대로 유지했다 — 다만 저장된 형식 선언을 되돌려 주지 않고 우리가 아는 둘(application/pdf · text/markdown; charset=utf-8)로만 낸다(브라우저가 .md 를 text/plain 으로 올리는 일이 흔하고, 남의 파일이 브라우저에서 실행되는 길을 열지 않기 위해서다). 그 밖의 형식은 종전대로 attachment. 테스트 1건 추가. 소수정 D38(안건 출처 다섯): AGENDA_SOURCES 를 manual·set·carried·derived·ai 로 넓히고 AI_AGENDA_SOURCE 상수를 세워 합성의 사람 안건 보존을 source != 'ai' 그대로 뒀다. set·derived 는 **값만 열었고 만드는 경로는 세우지 않았다**. 지금까지 임포트만 되고 아무도 부르지 않던 ensure_agenda_source 를 실제 게이트로 세웠다 — MeetingApplication._create_agenda 하나로 다섯 생성 자리를 모았으니 앞으로 어떤 경로가 생겨도 이 검사를 지난다. 스키마 변경 없음(문자열 열). 테스트 3건(다섯 값 허용 · 그 밖 MeetingError→422 · 지금 실제로 만들어지는 출처는 manual·carried 둘임을 고정). 커밋·push·PR 없음, frontend/ 미수정, Alembic 미사용.