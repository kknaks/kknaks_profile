변경 파일 6개(신규 1) — modules/meetings/templates/export_thread.html(신규 · 디자이너 정본의 마크업·CSS 를 그대로 옮기고 {{…}} 주석 자리를 조각으로 가름) · modules/meetings/export.py(전면 재작성: 조각 렌더러 + 값 고르기 + 장 나누기) · modules/work/graph.py(시스템 행위자를 사람 점으로 그리지 않음) · modules/work/requests.py(_view 에 source_meeting_id · SYSTEM_ACTOR_PREFIX 상수) · 테스트 2. 템플릿 위치 — backend/src/ax_workspace/modules/meetings/templates/export_thread.html. 새 의존성 0: Jinja 를 들이지 않고 <!--#fragment 이름--> … <!--/#fragment--> 로 조각을 갈라 {{키}}(이스케이프)·{{{키}}}(이미 만든 마크업)만 치환하는 40줄짜리 렌더러를 export.py 에 뒀다. 마크업과 CSS 는 그 파일이 소유하고 export.py 는 값만 채운다. schemas/*.json 과 같은 방식으로 패키지 안에 실리며, **휠에 실제로 실리는지**를 test_packaging.py 에 한 줄로 못박았다(import 만으로는 안 드러난다 — 부팅은 멀쩡하고 사람이 [내보내기] 를 누를 때 터진다). 이식 내용 — 회의명(제목|title_candidate|「제목 없는 회의」) · 일시(YYYY. MM. DD.(요일) + HH:MM~HH:MM, Asia/Seoul) · 참석자 한 줄(사내 표시명 + 사외 이름) · 요약(없으면 블록째 생략) · 안건(제목 · 「결론 남」/「결론 안 남」 칩 · 근거 시각 칩 최대 3 + 「+N」) · 줄(우측 시각 = started_at + evidence 첫 구간, 메모는 at_ms, 둘 다 없으면 빈칸) · 다음 할 일(「액션」 칩 · 제목 · 기한 월-일 · 근거 시각|「근거 없음」|「요청됨」, 0건이면 상자 없음) · 꼬리(쪽 번호 · 「SCAX · 회의록」 · 내보낸 시각). 페이지 나눔은 REPORT §3 소모 높이표(본문 986px · 첫 머리 74 · 이어지는 머리 28 · 안건 머리 36+간격 16 · 줄 21 · 할 일 상자 46+21)를 상수로 옮겨 서버가 계산하고, §5-4 경고대로 긴 줄은 60자 기준으로 접힘 수를 세어 높이에 곱한다. 쪽 번호는 서버가 박는다(counter(page) 는 @page 마진 박스에서만 돌고 Chrome 이 미지원). 검증 수치: uv run pytest -q tests/contract/test_meeting_finalize.py tests/architecture -m 'not integration' → **71 passed** (279.03s), 실패 0, 경고 2(starlette deprecation, 무관). 검증 1회. 새 테스트 10건 — 외부 자원 참조 0(<link>·<script>·@import·url() 정규식) · 조판 CSS 존재(@page A4 · 210mm/297mm · break-inside: avoid · page-no · 꼬리 문구) · 스레드 마크업(thread/thread-item, <table> 없음) · 칩과 시각 · 할 일 0건이면 상자 없음 · 이스케이프(<script> 가 글자로) 와 **회의·안건 id 미노출** · 여러 장 나눔과 쪽 번호·이어지는 머리·참석자 1회 · 요약 없으면 생략 · 「근거 없음」/「요청됨」/기한 월-일/빈 시각. 덤(D40 미결 ④): graph.py 가 requester_id 로 사람 점을 만들던 자리를 _link_requester 헬퍼로 모아, 'system:' 으로 시작하는 행위자는 **사람 점을 만들지 않고** 그 요청이 나온 회의에 followed_up 으로 잇는다. 상세(neighbors)에서는 회의를 찾아와 노드를 세우고, 첫 화면(overview)에서는 이미 화면에 있는 회의에만 선을 잇는다(요청마다 회의를 찾아가지 않는다). 회의를 모르면 선을 긋지 않는다. 테스트 2건(승격 요청에 system 사람 점 없음 + 회의와 연결 · 사람이 보낸 요청은 종전대로 person:mina 에 연결). 미결 4건 — ① **요약 자리에 meeting.purpose 를 넣었다.** 회의가 들고 있는 산문이 그것뿐이라서다(합성이 내는 요약 필드가 없다). AI 요약을 따로 두려면 필드와 합성 스키마가 함께 필요하다 — 코디 결정. ② 장 나눔은 **추정**이다: 실제 렌더 높이가 아니라 REPORT 의 소모 높이표 + 60자 접힘 추정이라, 아주 긴 줄이 몰리면 한 장이 넘칠 수 있다. 템플릿이 min-height 로만 잠가 두어 잘리지 않고 밀려 보인다. ③ 한 안건이 빈 장보다 큰 경우 줄 단위로 갈라 다음 장으로 잇고, 이어진 조각은 제목만 다시 세우고 칩·다음 할 일을 반복하지 않는다 — 정본에 없는 상황이라 내가 정한 규칙이다. ④ Chrome 인쇄 미리보기(A4·100%·여백 없음·배경 그래픽 켜기)는 브라우저 자동화 금지라 못 돌렸다 — REPORT §4 와 같은 두 가지 눈 확인이 코디 몫으로 남는다. 커밋·push·PR 없음, frontend/ 미수정, 스키마 변경 없음, 새 의존성 없음.