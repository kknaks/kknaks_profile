# 디자인 수정 피드백 — 회의록 페이지 (SCR-105 목록 · 패널 · MOD-102 예약)

기록 시작: 2026-09-11. 사용자가 「수정 시작하자」 하기 전까지는 **기록만** 한다. 정본 시안 `design/회의록.dc.html`, 코드 `frontend/src/meetings/MeetingListPage.tsx`·`BookingModal.tsx`.

| # | 자리 | 지금(실물) | 바꿀 것 | 상태 |
|---|---|---|---|---|
| 1 | 왼쪽 목록 패널 `section.meeting-panel[aria-label="회의 목록"] > .meeting-scroll` (예정·지난 목록) | 패널이 내용 높이만큼만 서고(368px), 목록이 페이지 안에서 늘어남 | **패널이 화면 높이 전체를 쓴다.** 목록 영역은 패널 안에서 **투명 스크롤**(스크롤바 트랙 안 보이게, 내용만 흐름). 뷰포트 1919×936 기준 | 반영(코드·시안) |
| 2 | 오른쪽 회의록 패널 `section.meeting-panel[aria-label="회의록"]` (머리 `.meeting-panel-head` · 본문 `.meeting-scroll` · 푸터 `.meeting-panel-foot`) | 본문이 558px 로 서고 패널이 내용을 따라감 | **패널 전체 높이 = 화면 높이**, 3단 고정: 위 **회의 정보(머리, 고정)** / 가운데 **상세 내용(스크롤, 투명)** / 아래 **[공유] [내보내기] [회의록 열기] (푸터, 고정)**. 1 과 같은 투명 스크롤 | 반영(코드·시안) |
| 3 | MOD-102 예약 모달 · 날짜 `input#meeting-date` | 브라우저 기본 `type="date"` | **캘린더 컴포넌트**로(DS `DatePicker`/`DateField` 가 있음 — 그걸로 교체) | 반영(코드·시안) |
| 4 | MOD-102 · 시작/종료 시각 `select[aria-label="시작 시각"]` | 브라우저 기본 `<select>` 00:00~23:30 | **시간 셀렉터 공용 컴포넌트** 필요(DS 에 없음 → 새 부품 후보, 코드에 `TimeField.tsx` 있음 — 확인 후 재사용/신설) | 반영(코드·시안) |
| 5 | MOD-102 · 입력 칸 포커스 `input#meeting-purpose`(목적 등 전부) | 포커스 시 주변에 **파란 테두리(브라우저 focus ring)** | 포커스 시 **테두리 변화 없음**(outline·box-shadow·border 색 전부 없음) — **전역**, focus-visible 표시도 없음(사용자 결정 09-11; 접근성 우려는 코디 기록) | 반영(전역, 83e3d69) |
| 6 | MOD-102 · 참석자 이름 찾기 결과 `PersonSearch` 팝오버 | 1차 반영이 틀림: 구분선 라벨 + 아바타 행 | **줄 둘**: `────` / 「직원 정보 없음」(텍스트 행) / `────` / 「이건학 사외 참석자로 추가」(한 줄 클릭 행, 아바타·좌우 분리 없음) | 반영(코드·시안, 83e3d69) |
| 7 | MOD-102 · 장소(회의실 목록) | 회의실 A 10인 · B 6인 · 소 4인 · 대 20인 | 목록 **맨 위에 「회의실 선택 안 함」** 항목 — 이걸 고르면 회의실 예약(the Connect) 호출을 안 함. ⚠ the Connect 예약은 SPEC §2.2 데모 범위 밖으로 둔 상태 — 「선택 안 함」 항목 자체는 넣되 호출 여부는 별건 | 반영(코드·시안) |
| 8 | MOD-102 푸터 · [회의실까지 예약] 버튼 | 두 버튼(회의실까지 예약 / 회의록만 생성) | **[회의실까지 예약] 버튼 제거** — 장소에 「선택 안 함」이 있으니 갈래가 필요 없음 | 반영(코드·시안) |
| 9 | MOD-102 푸터 · [회의록만 생성] 버튼 | 문구 「회의록만 생성」 | **「회의 생성」 하나만** — 위에서 선택·입력을 다 끝내고 아래는 버튼 하나 | 반영(코드·시안) |
| 10 | MOD-102 닫기 확인 `alertdialog` | 「쓴 내용을 버리고 닫을까요?」 [취소][버리기] | 문구 **「입력 정보는 저장되지 않습니다.」** · 버튼 **[취소][나가기]** | 반영(코드·시안) |
| 11 | 「예정」 행 삭제 모달(MOD 삭제, `alertdialog`) | 제목 「무엇을 삭제할까요?」 · 본문 「회의 삭제 시 회의 자료가 삭제되고 회의도 취소됩니다.」 · 버튼 [회의록만 삭제][회의 취소] | 제목 **「회의를 취소할까요?」** · 버튼 **[회의 취소] 하나만**(「회의록만 삭제」 갈래 제거, 본문은 그대로 두거나 한 줄로) | 반영(코드·시안) |
| 12 | 이 페이지 전체(목록 + MOD-102 예약 모달 + 삭제 모달)의 **모든 셀렉터·드롭다운·입력 부품** | 브라우저 기본 `<select>`·`<input type=date>` 등이 섞여 있음 | **공용 컴포넌트로 전부 교체** — DS/코드에 있는 것(`Select`·`DateField`/`DatePicker`·`TimeField`·`Popover` 등) 재사용, 없는 것은 새 공용 부품으로 세워 DS 후보에 올림(D25). **범위는 이 페이지만** — 다른 페이지는 나중에 하나씩 보며 교체 | 반영(코드·시안) |
| 13 | 오른쪽 패널 푸터 vs 우하단 AX 플로팅 버튼 | 패널이 화면을 채우자 FAB 가 [회의록 열기] 를 가림 | 회의록 페이지에서 겹치지 않게 | 반영(83e3d69) |
| 14 | 목록 구획(기획 v1.1.0 반영, D34) | 예정·지난 둘 | **「진행 중」 구획 맨 위**(0건이면 없음), 누르면 실시간 회의록 | 기록(라운드 2) |
| 15 | 어휘(기획 v1.1.0, D34) | 페이지 「회의 목록」 · 「주제」 · 상태 「완료」·「정리 중」 배지 | 페이지명 **「회의」** · **「회의명」** · 상태 **「종료」**, 「정리 중」은 배지가 아니라 회의록 자리 로딩만 | 기록(라운드 2) |
| 16 | 패널 [공유] 노출(기획 v1.1.0, D34) | 예정·진행 중에도 | **끝난 뒤(종료·실패)만** | 기록(라운드 2) |
| 17 | 목록 패널(항목 1 재발주) | 회의 19건이면 페이지 전체가 스크롤 — 패널이 내용만큼 자람 | 페이지는 뷰포트 고정, 목록은 패널 안에서만 스크롤(오른쪽도 같은 높이) | 반영(51ec0aa, 실측 문서 높이=뷰포트 1130) |
| 18 | MOD-102 일시 · 날짜 칸(`DateField` inline) | 텍스트 칸 「2026/09/11」 + **바깥에 따로** 달력 아이콘 버튼 | **한 칸 안에** `2026-09-11` + 오른쪽 안쪽 달력 아이콘(시각 칸의 시계 아이콘과 같은 자리·크기). 표기는 하이픈. 칸 어디를 눌러도 캘린더 열림 | 반영(54a4287 · 시안 v3 §1.2, DS-16) |
| 19 | 패널 푸터 [내보내기] **버그** | 토스트 「내보냈습니다」만 뜨고 실제 내보내기 없음(상세 페이지만 실제 링크) | 상세와 같은 실제 링크(export?format=html) | 반영(커밋·push, HMR) |
| 20 | 패널 푸터 [공유] | 모달은 열림 — 사람을 골라야 [공유] 버튼이 활성 | 동작 확인됨(사용자 재확인 요청) | 확인 |
