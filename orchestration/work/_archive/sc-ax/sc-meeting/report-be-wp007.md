조사 사실(§1, 2026-09-11 실측 · 로그인 뒤 조회만) — ① **외부인 등록 엔드포인트는 없다.** /api/external-participants·/api/externals·/api/guests·/api/participants·/api/non-members·/api/outsiders·/api/visitors·/api/reservations/external·/api/members/external 전부 404 이고 openapi.json·/docs 도 404 라 스펙을 물을 자리도 없다. 예약 조회 행의 참석자 필드는 participants: [{email,name}] 이고 attendees 는 응답에 아예 없다(POST body 에만 있는 표시 문자열). 참고 구현의 PUT 실측 주석(2026-08-07)도 participants 를 어떤 모양으로 보내도 이메일이 소실된다고 적고 있다. → **브리프의 폴백을 채택**: 사외 참석자는 POST body 의 attendees 표시 문자열에 이름으로 나열한다. ② 계정 매핑: /api/members 218명 {id,name,email,company_id,company_name,team,...}. 이메일 소문자 일치 우선 → 없으면 이름 일치. 우리 데모 이메일은 scax.example 이라 실제로는 **이름 축이 일하는 축**이고, 두 축 모두 잃으면 그 사람은 버려지지 않고 표시 문자열로 간다(조용히 빠지지 않게). ③ /api/rooms 12개 중 id 1~7 이 공용 회의실(1:12인·2:4인·3:6인·4:6인·5:12인·6:4인·7:4인), 11 스튜디오·12~15 대표실 — 참고 코드 필터(id<=7 + 스튜디오 제외)가 정확히 공용 7개를 낸다. 응답에 type 키는 없다(참고 코드의 not row.get('type') 는 항상 통과). ④ 계정의 company_id=3 으로 참고 구현 기본값과 일치 확인(companies: 1 더데이랩스·2 THE SC·3 메디솔브AI·5 글로뷰티). 변경 파일 10개(신규 3) — modules/meetings/rooms.py(순수 포트: MeetingRoom·ReservationRequest·RoomReservation·MeetingRoomGateway Protocol·map_participants·build_reservation·실패 사유 4종) · platform/the_connect.py(어댑터 하나만 그쪽 주소와 계정을 안다. 로그인 쿠키 메모리 보관·401/403 시 1회 재로그인·rooms 캐시 300초·create 전 그 방 그 시간 겹침 확인·update 는 시간/회의실만) · bootstrap/application.py(room_gateway 조립·_reserve_room·_sync_room_reservation·create/update/cancel_meeting 연결·meeting_rooms) · bootstrap/settings.py(room_booking_* 6개 + room_booking_configured) · entrypoints/http.py(CreateMeetingRequest.room_id·GET /api/meetings/rooms) · modules/meetings/application.py(reservation_input·attach_reservation·_detail 의 room_reservation) · platform/meetings.py(member_email) · platform/persistence.py(meetings.room_reservation JSON nullable) · Makefile(THECONNECT_ENV_FILE ?= $(HOME)/.config/theconnect/env 를 api·api-e2e 에 source — local-stack 은 api-e2e 를 부르므로 함께 걸린다) · tests/contract/test_meeting_rooms.py. 계약 — GET /api/meetings/rooms → [{room_id,name,capacity}], 계정 없거나 못 닿으면 **빈 목록**(없는 방을 지어내지 않는다). POST /api/meetings 에 room_id?: number, null 이면 호출 0. 예약은 **회의 생성 트랜잭션 밖**이고, 성공하면 meetings.room_reservation 저장 + location = 회의실 이름, 실패하면 회의는 그대로 서고 location 은 비우고 room_reservation={status:'failed',reason}. 응답에 나가는 것은 {status, room_name, reason} 셋뿐 — external_id·room_id·계정은 원장에만 산다. 시간은 Asia/Seoul 로 환산해 date·start_time·end_time 으로 보낸다(UTC 로 보내면 9시간 밀린다). 일시 변경 시 update 동기화(제목만 고치면 부르지 않음), 회의 취소 시 cancel 동기화 — 실패해도 회의 동작은 그대로이고 사유만 남는다. quick-start 는 호출 0. 검증 수치: uv run pytest -q tests/contract/test_meeting_rooms.py tests/contract/test_meeting_core.py tests/architecture -m 'not integration' → 62 passed (28.73s), 실패 0, 경고 1(starlette deprecation, 무관). 검증 1회. 새 테스트 16건 — 회의실 목록(실제 목록·계정 없으면 빈 목록) · room_id 없으면 호출 0 · 성공 시 저장·location·원장에만 external_id · RoomUnavailable/네트워크/계정없음 3갈래 실패에도 회의 생존 · 사내 이메일축·이름축 분기와 사외 표시 문자열 · 매핑 실패자는 버리지 않고 이름으로 · 지역시각 환산 · 일시 변경 동기화 · 제목만 고치면 미호출 · 취소 동기화 · 취소 동기화 실패해도 취소 성립 · quick-start 미호출 · **계정·비밀번호가 응답·로그 어디에도 없음**. 실제 호출은 §1 조사(조회만)뿐이고 테스트는 전부 FakeGateway 다. FE 인계 — ① GET /api/meetings/rooms 로 회의실 목록을 받아 BookingModal.tsx:28 의 정적 MEETING_ROOMS 4개를 교체한다(반환 빈 배열이면 「회의실 선택 안 함」만 세운다). ② POST /api/meetings 에 room_id: number|null 을 싣는다 — 지금 보내는 location 문자열 대신 방 번호다(location 은 서버가 예약 성공 시 채운다). ③ 회의 상세/생성 응답의 meeting.room_reservation: {status:'booked'|'failed'|'cancelled', room_name, reason}|null 을 읽어 failed 면 사유를 안내한다(reason 값 4종: room_unavailable · reservation_auth_failed · reservation_unavailable · room_reservation_failed). 미결 3건 — ① **스키마 변경**: meetings.room_reservation 이 늘었다. nullable JSON 이라 additive 이고 make sync-demo-schema(reset_demo --sync)가 ALTER 로 붙인다 — 코디가 실물 전에 한 번 돌려야 한다. ② 회의실을 **나중에 고르거나 바꾸는** 경로는 없다(PATCH 는 room_id 를 받지 않는다) — 브리프가 생성 시점만 열었다. 예약 뒤 방을 바꾸려면 지금은 회의 취소 후 재생성이다. ③ 예약 실패가 [만들기] 를 최대 왕복 상한(기본 20초, TDL_HTTP_TIMEOUT_SECONDS)만큼 붙잡는다 — 회의는 그 전에 이미 저장돼 있지만 사람이 보는 지연이므로 값 조정은 코디 판단. 커밋·push·PR 없음, frontend/ 미수정, Alembic 미사용.