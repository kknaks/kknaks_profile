# 검수 2 — SPEC-004 0.4.0 + WP-001~006 리포트

- 작성: 2026-09-10 / `reviewer_spec` (검수 1 과 같은 세션 · **판정은 새로** 했다)
- 대상: 워크트리 `/Users/kknaks/orca/workspaces/mediness-mediness/sc-meeting-spec` · 커밋 없음(working tree)
- 범위: M 4파일(`20-spec/spec-004-meeting-note.md` 518줄 · `10-decision.md` · `30-work.md` · `40-architecture/system.md`) + ?? 6파일(`30-work/work-001`~`006`)
- 실행한 검사: `git status --porcelain` · `git diff origin/main` · `python3 scripts/lint-pipeline.py --strict` · 시안 정본 2본과 리포트 2본 대조(`renderVals()` 가시성 로직 직접 판독) · 금지어 grep · `doc_no` 전역 유일성 · task-management 원형 **경로 4건 `ls` + 인용 주장 코드 대조** · `templates/work.md` 절 구조 대조 · 엔드포인트 중복 스캔

---

## 0. 총평

# FAIL — 재발주 필요

FAIL 2건 · WARN 9건. **검수 1의 FAIL(F-1)과 WARN 11건은 전부 해소됐고**, D12~D15 반영·WP 6건·lint·문서 규약은 흠이 없다. 원형 인용은 문서 절뿐 아니라 **코드 주장까지 실제 파일과 맞아** 지어낸 곳이 없었다.

남은 FAIL 둘은 **SPEC 본문이 시안과 어긋나는 자리** 둘이다. 성격이 다르다 — **F-A는 SPEC이 시안을 잘못 읽은 것**(권한 한 칸)이고, **F-B는 시안과 사용자 결정 D14가 서로 어긋나는데 SPEC이 한쪽만 따르고 그 사실을 적지 않은 것**이다. F-B의 수정은 SPEC이 아니라 시안 쪽일 가능성이 크다. 둘 다 고치는 데 각각 한두 줄이다.

| # | 축 | 판정 | 한 줄 |
|---|---|---|---|
| 1 | 범위 | **PASS** | M 4 + ?? 6, 전부 지시받은 것. `00-planning/` 무변경. WP 작성은 사용자 지시(`_RESUME.md:38`)로 승인됨 |
| 2 | 검수 1 해소 | **PASS** | F-1 완전 소거(금지어 4종 본문 0건) · WARN 11 + ESC 건 **12/12 해소** |
| 3 | 디자인 정합 | **FAIL** | 시안 요구 12항목 중 10 일치. **회의 정보 편집 권한**(F-A)과 **「기타」 블록**(F-B)이 어긋난다 |
| 4 | 결정 반영 | **PASS** | D12~D15 자기보고 절에 실재. 「정리됨」은 §12 R-25 인용 1회뿐 |
| 5 | WP 완결성 | **PASS(WARN 4)** | 6건 모두 Covers·계약·원형·검증 완비. 원형 코드 주장 실측 일치. 화자 매핑 BE 미배정 등 4건 |
| 6 | 문서 규약 | **PASS(WARN 1)** | frontmatter 10/10 · `templates/work.md` 절 구조 6/6 일치 · `doc_no` 중복 0 · lint sc-ax **ERROR 0 / WARN 0** |

---

## 1. 축 1 — 범위 · **PASS**

```
 M products/sc-ax/10-decision.md
 M products/sc-ax/20-spec/spec-004-meeting-note.md
 M products/sc-ax/30-work.md
 M products/sc-ax/40-architecture/system.md
?? products/sc-ax/30-work/work-001-meeting-agenda-domain-api.md ... work-006-meeting-screens.md
```

- 브리프 §2 목록과 **정확히 일치.** 그 밖의 파일 0건.
- `00-planning/` 무변경 확인 — `plans/plan-004-meeting-note.md` 는 `document_version: 0.1.2 / 2026-08-25` 그대로이고 `screens/modules/` 에는 `screen-001`~`004` 만 있다(`screen-005` 를 만들지 않았다).
- ERD·타 spec·`rules/`·`context/`·`templates/`·타 제품 무변경.

**권한 확인** — `sc-meeting-spec-fix-brief.md` §4 의 allowed_paths 는 3파일이고 §6 은 「WP 만들지 않는다」다. 그러나 `_RESUME.md:38` 이 **「WP 작성 지시 … 사용자 「스펙 WP 작성하라고 그리고 리뷰 돌려」」** 로 별도 승인을 기록한다. `30-work/`·`30-work.md` 변경은 그 지시 범위 안이다 — **이탈 아님.**

---

## 2. 축 2 — 검수 1 해소 · **PASS**

### 2.1 F-1 — 완전히 걷혔다

```
$ grep -c  (spec-004-meeting-note.md 전문)
일시정지 1 · 재개 2 · 마이크 3 · "다른 창에서" 0 · pause 1 · resume 1
```

히트 6건을 전수 확인한 결과 **전부 부정형 서술이거나 정상 용법**이다 — 지어낸 조작·문구가 하나도 남지 않았다.

| 줄 | 내용 | 판정 |
|---|---|---|
| `:98` | §2.2 데모 범위 밖 표 — 「**회의 중 일시정지·재개와 마이크 조작** … 조작을 두지 않는다 … **WS 계약에도 `pause`/`resume` 프레임을 두지 않는다**」 | ✓ 브리프 F-1 이 요구한 「한 줄로 내린다」 그대로 |
| `:224` `:227` | 「자기 **마이크**에서 캡처한 오디오」 · 「캡처는 내 **마이크**뿐」 | ✓ 정상 용법 |
| `:245` | 「원형이 가진 스트림 중단·**재개** 프레임은 **옮기지 않았다**」 | ✓ 부정형 |

§5.3 「거절·종료 사유」 표(`:258`~`:266`)도 **「서버가 하는 것」 한 열**로 다시 쓰여 화면 문구·버튼이 전부 사라졌고, 남은 안내 문구는 `OQ-311` 로 기획에 넘겼다(`:503`). 검수 1 이 요구한 처리 그대로다.

### 2.2 WARN 11 + ESC 건 — 12/12 해소

| 검수 1 | 요지 | 0.4.0 처리 | 판정 |
|---|---|---|---|
| W-1 | §1 「`PLAN-004` §4.1 In Scope와 같다」가 사실이 아니다 | 그 문장을 **삭제**하고 §1을 시안 기준 목록으로 다시 씀 | ✓ |
| W-2 | §2.1 「§4.2 Out of Scope와 같다」 | 제목을 「확정 제외와 미결」로 바꾸고 동일성 주장 삭제 | ✓ |
| W-3 | `E07` 「직원 누구나(`X-96`)」 미대조 | §12 **R-27** 신설 — 「`X-31` 열람 경계와 갈린다 — 뜻을 확정한다」 | ✓ |
| W-4 | R-9 열거 불완전(3건 누락) | R-9 가 `E29`·`I15`·`I11`·**`E22`·`E71`·`I46`·`E75`** 7개로 확장(`:451`) | ✓ |
| W-5 | D8 중 `DEC-015` 메뉴명 미반영 | §1 첫 줄(`:46`) 「메뉴 이름과 … 둘 다 **「회의 목록」**이다 (`DEC-015`·`X-39`)」 | ✓ |
| W-6 | WS 정상 종료 계약 누락 | §5.3 「**정상 종료**」 행 신설(`:256`) + §8-1(`:339`) + §10-14 + `system.md` §4 | ✓ |
| W-7 | `sources:` 가 없는 파일·다른 판본을 가리킨다 | frontmatter `:13`·`:14` 에 「리포 판본 v0.1.2/2026-08-25 — … 기획자 반영 대기」·「리포에 아직 없음」 명시 | ✓ |
| W-8 | R-6 근거 약함 | 「**보강**한다 (§6 흐름 B-2에는 이미 있다)」로 완화(`:448`) — 모순 주장 철회 | ✓ |
| W-9 | R-8 부분 오독 | 「`SCREENDEF-005` §4 「진행 표시」 행」 **한 곳으로 좁힘**(`:450`). `RSK-002` 오지목 삭제 | ✓ |
| W-10 | §10-16 이 구현 규칙을 SPEC 에 둠 | 「산출물의 멱등성과 stale 결과 차단은 **System Topology §4가 소유한다**」로 라우팅(`:392`) | ✓ |
| W-11 | 진행 중 공유받은 사람의 표면 미정 | §13 **`OQ-314`** 신설(`:506`) | ✓ |
| (참고) | `SCAX-ESC-002` 재사용은 층이 다르다 | **`SCAX-ESC-006` 신설** + `## 로그` 에 경위 기록 | ✓ |

---

## 3. 축 3 — 디자인 정합 · **FAIL**

### 3.1 시안 요구 12항목 대조 — 10 일치

`renderVals()` 로직을 직접 판독해 대조했다(리포트 서술이 아니라 **코드**를 기준으로 삼았다).

| # | 시안이 정한 것 | 근거 | SPEC | 판정 |
|---|---|---|---|---|
| 1 | 상태 여섯 = 예정·진행 중·정리 중·**완료**·실패·취소됨 | `회의실.dc.html:859` `screenState` options · `:1125-1126` | §5.1 `:200` 동일 | ✓ |
| 2 | 안건 = 줄 목록 / 근거 타임칩(본문 뒤·다음 할 일 앞) / 「연관 업무 ›」 | `REPORT-회의실:352-363` · `:74-75` · `:89` | §4.2 `:166-182` 도해까지 동일 | ✓ |
| 3 | 진행 중 탭 둘(메모 기본·AI 요약) · 완료 탭 없음 | `회의실.dc.html:1154-1156` · `REPORT:180` | §7.2 `:335` | ✓ |
| 4 | 머리 편집이 여는 상태 = **예정·완료** | `회의실.dc.html:1190` `(planned \|\| settled)` · `REPORT:418` · `M14②:512` | §3.1-7 `:114` | ✓ |
| 5 | 고치는 값 = 제목·날짜·시각(30분)·장소(글자)·참석자 / 조직 귀속 없음 | `REPORT:403-411` · `:428-429` · `회의실.dc.html:1174-1176` | §3.1-7 `:115-116` | ✓ |
| 6 | 자료 행 클릭 → 드로어 · PDF·Markdown · 20MB | `REPORT:98` · `§4-2:216-222` · `§4-9:462` | §5.7 `:294-295` · §2.2 `:99` | ✓ |
| 7 | 공유 모달 = 검색 + 조직도 + 볼 수 있는 사람 | `REPORT §4-5(2):311-314` | §3.2-3 `:126` | ✓ |
| 8 | 목록 — 모든 행에 상태 + 「열람」 나란히 · 패널 읽기 전용 · 후보 건수 줄 없음 | `회의록.dc.html:110`·`:589` · `REPORT-회의록:19`·`:39` | §3.4 `:144-147` | ✓ |
| 9 | 예약 칸 순서 주제·일시·목적·안건·참석자·장소 · 반복 없음 · 사외는 이름 찾기 안 | `회의록.dc.html:270·326·332·353·413` · `REPORT-회의록:89`·`:105` | §3.1-4 `:108` · §3.1-5 `:109` · §2.1 반복 제외 `:81` | ✓ |
| 10 | 바로 시작 — 참석자 추측 없음 · 제목은 합성 때 AI 후보 | `REPORT-회의실 M1:504`(추측 요소 미구현) · D14 | §3.1-6 `:110-112` | ✓ |
| 11 | 삭제 확인 = `×` + [회의록만 삭제] + [회의 취소] | `회의록.dc.html:463-471`·`:928-929` | §3.1-9 `:120` — **자료만 지우기** | ⚠ W-1 |
| 12 | 「기타」 블록이 선다 | `회의실.dc.html:915-916`·`:1138`·`:1273` | §4.1-7 `:160` — **폐기** | ✗ **F-B** |
| (권한) | 머리 편집은 **참석자** | `회의실.dc.html:1190` + `:1127` | §3.3 `:136` — **만든 사람만** | ✗ **F-A** |

### 3.2 위반 (FAIL)

#### F-A — 회의 정보 편집 권한이 시안보다 좁다

- `products/sc-ax/20-spec/spec-004-meeting-note.md:136` — §3.3 역할표 **「회의를 만든 사람」** 행에 「… · **회의 정보 편집**」이 들어 있고,
- 같은 파일 `:137` — **「참석자」** 행에는 회의 정보 편집이 **없다**.

**어긋난 기준(시안 · SSOT 1순위)**

```js
// design/회의실.dc.html:1127
const owner = V === '만든 사람', attendee = V === '만든 사람' || V === '참석자', ...
// design/회의실.dc.html:1190
btnHeadEdit: (planned || settled) && attendee && !st.headEdit,
```

연필(`E09`→`E59` 인라인 편집)은 **`attendee`**, 즉 **참석자 전원**에게 선다. 같은 파일 `:1134` 가 회의록 편집을 `canEditNote = **owner** && …` 로 따로 좁혀 둔 것과 대비되므로, `attendee` 는 실수가 아니라 **의도된 구분**이다. 시안 리포트도 두 자리에서 같은 말을 한다 — `REPORT-회의실-v2.md:49`(`E09` 「예정에만 × **참석자**」) · `:56`(`E59` 「예정 × **참석자**」).

이 어긋남은 §3.3 한 곳에 그치지 않고 계약 전체로 전파된다 — `work-001:97` 의 `PATCH /api/meetings/{id}` 가 actor 를 적지 않아 구현자는 §3.3 을 읽고 **owner 로 게이트할 것**이다.

**권장 수정** — §3.3 `:137` 참석자 행에 「회의 정보 편집」을 더하고 `:136` 만든 사람 행에서 뺀다(만든 사람은 참석자에 포함되므로 뜻이 좁아지지 않는다). `work-001` §Interface Contract 의 `PATCH` 줄에 「참석자」를 명시한다.

#### F-B — 「기타」 블록: SPEC이 폐기했는데 시안은 여전히 낸다

- `products/sc-ax/20-spec/spec-004-meeting-note.md:160` — 「**안건 없는 회의를 「기타」 블록 하나로 받던 규칙은 폐기한다** — AI가 안건을 세우므로 받을 자리가 필요 없다 (D14 · `X-94` 개정)」
- SPEC 전문에서 「기타」는 이 줄과 §12 R-24(`:466`) 둘뿐이고, **§4.2 안건 도해에도 없다.** 0.3.0 에 있던 「어느 안건에도 안 걸린 것은 「기타」 블록이 받는다」가 0.4.0 에서 삭제됐다.

**어긋난 기준(시안)** — 시안은 「기타」를 **구조로** 갖고 있다(픽스처 데이터가 아니다).

```js
// design/회의실.dc.html:915-916
/* 어느 안건에도 안 걸린 것 — 「기타」는 안건이 아니다 (결론 표시도 지우기도 붙지 않는다) */
const ETC = { id: 'etc', title: '기타', src: null, decided: null, ... };
// :1138
const noteSource = [...agendaLive, ETC];          // 여섯 상태 전부에서 항상 붙는다
// :1273
memoTargets: ... .concat(agendaLive.length ? [] : ['기타'])   // 안건 0건이면 메모 대상도 「기타」
```

**성격을 분명히 해 둔다** — 이것은 SPEC 의 오독이 아니라 **SSOT 1순위(시안)와 2순위(D14)가 서로 어긋나는 자리**다. D14 는 `X-94` 폐기를 명시했고 시안 리포트는 `X-94`·D14 를 어디에서도 언급하지 않는다(시안 파일 mtime 15:21 / fix 브리프 15:22 — 시안이 D14 를 아직 반영하지 못한 것으로 보인다). **고칠 곳은 SPEC 이 아니라 시안일 가능성이 크다.**

SPEC 의 실제 결함은 **그 충돌을 기록하지 않은 것**이다. §12 R-24 는 `SCREENDEF-005` 만 가리키고 **확정 시안이 아직 「기타」를 낸다는 사실은 어디에도 없다.** 지금 상태로 `work-006`(「시안이 정한 것을 그대로 구현한다」)을 실행하면 구현자가 두 문서 사이에서 멈춘다.

**권장 수정** — 둘 중 하나. ① 시안에서 `ETC` 를 걷어내고 SPEC 은 그대로 둔다(D14 를 따르는 길) ② SPEC §4.1-7 을 「`X-94` 의 *안건 없는 회의* fallback 만 폐기하고, 어느 안건에도 안 걸린 줄을 받는 「기타」는 남긴다」로 좁힌다. 어느 쪽이든 §12 에 「확정 시안이 아직 「기타」를 낸다」 한 줄을 더해 충돌을 드러낸다. **어느 쪽인지는 사용자 결정이다(§7-1).**

### 3.3 경미 (WARN)

- **W-1** `spec-004-meeting-note.md:120` · `work-001:98` — 삭제 두 갈래를 「회의까지 지우기 · **붙인 자료만 지우기**」로 적는다. 시안은 `회의록.dc.html:470-471` 이 **[회의록만 삭제]** · **[회의 취소]** 이고 토스트가 `:928-929` 「회의를 **취소**했습니다.」 · 「**회의록**을 삭제했습니다.」다. `REPORT-회의록-v2.md:65-66` 이 「옛 이름 [회의 삭제]」·「옛 이름 [자료만 삭제]」로 **같은 두 갈래의 개명**임을 밝히므로 갈래 수는 맞으나, **둘째 갈래가 지우는 대상**이 「자료」와 「회의록」으로 갈린다. 구현자가 반대로 만들 수 있는 자리다.
  - 정상 참작 — `sc-meeting-spec-fix-brief.md` §3 이 「**제외**: 삭제 확인 모달 문구·버튼명·×」로 이 영역을 명시적으로 뺐고, SPEC `:120` 도 「확인 문구와 버튼 이름은 기획자가 갱신 중이라 이 SPEC이 고정하지 않는다」로 자인한다. 그래서 FAIL 로 올리지 않았다. 다만 **「무엇을 지우는가」는 문구가 아니라 계약**이므로 기획자 갱신 때 함께 맞춰야 한다.
- **W-2** `spec-004-meeting-note.md:155` — 안건 출처를 「직접 입력 · 지난 회의에서 넘어옴 · **AI 정리**」 셋으로 확정했는데, 시안 픽스처는 아직 `회의실.dc.html:906` 이 `src: '세트'` 이고 「AI 정리」가 없다. **위반은 아니다** — 시안 리포트 `M10:516` 이 「실제로는 붙을 수 없는 값일 수 있다 … 고친다(**데이터 한 줄**)」로 스스로 잠정임을 밝혔고 SPEC §12 R-19 가 정리를 요청했다. R-19 회신 때 픽스처를 함께 고치면 닫힌다.
- **W-6** `spec-004-meeting-note.md:185-192` — §4.2-5 상태별 표에 열이 「내용 줄 / 다음 할 일」 둘뿐이라 **결론 표시(`E22`)가 언제 서는지**가 없다. 시안은 `REPORT-회의실:73` 이 「**완료에만.** 「기타」에는 안 붙고, 합성이 안 된 「실패」에도 안 낸다」로 못박았다. 모순은 아니고 **덜 구체적인 것**이다 — 열 하나를 더하면 닫힌다.
- **W-7 (시안 측 부채 · 이번 판정 제외)** — `REPORT-회의실-v2.md` 의 §1 요소표(`:49`·`:50`)와 §3 가시성 표(`:151`)가 §4-6·§4-8 이후 갱신되지 않아 자기모순이다: §1 `:50` 은 `E84` [공유]를 「예정을 뺀 다섯」이라 하는데 §3 `:151` 예정 행에는 「공유」가 남아 있고, §1 `:49` 는 `E09` 를 「예정에만」이라 하는데 §4-8 `:418`·M14② `:512` 는 「예정과 완료 둘」이다. **SPEC 은 두 자리 모두 최신 결정(`btnShare: attendee && !planned` · `btnHeadEdit: planned || settled`)을 정확히 따랐다** — 시안 리포트를 고칠 몫이다.

---

## 4. 축 4 — 결정 반영 (D12~D15) · **PASS**

| # | 결정 | 자기보고 | 실측 | 판정 |
|---|---|---|---|---|
| D12 | 안건 본문 = 줄 목록 · 종류 배지 없음 · 메모도 줄 · AI 는 줄 증분 · 합성은 줄 병합 | §4.2·§6-5·§7.1·§8-5·§10-7 | §4.2 `:164-194` 전체(도해 포함) · §6-5 `:305` · §7.1 출력 `:325` 「**줄을 더하거나 고치는 것이지 문단을 다시 쓰는 것이 아니다**」 · §8-5 `:343-348` 네 갈래 · §10-7 `:383` · §2.1 「줄의 종류 배지」 확정 제외 `:80` | ✓ 6곳 |
| D13 | 상태 여섯 · 「정리됨」→「완료」 | §5.1 전체 | §5.1 `:200`·전이도 `:203-206`·표 `:209-216` 전부 「완료」. **본문 전체에서 「정리됨」 0건**, §12 R-25(`:467`) 인용 1회만 — 브리프의 「§12 인용 제외」 조건 충족 | ✓ |
| D14 | ① AI 즉시 안건(출처 「AI 정리」) ② 참석자 추측 없음 ③ 제목은 합성 때 AI 후보 ④ 회의 정보는 예정·완료 머리 편집(장소 글자·조직 제거) | §3.1·§4.1·§8-5·§10-18 | ① §4.1-2 `:155`·§4.1-6 `:159`·§7.1 출력 `:325` ② §3.1-6 `:111` 「**참석자를 원문에서 추측하지 않는다**」 ③ §3.1-6 `:112`·§8-5 `:348` ④ §3.1-2 `:106`·§3.1-7 `:113-118`·§10-18 `:394` | ✓ 4/4 |
| D15 | 디자인이 정본 · 기획 차이는 §12 한 줄 목록만 | §0 · §12 | §0 `:25` 「**화면의 정본은 확정 시안이다** … §12에 **한 줄 목록으로 통보만** 하며 본문에서 다시 따지지 않는다」 · §0 근거표 `:29-30` 이 시안 2본을 1·2행에 올림 · §12 머리 `:437` 동일 선언 · R-13~R-30 전부 한 줄 | ✓ |

`10-decision.md` 로그도 D12·D13·D14·F-1·「화면 정본」 행을 전부 담았고 `SCAX-ESC-006` 을 미완료 board 에 신설했다.

---

## 5. 축 5 — WP 완결성 · **PASS(WARN 4)**

### 5.1 6건 판정

| WP | (a) Covers | (b) 계약 정합 | (c) 원형 인용 | (d) 폐기/계승 | (e) 검증 | 판정 |
|---|---|---|---|---|---|---|
| 001 도메인·API | SPEC §3·§4·§5.1 실재 | 엔드포인트 9종, SPEC 과 모순 0 | — (도메인은 원형 불필요) | §11.1 note version 폐기 명시 | 8건 · 「안건 21번째 거부」 등 실행 가능 | ✓ |
| 002 스트림 중계 | §5.2~§5.5 | **`pause`/`resume` 없음**(`:107`) · 프레임 6종 · 거절 5종 — §5.3 과 **완전 일치** | 코드 4파일 + `spec-007` §4 — **전부 실재 + 주장 일치**(아래) | 직결·credential 폐기 명시 | 9건 · 「프론트 번들에 provider 주소·키가 없다」 등 검사 가능 | ✓ |
| 003 메모·AI 배치 | §6·§7 | 배치 출력 스키마(`:96`)가 §7.1 출력·검증과 일치 | `meeting_batch_service.py` + `spec-007` §4 「AI 배치 계약」 | `provisional` 슬롯 폐기·구조화 출력 계승 명시(`:61-62`) | 「세션 없으면 제출 안 함」 등 | ✓ |
| 004 합성·승격 | §8·§9 | `end`·`finalize`·줄 CRUD·승격·export | `meeting_finalize_service.py` · **「① 재전사 단계는 옮기지 않는다」로 D3 차이를 명시** | — | — | ✓ |
| 005 자료·공유 | §5.7·§3.2 | 6 엔드포인트 · 알림 제외 명시(`:89`) | — | — | Phase 2 | ✓ |
| 006 화면 | 시안 2본 | WS 프레임 재기술이 §5.3·WP-002 와 일치 | 시안 원본 경로 | `MeetingDrawer.tsx`·`liveTranscription.ts` **삭제 대상** 명시 | 「예약에 반복 칸이 없다」 등 시안 대조 가능 | ✓ |

### 5.2 (c) 원형 인용 — 경로 `ls` + **코드 주장까지** 대조

4개 경로 전부 실재한다.

```
app/back/api/meeting_stream_router.py         4153B
app/back/service/meeting_stream_service.py   18907B
app/back/service/meeting_batch_service.py    41876B
app/back/service/meeting_finalize_service.py 27670B
```

WP-002 가 그 파일들에 대해 **주장한 내용**도 실측과 맞다 — 지어낸 인용이 아니다.

| WP-002 주장 | 원형 코드 | 판정 |
|---|---|---|
| `:71` 「**첫 프레임 5초 인증**만 하는 얇은 층」 | `meeting_stream_router.py:39` `_AUTH_TIMEOUT_SEC = 5.0` · `:3` 「여기서 하는 것은 **첫 프레임 인증뿐**」 | ✓ |
| `:72` 「**오디오 append 먼저 → 전달**」 | `meeting_stream_service.py:8` 「① `storage.append` → ② Soniox 전달 순」 · `:277` 동일 docstring | ✓ |
| `:72` 「`finally` 종료 처리」 | 같은 파일 `:15`·`:145`·`:253` 「**업스트림을 반드시 닫는다**」 | ✓ |
| `:72` 「세션 레지스트리」 | 같은 파일 `:156` 「레지스트리에서 뺀다」 | ✓ |

### 5.3 (f) `30-work.md` 동기 — 3표 모두 일치

Status Board 6행 · WP List 6행 · Spec Coverage `SCAX-SPEC-004 → WP-001~006` — **WP 파일 6건과 1:1.** lint 의 map 동기 3검사(ERROR 급)가 통과한 것으로 재확인된다. 의존 순서(`WP-001` → `002` → `003` → `004` · `005` 병렬 · `006` 후행)도 6건 frontmatter `depends_on` 과 어긋나지 않는다.

### 5.4 빠진 범위 · 경미 (WARN)

브리프가 예시한 셋은 **전부 덮였다** — 목록 삭제 두 갈래(`work-001:36`·`work-006:100`) · 자동 취소 `X-185`(`work-001:38` + Phase 3 검증 `:145`) · 내보내기(`work-004:98`·`work-006:38`). 아래 넷이 남는다.

- **W-3** — **화자 이름 매핑의 백엔드를 아무 WP 도 갖지 않는다.** `work-002:49` 가 「화자 이름 매핑 화면 → SCAX-WP-006」으로 밀었는데 `work-006:72` 는 「**프론트라 DB를 건드리지 않는다**」다. SPEC §5.4-5(`:274`)의 계약 — 「붙인 목소리가 남아 **다음 회의부터** 이름으로 뜬다 … 그러면 **지난 회의의 표시도 함께 바뀐다**」 — 은 목소리↔사람 매핑의 영속화와 소급 반영이 필요한 백엔드 일이다. `MeetingSpeakerName` 류 entity 가 어느 Domain/Schema 절에도 없다. → WP-002 Scope 에 넣거나 별도 WP.
- **W-4** — `POST /api/meetings/{id}/end` 가 **두 WP 에 선언**됐다: `work-001:99`(「상태 전이」) · `work-004:94`(「전이 + 스트림 닫기 + 합성 job 등록. 즉시 응답」). 내용은 모순이 아니라 후자가 상세하지만 **소유 WP 가 표기되지 않았다.** `work-001` §Scope 제외가 「종료 합성 → WP-004」라 의도는 읽히나, 한 줄로 못박는 편이 낫다.
- **W-8** — WP 6건의 `owner` 가 전부 `TBD` 이고 Status Board Owner 도 `TBD` 다. `30-work.md` 가 「Owner가 전부 `TBD`다 — 다음 review의 open item이다」로 **스스로 적어 두었으므로** `document-pipeline.md` §Owner 지정 규칙을 어긴 것은 아니다. 다음 review 의 open item 으로 남긴다.
- **W-9** — `work-006:39` 가 예약 모달의 **회의실 「가능」 판정** 화면을 담는데, 그 BE 계약을 내는 WP 가 없다. `work-001:47` 이 「회의실 「가능」 판정과 the Connect 예약 쓰기 → SCAX-SPEC-007 소유」로 라우팅했으므로 **경계 자체는 옳다** — 다만 SPEC-007 에 covering WP 가 없어(`30-work.md` Spec Coverage `SCAX-SPEC-007 → —`) WP-006 Phase 1 이 착수 시점에 막힐 수 있다. 일정 리스크로만 기록한다.

---

## 6. 축 6 — 문서 규약 · **PASS(WARN 1)**

### 6.1 frontmatter — 10/10

| 파일 | 확인 |
|---|---|
| `spec-004` | `id`·`doc_no: SCAX-DOC-7`(유지)·`type: spec`·`title`·**`status: draft`**·`owner: TBD`·`last_updated: 2026-09-10`·**`version: 0.4.0`**(0.3.0→0.4.0 ✓)·`sources` 4항 | 
| `work-001`~`006` | `id`(WP-001~006)·`doc_no`(**DOC-14~19**)·`type: work`·`title`·`status: proposed`·`owner: TBD`·`last_updated`·**`covers: [SCAX-SPEC-004]`**·`depends_on` — 6건 모두 최소셋 충족 |
| `10-decision` · `30-work.md` · `system.md` | 기존 필드 유지 + `last_updated` 갱신 |

- **`doc_no` 전역 유일성** — sc-ax 현행 전수 수집 결과 **중복 0건**(DOC-1~19 각 1회).
- **`templates/work.md` 절 구조** — 템플릿의 11절(`메타`·`Scope`·`Code Surface`·`Domain / Schema`·`의존 관계`·`Interface Contract`·`Execution`·`Pre-deploy Check`·`Rollback`·`Open Issues`·`Related`)이 **6건 모두 같은 순서로 실재.**
- **Phase `Status:` 형식** — 17개 전부 `- **Status**: TODO` 로 줄끝 앵커까지 깨끗하다. `document-pipeline.md` §자동 검증의 「형식 위반 ERROR」에 걸리지 않는다.
- **`sources:` 경로** — 검수 1 W-7 대로 판본 주석이 붙었다(아래 WARN 참조).
- **§14 관련 문서 링크** — SPEC 5건 + `system.md` + `30-work.md` **7개 전부 실재.**

### 6.2 경미 (WARN)

- **W-5** `products/sc-ax/10-decision.md` — `## 로그` 아래 소제목이 **「2026-09-09 · 09-10 — 회의·미팅노트 구조 결정 (D1~D11)」**인데 표에는 D1~D14 + F-1 + 「화면 정본」 행까지 열여섯 행이 있다. 0.3.0 때의 제목이 남은 것이다. → `(D1~D14 · F-1)` 로 고칠 것.
- (참고 · 위반 아님) `sources:` `:13`·`:14` 는 여전히 **리포에 없거나 다른 판본인 경로**를 가리키지만, 이번엔 각 항목에 「리포 판본 v0.1.2/2026-08-25 — 기획자 반영 대기」·「리포에 아직 없음」이 **명시**돼 오해 소지가 없다. 검수 1 W-7 의 요구를 충족한다.

---

## 7. 사용자가 정해야 할 것 (최소)

1. **「기타」 블록을 남기나 없애나** (F-B). 시안은 여전히 내고 D14 는 폐기를 정했다. ① 시안에서 `ETC` 를 걷어낸다(D14 를 따름) ② SPEC §4.1-7 을 「`X-94` 의 *안건 없는 회의* fallback 만 폐기, 어느 안건에도 안 걸린 줄을 받는 「기타」는 유지」로 좁힌다. **검수는 ①을 권한다** — D14 아래에서는 AI 가 안 맞는 발화에 새 안건을 세우므로 받을 자리가 실제로 남지 않는다. 다만 시안이 정본이라 검수가 정할 수 없다.
2. **삭제 둘째 갈래가 지우는 것**(W-1) — 「붙인 자료만」인가 「회의록만」인가. 기획자가 기획안을 갱신 중인 영역이라 그 회신에 붙여 한 번에 정하면 된다.

(F-A 는 결정 사항이 아니다 — 시안이 `attendee` 로 명확하므로 SPEC 을 고치면 닫힌다.)

---

## 8. lint 원문 요약 + git status

```
$ python3 scripts/lint-pipeline.py --strict
(exit code 0)
0 error, 264 warning

$ grep -i "sc-ax" <출력 266줄>
(무출력)
```

- **`products/sc-ax/` 범위: ERROR 0 · WARN 0.** WP 6건이 새로 들어왔는데도 0인 것은 `doc_no` 부여 · map 3표 동기 · 파일명 NNN↔ID 일치 · Phase `Status:` 형식이 모두 맞았다는 뜻이다.
- **무관** — WARN 264건은 전부 `products/selly/` 의 기존 부채(`doc_no` 누락 · `20-spec.md`/`30-work.md` frontmatter 부재)이고 `origin/main` 에서 이미 같은 상태다. 검수 1 과 동일한 수치다.

```
$ git status --porcelain
 M products/sc-ax/10-decision.md
 M products/sc-ax/20-spec/spec-004-meeting-note.md
 M products/sc-ax/30-work.md
 M products/sc-ax/40-architecture/system.md
?? products/sc-ax/30-work/work-001-meeting-agenda-domain-api.md
?? products/sc-ax/30-work/work-002-meeting-stream-relay.md
?? products/sc-ax/30-work/work-003-memo-ai-batch.md
?? products/sc-ax/30-work/work-004-final-merge-followup.md
?? products/sc-ax/30-work/work-005-materials-sharing.md
?? products/sc-ax/30-work/work-006-meeting-screens.md
```

브리프 §2 목록 그대로다. **이 검수는 리포 파일을 한 글자도 고치지 않았다** — 산출물은 이 리포트 1개뿐이다.

---

## 부록 — 확인한 것 / 확인 안 한 것

- [x] 시안 정본 2본을 **리포트 서술이 아니라 `renderVals()` 코드로** 판독해 가시성·권한 조건 대조
- [x] 금지어 6종 전수 grep + 히트 6건 개별 판정
- [x] 검수 1 F-1·WARN 11 + ESC 건 **12항목 개별 대조**
- [x] D12~D15 를 SPEC 본문 행 번호로 실측
- [x] task-management 원형 **경로 4건 `ls` + 코드 주장 4건 실측 대조**
- [x] WP 6건 × 6기준(Covers·계약·원형·폐기계승·검증·map동기)
- [x] `doc_no` 전역 유일성 · `templates/work.md` 절 구조 · Phase `Status:` 형식 · §14 링크 실재
- [x] 엔드포인트 중복 스캔(WP 간 계약 충돌 탐지)
- [ ] **확인 안 함** — `ax-workspace` 실제 코드(브리프 §6 금지 · `survey-01` 인용을 신뢰) · 시안의 브라우저 렌더(정적 판독만) · `확정사항.md`/`내부-결정지도.md` 정본(리포·reference 어디에도 없음)
