# 검수 1 — SPEC-004 0.3.0 리포트

- 작성: 2026-09-10 / `reviewer_spec`
- 대상: 워크트리 `/Users/kknaks/orca/workspaces/mediness-mediness/sc-meeting-spec` · 브랜치 `kknaksss/sc-meeting-spec` · **커밋 없음(working tree 변경 3파일)**
- diff 범위: `git diff origin/main`(=59cd71302) — `products/sc-ax/10-decision.md`(+26/-6) · `products/sc-ax/20-spec/spec-004-meeting-note.md`(+426/-87 상당) · `products/sc-ax/40-architecture/system.md`(+5/-3). 총 3 files, 447 insertions, 80 deletions
- 실행한 검사: `git diff origin/main --stat` · `git status --porcelain` · `python3 scripts/lint-pipeline.py --strict` · SPEC 인용 ID 127개 전수 대조(reference `plan-004` + `screen-005` grep) · 원형 `spec-007-meeting-live` §4 / `spec-008-meeting-close` §4 대조 · `survey-01-ai-provider-report` §4.1·§4.4·§6.2 대조

---

## 0. 총평

# FAIL — 재발주 필요

FAIL 1건 · WARN 11건. **문서 골격·결정 반영·기획 ID 정합·lint 는 전부 깨끗하다.** 유일한 FAIL 은 `§5.2`~`§5.3` 에서 **기획 정본에 없는 사용자 조작(일시정지·[재개])과 확정 노출 문구를 SPEC 이 신설**한 것이다 — task-management `spec-007` 의 WS 계약을 옮기면서 그 제품의 UX 까지 함께 실려 왔다. 브리프가 명시적으로 금지한 축(「화면 표면을 네가 정하지 않는다」·「없는 값은 만들지 말고 Open Question 으로」)이라 WARN 으로 내리지 않았다. 수정 범위는 좁다 — 해당 셀 3개 + 한 문장을 §13 Open Question 으로 옮기면 해소된다.

| # | 축 | 판정 | 한 줄 |
|---|---|---|---|
| 1 | 범위 준수 | **PASS** | 변경 정확히 3파일. `00-planning/`·`30-work/`·ERD·타 spec 무변경. allowed_paths 이탈 0 |
| 2 | 결정 반영 (D1~D11) | **PASS** | 11건 전부 SPEC 에 도달. 뜻이 바뀐 것 없음. D8 번들 중 `DEC-015`(메뉴 이름) 한 조각만 미반영 → W-5 |
| 3 | 기획 정합 | **FAIL** | 인용 ID 127개 전수 실재·의미 일치. 그러나 §5.2·§5.3 이 기획에 **없는** 조작·문구를 신설 → F-1 |
| 4 | §12 개정 요청 타당성 | **PASS(WARN 2)** | 12건 중 10건 reference 본에서 실재 확인. R-6 근거 약함 · R-8 부분 오독 · R-9 열거 불완전(누락 3건) |
| 5 | 계약의 완결성 | **PASS(WARN 3)** | 상태 6개·전이·실패 경로 완결. 원형은 **복사가 아니라 옮김**(권한 모델·상태명·구독 역할 재구성 확인). WS 종료 계약 누락 등 3건 |
| 6 | 문서 규약 | **PASS(WARN 1)** | frontmatter 최소셋 충족 · `status: draft` 유지 · `version` 0.2.0→0.3.0(리포 관행 일치) · lint sc-ax ERROR 0 / WARN 0. `sources:` 가 리포에 없는 파일을 가리킴 → W-7 |

---

## 1. 축 1 — 범위 준수 · **PASS**

```
$ git status --porcelain
 M products/sc-ax/10-decision.md
 M products/sc-ax/20-spec/spec-004-meeting-note.md
 M products/sc-ax/40-architecture/system.md
```

확인한 것:

- 변경 파일이 planner 브리프 §5 의 3개와 **정확히 일치**. 그 밖의 파일 0건, untracked 0건.
- `products/sc-ax/00-planning/` 무변경 — 리포의 `plan-004-meeting-note.md` 는 `document_version: 0.1.2 / updated_at: 2026-08-25` 그대로이고, `screens/modules/` 에는 `screen-001~004` 만 있어 `screen-005` 를 만들지 않았음을 확인.
- `30-work/`·`40-architecture/erd.md`·`21-html/`·타 spec·`rules/`·`context/`·`templates/`·타 제품 무변경.
- `40-architecture/system.md` 는 `§4 Meeting provider와 worker`(63~72행 범위)와 frontmatter `last_updated` 만 바뀌었다. 다른 절 무변경 — 브리프 「§4 만」 준수.

위반 없음.

---

## 2. 축 2 — 결정 반영 (D1~D11) · **PASS**

D1~D9 는 planner 브리프 §2 표, D10·D11 은 `_RESUME.md:32-33`(2026-09-10 terminal send) 을 기준으로 대조했다.

| # | 결정의 핵심 | SPEC 반영 자리 (실측) | 판정 |
|---|---|---|---|
| D1 | 두 트랙 + **자동** 합성 · **채택 없음** | §1:50(「사람이 「채택」하는 단계는 없다」) · §6 메모 트랙 전체 · §7 AI 트랙 전체 · §7.2 두 트랙의 경계 · §8-2 · §10-5·6·7 | ✓ 뜻 보존 |
| D2 | Soniox · **백엔드 중계** | §5.2-1·2(「STT provider 세션은 서버가 소유한다」) · §5.3 · §10-9 · system.md §4 | ✓ (본문은 provider 를 추상화 — 아래 주 참조) |
| D3 | 재전사 **완전 삭제** | §2.1 표(「이 SPEC이 없앤다」) · §5.4-3 · §8-4 · §10-11 · §11.1 표 3행 · system.md §4(「final transcription path는 두지 않는다」) | ✓ 6곳에서 일관 |
| D4 | 오디오 원본 서버 보관 · 화면 미노출 | §5.5 전체 · §10-12 · §12 R-5 · §13 `OQ-302` | ✓ |
| D5 | 회의 백엔드 플로우 = task-management | §5.3(원형 명시) · §7.1 · §8 | ✓ |
| D6 | 참석자에게 WS push | §5.2-6(구독 전용 역할 신설) · §5.3 `role:"subscribe"` · §13 `OQ-310` | ✓ |
| D7 | 300분 한도 | §5.6 · §10-20 · §12 R-11 | ✓ |
| D8 | 기획이 정본 (7조각) | 판 없음·덮어쓰기 §8-8·§10-2 / 확정 표시 없음 §2.1 / 재생성 없음 §2.1·§5.1·§10-19 / 수정은 만든 사람 §3.3·§8-8 / 「녹음」 금지 §5.1:144 / 상태 6개 §5.1 / **메뉴 「회의 목록」(DEC-015) — 없음** | ⚠ 6/7 → W-5 |
| D9 | Claude Design 참고용 | SPEC 전문 grep: `Claude Design`·`시안`·`디자인` **0 hit** | ✓ 올바르게 미참조 |
| D10 | 데모에서 알림 제외 | §1:55(「알림은 이번 데모 범위 밖이다」) · §2.2 표 4행(알림 셋·`CMP-104`·알림 진입 경로·`MOD-105-T04`) · §3.2-4 | ✓ 가장 촘촘히 반영 |
| D11 | 데모에서 `CMP-103` 제외 | §2.2 표 5행째(「회의 화면은 상단 바 없이 페이지 제목부터」·「탭 순서에서도 뺀다」) | ✓ |

특히 확인한 것:

- **D1 의 「자동」이 흐려지지 않았다.** §8-2·§10-6 이 「종료 시 한 번, 자동으로」를 못박고, §11.1 이 현행 `adopt_summary`(사람의 채택 조작)를 폐기 목록에 올려 두 방향에서 막았다.
- **D8 의 「기획이 정본」이 D1·D3 과 충돌하지 않는다.** §2.2 가 D10·D11 을 「범위 결정이지 기획 개정 요청이 아니다」로 갈라 두어, 기획을 고치라는 것(§12)과 이번 데모만 빼는 것(§2.2)이 섞이지 않았다. 설계 판단으로 옳다.
- **D2 의 provider 이름 처리** — SPEC 본문은 `Soniox` 를 한 번도 쓰지 않고(§12 R-2 인용문 안에만 등장) 전부 「STT provider」로 쓴다. 문서 서두의 「특정 provider는 사용자 계약이 아니다」와 `system.md` §4(Soniox 명시)로 층이 갈리므로 **계층 규율상 옳은 처리**다. 위반 아님. 다만 §12 R-2 는 기획서에 「Soniox」를 박으라고 요구하므로, SPEC 본문과 개정 요청의 추상화 수준이 다르다는 점만 기록해 둔다.

---

## 3. 축 3 — 기획 정합 · **FAIL**

### 3.1 인용 ID 전수 대조 — 통과

SPEC 본문의 백틱 인용 ID를 정규식으로 전수 추출해(`FTR-004-NN` · `SCR-10x-Exx/Ixx/Txx` · `MOD-10x-*` · `CMP-1xx` · `TXT-xxx` · `NAV-xxx` · `X-xxx` · `DEC-xxx` · `OQ-xxx` · `RSK-xxx`) reference 두 본과 대조했다.

- **127개 중 117개가 reference 본에 실재.** 미검출 10개는 전부 `OQ-301`~`OQ-310` 으로, §13.2 가 「이 SPEC이 새로 여는 것」으로 스스로 선언한 신규 ID다 — 정상.
- 의미 일치를 표본 검증한 것(전부 일치): `FTR-004-01`~`11`(plan §5 표) · `SCR-106-E26`(「예정」에 탭 없음) · `E29`/`E31`/`E35`/`E71`/`E77`/`E80`~`E83`/`E85`/`E54`/`E55`/`E59`~`E62`/`E65`/`E66`/`E73`/`E40`~`E43` · `I03`/`I05`/`I11`/`I15`/`I16`/`I46`/`I55` · `T05`/`T10`/`T34`/`T35` · `NAV-208` · `TXT-002` · `MOD-105-E05`/`T02`/`T04` · `MOD-102-E07`/`E08` · `CMP-103`/`104`/`105` · `OQ-005`/`006`/`201`/`208`/`209` · plan `DEC-002`/`005`/`007`/`009`/`010`/`014`.
- 특히 검증한 세 곳:
  - §8-8 「같은 사람이 다른 탭에서 먼저 저장했으면 … **판정은 안건 단위다**」 — `screen-005:712`(`I16`) 「**판정은 안건 단위다**」 원문 일치. `I16` 본문은 「남이」로 쓰였으나 `T10:826` 이 「고치는 사람은 하나지만(`X-186`) 탭이 여럿일 수 있다」로 스스로 정정해 두었고, SPEC 이 그 정정된 쪽을 따랐다 — 올바른 판단.
  - §5.1 상태표 6행 — `screen-005:800-805` §5.7 「회의 상태별」 표의 「가능한 행동 / 제한된 행동」과 **12칸 전부 일치**.
  - §8-8 「안건당 10000자」 — `screen-005:676` §5.4 `E29` 행 「여러 줄 · 안건당 10000자 · `X-100` 개정」 일치.

### 3.2 위반 (FAIL)

#### F-1 — 기획 정본에 없는 사용자 조작과 확정 노출 문구를 SPEC 이 신설했다

- `products/sc-ax/20-spec/spec-004-meeting-note.md:174` — 「끊기면 그대로 드러내고 사람이 **[재개]를 눌러야** 다시 연다」
- 같은 파일 `:188` — 클라이언트→서버 프레임 「**일시정지** `{"type":"pause","reason":"user"|"mic"}`」
- 같은 파일 `:189` — 「**재개** `{"type":"resume"}`」
- 같은 파일 `:205` — 화면 열: 「만료면 갱신 1회 후 한 번 다시 연다. 또 실패하면 로그인으로」
- 같은 파일 `:208` — 화면 열: **「다른 창에서 기록 중입니다」 · [재개] 비활성**
- 같은 파일 `:209` — 화면 열: 「**일시정지로 드러낸다.** [회의 종료]는 그대로 활성」

**무엇이 어긋났나** — `일시정지` · `재개` · `마이크` 는 `PLAN-004 v1.0.0` 과 `SCREENDEF-005 v1.0.0` **양쪽 전문에서 grep 0 hit** 이다. `SCR-106` §5.3 요소표에 [일시정지]·[재개] 버튼이 없고(머리 조작은 `E07` 회의 시작 · `E08` 회의 종료 · `E09` 수정 · `E11` 다음 회의 예약 · `E84` 공유 · `E65` 내보내기 · `E55` 다시 시도가 전부), 「다른 창에서 기록 중입니다」는 `SCR-106` §5.8 확정 노출 문구 22개 · §6 공통 문구 4개 어디에도 없다.

**어느 SSOT 절과 어긋나나**

1. `screen-005:1372` — 「**문구는 §5.8과 §6에 원문으로 다 적었다. 미확정으로 남긴 문구가 없다.**」 → 화면정의서가 문구 집합의 완결을 선언했으므로, SPEC 이 새 확정 문구를 더하는 것은 정본 침범이다.
2. `screen-005:52` — 「티켓에도 없는 값은 **이 문서가 추정하지 않고 §7 미결로 보낸다**」 → 기획서 자신이 세운 처리 규칙과 반대로 갔다.
3. planner 브리프 §7 — 「**화면 표면(두 트랙을 탭으로 할지 분할로 할지, 문구)을 네가 정하지 않는다.** 기획이 정한 것만 쓰고 나머지는 Open Question.」
4. planner 브리프 §6-4 — 「사실은 §1 SSOT 에서만. **없는 값은 만들지 말고 §13 Open Question 으로.**」
5. 검수 브리프 축 3 — 「기획에 없는 값을 SPEC 이 **지어낸** 곳이 있는가(있으면 FAIL — Open Question 으로 갔어야 한다)」.

**출처 추정(참고)** — 원형 `spec-007-meeting-live.md:305-308·324-328` 이 정확히 이 프레임 4종과 close code 화면 처리를 갖고 있다. WS 계약을 옮기면서 **task-management 의 UX 결정까지 함께 실렸다.** SPEC 은 §5.2-3 에서 오디오 포맷을, §5.4-2·§7.1 에서 블록 경계·트리거 수치를 각각 `OQ-306`·`OQ-307` 로 정확히 미룬 전례가 있으므로, 같은 처리를 이 항목에 적용하지 않은 것은 일관성 결여이기도 하다.

**권장 수정** (SPEC 재작성 불필요 — 국소 수정)

1. `:174` 를 「끊기면 그대로 드러낸다. **다시 여는 조작의 화면 표면은 미정이다**(§13 `OQ-3xx`)」로 바꾼다.
2. `:188`·`:189` 의 `pause`/`resume` 프레임은 **WS 계약으로 남겨도 된다**(원형 계승이고 서버 동작이다). 다만 「이 프레임을 무엇이 트리거하는지의 화면 표면은 기획 소유·미정」 한 줄을 붙인다.
3. `:205`·`:208`·`:209` 의 **화면 열에서 문구와 버튼 이름을 걷어내고** 서버 거동만 남긴다(예: `:208` → 「이미 살아 있는 업스트림이 있음을 알린다 — 안내 문구와 조작은 §13 `OQ-3xx`」).
4. §13.2 에 `OQ-311`(가칭) **「스트림 일시정지·재개·중복 연결의 화면 표면과 문구 — 기획 소유」** 를 추가하고, §12 에 개정 요청 R-13 「`SCREENDEF-005` `SCR-106` §5.3·§5.8 에 스트림 중단·재개 조작과 문구를 더한다」를 추가한다.

### 3.3 경미 (WARN)

- **W-1** `spec-004-meeting-note.md:41` — 「`PLAN-004` §4.1 In Scope와 같다.」 그런데 바로 아래 §1 목록은 「**실시간 전사**」(`:47`)를 In Scope 로 세운다. `plan-004:109` 는 「음성 인식 엔진 선택·연동」을 **§4.2 Out of Scope · 확정 제외(`X-197`)** 로 두었다. SPEC 이 §12 R-2·R-4 로 이 어긋남을 이미 신고했으므로 내용상 문제는 아니나, **「같다」는 문장이 사실이 아니다.** → 「`PLAN-004` §4.1 In Scope 를 따르되, 실시간 전사는 §12 R-4 의 개정 요청 아래에서 In Scope 로 둔다」로 고칠 것.
- **W-2** 같은 파일 `:66` — 「`PLAN-004` §4.2 Out of Scope와 같다.」 실제로는 plan §4.2 의 8행 중 **「음성 인식 엔진 선택·연동」 1행이 빠졌고**(의도적이나 표에서 사라져 독자가 대조 불가), 「AI 정리 재생성」·「녹음 파일 재전사」·「300분 초과」·`OQ-006`·`OQ-209` 5행이 더해졌다. → 같은 방식으로 문장을 완화하고, 빠진 1행은 「§12 R-2·R-4 로 이관」이라 명시할 것.
- **W-3** 같은 파일 `:122` — §3.3 역할표가 「참석자 … **회의 시작**·종료」로 적었다. `screen-005:584`(`SCR-106-E07`) 의 노출 권한은 「**직원 누구나 (`X-96`)**」이고 `screen-005:186`(`NAV-206`) 의 조건 참조도 `X-96` 이다. SPEC 은 `X-96` 을 **한 번도 인용하지 않는다**(grep 0 hit). §3.2-1 의 「참석자가 아닌 사람은 회의가 있다는 것도 알지 못한다」와 정면으로 갈리는 지점이라, §12 개정 요청 또는 §13 Open Question 으로 올라갔어야 한다. → R-9 계열에 합치거나 별도 항목으로 신고할 것.
- **W-11** 같은 파일 `:154`(§5.1 「진행 중 — 열리는 것: … 공유」) vs `:180`(§5.3 연결 조건 「그 회의의 **참석자**」) — 진행 중에 공유가 열리는데, 그때 공유받은 사람이 실시간 스트림을 받는지 아닌지가 어느 절에도 없다. §3.2-3 은 「회의록과 원문을 읽고」까지만 말한다. → §13 Open Question 후보.

---

## 4. 축 4 — §12 기획 개정 요청 12건의 타당성 · **PASS(WARN 2)**

12건 각각을 reference 본에서 직접 확인했다.

| # | 판정 | reference 본 확인 결과 |
|---|---|---|
| R-1 | **타당** | `plan-004:253`(`DEC-003` 「노트와 요약을 나누지 않고 「채택」 단계도 판 이력도 두지 않는다」) · `screen-005:39`(「**회의록은 한 층이다**」) 원문 일치. 「화면(`E82`·`E83`)은 이미 두 트랙으로 그려져 있어 **문구만** 어긋난다」는 판단도 정확하다 — `screen-005:39` 자신이 「회의가 도는 동안 AI 가 안건마다 채우고 … 던진 메모가 그 안건 요약의 재료가 된다」로 두 흐름을 서술한다 |
| R-2 | **타당** | `plan-004:109`·`:197`·`:254`(`DEC-004` 「우리가 엔진을 고르거나 붙이지 않는다」) · `screen-005:1364`(`X-197`) 전부 실재 |
| R-3 | **타당** | `screen-005:1346`(`OQ-201` 「실시간 회의록을 개발측이 받나 · 대기」) 실재. 「받는다」로 종결하는 것이 D2·D3·D6 의 필연적 귀결 |
| R-4 | **타당** | `plan-004:109`(§4.2 확정 제외) vs `plan-004:197`(§8 연동 요구 「소니웍스 PC 음성 인식 — 발화를 원문으로 받는다」). **같은 문서 안의 실제 모순** |
| R-5 | **타당** | `plan-004:168-178` §7 데이터 요구 표 9행에 오디오/녹음 원본 행 없음(회의·안건·회의록·회의 중 메모·원문·화자 이름·자료·후속업무 후보·열람 관계). `plan-004:187` 불변 조건에만 「녹음 원본은 아무에게도 화면에 내지 않는다(`X-164`)」가 있어, **보관 대상으로 표에 없다**는 지적이 정확 |
| R-6 | ⚠ **근거 약함 → W-8** | `plan-004:152`(B-2) 와 `:155`(B-5) 는 순차 서술이지 모순이 아니고, `screen-005:39` 는 이미 「도는 동안 채우고 … 끝나면 한 벌이 채워져 있다」를 **한 문장으로** 정리해 두었다. 실제로 남는 것은 「`plan-004` §14 `DEC-003` 이 진행 중 채움을 언급하지 않는다」는 **보강 요청**이다. 요청 자체는 무해하나 「문서 안에서 갈린다」는 진술은 과하다 → 문구 완화 권장 |
| R-7 | **타당** | `plan-004:241`(`RSK-001` 「우리가 **손댈 수 없는 값**이라 개설자 메모를 나란히 두어 사람이 보완한다」) 실재. R-2 아래에서 전제가 무너지는 것이 맞다 |
| R-8 | ⚠ **부분 오독 → W-9** | 실재하는 어긋남: `plan-004:225`(§11 녹음 표시 → **`DEC-006`**) vs `screen-005:208`(§4 진행 표시 → **`DEC-007`**) — 같은 규칙에 두 번호. **이 부분은 정확하다.** 그러나 SPEC 이 함께 든 `plan-004:242`(`RSK-002`)의 `DEC-007` 인용은, 그 행의 뒷문장이 「동의 절차는 회의를 여는 쪽의 일」이라 `DEC-007`(참석자 동의) 인용으로 **정합적이다.** → 어긋남을 `screen-005` §4 한 곳으로 좁혀 다시 쓸 것 |
| R-9 | **타당 · 열거 불완전 → W-4** | `screen-005:621`(`E29` 노출 권한 「참석은 쓰고 참석자 누구나 (`X-31`)」) vs `:613`(`E77` 「노출 — **만든 사람만**」) vs `:811-812`(§5.7 관계별 표) — **실재하는 내부 모순**이 맞다. 다만 같은 유형이 3건 더 있는데 빠졌다: ① `:616` `E22` 결론 표시 「노출 — **참석자가 고친다**」인데 같은 셀 본문은 「AI가 내고 **만든 사람이** 고친다」 ② `:624` `E71`·`:715` `I46` 안건 지우기 「노출 — **참석자**」인데 본문은 「편집은 **만든 사람만** 연다」 ③ `:655` `E75` 자료 삭제 「노출 — **참석자**」인데 `plan-004:184` 불변 조건은 「자료를 떼는 것은 **올린 사람**이다(`X-117`·`DEC-014`)」. SPEC 은 ①②를 §3.3 에서 「만든 사람」, ③을 「올린 사람」으로 이미 옳게 썼으므로, **R-9 에 이 세 셀을 더하기만 하면 된다** |
| R-10 | **타당** | `screen-005:678`(§5.4 `E60` 행 「빈 값이면 **[채우기]**가 비활성」) vs `:604`(`E62` 「머리의 **[저장]**」) — 원문 대조로 확인 |
| R-11 | **타당** | `plan-004:201-207` §9 전제·제약 5행 · `screen-005` §7 어디에도 회의 길이 상한 없음(grep 「300」·「상한」 0 hit) |
| R-12 | **타당** | `screen-005:1358` 「열람 판정이 정본과 어긋난다 — **정본은 공개 설정과 참석 두 축, 우리는 관계 하나**」 원문 일치 |

**놓친 어긋남(추가 권장)** — W-3 의 `SCR-106-E07` 「직원 누구나(`X-96`)」 ↔ `X-31` 열람 경계, 그리고 R-9 의 누락 3셀.

**참고(위반 아님)** — R-12 와 `10-decision.md:30` 이 escalation 자리로 **기존 `SCAX-ESC-002`** 를 지목한다. 그 행(`10-decision.md:37`)의 내용은 「HOLD-4·5·6 에 **누가 답하는가** — 회의록 공개 범위 · 기밀 기준 · 권한 단위」로, 주제는 정확히 인접하나 성격이 「누가 답하나」(메타)여서 R-12 의 실질 질문(관계 하나인가 두 축인가)과 층이 다르다. **새 ID 를 만들지 않고 재사용한 판단 자체는 옳다**(§10-decision 규약: 없는 escalation 을 발명하지 않는다). lint 도 통과한다. 분리가 필요한지는 사용자 판단 → §7.

---

## 5. 축 5 — 계약의 완결성 · **PASS(WARN 3)**

### 5.1 원형을 복사했나 옮겼나 — **옮겼다**

`spec-007-meeting-live.md` §4 와 1:1 대조한 결과, 기계적 복사가 아니라 **sc-ax 의 이름·권한 모델로 재구성**한 것이 확인된다.

| 항목 | 원형 (`spec-007`) | SPEC-004 §5.3 | 평가 |
|---|---|---|---|
| 연결 조건 | 「회의가 **본인 것**이고 `status='recording'`」(`:299`) | 「그 회의의 **참석자**이고 상태가 「**진행 중**」」(`:180`) | ✓ 단일 소유자 → 다중 참석자 모델로 옮김 |
| 역할 | 없음(업스트림 하나) | `role:"upstream"\|"subscribe"` 신설(`:186`·§5.2-6) | ✓ **D6 을 위해 새로 만든 것** — 원형에 없는 구조 |
| 상태명 | `recording`·`generating`·`ended` (4상태+플래그) | 「진행 중」·「정리 중」·「정리됨」 (6상태) | ✓ 옮김. 옮겨지지 않는 부분을 `OQ-301` 로 정직하게 남김 |
| 수치 | `5초`·`64KB`·`250ms`·`300자`·`2초`·`120초` | 전부 제거 → `OQ-306`·`OQ-307` | ✓ 실측 전 수치를 계약에 박지 않은 것이 옳다 |
| 거절 사유 | close code `4401`/`4404`/`4409` | 코드 대신 사유·뜻·화면 3열 | ✓ 「없는 회의」→`X-31`·`TXT-002` 「찾을 수 없는 페이지입니다」로 sc-ax 열람 경계에 맞춰 교체 |
| 종료 파이프라인 | ① async 재전사 → ② 최종 회의록(`spec-008:33`) | ①을 D3 로 제거, ② 합성만(§8) | ✓ `10-decision.md` D5 행이 이 차이를 명시 |
| 재시도 | 최종 회의록 실패 **재시도 2회**(자동) | [다시 시도] **사람이 누른다**(§8-7 · `SCR-106-E55` · `NAV-208`) | ✓ sc-ax 화면정의서 쪽으로 옮김 |

### 5.2 권한 모델과의 맞물림 — 통과

- §5.3 연결 조건(참석자)이 §3.2-1(참석자만 연다)·§3.2-2(공유는 열람만)와 정합. **공유받은 사람은 스트림에 붙지 못한다** — §3.2-3(읽기·내보내기까지)와 일관.
- 업스트림 단일성(§5.2-5)이 §3.3 의 「회의를 만든 사람」과 다른 축(「회의를 시작한 사람」)임을 명시하고, 승계 미정을 `OQ-305` 로 남겼다 — 정직한 처리.
- §6-1(메모는 만든 사람 하나)이 `screen-005:618`(`E81` 「쓰기는 만든 사람만」)·`:811-812` 관계별 표와 일치.

### 5.3 상태 6개 전이 — 빠짐없음

§5.1 다이어그램이 커버하는 것: 예정→진행 중→정리 중→정리됨 / 정리 중→실패→[다시 시도]→정리 중 / 예정→취소됨 / 취소됨→(회의록·기록으로 풀림). 「정리됨→정리 중 없음」을 명시적으로 막아(`X-16` 개정) 재생성 금지가 상태기계 수준에서 강제된다. **누락 전이 없음.**

### 5.4 실패·재시도 경로 — 5종 모두 존재

합성 실패→「실패」+[다시 시도](§8-7) / AI 배치 실패→조용히 다음 배치에 합침(§7.1) / AI 세션 열기 실패→회의는 정상 진행(§7.1) / 메모 자동저장 실패→재시도 + 입력 보존(§6-6 · `T34`) / 오디오 적재 실패→스트림 중단(§5.5-3).

### 5.5 경미 (WARN)

- **W-6** `spec-004-meeting-note.md:191-199` — §5.3 서버→클라이언트 표에 **회의 종료 시 스트림이 닫히는 계약이 없다.** 원형은 `spec-008:460` 에서 「`POST /end` … 서버가 하는 일(순서) — **스트림 종료 프레임 · WS 닫기(`1000`)**」로 명시한다. SPEC-004 는 `error` 후 종료(비정상)만 다루고 정상 종료 경로가 비어, 「진행 중 → 정리 중」 전이 시 구독자 연결의 운명이 미정이다. → §5.3 에 정상 종료 행 1개, 또는 §8-1 에 「[회의 종료]는 스트림을 닫는다」 한 줄 추가.
- **W-10** 같은 파일 `:330` — §10 Functional Rules 16번 「오래된 worker 결과는 저장하지 않는다 — **소유권과 fence**를 통과한 것만 쓴다」. `rules/document-pipeline.md` §문서별 추상화 수준은 SPEC 에 「내부 service/repository 구조」를 넣지 않도록 하고, 이 SPEC 자신도 §11.2 에서 durable job lease·fence 를 「[System Topology] §4」 소유로 라우팅했다. **같은 사실이 두 층에 있다**(한 곳 원칙). → §10-16 을 빼거나 「산출물의 멱등성은 architecture 가 소유한다」 수준으로 낮출 것.
- **W-11** (§3.3 에 기재) 진행 중 공유받은 사람의 표면 미정.

---

## 6. 축 6 — 문서 규약 · **PASS(WARN 1)**

### 6.1 frontmatter

| 파일 | 확인 |
|---|---|
| `20-spec/spec-004-meeting-note.md` | `id`·`doc_no`(SCAX-DOC-7 유지)·`type: spec`·`title`·**`status: draft`**·`owner: TBD`·`last_updated: 2026-09-10`·`version: 0.3.0`·`sources` — **최소셋 충족** ✓ |
| `10-decision.md` | `id`·`doc_no`·`type: decision`·`title`·`last_updated: 2026-09-10` — decision 타입은 `status`·`owner` 면제(document-pipeline §공통 필드) ✓ |
| `40-architecture/system.md` | `type: architecture`·`status: draft`·`owner: architecture`·`last_updated: 2026-09-10` ✓ |

- `version` 0.2.0 → 0.3.0: `document-pipeline.md` 의 엄격한 정의(상위기획 사이클 버전)로는 「문서를 고쳤다고 올리지 않는다」에 걸리나, **sc-ax 에는 `00-baseline/planning/{version}-상위기획.md` 자체가 없고**(`00-baseline/` 은 `00-overview.md`~`08-metrics.md` 산문 세트), 형제 `SCAX-SPEC-001` 이 이미 `0.3.0` 인 리포 관행이 있으며, planner 브리프 §6-2 가 명시적으로 올리라고 지시했다. → **위반 아님.**
- `status: draft` 유지: `20-spec.md:48` 의 SPEC List 행도 `draft` — 동기 유지 ✓ (lint 의 map 동기 검사 통과로 재확인).

### 6.2 관련 문서 경로 실재

§14 표의 5개 SPEC 링크(`spec-001`·`005`·`006`·`007`·`010`)와 `../40-architecture/system.md` — **6개 전부 실재** 확인.

### 6.3 경미 (WARN)

- **W-7** `spec-004-meeting-note.md:12-13` — `sources:` 가 `00-planning/plans/plan-004-meeting-note.md (PLAN-004 v1.0.0 · 2026-09-09)` 와 `00-planning/screens/modules/screen-005-meeting-note.md (SCREENDEF-005 v1.0.0 · 2026-09-09)` 를 가리킨다. 그러나 **리포의 전자는 `document_version: 0.1.2 / 2026-08-25` 로 다른 판본이고, 후자는 리포에 존재하지 않는다**(`screens/modules/` 에 `screen-001`~`004` 만). §0 마지막 문단(`:37`)이 산문으로 이 사실을 정확히 밝혀 두었으므로 은폐는 아니나, `sources:` 는 기계 판독 lineage 필드라 그 자체로 사실이 아닌 값을 든다. lint 는 source 경로 실재를 검사하지 않아 잡히지 않았다. → 두 항목에 `(기획자 반영 대기 — 현행 리포 판본은 v0.1.2 / 미존재)` 같은 표식을 붙이거나, 기획자가 올릴 때까지 reference 경로임을 명시할 것.

### 6.4 기존 부채 (이번 판정 제외)

- `log.md` 변경 이력 entry 가 이번 diff 에 없다. `document-pipeline.md` §변경 라우팅은 「decision/spec/work 본문 변경 **PR 이 머지될 때** entry 1건 추가」로 규정하고, planner 브리프 §5 는 변경 파일을 3개로 못박았으므로 **planner 의 위반이 아니다.** PR 을 올리는 코디네이터가 `log.md` 에 `spec-change, decision-change, arch-change` row 를 더할 몫으로 남는다.

---

## 7. 사용자가 정해야 할 것 — 검수로 못 가르는 것만

1. **F-1 의 수정 범위.** 스트림 일시정지·재개를 ① 화면 표면만 걷어내고 WS 프레임은 남길지 ② 프레임까지 통째로 Open Question 으로 내릴지. 검수는 ①을 권하나(원형 계승이자 서버 동작이므로) 데모 범위 결정이 걸려 있다 — **회의 중 마이크를 멈추는 조작이 이번 데모에 필요한가?** 필요 없으면 §2.2 「데모 범위 밖」 표로 내리는 세 번째 길도 있다.
2. **`SCAX-ESC-002` 를 R-12 에 재사용할 것인가.** 기존 행은 「회의록 공개 범위·기밀 기준·권한 단위에 **누가 답하는가**」이고 R-12 는 「열람 판정이 관계 하나인가 두 축인가」다. 같은 escalation 으로 묶을지, `SCAX-ESC-006` 을 새로 열지는 결정권자 라인의 문제다.
3. **`SCR-106-E07` 「직원 누구나(`X-96`)」의 뜻**(W-3). ㄱ) 참석자가 아니어도 남의 회의를 시작할 수 있다 ㄴ) 「바로 시작」으로 자기 회의를 여는 것이 누구나다 — 둘 중 무엇인지에 따라 §3.3 역할표와 §5.3 연결 조건이 달라진다. 기획자 회신이 필요하다.
4. **`version` 필드의 뜻을 sc-ax 에서 확정할지**(6.1). 리포 전체가 `document-pipeline` 의 정의와 다르게 쓰고 있어 언젠가 정리가 필요하다. 이번 건의 차단 사유는 아니다.

---

## 8. lint 결과 원문(요약)

```
$ python3 scripts/lint-pipeline.py --strict
(exit code 0)
0 error, 264 warning
```

- **`products/sc-ax/` 범위: ERROR 0 · WARN 0.** 출력 266줄 전체에 `sc-ax` 문자열이 **0회** 등장한다(`grep -i "sc-ax" ` 무출력). planner 자기보고와 일치.
- **무관** — WARN 264건은 전부 `products/selly/` 의 기존 부채다. 유형 두 가지: `30-work/work-*.md` 등의 `frontmatter 필드 누락: doc_no`, `20-spec.md`·`30-work.md` 의 `YAML frontmatter 없음 (type: map)`. 이번 diff 와 무관하며 `origin/main` 에서 이미 같은 상태다.
- 통과가 확인된 sc-ax 관련 검사: 정의 없는 ID 참조(§12 R-12·`10-decision.md:30` 의 `SCAX-ESC-002` 가 `10-decision.md:37` 표 행으로 정의됨) · SPEC 파일 ↔ `20-spec.md` SPEC List 동기 · Spec Coverage 동기 · 파일명 NNN ↔ 내부 ID 일치 · `doc_no` 전역 유일성(SCAX-DOC-7 유지).

### 워크트리 상태 (리포트 저장 후 재확인)

```
$ git -C /Users/kknaks/orca/workspaces/mediness-mediness/sc-meeting-spec status --porcelain
 M products/sc-ax/10-decision.md
 M products/sc-ax/20-spec/spec-004-meeting-note.md
 M products/sc-ax/40-architecture/system.md
```

planner 의 3파일(M) 그대로다. **이 검수는 리포 파일을 한 글자도 고치지 않았다** — 산출물은 이 리포트 1개뿐이다.

---

## 부록 — 확인한 것 (PASS 근거 체크리스트)

- [x] **린트** — `--strict` 실행, sc-ax ERROR 0 / WARN 0, 타 제품 264 WARN 을 「무관」으로 분리
- [x] **frontmatter** — 3파일 최소셋 · `status: draft` · `doc_no` 보존 확인
- [x] **자리 규칙** — 새 최상위 디렉토리 0개, 기존 구조 안에서만 수정
- [x] **출처** — SPEC 인용 ID 127개 전수 대조(117개 reference 실재 / 10개는 자기선언 신규 OQ). §11 폐기 7건·계승 8건·신설 4건을 `survey-01` §4.1·§4.4·§6.2 의 코드 인용까지 역추적해 확인
- [x] **표·인덱스 정합** — `20-spec.md` SPEC List 행의 status 와 SPEC frontmatter 일치, lint 로 재확인
- [x] **중복** — 새 문서 생성 0건. 기존 SPEC-004 를 제자리 재작성했고 `screen-005` 를 리포에 만들지 않았음을 확인
- [x] **allowed_paths** — 이탈 0
- [x] **결정 D1~D11** — 11건 전수 대조(반영 자리를 SPEC 본문 행 번호로 확인)
- [x] **§12 12건** — 전수 reference 본 대조
- [x] **원형 대조** — `spec-007` §4 WS 계약 7항목 · `spec-008` §4 종료 파이프라인 대조
- [ ] **확인 안 함** — `ax-workspace` 실제 코드(브리프 §7 금지 — `survey-01` 리포트의 인용을 신뢰) · Claude Design 시안(접근 불가) · `확정사항.md`/`내부-결정지도.md` 정본(`X-###`·`D-###` — 리포·reference 어디에도 없음, 사용자 지시로 풀지 않음)
