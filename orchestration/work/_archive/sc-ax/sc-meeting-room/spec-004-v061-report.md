# 개정 리포트 — SPEC-004 v0.6.0 → v0.6.1 (검수 반영) (2026-09-15)

## 한 줄

**FAIL 3 · WARN 6 · 문서에 없던 코드 계약 7을 전부 닫았다.** 모델도 결정도 건드리지 않았다 —
A6·B6·C가 PASS였고 틀린 것은 **문서가 「어제 코드」를 말하던 자리**뿐이었다. 기준 커밋
**`65a580c`**에 고정해 `git show 65a580c:<path>`로 읽고 맞췄다.

대상: `spec-004-meeting-note.md` (1009 → 1050행) · `log.md`(entry 1건). `20-spec.md`는 **변경 없음**(아래 §동기화).
코드·기획 정본 무수정. 커밋·push 없음. **새 D 코드 0 · 새 OQ 0 · `OQ-308` 열린 채.**

---

## FAIL 셋 — 어떻게 닫았나

### F-1 — 자리표시 제목: 한 문서가 두 말을 했다

**닫혔다.** 코드를 정본으로 세 자리를 한 방향으로 맞췄다.

코드 확인 — `backend/.../modules/meetings/application.py:67`에서 `QUICK_START_AGENDA_TITLE = ""`
(주석까지 「**빈 값이다** … 「안건 1」이라 적어 두면 화면 라벨과 겹쳐 「안건 1. 안건 1」로 보인다」로 적혀 있다).

| 자리 | 전 | 후 |
|---|---|---|
| **§4.1-7** `:324-325` | 「**지금 구현은** 값 자리에 「안건 1」이라는 글자를 넣고 … **「안건 1. 안건 1」**로 겹쳐 보인다」 | 「**그 값이 이미 그렇게 서 있다** — 제목은 빈 문자열이다. **0.4.x가** 「안건 1」을 넣어 겹쳐 보이던 것은 **그 값이 사라지며 함께 사라졌다**」 |
| **§12 R-50** `:975` | 「지금 구현이 「안건 1」을 넣고 … **자리표시일 때 무엇으로 보일지를 정해야 한다**」 | 「~~「안건 1. 안건 1」~~ — **그 증상은 해소됐다.** 남은 것은 **빈 제목을 무엇으로 그리나**이고 **화면은 이미 골라 서 있다** … **정해 달라가 아니라 이 선택을 시안에 반영해 달라**」 |
| **§11.4** `:878`·`:898` | (이미 맞았다 — 「서버 쪽 원인은 사라졌다」) | 「**해소됐다**」로 어휘만 정렬 |

**R-50을 철회하지 않고 다시 쓴 이유** — 통보할 것이 **남아 있다.** 「안건 1. 안건 1」 증상은 해소됐지만
**빈 제목을 화면이 무엇으로 그리나**는 여전히 시안에 없고, 화면은 이미 골라 서 있다
(`labels.ts:488` `agendaHeadNo` — 제목이 없으면 「안건 N」을 **마침표 없이** 내고, `:498`
`agendaTitleEmpty: "제목 없음"`을 흐린 말로 둔다). 전제만 갈아 끼우고 **요구를 「정해 달라」에서
「반영해 달라」로** 바꿨다. 철회하면 시안이 영영 그 선택을 모른 채 남는다.

### F-2 — §11.4의 「남은 차이」와 R-52: 그 자리는 이미 섰다

**닫혔다.** 코드 확인 — `AgendaBlock.tsx:133-145`가 제목 `span`을 `InlineText`로 열고,
`MeetingDetailPage.tsx:1167` `titleEdit={agendaAlways ? … : null}`, `labels.ts:490`
`agendaTitleEdit: "안건 제목 고치기"`. 커밋 `ca6ec4a`(2026-09-15) *「안건 제목을 제자리에서 고친다 —
입력칸이 나타나지 않는다」*.

- **§11.4** `:888-896` — 「**남은 차이 — 계약이 말하는데 화면에 자리가 없는 것 하나**」를
  **「남은 차이 — 없다」**로 바꾸고, 그 자리가 **새 컨트롤을 세우지 않는 방식**(제목을 이고 있던 글자가
  그 자리에서 칸이 된다)으로 풀렸다는 것과 **왜 디자인 결정을 기다릴 것이 없는지**를 적었다.
  여는 조건이 「지금 보는 벌의 `can_edit_agendas`」 하나라는 것(AI 벌은 눌러도 안 열린다)도 함께.
- **§12 R-52** `:974` — 「**어느 자리에 무엇으로 둘지를 정해야 한다**」 →
  **「정본에 없는데 화면에는 섰다 — 시안에 반영해 달라」**. 한 줄을 덧붙였다:
  **「새 컨트롤을 그려 오면 지금 화면과 충돌한다」** — 그것이 이 통보를 사후형으로 바꾼 이유다.

### F-3 — `can_add_agenda`를 가른 근거가 거짓이었다

**닫혔다.** 코드 확인 — `policy.py:45-53`에서 `MEMO_AGENDA_ADDABLE_STATUSES = MEMO_AGENDA_EDITABLE_STATUSES`
(같은 객체), `FINAL_…ADDABLE = FINAL_…EDITABLE`, `AI_…` 둘 다 빈 집합. **세 벌 여섯 칸이 전부 같다.**

`:289-303`을 네 불릿으로 다시 썼다. **필드는 그대로 뒀다**(계약을 되돌리지 마라 — 지시 그대로).

| | 무엇을 적었나 |
|---|---|
| `:292` | **「지금 두 필드의 값은 세 벌 모두에서 같다」** — 사람 벌이 「진행 중」에 열리며(D56) 두 집합이 만났고, **둘을 갈라 놓았던 D45의 근거(「더하기는 회의 중에도, 고치기는 아니다」)는 그때 사라졌다.** 그 문장은 **적힌 시점에 이미 사실이 아니었다**고 밝혔다 |
| `:296` | **「그래도 두 필드로 둔다 — 값이 아니라 뜻이 다르기 때문이다」** |
| `:299` | **⚠ 「화면은 「둘이 다를 수 있다」로 분기를 짜지 않는다」** — 각 값은 자기 동작에만 쓰고, **둘을 견주어 판정하는 코드는 근거가 없다**. 계약 테스트도 둘의 «차이»를 단언하지 않는다 |
| `:302` | **「하나로 접는 것은 이번 판에서 하지 않는다」** |

**내가 판단한 것과 근거** — **접지 않는다. OQ로도 열지 않는다.**

- **접지 않는 이유**: 접으면 **공개된 응답 모양이 바뀌고**(프론트가 두 필드를 읽고 있다) 얻는 것은
  필드 하나뿐이다. 둘은 **서로 다른 동작**을 판정하고 **한 번 갈렸던 적이 있다**(D45) — 다시 갈릴 때
  지금 모양이면 **값만** 바뀌고 계약 모양은 그대로다. 접어 두면 그때 다시 가르는 비용을 낸다.
- **OQ로 열지 않는 이유**: 미결은 **누군가 답을 기다리며 막혀 있는 자리**다. 여기서 막힌 사람이 없다 —
  코드도 화면도 지금 모양으로 돌고, 어느 쪽을 골라도 사용자에게 보이는 동작이 같다.
  리뷰가 걱정한 것은 **「근거 없는 코드가 서는 것」**이었고, 그것은 OQ가 아니라 위 `:299`의
  **금지 한 줄**로 막았다. 미결 장부에 「언젠가 정리할 것」을 쌓지 않는다.

---

## WARN 여섯 처리표

| | 무엇 | 어떻게 닫았나 | 자리 |
|---|---|---|---|
| **W-1** | 메모 줄 편집의 **404**가 없다 | §6-9 실패 코드를 **표 셋**으로 다시 썼다(422·409·404). **「403을 내지 않는다 — 만든 사람이 아닌 «참석자»도 404를 받는다」**를 명시하고, 그 이유(이 모듈은 권한 밖을 「없는 것처럼」 답하는 결 · §3.2-1)를 적었다 | `:572-582` |
| **W-2** | §6-2 「「진행 중」에만 선다」가 §4.0 표와 어긋남 | 주어를 갈랐다 — **「「메모」 탭은 「진행 중」에만 선다 … 벌 자체는 「예정」부터 있다(§4.0 표)」** | `:553` |
| **W-3** | 「취소」에서 게이트가 열리는데 자리가 없다 | 자리 목록에 **「취소된 회의 상세」**를 더하고 **여는 이유**를 적었다 — 취소는 「예정」에서만 걸리므로 사람 벌 안건이 그대로 남아 있고, 고쳐 쓰면 **취소가 풀린다**(§3.1-9). 잠그면 되살릴 길이 좁아진다 | `:316-317` |
| **W-4** | 최종 벌 줄 저장 게이트 `can_edit_note`가 문서에 0회 | §8-9에 세 줄 — **줄 저장의 판정은 `can_edit_note`**이고 안건 게이트와 **다른 필드**이며 **「종료·실패·취소」로 한 칸 넓다.** 「취소」가 §5.1(회의록 편집으로 취소가 풀린다)과 짝이라는 것과, **취소는 「예정」에서만 걸려 최종 벌 안건이 서 있지 않으므로 실제로 닿는 상태는 둘**이라는 것까지 적었다 | `:706-709` |
| **W-5** | 승격 후보의 거름이 셋인데 「하나」라고 셌다 | **「재직 중인 사람 중 답할 수 있는 사람」**으로 고치고(퇴사·재직기간 종료는 조직 데이터가 거른다), **정렬**(참석자 순서 그대로 먼저 · 그다음 이름순 · 화면이 다시 정렬하지 않는다)을 한 줄 더했다 | `:775-776` |
| **W-6** | 프레임 필드 표기가 snake/camel 혼재 | §5.3에 **「표기가 두 층이다」** 한 불릿 — 최상위 키는 **camelCase**, 실린 줄·안건 객체는 **상세 응답과 같은 snake_case**. 「화면이 상세 응답과 같은 코드로 그 객체를 그린다」까지 | `:488-490` |

---

## 새로 적은 계약 일곱 — 어디에 넣었고 왜 거기인가

전부 **`65a580c` 코드에서 직접 확인**하고 적었다.

| # | 계약 | 넣은 자리 | 왜 거기인가 |
|---|---|---|---|
| 1 | **안건을 세울 때 벌을 고를 수 없다 — 상태가 정한다** (예정·진행 중·취소 → 사람 벌 / 종료·실패 → 최종 벌 / 정리 중 없음) | **§4.1-6** 게이트 표 아래 `:312-314` | 표가 「어느 벌이 열리나」를 말하는 바로 그 자리다. 표만 있으면 **클라이언트가 track을 보낼 수 있다고 읽힌다** — 「고를 것이 없다」를 표 옆에 붙여야 그 오독이 안 생긴다 |
| 2 | **사람 벌 안건에는 stale 판정이 없다** — 마지막에 고친 사람이 이긴다 | **§4.1-6** `:320` | 사람 벌 편집 게이트를 말하는 자리이고, **§8-9의 최종 벌 stale 판정과 대비**해야 뜻이 선다. 이유도 적었다 — 판정을 두면 **어긋났을 때 무엇을 보여 줄지**가 따라붙는데 그것이 `OQ-308`이다 |
| 3 | **`title_placeholder`가 상세 응답에 실린다** | **§4.1-7** `:326` | 자리표시를 정의하는 절이다. R-50이 기획에 넘기는 「빈 제목 표시 규칙」이 **딛는 값**이라 그 옆에 있어야 한다 |
| 4 | **제목을 고치면 `title_placeholder`가 거짓이 된다** | **§4.1-7** `:327` | 3과 한 짝 — **표시 규칙의 수명**을 정하는 값이라 같은 불릿에 이어 적었다 |
| 5 | **최종 안건 20 초과는 스키마가 먼저 막고 적재가 조용히 버린다** | **§8-5** `:668-670` | 합성이 최종 벌을 짓는 자리다. **§8-6의 「줄을 버리지 않고 시도를 실패시킨다」와 결이 반대**라 그 차이를 적었다 — 빈 근거는 스키마를 지나온 뒤 결박에서 «생기는» 것이라 앞에서 못 막지만 **개수는 스키마가 셀 수 있고**, 앞에서 막히는 것을 뒤에서 또 실패시키면 **같은 위반이 두 가지 답**을 낸다. 뒤쪽은 **도달하지 않는 방어**임을 밝혔다 |
| 6 | **메모 줄 게이트가 닫혀 있으면 409** | **§6-9** 거절 표 `:576` | W-1의 404와 **한 표에 있어야** 화면이 셋을 갈라 다룰 수 있다 |
| 7 | **`DELETE …/lines/{lineId}`는 204** | **§6-9** `:570` | 같은 이유 — 409·422가 적혀 있는데 성공 코드만 없어 층이 불균등했다 |

---

## grep 전수 결과

`spec-004-meeting-note.md` 기준. 발주가 지정한 일곱 낱말. 판 이력 한 줄(`:41`)은 이력 서술이라 제외.

| 낱말 | 건수 | 확인 |
|---|---|---|
| **`자리표시`** | 10 | ✅ **한 방향.** 「AI가 채우던 규칙 폐기」(`:162`·`:322`·`:633`·`:878`) · 「제목 값은 빈 값」(`:323`·`:324`) · 「`title_placeholder`가 그 사실을 응답으로 말한다」(`:326-327`) · 「겹침은 해소됐다」(`:898`·`:975`). **「지금 구현이 「안건 1」을 넣는다」로 읽히는 문장 0건** |
| **`안건 1`** | 7 | ✅ **전부 과거형이거나 무관.** 「0.4.x가 넣어 겹쳐 보이던 것 … 사라졌다」(`:324-325`) · 「겹침은 해소됐다」(`:898`·`:975`) · 나머지 넷(`:347`·`:355`×2·`:555`)은 **도식·기본값 예시**로 증상과 무관 |
| **`R-50`** | 4 | ✅ **한 방향.** §4.1-7(`:328`)·§11.4(`:899`)·§12 행(`:975`) 셋이 전부 **「빈 제목을 무엇으로 그리나 · 화면이 고른 선택을 시안에 반영해 달라」**. (`:50`은 기획 정본 유입 문단으로 무관) |
| **`R-52`** | 2 | ✅ **한 방향.** §11.4(`:893` 「정해 달라가 아니라」) · §12 행(`:974` 「시안에 반영해 달라」). **디자인 결정을 요구하는 문장 0건** |
| **`can_add_agenda`** | 6 | ✅ **한 형·한 근거.** `{memo, ai, final}` 다섯 자리 일치(`:202`·`:283`·`:289`·`:300`·`:882`) + §8-9의 대비 참조(`:706`). **「서로 다른 상태 집합」이라 적힌 자리 0건** |
| **`제목 편집`** | 1 | ✅ §12 R-52 한 곳, **「섰다 · 반영해 달라」** |
| **`남은 차이`** | 2 | ✅ §11.4 머리의 안내(`:867`)와 본문(`:888` **「없다」**). **「자리가 없다」로 읽히는 문장 0건** |

**추가로 직접 확인한 것** (지난 세 판의 FAIL이 전부 「한 사실을 여러 자리가 다르게 말함」이었다):

- **`can_edit_note`** — 본문 **0건 → 1건**(§8-9 `:706`, 세 줄짜리 한 불릿) + 판 이력 1. 리뷰가 「문서 전체에서 0회」라 짚은 자리다.
- **`404`** — 본문 **1건 → 3건**(§6-9 거절 표 `:578` · 「403을 내지 않는다」 `:581` · §9-5 승격 후보 `:777`) + 판 이력 1.
  세 자리가 같은 결(「없는 것처럼 답한다」)을 말한다.
- **`204`** — 0건 → 1건(§6-9). **`409`** — §6-9 표와 §4.1-6 결론 표시 거절이 서로 안 부딪힌다.
- **`OQ-308`** — 여섯 자리 전부 **「이번 판에 없다 / 다음 논의」** 그대로. **`D58` 이상 0건.**
- **`merge_conflicts`·`판정 카드`** — **0건**.

---

## lint

```
python3 scripts/lint-pipeline.py --strict
→ 0 error, 264 warning (exit 0)
grep 'sc-ax' → 0건
```

**`products/sc-ax/` 범위 ERROR 0 · WARN 0.** 264 WARN은 전부 타 제품(`procedure-hub`·`selly`·`linky` 등)의
`doc_no`/`version` frontmatter 누락이고 **이번 작업과 무관**하다 — 개정 전과 같은 수치다.

## 동기화

| 파일 | 무엇 |
|---|---|
| `spec-004-meeting-note.md` frontmatter | `version: 0.6.0 → 0.6.1` · `last_updated: 2026-09-15`(유지) · `doc_no: SCAX-DOC-7` 불변 |
| 판 이력(`:41`) | **0.6.1 항목 추가** — 틀림의 방향(「이미 선 것을 아직 안 섰다고」)과 닫은 셋 · 받아 적은 계약 일곱 · **「모델·결정 무변경」** |
| `log.md` | **entry 1건 추가**(2026-09-15 · 종류 `spec` · 영향 `spec-004`) — FAIL 셋의 내용과 각 판단, 새 계약 일곱, 「새 OQ 없음」. `최종 수정`은 이미 2026-09-15 |
| `20-spec.md` | **변경 없음.** 회의 행(`:62`)의 제공/미제공 내용이 0.6.1에서 **바뀌지 않았다**(최종 회의록 하나 · 임시 두 벌 · 미제공에 「원본 여는 화면 자리」와 `OQ-308`). SPEC List 행도 `status: draft` 그대로 — derived view라 미러할 변경이 없다 |
| `30-work.md` | **변경 없음** — SPEC ID 추가·삭제·개명 없음 |

---

## 하지 않은 것 (경계 확인)

- **모델 재설계 없음 · 새 결정 없음** — D 코드는 **D56·D57 그대로**(`D58` 이상 0건). A6·B6·C의 구조를 건드리지 않았다.
- **계약을 되돌리지 않았다** — F-3에서 `can_add_agenda` 필드를 **그대로 뒀다**(지시 그대로). 고친 것은 근거 문장이다.
- **`OQ-308` 무수정 · 새 OQ 0건.** 충돌 판정 필드·UI·규칙 0건 유입.
- **`PLAN-004`·`SCREENDEF-005` 무수정** — 차이는 §12 R-50·R-52 통보로만.
- **코드 무수정** — `ax-workspace/sc-meeting-room`을 **`git show 65a580c:<path>`로 읽기만** 했다(작업트리가 다른 워커의 `origin/main` 머지로 움직이는 중이라 커밋 고정으로 읽었다).
- **커밋·push 없음.**

## 다음 바퀴로 넘기는 것 (이번 범위 밖 · 리뷰 §참고)

문서가 아니라 **코드가 D56/D57을 아직 안 따라온 자리** 넷이다. 이번 발주에 없어 손대지 않았다.

- `batch.py:195` 배치 프롬프트가 「**회의록은 세 벌이다**」로 D56 이전 어휘를 쓴다 — AI에게 「임시 재료」라는 위계를 말하지 않는다
- `schemas/ai_final_output.json` description이 계보 대상을 **`human`·`ai`**로 적는다 (값이 아니라 설명문)
- `application.py:903-904` `export` docstring 「원본은 … **화면에만 선다**」 — **D57과 정반대**
- `policy.py:204` 주석 「안건을 더하는 판정 — **편집보다 한 자리 넓다**」 — **F-3과 같은 낡은 근거**
