# 회의 종료·회의 중 테스트 — 추가 발주 대기

2026-09-14 사용자 리뷰 기록. 기존 FE 발주(task_de9c3f345a04 / ctx_c29548503a5c)와 별개로 모으는 후속 항목. **아직 추가 발주하지 않았다.** 사용자가 현재 회의에서 회의 중 기능을 테스트하고 돌아올 예정. 결과를 합친 뒤 사용자가 발주하라고 하면 전달한다.

## 1. 스크립트 가독성

- 현재 시간·화자·발언 3열 배치 때문에 본문 폭이 너무 좁다.
- 시간과 화자는 위 한 줄(00:03 · 화자 1), 발언은 아래에 열 전체 폭으로 표시.
- 발언 사이 간격 확보. 시각 칩 클릭·회의록 근거에서 원문으로 점프 기능 유지.
- 글꼴·크기·색상·행간은 기존 DS 타이포 토큰. 본문 기본 크기/일반 굵기/진한 본문색, 화자는 작은 크기/중간 굵기, 시간은 같은 작은 크기/읽을 수 있는 보조색. 폭 때문에 글자를 축소하지 않는다.
- 캐릭터가 발언 본문을 가리지 않게 조정.
- 열 너비 드래그 조절은 제안만 했으며 확정 범위가 아니다.
- 이미지: visual-review-2026-09-14/17-current-transcript-narrow.png

## 2. 회의 후속 업무 요청 모달의 상태·요청자 영역 제거

- 사용자 확정: 회의에서 여는 업무 요청 생성 모달의 상태 | 요청자 영역 전체를 제거한다. 초기 판단 대기와 시스템 요청자는 고정값이므로 생성 폼에서 불필요.
- 실제 시스템 요청자 기록은 유지. 일반 업무 요청 모달에는 확대 적용하지 않는다.
- 확인 근거: mediness-mediness/sc-meeting-room-spec/products/sc-ax/20-spec/spec-004-meeting-note.md §9-5 D40 및 R-48. 요청자는 시스템(회의), 누른 사람은 promoted_by 및 참조자. 요청자 표시가 필요할 때는 회의 · {회의명}.
- 실제 backend/src/ax_workspace/modules/work/request_lifecycle.py에 SYSTEM_MEETING_REQUESTER = system:meeting 존재. frontend/src/features/work/WorkModals.tsx 생성 폼은 request 종류에서 판단 대기를 표시하며 현재 화면은 로그인한 사람 이름을 표시하는 불일치가 있다.
- 이미지: 18-current-followup-modal.png, 19-followup-fixed-metadata.png

## 3. 파일 첨부 업로드 실패

- 사용자 재현: 종료된 회의에 약 3MB PDF를 첨부하면 실패.
- 스크린샷에서 POST /api/meetings/8c63e19b-80f2-44a5-8be0-0e00b70fd1b9/materials → 500 Internal Server Error. 프론트 localhost:5173, 응답 Server uvicorn, text/plain. 화면에도 Internal Server Error 원문 노출.
- 프론트 multipart 전송값과 실제 백엔드 계약을 대조하고 서버 로그로 원인을 확인해야 한다. 아직 정확한 원인은 확인하지 않았다.
- backend는 읽기 전용. 수정이 필요하면 원인·근거·필요 수정 지점을 보고한다. 가짜 성공이나 임의 API 구현 금지.
- 사용자용 실패 안내 문구는 DS/labels 규칙에 맞게 처리하고 선택한 파일을 실패 시 잃지 않게 한다.
- 이미지: 20-upload-http-500.png

## 4. 회의 중 메모 입력 하단

- 구현 입력줄이 왼쪽에 짧게 몰려 있고 안건 선택/전송 아이콘이 있다. 시안은 상세 열 대부분을 채우는 입력창과 오른쪽 기록 버튼.
- DS 기준 너비·하단 바 여백·기록 버튼으로 정합. 안건 선택이 메모 연결에 필요한지는 계약 확인 후 기능 보존.
- 구현 21, 시안 22/23 이미지 참고. 시안 하단 데모 상태 컨트롤은 복제 금지.

## 5. 진행 중 자료 첨부 버튼 누락

- 시안은 진행 중에도 파일 목록 아래 + 자료 첨부 버튼 존재. 현재 구현에는 빠짐.
- 실제 API/envelope를 확인해 연결, 미지원이면 보고. backend 임의 구현 금지.

## 첨부 실패 원인 정정 — 사용자 확인

- 사용자가 첨부 실패 원인을 처리 워커 미기동이라고 확인했다. 앞의 500은 관찰 증거로 보존하되 코드 결함으로 확정하지 않는다.
- 워커 기동 후 재검증 필요. 아직 재검증 결과는 없다. 진행 중 첨부 버튼 누락과 별개다.

## 다음

사용자의 회의 중 테스트 결과를 받아 추가 기록. 사용자가 발주를 명시하면 기존 FE 변경과 충돌 여부를 확인한 뒤 후속 브리프로 전달. 현재는 기록만 한다.

## 2026-09-14 후속 확정

- 첨부 업로드는 사용자가 직접 수정 완료했다고 통보. 해당 실패 수리는 발주 제외.
- 회의 중 AI 결과 없음: 정확히 「AI 요약이 곧 생성됩니다.」. 사람 안건 fallback 제거, 저장된 AI 결과는 보존.
- 종료 후 로딩: 스켈레톤 대신 스피너 + 「최종 회의록 생성 중입니다.」. 이번에는 미발주.
- 회의 중 5항목만 승인: `sc-meeting-room-fe-live-brief.md`. 종료 후 모달/스피너는 다음 범위로 대기.
