# 발주 — SPEC-004 v0.5.0 리뷰 지적 반영 (→ v0.5.1)

## 0. 너는 누구인가

`sc-meeting-room` 의 planner 다. 앞선 개정(v0.4.11 → v0.5.0)이 리뷰에서 **FAIL** 을 받았다.
이 바퀴는 **그 지적만** 닫는다. 모델을 다시 설계하지 마라 — 리뷰가 「모델 자체는 사용자 결정
다섯을 정확히 담았다」고 판정했다. 고칠 것은 **0.5.0 이 손대지 않고 남긴 옛 문장** 과
**새 규칙끼리의 충돌** 이다.

## 1. 읽을 것 (순서대로)

1. 리뷰 리포트 — `/Users/kknaks/orca/workspaces/kknaks_profile/sc-meeting-redesign/orchestration/work/sc-meeting-room/review-spec-004-v050-report.md` (359줄, **정본**)
2. 개정 대상 — `products/sc-ax/20-spec/spec-004-meeting-note.md` (작업트리 = v0.5.0, 미커밋)
3. 근거 조사 — `products/sc-ax/00-baseline/_working/meeting-two-copies-investigation.md`
4. `rules/document-pipeline.md`

## 2. 사용자 결정 (변하지 않았다)

- **D-1** 뼈대도 두 벌 — 사람/AI 가 각자 자기 안건 목록을 갖는다
- **D-2** 최종본은 **AI 가 짓는다**. 사람이 「고르는 단계」는 두지 않는다. 고칠 수는 있다
- **D-3** 충돌 판정은 **이번 판 범위 밖** (`OQ-308` 로 열어 둔다) — 절대 끌어들이지 마라
- **D-4** 원본 두 벌은 최종본 뒤에도 남는다
- **D-5** 「안건 1. 안건 1」은 별건 — 한 줄 통보로만

**`OQ-318`·`OQ-319`·`OQ-320` 은 열린 채로 둔다.** 사용자·기획 소유이고 아직 답이 안 왔다.
네가 닫지 마라.

## 3. 닫아야 할 것

### FAIL 셋 — 반드시 닫는다

- **F-1** `:145` (§3.1-7) 에 폐기된 **D48** 이 그대로 살아 있다 — 「AI 배치가 제목을 채우고 매 배치마다
  최신화하며」. 같은 파일 `:269`·`:532`·`:730` 이 정반대(폐기)를 말한다. `:145` 를 폐기 쪽으로 고치고
  §4.1-7 을 가리켜라.
- **F-2** §4.1-6 게이트 표(`:260`)와 §3.3 권한 표(`:189`)가 **벌 축으로 재단되지 않았다.**
  글자 그대로 읽으면 종료·실패에서 **사람 벌 안건도 고치고 지울 수 있다** — §6-9(`:482`)·§8-9(`:577`)·
  §8-11(`:590`) 이 「읽기 전용」이라 말하는 것과 정면 충돌이고 **D-4 가 지키려던 대조의 근거가 무너진다.**
  표에 `벌` 칸을 더해 human/ai/final 로 가르고, `can_edit_agendas` 가 **어느 벌의 판정인지** 못박아라.
  응답 필드 모양(불리언 하나인가 벌별인가)까지 계약으로 적어라 — 구현자가 거기서 갈린다.
- **F-3** `:576` 「사람이 고친 최종 줄의 `from_lines` 는 지운다」 ↔ `:578` 「저장은 안건의 줄 목록을
  통째로 덮어쓴다」. 통째로 덮어쓰면 **어느 줄이 고쳐졌는지 알 수 없어** 손대지 않은 줄의 계보까지
  첫 저장에 전멸한다. **둘 중 하나를 골라 적어라.**
  - 판단 근거는 문서 안에 있다 — §4.1-3 이 계보를 「내가 적은 안건이 어디로 갔나에 답하는 **유일한 길**」
    이라 했고 `OQ-308`(다음 판)이 그 위에 서 있다. **계보를 살리는 쪽을 권한다.**
  - 고르면 **왜 골랐는지**를 한 문장으로 남겨라. 반대쪽을 고르면 `§4.2-10` 에 계보의 **수명**을 적어라.

### WARN 여덟 — 전부 처리하고 무엇을 했는지 적는다

W-1 (조사 Q6 「한쪽 벌이 비면」이 답도 OQ 도 없다) · W-2 (§4.0 표 「진행 중」 ↔ 본문 「예정부터」) ·
W-3 (`:220` 이 없는 §5.6 줄 한도를 가리킨다) · W-4 (§12 R-1 ↔ R-49 가 기획에 서로 다른 개정을 요구) ·
W-5 (실패에서 사람이 채운 최종 벌이 [다시 시도]에 날아간다) · W-6 (배치 출력의 `agenda_id` 거취) ·
W-7 (자리표시 제목이 빈 값인지 문자열 유지인지) · W-8 (`OQ-312` 종결문이 「이름만 옮긴 것」처럼 읽힌다).

각 권장 수정은 리포트에 있다. **W-1 과 W-6 은 값을 정해야 한다** — 정하고 근거를 적어라.
정할 수 없는 것만 새 `OQ` 로 열되, **여는 이유를 적어라**(「나중에」는 이유가 아니다).

## 4. 하지 말 것

- **모델 재설계 금지.** §4.0 벌 축 · §8 합성 · §11.4 표는 리뷰가 PASS 했다. 손대는 것은 위 지적뿐이다
- **충돌 판정을 끌어들이지 마라** (D-3). `merge_conflicts` 류 필드·UI·규칙 신설 금지
- **`OQ-318`·`319`·`320` 을 닫지 마라**
- **기획 정본(`PLAN-004`) 을 편집하지 마라** — 차이는 §12 통보 한 줄로만
- **코드를 고치지 마라.** `ax-workspace/sc-meeting-room` 은 **read-only** 다
- 커밋·push 하지 마라

## 5. 별건 — 판단만 하고 보고

리뷰 §기존 부채: `products/sc-ax/30-work.md:11` 이 「v0.4.2」로 낡았고 `30-work/work-001`~`007`
일곱 본문이 전부 「트랙」 모델로 서 있다 — **0.5.0 이 WP 전량을 낡게 만든다.**
이번 바퀴에서 고치지 마라. **얼마나 낡았는지만 세어 보고하라**(파일별 「트랙」 언급 수 정도).
후속 발주는 코디가 판단한다.

## 6. 검증

```
python3 scripts/lint-pipeline.py --strict
```
→ `products/sc-ax/` 범위 ERROR 0 · WARN 0 확인. 타 제품 264 WARN 은 「무관」으로 분리 보고.

추가로 **네가 직접 대조하라** — F-1·F-2 는 「한 사실을 여러 자리가 말한다」가 원인이었다.
고친 뒤 다음을 grep 으로 전수 확인하고 결과를 보고에 실어라:
- `D48` · `자리표시` — 남은 자리가 전부 「폐기」로 정렬됐는가
- `can_edit_agendas` · `안건 추가` · `안건 삭제` · `결론 표시` — 전부 벌이 붙었는가
- `from_lines` · `merged_from` — 계보 규칙이 한 방향으로만 말하는가

frontmatter `version` 을 **0.5.1**, `last_updated` 를 **2026-09-14** 로 올리고
`20-spec.md`·`log.md` 를 동기화하라.

## 7. 산출물

- 개정: `products/sc-ax/20-spec/spec-004-meeting-note.md` (+ `20-spec.md` · `log.md`)
- 보고서: `/Users/kknaks/orca/workspaces/kknaks_profile/sc-meeting-redesign/orchestration/work/sc-meeting-room/spec-004-v051-fix-report.md`
  구성: FAIL 3 각각 「무엇을 어떻게 닫았나 + 파일:줄」 · WARN 8 처리표 ·
  **네가 정한 값과 그 근거**(F-3 · W-1 · W-6) · 새로 연 OQ 와 여는 이유 ·
  grep 전수 확인 결과 · lint 결과 · §5 별건 집계

## 8. 보고

- FAIL 3 이 닫혔는지 (각 한 줄)
- **F-3 을 어느 쪽으로 골랐고 왜**
- 새로 연 OQ 가 있으면 무엇이고 왜
- lint 결과 · §5 별건 집계
