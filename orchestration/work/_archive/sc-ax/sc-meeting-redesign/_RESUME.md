
# 재개 노트 — sc-meeting-redesign (sc-ax)

**지금**: 바퀴 0 리포트 3종 수령·채택(바퀴 9개 확정, L-1~L-6 · G-01/02 코디 기본값으로 닫음). **바퀴 1(토큰) 진행 중.** 사용자 자리 비움 — 루프 끝까지 돌리고 최종 보고.
**다음**: 조사 리포트 받아 검증 → §11 바퀴 목록을 §1 로 펴고 바퀴 1(토큰) 발주 → 리뷰 → 수정 → 다음 바퀴
**최종 산출물**: §4 의 문건 3(`FINAL-01-errors` · `FINAL-02-ds-update` · `FINAL-03-frontend`) + 코드 PR 1

세팅: `scripts/new-work.sh sc-ax sc-meeting-redesign` · 설정 SSOT `config/projects/sc-ax.json`
코디handle: `term_18011a51-e9ec-4cad-9370-1836d3ee8d2d`

## 워크트리

- `app`: `/Users/kknaks/orca/workspaces/ax-workspace/sc-meeting-redesign` (branch `kknaksss/sc-meeting-redesign`, base `origin/main` → PR `main`)

## 1. 지금

열린 것만 둔다. 닫히면 지우고 §5 이력으로 내린다.

- [~] **바퀴 0 조사 진행 중** — designer `term_326be2ea…`. 산출물 3종: `design/`(사본+`_MANIFEST.md`) · `ds-token-report.md` · `DS-gaps.md`
  - 중간 보고(02:2x): tokens·shell·handoff 내려받음. **토큰 충돌 0** — 이름이 안 겹친다 → 과도기에 스코프 격리 없이 두 묶음을 같이 실어도 된다(브리프 §3-4 판정). 리포트로 확인 필요
- [!] **[사용자 컨펌 대기]** `DS-gaps.md` **30건** (G-28 인라인 링크버튼 22곳 · G-29 테두리형 배지 12곳 · G-30 클래스 기반 datetime) 처분 — 새 DS 에 없는 항목 목록. 방침은 「대부분 DS 에 추가·DS 갱신」이나 최종 판단은 사용자
- [ ] 조사 리포트(은퇴 매핑표 + 토큰 충돌표) 받은 뒤 **교체 순서 확정** — 바퀴 단위를 여기 목록으로 편다
- [ ] 바퀴 1: 토큰 — 구 `styles.css :root` → 새 `tokens/*.css`
- [ ] 바퀴 2: 셸 — `AppShell`·`SideNav`
- [ ] 바퀴 3~: 컴포넌트 20개 → 새 부품으로 갈아끼우기
- [ ] 마지막: 화면 — 시안 있는 업무·회의부터, 나머지는 부품 교체로 따라오는지 확인
- [!] **구 DS 는 이번에 은퇴시킨다.** 다만 은퇴는 **마지막 바퀴** — 중간엔 둘이 같이 떠 있어야 앱이 안 깨진다. 「지우는 시점」을 조사 리포트가 정하게 한다
- [!] 시안은 **레이아웃의 정본**이다 — 기존 레이아웃과 다르면 시안으로 다시 그린다. 다만 **로직·구조는 우리 것**(상태·`api.ts`·envelope·`viewModels`·책임 분리는 안 바꾼다)
- [!] FE 테스트 467개 — 바퀴마다 깨진다. 리뷰 단계에서 회귀를 본다

## 2. 결정 (SoT)

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-13 | **목표는 구 DS 은퇴.** 새 `TheSC AX Design System`(`7e839512…`)을 앱 전체에 적용하고, 구 `SCAX`(`8fa54d76…`)·`styles.css`·`design-system-v2.dc.html` 기반은 이번 작업에서 물러난다. 공존은 과도기일 뿐 끝 상태가 아니다 | 사용자 지시 (「이번에 구 디자인 시스템의 은퇴야」) |
| 2026-09-13 | ~~두 디자인 시스템은 공존한다. 구 것은 건드리지 않는다~~ — **2026-09-13 뒤집힘**: 코디가 「2개가 공존해야 하는 상황」을 끝 상태로 잘못 읽었다. 사용자 교정으로 은퇴가 목표 | 코디 오독 → 사용자 교정 |
| 2026-09-13 | **DS 가 화면보다 먼저다.** 토큰 → 셸 → 컴포넌트를 갈면 시안 없는 화면(홈·프로젝트·조직·캘린더·일일보고·채팅…)도 따라온다 | 사용자 지시 (「우리 디자인 시스템이 우선적」) |
| 2026-09-13 | **시안은 레이아웃의 정본, 로직의 참고용.** ① **레이아웃·배치·시각 = 시안대로 그린다** — 시안 ↔ 기존 레이아웃이 다르면 시안으로 다시 그린다. ② **로직·구조 = 우리 것 유지** — 상태·`api.ts`·envelope 권한 판단·`viewModels`·컴포넌트 책임 분리는 시안 때문에 바꾸지 않는다 | 사용자 지시 (「시안은 참고용이야 우리 로직 구조는」 + 「시안의 레이아웃은 유지해야해… 시안레이아웃으로 그리는거야」) |
| 2026-09-13 | ~~새 DS 는 Claude Design 이 정본 — 시안과 기획서가 다르면 시안을 따른다~~ — **범위 축소**: 정본인 것은 **디자인 시스템**(토큰·부품·가이드라인)이고, **화면 시안은 참고용**이다 | 사용자 교정 |
| 2026-09-13 | **작업 단위는 루프** — 조사 → 발주 → 리뷰 → 수정 한 바퀴. 바퀴마다 리뷰(`reviewer_code`)를 끼운다 | 사용자 지시 |
| 2026-09-13 | DS 내려받기·업로드는 **orca 워커 세션**이 한다(서브에이전트엔 DesignSync 없음, 코디가 대신 받으면 컨텍스트 낭비) | 09-10 사용자 질책 |
| 2026-09-13 | **적용 사다리** — ① 시안 있으면 시안 레이아웃 → ② 같은 종류 부품 있으면 골격만 DS·내용은 우리 것 → ③ 부품만 있으면 레이아웃 유지·토큰/부품 교체 → ④ 없으면 **발명 금지**, `DS-gaps.md` 로 올린다 | 사용자 질문(회의 생성 모달 시안 없음)에 코디 답변 → 사용자 승인 |
| 2026-09-13 | **새 DS 에 없는 것은 `design/DS-gaps.md` 목록으로 모으고 사용자가 최종 컨펌한다.** 방침은 「대부분 새 DS 에 추가하고 DS 를 갱신」 — 워커는 제안만, 결정 금지 | 사용자 지시 |
| 2026-09-13 | 조사 바퀴에선 Claude Design 에 **쓰지 않는다**(읽기만). 단 gaps 컨펌 뒤 **DS 갱신 바퀴가 따로 온다** — 그때 쓰기·`.design-sync/` 재지정을 판단 | 코디 기본값 + 사용자 방침 |
| 2026-09-13 | **과도기 공존 = 스코프 격리 불필요.** 토큰 이름 교집합 0(새 569 : 구 65). 충돌은 변수가 아니라 **전역 element 규칙 6줄**(`a`·`a:hover`·`body`·`button`·`h1~h4,p`·`*`+스크롤바) — 그것만 잘라내고 싣는다 | 조사 리포트 §3·§4 |
| 2026-09-13 | **바퀴 9개**로 끊는다: 1 토큰 · 2 셸 · 3 프리미티브 · 4 아이콘 · 5 업무화면 · 6 회의화면 · 7 AX드로어·액션센터 · 8 남은 7장 · 9 은퇴 | 조사 리포트 §11 (코디 채택) |
| 2026-09-13 | **[L-1] 회의 2화면 → 1화면 4칸.** 시안이 레이아웃 정본이므로 합친다. `openMeetingId` 는 「목록 칸의 선택 상태」로 내린다. **이탈 가드는 없애지 않는다** — 페이지 전환 대신 **선택 전환 시점**에 건다 | 코디 결정 (사용자 「시안 레이아웃으로 그린다」) |
| 2026-09-13 | **[L-2] breadcrumb 삭제.** 1화면이 되면 목록 칸이 옆에 상시 있어 돌아갈 길이 UI 에 내장된다. 집중 모드에선 집중 해제가 그 역할 | 코디 결정 (L-1 의 따름수) |
| 2026-09-13 | **[L-3] 업무 탭 — 시안 이름·모양, 우리 권한 조건 유지.** 권한 없으면 탭이 안 뜨는 현 동작(`MyWorkPage.tsx:366·372`) 그대로. 시안에 자리 없는 「조직 업무」는 권한 보유자에게만 넷째 탭 | 코디 결정 (레이아웃은 시안·로직은 우리 것) |
| 2026-09-13 | **[L-4] 수신함 — 우리 `actionItems` 를 시안 `InboxCard` 모양으로 렌더.** 프론트 어댑터로 맞춘다. **BE 무변경.** 시안이 요구하는데 데이터가 없는 필드는 **그리지 않는다**(빈 값 발명 금지) | 코디 결정 |
| 2026-09-13 | **[L-5] 칸반·타임라인 유지.** 사용처 100곳짜리 기능을 「시안에 자리가 없다」는 이유로 삭제하지 않는다 — 기능 삭제는 사용자 결정 사항이다. 시안 칩 바 우측에 보기 방식 세그먼티드를 둔다 → G-14 로 디자이너 요청 | 코디 결정 (되돌리기 어려운 방향은 안 간다) |
| 2026-09-13 | **[L-6] `min-width:1542` · 페이지 스크롤 제거는 시안대로.** `MinWidthNotice` 폐기(프로덕션 사용처 0 = G-20 제안과 일치). 1280 반응형(`styles.css:826~910`)은 바퀴 8 에서 제거 | 코디 결정 |
| 2026-09-13 | **[G-02] 폰트는 로컬 동봉.** `tokens/fonts.css` 의 jsDelivr 원격 URL 을 그대로 두지 않는다 — 사내망·오프라인에서 글꼴이 깨진다. woff2 2벌을 `frontend/public/fonts/` 에 두고 `src` 를 로컬 경로로 바꿔 싣는다(Pretendard = OFL) | 코디 결정 |
| 2026-09-13 | **[G-01] 글꼴 가족은 DS 값 그대로**(`--font-ui: "Pretendard JP"`). DS 가 정본이다. 한글 자형이 미세하게 달라지는 건이라 **디자이너 확인 항목으로 `FINAL-02` 에 남긴다** | 코디 결정 (되돌리기 쉬움 — 한 줄) |
| 2026-09-13 | 바퀴 1 이 만드는 자리는 `frontend/src/styles/` — DS 토큰 원본 + `index.css` 한 벌. 구 `src/styles.css` 는 **그대로 둔 채** 추가로 싣는다(바퀴 9 에서 삭제) | 코디 결정 (핸드오프 README 의 이식 위치 표와 일치) |
| 2026-09-13 | **DS 부품 소스는 `.d.ts` 전부 + 쓰는 8개만 `.jsx`** (`.prompt.md` 제외). 193개는 Figma variant 정적 렌더러라 다음 바퀴가 열 일이 없다 — 580파일·200만 토큰을 쓰지 않는다. 예외로 `components/icon/`(바퀴 4 매핑 정본)과 `guidelines/`는 전부 받는다 | 워커 질문 → 코디 결정 |
| 2026-09-13 | `_ds_bundle.js` 는 256KiB 캡에 절단 — **대체 조치 없음.** 캔버스 렌더용이고 우리는 소스를 쓴다. `_MANIFEST` 에 사유까지 적어 다음 세션이 결손으로 오해하지 않게 한다 | 코디 결정 |
| 2026-09-13 | **[D-A] breadcrumb 삭제는 바퀴 6 으로 미룬다.** L-2 결정은 유효하나 지금 지우면 회의 상세 → 목록 복귀 경로가 바퀴 6 까지 사라진다. 과도기엔 `AppHeader` 의 `breadcrumb` prop 으로 넘긴다 | 코디 결정 (회귀 창 회피) |
| 2026-09-13 | **[D-B] 셸의 `min-width:1542`·`height:100vh`·`overflow:hidden` 은 시안대로.** 대신 **화면마다 본문 스크롤 컨테이너를 잡는 것**이 바퀴 2 의 합격선 | 코디 결정 |
| 2026-09-13 | **[D-F] 사이드 메뉴는 우리 `ProductSurface` 8종이 정본.** 순서·아이콘·그룹·접기는 시안을 따르되 **시안에만 있는 메뉴(수신함·진행현황·자료)는 만들지 않는다** — 갈 화면이 없다 | 코디 결정 |
| 2026-09-13 | 바퀴 1·2 는 **전체 스위트 1회를 예외 허용.** 토큰·셸은 전역이라 부분 테스트로 회귀를 못 잡는다 | 코디 결정 (사용자 「전체 스위트 금지」 방침의 예외) |
| 2026-09-13 | **바퀴 3 을 a/b 로 나눈다** — a 표시(읽기) 부품 13종 · b 폼·오버레이(쓰기) 부품 12종. 30종을 한 번에 갈면 검증이 안 된다(구 sc-design-system 의 2차-a/b 선례) | 코디 결정 |
| 2026-09-13 | **`DS-gaps.md` 에 오른 부품은 바퀴에서 손대지 않는다** — 사용자 컨펌 전까지 구 부품을 그대로 둔다. 바퀴 3a 해당분: `ProgressBar`(G-04) · `EmptyValue`(G-07) | 코디 결정 (사용자 「gaps 는 내가 컨펌」) |
| 2026-09-13 | **부품 props 시그니처는 우리 것을 지킨다.** 핸드오프와 다르면 호출부가 아니라 부품 안에서 맞춘다 — 소비처가 수십 곳이라 시그니처를 바꾸면 부품 바퀴가 화면 바퀴가 된다 | 코디 결정 |
| 2026-09-13 | **바퀴 3c 신설 — 바퀴가 9→10개.** 현 앱의 `Button`·`Badge`·`Chip`·`Tabs`·`SegmentedControl` 7종은 React 부품이 아니라 **맨 CSS 클래스**였다(`.btn` 221곳/35파일 · `.badge` 31 · `.chip` 12 · `.segmented` 4 · `.page-tabs` 1 = 269곳). 3a 는 부품을 만들고 17곳만 이관, **`.btn`·`.badge` 252곳은 전용 바퀴 3c** 로 뺀다 | 워커 질문 → 코디 결정 |
| 2026-09-13 | **별칭 CSS(구 클래스를 새 기하로 다시 칠하기)는 안 쓴다.** 일을 없애는 게 아니라 바퀴 9 로 미루는 것이고, 은퇴가 목표인 작업에서 별칭은 부채다 | 코디 결정 (워커 선택지 D 기각) |
| 2026-09-13 | **`.btn` 이관을 화면 바퀴(5·6·8)에 얹지 않는다.** 「레이아웃 재설계 + 버튼 이관」을 한 바퀴에 넣으면 회귀 원인을 못 가른다. **한 바퀴는 한 가지 원인만** | 코디 결정 |
| 2026-09-13 | **`GutterList`·`StatusNote` 는 이름만 같은 별개 부품 — 안 간다.** 우리 `GutterList`=2단 메타 목록 / 핸드오프=레일 목록. 우리 `StatusNote`=인라인 한 줄(tone muted) / 핸드오프=블록 오류(role=alert). 둘 다 **`DS-gaps.md` 에 G-25·G-26 으로 추가**해 사용자 컨펌 목록을 완성한다 | 워커 판단 → 코디 승인 |
| 2026-09-13 | **바퀴 3a 는 B 가 아니라 A+segmented 로 착지.** 워커가 게이트를 띄운 시점에 orca 런타임이 끊겨 B 지시가 전달되지 않았다. 남은 `.chip` 18 · `.page-tabs` 1 은 **수정 발주 대신 3c 범위에 흡수** — 19곳 때문에 발주·검증 한 바퀴를 더 도는 게 더 비싸다 | 코디 결정 |
| 2026-09-13 | **`DS-gaps.md` 24 → 27건.** 바퀴를 돌며 나온 G-25(인라인 상태 한 줄·7곳) · G-26(2단 메타 목록·3곳) · G-27(**스켈레톤 0.4초 규칙 소실**·16곳)을 코디가 추가. G-27 은 이미 동작이 사라진 상태다 | 코디 추가 |
| 2026-09-13 | **바퀴 3c 는 매핑표 승인 게이트를 먼저 통과한다.** 277곳을 고친 뒤 매핑이 틀리면 되돌릴 방법이 없다. 런타임이 끊겨 답을 못 받으면 **건수 많은 조합만 남기고** 진행하도록 브리프에 대비책을 넣었다 | 코디 결정 (3a 의 런타임 단절 경험 반영) |
| 2026-09-13 | **[3c] 크기 사다리 = 높이 근접 매핑** (h30→sm32 · 기본34→sm32 · h40→md39). 서수 매핑(h40→lg47)은 7px 밀려 모달 푸터가 움직인다 — 「같은 자리의 같은 것」 계약 위반 | 워커 제안 → 코디 승인 |
| 2026-09-13 | **[3c] `.badge.outline` 12곳은 `tone=neutral` 로 접지 않고 남긴다.** 화면에 `.badge.neutral`(회색 채움)이 이미 7곳 있어, 접으면 **서로 다른 두 상태가 똑같아진다** — 시각 변화가 아니라 의미 구분의 소실 | 코디 결정 (워커 제안 뒤집음) |
| 2026-09-13 | **[3c] override 17줄은 (가) 다시 겨누기 — 단 `src/styles/overrides-transitional.css` 로 분리.** 구 `styles.css` 에 두면 구 DS 파일이 새 DS 부품을 스타일링하는 교차 결합이 된다. 규칙마다 「어느 화면·어느 바퀴에서 죽나」 주석. 죽은 규칙 2줄은 옮기지 말고 삭제 | 코디 결정 (워커 (가) 채택 + 위치 변경) |
| 2026-09-13 | **`DS-gaps` 취급을 두 종류로 가른다.** ㉮ **껍데기가 DS·핸드오프에 있는 것**(G-10 Drawer · G-11 ConfirmModal · G-12 TimeChip · G-13 Toast/Popover/Skeleton) → **사다리 ②로 진행**(껍데기는 DS 규약·안은 우리 것). 사용자 컨펌은 「DS 에 부품으로 올릴지」지 우리가 쓸 수 있느냐가 아니다. ㉯ **기하·값 자체가 없는 것**(G-04·05·07·08·09·28·29·토큰류) → **손대지 않는다** | 코디 결정 (3a 의 뭉뚱그린 규칙을 정밀화) |
| 2026-09-13 | **[3b] `Drawer` 는 `.scax-drawer` 골격 + 사이즈 variant**(`--sm` 520 / `--lg` 840). 지금 840 쓰던 6곳은 `--lg` 로 — 화면 그대로 | 코디 결정 (G-10 제안 채택) |
| 2026-09-13 | **[3b] datetime 3종(`DateField`·`DatePicker`·`TimeField`)은 교체하지 않는다.** 리포트 §9 의 「1:1」은 props 이야기였고, DS `components/datetime/` 3종은 **`className` 이 하나도 없이 전부 인라인 스타일**이라 「껍데기만 갈기」가 구현 수준에서 성립하지 않는다(코디가 파일로 확인). 바꾸면 `role=grid`·방향키·min/max·포커스 복귀(테스트 14건)·「지우기/오늘」 푸터를 잃는다 → **G-30** | 워커 판단 → 코디 승인 |
| 2026-09-13 | **[4] DS 아이콘 세트를 통째로 싣지 않는다.** 120종·167KB 중 우리가 쓰는 건 30여 종 — **쓰는 것만** 가져오고 이름만 DS 쪽으로 맞춘다 | 코디 결정 |
| 2026-09-13 | **[4] DS 에 없는 살아 있는 글리프 5종**(`paperclip`·`arrow-up`·`arrow-down`·`sparkle`·`folder`·12곳)은 **구 16그리드 path 유지.** 24그리드로 다시 그리는 것은 발명이다(G-03). **죽은 5종도 안 지운다** — G-03b 「폐기」는 사용자 컨펌 전 | 코디 결정 |
| 2026-09-13 | **[4] G-03b 정정 — 「폐기 5종」 중 셋이 살아 있었다.** `square`(WorkViews:138)·`circle`(TodayPage:241)·`empty`(Empty.tsx:42)가 **삼항·동적 `name={…}`** 자리라 조사 때 grep 에 안 잡혔다. 그 목록대로 지웠으면 화면 셋이 깨졌다. 진짜 0곳은 `ban`·`pending` 둘 | 워커 발견 → 코디 확인·`DS-gaps` 정정 |
| 2026-09-13 | **[4b] 두 그리드의 선 굵기는 「보이는 굵기」로 맞춘다.** v2 07 의 1.5/1.3 은 viewBox 16 전제였다 — viewBox 24 가 섞인 지금 숫자를 고정하면 오히려 규칙을 어긴다. DS(24그리드) 값을 두고 16그리드를 `×16/24` | 워커 질문 → 코디 결정 |
| 2026-09-13 | **[5~] 화면 바퀴부터 규칙이 뒤집힌다** — 1~4b 는 「화면 바꾸지 마라」, 5 부터는 **「시안대로 다시 그려라」**. 로직·구조만 우리 것 | 코디 정리 (사용자 지시의 적용) |
| 2026-09-13 | **[5] ★ J-1 — 시안에 있어도 BE 계약이 없으면 그리지 않는다.** 껍데기만 만들면 가짜 기능이다. 걸리는 것: 별표 열(서버 상태) · 상태 셀 인라인 편집(envelope `allowed_commands` 있는 행만) · 「일일보고 생성」. **안 그린 것은 전부 백엔드 태스크의 입력** | 코디 결정 (사용자 「백엔드는 다음 태스크」) |
| 2026-09-13 | **[5] 표는 시안 열 구성을 따른다.** ~~시작일·출처 열이 화면에서 빠진다(정보 손실)~~ → **5a 결과 정정**: 값이 사라진 게 아니라 **상세 드로어로 자리를 옮겼다.** 손실 아님 | 코디 결정 → 워커 실측으로 정정 |
| 2026-09-13 | **바퀴 6 을 셋으로 나눈다** — 6a 구조(2화면→1화면 4칸) · 6b 상세칸·회의록 내용 · 6c 목록칸·오버레이. 차이 23건에 화면 하나가 없어지는 작업이라 한 발주로는 검증이 안 된다 | 코디 결정 |
| 2026-09-13 | **[6a] 이탈 가드는 없애지 않는다.** 페이지 전환이 사라지면 **선택 전환 시점**에 같은 가드를 건다. 「고치던 것이 있을 때 묻는」 장치라, 묻지 않고 내용이 날아가면 회귀다 — **테스트로 잠그게 했다** | 코디 결정 (L-1 의 따름수) |
| <YYYY-MM-DD> | <무엇을 정했나> | <사용자 지시 · 조사 리포트 · 리뷰 판정> |

뒤집힌 결정은 지우지 않는다. ~~취소선~~ 을 긋고 같은 행에 뒤집은 날짜와 사유를 남긴다 —
지우면 왜 그렇게 갔는지가 사라져서 같은 논의를 다시 한다.

## 3. 발주 (살아 있는 것만)

| 워커 | handle | task_id | dispatch_id | 브리프 | 상태 |
|---|---|---|---|---|---|
| designer(바퀴 0 조사) | `term_326be2ea-10fe-4bff-bfd6-4d65645eec99` | `task_d5406b826209` | `ctx_69430b6b5a2b` | `sc-meeting-redesign-ds-import-brief.md` | 완료 · 검증 통과 → 커밋 `c7b8eb4` (사본 279파일 · ax-workspace 무변경 · Claude Design 쓰기 0회) |
| frontend(바퀴 1 — 토큰) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_e649adb8927f` | `ctx_59c934fd829a` | `sc-meeting-redesign-fe-brief.md` | 완료 · 코디 검증 통과 → 커밋 `1ff3e51` |
| frontend(바퀴 2 — 셸) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_82dcd9308b35` | `ctx_f853438f8876` | `sc-meeting-redesign-fe-2-brief.md` | 완료 · 코디 검증 통과 → 커밋 `c7a7005` |
| frontend(바퀴 3a — 표시 프리미티브) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_a76e8cad9261` | `ctx_9238d9e52d60` | `sc-meeting-redesign-fe-3a-brief.md` | 완료 · 검증 통과 → 커밋 `fbe5801` |
| frontend(바퀴 3c — 클래스 이관) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_14037615eca9` | `ctx_b84142e035d5` | `sc-meeting-redesign-fe-3c-brief.md` | 완료 · 검증 통과 → 커밋 `36ba1d5` (217 이관 · 32 남김) |
| frontend(바퀴 3b — 폼·오버레이) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_ba329e93173b` | `ctx_e294e8643c77` | `sc-meeting-redesign-fe-3b-brief.md` | 완료 · 검증 통과 → 커밋 `4b7ae91` (8종 이관 · F-2 는 근거 있는 미실행) |
| frontend(바퀴 4 — 아이콘) | `term_f90f1c43…` | `task_ac405b5bd818` | `ctx_48962d235d28` | `sc-meeting-redesign-fe-4-brief.md` | 완료 · 검증 통과 → 커밋 `2f6e9ea` (35종 7.9KB · G-03b 오류 정정) |
| frontend(바퀴 4b — 선 굵기) | `term_f90f1c43…` | `task_65240fd967d4` | `ctx_e3c1df8798ff` | `sc-meeting-redesign-fe-4b-brief.md` | 완료 · 검증 통과 → 커밋 `2a353ff` |
| frontend(바퀴 5a — 업무화면 머리·본문) | `term_f90f1c43…` | `task_039e96eddc60` | `ctx_7fdc23606736` | `sc-meeting-redesign-fe-5a-brief.md` | 완료 · 검증 통과 → 커밋 `15f5301` (§8-A 10건 · J-1 미구현 4건) |
| frontend(바퀴 5b — 레일 2칸) | `term_f90f1c43…` | `task_fa7c64245272` | `ctx_820c18e8d309` | `sc-meeting-redesign-fe-5b-brief.md` | 완료 · 검증 통과 → 커밋 `ceb46b2` |
| frontend(바퀴 6a — 회의 구조) | `term_f90f1c43-4f6b-43f6-b048-cdffb24a48f6` | `task_e90c2e106615` | `ctx_b975cce03ccc` | `sc-meeting-redesign-fe-6a-brief.md` | **발주됨** |
| reviewer_code(바퀴 1 리뷰) | `<term_…>` | `<task_…>` | `<ctx_…>` | (미작성) | 대기 |

핸들은 세션 재연결로 바뀐다. 바뀌면 **덮어쓴다.** 워커 보고는 dispatch preamble 의 값을 따르므로
여기 옛 핸들을 남겨 두면 어느 것이 산 것인지 판단이 안 된다.

## 4. 산출물

**최종은 문건 3 + 작업 결과물 1.** (사용자 지시 2026-09-13) 루프가 다 끝난 뒤 이 넷으로 보고한다.

| # | 문건 | 파일 | 무엇이 들어가나 |
|---|---|---|---|
| 1 | **최종 결과 오류** | `FINAL-01-errors.md` | 끝까지 안 풀린 것 전부 — 깨진 채 남은 테스트 · 시안대로 못 그린 자리와 사유 · 리뷰 지적 중 미반영 · 구 DS 를 못 지운 부분과 그 이유 · 로직이 걸려서 손 못 댄 항목. **숨기지 않는다.** 항목마다 `무엇 / 어디(파일:줄) / 왜 / 다음에 무엇이 필요한가` |
| 2 | **디자인 시스템 업데이트** | `FINAL-02-ds-update.md` | 새 DS 에 없어서 **추가·갱신한 것**의 최종 명세. `DS-gaps.md` 의 각 항목이 어떻게 처분됐는지(추가 / 대체 / 폐기 / 보류) · 추가한 부품의 규격 · Claude Design 에 반영했는지 여부와 경로. 디자이너에게 넘길 목록도 여기 |
| 3 | **최종 프론트 결과** | `FINAL-03-frontend.md` | 새 DS 가 반영된 프론트의 최종 상태 — 페이지 전수표(`화면 / 적용 방식(①②③) / 결과 / 확인 방법`) · 바뀐 파일 묶음 · 토큰·부품 매핑 최종본 · 구 `styles.css` 잔존분 · 테스트 결과(vitest) |
| 4 | **작업 결과물** | code PR | `ax-workspace` `kknaksss/sc-meeting-redesign` → `main`. **프론트만** — `backend/` 무변경이 PR 본문에 명시돼야 한다 |

- 문건 3장은 코디 워크트리 `orchestration/work/sc-meeting-redesign/` 에 둔다(kknaks_profile, 별도 PR).
- 바퀴별 중간 리포트(`ds-token-report.md` · `DS-gaps.md` · `review-*.md`)는 **원료**다. 최종 문건이 그것을 대체하지, 그것을 그대로 내지 않는다.

- spec PR: 없음 (이번 작업은 스펙 리포 안 건드림)
- code PR: <링크>
- 커밋: `1ff3e51` — 바퀴 1 토큰 탑재 (tsc 0 · vitest 467/467 · 새로 깨진 테스트 0)
- 디자이너 전달 대상(누적): **DS `tokens/fonts.css` 의 비-JP 원격 URL 이 jsDelivr 404 죽은 링크** — 실 경로는 `@v1.3.9/packages/pretendard/dist/web/…` (바퀴 1 발견)
- 커밋: `c7a7005` — 바퀴 2 셸 교체 (tsc 0 · vitest 468/468 · 스크롤 13/13 · 새로 깨진 테스트 0)
- 커밋: `fbe5801` — 바퀴 3a 표시 프리미티브 (갈았다 4 · 새로 만들었다 7 · 안 갔다 2 · vitest 468/468)
- 커밋: `c7b8eb4` — 바퀴 0 조사 산출물 (문서, kknaks_profile)
- 커밋: `36ba1d5` — 바퀴 3c 클래스 이관 (217곳 · 남김 32 · 속성손실 0 · vitest 468/468)
- 커밋: `4b7ae91` — 바퀴 3b 폼·오버레이 (8종 이관 · vitest 469/469 · 접근성 감사 차이 0)
- 커밋: `2f6e9ea` — 바퀴 4 아이콘 통합 (35종 7.9KB · vitest 471/471)
- 커밋: `2a353ff` — 바퀴 4b 선 굵기 일치 (네 크기 전부 실측)
- 커밋: `15f5301` — 바퀴 5a 업무화면 머리·본문 (§8-A 10건 · vitest 472/472 · 빈 단언 1건 발견·수정)
- 커밋: `ceb46b2` — 바퀴 5b 레일 2칸 신설 (vitest 472/472 · 조용한 구멍 3건 발견·수정)
- 커밋: `<sha>` — <한 줄>

## 5. 이력 (최신이 위)

- `2026-09-13` 세팅: `new-work.sh sc-ax sc-meeting-redesign --workers frontend` — ax-workspace 워크트리 생성(base `origin/main` = `0a8a04b`). 코디가 `list_projects`·`list_files`·`get_project`·`SKILL.md` 만 read-only 로 확인해 새 프로젝트 구조 파악(토큰 7 · guidelines 19 · components 12분류 ~150부품 · handoff 3벌 · 화면 4장). 나머지 다운로드는 워커 몫
- `<YYYY-MM-DD>` <무슨 일이 있었나 — 한 줄>

이 절은 **재개에 필요한 만큼만** 쓴다. 회고·배운 것은 `SUMMARY.md` 몫이다.
