# [frontend] 바퀴 6a — 회의: 두 화면을 한 화면 4칸으로 합친다 (구조만)

너는 **sc-ax `frontend` 워커**다. 바퀴 5b 커밋 `ceb46b2` 위에서 이어간다.
작업 워크트리: `/Users/kknaks/orca/workspaces/ax-workspace/sc-meeting-redesign`

## 1. 이 바퀴가 무엇인가

바퀴 6 은 차이 **23건**에 **화면 하나가 없어지는** 작업이라 셋으로 나눈다.
**너는 6a — 구조만.** 칸 내용은 6b(상세·회의록)·6c(목록·오버레이)가 한다.

> **6a 의 계약: 틀을 바꾸되 내용은 옮기기만 한다.**
> 지금 `MeetingListPage` · `MeetingDetailPage` 안에 있는 것들을 **네 칸 안으로 자리만 옮겨라.**
> 카드 모양·배지·진행 바 모양 같은 것은 **손대지 마라** — 6b·6c 몫이다.

시안은 **한 화면 4칸**이다 (`workspace.v1.jsx:1` 이 직접 말한다 —
「회의 한 화면(4칸) … 목록과 상세를 페이지로 나누지 않는다」):

| 칸 | 폭 | 무엇 |
|---|---|---|
| 1 nav | 180 / 접힘 65 | 이미 있다(바퀴 2) |
| 2 목록 | **380 고정** | 지금 `MeetingListPage` 의 왼쪽 열 |
| 3 상세 | 가변 min **640** | 지금 `MeetingDetailPage` 의 왼쪽 열 + 페이지 머리 |
| 4 첨부·스크립트 | **342 고정 · 늘 선다** | 지금 `MeetingDetailPage` 의 오른쪽 열 |

## 2. SSOT (전부 read-only)

- **`ds-token-report.md` §8-B**(551~583줄) — 차이 23건. 이 바퀴가 쓰는 행은
  「화면 수 · 칸 구성 · 목록 칸 · 목록의 오른쪽 미리보기 · 상세 칸 · 첨부·스크립트 · 칸 접기 · 페이지 머리」 **8건**
- **`ds-token-report.md` §7 판정**(482~520줄) — 시안이 무엇인지, `meetings.css` 의 고아 11종이 무엇인지
- **`MeetingWorkspace.html`** · **`handoff/meetings/js/workspace.v1.jsx`** · `css/workspace.css` · `css/meetings.css`

현 앱: `src/App.tsx`(`openMeetingId` 분기 `58~72`·`469~493`) · `src/meetings/MeetingListPage.tsx` · `src/meetings/MeetingDetailPage.tsx`

## 3. 결정 (재논의 금지)

| # | 결정 |
|---|---|
| **M-1** | **[L-1] 두 화면 → 한 화면.** `openMeetingId` 는 **「목록 칸의 선택 상태」**로 내린다. 라우팅 분기를 없애고 `surface="meetings"` 하나가 네 칸을 그린다 |
| **M-2** | **★ 이탈 가드를 없애지 마라.** `registerMeetingLeaveGuard`(`App.tsx:58~72`)는 **고치던 것이 있을 때 묻는** 장치다. 페이지 전환이 사라지면 **선택 전환 시점**(다른 회의를 고를 때)에 같은 가드를 건다. 이게 이 바퀴에서 가장 조용히 깨지기 쉬운 것이다 — **묻지 않고 내용이 날아가면 그건 회귀다** |
| **M-3** | **[L-2] breadcrumb 을 지운다.** 이제 목록 칸이 상시 옆에 있어 돌아갈 길이 UI 에 내장된다. `AppHeader` 에 넘기던 breadcrumb 을 빼고, `App.tsx` 의 관련 상태도 정리해라 |
| **M-4** | **목록의 오른쪽 미리보기 패널을 삭제한다**(`MeetingListPage.tsx:335~369`). 시안에 없다. `meetings.css` 의 고아 11종(`.scax-note-panel*` 9 · `.scax-meeting-placeholder*` 2)은 **가져오지 마라** |
| **M-5** | **접는 칸이 반대다.** 지금은 `.detail.single` 로 **첨부 칸**을 접지만, 시안은 `focus` 일 때 **목록 칸**이 사라진다(nav\|상세\|첨부). 시안대로 뒤집어라. 첨부 칸은 **늘 선다**(조건부 렌더 제거) |
| **M-6** | **페이지 머리를 하나로 통합.** h1 「회의」 + `회의 생성`(outlined primary) + `빠른 시작`(solid primary, `play`). **순서가 생성이 왼쪽**이다. 지금 상세 페이지 머리의 편집 폼(`meeting-meta-edit`)은 **6b 가 3칸 안으로 내린다 — 네가 지우지 마라.** 자리만 비워 두고 6b 에 넘겨라 |
| **M-7** | **선택이 없을 때**(회의를 안 고른 상태) 3·4칸이 무엇을 그릴지는 **네가 정한다.** 시안에 그 상태가 없으면 `Empty` 로 두고 보고해라. **가짜 화면을 만들지 마라** |
| **M-8** | **J-1 유지 — BE 계약이 없는 기능은 그리지 않는다.** `더 보기`·`회의 생성` 이 계약에 있는지 확인하고, 없으면 안 그리고 보고 |
| **M-9** | `DS-gaps` ㉯ 항목은 손대지 마라 |

## 4. allowed_paths

- `frontend/`

**`backend/` 는 한 줄도 안 건드린다. 읽는 것까지만이고 고치자는 제안도 쓰지 마라.**

## 5. 검증

```
cd frontend && npx tsc --noEmit     → 0 에러
cd frontend && npx vitest run        → 전체 1회
```

> 기준선: 바퀴 5b 시점 **`42 files · 472 tests` 전부 통과.**

**이 바퀴는 회의 테스트가 크게 흔들린다**(`MeetingList` 11 · `MeetingAfter` 11 · `MeetingLive` 4 · `MeetingDetail`).
화면이 하나로 합쳐지니 **「목록 화면이 뜬다」·「상세 화면으로 간다」를 보던 테스트는 전제가 사라진다.**

- 그런 테스트는 **「목록 칸에서 고르면 상세 칸이 바뀐다」로 다시 써라.** 지우지 마라
- **이탈 가드 테스트가 있으면 반드시 살려라.** 없으면 **새로 하나 써라**(M-2 가 조용히 깨지는 걸 막는 잠금)
- 5a·5b 처럼 **접근성·텍스트 질의가 깨지면 멈추고 기능부터 확인**해라
- **「없음」 단언이 통과로 바뀌는 것**을 특히 조심해라 — 미리보기 패널 삭제(M-4)로 그런 게 생긴다

자기점검:
- `backend/` 가 diff 에 **안 나오는가**
- **이탈 가드가 살아 있고 테스트로 잠겨 있는가**(M-2)
- 칸 내용을 **옮기기만 했는가** — 모양을 고친 곳이 없는가(6b·6c 몫)
- `meetings.css` 고아 11종을 **안 가져왔는가**(M-4)
- 선택 없음 상태에 **가짜 화면을 안 만들었는가**(M-7)

## 6. 보고

코디(`term_18011a51-e9ec-4cad-9370-1836d3ee8d2d`)에게:

- **§8-B 8건 처리 결과**
- **이탈 가드를 어디에 어떻게 다시 걸었는지** + 그걸 잠그는 테스트
- **선택 없음 상태**를 무엇으로 그렸는지
- M-8 로 안 그린 것과 근거
- `tsc` · **vitest passed/failed** · 다시 쓴 테스트 수 · 접근성 질의로 깨진 것과 처리
- **「없음」 단언이 빈 검사가 된 곳을 찾았는지**
- 막혀서 못 한 것
