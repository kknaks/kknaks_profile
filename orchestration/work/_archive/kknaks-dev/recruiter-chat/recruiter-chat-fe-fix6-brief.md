
# [frontend] fix6 — 패널·컴포저 타이포 + 모바일 드로어 (owner 피드백 3건)

너는 WORK-024 의 **kknaks-dev `frontend` 워커**다. owner 실사용 피드백이다. spec **v0.0.13** 반영 완료(U-4 모바일 · U-5 패널 타이포 · U-6 컴포저).

작업 워크트리: `/Users/kknaks/orca/workspaces/kknaks_profile/recruiter-chat`

## 수정

1. **패널 타이포 컴팩트**: 우측 문서 패널(480px) 안에서 md 제목·본문이 페이지 스케일 그대로라 너무 크다(h1 「실시간 주가를 흘린다」가 화면을 먹는다). 패널 컨테이너 스코프에서 제목·본문·표를 한 단계 이상 줄여라(예: h1→h3급, 본문 13~14px급 — 실제 값은 기존 토큰 스케일에서 골라라). **career 페이지 모달 렌더는 무변경** — 공용 렌더러 자체를 줄이지 말고 패널 쪽 스코프에서 줄인다.
2. **컴포저 입력 글자**: 입력창·placeholder 글자가 스레드 본문보다 커서 겉돈다. 스레드 본문과 시각적으로 같은 급으로 맞춰라(홈 히어로 입력창도 함께 볼 것 — 히어로는 무대라 좀 커도 되지만 /chat 컴포저는 본문과 맞아야 한다. 판단해서 보고).
3. **모바일(≤720px) — owner 확정**: 
   - 사이드바: 숨기는 대신 상단 **왼쪽 햄버거** 버튼 → 좌측 드로어(막 클릭 시 닫힘)
   - 문서 패널: **오른쪽 드로어**(화면 대부분 폭 + 막) — 지금 ≤1200px 오버레이 패턴을 모바일까지 자연스럽게 확장
   - 스크롤 계약·컴포저 고정은 동일. 햄버거는 /chat 한정(홈은 그대로)

## 검증

```
cd app/front && npx tsc --noEmit (만진 파일 0 에러). 검증은 1회만
```
+ mock dev 로 수동 확인(Playwright 실측 환영): 패널 타이포 축소 · 컴포저 크기 · 375px 폭에서 햄버거 드로어/오른쪽 드로어 열고 닫기 · career 페이지 무회귀.

## 완료 보고 — 2채널, 핸들은 preamble 우선 (subject "frontend fix6 완료")
orca send: --to term_53806a6d-ced5-4948-88bd-4181b7ba4323
