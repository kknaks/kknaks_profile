# reviewer_spec — 시안 v1 스펙 개정 검수 리포트 (2026-09-01)

**대상**: 미커밋 diff 6파일 (`git diff`, +353/-158) — `spec-154`·`spec-155`·`30-work.md`·`work-129`·`work-130`·`log.md`
**워크트리**: `/Users/kknaks/orca/workspaces/mediness-mediness/task-improve-spec` (branch `kknaksss/task-improve-spec`, 직전 커밋 `522f485ac` 는 검수 통과분 — 이번 diff 만 대상)
**판정 SSOT**: `design-analysis.md` (사용자 판정 6 · 규율 교정 3 · 코디 기본값) · `_RESUME.md` §2 · 시안 번들 `2026-09-01-mediness-task-improve/` · 발주 브리프 A~D
**코드 실측**: `/Users/kknaks/orca/workspaces/mediness-app/task-improve` (read-only)

---

## 판정: **FAIL** — FAIL 2건 · WARN 6건

발주 계약(A~D)의 **본체는 전부 착지**했다. 사용자 판정 6건 위반 0 · 규율 교정 3건 반영 · gap 실측 3건은 **코드로 전부 재확인** · lint ERROR 0 · 인덱스 3표 동기. 구 서술 스윕도 **개정 대상 2파일 안에서는** 취소선·폐기 표기로 잘 닫혔다.

FAIL 2건은 **이번 라운드가 새로 만든 계약 모순**이다 — 둘 다 「시안의 값을 계약 판정 없이 그대로 박은」 같은 결이며, design-analysis 가 스스로 세운 규율(**«시안이 계약을 정의하지 않는다 — 계약이 시안을 판정한다»**)이 두 자리에서 지켜지지 않았다.

---

## FAIL

### F-1. 좌측 사이드바 `224px` — SPEC-220 의 **사용자 확정 `288px 단일`** 과 정면 충돌 (+ SoT 복제 + 오귀속)

`products/mediness/20-spec/spec-154-decision-workflow.md:1639` (§4.19.1 치수표 첫 행, 이번 라운드 신설)

```
| 셸 | 상단 헤더 `52px` · 좌측 사이드바 `224px` · 우측 레일 `320px` | 글로벌 셸([SPEC-220](spec-220-global-ia-shell.md))과 같은 축 |
```

**어긋나는 자리** — `spec-220-global-ia-shell.md:110` · `:226` · `:493`

> **② 사이드바 폭 288px 단일 (사용자 결정).** 디자인 핸드오프의 `224px`(일반)/`288px`(문서 관리·회의실) **이원 규격을 계약 차원에서 288 단일로 override** 한다 … 핸드오프와 어긋나는 **유일한 규격 항목**이며 **본문에 px 를 적은 유일한 항목**(= 값 자체가 계약)

- 적용 범위 확인: `/ax/tasks` 는 **`내 업무` 영역**이다(`spec-220:330` §3.1 정본 매핑 2행). SPEC-220 의 폭 예외는 **`AX` 영역 하나뿐**(`spec-220:225`)이므로 **288 규칙이 그대로 걸린다.**
- 시안 `SPEC_screens.md:3` 이 `좌측 사이드바 224px` 로 그린 것은 맞다. 그런데 그 값은 SPEC-220 이 **2026-08-04 사용자 결정으로 이미 기각한 핸드오프 이원 규격의 «일반» 쪽**이다 — 즉 **시안이 기존 계약을 어긴 자리**이므로 ⒜⒝⒞ 처럼 **규율 교정 항목으로 올라갔어야** 했다. 대신 계약이 시안 쪽으로 끌려갔다.
- 오귀속: 「글로벌 셸(SPEC-220)과 **같은 축**」이라 적었으나 SPEC-220 이 말하는 값과 **다르다.** 독자가 근거를 확인하러 가면 반대 값을 만난다.
- 겹치는 규율 위반: 같은 §4.19.1 이 바로 아래에서 **「여기에 팔레트를 복사하면 두 벌이 된다」**(`:1654`)며 색 실값 복제를 금지해 놓고, **다른 SPEC 이 소유한 폭 수치는 복제**했다. `rules/document-pipeline.md` §한 곳 원칙 위반.

**처분(택1)** — ① 이 행에서 사이드바·상단 헤더 항목을 **빼고** SPEC-220 링크만 남긴다(권장 — 상세 화면 계약은 `본문:레일` 부터가 자기 소유다) ② 굳이 적으려면 **`288px`** 로 적고, 시안의 224 는 ⒜⒝⒞ 계열 **규율 교정 항목**으로 「시안 224 → 계약 288」을 명시한다.

---

### F-2. `[⋯]` 오버플로 메뉴 항목이 같은 SPEC 안에서 두 가지로 갈렸다 (조용한 교체)

`spec-154-decision-workflow.md:1609` (§4.19.1 셸 구조 — 이번 diff 에서 **`수정` → `재배정` 으로 교체**)

```
│   │                          + [⋯] 오버플로 (재배정 · 취소 · 삭제 — 다른 축)
```

**어긋나는 자리 3곳 (전부 미개정, 활성)**

| 자리 | 현행 서술 |
|---|---|
| `spec-154:2417-2426` §4.19.10 「`[ ⋯ ]` 오버플로 메뉴」 **계약 표** | 행 3개 = **`수정` · `취소` · `삭제`** (「셋의 자격…은 §4.8 «수정 · 삭제 · 취소»가 소유」) |
| `spec-154:2313` §4.19.9 read-only 표 | 「**수정·취소·삭제** 자격이 모두 없음 → `[⋯]` 자체 미렌더」 |
| `spec-154:2592` **AC-3b** | 「`[⋯]`(**수정·취소·삭제**)이 다른 축으로 분리」 |

- 결과: **§4.19.1 은 `수정` 을 잃고, §4.19.10·AC-3b 는 `재배정` 을 잃는다.** 어느 쪽도 네 항목 전부를 말하지 않고 **두 자리가 서로 다른 메뉴를 계약**한다.
- 이 SPEC 은 `:2394` 에서 이미 「거절 대체 = **`[ ⋯ ]` 의 `재배정 요청`**」(2026-08-31)을 말하고 있어 **`재배정` 이 메뉴에 있어야 하는 것 자체는 맞다.** 문제는 §4.19.10 표에 그것이 없다는 선재 구멍을, 이번 개정이 **닫지 않고 §4.19.1 쪽만 바꿔 모순으로 키운** 것이다.
- 절차 위반: 취소선·개정 노트·처분 표 어디에도 이 교체가 기록돼 있지 않다. 이 SPEC 이 스스로 「개정 = 추가 + **구 서술 grep 삭제**」(㉖-a)로 못 박은 절차가 지켜지지 않았다.

**처분** — §4.19.10 `[ ⋯ ]` 표에 **`재배정`** 행을 추가하고(자격·리셋 의미는 §4.8·domains 링크), §4.19.9 `:2313`·AC-3b `:2592` 의 열거를 **네 항목**으로 맞춘다. §4.19.1 은 그대로 두어도 된다.

---

## WARN

### W-1. §4.19.9 와이어프레임에 `메타 340` 잔존 — ⛔ 배너 누락 (스윕 구멍)

`spec-154:2243` · `:2275` (2자리) — `┌ 본문 ─────┐ ┌ 메타 340 ─────┐`

§4.19.3(`:1728`) · §4.19.4(`:1780`) · §4.19.8(`:2190`) 세 절은 이번 라운드에 **⛔ 「아래 그림은 개정 전 시안」 배너**를 받았는데 **§4.19.9(loading / error / read-only / empty) 만 빠졌다.** 그림이 계약이 아니라는 원칙은 있으나, 같은 라운드가 형제 절 셋에 붙인 표식이 하나에만 없으면 그 하나는 «아직 유효» 로 읽힌다.

### W-2. SPEC-152 의 「단일 timeline」 활성 서술 — 크로스-SPEC 스윕 누락

`products/mediness/20-spec/spec-152-incident-response-workflow.md:315`

> … → 공통 셸의 「댓글 · 진행 로그」 **단일 timeline**([SPEC-154 §4.19.10])이 이 축을 그대로 소비한다.

§4.19.10 이 이번 라운드에 **탭 2개**로 바뀌었으므로(`spec-154:2430`) 이 문장이 가리키는 구조가 더 이상 없다. 모집단·소비 관계는 그대로라 **의미는 살아 있지만 용어가 낡았다.** (스윕이 개정 대상 2파일 안에서만 돌았다.)

### W-3. WP-104(`in_dev`)의 「메타 존 340px」 활성 잔존 — 자기 참조 모순

`products/mediness/30-work/work-104-wbs-org-axis-task-entry.md:711`

> **셸** — 헤더 존 … / 본문 존(좌, `flex:1`) / **메타 존(우 `340px` sticky**, 자체 스크롤). … **치수·간격은 §4.19.1 표 그대로**

같은 줄이 §4.19.1 을 정본으로 가리키면서 **§4.19.1 이 폐기한 값(340px 메타 존)** 을 적고 있다. WP-104 W9 는 `in_dev` 라 실제로 이 문장을 읽고 작업할 사람이 있다.

### W-4. 「지남 강조 금지」가 WP-114·30-work.md 에 활성으로 남음

- `30-work/work-114-task-kanban-redesign.md:127` — `- [ ] **due 표시 추가** … ⚠ **지남 강조 금지**`
- `30-work.md:84` — 「due 표시 추가 … ⚠ **지남 강조는 범위 밖** — 판정 규칙이 WBS 축에 한 벌 있어 두 벌을 만들지 않는다」
- `30-work.md:287` — 「due 표시(**지남 강조 제외**)」

WP-114 **스코프** 서술로는 여전히 참이지만, **근거로 적힌 문장(«두 벌 금지»)은 이번 라운드가 명시적으로 폐기**했다(`spec-154:2039`·`:2057` — 「서버 판정 축에만 걸린다」). 이대로면 WP-114 를 읽는 dev 가 D-3 강조를 **제품 전역 금지**로 읽는다. 최소 「→ WP-130 P7 이 도입(프론트 파생)」 한 줄 pointer 가 필요하다.

### W-5. AC-38 / WP-130 P7 검증의 Given 이 v1 보드에서 도달 불가하게 읽힌다

- `spec-154:2627` **AC-38** — 「**Given** 담당자가 아닌 **요청자로** 「내가 요청한 일」에 해당하는 태스크의 **카드를 본다**」
- `30-work/work-130-task-detail-unification.md:301` — 「요청자 자격으로 **카드** 상태 칩을 열면 `취소` 하나만 뜸」

같은 라운드의 **AC-39**(`:2628`)와 §4.19.6-R v1 표(`:2110`)가 **v1 보드 = `scope=mine` 단일 모수 · 요청 축 진입 0** 을 계약했다. 요청자는 자기가 요청한 태스크의 **담당자가 아니므로 그 카드가 보드에 뜨지 않는다.** 도달 경로는 **관리자 `태스크(전체)`(`scope=all`)** 뿐인데 AC 가 그 전제를 적지 않아, 문면 그대로는 **실행할 수 없는 수용 기준**이다. Given 에 `scope=all` 을 명시하거나 AC 를 v2 로 미루는 편이 낫다.

### W-6. `due` 가 세 자리에 렌더되는데 근거가 없다

`spec-154:1605`(헤더 `D-N · M. D. 마감` pill) · `:2453`(메타 스트립 「기한 — 최종 기한만」) · `:1662`(레일 「일정」 카드 **마감기한** 행, 강조 표기)

시안이 실제로 셋 다 그렸으므로 **기재는 충실**하다. 다만 같은 절이 **출처 중복**(참고자료 첫 줄 ↔ 메타 「출처」)에는 「**중복이 아니다** — 메타는 «어디 소속인가», 첫 줄은 «무엇을 근거로 하는가»」라는 근거를 달았는데(`:2336`), **같은 값의 3중 렌더에는 근거가 없다.** 「pill = 남은 일수 판정 / 스트립 = 값 / 일정 = 시간축 위치」 정도의 한 줄이면 닫힌다.

---

## PASS — 항목별 근거

### 1. 사용자 판정 6건 (위반 0)

| # | 판정 | 착지 | 활성 잔존 |
|---|---|---|---|
| ① | 상세 2단 배치 · **5부 = 구성 요소 목록**(순서 계약 아님) | 개정노트 ㉘ ⑴(`:37`) · §4.19.1 셸(`:1597~`) · §4.19.10 「5부 요소의 자리」 표(`:2320~`) · AC-1(`:2587`) · WP-130 P5(`:224`) | **«6블록» 활성 0** — 전 자리 취소선/폐기 표기(`:1629`·`:1786`·`:2517`·`:2587`, WP-130 `:49`·`:224`) |
| ② | 완료 모달 활성 = **요약 또는 제출자료 1** | §4.8 필수 조합 행(`:904`) · §4.19.10 「활성 조건」(`:2360`) · AC-35(`:2624`) · WP-130 P5(`:239`) | **«요약 필수» 활성 0** — 두 자리 모두 「시안 프로토의 …는 **계약이 아니다**(기각)」 형태. `*` 필수 표식 제거도 2자리에 명시 |
| ③ | 출처 마커 **5값 유지** | 개정노트 ⑶(`:38`) · §4.19.12 미도입 행(`:2516`) · AC-20 무변경 · WP-130 P7(`:289`) | 시안 2값 채택 흔적 0 |
| ④ | **D-3 이하 강조**(프론트 파생) | §4.19.6 (b) 신설 행(`:2040`) · AC-33(`:2622`) · 구 서술 2자리 취소선(`:2039`·`:2057`) | **서버 축 신설 서술 0** — 「`overdue`·`urgent` 판정 축·응답 필드를 신설하지 않는다」가 3자리(개정노트·(b)행·AC-33) |
| ⑤ | 「내가 요청한 일」 = **v2 OQ** | §4.19.6-R v1 표(`:2108~`) · OQ-19 v1 해소 + **OQ-22 신설**(`:3045`·`:3046`) · AC-39(`:2628`) | 「내가 요청한 일」 27자리 전수 확인 — **v1 확정 서술 0**. 전부 v2/필터 축/조회 축 문맥 |
| ⑥ | 권한·상태별 수정 = **v2**(삭제 3원칙 이월) | §4.8 삭제 자격 행 취소선(`:893`) · **OQ-23 신설**(`:3047`) · AC-40(`:2629`) · WP-130 P4 작업·검증 동기(`:205`·`:206`·`:218`) | **v1 계약으로 남은 3원칙 0** — 활성 「올린 본인」 언급은 전부 «v2 이월» 문맥 |

### 2. 규율 교정 3건

- **드롭다운 항목 소스 = 서버 `allowed_transitions`** — 개정노트 ⒜(`:42`) · §4.19.6 (b) 상태 칩 행(`:2037`) · AC-38 · WP-130 P7(`:291`). 「시안의 5값 고정 나열은 **프로토타입 한정**」이 3자리에 명시. 사유 필수 전이 2단계도 함께.
- **태그 → 배지 5슬롯 매핑** — §4.19.2 「시안 헤더 태그 → 5슬롯 매핑」 표 신설(`:1704~`). `[제품]+[메디니스]` = ①2칩 한 쌍 / `[WBS]` = ②출처 / ③④⑤ = *(시안에 없음)*. 「**새 슬롯·새 배지행을 만들지 않는다**」 + 「상태 칩·`D-N` pill 은 배지행이 아니다」 명시.
- **0차 숨김 · 빈 그룹 미렌더 · 빈 컬럼 유지** — 개정노트 ⒞(`:44`) 불변 확인 + WP-130 P7 작업·검증 양쪽에 재기재(작업 `:285`·`:288`·`:289` / 검증 `:299`·`:302`).

### 3. gap 실측 3건 — **코드로 전량 재검증, planner 판단 «신규 서버 작업 0» 이 맞다**

| 주장 | 실물 | 판정 |
|---|---|---|
| 상세 응답 일정 6행 이미 실림 | `back/app/services/action_runtime/tasks/manual_surface.py:600-608` — `planned_start_at`·`due`·`expected_completion`·`started_at`·`completed_at`·`canceled_at` + `:610 created_at` | ✅ 정확 |
| 목록 행 `completed_at` + 전량 로드 | `workflow/tasks_surface.py:project_tasks` 에 `created_at`·`started_at`·`completed_at`·`canceled_at`·`due`·`expected_completion`; `planned_start_at` 은 `manual_surface.py:1102 decorate_task_rows` 가 additive 로 얹음(§4.19.13 표의 4필드 주장 **전부 성립**). `GET /tasks`(`routers/action_runtime_v2.py:411`)는 `list_by_assignee/list_all/list_requested_by` 를 그대로 반환 — **limit·offset·cursor 없음** | ✅ 정확 |
| `sort_order` + 재배열 endpoint(부분 거절) | 컬럼 `models/action_runtime.py:722` (+ 인덱스 `:728`) · endpoint `routers/action_runtime_v2.py:631 POST /tasks/{task_id}/check-items/reorder` · **부분 목록 거절** `tasks/check_items.py:99-104`(집합 불일치·길이 불일치 → `ValidationError`) | ✅ 정확 |

§4.19.13 「2026-09-01 2차 — 신규 gap: 없음」 표(`:2566~`, 목록 행 근거 `:2573`)와 WP-130 P0 체크(`:132-136`)의 서술이 **셋 다 실물과 일치**한다. 과대 주장 없음.

### 4. WP-130 정합 (6항 전부)

- **BLOCKED 0** — Phase 7 `Status: TODO`(`:281`) · 상단 ✅ 배너(`:27`) · PR 분리 가정(`:120`) · `30-work.md:116` Status Board Blocker 칸 「**없음 — BLOCKED phase 0**」. 문서 전체 `BLOCKED` 활성 0.
- **P5 ↔ P7 선후행** — PR 분리 가정(`:120`) + **OI-7 닫힘 각주**(`:355`) 양쪽에 「P7 은 P5 뒤」와 그 이유(모달 컴포넌트 소비) 기재. `30-work.md:116` 에도 「내부 선후행 = P7 은 P5 뒤」.
- **P4 자격 단순화** — `:205` 작업(추가·삭제 둘 다 접근 가능자, 3원칙 취소선 + OQ-23 링크) · `:206` projection 키는 **유지**(v2 게이팅 자리) · `:218` 검증에 「접근 자격 «안» 이면 올린 사람이 아니어도 삭제된다(의도된 상태)」 명시. 좋은 처리 — 키를 지우지 않아 v2 가 additive 로 얹힌다.
- **OI-6 / OI-7 닫힘** — `:354`·`:355` 둘 다 취소선 + 닫힌 사유 + 이월처(OQ-22 ③) 기재.
- **OI-8 신설** — `:356` 완료 컬럼 초기 월·빈 월 문구, 「planner 판단 · 사용자 뒤집기 가능」 + 기각한 대안(「최근 완료 달로 점프」)과 그 이유까지.
- **Pre-deploy 공지 2건** — `:331`(완료 컬럼 월 단위 절단) · `:332`(참고자료·산출물·일정의 레일 이동). 둘 다 「사용자가 없어진 줄 안다」 관점.

또한 **WP-129 6번째 입구 3자리 등재 확인** — P0 목록화(`:124`) + P0 상세 각주(`:125`) + P5 작업(`:210`) + P6 테스트 「**6경로 전부**」(`:239`). 코드 실측도 일치: `back/app/services/version_wbs.py:1730 register_decision_from_wbs_task`, 호출자는 `back/tests/api/test_wp104_w6_task_surface.py:30,638` **테스트 하나뿐 = 프로덕션 호출자 0**(휴면 seam 주장 정확).

**SPEC-155 `assignee_name` 정정 3자리** — §6.1 산출 필드 행(`:282`) + 신설 경고 행(`:283`) / 「수정 산출의 의미」 예외 표기(`:291`) / §6.4 매핑 행(`:384`). 개정 노트도 상단에 추가(`:24`). 「원장 저장 컬럼은 여전히 `assignee_member_id`」 구분이 3자리 전부에 붙어 있어 혼동 방지됨. **요청 3자리 충족.**

### 5. planner 기본값 «사용자 뒤집기 가능» 표기

개정노트 머리(`:36` — 「⒟~⒡ 는 … planner 가 채운 기본값(**사용자 뒤집기 가능**)」)에 더해 개별 자리에도 실재:

| 기본값 | 표기 자리 |
|---|---|
| 완료 컬럼 월 필터 = `completed_at` | 개정노트 ⒟(`:45`) · §4.19.6 (a) 신설 행(`:2002`) |
| `goal` 렌더 = 개행 → 불릿 | 개정노트 ⒠(`:46`) · §4.19.1 본문 4블록 표 「목표」 행(`:1675`) |
| 출처 자동 링크 행 = 참고자료 최상단(삭제 X 없음·시각 구분) | §4.19.1 레일 3카드 표 「참고자료」 행(`:1663`) · §4.19.10(`:2336`) |
| 체크리스트 드래그 재정렬 | 실측으로 **채택** 확정(개정노트 ⒡ `:47`) — 기본값이 아니라 사실 판단으로 승격, 정당 |
| @멘션 v2 | OQ-24(`:3048`)로 착지. **«뒤집기 가능» 문자 표기는 없으나** OQ 자체가 미결 표현이라 실질 충족 |

추가로 WP-130 OI-3·OI-4·OI-8 도 같은 표기를 달고 있다. **요구 4건 충족.**

### 6. lint · 인덱스 3표

```
python3 scripts/lint-pipeline.py --strict → 0 error, 255 warning (exit 0)
```

- mediness 범위 **ERROR 0**.
- mediness WARN 은 `30-work.md:242 MEDINESS-SPEC-030 Spec Coverage 구현 상태 derive 불일치` **1건뿐이고 이번 diff 와 무관한 선재분**(SPEC-030 / WP-045·089·090·113). 나머지 254 WARN 은 전부 타 product(`procedure-hub`·`selly`)의 `doc_no`·`version` 누락.
- 인덱스 3표: `30-work.md` Status Board(`:116`) · WP List(`:207`) · Spec Coverage SPEC-154 행(`:287`) 전부 갱신됨. WP List 는 BLOCKED 컬럼이 없어 변경 불필요(정상).
- `log.md` entry 1행 추가(`:18`) — `종류 = spec-change, wp-change` · 영향 ID 4건 · PR `#675` · 결정 근거를 담은 장문 요약. 규약 부합.

---

## 참고 — 브리프가 «FAIL 금지» 로 못 박은 자리 (검수 제외 확인)

- **v2 OQ 3건**(OQ-22·23·24)이 «미완성» 인 것 → 발주 계약. FAIL 로 세지 않음.
- **planner 기본값**이 «사용자 미확정» 인 것 → 발주 계약. 표기 실재 여부만 검사(§5 PASS).
- WP-129 frontmatter/Board 가 아직 `proposed` 인 것 → code PR #140 머지 후 갱신 예정(_RESUME §1), 이번 라운드 범위 밖.

---

## 처분 요약

| # | 심각도 | 자리 | 한 줄 |
|---|---|---|---|
| F-1 | **FAIL** | `spec-154:1639` | 사이드바 `224px` → SPEC-220 의 사용자 확정 `288px 단일` 과 충돌. 행에서 빼거나 288 로 고치고, 시안 224 는 규율 교정 항목으로 |
| F-2 | **FAIL** | `spec-154:1609` vs `:2417`·`:2313`·`:2592` | `[⋯]` 메뉴가 «재배정·취소·삭제» ↔ «수정·취소·삭제» 로 갈림. §4.19.10 표에 `재배정` 추가 + 3자리 열거를 4항목으로 |
| W-1 | WARN | `spec-154` §4.19.9 | `메타 340` 잔존 · ⛔ 배너 누락(형제 절 3개는 받음) |
| W-2 | WARN | `spec-152:315` | 「단일 timeline」 활성 — 탭 2개로 개정됨. 크로스-SPEC 스윕 누락 |
| W-3 | WARN | `work-104:711` | 「메타 존 340px … §4.19.1 표 그대로」 자기모순 (WP-104 는 `in_dev`) |
| W-4 | WARN | `work-114:127` · `30-work.md:84,287` | 「지남 강조 금지」 근거가 폐기됨 — WP-130 P7 pointer 필요 |
| W-5 | WARN | `spec-154:2627` · `work-130:302` | 요청자 카드 Given 이 v1 단일 모수 보드에서 도달 불가로 읽힘 (`scope=all` 전제 명시 필요) |
| W-6 | WARN | `spec-154:1605`·`:2453`·`:1662` | `due` 3중 렌더에 근거 문장 없음 (출처 중복에는 있음) |

---

# R2 재검수 — 시안 정정 라운드 (2026-09-01, targeted)

**범위**: 위 F-1·F-2·W-1~W-6 의 처분 착지 + 좌표 밖 잔존 3건(코디 원라이너) + 새 모순 여부(변경 파일 안) + lint. **그 밖은 검사하지 않음**(R1 PASS 항목 재검증 X).
**대상**: 미커밋 diff 9파일 — `spec-152`·`spec-154`·`spec-155`·`30-work.md`·`work-104`·`work-114`·`work-129`·`work-130`·`log.md`
**방식**: read-only (문서 수정 0)

## 판정: **PASS** — FAIL 0 · WARN 0 · 참고 2

8건 전부 착지했고 **새 모순 0**, lint mediness ERROR 0. 두 FAIL 은 처분 표의 권장안(F-1 ①, F-2 그대로)으로 닫혔다.

---

## 1. F-1 해소 — 치수표 셸 폭 잔존 0 · 규율 교정 ⒟ 신설 · SPEC-220 정합 ✅

| 검사 | 결과 |
|---|---|
| §4.19.1 치수표 `셸` 행(`spec-154:1640`) | **`224px`·`52px` 제거** — 「글로벌 셸 폭(상단 헤더 · 좌측 사이드바)은 이 표가 소유하지 않는다 → SPEC-220」 + 「이 화면이 소유하는 치수는 **`본문:레일` 부터**」. 처분 ① 채택 |
| 규율 교정 ⒟ 신설(`:46`) | 실재. 「시안 `224px` 는 채택하지 않는다」 + 기각 근거(2026-08-04 사용자 결정 · 핸드오프 이원 규격의 «일반» 쪽) + **적용 범위 재확인**(`/ax/tasks` = `내 업무` 영역이라 폭 예외 `AX` 에 안 걸림) + 소유자 지정 |
| 문자 재부여 정합 | 개정노트 머리(`:36`)가 **「⒜~⒟ = 규율 교정 / ⒠~⒢ = planner 기본값」** 으로 갱신. 본문 `:43~:49` 7항목 순서 일치. **⒜~⒢ 참조는 spec-154 자기 노트와 log.md 뿐**(전 product grep) — 타 문서 stale 참조 0 |
| SPEC-220 정합 | `spec-220:110`·`:226`·`:493` 의 **`288px 단일`** 과 충돌 서술 0. 오귀속(「같은 축」) 문장 제거됨 |
| 활성 `224px` 잔존 | **0** — 전 product grep 결과 `224px` 는 `spec-220`(SoT, 기각 대상 기술) 2자리와 `spec-154:46`(규율 교정에서 «채택 안 함» 으로 기술) 뿐 |

## 2. F-2 해소 — `[⋯]` 4항목이 4자리 + WP 3자리에서 같은 말 ✅

| 자리 | 현행 |
|---|---|
| `spec-154:1610` §4.19.1 셸 | `수정 · 재배정 · 취소 · 삭제` |
| `spec-154:2316` §4.19.9 read-only | `수정·재배정·취소·삭제` 자격이 모두 없음 *(~~수정·취소·삭제~~ 취소선 정정 표기)* |
| `spec-154:2427` §4.19.10 **계약 표** | **`재배정` 행 신설** — 의미·terminal 금지·`todo` 리셋·정본 위임(§4.8·domains)·AC-18c 근거까지. `:2431` 에 「이 표는 ~~셋~~ 이 아니라 «네 항목»」 정정 노트 + 열거 자리 3곳 명시. `:2433` 「**넷 다** 없으면 `[⋯]` 미렌더」 |
| `spec-154:2598` AC-3b | `수정 · 재배정 · 취소 · 삭제` + 구 3항목 취소선 + 원인(2026-08-31 재배정 도입 미반영) |
| `work-104:167`·`:721`·`:958` | 3자리 전부 `수정·재배정·취소·삭제` + 「넷 다 없으면 미렌더」 동기 |

- **활성 3항목 열거 0** — 전 product grep(`수정·취소·삭제` 4변형). 남은 hit 은 전부 **역사 기록 또는 §4.8 절 이름 참조**: `spec-154:81`(⑲-a 개정노트) · `:823`(§4.8 헤딩 «수정 · 삭제 · 취소»는 세 개념 구분 절이지 메뉴 열거가 아님) · `:2184`·`:2432`·`:2640`(그 절 이름 참조) · `spec-155:997`·`work-091:314`(다른 축·타 WP 역사).
- 교차 정합: §4.20.11 `재배정` 행(`:2832`)·AC-D14(`:2870`)·AC-18c(`:2616`)·§4.8(`:819`)이 §4.19.10 신설 행과 **같은 말**(terminal 금지 · `todo` 리셋 · `started_at` 클리어). 새 계약을 발명하지 않고 기존 정본을 링크만 했다.

## 3. W-1~W-6 처분 착지 ✅ + 340/224 활성 잔존 0

| # | 착지 |
|---|---|
| W-1 | `spec-154:2238` **⛔ 배너 신설** — 「그림에 남은 `메타 340`(**2자리**)…전부 폐기」 + 현행 존 구성 + 「살아 있는 것은 각 상태의 «규율» 다섯」. 형제 절 3개와 동형. 배너가 말한 2자리 = `:2246`·`:2278` **실측 일치** |
| W-2 | `spec-152:315` — 「단일 timeline」 삭제 → **탭 2개 + 각 건수**로 용어 동기 + 「바뀐 것은 «표현 형태» 뿐, 모집단·저장 축(`TaskEvent` 한 벌)은 그대로라 이 절의 «충돌 없음·불변» 은 유효」 |
| W-3 | `work-104:709` phase 배너 신설(폐기 5항목 열거 + 「W9 착수 시 §4.19.1·§4.19.10 먼저 읽는다」) + `:712` **`340px` 취소선 → 우측 레일 `320px`**. 배너가 `:723`(반응형 `메타 340/280`)·`:713`(본문 순서)를 명시 커버 |
| W-4 | `work-114:127` — 「지남 강조 **금지**」 → 「**이 WP 범위 밖**」 + 「🔑 제품 전역 금지가 아니다」 + 구 근거(«두 벌 금지» = 서버 판정 축 한정) + **도입 자리 = WP-130 P7** pointer. `30-work.md:84`·`:287` 동일 pointer 동기 |
| W-5 | `spec-154:2633` **AC-38 Given 재서술** — 「v1 보드(`scope=mine`)에 선 **내 담당 태스크** ⓐ 허용 전이 여럿 / ⓑ terminal」 2케이스로 교체 + 「요청자 자격은 v1 보드 카드로 **도달하지 않는다**」 명시 + 요청자 모집단 확인은 **상세**(§4.19.6-R R-5) · 보드 카드 확인은 **v2**(OQ-22). `work-130:302` 검증 문장 동기. R-5(`:2127`)가 「v1 준수 — `allowed_transitions` 가 요청자에게 `canceled` 만」으로 실재 |
| W-6 | `spec-154:2466` — 「`due` 가 세 자리에 서는 것은 중복이 아니다」 + **pill = «얼마 남았나» / 스트립 = «값 자체» / 레일 = «시간축 위의 위치»** + 「그래도 **판정은 한 벌**」. 출처 중복 근거(`:2336`)와 같은 결로 정렬 |

**340/224 활성 잔존 0** — `340px`·`메타 340`·`우 340`·`340/280`·`224px` 전 product grep 25 hit 전수 분류:

- 취소선/폐기 표 8자리(`spec-154:37`·`1626`·`1631`×2·`1789`·`1877`·`2450`·`2523`·`2605`)
- ⛔/⚠ 배너 커버 ASCII 7자리(§4.19.3 `:1740` / §4.19.4 `:1801`·`1851`·`1889` / §4.19.9 `:2246`·`2278` / work-104 `:723`)
- 배너 본문 4자리(`:1729`·`:2238`·`work-104:709`×2)
- 규율 교정 서술 1자리(`:46`)
- SPEC-220 SoT 2자리(`:110`·`:226` — 기각 대상 원본 기술, 소유자 자리)
- work-104 취소선 정정 1자리(`:712`)

⇒ **계약으로 읽히는 활성 340/224 = 0.**

## 4. 새 모순 — 없음 (변경 파일 안)

- **인덱스 3표** — lint 의 SPEC/WP List·Spec Coverage 동기 + **WP status 3자 일치**(ERROR 급)가 전부 통과. 정정이 Board/List 를 건드리지 않았다.
- **§4.19.1 치수표** — 셸 폭 행을 뺐지만 `본문:레일 320px 고정` 이 남아 있어 `work-104:712`·§4.19.10 `:2463`·AC-37 `:2632`·AC-9 `:2605` 가 참조할 정본이 그대로 성립.
- **AC-38 재서술 ↔ 규율 교정 ⒜** — 드롭다운 항목 소스(서버 `allowed_transitions` · 시안 5값 고정 나열 기각) 검증이 담당자 카드 ⓐ/ⓑ 로 **보존**됐다. ⒜ 의 커버리지가 재서술로 사라지지 않았다.
- **`재배정` 행 신설 ↔ 삭제 축** — §4.20.11 「terminal 재배정 금지와 삭제는 다른 축」·AC-D14 와 충돌 0.
- **개정노트 문자 재부여** — log.md `:18` 이 「기존 ⒟~⒡ → ⒠~⒢ 재부여」를 명시 기록. 외부 stale 참조 0(grep).
- **log.md** — entry 1행 안에서 정정 8건 전부 서술(⒜=F-1 · ⒝=F-2 · WARN 6). 규약(표 1행 · 결정 entry 근거 충분) 부합. 새 행 추가 없이 기존 행 확장 — 같은 PR·같은 날 라운드라 정당.

## 5. lint ✅

```
python3 scripts/lint-pipeline.py --strict → 0 error, 255 warning (exit 0)
```

- **mediness ERROR 0.** mediness WARN = `30-work.md:242 MEDINESS-SPEC-030 derive 불일치` **1건뿐**이며 R1 과 **동일한 선재분**(SPEC-030 / WP-045·089·090·113 — 이번 diff 무관). 나머지 254 는 타 product(`procedure-hub`·`selly`) frontmatter 누락.
- 총계가 R1 과 **동일**(0/255) — 정정이 새 lint 신호를 만들지 않았다.

---

## 참고 (비차단 · 판정에 반영 안 함)

- **N-1. `치수표:1640` 근거 칸이 `288px` 를 적는다.** 같은 셀이 「폭 수치를 여기 복제하면 두 벌이 된다」고 말하므로 문면상 약한 긴장이 있다. 다만 ⒟ 와 처분 ② 가 **288 기재를 명시 허용**했고, 소유자(SPEC-220)·결정일(2026-08-04)이 함께 박혀 있어 오귀속 위험은 없다. log.md 의 「**링크만** 두고」 표현만 실물보다 약간 앞선다.
- **N-2. `work-104:165`(행위 변화 ⑭)의 본문 순서 서술이 여전히 구 계약**(`제목 → 1. 배경 및 목표 → 조건부 확장 → 2. 체크리스트 → 댓글`). **선재분**이고 R1 도 지목하지 않았으나, 같은 표의 ⑯(`:167`)은 이번에 손봤으므로 같은 결의 잔존이다. W9 phase 배너(`:709`)의 커버 범위 밖(그 배너는 「아래 작업 목록」 대상). 다음 라운드에서 한 줄이면 닫힌다.
