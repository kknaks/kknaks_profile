# 리뷰 리포트 — task-improve / planner (문서 리포) (2026-09-01)

> 이 파일은 **덮어쓴 것**이다. 이전 내용(롤백된 구 라운드 검수)은 이번 판정과 무관하다.

## 판정: FAIL — 위반 1건 (V-1: 「배정 부트스트랩 비활성」의 SPEC 착지 부재 + WP 간 모순)

결정 SoT(`_RESUME.md` §2) 15건 대조에서 **결정 위반 0건**, 구 서술 활성 잔존 grep 스윕 **0건**, lint mediness 범위 **ERROR 0**, 비어 있어야 할 자리(§4.19.6-R · WP-130 P7) **선확정 없음**. 직전 두 라운드를 FAIL 시킨 「구 서술 잔존」 패턴은 이번 라운드에서 재발하지 않았다 — 원장 렌더·마크다운 합침·본인 고정·자동 cc 네 축 모두 취소선/폐기 표기로 정리됐다.

**막는 것은 한 자리다.** WP-129 가 «배정 부트스트랩 비활성» 을 작업 항목으로 들고 있는데 **그 계약을 받는 SPEC 이 어디에도 없고**, SPEC-154 §4.8 의 부트스트랩 계약은 개정 표기 없이 활성으로 남아 있으며, **WP-130 P3 검증은 그 부트스트랩이 계속 도는 것을 전제**한다. 두 WP 가 같은 라운드에서 같은 메커니즘에 반대되는 말을 한다.

---

## 검수 범위

- diff: `origin/mediness`..worktree, **수정 13 파일 + untracked 2 파일**
  - SPEC 9: `spec-060` · `spec-111` · `spec-115` · `spec-125` · `spec-152` · `spec-154` · `spec-155` · `spec-156` · `spec-230`
  - 아키텍처 2: `40-architecture/domains/runtime_task.md` · `40-architecture/erd.md`
  - 인덱스 2: `30-work.md` · `log.md`
  - 신규 2: `30-work/work-129-task-request-axis.md`(doc_no 249) · `30-work/work-130-task-detail-unification.md`(doc_no 250)
- **allowed_paths 이탈 0** — 전 파일이 `products/mediness/` 안. `context/` 는 미변경.
- 실행한 검사:
  - `python3 scripts/lint-pipeline.py --strict` → **0 error / 255 warning**(exit 0). mediness 범위 WARN 은 **1건뿐이며 선재분**(`30-work.md:242` SPEC-030 coverage derive — WP-045/089/090/113 축, 이번 diff 밖)
  - 구 서술 grep 스윕 7패턴 × `20-spec/` + `40-architecture/` 전량
  - `30-work.md` Spec Coverage ↔ 두 WP frontmatter `covers` 기계 대조(파이썬)
  - `doc_no` 최대값 스캔(현행 max 248 → 249·250 순차 정확)
  - 구 파일명(`work-129-task-request-and-detail-unification`) 잔존 grep → 0

---

## 위반 (FAIL 사유)

### V-1. 「배정 부트스트랩 비활성」이 WP 에만 있고 SPEC 착지가 없다 — 게다가 두 WP 가 반대로 말한다

- `products/mediness/30-work/work-129-task-request-axis.md:208` — P5 작업 `- [ ] 배정 부트스트랩 비활성` (한정어 없음. `:50` Scope · `:123` P0 목록화에도 동일 항목)
- `products/mediness/20-spec/spec-154-decision-workflow.md:972-990` — **§4.8 「배정 부트스트랩 태스크」 계약이 활성으로 그대로 있다.** 개정 표기·취소선 0. 그 표의 `생성 시점` = **「라운드를 여는 승인 3곳 — 결정 승인(요청) · 지시 접수 자동 승인(지시) · [후속 실행] 승인(루프)」** 이라 **지시 흐름 밖의 두 자리를 포함**한다. 같은 계약이 `:298`(상세 모달 배정 동선) · `:530`·`:1054`·`:1060`(알림 ⑤) · `:1187`(§4.5.4 태스크 0건 착지) · `:1642`(§4.19.2 `[배정]` 문맥 배지) · `:1724`·`:2248`(§4.19.1 `[+ 태스크]` 안내 블록 **«남는다»**) · `:1748`(와이어프레임 조건부 블록) · `:1965-1966`(§4.19.6 속 빈 마커 · 툴팁 `태스크 배정 안내`) 에 활성으로 반복된다
- `products/mediness/20-spec/spec-115-personal-dashboard.md:71` — 이번 개정 노트가 비활성 대상을 **명시적으로 3자리로 열거**한다(`① §U-3 업무수행 세그먼트 · ② §U-2 실행③ 인라인 배정 · ③ GET /decisions/me/tasks`). **부트스트랩은 그 목록에 없다** (`grep -c 부트스트랩 spec-115` = **0**)
- `products/mediness/20-spec/spec-154-decision-workflow.md:41`(개정 노트 ㉗ ⑤) — 입구 차단의 **계약 착지를 `SPEC-111 §3.3 · SPEC-156 §6.1 · SPEC-115 §U-2·§U-3` 로만 명시**한다. 부트스트랩 착지 자리를 지정하지 않았다
- `products/mediness/30-work/work-130-task-detail-unification.md:187` — P3 검증 `- [ ] 시스템 완료(배정 부트스트랩 자동 완료·incident 슬랙 [완료])가 **그대로 성공**`. **WP-129 P5 가 끄겠다는 그 메커니즘이 계속 돈다는 전제**. 같은 전제가 `spec-154:890`(완료 근거 시스템 액터 예외 예시)에도 활성으로 실려 있다

**어긴 규칙의 출처**

- `_RESUME.md` §2 (2026-09-01) — 「+ Q3의 개인 대시보드 3자리·**배정 부트스트랩** 비활성 포함」 = 확정 결정. 결정이 있는데 **SPEC 이 그것을 받지 않았다**
- `task-improve-spec-brief.md` §6-2 — 「«추가»만 하지 말고 **구 서술을 grep 으로 찾아 취소선/정정하라** — 직전 두 라운드가 전부 이 잔존으로 FAIL 났다」
- `rules/document-pipeline.md` §한 곳 원칙 / §문서별 추상화 수준 — **사용자에게 보이는 동작·계약은 SPEC 이 정본**이고 WP 는 계약 본문을 소유하지 않는다. WP 에만 있는 계약 변경은 정본이 없는 변경이다
- `roles/mediness/reviewer/rules.md` §planner 리뷰 — 「spec↔WP 정합: WP 가 참조하는 SPEC 조항이 실제로 존재하고, 개정된 spec 내용과 WP 본문이 어긋나지 않나」

**왜 «문체 지적» 이 아닌가** — 실행자가 이 두 문서만 보고 착수하면 **WP-129 담당은 부트스트랩을 끄고, WP-130 담당은 그것이 도는지 검증한다.** 게다가 SPEC-154 §4.8 은 「끄지 않는다」고 읽히는 유일한 정본이라 **코드 리뷰에서 어느 쪽이 맞는지 판정할 근거가 없다.**

**권장 수정 — 둘 중 하나를 planner 가 고른다 (사용자 확인 대상)**

- **ⓐ 좁은 해석**(부트스트랩은 지시 흐름 자리만 소멸): WP-129 `:50`·`:123`·`:208` 을 **「지시 접수 자동 승인 발 부트스트랩 한정」** 으로 한정하고, SPEC-154 §4.8 부트스트랩 표 `생성 시점` 행의 «지시 접수 자동 승인(지시)» 만 취소선 + 「입구 차단으로 이 자리는 새로 발화하지 않는다(§4.4)」 각주. WP-130 P3 검증은 그대로 유효해진다
- **ⓑ 넓은 해석**(부트스트랩 전면 비활성): SPEC-154 §4.8 §배정 부트스트랩 태스크 전체에 「2026-09-01 비활성 — 주석 수준·재활성 가능」 블록을 얹고 `:1724`·`:2248`(「안내 블록은 남는다」)·`:1642`·`:1965-66`·`:1187`·`:1054`·`:298` 을 함께 정정, `spec-154:890`·WP-130 P3 검증에서 부트스트랩 예시를 걷어낸다

ⓐ 가 결정 문맥(샤라웃 지시 배제 묶음)과 SPEC-115 의 3자리 열거에 더 맞아 보이나, **판단은 planner/사용자 몫**이다 — 리뷰어가 계약을 고르지 않는다.

---

## 경미 (WARN)

- **W-1. `spec-154:1745`·`1832` — ASCII 와이어프레임 안에 구 문자열 잔존.** `│  ↑ 원장에서 **렌더**한다 — 저장하지 않아` / `│   ↑ incident 원장에서 **렌더**. 저장 X`. **FAIL 로 잡지 않는 이유** = 바로 위 `:1722` 가 그 두 문자열을 **원문 인용 + 취소선으로 폐기 선언**하고 재도해를 WP-130 FE phase 로 넘겼으며, 이는 ㉖ 개정이 `수락 대기` 칩에 쓴 **선례 그대로**다(`spec-154:46` 「계약은 표에 있고 그림이 계약을 정의하지 않는다」). 다만 다음 라운드 grep 스윕에서 같은 히트가 계속 나온다 — WP-130 P5 재도해 때 실제로 지워지는지가 확인 지점.
- **W-2. `spec-060:250` 개정이 어느 WP 에도 안 실린다.** `task_draft_request` 의 「담당자 본인 고정」 각주 정정인데 SPEC-060 은 두 WP 의 `covers` 에도, Spec Coverage 의 129/130 행에도 없다. **본문이 「툴 입력 스키마는 여전히 `utterance` 하나이고 담당자 파라미터가 생기지 않는다」고 못 박아** 구현 작업이 없는 문서 미러 정정이므로 정합 위반은 아니다. 다만 `log.md` 영향 ID 에는 SPEC-060 이 들어 있으니, 이 자리를 「구현자 없는 변경」으로 읽을 수 있다는 점만 남긴다.
- **W-3. WP-130 OI-2(`apply_derived_transition` 완료 근거 취급)가 P3 착수 전까지 열려 있다.** 「체크박스 다 켰는데 완료가 안 되는」 상태 vs 「근거 없는 완료 뒷문」의 갈림인데, 기본 제안(파생 경로 면제)이 `runtime_task.md:273` 과 WP-130 OI-2 양쪽에 같은 문장으로 있어 모순은 없다. 사용자 WP 리뷰에서 짚을 후보.
- **W-4. `30-work.md:242` SPEC-030 coverage derive WARN** — 선재분(WP-045/089/090/113 축). 이번 diff 와 무관.
- **W-5. 타 제품 WARN 254건**(linky·charty·procedure-hub·selly 의 `doc_no`/`version` frontmatter 누락, `20-spec.md`/`30-work.md` frontmatter 부재) — **전부 무관·선재분**. mediness 범위 아님.

---

## 기존 부채 (이번 판정 제외)

- `spec-154` 는 개정 노트가 ㉗ 까지 누적돼 파일 상단 30줄이 개정 이력이다. 계약 본문 대비 이력 비중이 커지는 중 — cut 시점에 archive 로 눌리는 축이라 이번 라운드가 만든 문제는 아니다.
- `40-architecture/domains/runtime_task.md` frontmatter `status: draft` 유지(신설 이후 그대로). 이번 개정이 건드릴 축이 아니다.

---

## 확인한 것 (PASS 근거)

### A. 결정 SoT 대조 (`_RESUME.md` §2 — 행별)

| # | 결정 | 판정 | 근거 |
|---|---|---|---|
| 1 | 요청 판정 = 파생(비워크플로 ∧ `created_by ≠ assignee`), 새 컬럼·type·상태·TaskEvent 0 | ✅ | `spec-154:760-762`(판정 표) · `runtime_task.md:218-236`(파생식 코드블록 + 「저장 없다」) · `work-129:44,88` · `erd.md:161`. 새 상태·이벤트 grep 0 |
| 2 | **요청자 자동 cc 없음** | ✅ | `spec-154:763`(요청자 식별 = `created_by` 파생) · `spec-154:1090`(§4.11 ⛔ 행 신설) · `runtime_task.md:286`(invariant) · `work-129:91,156,163`(P2 작업 ⛔ + 검증 「`task_ccs` 자동 추가 0건」). **`자동 cc` grep 결과 전부 부정문** — 08-31 구 결정 잔존 0 |
| 3 | 게이트 0(수락·검수 어떤 형태로도) · 거부 = 재배정 요청 | ✅ | `spec-154:759`(⛔ 게이트 없음) · `:760`(거부 = 재배정) · `runtime_task.md:233-234` · `spec-154:2264`(§4.19.6-R R-2 「요청 대기 열 = 수락 게이트 부활」) · `work-129:23,57` · `work-130:23`. `accept_pending` 신규 도입 grep 0 |
| 4 | DM graceful(SPEC-119 재사용·실패해도 생성) — fail-loud 혼동 없나 | ✅ | `spec-154:765-766` 이 **incident fail-loud 와 다른 축임을 명시적으로 분리**(「슬랙이 유일한 완료 통로 vs 웹 목록이 이미 전달 통로」) · `work-129:47,92,153,161,229`. **SPEC-119 신설 0** — 새 알림 인프라 grep 0 |
| 5 | 본문 5부: `background`/`goal` 컬럼 · 원장 렌더 폐지(스냅샷) · `description` fallback · 댓글·로그 = `task_events` 현행(신설 0) | ✅ | `spec-154:833-856`(구 계약 3행 취소선 처분표 + 「왜 라이브 렌더를 버리는가」) · `:824`(댓글 현행·신설 0) · `spec-152:306`(incident 착지) · `runtime_task.md:44-46`(스키마) · `erd.md:117-121`. `task_comments` 신설 grep 0 |
| 6 | `task_references`: role×kind · soft delete · 출처 행 저장 금지 · 25MB · denylist · 권한 3원칙 · 저장 경로 | ✅ | `spec-154:156-173`(축 표 + 항목 표 전량) · `runtime_task.md:238-260` · `erd.md:33,51,131-137,163`. 출처 = execution 사슬 자동 렌더 3자리 일치(`spec-154:164`·`runtime_task.md:258`·`work-130:96`) |
| 7 | 완료 모달: 3조합 중 1 필수 · 서버 422 · 시스템 액터 예외 | ✅ | `spec-154:882-892` + `:2265`(2단계 CTA) · `runtime_task.md:262-273` · `work-130:177-189`(P3) · `spec-154:892`(근거 검사가 강제완료보다 **앞**) |
| 8 | 채팅: 배경·목표·체크리스트 필수 · 첨부 스테이징 · 발화 패턴 | ✅ | `spec-155:275`(산출 필드 필수화) · `:276`(「반드시 채운다」 + fallback 우선) · `:277`(담당자 = 발화 명시 시에만) · `:269-286`(§6.12 스테이징) · `spec-230:404-414`(§U-8b) · `work-130:241-248` |
| 9 | 샤라웃: 지시 **입구만** 주석 비활성 · 내부 보존 · 재활성 가능 · 결정요청/공유/결재 유지 | ⚠→V-1 | 슬랙 = `spec-111:18,36-49`(차단 범위 = 폼·봇태그·AI gate·REST·MCP·웹 모달) · 채팅 = `spec-156:344,355-356`(산출 2종화 + 승인 실행 거절 이중 그물) · `spec-154:45-49`(§4.4 「계약은 폐기되지 않는다」) · `spec-115:71,81-82,89-100,109`(3자리). **재활성 가능 명시 5자리** · 결정요청/공유/결재 유지 명시 4자리. **단 「배정 부트스트랩」 자리만 SPEC 착지 없음 → V-1** |
| 10 | migration 총계 = 컬럼 2 + 인덱스 1 + 테이블 1, 분배 129=인덱스 / 130=컬럼+테이블, k8s 사전조건 130 | ✅ | `runtime_task.md:300-310`(총계 + 분배 + 「서로를 기다리지 않는다」) · `erd.md:290-291` · `work-129:85,135-140`(객체 «정확히 1개» 검증) · `work-130:89,140-151`(«정확히 3개» 검증) · `work-130:288`(k8s hostPath 선행 배포 PR) · `spec-154:169`(배포 사전조건) |

### B. 교차 정합

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 11 | 구 서술 grep 스윕(7패턴 × 전 `20-spec/`·`40-architecture/`) | ✅ **활성 잔존 0** | `원장.*렌더` 활성 히트는 전부 **다른 축**(`spec-100:132` decision_intake 대시보드 · `spec-220:86,296`·`spec-154:1394,1403,2877` 레거시 «의사결정 목록» 소스 전환 · `spec-155:423`·`spec-156:355` draft 항목 목록 렌더). 태스크 본문 라이브 렌더는 `spec-154:844,1611,1722,1775,2248,2381`·`spec-152:306` 전부 취소선/폐기. `마크다운 합침`·`합침 규칙` 전부 취소선(`spec-155:27,106,335,374,390`·`spec-154:843,854`·`runtime_task.md:23,44`). `본인 고정` 활성 잔존은 `spec-156:575`(**보고자** 축 — 샤라웃 결정요청/공유 유지분, 이번 결정 대상 아님) 하나. `자동 cc` 는 전부 부정문. **와이어프레임 2줄은 W-1** |
| 12 | WP 분할 경계(«background/goal·task_references 필요 여부») · phase 중복·누락 0 · 스펙 «구현 WP =» 재배선 | ✅ | 분할 계약(`task-improve-wp-split-brief.md` §2) 항목 10개가 129(7 phase)/130(9 phase)에 1:1 배치. **경계 명시가 양방향**: `work-129:191,195`(본문 존 diff 0 — 130 P5 몫) ↔ `work-130:228,233`(메타 존 diff 0 — 129 P4 몫), 양쪽 OI-1 동일 문구. `work-130:105`(P1~P6 은 129 를 안 기다림 → `depends_on` 미포함) 의존 방향 정확. 재배선 = `spec-154:44`(WP-129 = ①⑤ / WP-130 = ②③④+채팅+시안) · `spec-154:309-310`(§4.19.6-R) · `spec-111`·`115`·`156` → 129 · `spec-125`·`152`·`230` → 130 |
| 13 | planner 판단 «뒤집기 가능» 표기 실재 + 기존 계약과 무모순 | ✅ **10건** | `spec-154:768`(배정 후보 범위) ↔ `work-129:261` OI-3 **동일 주장** · `spec-154:2250`(참고자료 첫 줄 vs 메타 중복) · `spec-155:932`(수정 카드 첨부 경로) ↔ `work-130:311` OI-4 **동일** · `spec-155:948`(Decision 배정과의 관계) · `spec-111:180`(차단 발화 안내 여부) · `runtime_task.md:273`(파생 전이) ↔ `work-130:309` OI-2 **동일** · `work-130:310` OI-3(denylist) ↔ `spec-154:168`(「구체 목록은 구현 WP」) **정합**. 짝지어진 4쌍 전부 같은 결론 — 모순 0 |
| 14 | `30-work.md` 3표 · `20-spec.md` · `log.md` 동기 | ✅ | Status Board 2행 신설(129 = 7 phase·BLOCKED 없음 / 130 = 9 phase·P7 BLOCKED) · WP List 2행 · Spec Coverage 8행 갱신. **파이썬 대조 결과 Spec Coverage 의 129/130 매핑이 두 WP `covers` 와 완전 일치**(111·115·154·155·156 → 129 / 125·152·154·155·230 → 130). lint 의 WP status 3자 일치·map 동기·`doc_no` 유일성 전부 통과. `20-spec.md` 는 SPEC 추가·이름변경·삭제가 없어 무변경이 정상(lint SPEC List 동기 통과). `log.md` = `2026-09-01 / spec-change, wp-add, domain-change` 1행, 영향 ID 11개(SPEC 8 + WP 2 … SPEC-060 포함) |
| 15 | 시안 대기 슬롯(§4.19.6-R) · WP-130 P7 — 화면 구조 선확정 없나 | ✅ | `spec-154:291-310` — **「⛔ 이 절은 비어 있는 것이 계약이다」** 명시 + 경계 7항목(R-1~R-7)만. 레이아웃(탭/세그먼트/보드)·요청 표기 형태를 **정하지 않았다**. `work-130:256-267` P7 `Status: BLOCKED` + 「⛔ 시안 도착 전 착수 금지」. `work-129:56,186,190`·`work-130:49` 가 목록 화면 비접촉을 반복 선언. `spec-154:391`(OQ-19)·`work-129:263` OI-5·`work-130:313` OI-6 이 표기 형태를 시안 소관으로 남김. **데이터 계약은 시안을 안 기다린다**는 분리도 명시(`spec-154:310`) |

### C. 층위 SoT 불변 확인

- **`runtime_task.md` 상태·이벤트 축 = 무변경.** diff 는 **전부 additive** — `background`/`goal`/`created_by_member_id` 스키마 행 · 인덱스 1 · §업무 요청 판정 · §`task_references` · §완료 근거 · invariant 2행 · §마이그레이션 · Related 재배선. **Enum 5값·전이표·스탬프·TaskEvent 어휘 블록은 diff 에 등장하지 않는다.** 문서 자신이 `:23` 에서 「상태 축은 한 글자도 바뀌지 않았다」를 선언하고 실제 diff 가 그것을 지킨다
- **SPEC-119 DM 인프라 신설 0** — `spec-119` 는 diff 에 없다. `spec-154:764`·`work-129:37,47,152,262` 전부 「재사용·신설 0」. `slack_id` 백필 도구도 명시적 제외(`work-129:60`)
- **SPEC-127 path-guard 재사용** — `spec-154:169`·`work-130:75,201`(⛔ 새 구현 금지). `spec-127` 무변경

---

## 재검수 시 확인할 것 (V-1 수정 후)

1. `work-129:50,123,208` 의 부트스트랩 항목이 한정되거나(ⓐ) SPEC 착지가 생겼는가(ⓑ)
2. ⓑ 를 골랐다면 `spec-154:298,530,890,1054,1060,1187,1642,1724,1748,1965,1966,2248` 12자리가 함께 정정됐는가 — **한 자리라도 남으면 같은 위반의 재발**
3. `work-130:187` P3 검증이 선택한 해석과 어긋나지 않는가
4. `python3 scripts/lint-pipeline.py --strict` mediness ERROR 0 유지 + `30-work.md`·`log.md` 재동기

---

## R2 재검수 (2026-09-01) — V-1 targeted

## 판정: PASS — V-1 해소. 새 모순 0. lint mediness ERROR 0 유지.

검수 범위 = **V-1 관련 5파일만**(`spec-154` · `spec-115` · `work-129` · `work-130` · `30-work.md`). 나머지 10파일은 R1 판정 유지(재검수 대상 아님). 파일 집합 불변(13 M + 2 untracked) — 리포리 read-only 유지.

### 1. V-1 해소 — 네 문서가 같은 말을 한다 ✅

planner 가 ⓐ안(지시 발 한 자리 한정)을 골랐고, **네 문서의 진술이 문자열 수준에서 일치**한다.

| 자리 | 진술 |
|---|---|
| `spec-154:978` (§4.8 생성 시점 행 — **계약 정본**) | 「라운드를 여는 승인 **2곳** — 결정 승인(요청) · [후속 실행] 승인(루프) *(2026-09-01 개정 — 구 «3곳»)*. ~~지시 접수 자동 승인(지시)~~ = **비활성**」 |
| `spec-154:987` (각주) | 「비활성은 «지시 발» 한 자리뿐 … **결정 승인 발 · [후속 실행] 발 부트스트랩은 그대로 돈다** … 메커니즘 자체는 하나도 폐기되지 않았다」 |
| `spec-154:41` (개정 노트 ㉗⑤ 착지 목록) | 착지에 「본 SPEC §4.8 생성 시점(지시 접수 자동 승인 발 한 자리 — 결정 승인 발·[후속 실행] 발은 **불변**)」 **추가** |
| `spec-115:29` (개정 노트) | 「이 3자리는 «이 SPEC 이 소유한 자리» **전량** — 부트스트랩은 SPEC-154 소관이며 **이 페이지에는 부트스트랩 자리가 없다**」 |
| `work-129:50`(Scope) · `:123`(P0) · `:210`(P5 작업) | 「**지시 접수 자동 승인 발** 배정 부트스트랩」 한정 + `:210` ⛔ 「결정 승인 발·[후속 실행] 발은 손대지 않는다」 + `:123` ⛔ 「목록에 넣지 않는다 — 유지 대상」 |
| `work-130:187` (P3 검증) | 「여기서 말하는 부트스트랩은 **결정 승인 발·[후속 실행] 발** … WP-129 P5 가 비활성하는 것은 한 자리뿐이라 **이 검증은 그대로 유효**」 |
| `30-work.md:115` (Board 미러) | 「**지시 접수 자동 승인 발** 배정 부트스트랩[결정 승인 발·[후속 실행] 발 부트스트랩은 **유지**]」 |

**WP↔WP 모순 소멸.** R1 이 잡은 `work-129:208` ↔ `work-130:187` 의 정면 충돌이 양쪽 상호참조로 닫혔고, 나아가 `work-129:220` 에 **양성 회귀 검증이 신설**됐다 — 「결정 승인 발·[후속 실행] 발 부트스트랩이 정상 생성되고 첫 실 태스크에서 멱등 자동 완료됨 — 이 경로가 죽으면 유지 축을 깬 것이다」. 끄는 쪽만이 아니라 **남기는 쪽도 테스트로 고정**됐다. `work-129:211` 이 「제목·초기 상태·담당·판정 제외·멱등 자동 완료·재배정 규칙을 건드리지 않는다 — 끄는 것은 발화 지점 하나이지 메커니즘이 아니다」로 경계를 한 번 더 못 박는다.

### 2. 정본 각주 방식이 12자리 흡수에 충분한가 ✅

R1 이 ⓑ안 조건으로 열거한 12자리를 **전수 재확인**했다. `spec-154:988` 의 정본 선언(「이 절이 부트스트랩 계약의 정본이다 — 다른 자리가 부트스트랩을 언급할 때 「지시 발」 자리는 이 각주를 따른다」) + 「그 자리들의 계약 본문은 폐기하지 않는다(§4.4 「입구 차단 — 계약은 보존」과 같은 축)」로 **전부 흡수된다.**

| 자리 | 소속 절 | 처분 근거 |
|---|---|---|
| `:298` | §3 표면 | 「라운드가 열리면 부트스트랩이 뜬다」 — **흐름 무관 서술**. 유지 2자리에 그대로 참 |
| `:530` | **§4.5.3 결정 — decide** | **결정 승인 = 유지 축**이라 애초에 대상 아님 |
| `:890` | §4.8 완료 근거 시스템 액터 예외 | 유지 2자리가 계속 멱등 자동 완료를 낸다 → 예시 유효 |
| `:1056`·`:1062` | §4.10 알림 ⑤·폐지 ④ | 각주 열거 대상. 계약 보존 |
| `:1189` | §4.13 레거시 원장 이관 | **이미 선 legacy run 의 1회성 소급**이고, 각주가 「기존 `instruction` run 에 이미 선 부트스트랩 태스크도 그대로 산다」로 정면 커버 |
| `:1496`·`:1517` | §4.18 사람 참조 C-5 | 담당 **파생 규칙**(보고자/수신자)이지 생성 발화가 아님. 보존 대상 |
| `:1644` | §4.19.2 배지 문맥 슬롯 `[배정]` | 각주 열거. 유지 2자리에 유효 |
| `:1726`·`:1750`·`:2250` | §4.19.1·§4.19.4 안내 블록·조건부 | 「안내 블록은 남는다」 — 유지 2자리 기준 참 |
| `:1967`·`:1968` | §4.19.6 마커·툴팁 | 각주 열거. 유지 2자리에 유효 |
| `:980`·`:982` | §4.8 같은 표(`round_no`·담당의 「지시」 값) | **표 첫 행 `:978` 이 이미 비활성을 선언**한 뒤라 top-down 으로 읽힌다. 재활성용 보존값 |

**결론: 12자리 중 «지시 발 생성을 활성으로 주장하는» 자리는 0이다.** ⓑ안의 12자리 일괄 정정이 필요 없었던 이유가 확인된다 — 그 자리들은 대부분 **흐름 무관 서술이거나 유지 축(결정 승인)에 속하거나 보존 대상 계약**이었고, 진짜로 「3곳」을 주장하던 자리는 §4.8 표 한 곳뿐이었다.

### 3. 정정이 새 모순을 만들지 않았는가 ✅

- **`spec-154:992` 의 stale 문장을 planner 가 스스로 잡았다.** 2026-08-13 개정 ㉔ 의 「생성 시점 **3곳**·… 전부 불변」이 남아 있었으면 새 표(2곳)와 정면 충돌인데, 그 줄에 시점 한정 괄호가 붙었다 — 「이 문장은 2026-08-13 시점 기준이다 … 2026-09-01 개정 ㉗ 이 생성 시점 중 「지시 발」 한 자리만 비활성했다. **위 표와 각주가 현행이다**」. R2 가 잡으려던 유일한 잔여 자리가 선제 해소돼 있다.
- **`work-129:218` ↔ `:210` 정합.** 「기존 `instruction` run 이 계속 돈다」와 「[후속 실행] 발 부트스트랩 유지」가 서로를 성립시킨다 — 기존 지시 run 이 후속 실행 루프를 돌 때 배정 동선이 계속 열린다. 끊었으면 `:218` 이 거짓이 됐을 자리다.
- **`spec-115` 정정이 범위를 넓히지 않았다.** 3자리 열거는 그대로 두고 **소유 경계 한 문장만** 추가했다(부트스트랩 = SPEC-154 소관). SPEC-115 의 계약 표면 변화 0.
- **결정 SoT 재대조** — `_RESUME.md` §2 「+ Q3의 개인 대시보드 3자리·배정 부트스트랩 비활성 포함」이 **샤라웃 지시 배제 묶음 안의 항목**이므로 ⓐ안(지시 발 한정)이 결정 문맥과 정합한다. 결정 위반 없음.
- 5파일 diff 안에서 **새 상태·새 TaskEvent·새 endpoint·게이트 재도입·migration 증가 0** (재grep 확인). 비어야 할 자리(§4.19.6-R · WP-130 P7)도 무변경.

### 4. lint ✅

`python3 scripts/lint-pipeline.py --strict` → **exit 0 / 0 error / 255 warning**. mediness 범위 WARN 은 여전히 **1건뿐이고 선재분**(`30-work.md:242` SPEC-030 coverage derive — WP-045/089/090/113 축, 이번 라운드 밖). WP status 3자 일치·map 동기·`doc_no` 유일성 전부 통과.

구 서술 재스윕에서 남은 활성 히트 2건은 **둘 다 무관**: `spec-156:575`(**보고자** 본인 고정 — 샤라웃 결정요청/공유 유지분, 이번 결정 대상 아님) · `spec-154:2905`(OQ-21 이 legacy `description` 데이터를 지칭 — 폐기된 계약이 아니라 남은 데이터 이야기).

### R1 WARN 의 현재 상태

W-1(와이어프레임 구 문자열 2줄) · W-2(spec-060 미배속) · W-3(WP-130 OI-2) · W-4·W-5 는 **이번 정정 범위 밖이라 그대로**다. 전부 R1 에서 이미 FAIL 이 아니라고 판정한 것들이며 판정을 바꾸지 않는다.
