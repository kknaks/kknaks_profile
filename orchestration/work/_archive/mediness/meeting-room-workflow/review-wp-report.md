# 리뷰 리포트 — WP-125 (예약→회의 체인 착지 작업서) + 경미 2건 정리분 / R3 (2026-08-31)

> 선행: [review-spec-report.md](review-spec-report.md)(R1 FAIL) · [review-spec-report-r2.md](review-spec-report-r2.md)(R2 PASS)

## 판정: FAIL

**위반 1건.** WP 의 골격·계약 준수·코드 좌표·3자 동기·경미 2건 정리는 **전부 통과**했고 lint 도 깨끗하다. 걸리는 것은 **P3(수정·취소 파급)이 「모달 발로 만들어진 회의」까지 삼킨다**는 한 자리다 — 계약이 **열어 둔 축**(OPEN-031-Y)을 코드로 닫는 데다, 그것이 안 일어난다는 WP 자신의 근거가 **코드 사실과 반대**다. ⚠ **이 갈림의 뿌리는 SPEC-151 §7.9.5 에도 있고, 내 R2 가 그것을 놓쳤다** — 아래 V-3 에 그대로 적는다.

## 검수 범위

- 대상: `products/mediness/30-work/work-125-reservation-meeting-autoregister.md`(**untracked 신규**, 274줄) + 미커밋 diff 4파일(`30-work.md` +3/−2 · `log.md` +2 · `spec-031` · `spec-151`)
- 실행한 검사
  - `python3 scripts/lint-pipeline.py --strict` → **0 error / 255 warning**, exit 0. mediness 범위 **ERROR 0**, WARN 1건은 SPEC-030 Coverage derive **기존분(무관)**
  - **실코드 대조** — `mediness-app` 워크트리 `meeting-room-workflow` 브랜치에서 Code Surface 표의 **파일 11개 · 심볼 15개 전수 확인**
  - 조사 리포트(`research-meeting-create-flow.md`) 절 번호 대조
  - SPEC-151 §7.9.1~§7.9.7 · §5.3.2 / SPEC-031 §3 역방향 가산 · §6 OPEN-031-Y 와 WP 본문 대조
- 리포 파일 수정·생성 **0**(`git status` = planner 산출물 그대로)

---

## 위반 (FAIL 사유)

### V-3. P3 파급이 «모달 발로 만들어진 회의» 를 막지 않는다 — 계약이 열어 둔 축을 코드로 닫는다

- **자리**: `work-125-...md:166` (P3 작업 1번째) · 같은 파일 `:45`(Scope 「수정·취소 파급」) · `:208`(P5 동기화 분기 6종)
- **무엇이 어긋났나**

  P3 의 진입 가드는 **「연결 회의가 있는가」 하나**다:

  > `:166` — **역방향 조회 1자리** … 연결 회의가 없으면 **아무 일도 하지 않는다**(**모달 발**·구 예약 건이 여기 온다)

  ⚠ **괄호 안이 코드 사실과 반대다.** 모달 발 회의는 **연결을 갖는다** — 모달 생성 경로가 `link_reservation()` 으로 `meeting_v2.reservation_run_id` 를 채운다(`back/app/repositories/meeting_v2_repo.py:115` · 조사 리포트 §3-1 1-a 6-a · 그 연결 축을 만든 것이 [WP-099](../../../../mediness-mediness/meeting-room-workflow-spec/products/mediness/30-work/work-099-meeting-v2-create-modal.md) M9 이고, **이 WP 자신이 Code Surface `:71` 에서 그 함수를 「재사용」으로 적어 두었다**). SPEC-031 §3 「연결 축」도 원래 **모달 발 방향**을 정의한 절이다. ⇒ **가드가 걸리지 않는다.**

  결과: **모달 발 예약을 취소하면 그 모달 발 대기 회의가 soft delete 된다.** 그런데 그 축은 계약이 **명시적으로 열어 둔 자리**다 —

  > `spec-031-...md:2340` OPEN-031-Y ▸ **여전히 열려 있는 것** — ⓐ 회의 화면에서 직접 취소 · ⓒ THE CONNECT 발 삭제의 반영 · ⓑ 의 반대 방향 · **「모달 발로 만들어진 회의」의 취소 축**

  WP 의 비목표(`:23`)도 「반대 방향(OPEN-031-Y ⓐ·ⓒ·ⓑ 역방향)」만 제외할 뿐 **모달 발 회의 취소 축은 빠져 있다.** P5 의 동기화 분기 6종(`:208`)에도 **모달 발 origin 케이스가 없어** 테스트가 이 경로를 잡지 못한다.

  🔑 **발동(P1)은 이중으로 막혀 있는데 파급(P3)은 무방비다** — P1 은 `source` 게이트 + 멱등을 함께 걸어 모달 발을 확실히 배제하지만, P3 에는 **어느 쪽도 없다.** 같은 WP 안에서 두 자리의 가드 수준이 갈린다.

- **근거(규칙 출처)**: `rules/document-pipeline.md` §Work Tracking — WP 는 **빌드 계획**이고 SPEC 계약을 확장하지 않는다 / §의사결정 레벨 구분 — 「사용자가 보게 되는 동작이 바뀌면 SPEC」. 열려 있는 OQ 를 WP 가 코드로 결정하면 그 결정이 **계약 밖에서** 서게 된다. 피해가 **파괴적**(대기 회의가 목록·상세에서 사라진다)이라 「구현하며 정하면 되는 내부 선택」의 범주도 아니다.
- **권장 수정 (작다)**
  - ① P3 진입 가드를 **「자동 등록된 회의만」** 으로 좁힌다 — 연결 존재 + **run 의 `source` 가 모달 발이 아닐 것**. P1 과 **같은 판정을 재사용**한다(정의는 한 자리 — WP 자신의 Open Issues `:258` 규율 그대로).
  - ② `:166` 괄호를 사실대로 고친다 — 「구 예약 건(연결 없음)」과 「모달 발(연결 **있음** → source 로 배제)」은 **다른 이유로** 걸러진다.
  - ③ 비목표(`:23`)에 **「모달 발로 만들어진 회의의 취소·변경 파급(OPEN-031-Y)」** 을 명시하고, P5 분기에 **모달 발 origin × (취소 / 변경) 음성 단언**을 더한다.
  - ④ 만약 **모달 발도 파급시키는 것이 의도**라면 그건 WP 가 정할 일이 아니다 — **SPEC-151 §7.9.5 + OPEN-031-Y 를 먼저 고치는 환류**가 선행해야 한다(아래 V-3b).

### V-3b. (뿌리) SPEC-151 §7.9.5 의 동기화 범위가 `source` 로 한정돼 있지 않다 — R2 에서 내가 놓친 자리

- **자리**: `spec-151-...md:768` — "**연결된 회의는 예약을 따라간다.** 방아쇠는 **사후 변경 게이트의 승인·실행 성공**"
- §7.9.1 의 `source` 게이트는 **체인 발동**에만 걸리고("취소·변경 게이트의 실행은 이 체인을 발동시키지 않는다 — 그쪽은 §7.9.5 동기화가 받는다"), **§7.9.5 본문에는 source 한정이 한 줄도 없다.** 그런데 SPEC-031 OPEN-031-Y 는 「모달 발로 만들어진 회의의 취소 축」을 **열린 것**으로 남긴다 ⇒ **두 문장이 모달 발 케이스에서 갈린다.**
- SPEC-031 §3 의 처분 정의는 **「역방향 — 예약 발 회의 자동 등록」 bullet 아래**에 있으므로 **자동 등록 회의 한정**으로 읽는 것이 자연스럽고, 그것이 OPEN-031-Y 와도 맞는다. 다만 **§7.9.5 가 그 한정을 말하지 않아** WP-125 가 넓게 읽을 여지를 줬다.
- **권장 수정**: §7.9.5 첫 문장을 「**자동 등록된**(이 체인이 만든) 회의는 예약을 따라간다」로 한정하고, 「모달 발로 만들어진 회의는 이 절의 대상이 아니다(§6 OPEN-031-Y)」 한 줄을 단다. **계약 내용이 바뀌는 것이 아니라 이미 의도된 경계를 명시하는 것**이다.
- ⚠ **내 R2 판정의 한계를 명시한다** — R2 는 §7.9.5 를 「회의 상태 분기가 SPEC-031 과 맞는가」 축으로만 봤고 **origin 축을 보지 않았다.** WP 가 그 구멍을 밟아서 드러났다.

---

## 경미 (WARN — 비차단)

- **RW-4. 조사 리포트 절 번호가 틀렸다.** `work-125-...md:71` 이 「현행 모달 경로는 커밋이 3회로 갈려 고아 예약 창이 있다(조사 리포트 **§C-11**)」이라 적었는데, 그 리포트에 **`§C-11` 이라는 절이 없다**(절 체계는 §3-1 ~ §3-6). 해당 사실의 실제 자리는 **§3-2 ▸ 2-b 「예약은 회의 생성과 한 트랜잭션인가」**("커밋 3회다(`:383`, `:392`, `:253`) … ④ 직전에 프로세스가 죽으면 THE CONNECT 예약은 잡혔는데 회의는 없는 고아 예약이 남는다. 보상 경로는 코드에 없다"). **주장 자체는 정확**하고 앵커만 틀렸다 → 절 번호만 고치면 된다.
- **RW-5. `log.md` 두 신규 행의 `PR` 칸이 `—`.** R1 W-3·R2 RW-3 과 동일 — PR 은 코디네이터가 올리므로 **생성 후 기입** 항목. 차단 아님.
- **RW-6. (참고 — 결함 아님) P0-① 은 코드에서 이미 답이 보인다.** `MeetingDomain.resolve_attendees_for` 가 `payload.participants = resolved.participants` · `payload.participant_names = resolved.display` 로 **결과 축을 이메일 배열, 표시 축을 별도 필드**로 채운다(`back/app/services/action_runtime/workflow/meeting/workflow.py:585-604`). WP 의 계약 방향(「결과 축을 역해소한다」)과 **정확히 일치**하므로 P0-① 은 착수 시 확인 한 번으로 닫힌다 — 실사 항목을 남겨 둔 것 자체는 문제가 아니다.

---

## 항목별 판정 (브리프 §2)

### 1. 계약 확장 0 인가 — ⚠ **1건 초과**(V-3), 나머지는 ✅

- **비목표(`:23`)가 SPEC 무개정 목록과 일치** — 게이트 원칙(§7.1) · 승인 시점 방 배정(§7.2) · 카드 명령 파생(§4.2) · **§5.3 35칸** · API 경로·요청 shape·권한(§6) · 모달 발 경로 · ⛔ 스케줄 화면 → **SPEC-151 §7.9.7 의 목록과 동형** ✅. 여기에 OQ-9(상시 탐지) · OPEN-031-Y 반대 방향 · 새 endpoint/leaf/카드 명령까지 스스로 더해 닫았다 ✅
- **제외 목록(`:50-57`)** 도 계약과 정합 — 모달 발 코드 diff 0 · 승인/거절/철회 무변경 · 회의 도메인 계약 무변경 · `state` enum·`DELETE` 표면·카드 상태값·run 종료값 신설 0 · `payload` 수정 0 · **30분 슬롯 검증 이식 0** ✅
- **파생 매핑(P1 `:127`)** 이 §7.9.2 표와 1:1 ✅ · **조합표 A~D 처분(P2)** 이 §7.9.4 와 1:1(특히 **D 의 실행 축 «성공» 유지**와 그 이유를 그대로 옮김) ✅ · **실행 축 기록 시점 이동**(`:44`·`:145`)이 R2 에서 명문화된 그 계약 ✅
- **새로 만든 것으로 보이는 유일한 항목 = D 행 관측 로그**(`:150`)는 **계약 확장이 아니다** — 사용자 표면 0 이고, OQ-9 의 재개 조건("D 행이 실제로 관측")이 요구하는 **입력**이다. WP 몫이 맞다 ✅
- ⚠ 초과분 = **V-3**(모달 발 회의까지 파급)

### 2. WP 몫으로 넘겨진 항목이 전부 실렸나 — ✅ **전건 실림**

| 넘긴 항목 | WP 자리 | 판정 |
|---|---|---|
| participants 실제 필드 형태 실측 (= R1 W-4 · SPEC §7.9.3 ⏳) | **P0-①** `:106` | ✅ 계약 방향 불변 단서까지 그대로 옮김 |
| §7.2 사람 원천·이메일 컬럼 대조 | **P0-②** `:107` | ✅ **실측으로도 실재하는 어긋남**(아래 3번) + 「코드에서 정하지 말고 SPEC 을 먼저 고친다」 환류 규율 명시 |
| 멱등 보증 수단 (= R1 W-1 · SPEC §7.9.1) | **P0-③** `:108` + §Domain/Schema `:83` | ✅ 두 후보·판단 기준(동시 재진입)·migration 범위 영향까지 |
| R1 주의점 ① = 「예약 건당 회의 최대 1」이 신설 불변식 | §Domain/Schema `:83` | ✅ "현행 코드에는 유니크 제약이 없다" 를 근거로 명시 |
| 실행 축 기록 시점 (= R1 W-2) | Scope `:44` · P2 `:145` · P5 `:206` | ✅ 음성 단언(«성공 → 실패» 전이 없음)까지 |

**P0 를 별도 Phase 로 둔 판단이 옳다** — 세 항목 모두 **설계 입력**(어느 필드·어느 술어·유니크 여부)이고 WP 가 그 이유를 적어 뒀다(`:99`·`:104`).

### 3. 코드 좌표가 맞나 — ✅ **전수 실재 · 줄번호 단정 0**

`mediness-app` 워크트리(`meeting-room-workflow` 브랜치) 실측:

| WP 가 적은 좌표 | 실재 | 확인 |
|---|---|---|
| `action_runtime/workflow/meeting/definitions.py` `_execute_reserve` / `_execute_cancel` / `_execute_update` / `_run_write_step` | ✅ | `:261` / `:354` / `:371` / `:225` |
| 같은 파일 — `complete_execution` 이 **외부 예약 성공 직후** 호출됨 | ✅ | `:281-283`(`refs["external_id"]` 확보 직후) — **「체인 뒤로 옮긴다」가 정확히 그 자리** |
| **선점·대안 0 경로는 WP-119 가 이미 `fail_execution` 으로 고쳤다** | ✅ | `:288-320` 주석·`fail_execution(retryable=False)` — WP 의 「그 수리를 되돌리지 않는다」가 사실에 근거 |
| `tools/steps.py` `run_idempotent_steps` · `ExternalRefStep` | ✅ | `:39` / `:24` |
| `workflow/meeting/workflow.py` `MeetingDomain` · `org_members` · `resolve_attendees_for` | ✅ | `:468` / `:544` / `:585` |
| `workflow/orgref/people.py` `resolve_participants` | ✅ | `:56` |
| `workflow/meeting/composition.py` | ✅ | 파일 실재 |
| `meeting_v2_service.py` `create_meeting` · `_assert_invitable` · `_reserve_room_now` | ✅ | `:206` / `:259` / `:312` |
| `meeting_v2_repo.py` `create` · `link_reservation` | ✅ | `:79` / `:115` |
| `models/meeting_v2.py` `reservation_run_id` · `deleted_at` · `meeting_v2_attendee` | ✅ | 조사 리포트 §3-a 와 일치 — **`deleted_at` 실재**(「닫는다」가 새 컬럼을 안 부르는 근거) |
| `meeting_v2_participants.py` `invitable_member_ids` | ✅ | `:61` |
| `clients/the_connect.py` `cancel(external_id)` | ✅ | `:136`(proto) `:365`(impl) |
| Open Issues — 「모달 발 상수가 지금 **회의 서비스 파일**에 있다」 | ✅ | `MEETING_MODAL_SOURCE = "meeting_modal"` = `meeting_v2_service.py:79` — **방향 뒤집힘 지적이 정확** |
| **P0-② 의 어긋남 주장** | ✅ | `org_members()` 가 **`select(User.name, User.email)`** 로 **계정 축 이메일**을 읽는다(`workflow.py:549-551`). SPEC-151 §7.2 문면은 「`organization_member`, 메일 = `work_email`」 ⇒ **실사 항목이 진짜 어긋남을 겨눈다** |

- **줄번호 단정이 없다** — 표는 **심볼 기준**이고 `:77` 에 "정확한 행 번호는 착수 시점 HEAD 기준으로 재실사한다" 를 달았다 ✅
- 유일한 좌표 오류는 **문서 절 번호**(RW-4, §C-11) 하나 — 코드 좌표가 아니다

### 4. 3자 동기 — ✅

- **Status Board**(`30-work.md:113`) · **WP List**(`:198`) · **Spec Coverage** — SPEC-151 행(`:224`)·**SPEC-031 행(`:244`) 양쪽에** WP-125 가산 ✅ (covers 2건이므로 두 행 모두가 맞다)
- 3자 status 일치 — frontmatter `proposed` · Board `proposed` · WP List `proposed` ✅ (lint 의 3자 일치 ERROR 0 이 기계적으로도 보증)
- Owner 3자 일치 `TBD` ✅ · Coverage 구현 상태 `in_dev` 유지 = derive 규칙대로(covering 에 done/in_dev 혼재) ✅
- **`doc_no: MEDINESS-DOC-245` 유일** ✅ — 현행 전체 스캔에서 중복 0, 직전 max 가 244 라 **stateless scan +1 규약대로**
- **`MEDINESS-WP-125` 유일** ✅ — 파일 1개, 참조는 `30-work.md`·`log.md`·본문뿐
- `log.md` 에 **`wp-add` 행 신설** ✅ (영향 ID = WP-125 + SPEC 2건)
- 파일명 NNN ↔ 본문 WP ID 일치 ✅ · frontmatter 필수 필드(`id`·`type`·`title`·`status`·`owner`·`last_updated`·`covers`·`depends_on`) 전부 ✅

### 5. 경미 2건 정리 — ✅ **정확히 반영 · 새 모순 0**

- **RW-1(라벨 수치)** — `spec-031-...md:30` 변경 노트 ⑭ 제목이 「가산 4줄」 → **「§3 가산 6줄 + 정합 3자리」**, `log.md:20` spec-change 행도 **「§3 가산 6줄·정합 3자리」** 로 동일 정정 ✅. §3 본문의 하위 bullet 을 세어 **실제 6개**임을 확인했고, 본문 스스로도 "아래 여섯" 이라 센다 — **수치가 이제 맞는다** ✅
- **RW-2(파급 bullet 대칭)** — `spec-031-...md:1394` 변경 파급 bullet 에 **"(진행 중·종료 회의는 제외 — 지난 일을 고쳐 적는 것이 되므로 예약만 바뀐다. 상태 분기의 정본 = SPEC-151 §7.9.5, 형제 「취소 파급」 bullet 과 같은 축)"** 가산 ✅. 내가 지적한 **형제 비대칭이 해소**됐고, 정본 포인터까지 붙어 소유 경계도 유지된다 ✅
- 두 정리분이 **새 모순을 만들지 않았다** — 상태 분기 정본은 여전히 §7.9.5 한 곳, SPEC-031 은 그것을 참조만 한다 ✅

### 6. lint — ✅

`--strict` **0 error / 255 warning**(exit 0). **mediness 범위 ERROR 0**, WARN 1건은 `30-work.md:233` SPEC-030 Coverage derive — **이번 diff 밖 기존분**. 신규 WP 가 doc_no·3자 일치·파일명·ID 참조 검사를 **전부 통과**했다.

### 7. 테스트 계획 충분성 — ⚠ **1칸 빔**(V-3), 나머지 ✅

| 커버 대상 | WP 자리 | 판정 |
|---|---|---|
| 조합표 A~D 4행 | P5 `:205` + P2 검증 `:154-155` | ✅ 4행 각각 |
| 실행 축 시점 + **«성공 → 실패» 음성 단언** | P5 `:206` | ✅ 회귀 방지 의도까지 명시 |
| 멱등 재진입(동시 포함) | P5 `:207` · P1 검증 `:134` | ✅ |
| `source` 게이트(이중 생성 0) | P5 `:209` · P1 검증 `:133` | ✅ **발동 축은 커버됨** |
| 역해소 경계 3종(전원 / 일부 미해소 / **host 미해소 → 보상**) | P5 `:210` | ✅ |
| 동기화 분기 | P5 `:208` — 변경×(대기/진행중·종료) · 취소×(대기/진행중·종료) · 연결 없는 run×(변경/취소) | ⚠ **모달 발 origin × (취소/변경) 이 없다** → V-3 |
| 슬롯 밖 시각 통과(`:15`·`:40`) | P1 검증 `:136` | ✅ 계약의 핵심 델타를 테스트로 못박음 |
| 무변경 축(모달 발·게이트·권한·API·프롬프트·MCP) | P5 `:211` | ✅ |
| 전체 스위트 미실행 + 지목 범위 baseline 대조 | P5 `:214-215` | ✅ WP-122 선례와 같은 방침 |

---

## 기존 부채 (이번 판정 제외)

- `30-work.md:25` Status Board 헤더에 **`QA` 컬럼**이 있다 — `rules/document-pipeline.md` §Work Tracking 은 "**`QA` 컬럼은 두지 않습니다**"(QA 진행은 Google Sheet 가 단일 source). **WP-125 행은 형제 행들과 동형**이라 이 WP 의 문제가 아니고, 보드 전체의 선재 드리프트다.
- `30-work.md` WP List 에 **`MEDINESS-WP-102` 가 2행**(landing-chat / ax cutover) — 번호 중복, 이번 diff 밖.
- `30-work.md:233` SPEC-030 Coverage derive WARN — R1·R2 와 동일.

## 확인 안 함

- WP 본문의 **일정 추정치**(3~5d) 타당성 — 리뷰 기준이 없다(owner `TBD` 라 다음 review 의 open item).
- `mediness-app` 실행·테스트 — 이 WP 는 `proposed`(코드 0)이라 대상이 없다.
- SPEC-151 §7.9 전 절 재검증 — R2 에서 수행. 이번엔 **WP 와 맞물리는 절**(§7.9.1·§7.9.2·§7.9.4·§7.9.5·§7.9.7 · §5.3.2)만 봤고, 그 과정에서 V-3b 가 나왔다.
