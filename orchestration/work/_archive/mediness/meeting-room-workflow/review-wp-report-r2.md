# 재검수 리포트 R4 — WP-125 V-3 해소 확인 (파급 가드 · source 한정) / 2026-08-31

> 선행: [review-wp-report.md](review-wp-report.md)(R3 FAIL — V-3 · V-3b · RW-4~6) · [review-spec-report-r2.md](review-spec-report-r2.md)(R2 PASS)

## 판정: PASS (경미 3건 동반 — 비차단)

**V-3 · V-3b · WARN(절 번호) 전부 해소.** 특히 V-3 는 내가 낸 권장 수정 4가지를 **하나도 빠뜨리지 않고** 이행했고, V-3b 는 **SPEC 양쪽(151·031)에 같은 한정을 세우면서 OPEN-031-Y 를 대신 닫지 않는** 형태로 정확히 착지했다. 새 모순은 **없다** — 남은 3건은 **라벨 수치·요약 비대칭** 수준이다. ⚠ 그중 하나(R4W-1)는 **R2 의 RW-1 과 같은 종류가 다시 난 것**이라 근본 대책을 함께 적는다.

## 검수 범위

- 대상: `work-125-...md`(untracked, R3 대비 수정분) + 미커밋 diff 4파일. R4 diffstat = **+153/−9**(R3 = +150/−9 ⇒ SPEC-031 +1줄 · SPEC-151 +2줄)
- 재검수 방식: R3 리포트의 **V-3 · V-3b · RW-4** 와 그 주변만. 전면 재리뷰 아님
- `python3 scripts/lint-pipeline.py --strict` → **0 error / 255 warning**(exit 0). mediness 범위 **ERROR 0**, WARN 1건은 `30-work.md:233` SPEC-030 Coverage derive **기존분(무관 — R1~R3 동일)**
- 리포 파일 수정·생성 **0**(`git status` = planner 산출물 그대로: M 4 + ?? 1)

---

## 항목별 판정

### V-3 (P3 파급이 모달 발 회의를 막지 않는다) — ✅ **해소** · 권장 수정 4/4 이행

| 내가 요구한 것 | 반영 자리 | 판정 |
|---|---|---|
| ① 진입 가드를 **「연결 + `source`」 2겹**으로, ②는 **P1 판정 재사용** | P3 작업 1번째 — "**진입 가드 2겹 — 「자동 등록된 회의만」** ⚠ **연결 유무만으로는 못 거른다.** … 가드는 **① 연결이 있을 것 + ② run 의 `source` 가 모달 발이 아닐 것** 둘이며, ②는 **P1 과 같은 판정을 재사용**한다(정의는 한 자리 — §Open Issues)" | ✅ 2겹 · 재사용 · **정의 한 자리** 규율까지 |
| ② 괄호를 **코드 사실대로** 정정 | P3 작업 2번째 — "**걸러지는 두 부류는 이유가 다르다** — **구 예약 건**은 «연결이 없어서», **모달 발 회의**는 «연결은 있지만 `source` 로 배제돼서»" | ✅ **틀린 전제가 제거**됐을 뿐 아니라, 가드 1번째에 "모달 발 회의도 생성 시 `link_reservation` 으로 `reservation_run_id` 를 채우므로 … **연결은 있다**"를 **근거로** 실었다 |
| ③ **비목표**에 모달 발 축 명시 + **P5 음성 단언** | 비목표 — "⚠ **「모달 발로 만들어진 회의」의 취소·변경 파급**(같은 OQ 가 **열어 둔 축** — 모달 발 회의도 예약 참조를 **갖지만** 이 WP 는 `source` 로 배제한다. §Phase P3 진입 가드)" / P5 — **동기화 분기 «8종»**(구 6종 + **모달 발 origin × (취소 / 변경) = 음성 단언**) | ✅ 8종 확인. P3 §검증에도 같은 음성 단언이 따로 섰다 — "연결은 **있지만** soft delete 0 · 예정 일시 변화 0 · 연결 유지. 🔑 **가드가 실제로 `source` 로 걸린다는 증거**다(연결 유무로 걸었다면 여기서 회의가 사라진다)" |
| ④ 파급이 의도라면 **SPEC 환류가 선행** | P3 작업 3번째 — "**모달 발 회의를 건드리지 않는다** … 🔑 **WP 가 코드로 그 OQ 를 닫지 않는다** — 파급시키는 것이 의도라면 **SPEC 을 먼저 고치는 환류**가 선행한다" | ✅ 규율을 WP 본문에 박았다 |

- **Scope 도 함께 좁혔다**(내가 요구하지 않은 자리인데 스스로 정렬) — "**수정·취소 파급** — **이 체인이 자동 등록한 회의에 한해**(⚠ 모달 발 회의는 연결이 **있어도** 대상이 아니다 — §Phase P3 진입 가드)" ✅
- **P3 §설명**도 "**이 체인이 자동 등록한 회의를 따라가게** 한다(⚠ **연결이 있는 모든 회의가 아니다**)" 로 선언 ✅
- 🔑 **가드 수준의 비대칭이 사라졌다** — 발동(P1: `source` + 멱등)과 파급(P3: 연결 + `source`)이 **같은 축으로 정렬**됐고, 정의는 한 자리(§Open Issues 의 `source` 판정 값 소재 항목)로 유지된다.

### V-3b (뿌리 — §7.9.5 의 source 무한정) — ✅ **해소** · OQ 를 대신 닫지 않음

- **SPEC-151 §7.9.5 첫 문장 한정** ✅ — 구 "연결된 회의는 예약을 따라간다" → "**이 체인이 자동 등록한 회의**(§7.9.1 발동 조건으로 만들어진 회의)는 예약을 **따라간다**". 🔑 발동 조건 절을 **명시적으로 인용**해 「자동 등록」의 정의를 떠돌지 않게 했다.
- **경계 문단 신설** ✅ — "⚠ **대상은 «연결이 있는 모든 회의» 가 아니라 «자동 등록된 회의» 다** *(경계 명문화 — 계약 변경이 아니라 이미 의도된 범위를 적는 것)*. **회의 모달 발로 만들어진 회의도 예약 참조를 갖지만**(그 연결 축은 원래 모달 발 방향이 만든 것이다) **이 절의 대상이 아니다.** 🔑 그쪽의 취소·변경 파급은 … **OPEN-031-Y 가 「모달 발로 만들어진 회의의 취소 축」으로 열어 둔 채**다 — **이 절이 그것을 대신 정하지 않는다.** ⇒ 판정은 **§7.9.1 과 같은 축(`source`)**"
  - ⇒ 브리프가 물은 **「경계 문단이 OPEN-031-Y 를 대신 닫지 않는가」** = ✅ **닫지 않는다.** 열려 있음을 **명시**하고 판정 축만 빌려 온다.
  - ⇒ 「계약 변경이 아니라 의도된 범위의 명시」라는 자기 규정도 정확하다 — 내가 R3 에서 권고한 성격 그대로다.
- **SPEC-031 §3 두 파급 bullet 정렬** ✅
  - 변경 파급 — "그 예약이 **자동 등록한** 회의의 `scheduled_*` 가 새 값으로 따라간다"
  - 취소 파급 — "**자동 등록된 대기(`waiting`) 회의를 soft delete** 한다"
  - **경계 bullet 신설** — "⚠ **두 파급의 대상은 «자동 등록된 회의» 뿐이다** … **모달 발로 만들어진 회의도 예약 참조를 갖지만** … **§6 OPEN-031-Y 가 열어 둔 자리**이며 여기서 대신 정하지 않는다. 🔑 **판정은 연결 유무가 아니라 접수 표면(`source`)**"
  - 🔑 **소유 경계 유지** — SPEC-031 이 판정 축을 스스로 정의하지 않고 "[SPEC-151 §7.9.5] — **그 절이 같은 축으로 한정을 소유한다**" 로 정본을 가리킨다 ✅
- **OPEN-031-Y 열린 목록** ✅ — 「이 가산이 닫지 않는 것」 bullet 에 "**그리고 「모달 발로 만들어진 회의의 취소·파급 축」은 그대로 열려 있다**" 가산. OQ 본문 「여전히 열려 있는 것」에는 R2 시점부터 이미 그 항목이 있었고 **그대로 유지**된다 ✅
- ⇒ **SPEC 두 곳 · WP 한 곳이 같은 한정을 말하고, 그 한정이 OQ 를 닫지 않는다** — R3 에서 갈렸던 세 문장이 한 방향으로 모였다.

### RW-4 (조사 리포트 절 번호) — ✅ **정정**

`work-125-...md` Code Surface `meeting_v2_repo.py` 행 — "(조사 리포트 **§C-11**)" → "(조사 리포트 **§3-2 ▸ 2-b**)". 그 절이 실제로 「예약은 회의 생성과 한 트랜잭션인가」이고 커밋 3회·고아 예약 서술을 담는다 ✅. 문서 전체에 `§C-11` 잔존 **0**.

### 신규 모순 — **없음**

R4 가 건드린 자리(§7.9.5 첫 문장·경계 문단 / SPEC-031 §3 두 파급 bullet·경계 bullet·OQ 목록 / WP Scope·비목표·P3·P5·Code Surface) 주변을 훑었다.

- **한정이 계약을 좁히기만 하고 넓히지 않았다** — 자동 등록 회의에 대한 처분(예정 일시 갱신 · 대기 soft delete · 진행 중·종료는 연결만 끊기)은 **한 글자도 바뀌지 않았다** ✅
- **§5.3.2 35칸 · 조합표 4행 · 발동 조건 · 원자성 · 표면** 무변경 ✅ (R4 diff 는 §7.9.5 2줄·SPEC-031 1줄·WP 문면뿐)
- **`source` 축이 두 벌이 되지 않았다** — §7.9.1(발동) · §7.9.5(파급) · SPEC-031 §3(경계) · WP P1·P3 가 **모두 같은 축 하나**를 가리키고, 값의 소재는 WP §Open Issues 가 「정의는 한 자리」로 계속 잡고 있다 ✅
- **모달 발 경로 무변경 선언과 충돌 없음** — §7.9.7 "모달 발 경로도 한 글자도 바뀌지 않는다" 가 이제 **파급 축에서도 참**이 된다(R3 에서는 P3 가 그것을 깨고 있었다) ✅
- lint **0 error**, 3자 동기·doc_no 검사 전부 통과 유지 ✅

---

## 경미 (WARN — 비차단)

- **R4W-1. 「6줄/6종」 라벨이 다시 낡았다 — RW-1 재발(4곳).** R4 가 bullet 과 테스트 분기를 늘리면서 수치 라벨이 따라오지 않았다.
  - `spec-031-...md:1390` 부모 bullet — "회의 도메인이 소유하는 것은 아래 **여섯**(… · OQ 경계)" ⇒ **실제 하위 bullet 7개**(경계 bullet 신설분 미반영)
  - `spec-031-...md:30` 변경 노트 ⑭ — "§3 가산 **6줄** + 정합 3자리" ⇒ **7줄**
  - `log.md` spec-change 행 — "§3 가산 **6줄**·정합 3자리" ⇒ **7줄**
  - `log.md` wp-add 행 — P5 요약이 "동기화 분기 **6종**" ⇒ **8종**(WP 본문은 8종으로 갱신됨)
  - 🔑 **근본 대책 제안** — 이 라벨은 R2(4→6)·R4(6→7)에서 **두 번 연속** 어긋났다. 수치가 본문과 따로 노는 구조라 매 라운드 드리프트한다 ⇒ **수치를 빼고 「가산 bullet + 정합 자리」처럼 세지 않는 표현**으로 바꾸는 것이 재발을 끊는다. 계약에는 영향 0.
- **R4W-2. 「열린 목록」이 두 SPEC 사이에서 비대칭이다.** SPEC-031 의 「이 가산이 닫지 않는 것」 bullet 은 **모달 발 축을 센다**(R4 가산분)는데, SPEC-151 쪽의 같은 성격 문장 둘은 **세지 않는다** —
  - `spec-151-...md` §7.9.5 마지막 bullet — "ⓐ·ⓒ 와 **ⓑ 의 반대 방향** 은 그대로 열려 있다"
  - 같은 파일 개정 노트 ⑤ — "나머지(ⓐ·ⓒ·**반대 방향**)는 열려 있다"

  §7.9.5 의 **경계 문단**이 모달 발 축을 명시하므로 **모순은 아니고**, SPEC-151 전체로 읽으면 정보는 다 있다. 다만 「열려 있는 것」을 요약하는 자리끼리 목록이 달라 R2 의 RW-2(형제 bullet 비대칭)와 같은 종류다 → 두 문장에 **「그리고 모달 발로 만들어진 회의의 취소·파급 축」** 한 구절만 더하면 정렬된다.
- **R4W-3. `30-work.md` 파생 view 셀이 한정 없이 파급을 요약한다.** Status Board `:113` scope 칸 = "**수정·취소 파급 포함**(예정 일시 서버 갱신 · 대기 회의 soft delete · **진행 중·종료는 연결만 끊기**)", Spec Coverage SPEC-031 행 `:244` 도 동형 — **「자동 등록 회의 한정」이 빠져 있다.** 인덱스는 **derived view** 이고 정본(WP 본문·SPEC)이 한정을 담고 있어 **모순이라기보다 요약 누락**이지만, 이번 라운드의 쟁점이 바로 그 경계였으므로 한 구절 넣기를 권한다.
- **(캐리) `log.md` 두 행의 `PR` 칸 `—`** — R1 W-3 · R2 RW-3 · R3 RW-5 와 동일. PR 생성 후 기입 항목, 차단 아님.

## 기존 부채 (이번 판정 제외 — R3 와 동일)

- `30-work.md:25` Status Board 헤더의 **`QA` 컬럼**(규칙상 두지 않음) · WP List 의 **`MEDINESS-WP-102` 2행** · `:233` SPEC-030 Coverage derive WARN — 전부 선재 드리프트.

## 확인 안 함

- WP 전면 재검수(Code Surface 심볼 재실측·3자 동기·테스트 계획 전수) — **R3 에서 통과**했고 R4 diff 가 그 결론을 건드리지 않는다(코드 좌표 변경분 = 절 번호 1곳뿐).
- `mediness-app` 코드 — WP 는 여전히 `proposed`(코드 0).
