# 리뷰 리포트 — meeting-room-workflow / planner(문서) 리뷰 (2026-08-31)

## 판정: FAIL

위반 2건 — 둘 다 **신설 §7.9 가 기존 SPEC-031 계약과 정면으로 어긋나는데 그 어긋난 자리를 개정하지 않은 것**이다. 사용자 확정 6건 위반은 **0건**이고, 린트도 깨끗하다. 수정 범위는 작다(SPEC-031 두 자리 + SPEC-151 문면 1곳).

---

## 검수 범위

- diff: 워크트리 미커밋 변경 (base `origin/mediness` 대비), **3 파일 / +129 −2**, untracked **0건**
  - `products/mediness/20-spec/spec-151-ax-assistant-reservation.md` (+123 −2 — §7.9 신설 · 개정 노트 · S-7 · §4.1 · §5.1 · OQ-9 · frontmatter `last_updated`/`sources`)
  - `products/mediness/20-spec/spec-031-meeting-v2-diarization.md` (+7 — 변경 노트 ⑭ 1문단 + §3 가산 bullet 1개(하위 4줄))
  - `products/mediness/log.md` (+1행)
- allowed_paths(브리프 §5 = planner 브리프 범위 = 위 3파일) **이탈 없음**
- 실행한 검사
  - `python3 scripts/lint-pipeline.py --strict` → **0 error / 255 warning**, exit 0. mediness 범위 WARN **1건**(무관·기존분, 아래 §기존 부채)
  - SPEC-031 대조: §Domain note(state enum) · §API 계약 · §Request/Response · §Validation · §케이스 매트릭스 · §3 회의실 예약 연동 계약 · §6 OPEN-031-Y / OPEN-031-Q
  - SPEC-151 대조: §2 · §5.1 · §5.3.1 · §5.3.2(35칸) · §6.2 · §7.1 · §7.2 · §9 OQ-7
  - 코드 사실 대조: `research-meeting-create-flow.md`(§1-b, §3-a, §3-b, §3-2)

---

## 위반 (FAIL 사유)

### V-1. 「30분 슬롯은 모달 입력 폼의 검증」이 사실이 아니고, SPEC-031 의 계약 자리를 안 고쳤다

- **자리**: `spec-151-...md` §7.9.2 마지막 ⚠ bullet / `spec-031-...md` §3 회의실 예약 연동 계약 가산 3번째 ⚠ bullet (+ 변경 노트 ⑭ 의 "30분 슬롯 제약 비적용")
- **무엇이 어긋났나**: 두 곳 모두 30분 슬롯 제약을 **「모달 입력 폼의 검증」/「입력 검증 규칙」** 이라 규정하고 회의 생성에 적용하지 않는다고 선언한다. 그런데 SPEC-031 의 계약 본문은 그것을 **서버 검증(422)** 으로 소유하고 있다:
  - `spec-031-...md:1549-1550` §Validation — `scheduled_start` = "필수, `HH:MM`, **30분 슬롯**(`:00` 또는 `:30`)", `scheduled_end` 도 동일
  - `spec-031-...md:1569` §케이스 매트릭스 — `VALIDATION_ERROR (시간) | **422** (…, **슬롯 위반**, …)` → 백엔드 출력 칸이다
  - 같은 SPEC 은 **"모달 입력 규칙"과 "서버 검증"을 구분하는 서술 관례를 이미 갖고 있다** — `product_tags` 행: "모달의 「칩 필수」는 **입력 규칙**이며 **서버는 빈 `product_tags` 를 거절하지 않는다**", 케이스 매트릭스 `TAG_REQUIRED` 행: "— (**서버 검증 아님**)". 슬롯 규칙에는 그 표식이 **없다.**
  - ⇒ 개정 후 SPEC-031 은 §3 에서 "이 경로엔 적용 안 함", §Validation·§케이스 매트릭스에서 "필수·위반 시 422" 를 **동시에** 말한다. **같은 문서 안의 모순**이며, 구현자가 §Validation 을 읽으면 `15:10` 예약 발 회의는 그대로 422 로 막힌다(예약 payload `{date,start,end}` 에는 슬롯 제약이 **없다** — SPEC-151 §6.2).
- **근거(규칙 출처)**: `rules/document-pipeline.md` §한 곳 원칙 — "API endpoint 계약 … 은 `20-spec/spec-NNN-*.md` `## API 계약` 이 소유", 같은 사실을 두 곳이 다르게 말하면 reader 가 어느 쪽을 믿을지 모름 / §변경 라우팅 — "API endpoint · request/response 스키마 · error code 가 바뀌면 해당 SPEC 을 수정한다"
- **참고(사실 관계는 planner 쪽이 부분적으로 맞다)**: 코드에서 슬롯은 **요청 스키마**에 있고(`back/app/schemas/meeting_v2.py:25` `_SLOT = ^([01][0-9]|2[0-3]):(00|30)$`) **DB CHECK 는 `HH:MM` 형식 + `end > start` 둘뿐**이다(`0115_meeting_v2_schedule_split.py:167-176` — 리서치 리포트 §1-b·§3-a). 즉 "도메인 불변식은 둘" 이라는 주장 자체는 코드와 일치한다. **틀린 것은 「모달 입력 폼의 검증」이라는 성격 규정과, 계약 문서를 안 고친 것**이다.
- **권장 수정**: SPEC-031 §Validation `scheduled_start`/`scheduled_end` 행과 §케이스 매트릭스 시간 행에 **경로 단서**를 박는다(예: "30분 슬롯은 `POST /meetings-v2` 요청 스키마 규칙이며, 예약 발 자동 등록 경로에는 적용하지 않는다 — §3 예약 발 회의 자동 등록"). SPEC-151 §7.9.2 의 "모달 입력 폼의 검증" 은 "모달 경로 **요청 스키마**의 검증(도메인 CHECK 는 `HH:MM`·`종료 > 시작` 둘)" 으로 문면을 정정.

### V-2. §7.9.5 가 요구하는 회의 쪽 처분 2종(예정 일시 변경 · 회의 닫기)이 SPEC-031 에 자리가 없고, 바로 옆 기존 서술과 어긋난다

- **자리**: `spec-151-...md` §7.9.5 (동기화 표 + 취소 파급 상태 분기 표) / `spec-031-...md` §3 가산 4번째 bullet
- **무엇이 어긋났나**: §7.9.5 는 회의 도메인에 **두 가지 변형**을 요구한다 — ① 변경 게이트 실행 성공 시 **회의의 예정 일시를 바꾼다**, ② 취소 게이트 실행 성공 시 **대기 회의를 «닫는다»**. 둘 다 SPEC-031 이 정의한 적이 없고, 인접 기존 문단이 명시적으로 부정한다:
  - `spec-031-...md:1387`(**미개정, 가산 bullet 바로 위**) — "**회의 쪽 변경이 예약에 파급되는 경로는 없다** — v2 회의의 수정 표면은 `PATCH .../visibility` 하나뿐이라 **예정 시간을 나중에 바꾸는 기능 자체가 없고** … **회의 시간 수정 기능이 생기면 그때 파급 여부를 새로 정한다**" ⇒ ①이 그 트리거를 당기는데 그 문단은 손대지 않았다.
  - `spec-031-...md:1388`(**미개정, 가산 bullet 바로 위**) — "⚠ **회의 취소는 아직 계약이 없다** … **이번 라운드에서 계약을 신설하지 않는다**(§6 OPEN-031-Y)" ⇒ ②가 «회의를 닫는» 처분을 계약으로 만든다.
  - **「닫는다」의 뜻이 어디에도 없다** — SPEC-031 §Domain note(`:340`)의 state enum 은 **`waiting → live → paused → ended` 4값뿐**(코드 동일: `meeting_v2.py` `state` enum, 리서치 §3-a `:162`)이라 «닫힘/취소» 값이 없고, §API 계약 표에 `DELETE /meetings-v2/{id}` 도 **없다**. 행 삭제인지, 새 상태인지, ended 로 보내는지가 미정이면 구현이 임의로 정하게 된다.
  - OPEN-031-Y 는 "**회의 취소 3표면**(회의 화면 직접 · **채팅** · THE CONNECT 발 reconcile)" 이다(`:341`, `:226`). §7.9.5 는 그중 **채팅 표면의 회의 취소를 사실상 정한다.** 그런데 두 변경 노트 모두 OPEN-031-Y 를 "**회의를 없앨 때 걸린 예약의 처분**" 으로만 좁혀 인용하며 "이 가산으로 닫히지 않는다" 고 적었다 — **OQ 의 실제 범위와 다른 축소 인용**이다.
- **근거(규칙 출처)**: `rules/document-pipeline.md` §문서별 추상화 수준(SPEC = 상태 lifecycle 의 소유자) · §한 곳 원칙 「Entity 상태(외부 노출)」 = 해당 SPEC `## 상태 / Lifecycle` 이 소유 / 본 SPEC 자신의 §7.9.7 — "**회의 도메인의 계약은 SPEC-031 이 소유**하고, 본 절은 「무엇으로 만드는가」와 「무엇이 따라가는가」만 정한다"
- **방향 자체는 사용자 확정 4번(수정·취소 동기화 이번 범위 포함)이라 문제 없다.** 문제는 **회의 쪽 처분의 정의가 SPEC-031 에 서지 않았다**는 것.
- **권장 수정**: SPEC-031 §3 가산 bullet 에 (a) 「예약 발 자동 등록 회의는 예약 변경 파급으로 **예정 일시가 서버에 의해 갱신된다**(사용자 수정 표면은 여전히 `PATCH visibility` 하나)」, (b) 「대기 회의의 «닫는다» = **어느 상태/행 처분인지**」를 한 줄로 못박고, `:1387`·`:1388` 두 기존 bullet 에 "예약 발 자동 등록 경로는 예외 — §3 가산" 단서를 단다. 겸해서 OPEN-031-Y 인용을 실제 범위(3표면)에 맞춰 "채팅 표면 중 **예약 → 회의** 방향만 이번에 정한다" 로 정정.

---

## 경미 (WARN)

- **W-1. `spec-151-...md` §7.9.1 마지막 bullet — 「예약 건당 회의 최대 1」을 SPEC-031 §3 연결 축 인용으로 붙였는데, 그 절에는 그 방향이 없다.** SPEC-031 `:1384` 는 "**회의당** 살아 있는 예약 참조는 최대 1개" 한 방향만 말하고, 코드도 `meeting_v2.reservation_run_id` 가 nullable FK 일 뿐 **유니크 제약이 없다**(리서치 §3-a `:161`). 멱등을 그 인용에 기대면 구현이 근거 없는 제약을 전제한다. → 인용을 「회의당 최대 1」로 줄이고, 「예약 건당 회의 최대 1」은 **이 절이 새로 세우는 불변식**임을 명시(유니크 인덱스 필요 여부는 WP 로).
- **W-2. `spec-151-...md` §7.9.4 — 「체인이 끝날 때까지 실행 축이 «진행 중» 에 머문다」가 조합표 안에 안 적혀 있다.** 조합표 4행은 §5.3.2 의 35칸을 벗어나지 않고(A·B·C·D 전부 `승인 ×` 합법 칸 — **새 칸 0**, 확인함), C 행이 «성공 → 실패» 라는 **표에 없는 전이**가 되지 않으려면 외부 예약 성공 시점에 실행 축을 «성공» 으로 적지 않아야 한다. 그 전제는 지금 **§5.1 가산 한 줄**("실행 단계는 «외부 예약 + 회의 자동 등록» 까지다")에서만 유추된다. §5.3.2 가 실증으로 잡아낸 결함이 정확히 «성공 기록 후 최종 실패 대입» 이므로, §7.9.4 안에 한 줄로 못박기를 권한다.
- **W-3. `log.md` 신규 행의 `PR` 칸이 `—` 다.** `rules/document-pipeline.md` §log.md 는 "PR 번호(`#NNN`) 또는 근거가 있는 실제 링크" 를 요구한다. 직전 행(HEAD 기존분)도 `—` 라 선례는 있고 PR 은 코디네이터가 올리므로 **차단 사유는 아니다** — PR 생성 후 채우면 된다.
- **W-4. 코드 사실 「`participants` = 이메일」은 리서치 리포트가 «필드 실재»까지만 확인한다.** 리포트 §3-b `:197` 은 `actions.payload` 에 `{…, attendees, participants}` 두 축이 함께 있음을 보이지만 `participants` 의 값 형태(이메일)는 명시하지 않는다. SPEC-151 §7.2 는 입력 축을 `attendees`(발화 라벨 = **이름 문자열**)로 정의하므로, §7.9.2 표의 「plan 의 `participants`(초대 확정 이메일)」는 **사용자 확정 1번을 근거로 수용**했다. 착지 WP 에서 실제 필드 형태를 한 번 확인할 것을 권한다(계약 문면 자체는 사용자 확정과 일치하므로 위반 아님).

---

## 기존 부채 (이번 판정 제외)

- `products/mediness/30-work.md:231` — `MEDINESS-SPEC-030` Spec Coverage 구현 상태 `in_dev` ≠ covering WP derive 값 `done` (WARN). **이번 diff 가 건드리지 않은 파일**이고 HEAD 기존분이다.
- 타 제품(`procedure-hub`·`selly`·`linky`·`charty`) frontmatter `doc_no`/`version` 누락 WARN 다수 — **무관**(mediness 범위 밖).

---

## 확인한 것 (PASS 근거)

- **린트**: `--strict` **0 error**, exit 0. mediness 범위 ERROR **0**, WARN 1건은 위 기존 부채. ✅
- **allowed_paths**: diff 3파일 전부 브리프 범위 안, untracked 0건. ✅
- **사용자 확정 6건 + 추가 1건 전수 대조** — 위반 0, 재론·확장 0:
  1. 참석자 = payload `participants`(이메일) → organization_member 역해소 → §7.9.2 표 + §7.9.3 ✅ (형태 확인은 W-4)
  2. visibility 기본 `private` → §7.9.2 표 ✅ (SPEC-031 §Validation default 는 `public` 이나 명시값 지정이라 충돌 아님)
  3. 재시도 없음 · 보상(예약 취소) · all-or-nothing → §7.9.4 ✅
  4. 수정·취소 동기화 이번 범위 포함 → §7.9.5 ✅ (단 회의 쪽 처분 정의가 V-2)
  5. 발동은 `source != meeting_modal` → §7.9.1 ✅ (코드 `MEETING_MODAL_SOURCE = "meeting_modal"` — 리서치 §3-2)
  6. host = 예약 requester · `product_tags` 빈 배열 → §7.9.2 표 ✅ (SPEC-031 §Validation "빈 배열은 유효하다" 와 정합, host=requester 는 코드 `meeting_v2_service.py:225` 와 동형)
  - 추가: 시간대별 회의실 점유 스케줄 화면 **범위 제외** → §7.9.7 ⛔ + 개정 노트 ⑧ ✅
- **§5.3.2 합법 조합표 정합**: §7.9.4 A~D 4행의 (판단, 실행) 좌표가 전부 §5.3.2 의 **✅ 칸 안**(승인 × 진행중/성공/실패(재시도)/실패(종료))이며 **새 칸·새 열 0**. D 행이 실행 축을 «성공» 으로 유지하는 것도 §5.3.2 의 실증 bullet(「성공으로 기록한 뒤 최종 실패로 대입」이 결함이었다)과 **같은 방향**이다 — 표를 거스르지 않는다. 전이 legality 는 W-2 단서 하나만 붙는다. ✅
- **소유 경계(중복 서술)**: §7.9 는 참석자 «초대 가능 판정»을 스스로 정의하지 않고 SPEC-031 OPEN-031-Q RESOLVED 를 인용해 회의 쪽 판정에 양보한다(§7.9.3) ✅. 연결 축·참조 projection(U-7·U-8)도 SPEC-031 인용만 하고 재정의하지 않는다 ✅. SPEC-031 가산분은 **§3 bullet 1개(하위 4줄) + 변경 노트 1문단**으로 최소이고, 발동 조건·원자성·보상·조합표를 **전부 SPEC-151 소유로 위임**한다 ✅. (단 V-2 는 «위임하면 안 되는 것을 위임한» 반대 방향의 문제다.)
- **모달 발 경로 불변 주장 검증**: §7.1 자동승인 예외 · §7.2 승인 시점 방 배정 · §4.2 명령 파생 · §6 API·권한 — diff 상 **변경 0줄**. SPEC-031 모달 발 흐름(`:646`·`:678`) 도 무변경. ✅
- **원장 방향 불변**: §7.9.7 의 "참조는 회의가 소유 · 예약 원장은 회의를 모른다" 는 SPEC-031 `:1384` 및 코드(`meeting_v2.reservation_run_id` 단방향 FK, 리서치 §3-a)와 일치. ✅
- **frontmatter**: SPEC-151 `last_updated: 2026-08-31` 갱신 ✅ · `sources` 에 spec-031 가산 ✅ · `doc_no`(DOC-167 / DOC-95) 유지, 중복 없음(린트 통과) ✅. SPEC-031 은 `last_updated` 가 이미 2026-08-31 이라 무변경이 정상 ✅.
- **log.md**: `spec-change` 종류 · 영향 ID 2건 · 결정 entry 수준의 근거 서술 — §log.md 규약 충족 ✅ (PR 칸만 W-3).
- **WP 갱신 체크리스트**: 이번 라운드는 **SPEC 전용**이며 WP 신설·상태 변경이 없다(브리프도 "구현 WP 는 별도 발주"). 따라서 `30-work.md` WP List / Status Board / Spec Coverage **동기 대상 없음** — 린트 3자 일치 검사도 통과. **확인 안 한 것 없음.**
- **확인 안 함**: 백엔드/프론트 코드 리뷰(이번 모드는 planner 리뷰 — diff 에 코드 파일 0) · 21-html 시안(이번 diff 에 없음).
