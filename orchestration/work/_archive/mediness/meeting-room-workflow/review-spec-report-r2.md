# 재검수 리포트 R2 — meeting-room-workflow / planner(문서) 리뷰 (2026-08-31)

> 1차: [review-spec-report.md](review-spec-report.md) — 판정 FAIL (V-1 · V-2 + W-1~W-4)

## 판정: PASS (경미 3건 동반 — 비차단)

**FAIL 사유 0건.** V-1 · V-2 둘 다 해소됐고, W-1 · W-2 도 반영됐다(덤으로 W-4 까지). 수정이 만든 **새 모순 0건** — 오히려 soft delete 근거가 내가 요구한 것보다 단단하다(코드 컬럼 + 기존 §권한 문장 두 곳이 이미 받쳐 준다). 남은 3건은 **라벨 신선도·서술 대칭** 수준이고 계약을 흔들지 않는다.

## 검수 범위

- diff: 미커밋 3파일 **+145 −7** (R1 대비 SPEC-031 +7→+22, SPEC-151 +123→+129, log 1행 갱신). untracked 0건, allowed_paths 이탈 0.
- 재검수 방식: 1차 리포트의 **「권장 수정」 4항목 + 신규 모순**만. 전면 재리뷰 아님.
- 실행: `python3 scripts/lint-pipeline.py --strict` → **0 error / 255 warning**, exit 0. mediness 범위 **ERROR 0**, WARN 1건은 SPEC-030 Coverage derive **기존분(무관, R1 과 동일)**.
- 리포 파일 수정·생성 **0** (`git status` = planner 3파일 그대로).

---

## 항목별 판정

### V-1 (30분 슬롯 소유 자리) — ✅ **해소**

권장 수정 3가지를 전부 이행했고, 「필수·422」↔「비적용」 동시 발화가 사라졌다.

- `spec-031-...md:1551` §Validation `scheduled_start` — "⚠ **슬롯은 `POST /meetings-v2` 요청 스키마의 규칙**이며 **예약 발 자동 등록 경로에는 적용하지 않는다**(§회의실 예약 연동 계약 ▸ 역방향 가산). 도메인 제약은 `HH:MM` 형식과 아래 `종료 > 시작` 둘이다" ✅
- `spec-031-...md:1552` `scheduled_end` — 같은 경로 단서 + "**`종료 > 시작` 은 경로 무관 도메인 제약**" 구분 ✅ (두 규칙의 성격을 갈라 적은 것이 정확하다)
- `spec-031-...md:1571` §케이스 매트릭스 시간 행 — "슬롯 위반은 요청 스키마 판정이라 예약 발 경로에는 적용되지 않는다 / `종료 > 시작`·날짜 형식은 경로 무관" ✅
- `spec-151-...md` §7.9.2 — 「모달 입력 폼의 검증」 → **「모달 경로 요청 스키마(`POST /meetings-v2`)의 검증」** 으로 문면 정정, "도메인 CHECK 는 `HH:MM`·`종료 > 시작` 둘이고 **그 둘은 경로 무관으로 이 경로에도 적용된다**" 명시 ✅. 개정 노트 ③ 도 같은 문면으로 정정 ✅
- **양방향 참조**가 붙었다 — SPEC-151 §7.9.2 → SPEC-031 §Validation·§케이스 매트릭스, SPEC-031 §3 → 같은 두 자리. "한쪽만 적으면 그 SPEC 이 「필수·422」와 「비적용」을 동시에 말한다" 는 이유까지 남겼다 ✅
- 코드 사실과도 여전히 정합 — 슬롯은 요청 스키마(`schemas/meeting_v2.py:25`), DB CHECK 는 `HH:MM` + `end > start` 둘뿐(`0115_...py:167-176`, 리서치 §3-a) ✅

### V-2 (회의 쪽 처분 2종) — ✅ **해소** · 근거가 요구치보다 단단함

- **처분 정의가 SPEC-031 자기 자리에 섰다** (`:1394`·`:1395`, §3 역방향 가산):
  - 변경 파급 = **예정 일시를 서버가 갱신**(`scheduled_date`·`start`·`end`). "사용자 수정 표면이 늘어난 것이 아니다 — 사람이 만질 수 있는 것은 여전히 `PATCH .../visibility` 하나" 를 함께 못박음 ✅
  - 취소 파급 = **대기(`waiting`) 회의 soft delete** · **`state` enum 무신설**(`waiting → live → paused → ended` 불변 명시) · **`DELETE` 사용자 표면 신설 0** ✅ — 내가 지적한 "「닫는다」의 뜻이 없다" 가 정확히 메워졌다
- **기존 도메인 어휘 안에서 성립한다 (실측 대조)**:
  - `meeting_v2.deleted_at` 컬럼이 **코드에 이미 있다**(리서치 §3-a `:166` "soft delete") — 새 컬럼 축 0 ✅
  - "목록·상세에서 빠진다" = SPEC-031 `:1499` 기존 문장 "기존 validation, **soft delete 제외**, 목록 정렬…" 과 정합 ✅
  - "`system_admin` read override 도 **soft-deleted 아닌** 회의만 본다는 §권한 전제 그대로" = SPEC-031 `:1496` 기존 문장 "**`system_admin` 에게는 실제 미존재·soft-deleted 만 404**" 과 **글자 그대로 일치** ✅ — 근거 없는 「전제」 인용이 아니었다
- **인접 미개정 bullet 두 개에 예외 단서가 붙었다** (내 권장 그대로):
  - `:1388`(회의 취소 계약 없음) → "⚠ 예외 — 예약 발 자동 등록 경로는 아래 「역방향」 가산이 정한다 … 이 bullet 이 계속 열어 두는 것은 **회의 → 예약 방향**" ✅
  - `:1389`(예정 시간 수정 기능 없음) → "⚠ 예외 — 그쪽은 **반대 방향**이며 **서버 처분** … 사용자가 회의 시간을 고치는 기능이 생긴 것이 아니다" ✅
- **OPEN-031-Y 범위 인용 정정** — `:2338-2340` 에 "⏳ **부분 해소 (2026-08-31) — ⓑ 의 «예약 → 회의» 방향만**" + "**여전히 열려 있는 것** — ⓐ · ⓒ · ⓑ 의 반대 방향 · **모달 발 회의의 취소 축**" 을 달았고, OQ 본문의 물음 중 어느 것이 답을 얻었는지("예약만 취소하면 회의는 남는가")까지 짚었다 ✅. 변경 노트 ⑭·SPEC-151 개정 노트 ⑤·§7.9.5 마지막 bullet 도 전부 **3표면 기준**으로 통일 ✅ (구 좁은 인용은 *(범위 정정)* 로 표기 — 번복을 숨기지 않았다)
- **소유 경계도 지켜졌다** — SPEC-151 §7.9.5 는 처분의 **정의를 발명하지 않고** SPEC-031 로 위임하고("본 절은 「무엇이 따라가는가」만 정하고 처분의 정의를 발명하지 않는다"), §7.9.7 의 소유 선언과 어긋나지 않는다 ✅

### W-1 (「예약 건당 회의 최대 1」의 근거) — ✅ **반영**

`spec-151-...md` §7.9.1 이 두 방향을 **분리해서** 적었다 — "「회의당 예약 참조 최대 1」은 **기존 계약**(SPEC-031 §3 연결 축)" / "⚠ 「예약 건당 회의 최대 1」은 **이 절이 새로 세우는 불변식**이다 — 위 인용은 반대 방향만 말하므로 **여기에 기대면 안 된다**". 보증 수단(유니크 제약 vs 생성 직전 조회)을 **착지 WP 로 넘긴** 것도 계약/구현 경계상 맞다. 코드에 유니크 제약이 없다는 실측(리서치 §3-a `:161` — nullable FK)과도 어긋나지 않는다. ✅

### W-2 (실행 축이 체인 끝까지 진행 중) — ✅ **반영**

`spec-151-...md` §7.9.4 조합표 **직후 첫 bullet** 으로 명문화 — "⚠ **체인이 끝날 때까지 실행 축은 «진행 중» 에 머문다 — 외부 예약이 성공한 «시점» 에 «성공» 을 적지 않는다** … 이 한 줄이 없으면 **C 행이 «성공 → 실패(종료)» 라는 §5.3.2 표에 없는 전이**가 된다 … 표의 실행 축 칸은 **그 최종값**이다". 내가 지적한 전이 legality 구멍이 정확히 메워졌고, §5.3.2 실증 결함과의 연결도 붙었다. ✅

### W-4 (참석자 `participants` 필드 형태) — ✅ **덤으로 반영**

§7.9.3 에 "⏳ **착지 WP 가 확인할 것 (1건)** — 입력 축은 `attendees`(발화 라벨 = 이름 문자열)이고 `participants` 는 그 해소 **결과**다(§7.2). 이 절이 읽는 것은 **결과 축**이며 그 필드의 실제 형태를 착지 시 한 번 실측한다 — **계약 방향은 바뀌지 않고**, 확인은 어느 필드를 부르는지에 대한 것" 으로 정확히 정리됐다. ✅

### 신규 모순 — **없음**

R2 가 건드린 자리(§Validation · §케이스 매트릭스 · §3 역방향 가산 · `:1388`·`:1389` · OPEN-031-Y · 개정 노트 ⑭ / SPEC-151 개정 노트 · §7.9.1 · §7.9.2 · §7.9.4 · §7.9.5) 주변을 훑었다.

- §5.3.2 **35칸 불변** 재확인 — 조합표 4행이 여전히 `승인 ×` 합법 칸 안, **새 칸·새 열 0** ✅
- 사용자 확정 6건 + 스케줄 화면 제외 — 재확인, **위반 0 · 재론 0** ✅
- 모달 발 경로·게이트 원칙(§7.1)·§7.2·§4.2·§6 — R2 diff 에서도 **변경 0줄** ✅
- state enum·`DELETE` 표면·새 컬럼 축 — **신설 0** (R2 가 스스로 그렇게 못박았고 실제로도 늘지 않았다) ✅
- `log.md` 신규 행에 **「SPEC-031 쪽 정합 3건」** 절이 추가돼 R2 환류(슬롯 소유 자리 · 회의 쪽 처분 2종 · OPEN-031-Y 부분 해소)가 **변경 장부에 남았다** ✅

---

## 경미 (WARN — 비차단, 코디네이터 판단)

- **RW-1. 「가산 4줄」 라벨이 낡았다.** `spec-031-...md:30` 변경 노트 ⑭ 제목 "(**가산 4줄** · 계약 소유는 SPEC-151 §7.9)" 과 `log.md` 신규 행 첫 문장 "SPEC-031 §3 **가산 4줄**" 이 R1 시점 수치다. R2 에서 §3 가산은 **하위 6줄**(본문 스스로 "아래 **여섯**" 이라 센다)로 늘었고 §Validation·§케이스 매트릭스·OPEN-031-Y 세 자리가 더 붙었다. → 「가산 6줄 + 정합 3자리」 정도로 라벨만 맞추면 된다. **계약 내용에는 영향 없음.**
- **RW-2. 「변경 파급」 서술이 두 SPEC 사이에서 비대칭이다.** `spec-031-...md:1394` 는 "변경 게이트의 실행이 성공하면 그 예약이 연결한 회의의 `scheduled_*` 가 새 값으로 **따라간다**" 를 **조건 없이** 적는데, `spec-151-...md` §7.9.5 는 "**진행 중·종료 회의의 예정 시간은 따라가지 않는다**(지난 일을 고쳐 적는 것이 된다)" 로 상태 분기를 건다. 소유 분담(상태 분기 = 「무엇이 따라가는가」 = SPEC-151)상 모순은 아니지만, **바로 옆 「취소 파급」 bullet 은 상태 분기를 자기 문장 안에 담고 있어** 형제 두 bullet 이 비대칭이다 — SPEC-031 만 읽은 구현자가 라이브 회의의 예정 시각을 갱신할 여지가 남는다. → 변경 파급 bullet 에 "(진행 중·종료 회의는 제외 — [SPEC-151 §7.9.5])" 한 구절만 붙이면 해소.
- **RW-3. `log.md` 신규 행의 `PR` 칸이 여전히 `—`.** R1 W-3 과 동일 — PR 은 코디네이터가 올리므로 **생성 후 채우면 되는 항목**이다(직전 행도 동일 선례). 차단 사유 아님.

## 기존 부채 (이번 판정 제외 — R1 과 동일)

- `products/mediness/30-work.md:231` SPEC-030 Coverage derive WARN — 이번 diff 밖, HEAD 기존분.
- 타 제품(`procedure-hub`·`selly` 등) frontmatter WARN 다수 — 무관.

## 확인 안 함

- 전면 재리뷰(§7.9 전 절 정합·코드 사실 전수)는 재검수 범위 밖 — R1 에서 이미 수행했고 R2 diff 가 그 결론을 뒤집는 변경을 담지 않았다.
- 백엔드·프론트 코드(이번 diff 에 코드 파일 0) · 21-html 시안(없음).
