# 재발주 R2 정정 리포트 — planner (2026-08-31)

리뷰 리포트 `review-spec-report.md` 의 **FAIL 6건 + WARN 2건 + 코디 판단(W-4)** 을 전부 정정했다.

## 검증 결과

| 항목 | 결과 |
|---|---|
| `python3 scripts/lint-pipeline.py --strict` | **0 error / 255 warning (exit 0)** — mediness 범위 **0 ERROR · 1 WARN**(SPEC-030 기존 부채, 이번 diff 무관) |
| 폐기 키워드 grep 스윕 (`accept_pending`·`수락 대기`·`decline`·`task_declined`·`accepted_at`·`/slack/complete`) | **활성 계약 잔존 0.** 남은 히트는 전부 ① 폐기/취소선 표기 ② 개정 이력 ③ 마이그레이션 좌표(0094/0108 이 무엇을 했는가) ④ 폐기 근거 서술 ⑤ 동결 work-074·완료 WP 기록 |
| 리포트 §재검수 8항목 체크리스트 | **8/8 통과** (아래 대조표) |
| allowed_paths | 준수 — 변경 22 파일 전부 `products/mediness/`. 코드 레포 무변경. WP2 미작성 |

diff: **수정 19 파일 + untracked 3 = 22**, `+790 / −601`. 이번 라운드가 새로 손댄 파일 **6개** = `spec-060` · `spec-113` · `spec-150` · `spec-155` · `spec-230` · `40-architecture/decision-tracking-engine.md`.

## FAIL 6건

### V-1 · V-6 — `spec-150` endpoint 인덱스 2행 폐기

- `:573` `/incidents/slack/complete` · `:584` `/tasks/{task_id}/decline` → **취소선 + 「폐기 (2026-08-31)」**. 행을 삭제하지 않고 폐기 표기로 둔 이유 = 그 자리가 왜 있었는지가 사라지면 같은 endpoint 가 다시 등재된다(리포트 권장안 중 후자).
- `:588` 집계 정정 — **35/35(capability 34 + Slack callback 1) → 현행 33/33 전부 capability**. ⚠ **「의도적 전환 제외 1개」라는 축이 함께 소멸**했다(그 1개가 D-13 이었다). 과거 노트의 34·42 수치는 그 시점 기록으로 남기고 각주로만 이었다.
- `:656` §범위 제외 항목 폐기 — 「전환에서 제외한 endpoint」가 아니라 **애초에 신설된 적 없는 endpoint** 였음을 명시. 실경로(`POST /api/v1/slack/interact`, 서명 검증 있음)와 **인증·서명 없는 구현 변형을 닫는 것이 WP-124 확인 항목**임을 링크로 이었다.
- 파일 최상단에 **2026-08-31 변경 노트** 신설 (①~④, 무변경 절 명시).

### V-2 — `spec-155 §6.11d` 결정 SoT 정면 위반 정정

- `:869` 「수신자 쪽 = **기존 수락 대기 플로우 재사용**」 → **「수신자의 `todo` 로 선다 — 수락 대기가 아니다」**. ⚠ **근거로 인용하던 SPEC-154 §4.8 이 바로 그 게이트를 폐기한 절**이라는 사실을 각주로 박았다.
- `:871` 「수락 게이트 리셋」 → **「상태 리셋 = `todo` 리셋 + `started_at` 클리어, 전이가 아니라 담당자 축의 변경」**.
- 행 2개 신설 — **terminal 가드**(`done`·`canceled` 는 재배정으로도 안 열림, `system_admin` 예외 없음) · **담당자 본인의 재배정 요청**(폐기된 거절의 대체 통로).
- `:813` 「`ax_task.start` 가 **수락 대기 → 진행**을 한 번에 지난다」 → **「`todo → in_progress` 시작 전이 1회」**. `ax_task.accept` 를 안 만든다는 **결론은 유지하고 근거만 「이미 덮는다」→「덮을 것이 없다」로** 바꿨다.

### V-3 — decline write 표면 3벌 폐기 (`spec-155` · `spec-060`)

- `spec-155`: `:545` REST `POST /task-declines` · `:617` MCP `mediness.task_decline` · `:678` leaf `ax_task.decline` · `:884` §6.11e 매핑표 → 전부 폐기 표기. `:552` REST 카운트 **접수 9+1=10 → 8+1=9**. `:702` §6.10 분류표 「거절 = ⚡즉시 반영」 행 폐기. §6.11 상단에 **처분 표**(거절 폐기 / 재배정 존치·지위 상승 / ⓐⓑ 불변) 배너.
- §6.11b·§6.11c 는 **지우지 않고 폐기 표기**로 뒀다 — 「왜 그렇게 갔는지」 보존. 다만 **§6.10d ⑤ 의 해석**(「되돌릴 수 없는 파괴가 없나」)만 **일반 규칙으로 살려** 명시했다(거절은 그 첫 시험대였을 뿐 규칙 자체는 조작과 무관).
- `spec-060`: `:247` 인벤토리 행 + `:63` 등재 블록쿼트 행에 **🔴 계약 폐기 · 등록 해제 대기** 표기 + 최상단 개정 노트. ⚠ **행을 지금 빼지 않았다** — 그 표는 `/health` 실물의 거울이고 툴이 아직 등록돼 있다. **등재 때 「계약 확정만으로 먼저 옮기지 않는다」로 지킨 규율의 반대 방향**이며, 수치 이동(**write 18→17 · `/health` 57→56**)은 WP-124 P5 등록 해제 착지 시. §5 AC 2행에도 예고를 달았다.
  - ⚠ **리포트가 쓴 「write 19→18」은 오독이다** — 2026-08-25 `decision_register` 해제로 **현행이 이미 write 18 · `/health` 57** 이라 제거 후는 **17 / 56**. 전 문서에서 이 수치로 통일했다.
- `spec-152` 잔존 3곳도 함께 정리 — §MCP 「노출하지 않는 것」 ④(`/slack/complete`) · ①·§상단 ①·§1766 의 write 툴 6종 목록에서 「거부」 제거 · OQ-12 후보 6종 → 5종.

### V-4 — `spec-230 §U-7` 5값 정정

- `:325` 「여섯 상태」 → **다섯**, `:323`·`:326` 칩 `수락 대기` → **`대기`**. 라벨은 **SPEC-154 §4.19.6 칸반 4열(`대기 · 진행 중 · 완료 · 중단`)과 같은 말**을 쓰도록 못박았다 — 같은 상태가 화면마다 다른 한글로 보이지 않게(공용 라벨 사전 1벌 = WP-124 FE phase).
- 🔑 **칩의 존재 이유는 그대로 뒀다** — 「내가 지금 잡아야 할 것」이고, 수락 게이트가 없어진 지금 그 답이 `todo` 다. 칩을 3개로 줄이지 않고 `수락 대기` 자리를 `대기`가 이어받는 쪽을 골랐다.
- `:101` 2026-08-07 마감 소정합 ③ 은 **그 시점 기록으로 남기고 각주로만** 이었다(이력을 고쳐 쓰지 않는다).

### V-5 — `spec-154` 내부 모순 해소

- `:715` §4.8 필드 계약표 · `:2357` §4.20.1 삭제 축 계약표에서 **`accepted_at` 제거 → 스탬프 3종**. `runtime_task.md` 스탬프 표와 일치.
- 개정 노트에 **㉖-a** 서브항목 추가 — 「이번 라운드가 **스스로 강제하겠다고 선언한 절차**(«개정 = 추가 + 구 서술 grep 삭제»)가 **개정 대상 파일 안에서 지켜지지 않았다**」를 명시. 감추지 않는 편이 다음 라운드에 유효하다.

## WARN 2건

- **W-1** `20-spec.md:133`·`:200` — SPEC-152 제목 「이슈라이징 기반」 → **「에러 트리거 기반」** (2행 동기). 잔존 0.
- **W-2** WP-124 `covers` **4 → 11**. 추가 = `SPEC-031`(P6) · `060`(P5) · `110`·`113`(P7) · `150`(P3·P5) · `155`(P5) · `230`(P5). `30-work.md` **3표 전부 동기**(Status Board 는 무변경 — WP-124 1건, WP List Covers 열 · Spec Coverage 7행).
  - ⚠ **Spec Coverage 3행이 `done` → `in_dev` 로 내려갔다**(113·155·230) — derive 규칙(`done` 들 + `proposed` → `in_dev`)의 결과이고, **「끝난 SPEC 에 새 일이 생겼다」를 표가 말하게** 하는 편이 맞다. lint derive 검증 통과.
  - WP-124 본문에도 추적 가능한 작업 항목을 박았다 — P3 에 `/incidents/slack/complete` 구현 변형 폐쇄 + 검증 1줄 · P5 에 SPEC-060 인벤토리 수치 동기(⚠ 먼저 내리지 않는다) · 랜딩 칩 정정 · `/health` 56 3자 일치 검증. §Related 에 SPEC-060·150·155·230 4줄.

## 코디 판단 (W-4)

`decision-tracking-engine.md §기본 실행 모델` 과 `spec-113 §3.6` 에 **폐기 예정 표기가 없었다** — 확인 후 각각 **한 줄(인용 블록) 추가**했다. 내용: `is_required` 완료 자동 게이트는 A축(`decision_execution_task`) 소유이고 그 원장은 canonical 이관·폐기 대상이며, **지금 지우지 않는 이유** = SPEC-154 §4.9 run 전이와 중복인지가 아직 확인 전이고 그 확인이 **WP-124 P7 검증 항목**이라는 것. 「현행 동작의 기록이지 존속을 약속하는 계약이 아니다」로 지위만 낮췄다. `is_required` 자체는 **지우지 않았다.**

W-3(21-html 시안)·W-5(spec-125:616 의도된 동형 서술)는 지시대로 **손대지 않았다.**

## 리포트 §재검수 8항목 대조

| # | 항목 | 결과 |
|---|---|---|
| 1 | `spec-150` D-13·decline 2행 + 개정 노트 | ✅ 폐기 표기 + 집계 33/33 + 변경 노트 신설 |
| 2 | `spec-155` decline·수락 대기 계약 정리 (§847 최우선) | ✅ §6.11d 2행 정정 + terminal 가드·본인 요청 명시 + 표면 3벌 폐기 |
| 3 | `spec-060` MCP `task_decline` + 카운트 | ✅ 폐기 기장 · 수치는 착지 시 이동(규율 근거 명시) |
| 4 | `spec-230` 칩·「여섯 상태」 | ✅ `대기` 칩 · 다섯 상태 · SPEC-154 라벨 정합 |
| 5 | `spec-154` `accepted_at` 2곳 | ✅ 제거 + 개정 노트 ㉖-a |
| 6 | `20-spec.md` SPEC-152 제목 | ✅ 2행 동기 |
| 7 | WP-124 `covers` + Spec Coverage | ✅ 4→11 · 3표 동기 · derive 통과 |
| 8 | lint --strict + grep 재실행 | ✅ 0 error · 활성 잔존 0 |

## 남긴 것 (판단 필요 · 이번 정정 범위 밖)

1. **`work-114`(in_dev)가 「5열 보드 · 수락 대기 컬럼 · 수락 CTA 가 살아 있는 세계」를 명시 계약한다**(`:43`·`:157`·`:184`·`:223`). 리포트가 지적하지 않았고 **WP-124 `OI-1`(FE 파일 충돌 — 착수 전 소유 조정)이 이미 소유한 자리**라 이번엔 손대지 않았다. 그 WP 스스로 「수락 대기 게이트 은퇴 — 별도 트랙」을 후속 트랙으로 적어 두고 있어 인지 밖은 아니다. **착수 전 조정 시 함께 정리 대상.**
2. **완료된 WP 들의 기록**(`work-107` 재배정 실기동 증거 「수신자 인박스에 수락 대기」 등)은 **그 시점 사실의 기록**이라 고치지 않았다. 계약 SoT 는 SPEC 이고 그쪽은 정정됐다.
3. **SPEC-030 Spec Coverage derive WARN 1건** — 이번 diff 무관한 기존 부채(리포트 §기존 부채와 동일 판단).
