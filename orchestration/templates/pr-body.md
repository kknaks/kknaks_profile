# PR 본문 표준형

> 코디네이터가 PR 을 올릴 때 이 형식을 따른다 (모든 프로젝트 공통). 참조 표본: mediness-app PR #61.
> 소문자 `<…>` 는 실값으로 채운다. 해당 없는 섹션은 통째로 지운다 — 단 "배포 주의" 는 없어도 "동형 — 공지 없음" 으로 판단을 남긴다.
> 제목: `type(scope): 한 줄 요약 — 범위` · spec PR 과 code PR 은 분리하고 서로 링크.
>
> **문체 원칙 (PR #61 기준)**
> - 변경 나열이 아니라 **판단의 기록**이다. 각 항목에 "무엇을" 만이 아니라 **"왜 그렇게 했는지"** 를 붙인다 — 기각한 대안이 있으면 그 이유까지.
> - 리뷰어가 diff 를 안 열고도 구조를 이해하게 쓴다. 파일 목록은 안 쓴다.
> - 워커/코디가 임의로 정한 지점, 뒤집을 수 있는 판단은 숨기지 말고 **리뷰 포커스**로 꺼낸다.

---

## 배경

<왜 이 작업이 필요했나 — 무엇이 문제였고(현상·원인), 무엇을 근거로 착수했는지(SPEC/WP/사용자 결정 링크). 작업 도중 방향이 바뀌었으면 그 경위도. 2~4문단.>

## 변경 내용

<중요 골자를 번호 섹션으로 — 파일 나열이 아니라 설계 단위. 각 섹션은 "무엇을" + "왜 그 방식인지(근거·기각한 대안)". 예:>

### 1. <골자 — 예: 인가 축>

- <변경 + 근거. "X 를 Y 로 했다 — Z 하면 W 가 깨지기 때문" 꼴>

### 2. <골자>

- <…>

## 검증

- <테스트 수치 "N passed / M failed" + 신규 테스트 수와 커버 축>
- <정적 검증 — lint·tsc·ruff·migration 왕복>
- <코디네이터 독립 재검증 수치 / 라이브·수동 검증 범위>

## 리뷰 포커스

<사람이 판단해 주어야 하는 지점. 각 항목: 무엇을 어떻게 판단해 뒀는지 + 그 근거 + 뒤집으면 무엇이 달라지는지. 미해결로 남긴 것은 "이 PR 에는 넣지 않았다" 를 명시.>

**① <판단 지점>** — <현재 판단·근거·대안>

**② <…>** — <…>

## ⚠ 배포 주의

<행위가 바뀌는 것(비동형)·공지 대상·migration 실패 대응·배포 시 실측 항목. 없으면 "동형 전환 — 배포 공지 없음">

1. <…>

## 후속 (이 PR 범위 밖)

- <알고 있지만 의도적으로 안 한 것 — 근거 한 줄. 없으면 섹션 삭제>

<재이식·cherry-pick 등 원본이 있으면: ## 원본 커밋 매핑 표>

<상대 PR 링크: `Spec: <owner>/<repo>#<번호>` 또는 `Code: <owner>/<repo>#<번호>`>

🤖 Generated with an AI coding agent
